# plan-20260819 R30-r06 review — Claude
# reviewer: claude -p --effort xhigh --allowedTools Read Grep Glob (read-only, prompt via stdin)
# date: 2026-09-21T14:22:41+08:00
2b21b007c2877504ebcfdb302466b1f60f2b83429dd48846d4210d6756066a90  docs/development/plan/plan-20260819.md
# review prompt: plan-20260819.review/review-prompt-r06.md

我已完整核对计划、模板 v2.8、plan-status 与仓库事实（`git rev-parse HEAD` = `45ad1f7f2cf4af030dc9f16486f182874ffcfdbf`）。

# R30-r06 计划评审（Claude，只读）

**被审对象**（核对日 2026-09-21，worktree `/run/media/genedna/data/libra-main`）

| 文件 | 行数 |
|---|---|
| `docs/development/plan/plan-20260819.md` | 2690 |
| `docs/development/plan/plan-template.md` | 709（`v2.8`） |
| `docs/development/plan/plan-status.md` | 289 |

**排除项：** 未执行任何写操作；未访问 `/run/media/genedna/data/libra-pr456` 与 `../libra-backend`（沙箱外，已由 `DEP-M2-SRC-01` / ER-06a 登记），#456 事实按计划自述 + `gh` 证据链核对结构一致性。

---

## 一、r05 findings 关闭核对（逐条，附实际核对命令/行号）

**Codex r05 P1（5 条）**

| 条目 | 结论 | 证据 |
|---|---|---|
| 1 M2-16A 引用下游 target | **关闭** | `:817` 改为「本卡只跑 `--lib`…不建立也不依赖 `tests/memory_episode_test.rs`（該 target 由 M2-01K 首建），因此不更新 `tests/INDEX.md`」；`:832` 写集已无 `tests/INDEX.md`；`ls tests/memory_episode_test.rs` → 不存在，M2-01K 写集 `:1294` 首建 |
| 2 裸 `git` 命令 | **部分关闭** | 新增 `DEP-M2-SRC-01`（`:681`）+ §0.5 `:71` 总括句；`:820` AC 改 `libra status --short` + `rg --files`；但 `:806/:941/:1144` 仍是裸 `git ls-tree -r bc1be587e4`（见 P2-8） |
| 3 proposal-only envelope | **部分关闭** | `MemoryRecordEnvelopeV1` 已落 `:503`、`:580`、`:932/:948`、`:1901`、`:2238`；但 crate seam 签名未同步（见 **P1-1**） |
| 4 bridge 公开协议文档 | **关闭** | `:951` AC + `:968` 写集含 `docs/commands/agent.md` EN/zh；实际文档 `docs/commands/agent.md:89`、`docs/commands/zh-CN/agent.md:75` 确实写死「20-method allowlist」 |
| 5 M2-16D/E staged C/D | **关闭** | `:1057`、`:1191`、`:1151`、M2-15 AC1 `:2441` 四处一致（先 `locally-accepted` 参与 M2-15 C/D → D 证据后回填 `complete` → 再关 #456） |

**Claude r05 P1（3 条）**

| 条目 | 结论 | 证据 |
|---|---|---|
| 1 GC-M2-10 `state.rs` 无交付者 | **关闭** | `:625` 改 `reader_state.rs`（M2-06 交付）；M2-06 写集 `:1781` 含 `reader_state.rs`；M2-14C AC `:2372`、矩阵 `:2508`、风险 `:2590` 口径一致 |
| 2 `libra cloud sync` 泄漏 Memory ref | **关闭** | `:1695` AC 补 `sync_metadata` + `object_index` 分类；`:1706` VER `memory_branch_excluded_from_vcs_and_cloud_transport`；`:1714` 写集含 `src/command/cloud.rs`；`:2583` 风险行。仓库侧确认该面确实无过滤：`src/command/cloud.rs:2128` `reference::Entity::find().all(db_conn)`、`:1001-1005` 整仓 `object_index` |
| 3 M2-16A AC 越权 | **关闭** | 同 Codex P1-1 |

**Claude r05 P2（12 条）：** 1 关闭（template `:198` 已含 `.env.live-test`）、2 **部分关闭**（plan-status `:46` 已同步 r05/r06，但 `:199` 仍写「r01–r04…r05 待跑」→ P2-1）、3 关闭（`:70` doctest 例外，矩阵 `:2490` 指向 §0.5）、4 关闭（`:2501` 已补 status/evidence 一致性 + 关系字段指纹 + `observe_terminal_mismatch_and_relation_fingerprint_matrix`）、5 关闭（`:2330` 明写不新增 Cargo 依赖/`[[test]]`，`:2346` `none`）、6 关闭（`:504` 改引 `compat_agent_bridge_schema`，`Cargo.toml:350` 存在）、7 关闭（`:1344` 明写「可 NULL」）、8 关闭（全文 `rg "fmt --all --check|cargo build --release"` 仅命中完成判据 `:2688`，各卡 VER 已剔除；M2-16C VER=7 实计 7）、9 关闭（`EX-M2-02` 登记 `:758`，M2-13 AC/VER 各 9 条，审计表 `:779` 一致）、10 关闭（§0.4 `:64` 归 M2-07 `evidence.rs`，M2-01 AC4 `:1218` 已并入两型）、11 **部分关闭**（审计表已标 ID，但 6 张卡 `Granularity` 仍写 `writeset=serialized` → P2-2）、12 关闭（`:1019`/`:1028` 已是模板 exit-code 形式）。

**事实基线抽检（全部在核对日 commit 上成立）：** `src/internal/ai/mod.rs:50-68` 无 `memory` 且 `ls src/internal/ai/memory` 不存在；migration tip `2026091901`（`src/internal/db/migration.rs:1862`、`:2331`）；版本面集合 = 3 处（`tests/compat/version_surface_sync.rs:13,23,45,66`；target `compat_version_surface_sync` 见 `Cargo.toml:260`）；`base.yml` jobs = `format/clippy/owner-liveness-macos/redundancy/test/network-remotes/opencode-export-linux`（`:11/:32/:54/:77/:106/:312/:358`，共 422 行）与 `DEP-M2-CI-02` 一致；`codeql.yml:21 analyze` + `:44/:46` actions/rust matrix ✓；`AgentRunStatus::{Completed,Failed}` 仅 `run.rs:99,100,117,118,142,143`（定义+单测）✓；`op.rs:1144 !name.starts_with("libra/")` ✓；`history.rs:1026 pub fn new_with_ref` ✓；`client_storage.rs:2023 pub fn put` ✓；`agent_bridge_capture.sql:16` `CHECK(source='deepseek-harness')`、`:41 payload_sha256`、`:45 UNIQUE(bridge_session_id,event_seq)` ✓；`--test-threads` / `test-provider` 全文零残留 ✓；M2-05 的 16 个命令文档全部存在 ✓；25 卡 / 24 收口卡计数与审计表、DAG、Phase、里程碑、追溯表一致，各卡 AC/VER 宣称值与实际条目数逐卡相符 ✓。

---

## P0

无。

---

## P1

### P1-1　`MemoryRecordEnvelopeV1` 的 `proposal`/`model_label` 没有任何通道到达 Writer/runner —— host 提案补录路径在计划内不可实现

- **位置：** ADR-M2-05 `plan-20260819.md:503` 对比 `:505`、`:508`；§0.3 `:46`；ADR-M2-12 `:580`、`:582`；M2-08 `:1884`、`:1901`；M2-13 `:2238`、`:2240`；M2-16C `:948`、`:968`
- **问题：** r05 修订把统一入口定义为 `MemoryRecordEnvelopeV1 = { schema_version, observation（必填）, proposal: Option<EpisodeProposalV1>, model_label: Option<String> }`，并声明它由「bridge、CLI `memory record`、进程内 API 共用」（`:503`）。但**同一 ADR 的下一行**（`:505`）、§0.3（`:46`）与 M2-08 的卡片描述（`:1884`）仍把唯一的 crate seam 签名固定为 `MemoryRuntime::observe_terminal(TerminalObservationV1)` —— 参数类型只有 observation。同时两条入口卡都被 AC 锁死为只能走这个 seam：M2-13 AC7（`:2240`）「CLI Adapter **只调用** … 与 `MemoryRuntime::observe_terminal`（record seam）」，M2-16C 写集注（`:968`）「**只呼叫** M2-08 的 `observe_terminal` API；不修改或重实现 `memory/{runtime,job}` 語義」。于是 envelope 里的 `proposal` 与 `model_label` 没有任何声明过的传递路径。反向证据：M2-08 AC5（`:1901`）写「未注册且 `MemoryRecordEnvelopeV1` **未携带 `proposal`** 时 job 保持 pending」——runner 被要求判断 envelope 是否携带 proposal，但它只会收到 `TerminalObservationV1`。
- **为何重要：** 这条路径不是边角能力，而是 ADR-M2-12「Consequences」（`:582`）指定的核心闭环：「CLI `memory record` 与 bridge `memory.episode.record` 均可由 host 携带 `EpisodeProposalV1`（`model_label` 记录）**在无进程内 model 时完成补录**」。按当前签名，无凭证/无进程内 model 的 host（CI、bridge、DSH）提交带 proposal 的 envelope 后 job 只会永远 pending，M2-13 AC5、M2-16C AC3、ADR-M2-12 的 consequence 与 M2-08 AC5 互相矛盾且都判不出 pass。这属于 G-05（AC 自洽可判定）与 GC-M2-03（单一写入 seam）在同一处的冲突：要么突破 seam（违反两卡 AC），要么丢弃 proposal（违反 ADR）。
- **最小修复：** 把 seam 签名统一为 envelope（例如 `MemoryRuntime::observe_terminal(MemoryRecordEnvelopeV1)`，或新增具名 `MemoryRuntime::record(MemoryRecordEnvelopeV1)` 并保留 observation-only 的薄封装供 M2-16D1 使用），**同批**同步：§0.3 `:46`、ADR-M2-05 `:505/:508`、ADR-M2-12 `:580/:582`、M2-08 Description `:1884` 与 AC1 `:1897`/AC5 `:1901`、M2-13 AC7 `:2240`、M2-16C 写集注 `:968`、M2-16D1 AC4 `:1084`、故障矩阵 `:2566`；并在 M2-08 或 M2-13 增一条具名回归断言「带 proposal 的 envelope 在无进程内 model 时可完成补录并产出 Episode」（M2-13 已有 `memory_search_show_status_rebuild_record_with_mismatch_and_proposal`，需在 AC 中明确它覆盖该断言）。

### P1-2　M2-05 的实现写集实际为 14 个生产文件，超过 `M` 的 12 上限（G-04），且审计表与卡内都写 12

- **位置：** `plan-20260819.md:1714`（Implementation write set）、`:1718`（Files likely touched）、`:1740`（Granularity `prod-files=12; scope=M`）、审计表 `:771`；模板 `plan-template.md:446`（`M`：≤ 4 落点、≤ 12 生产文件）、`:441`（成稿后不得存在 L/XL 卡）、`:447`（唯一出路是登记 `L-exception:EX-*`）
- **问题：** 我逐项展开了 `:1714` 的写集并核对仓库存在性：`src/internal/{branch.rs, ref_policy.rs}`（2；`ls` 确认 `branch.rs` 存在、`ref_policy.rs` 为本卡新建）+ `src/internal/operation/{snapshot.rs, restore.rs, store.rs}`（3，均存在）+ `src/command/{branch, checkout, switch, reset, op, push, fetch, clone, cloud}.rs`（9，均存在）= **14 个 `src/**` 生产文件**，每个都承载本卡的 classifier callsite 行为变更（G-04 `:442` 明确这类文件「计入」）。测试、`tests/SERIAL_REGISTRY.tsv`、`.config/nextest.toml` 与命令文档按 G-04 `:443` 的随附同步集不计入，因此计数无法压到 12。r05 修订把 `cloud.rs` 加入写集（关闭 Claude r05 P1-2）时把 13 推到了 14，但 `Granularity` 与审计表的 `prod-files` 仍是修订前的 12。
- **为何重要：** 这是 ER-03 开工门要逐字段核对的那一行（模板 `:197`、`:536`「任一列不达标即不得开工」）。14 > 12 意味着该卡客观上是 `L`，按模板只有两条合规出路：拆卡，或在 waiver 表登记 `L-exception:EX-*`（具名审批）。当前既没拆也没登记，且分子被写成 12 —— 审计表存在的唯一意义（机械可核对）在这一行失效，卡在开工日会被 ER-03 直接判为不达标。
- **最小修复：** 二选一并同批同步 `:1714/:1718/:1740` 与审计表 `:771`：① 按「表面轴」拆卡——`src/internal/{branch.rs,ref_policy.rs}` + `src/internal/operation/**` + 用户 branch 命令为一卡，`push/fetch/clone/cloud` 传输面为另一卡（传输排除是独立可恢复的行为轴，符合 G-01/G-02），两卡各自 ≤12；② 在 waiver 表新增 `EX-M2-03`（`G-04 规模上限`，理由落在「跨文件同型 API 机械替换」白名单，附具名 Approver/Review round/证据），`Estimated scope` 改 `L-exception:EX-M2-03`、`prod-files=14`，审计表同步。

---

## P2

1. **`plan-status.md:199` 与 `:46` 自相矛盾。** `:46` 已同步「r05 兩方 `FAIL`…；r06 待跑」，但 §4.0 零依赖表的 M2-16A 行仍写「**未过**（r01–r04 雙評審皆 `FAIL`；r05 待跑）」。**修复：** `:199` 同步为「r01–r05 皆 `FAIL`；r06 待跑」。

2. **6 张卡的 `Granularity.writeset` 不符模板枚举，且与审计表不一致（r05 P2-11 半闭合）。** 模板 `:540`/`:621` 只允许 `no-overlap` 或「序列化于 `TASK-ID`」；`:924`(M2-16B)、`:994`(M2-16C)、`:1059`(M2-16D)、`:1127`(M2-16D1)、`:1193`(M2-16E) 写 `writeset=serialized`（无 ID），`:2486`(M2-15) 写 `serialized-after-all-members`，而审计表 `:783-788` 已分别写 `serialized-at-M2-16A / M2-12 / M2-08 / M2-16D / M2-14C / M2-16E`。**修复：** 把这 6 行的 `writeset` 改成与审计表相同的 `serialized-at-<ID>`。

3. **`MemoryRecordEnvelopeV1` 无首建归属，也未进 M2-01 的版本门。** §0.4「移植所有權」（`:64`）为 `TerminalObservationV1`/`TerminalEvidenceRefV1`（M2-07 `evidence.rs`）与 `EpisodeProposalV1`（M2-07 `compiler/mod.rs`）都指定了首建卡，唯独新引入的 envelope 没有；它带 `schema_version`（`:503`）且是 bridge/CLI 的最外层 host-facing 类型，却不在 M2-01 AC4（`:1218`）的版本门清单里。**修复：** §0.4 或 ADR-M2-05 追加「`MemoryRecordEnvelopeV1` 由 M2-07 `evidence.rs`（或 M2-08 `runtime.rs`）首建」，并把该型并入 `:1218`。与 P1-1 同批处理。

4. **审计表末行与「Verification 新增标记」段落同行，渲染时被吞进表格。** `:788` 实际内容是 `| M2-15 | … | N/A |**Verification 新增标记：** …`（`cat -A` 确认同一行、行尾无换行分隔）。GFM 会把最后一个 `|` 之后的文本并入/丢弃该单元格，导致这段**规范性**的 `(new)` 标记契约（模板 `:558` 要求）在渲染视图里消失。**修复：** 在 `:788` 行尾断行并空一行，使该段成为独立段落。

5. **M2-16C 未具名 `protocol.rs` 内的 20-method 守卫与错误文案更新。** 本卡新增 2 个 method，仓库侧有硬编码断言 `src/internal/ai/agent_bridge/protocol.rs:532 fn allowlist_has_exactly_20_methods()` 与用户可见错误文案 `:284`「the v1 allowlist has 20 methods」。AC6（`:951`）只要求更新 EN/zh 文档，写集（`:968`）只笼统写「相关 tests」。**修复：** AC6 补一句「同批更新 `METHOD_ALLOWLIST`、`allowlist_has_exactly_20_methods` 守卫（重命名为 22）与 `UNKNOWN_METHOD` 错误文案」，写集显式列 `src/internal/ai/agent_bridge/protocol.rs` 的这两处。

6. **`EX-M2-02` 的理由自述「兩個恢復軸」，与永不可豁免的 G-01 冲突。** waiver 行 `:758` 写「rebuild 與 record 屬同一公開命令面的**兩個恢復軸**」；模板 `:520` 明确 `G-01`（单一可独立恢复的行为轴）**永不可豁免**，waiver 只覆盖 G-03 条目上限。同卡 `Granularity`（`:2289`）实际声明的是单一恢复动作 `revert command adapter while retaining internal data`。对比 `EX-M2-01`（`:757`）用词正确（「属同一恢复轴上的机械门族」）。**修复：** `:758` 改为「同一 CLI 表面、同一恢复动作（revert command adapter）上的两条判据链（重建 / 补录）」。

7. **测试矩阵「Terminal adapter」行未标条件性。** `:2507` 无条件要求 `--filter agent_run_terminal_adapter`，但该用例只有 M2-16D 判定「可映射」并启动 M2-16D1 时才存在（`:1065`、`:2677` `DEFER-M2-09`、完成判据 `:2681`）。矩阵前言（`:2490`）规定零命中即失败，因此在「不可映射」分支下该行永远不可满足。**修复：** 该行「必须覆盖」列加「（条件性：仅当 M2-16D 判定可映射；否则以 `DEFER-M2-09` 墓碑记为 N/A）」。

8. **仍有不带 `-C <source-clone>` 的裸 `git` 审计命令。** `:806`、`:941`、`:1144` 是 `git ls-tree -r bc1be587e4 …`（#456 面，被 §0.5 `:71` 总括句覆盖但命令本身不可直接复制执行）；`:13`、`:373`、`:874`、`:2161` 是 `git ls-tree -r origin/main …`（main 删除审计面，**不在** `:71` 的「#456 正向移植的來源審計命令」措辞范围内）。`DEP-M2-SRC-01`（`:681`）只声明了 #456 clone。**修复：** 把这 7 处统一改写为 `git -C <DEP-M2-SRC-01 source clone> …`，并把 `:71` 的措辞从「#456 正向移植的來源審計命令」放宽为「一切 `git` 来源审计命令（#456 与 main 删除面）」。

9. **M2-16E 的 `Dependencies` 未登记 `DEP-M2-SRC-01`。** 依赖表 `:681` 的 Owner 列写的是「M2-16A / M2-16E」，且该 DEP 的失败策略是「clone 缺失時相關卡 `Lifecycle=blocked`」；但 M2-16E 的 `Dependencies`（`:1163`）只有 M2-14C、M2-16D，而其 AC1/AC4（`:1149`、`:1152`）要求逐文件、逐测试映射 #456，必须读到该 clone。**修复：** `:1163` 与 Granularity `:1193` 的 `deps` 补 `DEP-M2-SRC-01`。

10. **每卡 `**Dependencies:**` 紧贴 Verification 的最后一个列表项，无空行。** 例如 `:827→:828`、`:893→:894`、`:963→:964`、`:1028→:1029`（全文 25 处同型）。GFM 会把它作为上一个 `- [ ]` 的 lazy continuation 渲染进列表项内，字段在渲染视图里不再是独立字段。**修复：** 在每处 `**Dependencies:**` 前插入空行。

11. **部分 AC 仍写无 pattern 的「`rg` 证明…」。** `:884`（「`rg` 证明 api key/env 值不进入对象、SQLite 明文、日志与 job row」）、`:949`（「`rg` 证明 bridge 层无直接 ref/OID 写入」）、`:1084`（「`rg` 证明其只调用 `observe_terminal`」）既没有具体 pattern/路径，也没有对应的 VER 行按模板 `:544-556` 的 exit-code 或 allowlist 口径落地（对比 `:827`、`:893`、`:1028`、`:1096` 已是正确形式）。**修复：** 这三条各补一条同形的 exit-code 守卫（或 allowlist 比对 + 命中 diff）到本卡 `Verification`，若因此超限则按门族口径处理。

---

**结论：** r05 的 8 条 P1 已关闭 6 条、部分关闭 2 条（裸 `git`、envelope），12 条 P2 已关闭 10 条、部分关闭 2 条；事实基线锚点（`ai/mod.rs`、`client_storage.rs`、`history.rs`、`op.rs`、`base.yml`、`codeql.yml`、版本面、migration tip、bridge SQL）全部在核对日 commit 上成立。但本轮新发现 2 条 P1：r05 引入的 `MemoryRecordEnvelopeV1` 只加在两端入口、未同步唯一 crate seam 签名，使 host 提案补录路径在计划内不可实现；以及 M2-05 因补入 `cloud.rs` 后实际生产文件数 14 超过 `M` 的 12 上限而未拆卡也未登记 `L-exception`。按「P0=0 且 P1=0」的通过口径，本版不通过。

VERDICT: FAIL
EXIT=0
