# plan-20260819 R30-r26 review — Claude（評審期間凍結：plan sha256 181fdf52…、template 3485bd9f…）
# reviewer: claude -p --effort xhigh --allowedTools Read Grep Glob (read-only, prompt via stdin)
# date: 2026-09-22T02:53:09+08:00
181fdf52b07ad31d19f72d1551536eea5e603e53ce01486b2ff5750f7121e5cc  docs/development/plan/plan-20260819.md
3485bd9fe5330e8980398b2326764d80c4c753e76f72fe5f09533ac92fc80030  docs/development/plan/plan-template.md
# review prompt: plan-20260819.review/review-prompt-r26.md

我已完成只读评审。以下是发现。

---

# plan-20260819 R30-r26 评审（Claude，只读）

**被审版本：** `docs/development/plan/plan-20260819.md` sha256 `181fdf52b07ad31d19f72d1551536eea5e603e53ce01486b2ff5750f7121e5cc`（与 `r30-r26-sha256.txt` 一致，评审期间未变更）；模板 `plan-template.md` sha256 `3485bd9f…`（v2.8）；`plan-status.md` sha256 `0fc183e2…`
**仓库核对：** `/run/media/genedna/data/libra-main` @ `45ad1f7f2`，crate `0.23.36`
**未能直接核对项：** `/run/media/genedna/data/libra-pr456`（沙箱只允许 `libra-main`，`ls` 被拒）。对该路径的判定改用**本工作树的同构证据**加**已归档的 r06/r25 探测输出**，见 P1-1。

## r25 findings 关闭核对（实际执行的命令/行号）

| r25 项 | 结果 | 证据 |
|---|---|---|
| **Codex P1-1** `DEP-M2-SRC-01` 用 `test -d "$SRC_CLONE/.git"` 误判 worktree | **未关闭** | `Grep 'is-inside-work-tree\|rev-parse --verify'` 在计划内**只命中 L453（修订历史自述）**，`:709` 仍为 `test -d "$SRC_CLONE/.git"`。见 P1-1 |
| **Codex P1-2** 158-path 计数范围用 `45ad1f7f2` | **未关闭** | `grep -n 'diff --name-only'` → `:709` 仍是 `45ad1f7f2 bc1be587e4 … = 158`；`:1219`/`:1226` 用 `2ebd409… bc1be587e4`。见 P1-2 |
| **Claude P1-1** 审计表末行与 `(new)` 段落缺物理空行 | **已关闭** | `Read :793-830`：`:825` 为 M2-15 末行，`:826` 空行，`:827` 为 `**Verification 新增标记：**` |
| **Claude P1-2** identity entity 反向依赖 | **已关闭** | `:1417` 改为「`memory_compiler_identity` 由 M2-08（唯一數據訪問層）」；`:2061` M2-08 写集含 `memory_compiler_identity.rs`；`:2065` `11 = 7+4`；`:2087` `prod-files=11`；`:2416` M2-13 写集已删该 entity，`:2442` `prod-files=5`；审计表 `:811`=`3/11`、`:816`=`4/5` |
| **Claude P1-3** §0.2 `agent_bridge/memory.rs` 归属 | **已关闭** | `:34` 改为「`memory.rs` 由 **M2-16C** 首建…入口類型由 **M2-07 `evidence.rs`** 首建」；`:65` 新增 `agent_bridge/memory.rs → M2-16C`；`:469` r09 行补「（r25 更正為 M2-16C）」 |
| **Claude P1-4** M2-01K 未登记 serial/nextest 基建 | **已关闭** | `:1359` 新增 `compat_serial_registry` VER（共 6 条）；`:1365` 写集含 `tests/SERIAL_REGISTRY.tsv`、`.config/nextest.toml`；`:1381` trigger=`T-1/T-4`；审计表 `:802`=`6/8` + `3/6` |
| r25 P2-1..P2-6（M2-08 trigger、M2-06 Files、M2-02R 连续空行、GC-M2-15 措辞、M2-13 entity 无消费者、模板 web/worker） | **全部已关闭** | `:2077`、`:1898`「6 个生产文件以内」、`:1603` 已减为单空行、`:654`「倉庫現有子模組為裸 `pub mod`；本計劃額外加註 `#[doc(hidden)]`」（与 `src/internal/ai/mod.rs:53-68` 事实一致）、`:2416`、模板 `:71/:228/:229/:230/:233/:262/:443` |

## 事实基线复核（全部成立）

`src/internal/ai/mod.rs:53-68` 无 `memory`（`sed` 输出为 `permission…util` + `pub use completion::…`）；`grep -n 'Memory' src/cli.rs` 零命中；`grep -n 'memory' src/internal/model/mod.rs` 零命中（registry 为 45 行扁平 `pub mod`）；migration tip = `2026091901_operation_boundary_claim_columns`（`ls sql/migrations | tail`）；`src/command/op.rs:1144` = `&& !name.starts_with("libra/")`；版本面集合 = `Cargo.toml`/`install.sh`/`install.ps1`（`tests/compat/version_surface_sync.rs:13`）；`base.yml` 七 job（`format/clippy/owner-liveness-macos/redundancy/test/network-remotes/opencode-export-linux`，422 行）、`codeql.yml:21` = `analyze`、`release.yml` 五 job 与 `DEP-M2-CI-01/02/03` 一致；`src/internal/ai/history.rs:1026` = `pub fn new_with_ref`；`METHOD_ALLOWLIST`（`protocol.rs:62`）20 项 + `allowlist_has_exactly_20_methods`（`:532`）+ `tests/agent_bridge_protocol_test.rs:15`；`Cargo.toml` 未设 `autotests=false`（`tests/*.rs` 自动发现成立）；25 张卡 × 21 个字段齐备（`Grep -o` 逐块计数）。

---

## P0

无。

---

## P1

### P1-1　`DEP-M2-SRC-01` 的来源可用性判据在实际来源上恒为假，且修订历史自述与事实不符

- **位置：** `plan-20260819.md:709`（第 4 格与第 7 格）；对照自述 `:453`
- **问题：** 第 4 格写「實際存在與否以 `test -d` 判定」，第 7 格首条判据为 `test -d "$SRC_CLONE/.git"`。计划指定的来源 `/run/media/genedna/data/libra-pr456` 是 **linked worktree**，其 `.git` 是指针**文件**而非目录，该判据必然 exit 1。而 `:453`（R30-r25 修订行）明确写着「`DEP-M2-SRC-01` 改用 `rev-parse --is-inside-work-tree` + pinned `rev-parse --verify`（worktree 的 `.git` 檔案不再誤判）」——**该修订未落地**：`is-inside-work-tree` / `rev-parse --verify` 在全文只出现在 `:453` 这一行自述里，`:709` 一字未改。
- **我实际核对的命令与结果：**
  - `Grep 'is-inside-work-tree|rev-parse --verify' docs/development/plan/` → 计划内唯一命中 `plan-20260819.md:453`（其余命中都在 `*.log` 与 `review-prompt-r26.md`）。
  - 同构证据（本工作树，与 `libra-pr456` 同为 `libra-pr507` 的 linked worktree）：`ls -ld .git` → `-rw-r--r-- 1 genedna genedna 70 … .git`；`test -d .git` → `NOT-A-DIR`。
  - 归档探测（`codex-plan-r06.log:12746+`、`codex-plan-r25.log:9060+`）：`ls -ld /run/media/genedna/data/libra-pr456/.git` → `-rw-r--r-- … 71`；`gitdir=gitdir: /run/media/genedna/data/libra-pr507/.git/worktrees/libra-pr456`；`DEP-gitdir-check-exit=1`；`recommended-check-exit=0`。
- **为何重要：** 第 8 格的失败策略是「來源 clone 不可用時**直接阻塞**（`Lifecycle=blocked`）」。M2-16A 是全计划的**执行起点**，M2-16E 是收口卡；按写死的判据执行，一个完全可用、所有 `git -C` 审计命令都能跑的来源会被机械判为不可用，整份计划从第一张卡起阻塞。加上自述与事实相反，执行者按修订历史会误以为已修。
- **最小修复：** `:709` 第 7 格首条改为 `git -C "$SRC_CLONE" rev-parse --is-inside-work-tree` 且 `git -C "$SRC_CLONE" rev-parse --verify bc1be587e4^{commit}` 均成功（或显式接受 `.git` 为文件或目录两种形式）；第 4 格「以 `test -d` 判定」同步改为该判据；`:453` 的 r25 行改为如实记录（未落地则删除该子句，并在 r26 行登记为第二次修复）。

### P1-2　`DEP-M2-SRC-01` 的 158-path 计数命令用了错误的 diff 范围，与 M2-16E 自相矛盾

- **位置：** `plan-20260819.md:709`（第 7 格）；对照 `:1213`、`:1219`、`:1226`
- **问题：** 第 7 格写 `git -C "$SRC_CLONE" diff --name-only 45ad1f7f2 bc1be587e4` 輸出以 `wc -l` 計數 **= 158**。但 158 是 **pinned merge-base `2ebd409045b5114707986b66a7a6b42b5a4a4b7e` → `bc1be587e4`** 的差异数；`45ad1f7f2 → bc1be587e4` 还包含 main 自 merge-base 以来 296 个 commit 改动的全部路径（r25 实测 1459）。`:453` 同样自述「把 158-path 命令範圍改為 `2ebd409… bc1be587e4`」，该修订**未落地**。
- **我实际核对的命令与结果：** `grep -n 'diff --name-only' docs/development/plan/plan-20260819.md` → 三处命中：`:709` 用 `45ad1f7f2 bc1be587e4`；`:1219` 与 `:1226`（M2-16E AC7 与 VER3）用 `2ebd409045b5114707986b66a7a6b42b5a4a4b7e bc1be587e4`。归档实测见 `codex-plan-r25.log`（1459 vs 158）。**这一矛盾不依赖 source clone 即可在文件内证实。**
- **为何重要：** 该 DEP 是 M2-16A/M2-16E 的「可复制证据」合同，写成恒不可满足即让两卡永远无法取得 `Acceptance`；同时 DEP 与 M2-16E 对「#456 映射输入集合」给出两个不同的范围，`M2-16E` AC1「不得只枚舉前綴子集」与 VER3 的覆盖完整性断言因此失去唯一基准。
- **最小修复：** `:709` 第 7 格的计数命令改为 `git -C "$SRC_CLONE" diff --name-only 2ebd409045b5114707986b66a7a6b42b5a4a4b7e bc1be587e4 | wc -l` = 158，并保留 merge-base pin 验证在计数之前；`:453` 同步如实记录。

### P1-3　M2-02F 与 M2-08 各自交付新 SeaORM entity，但写集都缺唯一注册文件 `src/internal/model/mod.rs`

- **位置：** `plan-20260819.md:1530`（M2-02F 写集）、`:1534`、`:1556`；`:2061`（M2-08 写集）、`:2065`、`:2087`；对照 `:1417`（M2-02 AC5）、`:1451`（M2-02 写集）、审计表 `:804`/`:811`
- **问题：** M2-02 AC5 明写「`memory_terminal_observation` entity 由 M2-08、`memory_compiler_identity` 由 M2-08…、`memory_episode_search_doc` 由 M2-02F 各自交付**並更新 registry**」。但两卡的 `Implementation write set` 与 `Files likely touched` 都**不含** `src/internal/model/mod.rs`：M2-02F 只列 `src/internal/model/memory_episode_search_doc.rs`，M2-08 只列四个 entity 文件。M2-02 是唯一列出该 registry 的卡（`:1451`），而它在 DAG 上位于两卡之前，无法预先注册尚不存在的模块（`pub mod memory_compile_job;` 指向不存在的文件即编译失败）。
- **我实际核对的命令与结果：** `sed -n '1,48p' src/internal/model/mod.rs` → 该文件是扁平的 `pub mod <entity>;` 清单（`ai_decision_proposal` … `working_dirty_meta`，45 个模块 + `#[cfg(test)] mod reference_test;`），没有 glob/自动发现机制。因此任何新增 entity 文件**必须**同卡修改它。
- **为何重要：** ① 违反模板 G-05「一张卡必须能在不阅读其它卡正文的前提下被执行」——按写集执行，新 entity 文件不进入 crate，`cargo check` 即失败，M2-02F 的 `cargo test --lib internal::model::memory`（由 M2-02 的 VER 覆盖）与 M2-08 的 identity 矩阵都无法通过；② 写集是 G-10 的 I–I 相交判定与 `prod-files` 计数的唯一依据，遗漏使审计表 `:804`=`4/8`、`:811`=`3/11` 失真；③ 这与本计划在 R30-r13 已确立的「首建卡写集补 `mod.rs` 註冊並同步 prod-files」口径（`:465`）不一致，其余模块（`memory/mod.rs`、`compiler/mod.rs`、`context/mod.rs`、`adapters/mod.rs`）都已遵守。
- **最小修复：** 把 `src/internal/model/mod.rs` 加入 M2-02F 与 M2-08 的 `Implementation write set`；`prod-files` 由 `8→9`（M2-02F）、`11→12`（M2-08），`Files likely touched` 同步（M2-02F「…+ model registry」、M2-08「12 个生产文件以内（7 个 memory 模組 + 4 个 entity + model registry）」），审计表 `:804` 改 `4/9`、`:811` 改 `3/12`。两者仍满足 G-04 的 `M`（≤4 落点、≤12 生产文件），**不需要新 waiver**。

### P1-4　M2-16C 首建 `agent_bridge/memory.rs`，但写集缺模块注册文件 `src/internal/ai/agent_bridge/mod.rs`

- **位置：** `plan-20260819.md:1028`（M2-16C 写集）、`:1032`、`:1054`；对照 `:65`（§0.4 移植所有权）、审计表 `:821`
- **问题：** §0.4 `:65` 已定「`agent_bridge/memory.rs` → **M2-16C**；`agent_bridge/{protocol,ingress,**mod**,transport,workspace,authorization}.rs` 之行為由 M2-16C 收口」，但 `:1028` 的写集只有 `src/internal/ai/agent_bridge/{protocol.rs,ingress.rs,memory.rs}`，没有 `mod.rs`。
- **我实际核对的命令与结果：** `ls src/internal/ai/agent_bridge/` → `authorization.rs ingress.rs methods.rs mod.rs mutations.rs protocol.rs provenance.rs redaction.rs storage.rs transport.rs vcs.rs workspace.rs`；`Grep '^(pub )?mod |^pub use' .../agent_bridge/mod.rs` → `:18-28` 为 `pub mod authorization; … pub mod workspace;` 十一行显式注册。新建 `memory.rs` 不加 `pub mod memory;` 即不进入 crate。
- **为何重要：** 与 P1-3 同为 G-05/G-10 违规：按写集执行该卡无法编译，`prod-files=5`（`:1054`）与审计表 `:821` 的 `4/5` 失真。M2-16C 处在 `M2-12 → M2-16C → M2-13` 的串行窗口上，写集不准确直接影响 I–I 判定与发布窗口锁定。
- **最小修复：** 写集加 `src/internal/ai/agent_bridge/mod.rs`（`pub mod memory;` 注册），`prod-files` `5→6`，审计表 `:821` 改 `4/6`（仍在 `M` 范围内，无需 waiver）。

---

## P2

1. **`:2842`–`:2843` review log 的 R30-r26 两行写成「R30-r25/**r31** 修订后的完整计划」**——`r31` 是笔误（r26 评审的对象是 r25 修订版）。最小修复：改为「R30-r25 修订后的完整计划（r26 期間凍結）」。
2. **`:1028` M2-16C 未处理 `authorization::classify` 的默认拒绝语义。** `src/internal/ai/agent_bridge/authorization.rs:32-43` 的 `classify` 对未列名方法一律返回 `ActionClass::Dangerous`（fail-closed，需显式 `approval`）。新增的 `memory.recall` 是只读召回（ADR-M2-13 要求 host 常规调用），若不把它加入 `Normal` 分支，AC7 的「fake host 走通 recall/record 往返」E2E 与「audited `memory.recall`」的设计意图不符。最小修复：把 `src/internal/ai/agent_bridge/authorization.rs` 纳入写集并在 AC3 明写两个新方法的 `ActionClass`，或在 AC 中显式声明 record/recall 均走 approval 门。
3. **`:2061` M2-08 写集中混入整句并留下「。、」标点残留**：「…（entity 交付，含 identity 表的唯一數據訪問層）；bridge 落點由 M2-16C 交付，本卡序列化消費**。、**`tests/memory_episode_test.rs`…」。写集应是可机械解析的路径清单。最小修复：把说明句移到写集段落末尾独立成句，路径清单用顿号连续列出。
4. **`:1818` M2-05 的 V9 文档门在开工前即恒真。** 该 VER 是 `for f in … ; do test -f "$f" || exit 1; done`，只校验 33 份文档**存在**。我逐一 `ls` 核对：11 份 `docs/commands/*.md`、11 份 `docs/commands/zh-CN/*.md`、11 份 `docs/development/commands/*.md` **今天全部已存在**，因此该门在本卡做任何文档同步之前就通过，无法失败。最小修复：改为内容性守卫（如 `rg -q 'libra/memory' <每份文档>` 的退出码形式），或在 AC8 注明该门只作清单完整性、同步性由 review 逐份 diff 判定。
5. **`:1894` M2-06 写集有自相矛盾的括注**：「`src/utils/error.rs`（`LBR-MEMORY-*` variant）**（仅 query helper）**」——后一括注疑为早期条目残留。最小修复：删除「（仅 query helper）」。
6. **`:1534` M2-02F 的 `Files likely touched` 写「≤12 个生产/配置文件」，与 `prod-files=8`（`:1556`）不一致**（与 r25 已修的 M2-06 同类）。最小修复：改为「8 个生产/配置文件」。
7. **`:1644`/`:1663` M2-03 的 Description 与 AC7 都承诺「以 `rg` 退出碼守衛證明無命令層後門」，但 `:1667-1670` 的 4 条 VER 全是 `--lib`，没有任何 `rg` 守卫 checkbox。** 最小修复：补一条模板 exit-code 形式的 `rg` 守卫 VER（VER 4→5，审计表 `:806` 同步），或把 AC7 的证明方式改为 `--lib` 可断言的形式。
8. **`:2383` 在非表格上下文的代码片段内使用了表格转义 `\|`**：`` `libra memory record --observation <file\|-> …` ``。GFM 只在表格单元格内解析 `\|`，此处位于 AC checkbox，渲染出的是字面 `\|`（ADR-M2-05 `:532` 的同一命令用未转义 `|`，是正确写法）。最小修复：`:2383` 改用未转义 `|`。
9. **模板 `plan-template.md:262` 的 `spike` 允许清单里 `sql/**` 出现两次**：「至少覆盖 `src/**`、`sql/**`（`web/src/**`、`worker/src/**` 於 0.23.x 已拆除…）、`sql/**`、`build.rs`…」。r25 的 web/worker 注记插入时留下重复项。最小修复：删除重复的第二个 `sql/**`。
10. **多张卡的 `Verification` 清单被单个空行切成两段**（`:1603` M2-02R、`:1817`/`:1819` M2-05、`:1005` M2-16C、`:1934` M2-16B、`:1158` M2-16D1）。CommonMark 下仍是同一个 loose list，不影响计数与渲染正确性，但与其余卡的紧凑写法不一致，历史上已多次被误判为「连续空行」回归。最小修复：统一删除这些分段空行。

---

**计数：`P0=0`，`P1=4`，`P2=10`。**

VERDICT: FAIL
EXIT=0
