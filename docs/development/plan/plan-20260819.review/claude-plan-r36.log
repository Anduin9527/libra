# plan-20260819 R30-r36 review — Claude（評審期間凍結：plan sha256 567dfc11…、template 49f94da3…）
# reviewer: claude -p --effort xhigh --allowedTools Read Grep Glob (read-only, prompt via stdin)
# date: 2026-09-22T05:04:00+08:00
567dfc117bc83b360694c5e52d921a98bb7cecd178e8f74f07e523bf52053e48  docs/development/plan/plan-20260819.md
49f94da35c8828e72caaae52006f52c84c72647016dbce188cad77715b2f7f4f  docs/development/plan/plan-template.md
# review prompt: plan-20260819.review/review-prompt-r36.md

# R30-r36 复审报告（Claude，只读）

## 零、被审版本与核对基线

| 项 | 值 |
|---|---|
| `plan-20260819.md` SHA-256 | `567dfc117bc83b360694c5e52d921a98bb7cecd178e8f74f07e523bf52053e48`（= `r30-r36-sha256.txt`） |
| `plan-template.md` SHA-256 | `49f94da35c8828e72caaae52006f52c84c72647016dbce188cad77715b2f7f4f`（v2.8，与 r35 同版，未改） |
| `plan-status.md` SHA-256 | `156099799bc22a2bd09383f75a1479064f3d82a642b8b3fd0466ad3e080027a1` |
| 仓库 HEAD | `45ad1f7f2`；crate `0.23.36` |

本轮只读；未修改任何文件。

## 一、r35 findings 逐条核对

**Codex r35 P1（minor/capability 会话隔离未定义）——已关闭。**
- `:1009` 新增 AC：握手 `protocol.minor` + 可选 `capabilities`、**协商结果按 session 保存于 dispatcher/session 状态**、`dispatch` 以该 session 集拒绝未协商 method、1.0 peer 只获且只能调用 20、1.2 peer 获 22、旧 peer 调用 `memory.*` 稳定 `UNKNOWN_METHOD`/`denied`。
- 具名 VER `:1028` `protocol_v1_0_peer_gets_only_20_methods`、`:1029` `unnegotiated_memory_method_rejected`；判据块 `:1046`/`:1047` B11/B12。
- 计数全链同步（实测）：`:1081` `AC=41/8@EX-M2-07; VER=13/8@EX-M2-06`；`:838` 审计行同值；`:800` EX-M2-07 waiver 分子 41；`:801` EX-M2-06 分子 13；`:812` 全域口径 `M2-16C=41`；`:1049` 算式 `2+4+6+1+9+3+2+2+2+5+1+1+3 = 41` ✓；`:1033` 声明 13 门与 13 个 checkbox 一一对应（实测 VER checkbox `:1019–:1031` = **13**）。
- 写集 `:1055` 含 `protocol.rs,ingress.rs,memory.rs,mod.rs,authorization.rs` + `src/command/agent/bridge.rs`。
- 陈旧口径零残留：`rg '38/8@EX-M2-07|11/8@EX-M2-06|B0–B10|M2-16C=38'` → **No matches**。

**Codex r35 P2 / Claude r35 P2-1（review log Scope 轮次）——未关闭**，见 P2-1。

**Claude r35 P2-2（§0.1 producer 证据不可复算）——已关闭。** `:25` 改为具名命令，本轮实测复现：
- `rg -n 'IntentSpec::|use .*IntentSpec|: IntentSpec|IntentSpec \{' src` → **No matches found**；
- `rg -n 'git_internal::internal::object::(task|intent)' src` → 仅 `src/internal/ai/agent_run/task.rs:21`（doc comment）；
- `src/command/cat_file.rs:155-184` 实读为 `AI_OBJECT_TYPES` 小写字符串表 ✓。

**Claude r35 P2-3（`EX-M2-09` AC5 未项目化）——已关闭。** `:2074` 为独立 bullet，位于 `:2073` AC4 与 `:2075` AC6 之间，含 `AC5-1..AC5-17`（17 条）；`:2068` 算式 `2+4+4+3+17+2+3+3 = 38`，与 `:798` waiver、`:828` 审计、`:2120` Granularity、`:812` 全域五处一致；`rg '34/8@EX-M2-09'` 零命中。

## 二、全量复审（本轮实测结论）

- **25 卡计数**：按 `Acceptance criteria:`→`Verification:`→`Dependencies:` 区间点 `^- \[ \]`，25 卡 AC/VER checkbox 与各卡 `Granularity`、审计表逐格一致（含 waiver 卡谓词还原 M2-05=25、M2-07=9、M2-08=38、M2-13=45、M2-16C=41）。
- **审计表**：`rg -c '^(\|[^|]*){15}\|\s*$'` = **27**（header+sep+25）；末行 `:842` M2-15 → `:843` 纯 `$`（`cat -A` 位元组确认物理空行）→ `:844` `**Verification 新增标记：**`。
- **waiver / 依赖表**：`EX-M2-*` 行 9 条，全部恰 8 列；`DEP-M2-*` 行 7 条，全部恰 8 列。
- **字段完整性**：`^\*\*Full-suite trigger:\*\*` = 25；`Granularity/Release boundary/C-D coverage/Version increment/Release write set` 合计 125 = 25×5。
- **格式门**：`rg -U '\*\*Verification:\*\*\n\S'` 零命中；`rg -U '\S\n\*\*Dependencies:\*\*'` 零命中；`rg '^\*\*[^*]+:\*\*\S'` 零命中；`` rg '`[^`]*\\\|[^`]*`' `` 仅 `:460`/`:2880` 两条描述该修复本身的历史文本。
- **DAG**：`:679–:711` 25 节点全覆盖、无环；`M201K→M216C`/`M201K→M213` 仅在第 3 个「实施顺序」图；各卡 `Dependencies` 与审计 `deps` 一致。
- **拆除面无执行性依赖**：`providers/`、`context_budget/`、`ai/client.rs`、`prompt/`、`mcp/`、`orchestrator/`、`web/`、`operation_wrapper.rs` 实测均不存在；计划内全部引用落在 §0.1 历史表、ADR Context、禁止清单与零命中守卫。
- **事实基线（核对日成立）**：migration tip = `2026091901_operation_boundary_claim_columns`；版本面 = 三处（`version_surface_sync.rs:11-13` 注释「now: `Cargo.toml`, `install.sh`, `install.ps1`」）；`base.yml` jobs = `format/clippy/owner-liveness-macos/redundancy/test/network-remotes/opencode-export-linux`（7，与 DEP-M2-CI-02 逐字一致）；`release.yml` = 5 job（与 DEP-M2-CI-03 一致）；`codeql.yml:21 analyze`、`:44-46` `actions`/`rust` matrix ✓；`authorization.rs:19 ActionClass`、`:86 enforce`、`tests/agent_bridge_approval_test.rs` 既有 ✓；`protocol.rs:62 METHOD_ALLOWLIST`、`:532 allowlist_has_exactly_20_methods`、`tests/agent_bridge_protocol_test.rs:15 protocol_v1_allowlist_is_exactly_20_methods` ✓；`src/cli.rs` 无 `Memory`、`src/internal/model/mod.rs` 无 `memory` ✓。
- **`$SRC_CLONE`**：`rg 'git -C [^"]'` 仅命中 `:73`/`:482` 两条策略/历史文本，全部可执行命令均为 `git -C "$SRC_CLONE"`；`<source clone>` 占位符零残留。source clone `/run/media/genedna/data/libra-pr456` 本轮经 Glob 确认存在（含 #456 的 `docs/commands/memory.md`），但本会话 Bash 策略拒绝对该路径执行 `git -C`，故 merge-base `2ebd409…` 与 158-path 计数本轮仍由 `codex-plan-r35.log` 的归档输出承担，符合 `DEP-M2-SRC-01`「双评审至少一轮在可访问环境归档」。
- **plan-status**：`:46` 含 r18–r35 全部轮次与计数（r35 行「Codex FAIL 0×P0/1×P1/1×P2：協議會話隔離未定義、Claude PASS 0×P0/0×P1/3×P2」与两份 log 一致）+ `r36 待跑`；`:201` M2-16A `blocked`、❌ 禁止开工。

---

## P0（0 条）

无。

## P1（0 条）

无。

---

## P2（5 条）

### P2-1　review log 的 Scope 轮次笔误第十一次复发（`R30-r35/r41`）：r35 旧行已修正，但同批新增的 r36 两行重犯

- **位置：** `plan-20260819.md:2896`、`:2897`；规则行 `:810`
- **问题：** 实测 `sed -n '2894,2898p'`：`:2894`/`:2895` 的 r35 行已是正确形态（`R30-r34 修訂後的凍結版（sha256 0cd9c198…）`，即上一轮修订生效），但同一批新增的 `:2896`/`:2897` 两行 Scope 逐字为「`R30-r35/r41` 修订后的完整计划（r36 期間凍結）」。本计划不存在 r41 轮；`:810` 规定「Round 栏轮次号必须等于 Scope 栏所写修订版之轮次 + 1」，Round = `R30-r36` ⇒ Scope 应为 `R30-r35`。`rg 'r3[7-9]|r4[0-9]'` 确认 `r41` 仅在这两行出现。
- **为何重要：** review log 是双 PASS 的唯一审计轨迹，Scope 轮次号决定被审 sha256 能否回指正确修订版。历次修复都只改「当前行」而不阻止下一轮新增行重犯，说明 `:810` 仍是纯文字提醒而非可执行断言。不阻塞任何任务卡执行，故列 P2。
- **最小修复：** 两行 Scope 改为「R30-r35 修订后的冻结版（sha256 `0cd9c198…`）」；并把 `:810` 改写为与 `:808` 同形态的可执行断言（例：抽出每行 Round 与 Scope 的轮次号比对 `Round − 1`，不等即 exit 1），使其对新增行同样生效。

### P2-2　卡边界空行在 `M2-08 → M2-09` 再次回归，计划自设的 `:808` 零命中机械检查当前实测失败

- **位置：** `plan-20260819.md:2120`（M2-08 `Granularity`）→ `:2121`（`### Task M2-09`）；规则行 `:808`
- **问题：** `:808` 规定「`rg -U '\S\n### Task M2-'` 必須零命中」。本轮实测该命令**命中 1 处**：`:2120` 的 Granularity 行下一行直接是 `:2121` 的 `### Task M2-09` 标题，无空行（`sed -n '2110,2125p'` 确认）。r34 曾把同一处修好（Claude r34 P2-2、Codex r34 P2 均记录），r35 冻结版上 Claude 实测为零命中；本轮该处随 `EX-M2-09` AC5 bullet 抽取的同批编辑再次丢失空行。其余 24 处卡边界正常。
- **为何重要：** 这是计划自己写下的硬性机械检查（「必須零命中」），当前不成立；同一自设门在其管辖的编辑动作上第三次失效，对编辑者已失去约束力。CommonMark 下 ATX 标题仍可中断段落，渲染不受影响，也不改变任何谓词/依赖语义，故列 P2 而非 P1。
- **最小修复：** 在 `:2120` 与 `:2121` 之间插入一个空行；并把 `:808` 的检查纳入每轮修订后的自检脚本（与 P2-1 的 review-log 断言同批落地），而不是只在评审时由 reviewer 复算。

### P2-3　M2-16C 新增的 AC9 把两个**已存在**的 wire 字段写成「新增」，且未登记必需的 `PROTOCOL_MINOR: 0 → 2` 常量 bump 与被反转的 `BridgeCapability.methods` 不变式

- **位置：** `plan-20260819.md:1009`（AC9）、`:997–:1001`（M2-16C `Current evidence`）、`:1055`（写集）
- **问题：** AC9 写「握手 wire **增加** `protocol.minor` 與可選 `capabilities`」。实测当前 main：
  - `src/internal/ai/agent_bridge/protocol.rs:97-101` 已有 `pub struct ProtocolVersion { major, minor }`，`:104-109` `current()` 返回 `PROTOCOL_MINOR`；`:588` 既有测试已发送 `{"major":1,"minor":0}` —— `protocol.minor` **已在 wire 上**。
  - `:115-124` 已有 `BridgeCapability { protocol, limits, methods, source }`，`tests/agent_bridge_protocol_test.rs:23 protocol_v1_capability_negotiation_has_expected_limits` 已覆盖 —— `capabilities` 概念也**已存在**。
  - 真正的缺口是 `dispatch_handshake`（`:484-519`）只读 `params.protocol.major`、忽略 peer 的 `minor`，并无条件返回 `METHOD_ALLOWLIST.to_vec()`（`:506`）。
  - 两项本卡未登记的机械前提：(a) `protocol.rs:25 pub const PROTOCOL_MINOR: u32 = 0;` —— 不 bump 到 `2`，AC9 所称「1.2 peer 獲 22 個」在本 build 上无法成立（虽会被 B1/B2 的 recall/record E2E 间接逼出）；(b) `protocol.rs:112-121` 的 doc 不变式「`methods` is the full allowlist … the bridge always enforces the full allowlist server-side / Always the full allowlist」正是本卡要**反转**的既有契约，AC/写集未点名该注释与 `MethodAllowlist::contains` 的「one of the 20 v1 methods」措辞需同批改写。
- **为何重要：** 本卡对同类常量/守卫的处理一向精确到名字（写集 `:1055` 逐项点名 `METHOD_ALLOWLIST`、`allowlist_has_exactly_20_methods`、`protocol_v1_allowlist_is_exactly_20_methods` 的 20→22 与 `UNKNOWN_METHOD` 文案）。`PROTOCOL_MINOR` 与被反转的不变式是同一层级的落点，遗漏会让 ER-02 开工刷新时出现「按 AC 去加一个已存在的字段」的错误判断。行为契约本身（session 级 method 集 + dispatch 拒绝）表述精确且有具名回归，不影响架构成立性，故列 P2。
- **最小修复：** (1) AC9 首句改为「握手 wire 的 `protocol.minor`（现有 `ProtocolVersion`，核对日 `PROTOCOL_MINOR = 0`）与 `BridgeCapability` 由本卡**赋予协商语义**：`PROTOCOL_MINOR` bump `0 → 2`……」；(2) `Current evidence` 增一行记录 `protocol.rs:25`（`PROTOCOL_MINOR = 0`）与 `:112-121`（现「always full allowlist」不变式）；(3) 写集 `:1055` 的 `protocol.rs` 括注追加「`PROTOCOL_MINOR` 0→2、`BridgeCapability.methods` 与 `MethodAllowlist::contains` 的 doc 不变式改写」。

### P2-4　M2-16C 的写集/`prod-files=7` 未涵盖 `agent_bridge/transport.rs` —— 现行 `validate_method` 全局门与连接循环都在该文件

- **位置：** `plan-20260819.md:1055`（`Implementation write set`）、`:1059`（`Files likely touched`）、`:1081`（`prod-files=7`）
- **问题：** AC9 要求「`dispatch` 以該 session 集拒絕未協商 method」。实测现行方法门的唯一 runtime 落点是 `src/internal/ai/agent_bridge/transport.rs:130-131`（`// Validate the method against the allowlist before dispatching.` + `super::protocol::validate_method(&request.method)`），且 `transport::run`（`:59`，由 `src/command/agent/bridge.rs:26` 使用）才是持有 per-connection 状态的生产循环；`methods.rs`/`mutations.rs`/`workspace.rs` 只是被 `src/command/agent/bridge.rs:75-91` 扇出的子 dispatcher。写集只列了 `{protocol.rs,ingress.rs,memory.rs,mod.rs,authorization.rs}` + `src/command/agent/bridge.rs`，未含 `transport.rs`。
- **为何重要：** 20→22 扩表后，`transport.rs:131` 的全局 `validate_method` 会对**所有** peer 放行 `memory.recall`/`memory.episode.record`，session 级拒绝只能退到更后面的 handler 层。该设计仍能给出稳定拒绝（故非 P1），但它是一个应被显式记录的分层决定；本计划对写集/`prod-files` 一向要求可机械核对，把承载现行门的文件留在写集外会让收口时的 callsite diff 抽检漏掉它。
- **最小修复：** 二选一并写进卡内：(a) 把 `src/internal/ai/agent_bridge/transport.rs` 纳入写集并把 `prod-files` 改 `8`（同步 `:838` 审计 `4/8` 与 `:1081`）；或 (b) 在 AC9 明写「session 级 method 集由 `src/command/agent/bridge.rs` 的 `BridgeDispatcher` 持有并强制，`transport.rs:131` 的全局 `validate_method` 保持不变（扩表后先放行、由 dispatcher 稳定拒绝）」，使 `transport.rs` 的不改动成为有据的显式决定。

### P2-5　`EX-M2-07` 的 AC 分组顺序在 r34 刚按 checkbox 序重排，本轮新增的 AC11 又被追加到末尾；`EX-M2-06` 的 B11/B12 同形

- **位置：** `plan-20260819.md:1049`（EX-M2-07 分组）对照 `:1005–:1015`（AC checkbox）；`:1035–:1047`（B0–B12）对照 `:1019–:1031`（VER checkbox）
- **问题：** r34 的修订项之一是「`EX-M2-07` 分組按 checkbox 序重排（AC3 bridge session → AC10）」（`:454` 修订历史）。本轮新增的 minor/capability checkbox 落在 AC 列表**第 5 位**（`:1009`），但分组把它编为 `AC11 minor/capability 會話隔離(3)` 并放在算式末尾，于是分组序 `AC1..AC10, AC11` 与 checkbox 序 `1,2,3,4,**11**,5a/b/c,6,7,8,9,10` 不再对应。`EX-M2-06` 同理：B9/B10（两条 `rg` 守卫）列在 B11/B12 之前，而其 checkbox 在 `:1030`/`:1031`，位于 `:1028`/`:1029` 之后。
- **为何重要：** 分组块是 waiver 准入条件②（门族在卡内逐条枚举）的落点，收口时要按条核对 41 条谓词/13 道门是否都在已审批范围内；顺序错位会让按位置映射的人工或脚本核对错配。总数与一一对应关系本身正确（算式 = 41、`:1033` 声明 13↔13 且逐条可按**名字**唯一定位），且 `EX-M2-02`（`:2422-2424` 的 V1b–V1f 插在 V1 之后而 checkbox 在第 9–13 位）与 `EX-M2-04`（`:1983-1993` T1 对应第 10 个 checkbox）长期为同一形态并已被双方接受，故列 P2 而非 P1。
- **最小修复：** 把 `AC11 minor/capability 會話隔離(3)` 移到 `AC4 ActionClass 契約(1)` 之后（对应第 5 个 checkbox），后续分组编号不变即可，算式总和仍为 41；`EX-M2-06` 把 B11/B12 两行上移到 B8 与现 B9 之间并重编为 B9/B10（现 B9/B10 顺延为 B11/B12），使 13 门与 13 个 checkbox 同序。若判定「一一对应即可、不要求同序」，则在 `:1049`/`:1033` 各加一句「分组按谓词聚合，不按 checkbox 位置排序」，与 `EX-M2-02`/`EX-M2-04` 统一口径。

---

**总计：P0=0，P1=0，P2=5。**

Codex r35 的 1 条 P1（bridge minor/capability 会话隔离）本轮已完整闭合并在 waiver、算式、Granularity、审计表、全域口径五处同步；Claude r35 的 P2-2、P2-3 已闭合。本轮 5 条 P2 分别为审计轨迹（P2-1）、自设机械检查回归（P2-2）、新增 AC 的事实基线措辞与两项机械落点（P2-3/P2-4）、判据块排版顺序（P2-5），均不改动任何谓词、依赖语义、ADR、安全边界或发布流程，也不影响 25 张卡的可执行性。结构性维度（25 卡 checkbox 点算、15 列审计表逐格一致、9 条 waiver 的 8 列格式与算式、DAG 无环且边落点正确、Phase/里程碑/追溯表/完成判据点算、仓库事实基线/版本面三处/CI job 集合/migration tip/模板 v2.8）本轮全部通过。

VERDICT: PASS
EXIT=0
