# plan-20260819 R30-r32 review — Claude（評審期間凍結：plan sha256 977a76fc…、template 49f94da3…）
# reviewer: claude -p --effort xhigh --allowedTools Read Grep Glob (read-only, prompt via stdin)
# date: 2026-09-22T04:18:41+08:00
977a76fcaf88b7fcbeba4ae5d138812b8ce08ef01f886491c1f6dc761a3e0b73  docs/development/plan/plan-20260819.md
49f94da35c8828e72caaae52006f52c84c72647016dbce188cad77715b2f7f4f  docs/development/plan/plan-template.md
# review prompt: plan-20260819.review/review-prompt-r32.md

## R30-r32 复审 — Claude（只读）

**被审版本（本轮冻结）：** `plan-20260819.md` sha256 `977a76fc…`、`plan-template.md` sha256 `49f94da3…`（与 `r30-r32-sha256.txt` / 两份 log 档头一致，本次核对期间未变更）。
**核对方式：** 全文逐卡机械核对 + 仓库事实重验；未修改任何文件。

---

### 一、r31 findings 逐条核对

| r31 finding | 状态 | 实际核对证据 |
|---|---|---|
| Codex r31（PASS，2×P2：producer 措辞、review log Scope） | **未闭合**（见 P2-1/P2-4） | `:32` 仍写 `producer 现为 src/internal/ai/review/store.rs`；`:2866/:2867` Scope 写 `R30-r31/r37` |
| Claude P1-1：`memory_compiler_identity` retention 无 owner | **实质闭合，计数未传导**（见 P1-1） | `:329` 表职责补「TTL 30 天、每倉 ≤1,000 行、原子 prune、fail-closed」；`:2041` M2-08 AC5 明写「`memory_terminal_observation` 與 `memory_compiler_identity` 的 retention/prune 由本卡 `job_sql.rs` 拥有（identity：…引用保護…fail-closed）」；`:2071` 写集含 `job_sql.rs`；矩阵更名 `job_crash_recovery_retention_and_identity_matrix`（`:2063`）；`:1427`/`:1473` 交接句指向 identity |
| Claude P1-2：`authenticated_repo_principal` 无 owner | **部分闭合**（见 P1-2、P2-3） | `:1377` M2-01K 写集新增 `src/internal/ai/principal.rs`（含签名）；`:1371` 新增具名 VER；`:533` ADR-M2-05 与 `:2394` M2-13 AC6 直接引用该函数；`:1037`/`:2422` 两卡 `Dependencies` 列入 M2-01K。**但 M2-01K 无对应 AC，AC 分子 9 无依据**；`:997` 仍写「與 CLI 同源」 |
| Claude P2-1：review log 轮次笔误 | **未闭合（第 7 次）** | `rg 'r3[2-9]'` 只命中 `:2866/:2867` 的 `r37` |
| Claude P2-2：§0.3 无 identity 契约痕迹 | **闭合口径调整** | `:453` r31 修订行已不再宣称 §0.3；ADR-M2-05（`:533`）契约完整 ✓ |
| Claude P2-3：M2-16C 写集括注与 AC3d 矛盾 | **闭合** | `:1040` 已改为 `MemoryRuntime::{observe_terminal, register_compiler_identity, resolve_session_compiler_identity}`，与 `:997` 三方法一致 |
| Claude P2-4：`EX-M2-09` AC5-⑦ 四方法 | **闭合** | `:2046` ⑦ 现为 `register/resolve(handle)/resolve_session/revoke/list` **五方法**；`:2041` AC5 正文亦为五方法集合 |
| Claude P2-5：8 行审计 `axis` 不逐字 | **7/8 闭合**（见 P2-2） | 逐行比对：M2-02/02F/02R/M2-12/M2-14/M2-14C/M2-16E 已同步；**M2-16A 仍差 `rebuild`** |

### 二、本轮机械核对结果

- **25 卡 AC/VER checkbox 点算 vs `Granularity`：** 全部逐卡点算（`rg '^- \[ \]'` 落在每卡 `Acceptance criteria`/`Verification` 区间）。**唯一不符：M2-01K 实际 8 个 AC checkbox，Granularity 与审计表写 `9/8@EX-M2-01`**（见 P1-2）。其余 24 卡一致（M2-02 9/9、M2-05 25 谓词/10、M2-07 9/10、M2-08 8 checkbox→38 谓词/8、M2-13 10 checkbox→45 谓词/15、M2-16C 10 checkbox→38 谓词/11）。
- **`prod-files` 与写集逐档点算：** M2-05=17、M2-08=12、M2-02=11、M2-07=10、M2-02F=9、M2-04=9、M2-16C=7、M2-02R=7、M2-16A=7、M2-01K=7、M2-06=6、M2-13=5 — 全部与写集一致；**`Files likely touched` 仅 M2-01K 落后（写 6，漏 `principal.rs`）**。
- **表格机械列数：** `grep -c '^|'`=520；按「列数 = 行内有效分隔符」分桶 2→105、3→116、4→86、5→155、6→12、8→19、15→27，合计 520，无第八种宽度；审计表 27 行全 15 列、waiver 表 + 依赖表共 19 行全 8 列 ✓（6 列桶中 2 行为 `:456`/`:2858` 的字面 `\|`，非表格错位）。
- **审计表末行物理空行：** `sed -n '831,835p' | cat -A` → `:833` M2-15 资料行、`:834` 纯 `$`、`:835` `**Verification 新增标记：**`；`rg '^\|.*\|\*\*'` 零命中 ✓。
- **空行统一：** 多行 `rg -U '^\*\*Verification:\*\*\n\S'` 与 `rg -U '\S\n\*\*Dependencies:\*\*'` 均零命中 ✓。
- **可复制命令零 `\|`：** `rg '`[^`]*\\\|[^`]*`'` 仅命中 `:456`/`:2858` 两条**描述该次修复本身**的历史文本 ✓。
- **DAG / `Dependencies` / waiver 集合：** DAG 无环；`EX-M2-01..07,09` 共 8 笔 8 列，`EX-M2-08` 仅存历史行 ✓；完成判据「24 张收口卡 + M2-16D1 条件性」点算一致 ✓；Phase 0 不含 M2-16B ✓。
- **plan-status：** `:46` 含 r18–r31 全部轮次与计数（r31 = Codex PASS 0/0/2、Claude FAIL 0/2/5，与 log 一致）+ `r32 待跑` ✓；`:201` M2-16A 为 `blocked`、禁止开工 ✓。

### 三、仓库事实基线（核对日成立）

`Cargo.toml version = "0.23.36"`；版本面 = 三处（`tests/compat/version_surface_sync.rs:11-13`「now: `Cargo.toml`, `install.sh`, `install.ps1`」）；migration tip = `2026091901_operation_boundary_claim_columns`（`sql/migrations/` 末档）；`base.yml` jobs = `format/clippy/owner-liveness-macos/redundancy/test/network-remotes/opencode-export-linux`（7，与 DEP-M2-CI-02 逐字一致）；`release.yml` = `build-and-upload/upload-install-scripts/update-homebrew-tap/verify-homebrew-formula/request-stable-manifest`（5，与 DEP-M2-CI-03 一致）；`authorization.rs:19 pub enum ActionClass{Normal,Dangerous}`、`protocol.rs:532 allowlist_has_exactly_20_methods`、`tests/agent_bridge_protocol_test.rs:15 protocol_v1_allowlist_is_exactly_20_methods`（20→22 前提成立）；`tests/agent_bridge_approval_test.rs` 存在 ✓；`src/internal/ai/agent_bridge/` 无 `memory.rs` ✓。模板 v2.8 的 `web/`/`worker/` 历史注记（`:5/:71/:228-230/:251/:262/:339/:443`）、`:251` 的 `opencode-export-linux` 示例、`:262` 单一 `sql/**` 均成立。全计划对已删表面的引用全部落在 §0.1 历史表、ADR Context、禁止清单或零命中守卫，**无执行性依赖** ✓。

**证据限制：** `$SRC_CLONE`（`/run/media/genedna/data/libra-pr456`）在本沙箱工作目录策略之外（`ls` 被拒），无法执行 `rev-parse`/`merge-base`/158-path 计数。`DEP-M2-SRC-01`（`:715`）已规定来源不可用即 `Lifecycle=blocked` 并要求双评审至少一轮在可访问环境归档，故不计为 finding，但本轮不构成该项证据。

---

## P0（0 条）

无。

---

## P1（2 条）

### P1-1　`EX-M2-09` 的 AC 分子在六处只同步了四处：waiver 补偿条款与粒度审计表仍写 `34`，而卡内、判据块、全域口径、waiver 理由都已是 `38`

- **位置：** `plan-20260819.md:792`（`EX-M2-09` waiver 行的补偿条款）、`:819`（审计表 M2-08 行）；对照 `:792` 理由句、`:802`（AC 計數口徑）、`:2046`（判据规范块）、`:2097`（M2-08 Granularity）
- **问题：** 本轮把 identity retention 的四条谓词（⑭ TTL 30 天、⑮ 每仓 ≤1,000 行、⑯ 原子 prune + 引用保护、⑰ prune fail-closed）加进 AC5，AC5 由 13 升到 17、总谓词由 34 升到 38。实测 `rg -o '34/8@EX-M2-09|38/8@EX-M2-09|M2-08=3[0-9]'` 得四处命中：`:792` → `34/8@EX-M2-09`、`:803`（AC 計數口徑行，实际行号 `:802`）→ `M2-08=38`、`:819` → `34/8@EX-M2-09`、`:2097` → `38/8@EX-M2-09`；`:2046` 的算式 `AC1(2)+AC2(4)+AC3(4)+AC4(3)+AC5(17)+AC6(2)+AC7(3)+AC8(3)` 逐项相加 = **38**，`:2046` 的 AC5 明细 ①–⑰ 点算 = **17**。同一 waiver 行内部也自相矛盾：理由句写「按獨立謂詞還原共 **38** 條」，紧接的补偿句写「AC 分子如實寫 `34/8@EX-M2-09`」。
- **为何重要：** 模板 `plan-template.md:526` 的 G-03 门族型 waiver 准入条件 ③「分子如实写作 `n/上限@EX-ID`」与 ④「门族增减时**同批**更新 waiver 行与粒度审计表」是缺一不可的准入项；`:537` 的审计表判定规则又规定「任一列不达标即不得开工」。审计表与 waiver 行正是执行前 ER-03 逐字段机械核对的两个落点，此处写 `34` 会让 ER-03 判定本卡 AC 超限而分子不实（34 ≠ 实际 38 谓词，也 ≠ 8 个 checkbox），直接卡住开工；更实质的风险是：被审批的豁免范围停留在 34 条谓词，而本轮新增的**四条 identity retention 谓词正好落在第 35–38 位**——收口时无法判定这四条是否在已审批范围内，等于把本轮唯一的安全性补丁放在豁免边界之外。
- **最小修复：** 把 `:792` 补偿句与 `:819` 审计表 AC 列一并改为 `38/8@EX-M2-09`，使 waiver 理由/补偿、`:802` 全域口径、`:2046` 判据块、`:2097` Granularity、`:819` 审计表五处同为 38；不另改谓词内容。

### P1-2　M2-01K 的 `AC=9/8@EX-M2-01` 三重不成立：卡内只有 8 个 checkbox、`EX-M2-01` 的 `任务` 列不含 M2-01K、`exception` 列写 `N/A`；而本轮新交付的 `authenticated_repo_principal` **没有任何 AC 承载**

- **位置：** `plan-20260819.md:1354-1361`（M2-01K 的 8 条 AC）、`:1371`（新增 VER）、`:1377`（写集新增 `principal.rs`）、`:1381`（`Files likely touched`）、`:1403`（Granularity）、`:810`（审计表 M2-01K 行）、`:795`（`EX-M2-01` waiver 行）、`:802`（AC 計數口徑）；对照 `:453`（r31 修订行自述「AC 9/VER 7/prod 7」）
- **问题：** 实测 `sed -n '1349,1362p' | grep -c '^- \[ \]'` = **8**（AC），`sed -n '1363,1372p'` 区间 = **7**（VER）。但 `:1403` 与 `:810` 都写 `AC=9/8@EX-M2-01`。四项独立不成立：
  1. **分子不实。** `:802` 的全域口径明写「`AC` 分子 = checkbox 數；僅 waiver 卡按獨立謂詞還原並在判據規範塊逐條注明還原依據（M2-05=25、M2-07=9、M2-13=45、M2-16C=38、M2-08=38）」——M2-01K **不在**该清单，且卡内**没有**「判据规范（非计数正文）」块，因此其分子只能等于 8。
  2. **waiver 作用域错误。** `:795` 的 `EX-M2-01` 行 `任务` 列逐字为 `M2-02,M2-02F,M2-02R`，理由亦限定「三张 migration 卡的迁移 up/down/fresh-vs-upgrade 与 GC-13 判据」。模板 `:537` 规定引用 `@EX-ID` 必须「`任务` 列等于引用它的卡（或显式 `ALL/<作用域>`）…任一条不满足仍判为不达标」。M2-01K 既非 migration 卡，也不在该列。
  3. **自相矛盾。** `:1403` 与 `:810` 的 `exception` 字段同时写 `N/A`，与 AC 列的 `@EX-M2-01` 直接冲突。
  4. **谓词本体缺失。** 8 条 AC（`:1354-1361`）全部围绕 keyed-digest seed 生命周期与 `libra config` 保留键保护，**无一条**提到 principal 派生。`rg -n 'authenticated_repo_principal'` 全文只命中 `:453`（修订历史）、`:533`（ADR-M2-05）、`:1371`（VER）、`:1377`（写集）、`:2394`（M2-13 AC6）——**M2-01K 自身的 AC 区零命中**。`:1381` 的 `Files likely touched` 仍写「6 个生产文件（`keyed_digest.rs`、`ai/mod.rs`、`vault.rs`、`config.rs`、`command/config.rs`、`utils/error.rs`）」，漏 `principal.rs`，与 `prod-files=7` 不一致。
- **为何重要：** 这是本轮为关闭 r31 P1-2 而做的核心补丁，落地只做了一半——写集与 VER 到位，AC 与计数没到位。后果有三层：(a) 机械层面，`:537` 的判定规则使 M2-01K **不得开工**，而 M2-01K 在全串行 DAG 上是 M2-02 之后所有卡的前置，等于整条链被一个不成立的 waiver 引用卡住；(b) 验收层面，`authenticated_repo_principal` 是整条 trust gate 授权链的根输入（`:533` ADR-M2-05、`:997` M2-16C AC3d、`:2394` M2-13 AC6 全部消费它），没有 AC 就没有「派生材料是哪个 vault 条目/config key」「缺失时返回哪个稳定拒绝码」的可判定验收——只有一条 VER 命令名，实现者可以任意选材料而仍让测试通过；(c) 审批层面，`:453` 修订历史记载「AC 9」这一未发生的修改，使修订历史作为审计轨迹的可信度受损（与 r31 §0.3 同型）。
- **最小修复：** 二选一并同批传导。**推荐**：在 `:1361` 后新增第 9 条 AC，写明 `authenticated_repo_principal(repo, worktree) -> Result<Principal>` 是唯一 principal 来源、具名派生材料（哪个 vault 条目或 `memory.*` config key）、缺失/解密失败时的稳定 `LBR-MEMORY-<三位數字>` 拒绝、同一 repo/worktree 跨重启派生一致、请求体自报一律不参与；随后在 `:795` 的 `EX-M2-01` `任务` 列加入 `M2-01K`（并把理由扩到「keyed-digest + principal 生命周期门族」）**或**新登记一条 `EX-M2-10`，并把 `:1403`/`:810` 的 `exception` 列由 `N/A` 改为该 ID、卡内补「判据规范（非计数正文）」块逐条还原 9 条谓词。**备选**：若不愿新增 waiver，把该 AC 与既有 AC1 合并为一条，分子回写 `8/8`、去掉 `@EX-M2-01` 与 `exception`。两条路线都须同批把 `:1381` 改为「7 个生产文件（…、`principal.rs`、…）」。

---

## P2（5 条）

### P2-1　review log 的 Scope 轮次笔误第七次复发（`R30-r31/r37`），再次违反同档 `:798` 自设的机械检查

- **位置：** `plan-20260819.md:2866`、`:2867`；规则行 `:798`
- **问题：** r32 两行 Scope 写「R30-r31/**r37** 修订后的完整计划」。本计划不存在 r37 轮。`rg -n 'r3[2-9]|r4[0-9]'` 全文只命中这两行。`:798`「Round 欄輪次號必須等於 Scope 欄所寫修訂版之輪次 + 1」正是 r30 为此新增；历次笔误为 `r31`(r26 修)、`r33`(r28)、`r34`(r29)、`r35`(r30)、`r36`(r31)，本轮 `r37`——上一轮已修好的 r31 行（`:2864/:2865`「R30-r30 修訂後的凍結版（sha256 `b78d3c89…`）」）与同一次编辑新增的 r32 行再次不同口径。
- **为何重要：** review log 是双 PASS 的唯一审计轨迹；Scope 的轮次号决定被审 sha256 能否回指正确修订版。一条自设机械检查连续三轮在其被新增的同一栏位上失效，该检查本身失去约束力。
- **最小修复：** 两行 Scope 改为「R30-r31 修訂後的凍結版（sha256 `977a76fc…`）」；并把 `:798` 改写为可执行断言（新增行 Scope 轮次 = Round 轮次 − 1），而非纯文字提醒。

### P2-2　审计表 M2-16A 的 `axis` 仍非逐字（缺 `rebuild`），与 `:453` 自述「8 行審計 `axis` 逐字同步」不符

- **位置：** `plan-20260819.md:827`（审计表）对照 `:906`（M2-16A Granularity）
- **问题：** 逐行比对 `rg -o 'axis=[^;]*'` 的 25 个卡内值与审计表第 3 列：r31 列出的 8 行中 M2-02、M2-02F、M2-02R、M2-12、M2-14、M2-14C、M2-16E 已改为逐字相同；**M2-16A 仍是审计表 `domain-layer port + MemoryConfidence` vs 卡内 `domain-layer port + MemoryConfidence rebuild`**。
- **为何重要：** R30-r27 起审计表被当作「`G-*` 的机器可核对摘要」逐格字符串相等比对（M2-03 的 `axis` 不一致当时即被列为 finding 并修正）。残留一行意味着该列仍需人工判读；同时 `:453` 记录了未完全发生的修改。
- **最小修复：** `:827` 的 `axis` 列改为 `domain-layer port + MemoryConfidence rebuild`。

### P2-3　`authenticated_repo_principal` 的三处引用口径不一：M2-16C AC3d 仍写「與 CLI 同源」（指向下游 M2-13），且 M2-16C/M2-13 的 `Granularity deps=` 与审计表 `deps` 列都漏了已写进 `Dependencies` 的 M2-01K

- **位置：** `plan-20260819.md:997`（M2-16C AC3d）、`:1067` 与 `:829`（M2-16C Granularity / 审计行）、`:2452` 与 `:824`（M2-13 Granularity / 审计行）；对照 `:1037`、`:2422`（两卡 `Dependencies`）、`:533`、`:2394`
- **问题：** `:533` ADR-M2-05 与 `:2394` M2-13 AC6 已改为直接引用 `M2-01K authenticated_repo_principal(repo, worktree)`，但 `:997` 的 AC3d 仍写「已認證 ingress 在 handshake 時以**與 CLI 同源**的 vault/config 派生 principal 呼叫 `register_compiler_identity(...)`」——DAG（`:697`）是 `M216C → M213`，M2-16C 执行时 CLI 尚未交付，该锚点方向仍倒置。另外 `:1037` 已列「M2-01K（principal 派生）」、`:2422` 已列「M2-01K（principal 派生）」，但 `:1067`/`:829` 的 deps 仍为 `M2-12,M2-16B,M2-08,DEP-M2-SRC-01,DEP-M2-DSH-01（非阻塞）`，`:2452`/`:824` 仍为 `M2-16C,M2-12`，DAG 也无对应边（M2-01K 仅通过串行链传递可达）。
- **为何重要：** G-05（`plan-template.md:450`，不可豁免）禁止「见上文／同上一卡式跨卡隐式约定」；「與 CLI 同源」正是该反模式的残留形式。`Granularity` 摘要行是 ER-03 开工前逐字段核对的对象，与 `Dependencies` 不一致会让核对结论取决于读哪一处；G-06 又要求 DAG 边与 `Dependencies` 一致。
- **最小修复：** `:997` 的「與 CLI 同源的 vault/config 派生 principal」改为「以 M2-01K `authenticated_repo_principal(repo, worktree)` 派生的 principal」；`:1067`/`:829` 的 deps 补 `M2-01K`，`:2452`/`:824` 的 deps 补 `M2-01K`（或按其它卡既有写法标注「經串行前置傳遞」）。

### P2-4　§0.2 事实面表把 `src/internal/ai/review/store.rs` 记为 `agent_run` 终态的 producer，与仓库事实及本计划 `:372` 自相矛盾

- **位置：** `plan-20260819.md:32`（§0.2 `agent_run` 行的「M2 用途」列）；对照 `:372`（终态触发点事实行）、`:1082`（M2-16D Current evidence）
- **问题：** `:32` 写「可选的自动触发适配面（子代理 run 终态）；**producer 现为 `src/internal/ai/review/store.rs` 与会话 JSONL**」。实测：`rg -n 'AgentRunStatus::Completed|AgentRunStatus::Failed' src/` 仅 6 处命中，**全部在 `src/internal/ai/agent_run/run.rs:99-143` 的单元测试内**；`rg -n 'AgentRun|agent_run' src/internal/ai/review/store.rs` 只有一处**文档注释**（`:8 //! Modeled on [crate::internal::ai::agent_run::event_store::AgentRunEventStore]`）；`rg 'AgentRunEventStore' src/ | grep -v 'agent_run/'` 也只命中这条注释——即 `review/store.rs` 既不写 `agent_run` 行，也不产生终态状态。同一计划的 `:372` 则正确写着「`AgentRunStatus::{Completed,Failed}` 只有定义与单测，**未发现生产 producer**（待 M2-16D 审计）」。
- **为何重要：** §0.2 是「拆除后仍存在、M2 可以依赖的事实面」，是整份计划的依赖前提表；一条夸大的 producer 断言会让 M2-16D 的审计带着「已有 producer」的预判开工（其 AC2/AC3 正是二选一分支），也与 GAP-M2-16、DEFER-M2-09 的存在理由冲突。
- **最小修复：** `:32` 的「M2 用途」列改为「可选的自动触发适配面（子代理 run 终态）；**核对日无 in-repo producer**（`AgentRunStatus::{Completed,Failed}` 仅定义与单测，见 `:372` 与 GAP-M2-16），`review/store.rs` 仅为同构事件存储、非 `agent_run` 写入者；是否可映射由 M2-16D 审计」。

### P2-5　M2-08 `EX-M2-09` 判据块 AC5 明细存在断句残留「⑬ …覆蓋。；⑭」，且 `job_crash_recovery_retention_and_identity_matrix` 的分支枚举未含 identity prune

- **位置：** `plan-20260819.md:2046`（AC5 明细 ⑬/⑭ 之间）、`:2041`（AC5 正文末段的矩阵覆盖清单）
- **问题：** (a) `:2046` 的 ⑬ 结尾为「…與具名矩陣覆蓋。**；**⑭ identity TTL 30 天」——句号后紧跟分号，是本轮插入 ⑭–⑰ 时的断句残留；同块的标签口径也混用（首行声明 `AC5-1..AC5-17`，正文用 ①–⑰）。(b) `:2041` 列举矩阵覆盖的分支为「边界重启、prune 后重放、未处理行不可删、删除前 digest 校验失败 fail-closed、容量饱和 admission 拒绝与释放后重试、CLI stdin 补录與 host-proposal 的 observe→crash→restart→Write Episode、bridge 同路徑 host-proposal、status-only/evidence-only/关系-only 变化後重啟重放、`CompilerIdentityV1` 六態」——**九项全部是 observation/identity 生命周期，无一项是 identity 的 TTL/行数上限裁剪或「被未過期 snapshot 或未處理 job 引用的 handle 不可刪」**。
- **为何重要：** ⑭–⑰ 四条谓词进入了 G-03 豁免计数，却没有任何具名分支把它们落到可失败的回归上；`memory_compiler_identity` 在本轮之后是「每个 bridge handshake 一行」的增长面，prune 的引用保护一旦实现错误（误删仍被未处理 job 引用的 handle）会使该 job 永久无法解析 identity，而现有矩阵不会发现。断句残留则使该块无法被脚本按统一分隔符解析。
- **最小修复：** `:2046` 把「覆蓋。；⑭」改为「覆蓋；⑭」，并统一标签为 `AC5-1..AC5-17`；`:2041` 的矩阵覆盖清单补三个分支：「identity 超 TTL/超每倉上限的原子裁剪」「被未過期 snapshot 或未處理 job 引用的 handle 拒絕裁剪」「identity prune 失敗 fail-closed」。

---

**总计：P0=0，P1=2，P2=5。**

本轮在结构性维度全部通过机械核对（25 卡 AC/VER checkbox 点算、`prod-files` 逐档点算、520 行表格列数全覆盖、审计表物理空行、`**Verification:**`/`**Dependencies:**` 空行、DAG 无环与完成判据点算、8 个 `EX-*` 的 8 列格式与判据块、M2-16C 的 38/11 与 B0–B10↔11 checkbox 同序、M2-13 的 45/15 与 V1–V10 拆分、仓库事实基线/版本面/CI job 集合/migration tip/模板 v2.8）。两条 P1 都是**本轮补丁只落地了一半**：identity retention 的语义完整交付但 AC 分子没有传导到 waiver 与审计表（P1-1）；principal 派生 owner 的写集与 VER 到位但没有 AC 承载，且 AC 分子 9 引用了一个作用域不含本卡的 waiver（P1-2）。两者都是纯机械同步，不需要改动任何架构决策。

VERDICT: FAIL
EXIT=0
