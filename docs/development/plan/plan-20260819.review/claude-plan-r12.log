# plan-20260819 R30-r12 review — Claude
# reviewer: claude -p --effort xhigh --allowedTools Read Grep Glob (read-only, prompt via stdin)
# date: 2026-09-21T16:41:31+08:00
2b105de673183bfd1a56fdcc0cda523a91f7eecd67cfe3ad662be3242a2b6033  docs/development/plan/plan-20260819.md
# review prompt: plan-20260819.review/review-prompt-r12.md

我已完整读完 `plan-20260819.md`（2767 行）、`plan-template.md` v2.8、`plan-status.md`，并对仓库事实逐条核对。以下是 R30-r12 只读评审结果。

## 一、r11 findings 逐条核对（全部已关闭）

| r11 finding | 核对方式 | 结果 |
|---|---|---|
| Codex P1-1 `EX-M2-05` waiver 与 `10/8` 同步 | `plan:769` waiver 行枚举 10 道门；`plan:1739-1743` 判据规范块；`plan:1747-1756` VER 恰 10 条；`plan:785,1788` `VER=10/8@EX-M2-05` | ✅ 四处一致 |
| Codex P1-2 `$SRC_CLONE` 未定义 | `plan:691` `DEP-M2-SRC-01` 已给出 `export SRC_CLONE="${SRC_CLONE:-/run/media/genedna/data/libra-pr456}"` + `merge-base`/`rev-parse` pin 校验；`rg "<source clone>\|<source-clone>\|<libra-pr507>"` 零命中 | ✅ |
| Claude P1-1 waiver 9/10 不同步 | 同上 | ✅ |
| Claude P1-2 M2-05 ER-06a | `plan:1737`(AC8) / `plan:1768`(Docs impact) / `plan:1762`(write set) 三处均具名 33 份 EN/zh/开发文档 + 11 页 `*.en.md`；`Glob` 核对 `docs/commands/*`、`docs/commands/zh-CN/*`、`docs/development/commands/*` 各 11 份**全部存在**（M2-05 的 for-loop VER 今日即可执行通过） | ✅ |
| Claude P1-3 `LBR-MEMORY-*` 码形 / owner | `plan:630` GC-M2-08 固定 `LBR-MEMORY-<三位数字>` + `src/utils/error.rs` variant；`plan:567`(001)、`plan:1815`(002)；`src/utils/error.rs:347-364` 确为 `StableErrorCode::as_str` 的 `LBR-<DOMAIN>-<3 位>` owner；`tests/compat/error_codes_doc_sync.rs` 存在 | ✅ M2-01K/04/06/13/16C 写集均含 `src/utils/error.rs` |
| Claude P1-4 审计表末行空行 | `sed -n '800,806p' \| cat -A`：`802` = M2-15 行、`803` = 空行、`804` = `(new)` 段落 | ✅ |
| Codex P2 / Claude P2×5 | 修订历史 `plan:450-461` 无重复 r10/r11 行；review log `plan:2702-2725` 无笔误、r12 两行 `待跑`；`plan-status.md:126-146,201,262` 已含 §3.3 / DEP-M2-ENV-01 / DEP-M2-SRC-01 / DEP-M2-DSH-01(非阻塞) / DEFER-M2-01..09 / r12 待跑 | ✅ |

## 二、全量复审中实测通过的关键事实

| 计划自述 | 我的核对命令 | 结果 |
|---|---|---|
| 版本面三处、以 `compat_version_surface_sync` 为权威 | `Cargo.toml:260-261` + `tests/compat/version_surface_sync.rs:13` | ✅ `Cargo.toml`/`install.sh`/`install.ps1` |
| `base.yml` 7 job | `grep -nE "^  [a-z-]+:" .github/workflows/base.yml` → `format/clippy/owner-liveness-macos/redundancy/test/network-remotes/opencode-export-linux` | ✅ 与 DEP-M2-CI-02 完全一致 |
| CodeQL `analyze` + actions/rust matrix | `codeql.yml:21,44,46` | ✅ |
| `release.yml` 五 job | `release.yml:15,310,480,628,690` | ✅ 与 DEP-M2-CI-03 一致 |
| migration tip `2026091901` | `sql/migrations/` + `src/internal/db/migration.rs:1862,2331`（`max_registered_version()==2026091901`）| ✅ `2026092101+` 重编正确 |
| 事实基线锚点 | `ai/mod.rs:53-68`（无 memory）、`cli.rs:668-680`、`model/mod.rs:38-44`、`op.rs:1144`（`!name.starts_with("libra/")`）、`reference.rs:26-39`（`ConfigKind`）、`agent_run/run.rs:22-43`（`is_terminal`）、`history.rs:1026`（`new_with_ref`，全文 7147 行）、`client_storage.rs:2023`（`ClientStorage::put`）、`vault.rs:59-62`、`config.rs:44-48`、`export_job.rs:1-8`、`Cargo.toml:66-70`、`sql/migrations/README.md:90`、`memory.md:498/621/638/736/981/1334/1959` | ✅ 全部成立 |
| `AgentRunStatus::{Completed,Failed}` 无生产 producer | `rg -n "AgentRunStatus::(Completed\|Failed)" src/` → 仅 `run.rs:99-143`（定义+单测）| ✅ |
| memory.md 零命中 / 17 处 taxonomy 引用 | `rg -c "EpisodePayloadV1\|AutoEpisodeTrustGateV1\|ContextSelectionReceiptV1"` = 0；`rg -c "memory_entity_index\|memory_taxonomy_node"` = 17 | ✅ M2-01 AC1/AC3 有实际目标 |
| `mainline.md` receipt owner 需改路径 | `mainline.md:279,532,606,771,1020,1319` 仍指 `context_budget/receipt.rs`（含 1020 的可执行断言行）| ✅ M2-01 AC3 覆盖 |
| bridge 20-method / `deepseek-harness` 双守卫 | `protocol.rs:62,532`、`tests/compat/agent_bridge_schema_test.rs`（含 `deepseek-harness`）| ✅ |
| M2-03 三个「既有」filter | `history.rs:5889/5981/6075`；`linear_ref_transaction` 零命中（确为 `(new)`）| ✅ |
| GC-M2-14 基建 | `tests/SERIAL_REGISTRY.tsv`、`tests/SRC_SERIAL_REGISTRY.tsv`、`tests/NEXTEST_GROUPS.sh`、`.config/nextest.toml` 均存在 | ✅ |
| M2-05 cloud classifier 落点 | `src/command/cloud.rs:2122 sync_metadata` + `:2128 reference::Entity::find()`；`client_storage.rs:503-629` repair marker / `object_index` | ✅ AC5、M2-04 AC7 锚点真实 |
| 25 卡 / 24 收口卡、AC/VER 计数 | 逐卡数 checkbox，与审计表 `778-802` 每行 `AC`/`VER`/`deps` 逐栏比对 | ✅ 25 行、25 条 Granularity 全部一致；DAG 边与 `Dependencies` 一致、无环 |
| 无残留可执行的已删表面依赖 | 39 处 `providers::`/`context_budget`/`operation_wrapper`/`test-provider` 命中，逐条落在 §0.1 历史表、§0.4、ADR-M2-12/15 或 `rg` 零命中守卫内 | ✅ |

**无法在本会话核对（沙箱限制，非计划缺陷）：** `$SRC_CLONE`（`/run/media/genedna/data/libra-pr456`）与 `../libra-backend/` 均在允许目录外，`ls` 被拒；因此 merge-base `2ebd409045b5114707986b66a7a6b42b5a4a4b7e`、158 路径清单与 11+1 份 `*.en.md` 页面我未能实测。计划已把 pin 校验写成卡内先决命令（`plan:1176,1189`），失败会 fail loud，设计上可接受。

---

## P0（0 条）

无。

## P1（1 条）

**P1-1｜M2-13 声称的 `compat_help_examples_banner` 门对 `libra memory` 是空跑（false green）**

- **位置：** `plan-20260819.md:2302`（AC）、`:2317`（VER）、`:2325`（Implementation write set）；仓库侧 `tests/compat/help_examples_banner.rs:53-117`、`:119-123`；`src/cli.rs:4116-4142`。
- **问题：** AC 写「对齐 `root_after_help_lists_every_visible_command`、`compat_help_examples_banner`、`compat_command_docs_examples_section` 三个既有守卫」，VER 也直接跑 `cargo test --test compat_help_examples_banner every_visible_command_help_renders_examples_section`。但我实测该守卫**不是动态发现**的：它只遍历文件内硬编码的 `const VISIBLE_COMMANDS`（`:53-117`，当前 60+ 个命令名，无 `memory`），而 `tests/compat/command_docs_examples_section.rs:39` 是 `fs::read_dir(docs/commands)` 动态遍历（所以那一门没问题）。该文件注释声称「if this list drifts from `src/cli.rs::Commands`, a separate cli unit test (`cli::tests::root_after_help_lists_every_visible_command`) flags it」，但我读了 `src/cli.rs:4116-4142`：它只断言 `ROOT_AFTER_HELP.contains(name)`，**完全没有**与 `VISIBLE_COMMANDS` 交叉比对。因此不把 `"memory"` 加进 `VISIBLE_COMMANDS`，该 VER 命令会绿，却一次都没跑过 `libra memory --help`。而 `tests/compat/help_examples_banner.rs` 不在 M2-13 的 `Implementation write set` 里。
- **为何重要：** ① 直接违反本计划 `:804`「这一集中标记适用于下列每张卡，**不把零命中视为通过**」的自定规则，等于把一条空跑门当作验收证据（G-02：同一行为轴的验收内容不完整）；② `CLAUDE.md` 的 cross-cutting EXAMPLES 合同明确写「Three compat guards protect this contract. New commands must satisfy all three」——只有把命令加进该清单才算 satisfy；③ 后果是留下一个**永久的回归缺口**：日后任何改动拆掉 `MEMORY_EXAMPLES` 的 `after_help` 接线，三道守卫无一会失败；④ 执行期该卡会写一个未声明在写集内的文件（G-10 写集失真）。
- **最小修复：** 在 M2-13 的 `Implementation write set` 加入 `tests/compat/help_examples_banner.rs`（`VISIBLE_COMMANDS` 追加 `"memory"`），并把 AC 第 7 条改为显式判据，例如「`tests/compat/help_examples_banner.rs::VISIBLE_COMMANDS` 含 `memory`，且 `libra memory --help` 渲染 EXAMPLES 段」，VER 增/改一条 `rg -q '"memory"' tests/compat/help_examples_banner.rs` 形式的非空断言（或沿用本计划的 exit-code 守卫模板）。AC 由 9 增至 10 时同步 `EX-M2-02` 分母写法与审计表。

## P2（10 条，不阻塞）

1. **`Files likely touched` 计数在 r11 加入 `src/utils/error.rs` 后未同步**（`:1331` 写「5 个生产文件」并逐名枚举 5 个，Granularity/审计表为 `prod-files=6`；`:1686` 「7 个…以内」vs 8；`:1834` 「8 个…以内」vs 9；`:2329` 「10 个…以内」vs 11）。模板 G-10 明确该字段为估计值、并发以写集为准，故不判违规。最小修复：四处各 +1 并在 M2-01K 枚举中补 `src/utils/error.rs`。
2. **M2-16C 只点名了两道 20-method 守卫中的一道。** 实测存在两处断言：`src/internal/ai/agent_bridge/protocol.rs:532 allowlist_has_exactly_20_methods` 与 `tests/agent_bridge_protocol_test.rs:15 protocol_v1_allowlist_is_exactly_20_methods`（`assert_eq!(..., 20)`）。AC（`:971`）与写集（`:991`）只名前者。因为后者会 assert 失败（而非空跑），且本卡 `Full-suite trigger=T-1`，可被兜底 → P2。修复：AC/写集补 `tests/agent_bridge_protocol_test.rs`。
3. **M2-05 写集缺 `src/utils/error.rs`，但 AC1 允许「新增」稳定码。** `:1730` 写「复用既有码**或**新增并登记 `docs/error-codes.md` + `COMPATIBILITY.md`」，走「新增」分支必须改 `src/utils/error.rs`（`compat_error_codes_doc_sync` 要求 variant↔文档成对），而 `:1762` 写集只有两份文档。同段 `Docs and compatibility impact`（`:1768`）点名 `memory.md`，写集也未列。修复：写集补 `src/utils/error.rs`（条件性）与 `docs/development/tracing/memory.md`，或把 AC1 收敛为「复用 `LBR-POLICY-001` 等既有码」（该 variant 实存于 `src/utils/error.rs:360`）。
4. **M2-16C VER（`:983`）的括注「（允許有命中；>1 由 shell 報錯）」与命令语义不符**：`rg -q` 在 ≥1 命中时返回 0，不存在「>1 报错」。修复：删掉该括注或改为「命中即通过；零命中 `exit 1`」。
5. **`EX-M2-05` waiver 行只论证了 `VER=10/8`，未提 `AC=24/8@EX-M2-05`。** 模板 `:526` 门族型 waiver 准入条件②要求门族清单在「判据规范」块逐条枚举；`:1742` 仅写「AC 判據共 24 條獨立謂詞，逐條見本卡 AC 清單」，指向 8 条合并式 checkbox。判定上仍满足模板 `:537` 的五项硬条件（豁免项=G-03、任务列=M2-05、在有效期内），故不判违规。修复：waiver「理由与补偿措施」补一句「AC 分子 24/8 同受本 waiver 覆盖」，并在判据规范块按谓词分组列出 24 条。
6. **M2-16A `Current evidence`（`:823`）仍用硬编码绝对路径 `git -C /run/media/genedna/data/libra-pr456 grep …`**，与 `DEP-M2-SRC-01` 统一的 `$SRC_CLONE` 写法不一致（值等价、可执行）。修复：改为 `git -C "$SRC_CLONE" grep …`。
7. **M2-04 的 “proposal” 类型归属未钉死。** `:1659` 说 `MemoryWriter::commit` 输入含 `proposal`，但 `EpisodeProposalV1` 按 §0.4 由 **M2-07** 的 `compiler/mod.rs` 首建，而 M2-07 在 DAG 上位于 M2-04 **之后**；M2-04 实际只能消费 M2-01 的 `EpisodePayloadV1`。按 G-05（卡可独立执行）应显式说明。修复：M2-04 AC1 改为「输入为 M2-01 冻结的已校验 `EpisodePayloadV1`（非 M2-07 的 `EpisodeProposalV1`）」。
8. **M2-16D1 入口命名在同一卡内混用**：Description（`:1091`）写「调用 `MemoryRuntime::observe_terminal`」，AC（`:1105`）写「經 `observe_terminal_observation` 薄封裝入隊」，VER（`:1108`）又写「只调用 `observe_terminal`」。语义可推断（`rg observe_terminal` 会同时命中薄封装），但建议统一为薄封装名。
9. **`memory.md:23` 仍把权威历史写成 `refs/libra/memory/*`**，与本计划固定的 `refs/heads/libra/memory/repo`（`:214`）冲突；M2-05 的 `Docs and compatibility impact` 只以散文提「`memory.md` 的 ref/revert/transport 边界」，无卡把该行列为 AC 判据。修复：在 M2-05 AC8 或 M2-01 AC1 内加一条「`memory.md` 全文的 `refs/libra/memory/*` 改为 `refs/heads/libra/memory/repo`」。
10. **`memory.md:604,618` 的 `action` 枚举含 `TaxonomyExpanded`/`Consolidated`**，而 M2-06 AC2（`:1810`）只列七态、非目标又排除 consolidation/taxonomy；M2-01 AC1 目前只处理 `memory_entity_index`/`memory_taxonomy_node` 两张表的引用。建议在 M2-01 AC1 一并声明这两个 action 的处置（删除或标注后续切片）。

---

**结论：** r11 的 6 条 P1 与全部 P2 均已实测关闭；25 张卡的 AC/VER/deps/写集/Granularity/审计表逐栏一致，DAG 无环，事实基线、版本面集合、CI job 集合、migration tip、错误码 owner 与 33 份文档存在性全部核对通过。唯一阻断项是 P1-1：M2-13 把一条对新命令实际空跑的守卫当成验收门，与计划 `:804` 自定的「不把零命中视为通过」规则直接冲突，且会留下永久回归缺口；最小修复是一行写集 + 一条 AC/VER。

VERDICT: FAIL
EXIT=0
