# plan-20260819 R30-r16 review — Claude
# reviewer: claude -p --effort xhigh --allowedTools Read Grep Glob (read-only, prompt via stdin)
# date: 2026-09-22T00:34:39+08:00
7867532ab6f98b8e1b5f780f4512bc02f8f8179692377e0c6043c13bdc93d912  docs/development/plan/plan-20260819.md
# review prompt: plan-20260819.review/review-prompt-r16.md

## plan-20260819 R30-r16 复审（Claude，只读）

**被审文件：** `docs/development/plan/plan-20260819.md`（2830 行，未提交工作树版本）
**实测 SHA-256：** `4840c651e4c6175b6e9d3dd40c1ab24e53dd58569869e7792bf55db0f4f01a17`
**对照：** `plan-template.md` v2.8（`0a3b7d85…`）、`plan-status.md`、仓库 `main` @ `45ad1f7f2`（crate `0.23.36`）
**只读声明：** 本轮未修改仓库任何文件（全部为 `Read`/`Grep`/`grep`/`sed`/`ls`/`stat`/`sha256sum`）。

---

## 一、r15 findings 逐条核对（实际命令与结果）

| r15 finding | 核对方式与结果 | 结论 |
|---|---|---|
| Codex P1-1 终态 observation 无可恢复持久化 | `:1393` 新增 `memory_terminal_observation`（M2-02 schema）、`:2008` M2-08 按 generation 重放 | ⚠️ **未闭环**（P1-1：无表职责合同行、无 retention 数值、无 access owner、与本卡 Security 条款互斥） |
| Codex P1-2 / Claude P1-2 identity 通道 | `:515` envelope 增 `compiler_identity`（认证 ingress/registry 填充、自填拒绝、CLI session 绑定）；`:974` M2-16C 填充；`:2008` registry 登记；`:2350` CLI 不得推导 | ✅ 关闭 |
| Codex P1-3 bridge 旧 host 兼容 | `:974` minor/capability 协商（旧 peer 20、新 peer 22）；`:987` `protocol_v1_minor_negotiation_matrix`；`:977`/`:1007` 20→22 守卫入 AC/写集 | ⚠️ **部分关闭**（P1-3：EX-M2-06 门族枚举缺 3 门，且 `protocol_v1_allowlist_is_exactly_22_methods` 无任何 VER 会执行） |
| Codex P1-4 / Claude P1-3 `job_sql` 交接 | `:1395` M2-02 AC5 改「本卡只定义 schema」；`:1431` Files 已删 job SQL owner；`:2028` M2-08 首建；`:2032`/`:2054` 均为 11 | ✅ 关闭 |
| Codex P1-5 M2-05 `src/internal/mod.rs` | `:1803` 写集含 `mod.rs` 註冊 `pub mod ref_policy;`；`:1807` Files=17；`:1829`/审计表 `:791` `prod-files=17`；`:777` EX-M2-03 = 17 档。实测 `grep -n ref_policy src/internal/mod.rs` 零命中（确需本卡新增） | ✅ 关闭 |
| Codex P1-6 `command_test` 源档 | `:1343` M2-01K 含 `tests/command/config_test.rs`（实测 `tests/command/mod.rs:650 mod config_test;` 存在）；`:2380` M2-13 含 `tests/command/{memory_test.rs,mod.rs}` | ✅ 关闭 |
| Claude P1-1 `CompilerIdentityV1` 归属冲突 | `:1263` M2-01 AC4 延后子句已含四型+`trust.rs` 建 identity；`:1925` M2-07 AC8 五型；`:1929` T1「五型」；`:1957` 写集含 `trust.rs` | ✅ 关闭 |
| Claude P1-4 M2-13 判据规范首行 11/41 | `:2356` **仍写「10 條 VER + 40 條 AC」**；同批 `:1780` M2-05 首行被改成「11 條 VER + 41 條 AC」（该卡实为 10/25） | ❌ **未关闭且改错卡**（P1-2） |
| Claude P2-1 M2-10/11 Files vs prod | `:2166` 5 / `prod=5`✓；`:2234` 7 / `prod=7`✓ | ✅ 关闭 |
| Codex P2-1 M2-08 prod 12→11 | `:2032`「11 个生产文件以内」、`:2054`/审计表 `:794` `3/11`；写集机械计数 = 7+4 = 11 | ✅ 关闭 |
| Codex P2-2 M2-13 prod 11→5 | `:2406`/审计表 `:799` `4/5`；写集生产文件 = `cli.rs`+`command/{memory,mod}.rs`+`utils/{error,output}.rs` = 5 ✓；但 `:2384` Files 仍写「11 个生产文件以内」 | ⚠️ 部分（P2-5） |
| Claude P2-2 修订历史重复行 | `:450`、`:451` **两行同为 R30-r15** | ❌ 同型第 4 次回归（P2-1） |
| Claude P2-3 / Codex P2-3 review log 轮次笔误 | `:2787`/`:2788` 写「R30-r15/**r19** 修订后」——计划不存在 r19 | ❌ 同型第 3 次回归（P2-2） |
| Claude P2-4 M2-16C AC3 句子破碎 | `:974` 已重写为完整主干 + M2-08/M2-07 边界句 | ✅ 关闭 |
| Claude P2-5 M2-02R prod 8→7 | `:1613`/审计表 `:788` `3/7`；写集生产文件 = 2 SQL + `migration.rs` + `context/{receipt,receipt_store,mod}.rs` + `memory/mod.rs` = 7 ✓ | ✅ 关闭 |

## 二、本轮全量机械核对（实测通过项）

| 项 | 命令 / 位置 | 结果 |
|---|---|---|
| 事实基线锚点 | `ls src/internal/ai/memory`→不存在；`sed -n '53,68p' src/internal/ai/mod.rs`；`wc -l src/internal/model/mod.rs`=48；`src/command/op.rs:1144`=`!name.starts_with("libra/")`；`client_storage.rs:2023`=`pub fn put(`；`history.rs:1026`=`new_with_ref`、`:2227`=companion-write 注释；`src/command/memory.rs` 不存在 | ✅ 全部成立 |
| migration tip | `ls sql/migrations/` → `2026091901_operation_boundary_claim_columns.sql` 为最新 | ✅（`2026092101+` 重编正确） |
| 版本面集合 | `tests/compat/version_surface_sync.rs:12-13` 明写三处；`Cargo.toml:3 = 0.23.36` | ✅ 与模板 v2.8 一致 |
| CI job 集合 | `base.yml` 7 job（`format/clippy/owner-liveness-macos/redundancy/test/network-remotes/opencode-export-linux`）；`release.yml` 5 job（`build-and-upload/upload-install-scripts/update-homebrew-tap/verify-homebrew-formula/request-stable-manifest`）；`codeql.yml:21 analyze` | ✅ 与 DEP-M2-CI-02/03 完全一致 |
| 既有守卫 | `protocol.rs:62 METHOD_ALLOWLIST`、`:532 allowlist_has_exactly_20_methods`、`agent_bridge_protocol_test.rs:15 protocol_v1_allowlist_is_exactly_20_methods`、`help_examples_banner.rs:53 VISIBLE_COMMANDS` | ✅ 全部存在 |
| M2-05 的 33 份文档 + 11 个 callsite | `ls src/command/{branch…symbolic_ref}.rs` 全部存在；`src/internal/branch.rs` 存在 | ✅ |
| 25 卡 AC/VER 实计 | 逐卡按区间数 `- [ ]`：16A 8/4、16B 7/5、16C 8/9、16D 6/4、16D1 8/6、16E 8/4、01 8/4、01K 8/5、02 9/8、02F 9/8、02R 9/8、03 7/4、04 8/7、05 8ck(25 谓词)/10、06 8/5、07 9/10、08 8/8、09 8/5、10 7/4、11 8/5、12 8/5、13 9ck(41 谓词)/11、14 8/5、14C 8/6、15 9/2 | ✅ 与 Granularity 分子一致 |
| 审计表 ↔ Granularity | 25 行 type/AC/VER/landing/prod-files/scope/deps/writeset/release/split-from/exception 逐栏比对 | ✅ 全部一致 |
| 审计表末行段落 | `sed -n '805,812p' … \| cat -A`：`808`=M2-15 行、`809`=`$`、`810`=`**Verification 新增标记：**` | ✅ 空行仍在 |
| DAG ↔ `Dependencies` | 28 条边与 25 卡逐条比对；无环；`M207→M216B→M208`、`M216D→M216E`、D1 条件边均在 | ✅ |
| `Full-suite trigger` | 25 卡各一行；三张 migration 卡 `T-1/T-3/T-4`；M2-15 `T-2`；no-release 卡 `N/A`；M2-13 `T-1/T-4`（写 `tests/command/mod.rs`）判定正确 | ✅ |
| 零残留 | `test-provider`、`code_ui_scenarios`、`--test-threads`、`<source clone>`、`<libra-pr507>`、`batch-release child` 在活动行零命中；裸 `git` 仅出现在 `$SRC_CLONE` 作用域与历史行 | ✅ |
| `Dependencies` 前空行 | `grep -n -B1 '^\*\*Dependencies:'` → 24 处，全部前有空行 | ⚠️ 但只有 **24** 处（见 P1-4） |
| 模板 v2.8 自洽 | `:347-353` ER-14 范围、`:339`/`:703` 双 env、`:228-233` `web`/`worker`/`sql/publish` 注记 | ✅ |
| plan-status | `:46`/`:126-148`/`:201`/`:262` 已含 r15 双 FAIL、r16 待跑、`DEP-M2-DSH-01` 非阻塞、`DEP-M2-SRC-01`、`DEFER-M2-01..09` | ✅ |

**沙箱限制（非计划缺陷）：** `$SRC_CLONE`（`/run/media/genedna/data/libra-pr456`）与 `../libra-backend` 在本会话不可读，merge-base `2ebd409…`、158 路径清单、后端 `*.en.md` 未能复核；计划已把 pin 校验写成 M2-16E 卡内先决命令（`:1192`、`:1205`），fail loud，设计上可接受。

---

## P0（0 条）

无。

---

## P1（4 条）

### P1-1｜`memory_terminal_observation` 只有一句 AC，没有字段合同、没有 retention 数值、没有 access owner，且与 M2-02 自身的 Security 条款、GC-M2-08、M2-01K AC6 直接互斥

- **位置：** `plan-20260819.md:1393`（M2-02 AC3）、`:2008`（M2-08 AC5）；对照 `:229-333`（「SQLite ER 图」+「SQLite 表职责」）、`:1395`（M2-02 AC5 的 owner 分配）、`:1439`（M2-02 Security and privacy）、`:635`（GC-M2-08）、`:1327`（M2-01K AC6）、`:2080`（M2-09 AC6）、`:515`（`MemoryRecordEnvelopeV1` 字段）。核对命令：`grep -n "memory_terminal_observation" docs/development/plan/plan-20260819.md` → 仅 `:450`（修订历史）、`:1393`、`:2008` 三处。
- **问题：** r15 为关闭「崩溃后无法重建受信任输入」新增了该表，但只写进两条 AC，四件事全缺：
  1. **无字段合同。** `:231` 明写「完整列合同在图后的表格和 `memory.md`」，而 `:317-331` 的表职责表没有它的行（`memory_compile_job`、`memory_compile_observer_state`、`context_selection_receipt(_retention)` 都有）。M2-02 AC1（`:1391`）的判据是「字段与本文 ER/表职责一致」——对这张表无从判定；M2-01（契约冻结卡、DAG 上游）也没有承接它。
  2. **无 retention 数值。** `:1393` 只写「retention 有界」。对照 receipt 账本有「30 天 + 10,000 行」+ `receipt_retention_20000_rows`（`:1552`/`:1563`）。本表无上限、无裁剪机制、无任何具名回归（`grep` 证明零 VER 引用它）。
  3. **无 access owner。** M2-02 AC5（`:1395`）把 raw-SQL owner 限定为「job/observer/**receipt**」→ M2-08 `job_sql.rs` / M2-02R `receipt_store.rs`；本表既不是「可重建投影表」（不建 SeaORM entity）也不在这三类里，实施者无从知道由谁提供读写模块。
  4. **与三条既有约束互斥（实质冲突，非措辞问题）。** 表内容是「已脱敏、**可重放**的 `MemoryRecordEnvelopeV1` 快照」，而 envelope（`:515`）含 `proposal: Option<EpisodeProposalV1>`（模型候选正文）与 `observation.principal`。但 `:1439` 明写「job/observer **禁止** transcript/body/provider response；只保存 opaque/HMAC identity、对象/source OID、水位、稳定 reason/error code 与 lease 元数据」；`:635` GC-M2-08 明写「job 内只持久化稳定 code 和脱敏摘要，**不存模型/provider 原始响应**」；`:1327` M2-01K AC6 明写「可逆 principal/query **不进入** SQLite 明文」。
- **为何重要：** ① 这是 r15 Codex P1-1 的唯一补救物，缺 retention 上限即把「有界本地运行状态」变成无界增长的本地库（违反 ADR-M2-03「投影必须可重建、job/receipt 有界」与 GC-M2-05）；② 存 `proposal` 正文 + 可逆 principal 是**新增的本地隐私面**，与完成判据 `:2825`「secret/PII probe 在 …job… 中零泄漏」直接冲突，且没有任何 probe 覆盖它；③ 无 owner + 无字段合同 → M2-02 带着一条本卡不可判定的 AC（违反 G-05，不可豁免），M2-08 的重放 AC 也没有可核对的输入契约。
- **最小修复：** 在 `:317-331` 表职责表补一行 `memory_terminal_observation | 有界本地运行状态 | (scope_key, root_kind, root_id, generation) | 保存已脱敏 envelope 快照 + keyed digest；默认保留 N 天/每仓 M 行，按 generation 裁剪`，并在 `:1393` 写死该 N/M；同批明确 ① 快照**只保存**通过 M2-07 脱敏且 principal 以 `RepositoryKeyedDigest` HMAC 化后的规范化 envelope（若保留 `proposal` 必须在 `:1439`/`:635` 同批写明「仅限已通过 trust gate 的脱敏 proposal，且不保存模型原始响应」，否则改为只存 `proposal` 的 keyed digest + 对象引用）；② access owner 归 M2-08 `memory/job_sql.rs`（同步 `:1395` 的三类表述）；③ M2-02 VER 增一条具名 retention/脱敏回归（如 `memory_episode_test::terminal_observation_snapshot_retention_and_redaction`），M2-08 VER 的 `job_crash_recovery_matrix`（`:2020`）在判据中点名「从 `memory_terminal_observation` 重放」，并覆盖 CLI stdin 补录与 status/evidence/关系-only 变化后的重启。

### P1-2｜M2-05 与 M2-13 的「判据规范」首行门数互相错位：r15 的修订改到了错误的卡，两处首行现在都与自身枚举、waiver、AC/VER 分子、审计表冲突

- **位置：** `plan-20260819.md:1780`（M2-05 判据规范首行）、`:1781-1782`（实际枚举）、`:1829`（Granularity）、`:775`（EX-M2-05 waiver）、审计表 `:791`；`:2356`（M2-13 判据规范首行）、`:2357-2358`（实际枚举）、`:2406`（Granularity）、`:778`（EX-M2-02 waiver）、审计表 `:799`。模板 `plan-template.md:526` 门族 waiver 准入条件 ②③④、`:537` 审计表判定规则。
- **问题：**
  1. **M2-05** `:1780` 写「本卡獨立門族 = **11 條 VER + 41 條 AC** 謂詞」，但 `:1781` 实际枚举 V1、V2/V3、V4–V7、V8、V9、V10 = **10 條 VER**，`:1782` 的 AC 分组 `4+8+3+1+3+2+1+3` 且行尾自写「＝**25**」。waiver `:775`、Granularity `:1829`、审计表 `:791` 三处都是 `AC=25/8@EX-M2-05`、`VER=10/8@EX-M2-05`。首行的 11/41 与其余五处全部不符。
  2. **M2-13** `:2356` 仍写「**10 條 VER + 40 條 AC**」（r15 Claude P1-4 点名要改成 11/41 未改），但 `:2357` 枚举 V1、V1b、V2..V10 = **11**，`:2358` 分组 `4+3+2+2+7+8+1+12+2` 行尾自写「= **41**」；waiver `:778`、Granularity `:2406`、审计表 `:799` 都是 41/11。
  显然是把 M2-13 应得的「11 VER + 41 AC」写进了 M2-05 的首行，两卡同时错。
- **为何重要：** 「判据规范」块是 `EX-M2-05` / `EX-M2-02` 越过 G-03 上限的**唯一合法补偿产物**。模板 `:526` 准入条件 ③ 要求「分子如实写作 `n/上限@EX-ID`」、④ 要求「门族增减时同批更新 waiver 行与粒度审计表」，`:537` 的判定规则是「任一列不达标即不得开工」。首行与块内枚举自相矛盾时，复核者无法判定该 waiver 是否仍在授权范围内——这与 r14（EX-M2-05 24 vs 25）、r15（EX-M2-02 10/40 vs 11/41）是同一条款、同一形态的第三次出现。
- **最小修复：** `:1780` 改为「本卡獨立門族 = **10 條 VER + 25 條 AC** 謂詞（AC 分組）：」；`:2356` 改为「本卡獨立門族 = **11 條 VER + 41 條 AC** 謂詞（括號為分組大小）：」。两处只改首行，其余五处已正确。

### P1-3｜`EX-M2-06` 的判据规范块只枚举 7 条，覆盖不到 M2-16C 的 9 条 VER 门；其中 B6 命名的 22-method exact-allowlist 守卫没有任何 VER 命令会执行它

- **位置：** `plan-20260819.md:988-995`（EX-M2-06 判据规范 B1–B7）、`:983-1001`（9 条 VER）、`:977`（AC6 要求两处守卫 20→22）、`:1007`（写集）、`:773`（EX-M2-06 waiver）、`:1033`（`VER=9/8@EX-M2-06`）；模板 `plan-template.md:526`（准入条件 ①「每个门都是可复制执行的具名命令」、②「门族清单在卡内判据规范块**逐条**枚举」）、`:497`（「幽灵验收」反模式）。实测基线：`tests/agent_bridge_protocol_test.rs:15 protocol_v1_allowlist_is_exactly_20_methods`、`src/internal/ai/agent_bridge/protocol.rs:532 allowlist_has_exactly_20_methods`。
- **问题：** 9 条 VER 与 B1–B7 的映射如下——
  - `:983` `--lib internal::ai::agent_bridge` → **无对应 B 条目**
  - `:984`→B1、`:985`→B2、`:986`→B3、`:987`→B5、`:997`→B4
  - `:998` `memory_episode_test bridge_observation_idempotency` → **无对应 B 条目**
  - `:999` `rg -q "memory.recall|memory.episode.record" docs/` → 勉强对应 B7（B7 还额外承诺 `docs/error-codes.md` 与 `agent.en.md` 文档门，而该 `rg` 只扫 `docs/`）
  - `:1001` ref 写入零命中守卫 → **无对应 B 条目**
  - **B6「20→22 守衛：`allowlist_has_exactly_20_methods` 與 `tests/agent_bridge_protocol_test.rs::protocol_v1_allowlist_is_exactly_20_methods`（更新為 22）」→ 没有任何 VER 命令会跑第二个守卫**：唯一触及该 target 的 `:987` 带过滤器 `protocol_v1_minor_negotiation_matrix`，不会命中 `protocol_v1_allowlist_is_exactly_22_methods`。
- **为何重要：** ① 模板 `:526` 准入条件 ①② 不满足 → `EX-M2-06` 不成立 → M2-16C 的 `VER=9/8@EX-M2-06` 按 `:537` 判为「粒度不达标，不得开工」；② 实质后果更重：`allowlist_has_exactly_20_methods` 与 `protocol_v1_allowlist_is_exactly_20_methods` 正是 r15 Codex P1-3 要求的「exact allowlist 斷言」，把 allowlist 从 20 改到 22 却不跑其中一条守卫，等于把整条旧/新 host 协商的回归缩水一半（`:694` 明写 `DEP-M2-DSH-01` 非阻塞**不免除** in-repo 门）。
- **最小修复：** 把 B 列表补成 9 条与 VER 一一对应：新增 `B0 bridge 单元面：cargo test --lib internal::ai::agent_bridge`（含 `allowlist_has_exactly_22_methods`）、`B8 幂等：memory_episode_test::bridge_observation_idempotency`、`B9 无直写 ref 守卫：:1001 的 rg 模板`；并把 B6 的第二个守卫变成可执行门——在 `:987` 之外增一条 `cargo test --test agent_bridge_protocol_test protocol_v1_allowlist_is_exactly_22_methods`（VER 随之为 10，同批更新 `:773`、`:1033`、审计表 `:804` 为 `10/8@EX-M2-06`），或把 `:987` 的过滤器放宽为 `protocol_v1_` 前缀并在 B5/B6 注明覆盖两条。

### P1-4｜M2-16C 缺失 `**Dependencies:**` 字段标签，依赖列表被并入上一条 Verification 列表项；同卡「判据规范」块也插在 Verification 列表中间且前无空行

- **位置：** `plan-20260819.md:1003`（应为 `**Dependencies:**` 的行）、`:988`（判据规范块插入点）；对照模板 `plan-template.md:594`（卡片 `Dependencies` 字段）、`:450`（G-05 卡必须自洽可执行）、`:451`（G-06「各卡 `Dependencies` 必须与实施顺序一致」）。核对命令与结果：
  - `grep -n '^\*\*Dependencies:' … | wc -l` → **24**，而 `grep -c '^### Task M2-' …` → **25**；
  - `sed -n '1003p' … | cat -A` → 行首为单个空格 + `M2-12（reader/assembler/receipt 交付面）、M2-16B…`，无 `**Dependencies:**`；
  - `sed -n '987,988p' … | cat -A` → `:987` 是 `- [ ] …minor_negotiation_matrix` 列表项，`:988` 紧接 `**判据规范（非计数正文，EX-M2-06）：**`，**中间无空行**。
- **问题：** 25 张卡里唯独 M2-16C 没有 `Dependencies` 字段。`:1003` 以单空格缩进紧跟在 `:1001` 的 VER 列表项之后，Markdown 会把它渲染成该 checkbox 的延续文本；`:988` 同理会被吸收成 `:987` 的延续。结果是：这张卡的依赖（M2-12 / M2-16B / M2-08 / DEP-M2-SRC-01 / DEP-M2-DSH-01）在渲染态与解析态都不再是一个可提取的字段，而 `EX-M2-06` 判据规范块也被塞进了 Verification 清单内部（其余五张带判据规范的卡 `:1401`、`:1479`、`:1558`、`:1780`、`:2356` 都是独立段落）。
- **为何重要：** ① `Dependencies` 是 G-06 的机器可核对面，也是审计表 `deps` 列（`:804` = `M2-12,M2-16B,M2-08,DEP-M2-SRC-01,DEP-M2-DSH-01（非阻塞）`）与实施顺序 DAG 的比对对象；字段消失后这条比对链断掉，且违反 G-05「不读其它卡正文即可执行」；② 这是 r15 修订（插入 EX-M2-06 判据规范块）引入的确定性回归，与 r09–r15 反复收敛的「审计表末行断行」属同一类渲染破坏——那一条被判 P1，口径应一致。
- **最小修复：** `:1003` 行首改为 `**Dependencies:** M2-12（…）、M2-16B（…）、M2-08（…）、DEP-M2-SRC-01（…）；DEP-M2-DSH-01 为**非阻塞** post-M2 互操作验证。`（去掉前导空格）；并把 `:988-995` 的判据规范块整体移到 `:1001` 之后、`:1003` 之前，块前后各留一个空行（与其余五卡一致）。

---

## P2（5 条，不阻塞）

1. **修订历史第四次出现同轮次重复行。** `:450`、`:451` 两行都是 `R30-r15`，内容重叠（都记 `job_sql` 交接、identity、判据分子）。r11 合并 r10、r13 合并 r12、r15 合并 r14，本轮又裂成两行。**修复：** 合并为一行 `R30-r15（Claude r15 + Codex r15）`。

2. **review log 轮次笔误第三次同型回归。** `:2787`、`:2788` 的 Scope 写「R30-r15/**r19** 修订后的完整计划」——计划不存在 r19 轮次（r14 写过 r16/r17，r15 写过 r16/r17）。**修复：** 统一写「R30-r15 修订后的完整计划」，并在本行填入实测 SHA-256 `4840c651…`。

3. **归档的 r16 校验和与被审文件不符。** `cat plan-20260819.review/r30-r16-sha256.txt` 记 `7867532a…`，实测 `sha256sum` 为 `4840c651…`；`stat` 显示计划 mtime `00:42:00` 晚于校验和文件 `00:34:30`。计划自订的 review 协议（`:2750`）要求 log 记录被审文件 SHA-256，归档快照与实际文件不一致会让本轮结论无法绑定版本。**修复：** 在生成 r16 日志前重算并覆写 `r30-r16-sha256.txt`。

4. **两处判据规范块的具名测试与实际 VER 命令名不一致。** `:1410`（M2-02 VER 门）写 `memory_episode_test observer_job_schema_transaction`，而 `:1421` 的实际命令是 `observer_job_schema_migration_transaction`；`:2357`（M2-13 V1b）写 `command_test::memory_record_rejects_forged_identity`，而 `:2372` 的实际命令是 `memory_record_rejects_forged_identity_and_binds_session_identity`。门族 waiver 的补偿要求是「逐条枚举**具名命令**」，名字对不上即无法复核。**修复：** 两处按 Verification 的实际名改写。

5. **两卡 `Files likely touched` / `prod-files` 与写集机械计数不一致。** `:2384` M2-13 写「11 个生产文件以内」而 `prod-files=5`（写集生产文件确为 5）；M2-12 写集生产文件按 `:2299` 机械计数为 8（`context/{memory,receipt,mod}.rs` 3 + `memory/{reader,view,delivery,mod}.rs` 4 + `agent_bridge/protocol.rs` 1），而 `:2325`/审计表 `:798` 写 `prod-files=9`、`:2303` 写「9 个生产文件以内」。虽仍在 `M`（≤12）内、不改变验收结论，但 G-04 的规模输入应可由写集机械复核。**修复：** M2-13 的 Files 改「5 个生产文件」；M2-12 的 `prod-files` 改 8 或在写集补出第 9 个生产文件。

---

**结论：** r15 的 10 条 findings 中，identity 通道、`job_sql` 交接、`CompilerIdentityV1` 归属、M2-05 `src/internal/mod.rs`、两卡 `command_test` 源档、M2-08/M2-02R/M2-10/M2-11 计数、M2-16C AC3 措辞均已实测关闭；但终态观测持久化（P1-1）只补了一句 AC、缺合同/retention/owner 并与三条既有隐私约束互斥，M2-13 判据首行未改且把修订误施于 M2-05（P1-2），新增的 `EX-M2-06` 门族枚举不完整且遗漏一条不可执行的守卫（P1-3），插入该块时破坏了 M2-16C 的 `Dependencies` 字段（P1-4）。事实基线锚点、版本面集合、CI job 集合、migration tip、25 卡 AC/VER/deps/DAG/审计表逐栏一致性、审计表末行段落本轮全部实测成立。

VERDICT: FAIL
EXIT=0
