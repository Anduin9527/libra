# plan-20260819 R30-r05 review — Claude
# reviewer: claude -p --effort xhigh --allowedTools Read Grep Glob (read-only, prompt via stdin)
# date: 2026-09-21T14:04:01+08:00
764747f552be39fa6eb574208b872dd2805051d404288e3d63829775bdc899dd  docs/development/plan/plan-20260819.md
# review prompt: plan-20260819.review/review-prompt-r05.md

# R30-r05 计划评审（Claude，只读）

**被审对象**（核对日 2026-09-21，worktree `/run/media/genedna/data/libra-main`，`git log -1` = `45ad1f7f2`）

| 文件 | SHA-256 |
|---|---|
| `docs/development/plan/plan-20260819.md` | `764747f552be39fa6eb574208b872dd2805051d404288e3d63829775bdc899dd` |
| `docs/development/plan/plan-template.md` | `ab2dac59b8bebe359f630e656a648ead4c16920a761a9a86fa5cf37c83b412ee` |
| `docs/development/plan/plan-status.md` | `33e9337a66016eac8cfcfc914cc8f36d35d46b8a4fa5bd6a9f516329452638b2` |

**排除项：** 未执行任何写操作；未访问 `../libra-backend`（沙箱拒绝，DEP 已登记）与 `/run/media/genedna/data/libra-pr456`（同上），#456 相关事实只按计划自述 + `gh` 证据链核对结构一致性。

---

## 一、r04 findings 关闭核对（逐条，附实际核对证据）

**Claude r04 P1**

| 条目 | 结论 | 证据 |
|---|---|---|
| P1-1 `related_run_ids` 指纹 AC/VER | **部分关闭** | ADR-M2-05:500、ADR-M2-07:521、GC-M2-02:614、M2-08 AC:1918、VER:1934（`observe_terminal_mismatch_and_relation_fingerprint_matrix`）均已落；**测试矩阵 Job/recovery 行（:2526）未补该用例**（见 P2-4） |
| P1-2 里程碑 M0 | 关闭 | :2571 M0 = M2-16A+M2-01+M2-01K，M2-16B 移入 M2（:2573） |

**Claude r04 P2（13 条）**：1 关闭(:2263)、2 关闭(:2531)、3 关闭(:2270)、4 关闭(:804 固定路径 `/run/media/genedna/data/libra-pr456` + `bc1be587e4`)、5 关闭(:498)、6 关闭(:753 Approver/Review round/证据齐全)、7 关闭(:1093 前缀正常)、8 关闭(M2-07:1869 `policy.rs`、M2-04:1659 `admission.rs`、M2-06:1799 `diagnostics.rs`)、9 关闭(template:338 双 env)、10 关闭(template:352 四空格)、11 **部分关闭**（plan-status:256 `DEFER-M2-01..09` ✓；:195 已同步 r04 ✓；但 :46 仍写「r04 待跑」→ P2-2）、12 关闭(§0.4:64 + ADR-M2-12:566 + M2-16B:866)、13 关闭(:685 指向 §0.5)。

**Codex r04 P1（6 条）**：关系字段完整指纹 ✓(:500/:521/:1918)、record mismatch/提案路径 ✓(:2261/:2270)、nextest filter 映射 ✓(§0.5:70 + 矩阵:2515 + template ER-14:346)、M2-16C 粒度拆分 ✓(:950)、M2-16B `tests/INDEX.md` ✓(:900)、plan-status 门控 ✓(:195)。

**事实基线抽检（r04 第 7 项）全部成立：** `src/internal/ai/mod.rs:50-68` 无 `memory`；`ls src/internal/ai/memory` 不存在；migration tip `2026091901`（`src/internal/db/migration.rs:1862`、`:2331 max_registered_version`）；版本面集合 = 3 处（`tests/compat/version_surface_sync.rs:13,23,45,66`，target `compat_version_surface_sync` 见 `Cargo.toml:260`）；`base.yml` jobs = `format/clippy/owner-liveness-macos/redundancy/test/network-remotes/opencode-export-linux`（:11/:32/:54/:77/:106/:312/:358，文件 422 行）与 DEP-M2-CI-02 一致；`codeql.yml:21 analyze` + `:42-48` language matrix ✓；`AgentRunStatus::{Completed,Failed}` 仅定义+单测（`rg` 只命中 `run.rs:99,100,117,118,142,143`）✓；`op.rs:1144 !name.starts_with("libra/")` ✓；`memory.md` 对 `EpisodePayloadV1|AutoEpisodeTrustGateV1|ContextSelectionReceiptV1` 零命中、`memory_entity_index|memory_taxonomy_node` 17 处 ✓；`bridge_event` 平面可用（`sql/migrations/2026081801_agent_bridge_capture.sql:41 payload_sha256`、`:45 UNIQUE(bridge_session_id,event_seq)`、`:16 CHECK(source='deepseek-harness')`）✓；M2-03 三个「既有」filter 真实存在（`src/internal/ai/history.rs:5889,5981,6075`）✓；`--test-threads` 全文零残留 ✓；M2-05 的 16 个命令文档全部存在 ✓；25 卡 / 24 收口卡计数与审计表、追溯表、DAG、Phase、里程碑一致，各卡 AC/VER 宣称值与实际条目数逐卡相符 ✓。

---

## P0

无。

---

## P1

### P1-1　`GC-M2-10` 的 `state.rs` 没有任何卡交付，M2-14C 的覆盖率门必然失败

- **位置：** `plan-20260819.md:622`（GC-M2-10 八文件清单）、`:2392-2393`（M2-14C AC1/AC2）、`:2533`（测试矩阵 Coverage 行）
- **问题：** GC-M2-10 把 100% line+region 口径固定为 `domain.rs / canonical.rs / validation.rs / **state.rs** / trust.rs / applicability.rs / selector.rs / job_state.rs`，M2-14C AC1 要求脚本「逐文件断言八个纯逻辑文件」、AC2 要求「目标文件缺失或零 instrumentation 必须失败」。但全文 `rg "state\.rs"` 只有三处命中：GC-M2-10 的清单（:622）、M2-06 写集的 `reader_state.rs`（:1799）、M2-08 写集的 `job_state.rs`（:1940）——**没有任何卡的 `Implementation write set` 或 §0.4「移植所有权」表（:64）创建 `src/internal/ai/memory/state.rs`**（§0.4 的 #456 文件清单里也无此文件）。
- **为何重要：** M2-14C 的验收门（`scripts/check-memory-coverage.sh`）按设计必须 fail-closed，于是该卡在计划内永远无法通过；「完成判据」第 7 条（:2709「核心纯逻辑 line+region coverage 100% 达到 M2-14C 门」）随之不可达。这不是风格问题，是一条被两处正文引用、却没有产出者的验收对象。
- **最小修复：** 二选一并同批同步 GC-M2-10、M2-14C AC、测试矩阵 Coverage 行：①把 note 生命周期状态机文件显式命名为 `state.rs` 并写入 M2-06（或 M2-16A/M2-01）的写集与 §0.4 所有权表；②把清单中的 `state.rs` 换成实际承载该纯逻辑的文件（如 `reader_state.rs`，或并入 `domain.rs`），并保持「八个文件」计数一致。

### P1-2　`libra cloud sync` 会把 Memory ref（及其对象）上传到远端，GC-M2-07 的 local-only 承诺没有任何卡覆盖

- **位置：** `plan-20260819.md:619`（GC-M2-07）、`:478`（ADR-M2-03「禁止普通远端传输」）、`:1707-1714`（M2-05 AC）、`:1731`（M2-05 写集）；仓库侧 `src/command/cloud.rs:2128-2144`、`:1001-1007`
- **问题：** 我实际核对了 `libra cloud` 的备份路径：`sync_metadata()` 对 `reference::Entity::find().all(db_conn)` **无任何过滤**地序列化整张 `reference` 表并 `put_metadata` 上传（`cloud.rs:2128-2144`）；对象同步按 `object_index` 的 `repo_id`（+`is_synced`）**整仓**取集合上传（`cloud.rs:1001-1007`）。`rg "libra/|is_locked_branch" src/command/cloud.rs` **零命中**——不存在 `libra/` 前缀豁免。而 ADR-M2-03 明确把 Memory 历史存成 `kind=Branch` 的 `libra/memory/repo` 行（:478），M2-04 一旦落地该 ref，任何一次 `libra cloud sync` 都会把 Memory 分支名 + ref OID（以及被登记进 `object_index` 的 Memory 对象）送进 D1/R2。M2-05 的 AC 只覆盖 branch 命令、operation snapshot/restore、prune/GC、`push --all/--mirror`、fetch/clone，写集里**没有** `src/command/cloud.rs`；风险表、GAP 表、非目标表也都未提及 cloud 备份面。
- **为何重要：** 这是计划自己写死的安全/隐私边界（GC-M2-07：「首个切片不把 Memory branch、Confidential 正文、job、receipt 或 input HMAC 进入 remote durable tier」）与仓库既有行为的**直接冲突**，且冲突在 M2-04 完成的那一刻就成立，不需要用户做任何异常操作。计划断言了一个它不交付也不验证的保证。
- **最小修复：** 把 `src/command/cloud.rs` 的 ref 选择（`sync_metadata`）与对象选择（`object_index` 查询）纳入 M2-05 的共享 `libra_owned_ref` classifier callsites：写集加 `src/command/cloud.rs`，AC 加一条「`libra cloud sync/restore` 不上传/不恢复 `refs/heads/libra/memory/*` 与其独占对象」，VER 加一条具名回归（如 `cargo test --test command_test cloud_sync_excludes_memory_ref`）；若判定此面延后，则必须改写 GC-M2-07/ADR-M2-03 的措辞为「cloud 备份面由 `DEFER-M2-1x` 承接」，并在 M2-04/M2-05 增加 fail-closed 行为（存在 Memory ref 时 `cloud sync` 明确拒绝或跳过）+ 风险表一行。

### P1-3　M2-16A（执行起点）的 AC 要求它向一个由后续卡首建、且不在其写集内的测试 target 增加用例

- **位置：** `plan-20260819.md:814`（M2-16A AC5）对比 `:832`（M2-16A 写集）、`:1305`（M2-01K 写集首建 `tests/memory_episode_test.rs`）、`:643-644`（DAG `M216A → M201 → M201K`）
- **问题：** AC5 写「`tests/memory_episode_test.rs` 的 target 由 M2-01K 首建**并已在** `tests/INDEX.md` 登记，**本卡只新增用例**并保持索引同步」。按 DAG，M2-16A 是全计划第一张卡，M2-01K 在其后两跳执行——核对日 `ls tests/memory_episode_test.rs` 不存在。因此这句话的前半段（「已登记」）在本卡执行时为假，后半段（「本卡只新增用例」）要求本卡修改一个既不存在、也**不在本卡 `Implementation write set`（:832）** 里的文件。
- **为何重要：** 违反 G-05（AC 必须自洽可判定、不读其它卡正文即可执行）与 ER-03（写集必须覆盖本卡实际改动）。执行代理若照 AC 执行就会越界建 target、踩掉 M2-01K 的首建所有权（这正是 r01–r04 反复收敛的所有权轴）；若照写集执行则该 AC 永远判不出 pass。第一张卡的验收门不可判定，会直接卡住开工。
- **最小修复：** 把 AC5 该句改为纯声明式的边界说明：「本卡不创建也不修改 `tests/memory_episode_test.rs`（该 target 由 M2-01K 首建并登记 `tests/INDEX.md`）；本卡的移植测试全部落在 `src/**` 的 `#[cfg(test)]`」，并据此确认 `tests/INDEX.md` 是否还需留在本卡写集（若本卡不新增/重命名 target，按模板「使用规则」第 43 条可移除）。

---

## P2

1. **模板 ER-04 第三门缺 `.env.live-test`。** `plan-template.md:198` 写「第三门 `source .env.test && cargo nextest run --all …`」，与 `:71`（术语「全量测试」）、`:265` C 组第 ④ 步、`:328` ER-13 触发、`:338` 收口门、`:702` 完成判据的双 env 口径冲突（r04 只补了 :338）。**修复：** :198 补 `source .env.live-test &&`。
2. **plan-status 自相矛盾。** `plan-status.md:46` 仍写「已按 `fix-checklist-r01..r03.md` 修訂，**r04 待跑**」，而同文件 `:195` 写「r01–r04 雙評審皆 `FAIL`；r05 待跑」。**修复：** :46 同步 r04 结果（Codex 0×P0/6×P1/3×P2、Claude 0×P0/2×P1/13×P2）与 `fix-checklist-r04.md`。
3. **doctest 在 ER-14 执行器下无等价映射。** `:70`/`:2515` 要求卡内 `cargo test` 必须以「等价 `cargo nextest run`（相同 lib/test filter）」重跑作为 C 组证据，但 M2-01 VER `:1238` 是 `cargo test --doc internal::ai::memory`，nextest 不支持 doctest。**修复：** §0.5 与测试矩阵前言补一句「doctest 无 nextest 等价，保留 `cargo test --doc` 作为 A 组门，不计入 ER-14 执行器唯一性」。
4. **测试矩阵 Job/recovery 行未补 r04 要求的用例（r04 P1-1 残留）。** `:2526` 的「必须覆盖」列与命令仍只到 `observer_task_revision_requeues_parent_intent`，未登记 `observe_terminal_mismatch_and_relation_fingerprint_matrix`（status/evidence mismatch + 仅关系字段变化→新 generation）。**修复：** 该行覆盖列加「terminal_status/evidence 一致性、关系字段指纹」，命令追加该 filter。
5. **M2-14 的 `Full-suite trigger` 与写集有条件冲突。** 写集 `:2350` 含「相关 Cargo dev tooling」，而 `:2366` 写 `none`；若真的新增 dev-dependency/`[[test]]`（`Cargo.toml` 非版本行）即客观命中 `T-1`，按 ER-13「客观命中却写 none 视为验收造假」。**修复：** 写集改为「不新增 Cargo 依赖（新 target 走 `tests/*.rs` 自动发现）」，或触发字段改为条件式并在 ER-03 复核时判定。
6. **`deepseek-harness` 守卫 target 名不精确。** `:501` 写「`source` CHECK 與 `agent_bridge_schema_test` 雙重守衛」；实际断言在 `tests/compat/agent_bridge_schema_test.rs:78-79`（target `compat_agent_bridge_schema`，`Cargo.toml:350`）；同名的 `tests/agent_bridge_schema_test.rs`（target `agent_bridge_schema_test`，`Cargo.toml:346`）对 `deepseek-harness` 零命中。**修复：** :501 改引 `compat_agent_bridge_schema`。
7. **`memory_compile_job` 的 terminal source OID 列必须可空。** `:326`/`:1355` 要求 job 行保存「最新 terminal source object OID」，但 ADR-M2-05（`:498`）允许 `bridge_event` 平面 `object_oid` 为空。**修复：** M2-02 AC3 注明该列 NULL 允许（并由 `terminal_evidence` locator+digest 承担唯一性），避免实现写成 `NOT NULL` 后 bridge 路径 fail-closed。
8. **VER 计数把全局门计了进去。** M2-16A `:824,:826`（`fmt && clippy`、`cargo build --release`）、M2-16B `:893`、M2-16C `:965`、M2-16D `:1032`、M2-16D1 `:1101` 都占了 `Verification` 条目，而 ER-04「门不计入条目上限」+ 模板 `:560` 明确这些不登记；M2-16C 因此宣称 8/8、按口径实为 7/8。**修复：** 从这几张卡的 `Verification` 删除 fmt/clippy/release-build 行，同步 `Granularity` 与审计表分子。
9. **M2-13 AC4 把两个命令压进一条判据。** `:2261` 一条 checkbox 同时约束 `memory rebuild --dry-run` 与 `memory record`（含 envelope 二选一、幂等、fail-closed、pending 语义），是 G-03「多轴伪装」的典型形态（其余卡也有，但此条最重）。**修复：** 拆成 `rebuild` 与 `record` 两条 AC，若因此超 8 条则按门族在 waiver 表追加 `EX-M2-02`（同样附「判据规范」块）。
10. **`TerminalObservationV1` / `TerminalEvidenceRefV1` 无首建归属，也未进 M2-01 的版本门 AC。** `:1227` 的版本门只列 `MemoryNoteV1/MemoryEventV1/EpisodePayloadV1/EvidenceRefV1/CompileRecordV1`；这两个类型却是 host 经 bridge 提交的对外 envelope（`:497-500`），且 M2-07（校验）与 M2-08（seam）都可能定义（`EpisodeProposalV1` 已在 r04 明确归 M2-07，这两个没有）。**修复：** §0.4 或 ADR-M2-05 追加一行「`TerminalObservationV1`/`TerminalEvidenceRefV1` 由 M2-07 `evidence.rs` 首建」，并在 M2-01 AC4 的版本门列表里并入这两个类型。
11. **审计表 `writeset` 列写法与卡内不一致。** 审计表 `:759-783` 统一写 `serialized`（无 ID），各卡 `Granularity` 写 `serialized-at-M2-XX`；模板 `:540` 要求 `no-overlap` 或「序列化于 ID」。**修复：** 审计表补 ID。
12. **M2-16D AC4 的零命中判据不是模板 exit-code 形式，且无 VER 兜底。** `:1023` 用裸 `rg -n "orchestrator" src/internal/ai/memory src/internal/ai/agent_run`，而本卡 VER（`:1029-1032`）没有对应守卫（M2-16D1 `:1093/:1102` 已是正确形式）。**修复：** 按模板退出码模板改写，或在 VER 增一行同形守卫。

---

**结论：** r04 的 15 条 findings 除「测试矩阵 Job/recovery 行」与「plan-status:46」两处残留外均已关闭，事实基线锚点全部在核对日 commit 上成立；但本轮新发现 3 条 P1（覆盖率门缺失交付物、local-only 承诺在 `libra cloud sync` 上有未覆盖泄漏面、首卡 AC 与写集/DAG 自相矛盾）。按「P0=0 且 P1=0」的通过口径，本版不通过。

VERDICT: FAIL
EXIT=0
