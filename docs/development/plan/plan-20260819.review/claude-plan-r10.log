# plan-20260819 R30-r10 review — Claude
# reviewer: claude -p --effort xhigh --allowedTools Read Grep Glob (read-only, prompt via stdin)
# date: 2026-09-21T16:05:21+08:00
08aca2bc71822bf497d901c73993943caaf3513bc3ba5c18f93cf768e9072e7c  docs/development/plan/plan-20260819.md
# review prompt: plan-20260819.review/review-prompt-r10.md

我已完整读完计划（2755 行）、模板 v2.8、plan-status，并在仓库上逐条核对了事实基线。先给 r09 findings 的关闭核对，再给本轮分组结论。

## r09 findings 关闭核对（实际执行的命令 / 行号）

| r09 finding | 结论 | 我的核对证据 |
|---|---|---|
| Codex-1 M2-16E merge-base 错 | **已关闭** | `git merge-base 45ad1f7f2 bc1be587e4` → `2ebd409045b5114707986b66a7a6b42b5a4a4b7e`；`git diff --name-only 2ebd409… bc1be587e4 \| wc -l` → `158`，与 plan:1171/1177/1184 一致；VER 用 disposition 列计数（不命中表头） |
| Codex-2 M2-04 local-only 写入 | **已关闭** | plan:1660 AC（不触发 remote tier `put`/repair marker/`object_index`）+ plan:1671 VER `writer_local_only_no_remote_put` |
| Codex-3 ER-06a 后端页面逐卡具名 | **未正确关闭**（见 P1-2） | plan:1265 / 919 / 1732 / 2324 四处；其中两处挂错卡 |
| Codex-4 release D 组合同 | **已关闭** | plan:688 `DEP-M2-CI-03` 五 job 与 `.github/workflows/release.yml` 实测一致（`build-and-upload:15`、`upload-install-scripts:310`、`update-homebrew-tap:480`、`verify-homebrew-formula:628`、`request-stable-manifest:690`）；§0.5 plan:69 与 M2-15 plan:2513 + 审计行均已引用 |
| Codex-5 / Claude-1 M2-05 exception 栏与 G-03 waiver | **已关闭** | waiver plan:766 为 8 列；plan:1734-1737 判据规范块；plan:1781 `VER=9/8@EX-M2-05`、`exception=EX-M2-03,EX-M2-05`；审计行 `cut -d'|' -f2,8,9,10,16` 输出一致 |
| Claude-2 审计表末行断行 | **未关闭（回归仍在）** | 见 P1-1 |

其余事实基线抽查全部成立：`src/internal/ai/` 无 `memory` 模块、`src/cli.rs` / `src/internal/model/mod.rs` 零 `memory` 命中、migration tip = `2026091901`、`METHOD_ALLOWLIST.len()==20` 与 `protocol.rs:532` 守卫、`base.yml` 7 job、CodeQL `analyze`、`compat_version_surface_sync` 权威三处（`Cargo.toml`/`install.sh`/`install.ps1`）、FTS5/bm25 零实现、`agent_bridge_event.payload_sha256` 与 `source CHECK='deepseek-harness'`、`AgentRunStatus::{Completed,Failed}` 仅定义+单测、`EpisodeCompiler|RepositoryKeyedDigest|llvm-cov` 零命中、M2-05 的 33 份命令文档与 11 个命令源文件均存在。

---

## P0（0 条）

无。

## P1（3 条）

**P1-1｜`plan-20260819.md:799`（任务卡粒度审计表末行）**
- **问题：** M2-15 数据行与 `**Verification 新增标记：**` 段落仍在同一行（`… | N/A |**Verification 新增标记：** M2-16A、…`）。`grep -n "Verification 新增标记"` 全文只命中 799 行，即粘连未修。r09 修订历史（plan:450）却自述"审计表末行断行（修复 r08 回归）"，自述与文件不符。
- **为何重要：** GFM 下该行会被解析成第 16 个单元格并被丢弃，整段 `(new)` 标记契约（覆盖 20+ 张卡的 Verification、零命中不得计为通过）在渲染后消失；同时机械解析审计表时最后一行多一列，ER-03 逐字段核对会失败。
- **最小修复：** 在 799 行 `| N/A |` 之后插入空行，使 `**Verification 新增标记：**` 成为独立段落。

**P1-2｜`plan-20260819.md:1265`（M2-01）与 `:919`（M2-16B）——ER-06a 后端页面绑错卡**
- **问题：** ①`config.en.md`（"Memory 保留键的拒绝/脱敏语义"）挂在 **M2-01**，但 M2-01 写集（plan:1259）只有 `memory/{mod,domain,canonical,validation}.rs` + `mod.rs` + 两份设计文档，完全不含 `src/command/config.rs` 或 `docs/commands/config.md`；真正改 `libra config set/unset/remove-section/rename-section/import` 行为的是 **M2-01K**（写集 plan:1322 含 `src/command/config.rs` 与 EN/zh/开发三份 config 文档，AC8 plan:1308），而 M2-01K 的 `Docs and compatibility impact`（plan:1328）写的是"**无公开命令或 wire schema**"，既与自身 AC/写集矛盾，也缺 ER-06a 判定。②`agent.en.md`（"bridge method allowlist/协议版本"）挂在 **M2-16B**，但 M2-16B 写集（plan:913）只有 `memory/model.rs`、`memory/mod.rs`、测试与 `memory.md`，不改任何 bridge 方法或协议版本；真正改协议与 `METHOD_ALLOWLIST` 的 **M2-16C**（plan:966/986）反而"后端网站文档按判定记录 N/A 理由"，且其 `Docs and compatibility impact`（plan:992）无 ER-06a 判定。（仓库已核实 `libra config` 五个子命令存在于 `src/command/config.rs:430-506`，`docs/commands/agent.md:89` 写明"Protocol v1: 20-method allowlist"，两处后端页面确有真实用户可见行为对应。）
- **为何重要：** 直接违反模板 ER-06a（template:288）"凡改变用户可见命令行为必须在**同一张任务卡**同步 EN/zh + 后端网站文档；每张卡必须在 `Docs and compatibility impact` 与 `Acceptance criteria` 给出判定或具名 N/A 理由"。按现状执行会出现：M2-01 在行为尚未实现时先发布 config 文档（M2-01K 在其之后），M2-01K 改了公开命令却自称无公开命令面；M2-16B 被要求改它不拥有的 agent 协议页，M2-16C 改了协议却记 N/A。这正是 r09 Codex-3 要求闭环的那一项。
- **最小修复：** 把 plan:1265 的 `config.en.md` 条目整体移到 M2-01K 的 `Docs and compatibility impact`，并把 M2-01K 的"无公开命令或 wire schema"改为"`libra config` 保留键行为变更 + EN/zh + `config.en.md`（`cf` 分支）"；把 plan:919 的 `agent.en.md` 条目移到 M2-16C 的 `Docs and compatibility impact`（替换其 N/A 判定），M2-16B 改为"ER-06a：N/A（不改任何用户可见命令表面）"。

**P1-3｜`plan-20260819.md:1925`（M2-07 Granularity）与 `:1012`（M2-16C Granularity）——机械字段与审计表冲突**
- **问题：** ①M2-07 卡内 `Granularity` 写 `AC=9/8@EX-M2-04; VER=9/8@EX-M2-04; …; exception=N/A`，但审计表第 784 行该卡 exception 列为 `EX-M2-04`（`sed -n '784p' … | tr '|' '\n'` 第 16 字段）。②M2-16C 卡内 `Granularity` 写 `landing=3; prod-files=4`，审计表第 795 行 landing/prod-files 列为 `4/4`（同法核对第 10 字段）。逐行比对 25 张卡，只有这两处不一致。
- **为何重要：** 模板 template:622-624 规定分子超限时 `exception` 必须逐个列出豁免该列的 `EX-ID`，写 `N/A` 即"审计判为不达标"；`landing`/`prod-files` 属 G-04 可计数字段，审计表是其计划级汇总，两值相左会让 ER-03 开工核对无法判定以哪一侧为准（这正是 r09 对 M2-05 exception 栏要求修的同类问题，M2-07/M2-16C 漏改）。
- **最小修复：** plan:1925 把 `exception=N/A` 改为 `exception=EX-M2-04`；plan:1012 与审计表 795 行取同一 landing 值（按写集 `agent_bridge/{protocol,ingress,memory}.rs` + `command/agent/bridge.rs` 判定后统一为 3 或 4）。

## P2（5 条）

- **P2-1｜`plan-20260819.md:1326`（M2-01K）：** `Files likely touched: 4 个生产文件以内` 与同卡 `prod-files=5`、审计表 `3/5` 不一致（写集实为 `keyed_digest.rs`/`ai/mod.rs`/`vault.rs`/`config.rs`/`command/config.rs` 共 5 个）。修：改成"5 个生产文件"。
- **P2-2｜`plan-20260819.md:59`（§0.4 融合表）：** `prompt::SystemPromptBuilder`（reader 测试）承接卡写 M2-16B，与 plan:64 的移植所有权（`reader/selector/...` → M2-11；`compiler/prompt.rs` 资产 → M2-09/10）及 M2-16B 写集（无任何 reader/prompt 文件）冲突。修：承接卡改为 M2-11（prompt fixture 由 M2-09 提供）。
- **P2-3｜`plan-20260819.md:688`（DEP-M2-CI-03）：** Owner 写"每张 independent 发版卡 / M2-15"，但 21 张独立发版卡的 `Dependencies` 均未登记该 DEP，只在 §0.5 正文说明。违反 G-05"不读其它卡正文即可执行"。修：在"任务卡字段默认值"中补一行"所有 `Release boundary=independent` 卡的 D 组默认继承 DEP-M2-CI-03"，或逐卡在 `Dependencies` 追加。
- **P2-4｜`plan-20260819.md:966`（M2-16C AC6）：** "`docs/commands/agent.md` EN + zh-CN（更新 20-method allowlist 契约与版本说明）"措辞仍指向 20，而同卡写集要求把守卫重命名为 22。修：改为"20→22 method allowlist"。
- **P2-5｜`plan-20260819.md:1184`（M2-16E VER）：** 命令内保留未替换占位符 `<source clone>`，而同族卡 M2-16A（plan:818）已用 `git -C /run/media/genedna/data/libra-pr456 …` 绝对路径。G-05 要求 Verification 为可直接复制执行的确切命令。修：统一用 DEP-M2-SRC-01 的默认绝对路径，或在 DEP 行定义 `SRC_CLONE` 变量并在命令里使用 `$SRC_CLONE`。

---

**结论：** P0=0，P1=3（审计表末行断行回归未修、ER-06a 后端页面绑错卡、M2-07/M2-16C 机械字段与审计表冲突），P2=5。不满足 `P0=0 且 P1=0` 的 PASS 条件。

VERDICT: FAIL
EXIT=0
