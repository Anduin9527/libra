# plan-20260819 R30-r27 review — Claude（評審期間凍結：plan sha256 13b83815…、template 69ce380a…）
# reviewer: claude -p --effort xhigh --allowedTools Read Grep Glob (read-only, prompt via stdin)
# date: 2026-09-22T03:07:15+08:00
13b83815c6ccb071c22acef28ec5cebe608523f0d315b2295aae0ef51089233d  docs/development/plan/plan-20260819.md
69ce380a9c7ecaa0e6776e53a24b11454cdc3c81514ccc62036c6ed54b0ab6b2  docs/development/plan/plan-template.md
# review prompt: plan-20260819.review/review-prompt-r27.md

# plan-20260819 R30-r27 review — Claude

**被審版本（凍結）：** `plan-20260819.md` sha256 `13b83815…`、`plan-template.md` sha256 `69ce380a…`、`plan-status.md` sha256 `ecd368f0…`（與 `r30-r27-sha256.txt` 一致，本輪期間未變）。
**倉庫：** `/run/media/genedna/data/libra-main`，HEAD `45ad1f7f2`，crate `0.23.36`。
**說明：** 本輪 sandbox 拒絕了對 `/run/media/genedna/data/libra-pr456` 的 `git` 調用（`git -C … rev-parse` requires approval），故 158-path/merge-base 只核對了計畫內部一致性與 `Glob` 可達性，未重跑來源命令。

---

## r26 findings 逐條核對

| r26 finding | 狀態 | 我實際核對的命令 / 行號 |
|---|---|---|
| Codex/Claude P1-1 `DEP-M2-SRC-01` `test -d` 恆假 | **已關閉** | `Grep 'is-inside-work-tree\|rev-parse --verify\|test -d "\$SRC_CLONE'` → 命中 `:453`、`:710`；`:710` 第 4 格已改「以 `rev-parse --is-inside-work-tree` 判定，接受 `.git` 檔案或目錄兩種形式」，第 7 格首條為 `git -C "$SRC_CLONE" rev-parse --is-inside-work-tree` 輸出 `true`；`test -d` 全文零命中 |
| Claude P1-2 158-path diff 範圍錯 | **已關閉** | `Grep 'diff --name-only'` → `:710`、`:1214`、`:1221` 三處統一為 `2ebd409045b5114707986b66a7a6b42b5a4a4b7e bc1be587e4` |
| Claude P1-3 M2-02F/M2-08 缺 `src/internal/model/mod.rs` | **已關閉** | `:1525` M2-02F 寫集含 `src/internal/model/mod.rs`（`pub mod` 註冊），`prod-files=9`（`:1551`）、審計 `4/9`（`:805`）；`:2052` M2-08 含之，`prod-files=12`（`:2078`）、審計 `3/12`（`:812`）；`:1529` Files 改「9 个生产/配置文件」 |
| Claude P1-4 M2-16C 缺 `agent_bridge/mod.rs` | **部分關閉** | `:1025` 寫集已含 `{…,mod.rs,authorization.rs}`、`prod-files=7`（`:1051`）、審計 `4/7`（`:822`）；但 `ActionClass` 語義未落地（見 P1-2） |
| P2-1 review log `r31` 筆誤 | **未關閉（變體復發）** | `:2837`/`:2838` 現寫「R30-r26/**r32** 修订后」 |
| P2-2 `authorization::classify` 默認拒絕 | **未關閉** | `Grep ActionClass plan-20260819.md` → 唯一命中 `:453`（修訂歷史自述）；AC3（`:990`）無 `ActionClass` |
| P2-3 M2-08 寫集「。、」殘留 | **已關閉** | `:2052` 改用「；」分隔，無「。、」 |
| P2-4 M2-05 V9 恆真存在性門 | **部分關閉** | `:1810` 已改內容性 `rg -q 'libra/memory'`，但命令是 `for f in $(...)` 占位符且 `:1798` 判據規範未同步（見 P1-3） |
| P2-5 M2-06 `（仅 query helper）` | **未關閉** | `sed -n '1884,1886p'` → `` `src/utils/error.rs`（`LBR-MEMORY-*` variant）（仅 query helper） `` |
| P2-6 M2-02F Files 計數 | **已關閉** | `:1529`「9 个生产/配置文件」 |
| P2-7 M2-03 補 `rg` 後門守衛 VER | **部分關閉（審計表寫錯欄）** | 卡內 `:1664` 已有守衛、`:1696` `VER=5/8`；審計表 `:807` 仍 `4/8` 且 landing/prod 欄被寫成 `5/8`（見 P1-1） |
| P2-8 `:2383` 非表格用 `\|` | **已關閉** | `:2374` 為未轉義 `<file\|->`… 實測為 `<file|->`；`Grep '\\\|'` 的全部 31 處命中均落在表格行或 `rg` 正則內（`:1214`/`:1221`） |
| P2-9 模板 `:262` 重複 `sql/**` | **已關閉** | `Grep 'sql/\*\*' plan-template.md` → `:262` 僅一次 |
| P2-10 VER 清單分段空行 | **已關閉** | 逐一讀取 M2-02R(`1590-1597`)、M2-05(`1802-1811`)、M2-16C(`998-1006`)、M2-16B(`930-934`)、M2-16D1(`1149-1154`)，均連續無空行 |
| Codex P2 模板 web/worker 註記 | **已關閉** | 模板 `:198`/`:339`/`:703` 均帶「0.23.x 已拆除，保留僅為歷史口徑」 |

**其他全量核對（通過）：** 審計表末行 `:826` 與 `:828` 之間確為物理空行（`sed -n '824,830p' | cat -A` 顯示獨立 `$`）；25 卡 AC/VER checkbox 實測數與 Granularity/審計表逐欄一致（僅 M2-03 例外，見 P1-1）；25 卡 `**Dependencies:**` 前均有空行（`Grep -B 1`）；DAG 無環且與各卡 `Dependencies`/審計表 deps 一致；`EX-M2-01/02/04/05/06/07/09` 的 waiver 行均 8 格、判據塊分子逐條可對賬（45=4+3+2+2+7+4+8+1+12+2；15=7+2+2+4；25=4+8+3+1+3+2+1+3；34=2+4+4+3+13+2+3+3；31=2+4+9+3+2+2+2+5+1+1；9/10；9/9）；事實基線復核通過（`src/internal/ai/mod.rs` 無 `memory`、`src/internal/model/mod.rs` 為 45 個扁平 `pub mod` 無 memory entity、migration tip `2026091901_operation_boundary_claim_columns`、`base.yml` job id 集合 `format/clippy/owner-liveness-macos/redundancy/test/network-remotes/opencode-export-linux`、`release.yml` 五 job、`Cargo.toml` `0.23.36`、`docs/benchmarks` 不存在、`tests/fts5_capability_test.rs` 不存在）；`plan-status.md` §3.3 含 r01–r26 且 r27 待跑。

---

## P0

無。

---

## P1

### P1-1　審計表 M2-03 行的 `VER` 與 `landing / prod-files` 兩欄同時與卡內不符（r26 P2-7 的修復貼錯欄）

- **位置：** `plan-20260819.md:807`（審計表）；對照 `:1696`（Granularity）、`:1674`（Files likely touched）、`:1660-1664`（5 條 VER）
- **問題：** 審計表寫 `… | 7/8 | 4/8 | 5/8 | M | …`，即 `VER=4/8`、`landing/prod-files=5/8`。卡內 Granularity 為 `AC=7/8; VER=5/8; landing=2; prod-files=3`，`Files likely touched` 為「上述 3 个生产文件」，Verification 實有 **5** 個 checkbox（含新補的 `rg` 後門守衛）。r26 的修復把新值 `5/8` 貼進了 **landing/prod-files** 欄，而 `VER` 欄仍是舊的 `4/8`。
- **我實際核對的命令與結果：**
  - `sed -n '800,808p' docs/development/plan/plan-20260819.md` → `| M2-03 | implementation | ref CAS primitive | revert | yes | yes | 7/8 | 4/8 | 5/8 | M | M2-02R | … |`（15 欄，欄數正確，值錯）
  - 卡內 `:1660-1664` 逐行計數 = 5 條 VER；`:1696` = `VER=5/8; landing=2; prod-files=3`；`:1674` =「上述 3 个生产文件和既有测试」
  - 其餘 24 行逐欄比對卡內 Granularity **全部一致**，M2-03 是唯一破損行
- **為何重要：** 審計表是模板 G-03（條目上限）/G-04（規模）/G-10（寫集與 prod-files）的機械對賬單，開工前 ER-03 要求「逐字段核對該卡的 `Granularity` 摘要行」。現在同一張卡在兩個權威位置給出三組互相矛盾的數字，且 `5/8` 會被讀成「8 個生產文件」（實際 3 個）——落在 `M` 的判定輸入上直接失真。修訂歷史 `:453` 又宣稱「M2-03 補 `rg` 後門守衛 VER（5/8）」已完成，執行者不會回頭查。
- **最小修復：** `:807` 改為 `| M2-03 | implementation | ref CAS primitive | revert | yes | yes | 7/8 | 5/8 | 2/3 | M | M2-02R | serialized-at-M2-02R | independent | N/A | N/A |`。

### P1-2　M2-16C 把 `authorization.rs` 納入寫集卻無任何 AC/VER 驅動，且 `classify()` 的 fail-closed 默認會讓 `memory.recall` 需要顯式 approval

- **位置：** `plan-20260819.md:1025`（寫集）、`:990`（AC3）、`:1019`（EX-M2-07 分組）、`:1029`（Files likely touched）、`:453`（修訂歷史自述）；`src/internal/ai/agent_bridge/authorization.rs:29-43,79-95`
- **問題：** ① 寫集加入了 `src/internal/ai/agent_bridge/authorization.rs`（並計入 `prod-files=7`、審計 `4/7`），但 AC1–AC8 與 EX-M2-07 的 31 條謂詞分組**沒有一條**說明該檔案要改什麼；`ActionClass` 在全計畫僅出現在修訂歷史自述行。② 倉庫事實：`classify(action)` 對 allowlist 之外的一切方法返回 `ActionClass::Dangerous`（fail-closed），`enforce()` 對 `Dangerous` 且無 `approved` approval 一律返回穩定 `denied`。新增的 `memory.recall` 與 `memory.episode.record` 若不進 `classify`，就會被歸為 `Dangerous`。
- **我實際核對的命令與結果：**
  - `Grep 'ActionClass' docs/development/plan/plan-20260819.md` → **唯一命中 `:453`**（修訂歷史「…與 `ActionClass` 語義」）
  - `Read src/internal/ai/agent_bridge/authorization.rs:1-95` → `:32-43` `pub fn classify` 的 `Normal` 分支只有 9 個既有方法（`checkpoint.create` … `diff.get`），`_ => ActionClass::Dangerous`；`:86-89` `enforce` 對 `Normal` 直接放行，`Dangerous` 無 approval 即 `denied`
  - `ls src/internal/ai/agent_bridge/` → `authorization.rs mod.rs protocol.rs …` 均**已存在**於 main
- **為何重要：** ① 違反模板 G-05「一張卡必須能在不閱讀其它卡正文的前提下被執行」——寫集列了一個生產檔案卻沒有任何驗收謂詞說明要改什麼，同時該檔案已計入 `prod-files`/審計 `4/7`，使 G-04/G-10 的計數失去依據。② 行為缺口是實質的：AC7「無憑據環境下 fake model + fake host 走通 **recall**/record 往返」與 ADR-M2-13「audited `memory.recall`」把 recall 定位為 host 常規只讀調用，但按當前 `classify` 它必須攜帶 `approval.decision="approved"` 才能執行，E2E 只能靠合成 approval 通過，與設計意圖相反；record 的授權等級也從未被計畫定案。③ `:453` 宣稱「與 `ActionClass` 語義」已落地，屬 G-09 修訂歷史失真，執行者會據此跳過。④ 附帶：`:1029` 的 `Files likely touched` 寫「agent_bridge mod/authorization **首建**」，與 `ls` 結果矛盾（兩者皆為既有檔案）。
- **最小修復：** 在 AC3（或新增一條 AC）明寫兩個新方法的 `ActionClass`（例如 `memory.recall` → `Normal`，`memory.episode.record` → `Dangerous`/approval-gated，並說明理由），把該謂詞計入 EX-M2-07 的 AC3 分組並在 B0 或 `agent_bridge_protocol_test` 加一條對應斷言；`:1029` 刪除「首建」改為「既有檔案，加 `pub mod memory;` 註冊與 `classify` 分支」；`:453` 改為如實記錄（寫集已補、`ActionClass` 語義待 r27 補）。

### P1-3　M2-05 的 V9 是字面未展開的占位符命令，且與 `EX-M2-05` 判據規範描述的不是同一道門

- **位置：** `plan-20260819.md:1810`（V9）、`:1798`（EX-M2-05 判據規範）；對照 `:791`（waiver 行）、`:1843`（Granularity `VER=10/8@EX-M2-05`）、`:809`（審計表）
- **問題：** V9 現為 `` `for f in $(...); do rg -q 'libra/memory' "$f" || exit 1; done` `` —— `$(...)` 是**字面占位符**（不是合法的命令替換內容），該門無法複製執行、也無法判定通過或失敗。同時 `:1798` 的判據規範仍把 V9 描述為「**33 份文檔存在性 for-loop**」，與實際的內容性守衛不是同一道門。
- **我實際核對的命令與結果：**
  - `sed -n '1808,1812p' docs/development/plan/plan-20260819.md` → V9 行逐字如上，`$(...)` 未展開
  - `:1798` 逐字：`V9 33 份文檔存在性 for-loop；V10 compat_serial_registry`
  - 對照：M2-05 的其餘 9 門（V1–V8、V10）都是可直接複製的 `cargo test …` 具名命令
- **為何重要：** `EX-M2-05` 是 G-03 的具名 waiver，其**補償條件**就是「M2-05『判據規範（非計數正文）』逐條枚舉具名命令，VER 分子如實寫 `10/8@EX-M2-05`」（`:791`）。一道無法執行的門使 10 條中的 1 條無法交付證據，waiver 的補償措施不成立；判據規範與 VER 清單描述分歧又讓 10/8 的分子無法逐門對賬。這正是 r10/r11 反覆收口的「每門可複製執行」規則。
- **最小修復：** 把 `$(...)` 展開為實際清單（例如 `for f in docs/commands/{branch,checkout,switch,reset,op,push,fetch,clone,cloud,update-ref,symbolic-ref}.md docs/commands/zh-CN/{…}.md docs/development/commands/{…}.md; do rg -q 'libra/memory' "$f" || { echo "FAIL: $f"; exit 1; }; done; echo OK`），並把 `:1798` 的 V9 描述同步改為「33 份文檔內容性守衛（每份須含 `libra/memory` 段）」。

---

## P2

1. **`:1885` M2-06 寫集殘留自相矛盾的括注**：`` `src/utils/error.rs`（`LBR-MEMORY-*` variant）（仅 query helper） ``。r26 P2-5 已提出、`:453` 宣稱已修，實測未刪。最小修復：刪除「（仅 query helper）」。
2. **`:2837`/`:2838` review log 的 R30-r27 兩行寫「R30-r26/**r32** 修订后的完整计划」**——`r32` 是筆誤（r26 的 `r31` 筆誤換個數字復發；r27 評審的對象是 r26 修訂版）。最小修復：改為「R30-r26 修訂後的完整計劃（r27 期間凍結）」。
3. **`:454` r25 修訂行仍宣稱 `DEP-M2-SRC-01` 當時已落地**（「改用 `rev-parse --is-inside-work-tree` + pinned `rev-parse --verify` … 並把 158-path 命令範圍改為 `2ebd409… bc1be587e4`」），而事實是到 r26 才落地（`:453` 已註「（第二次）實際落地」）。G-09 要求修訂歷史如實。最小修復：`:454` 刪除該子句或改為「（登記，未落地；見 r26 行）」。
4. **`:1029` M2-16C 的 `Files likely touched` 括注位置與事實**：「（agent_bridge mod/authorization 首建與 `pub mod memory;` 註冊） 上述路径。」括注放在「上述路径」之前且「首建」與倉庫事實不符（`ls src/internal/ai/agent_bridge/` 顯示兩者皆存在）。最小修復：改為「上述路径（`agent_bridge/mod.rs` 加 `pub mod memory;`、`authorization.rs` 加 `classify` 分支；兩者為既有檔案）」。
5. **模板 `plan-template.md:251` 仍以已拆除的表面作 ER-04 A 組示例**：「例如 `web-check` 的 `web/out` 漂移检查用 `libra status --porcelain=v1 web/out` 替代 `git status --porcelain -- web/out`」——`web-check` job 與 `web/` 目錄皆於 0.23.x 刪除（`base.yml` job 集合實測為 `format/clippy/owner-liveness-macos/redundancy/test/network-remotes/opencode-export-linux`），而模板其餘三處 `web/`/`worker/` 都已加歷史口徑註記，唯獨此處沒有；同句還保留 `--test-threads` 作為建議保留的約束（本計畫已於 r03 全面清除）。最小修復：該例改為仍存在的 job（如 `opencode-export-linux`）或加「0.23.x 已拆除，保留僅為歷史口徑」註記。
6. **`**Verification:**` 標題後是否空行在各卡之間不一致**：`:862`（M2-16A）、`:1080`（M2-16D）、`:1285`（M2-01）等有空行，`:929`（M2-16B）、`:997`（M2-16C）、`:1148`（M2-16D1）、`:1589`（M2-02R）、`:1801`（M2-05）無。CommonMark 下兩種都正確渲染，但與多輪「空行統一」的收口目標不符，且歷史上此類差異多次被誤判為回歸。最小修復：統一為「標題後一個空行」。
7. **`$SRC_CLONE` 引號用法不一致**：`Grep -o 'git -C \$SRC_CLONE'` 命中 `:13`、`:377`、`:846`、`:915`、`:983`、`:1203`、`:2296` 共 7 處未加引號，而 `:847`、`:1214`、`:1221`、`:710` 用 `"$SRC_CLONE"`。`DEP-M2-SRC-01` 明確允許 operator 自行提供路徑，路徑含空白時未加引號的命令會斷。最小修復：統一改為 `git -C "$SRC_CLONE"`。

---

**計數：`P0=0`，`P1=3`，`P2=7`。**

VERDICT: FAIL
EXIT=0
