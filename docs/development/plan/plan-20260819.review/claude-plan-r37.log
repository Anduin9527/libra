# plan-20260819 R30-r37 review — Claude（評審期間凍結：plan sha256 4fbaf4bd…、template 49f94da3…、plan-status fe8a06f2…）
# reviewer: claude -p --effort xhigh --allowedTools Read Grep Glob (read-only, prompt via stdin)
# date: 2026-09-22T05:16:24+08:00
4fbaf4bd360a54615f0a711b9dfcf2f23241ce2743c935a880a4832a1776e790  docs/development/plan/plan-20260819.md
49f94da35c8828e72caaae52006f52c84c72647016dbce188cad77715b2f7f4f  docs/development/plan/plan-template.md
fe8a06f2248fb0576425d3a0be32cb01f41d4b36a31d59ba18b1b30c9346bf61  docs/development/plan/plan-status.md
# review prompt: plan-20260819.review/review-prompt-r37.md

# R30-r37 計劃複審（Claude，只讀）

## 零、被審版本與核對基線

```
$ sha256sum docs/development/plan/plan-20260819.md docs/development/plan/plan-status.md docs/development/plan/plan-template.md
4fbaf4bd360a54615f0a711b9dfcf2f23241ce2743c935a880a4832a1776e790  plan-20260819.md
fe8a06f2248fb0576425d3a0be32cb01f41d4b36a31d59ba18b1b30c9346bf61  plan-status.md
49f94da35c8828e72caaae52006f52c84c72647016dbce188cad77715b2f7f4f  plan-template.md
```

與 `plan-20260819.review/r30-r37-sha256.txt` 三行逐字一致；複審開始與結束各算一次，雜湊未變（計劃檔全程凍結）。倉庫 `main` HEAD = `45ad1f7f2`。本輪未修改倉庫任何檔案。

## 一、r36 findings 逐條核對

| r36 finding | 核對命令／行號 | 結論 |
|---|---|---|
| **Codex P1**　`plan-status.md:139` 17 張卡合併成一行 | `rg -n '^\| M2-' plan-status.md` → `:132–:156` 共 **25 行**（M2-16A/B/C/D/D1/E + M2-01…M2-15 各一行），M2-16D1 條件狀態單列保留 | **已關閉** |
| **Codex P2-1 ≡ Claude P2-2**　M2-08→M2-09 卡邊界空行 | `Grep(multiline) '\S\n### Task M2-'` → **No matches**；`rg '^### Task M2-'` 得 25 個標題（`:849 … :2610`） | **已關閉** |
| **Codex P2-2 ≡ Claude P2-1**　review log Scope 輪次筆誤 | `:2896`/`:2898` 舊行已改為 `R30-r34…`／`R30-r35…`；但**同批新增**的 `:2900`/`:2901` 寫成 `R30-r36/r42` | **未關閉（見 P2-1）** |
| **Claude P2-3**　AC9 把既存 wire 欄位寫成「新增」、缺 `PROTOCOL_MINOR` bump | `:1010` 現為「握手 wire 的 `protocol.minor`（現有 `ProtocolVersion`；核對日 `protocol.rs:25 PROTOCOL_MINOR = 0`）與 `BridgeCapability` 由本卡**賦予協商語義**並 bump `PROTOCOL_MINOR 0 → 2`」；`:1060` 補「`BridgeCapability.methods`/`MethodAllowlist::contains` 不變式改寫」。實測 `protocol.rs:25 pub const PROTOCOL_MINOR: u32 = 0;` 錨點成立 | **已關閉** |
| **Claude P2-4**　寫集缺 `transport.rs`、`prod-files=7` | `:1056` 寫集含 `transport.rs`；`:1082` `prod-files=8`；審計 `:839` `4/8`。實測 `validate_method` 唯一生產呼叫點在 `transport.rs:131`（連線迴圈），落點判定正確 | **已關閉** |
| **Claude P2-5**　`EX-M2-07` 分組序／`EX-M2-06` B 序 | `:1050` 分組序 AC1→AC2→AC3→AC4→**AC11**→AC5a/b/c→AC6→AC10，與 `:1006–:1016` 的 11 個 checkbox 同序；`:1036–:1048` B0–B12 與 `:1020–:1032` 的 13 條 VER 逐條同序 | **已關閉** |

## 二、全量複審實測

**事實基線（核對日 `45ad1f7f2` 全部成立）**

- `sed -n '50,70p' src/internal/ai/mod.rs` → 無 `pub mod memory`；`sed -n '1,45p' src/internal/model/mod.rs` → 無 `memory_*` entity。
- `ls sql/migrations/ | tail` → tip 仍為 `2026091901_operation_boundary_claim_columns`。
- `rg '^  [a-z0-9-]+:$' .github/workflows/base.yml` → 7 個 job（format / clippy / owner-liveness-macos / redundancy / test / network-remotes / opencode-export-linux），與計劃一致。
- `tests/compat/version_surface_sync.rs:13` 明寫「now: `Cargo.toml`, `install.sh`, `install.ps1`」→ 版本面三處，與模板 v2.8「以 `compat_version_surface_sync` 為權威」一致。
- M2-16C 全部錨點實測存在：`METHOD_ALLOWLIST`（`protocol.rs:62`）、`allowlist_has_exactly_20_methods`（`protocol.rs:532`，斷言 `len()==20`）、`protocol_v1_allowlist_is_exactly_20_methods`（`tests/agent_bridge_protocol_test.rs:15`）、`BridgeCapability`（`:116`）、`authorization.rs::{ActionClass,classify}`、`tests/agent_bridge_approval_test.rs`（既有）、`agent_bridge/memory.rs` 不存在（本卡首建）、`tests/agent_bridge_memory_test.rs` 不存在（新 target）。
- `DEP-M2-SRC-01`（`:722`）8 格完好；source clone `/run/media/genedna/data/libra-pr456` 經 `Glob` 確認存在且含 `.git`。merge-base `2ebd409…` 與 158 路徑本輪因沙箱未放行 `git -C` 外部倉庫命令而未重算，沿用同一凍結版上 r36 雙評審已歸檔的實測結果（`codex-plan-r36.log` 核驗摘要）。

**機械檢查**

- 25 卡 `Granularity` vs 審計表 `:819–:843` **逐欄逐格比對全部一致**（AC/VER/landing/prod-files/scope/deps/writeset/release/split-from/exception）。
- `rg '^\|(?:[^|]*\|){15}$'` = **27**（審計表 25 列 + 表頭 + 分隔列），無漏網；`rg '^\|'` = 536，全部落在 {2..9, 15} 欄寬集合內，無孤立寬度。
- 審計表末行 `:843`（M2-15）→ `:844` **物理空行** → `:845` `**Verification 新增标记：**`（`sed -n '830,860p' | cat -n` 第 14/15/16 行）。
- `rg '^\*\*Verification:\*\*\n\S'`（multiline）→ **零命中**；`rg '\$SRC_CLONE'` → 13 處**全部加引號**；`rg '\\\|'` → 僅 `:461`/`:2882` 兩處歷史敘述（指涉該字元本身，非可複製命令）。
- DAG（`:680–:711`）25 節點、無環、無懸空；`M201K --> M216C` / `M201K --> M213` 只出現在「實施順序」圖；M2-16C/M2-13 的 `Dependencies`、審計 deps 與邊集一致。
- 計數：M2-16C AC checkbox 11、`EX-M2-07` 算式 2+4+6+1+3+9+3+2+2+2+5+1+1 = **41** ✓；VER checkbox 13 = B0–B12 = **13** ✓。M2-08 AC 8 / VER 8、`EX-M2-09` 2+4+4+3+17+2+3+3 = **38**、AC5-1..AC5-17 齊備 ✓。M2-01K AC 9 / VER 7 / prod 7 ✓。`:814`「AC 計數口徑」列舉的謂詞還原卡（M2-05=25、M2-07=9、M2-13=45、M2-16C=41、M2-08=38）與各卡分子一致。
- `plan-status.md` r18–r36 完整（`:46`），`:217` 記「r01–r36」，r37 待跑。

---

## P0（0 條）

無。

## P1（0 條）

無。

## P2（4 條）

### P2-1　新增的 R30-r37 兩行 review log 把 Scope 寫成 `R30-r36/r42`，違反計劃自設的 `:810` 機械檢查（第十二次復發）

- **位置：** `plan-20260819.md:2900`、`:2901`；規則行 `:810`
- **問題：** `:810` 規定「Round 欄輪次號必須等於 Scope 欄所寫修訂版之輪次 + 1（例：r30 行的 Scope 必須寫 R30-r29）」。r36 的兩行已按 r36 修訂改正為 `R30-r35 修訂後的凍結版`，但**同一批新增**的 r37 兩行寫成 `R30-r36/r42 修订后的完整计划` —— `r42` 不存在，且 Scope 欄只能寫單一修訂版輪次。
- **為何重要：** 這是計劃自己宣告為「必須」的機械檢查，且 review log 是雙評審凍結版的唯一追溯鏈；連續復發說明新增行沒有套用該檢查，實施期回填發布 SHA 時同一位置會再錯。
- **最小修復：** `:2900`/`:2901` 的 Scope 格改為 `R30-r36 修訂後的凍結版（sha256 4fbaf4bd…）`；並在 `:810` 規則後補一句「新增 review log 行時同步執行本檢查」。

### P2-2　`EX-M2-07` 的「獨立謂詞」分組把 minor 協商計了兩次（AC11 與 AC5c 描述同一要求）

- **位置：** `plan-20260819.md:1050`（分組算式）對照 `:1010`（AC11 checkbox）與 `:1011`（AC5 checkbox 內嵌句）
- **問題：** `:1010` 的獨立 checkbox 已完整定義「1.0 peer 只獲 20、1.2 peer 獲 22、未協商方法穩定拒絕」（計 AC11 = 3 謂詞）；`:1011` 內仍保留「本卡定義 bridge minor/capability 協商（舊 1.0 peer 只獲 20-method allowlist、新 peer 才獲 22）」一句，又被算成 `AC5c minor 協商(2)`。同一要求被計兩次，分子 41 中有 2 條非獨立。
- **為何重要：** G-03 門族豁免的准入條件是「按獨立謂詞還原、分子如實」；重複計數雖屬保守方向（不掩蓋軸數），但讓 `EX-M2-07` 的補償論據失真，也讓實施期無法用分組表當 checklist（同一條會被驗兩遍、或被誤認為兩個不同門）。
- **最小修復：** 刪除 `:1011` 中該內嵌句（語義已由 AC11 承載），並把 `:1050` 算式改為去掉 `AC5c minor 協商(2)`、總和 `= 39`；同步 `:1082` `AC=39/8@EX-M2-07`、`:801` waiver 分子與理由、審計表 `:839` 與 `:814` 全域口徑。

### P2-3　AC8 未要求刷新 `tests/INDEX.md` 中 `agent_bridge_protocol_test` 既有行的「20-method allowlist」描述

- **位置：** `plan-20260819.md:1014`（AC8）；實測 `tests/INDEX.md:93`「plan-20260818 LB-01 bridge protocol v1: **20-method allowlist**, …」
- **問題：** M2-16C 把 allowlist 由 20 改為 22，`tests/INDEX.md:93` 的既有描述隨之失真。AC8 只寫「新增 compat 测试登记 `tests/INDEX.md`」（指新 target `agent_bridge_memory_test`），未點名更新既有行；寫集雖含 `tests/INDEX.md`，但沒有判據會發現這處失真。
- **為何重要：** 模板「使用規則」明寫「只改動既有用例時，僅當索引行的描述失真才更新」—— 此處正是失真情形，而本卡是唯一有機會修正的卡；漏掉會在 M2-15 收口時留下與實現矛盾的索引。
- **最小修復：** `:1014` AC8 末補一句：「同卡更新 `tests/INDEX.md` 中 `agent_bridge_protocol_test` 既有行的 `20-method allowlist` → `22-method allowlist（1.0 peer 20 / 1.2 peer 22）`」。

### P2-4　AC11 只定義 minor = 0 與 2 兩點，未寫未知／中間 minor 的收斂規則

- **位置：** `plan-20260819.md:1010`（AC11）、`:1029`–`:1030`（VER B9/B10）
- **問題：** AC11 規定「1.0 peer 只獲且只能調用 20 個方法，1.2 peer 獲 22 個」，但沒說 peer 送 `minor=1`（或未來 `minor≥3`）時如何收斂。現行 `protocol.rs:484 dispatch_handshake` 只校驗 `major`、對所有 v1 peer 回完整 allowlist，實作者必須新寫這條規則。
- **為何重要：** 若實作選「精確匹配」而非 `negotiated = min(peer_minor, PROTOCOL_MINOR)`，未來 1.3 host 會被永久鎖在 20 方法；若反向對未知 minor 放行 22，則是 fail-open，與 AC11「不得落入全局 allowlist 放行」的本意相悖。兩種錯法都通不過任何一條既有具名 VER。
- **最小修復：** `:1010` 補一句「協商 minor = `min(peer.protocol.minor, PROTOCOL_MINOR)`，未知／更高 minor 向下收斂（fail-closed，不得放行未定義方法）」，並在 `:1029` 之後補一條 VER（如 `agent_bridge_protocol_test::unknown_minor_clamps_down`）＋ `EX-M2-06` B 列表相應順延。

---

（補充，非 finding：`plan-status.md:127` 同一段落把「整份遷到模板 v2.8（版本面集合以 `compat_version_surface_sync` 為權威）」寫了兩次，屬純編輯冗餘，不影響任何判據。）

VERDICT: PASS
EXIT=0
