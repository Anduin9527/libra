# plan-20260819 R30-r04 review — Claude
# reviewer: claude -p --effort xhigh --allowedTools Read Grep Glob (read-only, prompt via stdin)
# date: 2026-09-21T13:47:13+08:00
d740f503acae956ff4f39a33fa6d3c9ef6d1d79290e70fb730464f8cff8d803e  docs/development/plan/plan-20260819.md
# review prompt: plan-20260819.review/review-prompt-r04.md

我完成了只读评审（未修改任何仓库文件）。先给 r03 findings 的逐条核对，再给本轮问题。

## 一、r03 findings 关闭核对（实际执行的命令 / 行号）

| r03 条目 | 状态 | 核对证据 |
|---|---|---|
| Claude P0-1 bridge 无可用 source plane | **已关闭** | `plan:497` 枚举含 `bridge_event`（locator=`(bridge_session_id,event_seq)`、digest=`payload_sha256`）；`:613`、`:949`、`:1851`、`:2589` 贯穿。仓库核对：`sql/migrations/2026081801_agent_bridge_capture.sql:36-41` 有 `event_seq` / `payload_sha256` ✓ |
| Codex P1-3 / Claude P1-1 `EX-M2-01` waiver 登记 | **已关闭** | `plan:748-752` 新增 8 列 waiver 表（列名与 `plan-template.md:539-542` 一致）；任务列 = `M2-02,M2-02F,M2-02R`；`:746` 改为「除下表登记的 `EX-*` 外无其它 waiver」；`:2700` 改为「无未登记的…规则 waiver」；三卡「判据规范（非计数正文）」块 `:1362-1364`、`:1433-1435`、`:1504-1506` ✓ |
| Claude P1-2 M2-16A/16C `exception` | **已关闭** | `:857`、`:997` 均为 `exception=N/A` ✓ |
| Claude P1-3 model registry 承接卡 | **已关闭** | M2-08 AC `:1919`（`with_compiler_model` + pending + 稳定 reason code）、VER `:1932` `job_pending_without_compiler_model`；M2-16C AC `:949` 改为复用 ✓ |
| Claude P1-4 DAG `M216D→M216E` | **已关闭** | `:666`；D1 条件边 `:667` ✓ |
| Claude P1-5 审计表 M2-08 deps | **已关闭** | `:768` = `M2-07,M2-16B` ✓ |
| Claude P1-6 bridge host 仅 `deepseek-harness` | **已关闭** | `:500`、M2-16C Out of scope `:935`。仓库核对 `2026081801_agent_bridge_capture.sql:15` = `CHECK(source = 'deepseek-harness')` ✓ |
| Claude P1-7 / Codex P2-2 VER 计数 | **已关闭** | 我逐卡数过 25 张卡的 AC/VER：02/02F/02R/05/08/13/16C = 8 条 → `8/8`；07 = 7 → `7/8`；全部与 Granularity + 审计表一致；M2-05 的四个 `test -f` 已合并为 `:1724` 单退出码 for-loop ✓ |
| Claude P1-8 `sqlite_fts5_release_capability` 唯一 target | **已关闭** | `:1440`、`:2522`、`:675` 全部指向 `fts5_capability_test` ✓ |
| Codex P1-9 / Claude P1-9 `--test-threads=1` | **已关闭** | `Grep "test-threads" plan-20260819.md` 仅命中 `:448`、`:2665` 的历史行，正文零残留 ✓ |
| Claude P1-10 事实基线四处锚点 | **已关闭（实测）** | `wc -l src/internal/ai/mod.rs`=68（`:362` 写 53-68 ✓）；`grep -n "fn put" src/utils/client_storage.rs`=2023（`:369` 写 2023-2100 ✓）；`wc -l .github/workflows/base.yml`=422（`:2457` 写 11-422 ✓）；`sed -n '44p;70,76p;218,226p' src/internal/config.rs` 确认加密面在 44-74 / 218-290（`:1281` ✓） |
| Codex P1-1 ER-13/14 冲突 | **已关闭** | `plan-template.md:346` 明确「T-2 全量、其余独立发版卡以 nextest 跑 A 组 focused」；计划 `:70` 同步 ✓ |
| Codex P1-6 D1 条件边 / Codex P1-7 Phase 0 | **部分关闭** | `:667`、`:1171`、`:781`、`:1201`、`:2480` 条件依赖齐 ✓；Phase 0 `:698-701` 已移出 M2-16B ✓，但**里程碑 M0 `:2570` 未同步** → 见 P1-2 |
| Codex P1-4 `related_run_ids` 指纹 | **部分关闭** | ADR `:499`/`:520`、GC-M2-02 `:613`、M2-16D1 `:1085` 已加；**M2-08 AC `:1917` 与全部 VER 仍无** → 见 P1-1 |
| Codex P1-5 三处 mismatch 回归 | **部分关闭** | bridge `:961`、`observe_terminal` `:1933`、trust gate `:1862` ✓；`memory record` 无具名回归（`:2268-2275`）→ P2-3 |
| Codex P1-8 零命中守卫 exit-code 形式 | **已关闭** | `:824`、`:893`、`:1092`、`:1101` 均为模板 `plan-template.md:545-553` 的三分支形式 ✓ |
| Codex P2-1 / Claude P2-4 v2.7→v2.8 | **已关闭** | `plan:7`「迁移政策（v2.8）」；`plan-status.md:128` 首句已改 v2.8 ✓ |
| Codex P2-3 `<#456 tree>` 占位符 | **未关闭** | `:803` 仍为 `git -C <libra-pr507> …` → P2-4 |
| Codex P2-4 风险表 migration ownership | **已关闭** | `:2616` = `M2-02,M2-02F,M2-02R（M2-16A 仅提供领域层移植输入）` ✓ |
| Claude P2-1/2/3/5/6/8/9/10/11/12 | **已关闭** | `:64`（首建/扩展 + `delivery.rs`）、`:87`（补 `record`）、`:784`（并列 `compat_serial_registry`）、`:584`/`:1846`（ContextFrame 措辞）、`:1223`（memory.md 全文）、`:675`+`:1431`（三步判据 + pinned nextest）、`:1955`/`:2226`/`:989` 三卡统一 T-1、`:2413`（`AGENTS.md`）+`:2324`（目录尚不存在）、`:624-625`（GC-13/14 顺序）、`:2260`（record 并入 rebuild AC） ✓ |
| Claude P2-7 REL 表措辞 | **部分关闭** | `:690` 已改 PR→squash→tag 全流程 ✓；`:684` 窗口列仍按「推送、`gh release create`」叙述（形态列已回指 §0.5）→ P2-13 |
| Claude P2-13 模板 `.env.live-test` | **部分关闭** | `plan-template.md:328`、`:702` 已补 ✓；`:338`（ER-13 收口阶段）仍缺 → P2-9 |

**仓库事实独立复核（本轮实测）：** `ls src/internal/ai/` 无 `providers`/`context_budget`/`client.rs`/`prompt`/`mcp`/`orchestrator`/`web`/`memory` ✓；`ls src/internal/operation_wrapper.rs tests/operation_wrapper_test.rs` 不存在 ✓；`Grep "FTS5|bm25\("` 与 `Grep "EpisodeCompiler|EpisodePayload|RepositoryKeyedDigest|principal_hmac|query_hmac"` 在 `src` 零命中 ✓；`Grep "AgentRunStatus::(Completed|Failed)" src` 仅 `run.rs:99,100,117,118,142,143`（定义+单测）✓；`ls sql/migrations | tail` 确认 tip = `2026091901_operation_boundary_claim_columns`，故 `2026092101+` 合法 ✓；`base.yml` job 集合实测 = `format/clippy/owner-liveness-macos/redundancy/test/network-remotes/opencode-export-linux` 且 `on: pull_request`，与 DEP-M2-CI-02 `:676` 完全一致 ✓；`codeql.yml:21`=`analyze`、`:44-46`=`actions`/`rust` matrix ✓；`tests/compat/version_surface_sync.rs:13` 注释 = `Cargo.toml, install.sh, install.ps1` 三处，与模板 v2.8 与计划全部「版本面集合」表述一致 ✓；`.config/nextest.toml`、`tests/SERIAL_REGISTRY.tsv`、`tests/NEXTEST_GROUPS.sh` 均存在 ✓；`compat_version_surface_sync`/`compat_serial_registry`/`compat_help_examples_banner`/`compat_command_docs_examples_section` 四个 target 在 `Cargo.toml:248,260,276,590` 注册 ✓；`src/cli.rs:4116` `root_after_help_lists_every_visible_command`、`tests/compat/help_examples_banner.rs:120`、`tests/compat/command_docs_examples_section.rs:37` 三道守卫实名存在 ✓；`grep -c LBR-MEMORY docs/error-codes.md` = 0 ✓；M2-03 的三个「既有」filter 实测在 `src/internal/ai/history.rs:5889,5981,6075` ✓；M2-05 引用的 16 份 `docs/commands/{,zh-CN/}` 文档全部存在 ✓；DAG 无环、25 张卡与 Phase/审计表/REL/追溯表/完成判据（24+条件性 D1）计数一致 ✓。

---

## P0

无。

---

## P1

### P1-1　`related_run_ids` 进入幂等指纹的数据完整性规则：拥有该实现的卡没有 AC，全计划没有任何具名验证

- **位置：** `plan-20260819.md:1917`（M2-08 AC 第 3 条）、`:1926-1933`（M2-08 全部 VER）、`:2525`（测试矩阵 Job/recovery 行）；对照 `:613`（GC-M2-02）、`:499`（ADR-M2-05）、`:520`（ADR-M2-07）、`:448`（修订历史自述）
- **问题：** GC-M2-02 `:613` 把它定为强制规则——「canonicalized `related_run_ids` 必须进入 Task/Intent/record 的幂等材料，**不得静默丢弃 provenance**」，ADR-M2-05 `:499` 与 ADR-M2-07 `:520` 同步。但真正实现指纹的 M2-08（写集 `:1939` 含 `observer.rs`/`job.rs`/`job_state.rs`）其 AC `:1917` 复述指纹组成时写的是「Task 指纹绑定 `terminal_evidence`（locator+digest），Intent 指纹绑定 host 提供的 terminal Intent `root_id` + 有序 Task revision OID/缺失 marker」——**`related_run_ids` 不在其中**。我用 `Grep "related_run_ids" plan-20260819.md` 全文核对，命中仅 `:184,:496,:499,:613,:1085,:1990`（加两行历史），**没有任何 `Verification` 条目、也没有任何测试矩阵行**断言「不同 `related_run_ids` 必须产生不同指纹 / 不得被去重为同一终态记录」。M2-16D1 AC `:1085` 只断言正向（「重放同一 run 集合得到相同指纹」），不覆盖负向。
- **为何重要：** 这是 Codex r03 P1-4 的核心，而修订历史 `:448` 已宣称「`related_run_ids` 納入冪等指紋」关闭该项。按现状，实施者只照 M2-08 的 AC 实现即可通过本卡全部验收，却直接违反 GC-M2-02：两次带不同 run 集合的终态上报会被幂等键静默合并成同一条终态记录与同一个 Episode，provenance 永久丢失，且这是**写入侧不可逆**的（权威历史只能追加补偿）。属于「数据完整性 + 未测试假设」，且计划自述与实际不符。
- **最小修复：** ① 把 `:1917` 改为「…Task 指纹绑定 `terminal_evidence`（locator+digest）+ canonicalized `related_run_ids`（排序去重后），Intent 指纹绑定 host 提供的 terminal Intent `root_id` + 有序 Task revision OID/缺失 marker + canonicalized `related_run_ids`…」；② 在 M2-08 增加一条具名负向 VER（如 `cargo test --test memory_episode_test observation_distinct_related_runs_not_deduped`），并按 G-03 把 VER 计数由 `8/8` 调整（需合并现有某条或登记新 waiver）；③ 测试矩阵 `:2525` 的 Job/recovery 行补该用例名。

### P1-2　里程碑 M0 未随「Phase 0 移出 M2-16B」同步，成为在串行 DAG 下不可满足的循环门

- **位置：** `plan-20260819.md:2570`（里程碑 M0 行）；对照 `:698-701`（Phase 0 成员）、`:710`（Phase 2「M2-16B 在 M2-07 之后交付」）、`:652-654`（DAG `M216A→M216B`、`M207→M216B`、`M216B→M208`）、`:895`（M2-16B `Dependencies: M2-16A、M2-07`）、`:777`（审计表 deps=`M2-16A,M2-07`）、`:448`（修订历史自述「Phase 0 移出 M2-16B」）
- **问题：** M0 的「完成条件」仍写「M2-16A 正向移植 + **M2-01/16B** reviewer PASS；schema golden 固定」。但 r03 修订已把 M2-16B 依赖改为 `M2-07`，而 M2-07 属于 Phase 2（里程碑 M2 `:2572`）。同时 M0 **漏掉了 Phase 0 的第三张卡 M2-01K**（`:700`），而 M1 `:2571` 只列 `M2-02/02F/02R/03..06`，M2-01K 于是不属于任何里程碑。
- **为何重要：** 「并发声明：全串行」（`:686`）+「严格按 DAG」（`:690`）下，M0 是 M1 的前置门；按 `:2570` 执行会要求在 Phase 1 开工前完成 M2-16B，而 M2-16B 需要 M2-07（Phase 2）——门自身不可满足。这正是 Codex r03 P1-7 所指的同一处不一致，修订只改了「Phase 划分」一节，未按模板 G-09（拆分/改序必须同步「里程碑」）回填，也使评审要求 1 的「里程碑与 DAG 互相一致」不成立。
- **最小修复：** `:2570` 改为「M2-16A 正向移植 + M2-01 / M2-01K reviewer PASS；schema golden 与 keyed-digest 跨重启 fixture 固定」，并把 M2-16B 移入 `:2572`（M2 自动 Episode）的完成条件（例如「M2-07..10 + M2-16B 完成 host 上报的 Task/Intent 自动收敛…」）。

---

## P2

1. **M2-13 的 AC 自相矛盾，`record` 无合法调用面：** `:2262` 规定「CLI Adapter 只调用 EpisodeReader/Rebuilder/Job status Interface」，但同卡 `:2260` 要求实现 `libra memory record`，其唯一入口是 M2-08 的 `MemoryRuntime::observe_terminal`（`:1902`），不在该枚举内。最小修复：`:2262` 的接口清单补 `MemoryRuntime::observe_terminal`（或「record seam」）。
2. **测试矩阵与卡片的具名用例漂移：** `:2530` 写 `--filter memory_search_show_status_rebuild`，M2-13 VER `:2269` 实为 `memory_search_show_status_rebuild_record`。按 `:2514` 与 M2-14C AC `:2394`（`--filter <exact-name>` + `--list` 断言恰有命中）的合同，该行要么零命中失败，要么只能靠子串匹配侥幸通过。最小修复：`:2530` 补 `_record`。
3. **`libra memory record` 没有具名 status/evidence mismatch 回归：** `:448` 自述「具名回歸（bridge/observe_terminal/record）」，但 `:2268-2275` 八条 VER 无对应用例（仅靠 `:1862`/`:1933` 的下层覆盖）。最小修复：把 `:2269` 拆一条为 `memory_record_status_evidence_mismatch_quarantined`，或在 `:2260` 明确写「mismatch 语义由 `observe_terminal` 的 `:1933` 回归覆盖，CLI 不另测」。
4. **M2-16A 的 `Current evidence` 仍含未替换占位符：** `:803` = ``git -C <libra-pr507> grep -nE … bc1be587e4 …``。`<libra-pr507>` 未替换，且与 `:373` 登记的本地 worktree `/run/media/genedna/data/libra-pr456` 不一致；另外本仓库无 `.git`（CLAUDE.md / ER-07），`git -C` 在 libra-main 不可执行。模板 `plan-template.md:238`「不得留未替换占位符」。最小修复：改为固定路径 + 可重跑命令（或改用 `gh api` / `libra` 等价形式），并与 `:373` 统一。
5. **ADR-M2-05 的括注与同条枚举及 §0.2 自相矛盾：** `:497` 称 `bridge_event` 是「bridge 為現行唯一可用的 host 持久平面」，但同一条的枚举含 `session_jsonl`/`review_store`，且 `:32-33` 与我实测（`src/internal/ai/review/store.rs`、`src/internal/ai/session/jsonl.rs` 均存在）表明这两面同样可用；与 `:500`（非 deepseek host 走 `observe_terminal`/`record`，必须用别的 plane）冲突。最小修复：把括注改为「bridge 路径唯一可用的持久平面；嵌入式/补录路径使用 `session_jsonl` 或 `review_store`」。
6. **EX-M2-01 的「证据」与「Approver」列不支持该批准：** `:752` 的证据列指向 `fix-checklist-r03.md`，而该文件第 9 行的决议恰恰是「撤銷 `EX-M2-01`（刪 waiver 行與所有引用）」；Approver 写「计划 owner（本计划执行代理）」不是模板 `:539` 要求的「具名审批人」。最小修复：证据列改引 `codex-plan-r03.log` 的 P1-3（该条才主张登记 waiver）并具名 Approver；同批更新 `fix-checklist-r03.md` 的过时决议或标注被 Codex 决议取代。
7. **M2-16D1 AC 有重复 checkbox 前缀：** `:1092` = `- [ ] - [ ] \`if rg -n "orchestrator" …\``。最小修复：删去多余的 `- [ ] `。
8. **§0.4「移植所有权」与三张卡的写集不吻合：** `:64` 判 `policy.rs → M2-04（首建）/M2-07（規則擴展）`，但 M2-07 写集 `:1868` 不含 `policy.rs`；`:64` 判 `writer/store/tree/admission → M2-04`，M2-04 写集 `:1658` 无 `admission.rs`；`:64` 判 `projection/replay/diagnostics → M2-06`，M2-06 写集 `:1798` 只有 `{projection,replay,reader_state}.rs`。最小修复：三处二选一对齐（补进写集，或从 `:64` 删除/改名）。
9. **模板自身 ER-13 收口门仍缺 `.env.live-test`：** `plan-template.md:338` 写 `source .env.test && cargo nextest run --all …`，与同文件 `:71`（术语「全量测试」）、`:328`（触发命令）、`:346`（ER-14）、`:702`（完成判据）要求「两个环境文件必须同时加载」冲突。最小修复：`:338` 补 `source .env.live-test &&`。
10. **模板本批变更引入 Markdown 结构回归：** `plan-template.md:352` 的 `**与 ER-13 的关系:**` 段落在本次 diff 中丢失 4 空格缩进（`git diff docs/development/plan/plan-template.md` 可见），脱离编号项 14；其后 `:354` 的 `**不得替代:**`（仍为 4 空格缩进，且前有空行）将被渲染成缩进代码块。最小修复：给 `:352` 恢复 4 空格缩进。
11. **`plan-status.md` 两处过时：** `:195` 的开工看板仍写「**未过**（R30-r01 Claude `FAIL`；Codex 进行中）」，而 `:46` 已记录到 r03；`:256` 的 DEFER 登记仍写 `DEFER-M2-01..08`，未含计划 `:2696` 新增的 `DEFER-M2-09`。最小修复：两行同步到 r03/r04 与 `DEFER-M2-01..09`。
12. **`EpisodeProposalV1` 的落点归属未声明，存在 GC-02 双真源风险：** M2-16B Description `:865` 声明由本卡「定义并实现 `EpisodeProposalV1`」（写集 `:899` 仅 `model.rs`+`mod.rs`），但先于它执行的 M2-07 其 `EpisodeCompiler` 接口与 `AutoEpisodeTrustGateV1`（`:1849`、`:1851`）要消费 proposal，M2-16C `:949` 又要求 host 提交的 `EpisodeProposalV1` 走同一 trust gate。计划未说明 M2-07 消费的是 `EpisodePayloadV1`（M2-01 owns）还是 `EpisodeProposalV1`。最小修复：在 ADR-M2-12 `:576` 或 §0.4 `:64` 明确 `EpisodeProposalV1` 的所属文件与首建卡（并据此校正 M2-16B Description 或 M2-07 AC 的类型名）。
13. **REL-M2-01 窗口列措辞仍滞后于 §0.5 的 PR 流程：** `:684` 的「窗口与失败恢复」仍按「…签名提交、推送、`gh release create` 触发 `release.yml`…」叙述（形态列已回指 §0.5，`:690` 已改全）。最小修复：`:684` 括注改为「…签名提交、推送 feature branch、`gh pr create` → 同 SHA 全绿 → `gh pr merge --squash` → 以 merge SHA `gh release create`…」或直接回指 §0.5/`:690`。

---

**统计：P0=0、P1=2、P2=13。** 未达到 `P0=0 且 P1=0` 的 PASS 门槛。

VERDICT: FAIL
EXIT=0
