# plan-20260819 R30-r30 review — Claude（評審期間凍結：plan sha256 b78d3c89…、template 49f94da3…）
# reviewer: claude -p --effort xhigh --allowedTools Read Grep Glob (read-only, prompt via stdin)
# date: 2026-09-22T03:40:46+08:00
b78d3c896d28dd2fcd534847793ce1b49846a2c1be6b58e65d4752506f8dbdd1  docs/development/plan/plan-20260819.md
49f94da35c8828e72caaae52006f52c84c72647016dbce188cad77715b2f7f4f  docs/development/plan/plan-template.md
# review prompt: plan-20260819.review/review-prompt-r30.md

# plan-20260819 R30-r30 只讀評審（Claude）

**被審版本（凍結）：** plan `b78d3c896d28dd2fcd534847793ce1b49846a2c1be6b58e65d4752506f8dbdd1`、template `49f94da35c8828e72caaae52006f52c84c72647016dbce188cad77715b2f7f4f`、plan-status `f018740723e9…`（與 `r30-r30-sha256.txt` 逐字一致；進出場各 `sha256sum` 一次，未變）。**全程只讀，未修改倉庫任何檔案。**

## 一、r29 findings 逐條關閉核對

| r29 finding | 關閉？ | 實測證據 |
|---|---|---|
| Codex P1：bridge session→registry identity 未定義 | **部分**（見 P1-2 / P1-3） | `rg -n 'resolve_session_compiler_identity\|bridge_session_hmac\|bridge_session_bound_identity_matrix'` 命中 `:996`（M2-16C AC3）、`:1014`（VER）、`:1030`（B10）、`:1433`/`:1447`（M2-02 A1/VER 八列）、`:2038`（M2-08 AC5）、`:2390`（M2-13）。**但不命中 ADR-M2-05（`:520-538`）與表職責行（`:329`）**，r30 提示要求的「ADR/表職責」兩處皆未落地 |
| Claude P1-1：`EX-M2-07` 理由欄仍寫 31 | ✅ | `:790` 已改「共 **33** 條」，括號枚舉補入 `ActionClass`（協議/recall/ActionClass/record/identity/minor/文檔）；`:797` 口徑 `M2-16C=33`、`:1058` `AC=33/8@EX-M2-07`、`:824` 審計表一致 |
| Claude P2-1：review log 輪次筆誤 `r34` | ❌（見 P2-1） | `:2861`/`:2862` 現為「R30-r29/**r35** 修订后」——同一欄位第五次同類筆誤；且 `:798` 本輪新增的「Round 欄輪次號必須等於 Scope 欄所寫修訂版之輪次 + 1」機械檢查**被同檔這兩行直接違反** |
| Claude P2-2：三處 waiver 理由與門族不同步 | ✅ | `:791` EX-M2-06 補「（含 approval/`ActionClass` 分類與 20→22 守衛）」且分子 `11/8@EX-M2-06`；`:793` EX-M2-05 已改「33 份 EN/zh/開發文檔**內容性**（每份須含 `libra/memory` 段）」；`:794` EX-M2-04 已改「首建**五型**（四 wire 型 + `trust.rs` 的 `CompilerIdentityV1`）」 |
| Claude P2-3：`EX-M2-06` B4/B5 與 checkbox 位序互換 | **部分**（見 P2-3） | B4/B5 已對調正確（`sed -n '1006,1007p'` ↔ `:1023,1024`），但新增的 B10 置於列末而其 checkbox 在第 8 位，B7/B8/B9 相對位移 |
| Claude P2-4：AC6 守衛名仍 `..._20_methods` | ✅ | `:991` 已改「含**重命名後的** `allowlist_has_exactly_22_methods` 與 `tests/agent_bridge_protocol_test.rs::protocol_v1_allowlist_is_exactly_22_methods` 兩處守衛（原名 `..._20_methods`）」 |
| Codex P2：R30-r29 log Scope 寫 `r34` | ❌ | 同 P2-1（換成 `r35`） |

## 二、本輪機械核對（r30 指定項）

- **M2-16C 計數：** `rg -n '31/8@EX-M2-07\|9/8@EX-M2-06'` 僅命中 `:464`（r17 歷史行）。`:1058` `AC=33/8@EX-M2-07; VER=11/8@EX-M2-06`、`:824` 審計表同值、`:790`/`:791` waiver 同值、`:797` 全域口徑 `M2-16C=33`。VER checkbox 實測 **11**（`sed -n '1007,1017p'`），B0–B10 實測 **11**，集合 1:1 ✓。**唯獨 `:1032` 的 AC 算式各項相加 = 32 ≠ 宣稱的 33**（見 P1-1）。
- **可複製命令零 `\|`：** `rg -n '`[^`]*\\\|[^`]*`'` 僅 2 命中（`:454` 修訂歷史、`:2855` review log），兩者的 `` `\|` `` 都是**描述該次修復本身**的字面量，非可執行命令 ✓。
- **表格機械列數：** 以「恰 N 個 `|` 且以 `|` 開頭」分桶：N=3→105、N=4→116、N=5→86、N=6→149、N=7→12、N=9→19、N=16→27，**合計 514 = `rg -c '^\|'` 的 514**，無任何其它寬度。扣除 `:454`/`:2855` 兩行含 `\|` 造成的 +1 偏移後，真實分佈為 N=6→151、N=7→10，與 r29 的 148/10 相差的 +3 行恰為本輪新增的 1 條修訂歷史行 + 2 條 r30 review log 行 ✓。審計表 27 行全 16 格 ✓。
- **審計表末行空行（位元組核對）：** `sed -n '831,834p' | cat -A` → `:831` M2-15 資料行、`:832` 為純 `$`（物理空行）、`:833` `**Verification 新增标记：**`；`rg -n '^\|.*\|\*\*'` 零命中 ✓。
- **空行統一：** `rg -U '^\*\*Verification:\*\*\n\S'` 零命中（25 處後皆空行）；`rg -U '\S\n\*\*Dependencies:\*\*'` 零命中（25 處前皆空行）✓。
- **25 卡逐欄一致：** 25 條 `**Granularity:**` 與審計表 `:806-831` 的 `type/axis/recovery/complete/self-contained/AC/VER/landing/prod-files/scope/deps/writeset/release/split-from/exception` **逐格比對全部一致**；AC/VER 分子與實際 checkbox 逐卡點算一致（含 M2-02 9/9、M2-02F 9/8、M2-02R 9/8、M2-03 7/5、M2-04 8/7、M2-05 25 謂詞/10、M2-06 8/5、M2-07 9/10、M2-08 34 謂詞/8、M2-13 45 謂詞/15、M2-16C 33 謂詞/11、M2-15 9/2）；`prod-files` 與寫集逐檔點算一致（M2-05=17、M2-08=12、M2-02=11、M2-02F=9、M2-04=9、M2-07=10、M2-12=8、M2-13=5）。G-04 上限全部滿足，唯一超限者 M2-05 帶 `L-exception:EX-M2-03`。
- **DAG 與 25 卡 `Dependencies` 逐邊比對：** 無環、無缺邊（含 `M216A→M216B`、`M207→M216B→M208`、`M208→M216C`、`M212→M216C`、`M216C→M213`、`M216D→M216E`、D1 條件邊）✓。
- **waiver 集合：** `EX-M2-01..07,09` 共 8 筆，waiver 表 8 行 8 列；`EX-M2-08` 僅存於 `:463`/`:464` 歷史行與 `:2838` review log（已撤回）✓。
- **plan-status：** `:46` 含 r18–r29 全部輪次與計數（r29 Codex 0/1/1、Claude 0/1/4，與 log 一致），`r30 待跑` ✓。

## 三、倉庫事實基線（核對日成立）

`src/internal/ai/mod.rs` 無 `memory`/`keyed_digest`（子模組確為裸 `pub mod`，印證 GC-M2-15 措辭）；`src/cli.rs:670` 確在 Commands enum 內且無 `Memory` variant；`src/internal/model/mod.rs` 共 43 個 `pub mod`、無 `memory_*`；migration tip = `2026091901_operation_boundary_claim_columns`（`src/internal/db/migration.rs:1863-1865`）；`Cargo.toml version = "0.23.36"`；版本面 = 三處（`tests/compat/version_surface_sync.rs:1-14`「now: `Cargo.toml`, `install.sh`, `install.ps1`」）；`base.yml` job 集合 = `format/clippy/owner-liveness-macos/redundancy/test/network-remotes/opencode-export-linux`（7，與 DEP-M2-CI-02 逐字一致）；`release.yml` = `build-and-upload/upload-install-scripts/update-homebrew-tap/verify-homebrew-formula/request-stable-manifest`（5，與 DEP-M2-CI-03 一致）；`authorization.rs:19 pub enum ActionClass`、`:32 pub fn classify`；`protocol.rs:532 allowlist_has_exactly_20_methods`、`tests/agent_bridge_protocol_test.rs:15 protocol_v1_allowlist_is_exactly_20_methods`；`tests/agent_bridge_approval_test.rs` 現有 5 用例、確不含 `memory_recall_is_normal_and_record_requires_approval`；`sql/migrations/2026081801_agent_bridge_capture.sql:16` `CHECK(source = 'deepseek-harness')`。全計劃對 `providers`/`context_budget`/`prompt`/`client`/`mcp`/`orchestrator`/`web`/`operation_wrapper`/`test-provider` 的引用全部落在 §0.1 歷史表、ADR Context/Alternatives、禁止清單或零命中守衛，**無執行性依賴** ✓。

**證據限制：** `$SRC_CLONE`（`/run/media/genedna/data/libra-pr456`）在本次沙箱外（`ls` 被工作目錄策略阻斷），無法執行 `rev-parse`/`merge-base`/158-path 計數；`DEP-M2-SRC-01` 已規定「來源 clone 不可用即 `Lifecycle=blocked`」並要求雙評審至少一輪在可訪問 clone 的環境歸檔，故不計為 finding，但本輪不構成該項證據。

---

## P0（0 條）

無。

---

## P1（3 條）

### P1-1　`EX-M2-07` 判據規範塊的謂詞算式相加為 32，與同行宣稱的 33、waiver 分子、全域口徑與審計表矛盾；新增的第 33 條（bridge session 身份交接）未進入任何分組

- **位置：** `plan-20260819.md:1032`（EX-M2-07 判據規範塊）；對照 `:790`（waiver 理由/分子）、`:797`（全域口徑）、`:1058`（Granularity）、`:824`（審計表）
- **問題：** `:1032` 的算式為 `AC1 協議版本(2)+AC2 recall(4)+AC3a record 入參校驗(9)+ActionClass 契約(1)+AC3b identity(3)+AC3c minor 協商(2)+AC4 單一 seam/rg(2)+AC5 脫敏審計(2)+AC6 文檔/守衛(5)+AC7 E2E(1)+AC8 錯誤碼 Display-pin(1) = 33`。逐項相加：`2+4=6`→`+9=15`→`+1=16`→`+3=19`→`+2=21`→`+2=23`→`+2=25`→`+5=30`→`+1=31`→`+1=**32**`。r29 版本的算式（Claude r29 log `:1026` 記錄為 `2+4+9+1+3+2+2+2+5+1+1 = 32`）**項數與各項數值完全相同**；本輪為 bridge session 身份交接新增了第 10 個 AC checkbox（`:996`）並把總數改成 33、VER 改成 11，**卻沒有為該 AC 增加任何謂詞分組或提高既有分組計數**。因此 `AC3b identity(3)` 仍只覆蓋 r29 時期的三條 identity 謂詞，而 `:996` 這條 AC 本身至少含「有效 session 取得 handle／跨 session-worktree 拒絕／過期撤銷拒絕／payload 自填拒絕／禁止直接構造 `CompilerIdentityV1`／重啟後解析一致」六個獨立謂詞。
- **為何重要：** 模板 `plan-template.md:526` 對「G-03 條目上限（門族型驗收）」waiver 的准入條件明文要求「門族增減時同批更新 waiver 行與粒度審計表」，而 `:790` 本身登記的補償措施就是「**判據規範逐條枚舉**，AC 分子如實寫 `33/8@EX-M2-07`」。現況下被審批的枚舉只能還原 32 條，計劃其餘四處卻主張 33 條在豁免範圍內——收口時無法機械判定第 33 條（bridge session 身份交接）是否被已審批的豁免覆蓋，等同於審批記錄與被審批對象不符。這正是 r28（31→32）、r29（waiver 理由欄）連續兩輪處理過的同一「只改一半」失效模式第三次落在 `EX-M2-07` 上，且這次落在**唯一可機械驗算**的算式行。
- **最小修復：** 在 `:1032` 為新 AC 建立獨立分組並使算式自洽，例如把 `AC3b identity(3)` 拆為 `AC3b identity(3)+AC3d bridge session 交接(N)`，或直接把 `AC3b identity` 提高到覆蓋 `:996` 的謂詞數，使各項之和字面等於行尾宣稱值；同時確認 `:790`/`:797`/`:1058`/`:824` 的分子與新和一致（若調整後總數不是 33，五處需同批改為新值）。

### P1-2　表職責表的 `memory_compiler_identity` 行仍只列 7 欄（key + 6），與 M2-02 A1/VER 的「八列」與 M2-08 AC5 的 session 綁定直接矛盾

- **位置：** `plan-20260819.md:329`（「記錄與存儲結構 → 表職責」表）；對照 `:1433`（M2-02 A1）、`:1447`（M2-02 VER）、`:2038`（M2-08 AC5）
- **問題：** `:329` 寫「`memory_compiler_identity` | 有界本地运行状态 | `handle_hmac` | 由 M2-08 `runtime.rs` 签发/验证的 compiler identity：`principal_hmac`、`repository_id`、`worktree`、`issued_at`、`expires_at`、`revoked_at`」——主鍵 + **6** 個非鍵欄，**無 `bridge_session_hmac`**。而 `:1433` M2-02 A1 明寫「含 `memory_compiler_identity` 的 `handle_hmac`（主鍵/唯一）＋`principal_hmac/bridge_session_hmac/repository_id/worktree/issued_at/expires_at/revoked_at` **八列**」，`:1447` 的具名 VER `memory_compiler_identity_schema` 要求「逐列斷言……八列與約束」，`:2038` M2-08 AC5 要求「`memory_compiler_identity` 以 `bridge_session_hmac` 綁定 session」。實測：`rg -n 'memory_compiler_identity'` 的 8 處命中中，`:329` 是**唯一**定義該表欄位清單的規範段落，且是唯一漏列該欄的一處。
- **為何重要：** §「記錄與存儲結構」的表職責表是全計劃對每張表欄位合同的單一規範來源（`:336` 明寫「完整列合同在图后的表格和 `memory.md`」）。M2-02 是照這張表寫 `2026092101_memory_core.sql` 的卡；一個按 `:329` 實作的執行者會建出 7 欄的表，而同卡的 `memory_compiler_identity_schema` VER 斷言八列，門必紅；若反過來以 A1 為準，則 `:329` 這份被下游（M2-08 entity、M2-13 identity 命令）引用的規範文本就是錯的。兩份規範對同一張表給出不同欄位集，屬於執行期必然衝突，不是措辭偏差。
- **最小修復：** `:329` 的職責欄補入 `bridge_session_hmac`（置於 `principal_hmac` 之後，與 `:1433` 同序），並補一句說明其語義（由已認證 bridge session 綁定，允許為 NULL 表示非 bridge 來源的 CLI/嵌入 handle）；順帶在 ADR-M2-05 的「統一入口」段（`:531`）補一句「bridge 路徑的 session 綁定 identity 由 M2-08 `resolve_session_compiler_identity` 解析，持久化於 `memory_compiler_identity.bridge_session_hmac`」，使 ADR 層留下該契約的痕跡（目前 `rg` 在 ADR 區段零命中）。

### P1-3　`bridge_session_hmac` 只有「解析」端、沒有任何卡擁有「簽發/綁定」端；且 `resolve_session_compiler_identity` 的 `principal` 參數在 bridge 平面無可信來源

- **位置：** `plan-20260819.md:996`（M2-16C AC3）、`:2038`（M2-08 AC5）、`:2391`（M2-13 identity 授權契約）、`:531`（ADR-M2-05 統一入口）
- **問題：** 兩個缺口：
  1. **無簽發者。** `:996` 要求 bridge「經 M2-08 `resolve_session_compiler_identity(session_id, principal, repo, worktree)` **取得** registry identity handle（`bridge_session_hmac` 綁定）」，`:2038` 把該方法定義為「由已認證 bridge session **解析**」——兩處都是 lookup 語義。全計劃唯一的簽發面是 `register_compiler_identity`（`rg -n 'register_compiler_identity'` 命中 `:465`/`:531`/`:2038`×2/`:2390`/`:2391`/`:2393`），而 `:2391` 明確把它綁定到「**已認證的本地 repo principal**（由 vault/config 派生）」，由操作者面 `libra memory identity register` 一次性輸出 handle；沒有任何 AC、寫集或 VER 描述誰、在何時為一個 bridge session 寫入 `bridge_session_hmac` 行。M2-16C 的寫集（`:1036`）不含 `src/internal/ai/memory/**` 並自述「只呼叫 M2-08 的 `observe_terminal` API」，故無法自建；M2-08 早於 M2-16C 交付且其 VER 矩陣 `job_crash_recovery_retention_and_identity_matrix` 只覆蓋 `CompilerIdentityV1` 六態，不含 bridge session 註冊。結果：`resolve_session_compiler_identity` 永遠查不到行，bridge 帶 `proposal` 的 `memory.episode.record` 全部被穩定拒絕，`:1030` 的 `bridge_session_bound_identity_matrix` 的「有效 bridge session → 成功取得 handle」分支無法為真。
  2. **`principal` 無可信派生。** 實測 `rg -n 'principal' src/internal/ai/agent_bridge/` **零命中**；`BridgeContext` 僅 `{conn, repository_id, worktree_id}`（`src/internal/ai/agent_bridge/ingress.rs:22-26`）；`agent_bridge_session` 表僅 `bridge_session_id/source/repository_id/worktree_id/workspace_id/parent_session_id/agent_id/session_state/…`（`sql/migrations/2026081801_agent_bridge_capture.sql:13-29`），無 principal 欄。計劃把 `principal` 列為該方法的入參卻未規定其派生源，也未像 CLI 側 `:2391` 那樣寫明「不得自報、與已認證 principal 不符即穩定拒絕」。
- **為何重要：** ADR-M2-05（`:531`）與 ADR-M2-12（`:597`）把「`compiler_identity` **只由認證 ingress 或進程內 registry 填充**，請求體自填一律拒絕」定為 trust gate 的授權根；M2-07 AC7（`:1954`）據此要求「allowlist 只接受由認證 ingress/session 或進程內 registry 解析的 `CompilerIdentityV1`」。缺口 (1) 讓該 ADR 宣稱的「可選 host 面」在帶 proposal 時**功能上不可達**（Codex r29 P1 要求的正是「簽發**或**解析的具體協議和持久化所有者」，本輪只補了解析半邊）；缺口 (2) 則把唯一剩下的可行實作推向「由 host 在 handshake 自報 principal」——那恰好是 r23 為 CLI `--principal` 關閉過的自報通道，會讓任意 bridge peer 憑自選 principal 取得 identity handle、令 proposal 直達 `Confirmed`，直接擊穿 trust gate 的授權邊界。
- **最小修復：** 在 M2-08 AC5（`:2038`）指明簽發端：`register_compiler_identity` 接受可選 `bridge_session` 綁定參數（寫入 `bridge_session_hmac`），並規定只能由已認證 ingress 在 handshake 時以 `BridgeContext` 的 `repository_id`/`worktree_id` 與**與 CLI 同源的 vault/config 派生 principal** 呼叫，請求體提供的任何 principal/identity 一律拒絕；在 M2-16C AC3（`:996`）把「取得」改寫為「握手時 register-on-first-use + 後續 resolve」並把該呼叫加入 AC；`bridge_session_bound_identity_matrix`（`:1014`/`:1030`）補「握手簽發 → 重啟後解析一致 → 自報 principal 被拒」三分支。若採此修復，`:1032` 的謂詞分組與 `:790` 分子需按 P1-1 同批更新。

---

## P2（5 條）

### P2-1　review log Scope 欄輪次筆誤第五次復發（`R30-r29/r35`），且直接違反同檔本輪新增的機械檢查

- **位置：** `plan-20260819.md:2861`、`:2862`；規則行在 `:798`
- **問題：** 兩行 r30 條目的 Scope 欄寫「R30-r29/**r35** 修订后的完整计划（r30 期間凍結）」；本計畫不存在 r35 輪。`:798` 本輪新增的「**Review log 機械檢查：** Round 欄輪次號必須等於 Scope 欄所寫修訂版之輪次 + 1（例：r30 行的 Scope 必須寫 R30-r29）」正是為防止此類筆誤而加，卻在同一次編輯中被該表最後兩行違反。這是同欄位第五次（r26 修 `r31`、r28 修 `r33`、r29 修 `r34`、本輪 `r35`）。
- **為何重要：** review log 是雙 PASS 的唯一審計軌跡；輪次筆誤讓收口時無法把被審 sha256 對回正確輪次。新加的機械檢查若首次應用即失效，該檢查本身也失去可信度。
- **最小修復：** 兩行改為「R30-r29 修訂後的凍結版（sha256 `b78d3c89…`）」。

### P2-2　M2-08 AC5 在同一條目內給出兩個 resolve 方法名，且枚舉的 `MemoryRuntime::{…}` 集合漏掉 M2-13 CLI 路徑所需的 handle resolver

- **位置：** `plan-20260819.md:2038`（M2-08 AC5）；對照 `:2390`（M2-13 record）、`:2397`（M2-13 adapter seam allowlist）
- **問題：** `:2038` 前段枚舉 `MemoryRuntime::{register_compiler_identity,resolve_session_compiler_identity,revoke_compiler_identity,list_compiler_identities}`（4 個），後段又寫「本卡 `runtime.rs` 提供 `register_compiler_identity`/`resolve_compiler_identity`（簽發/驗證/撤銷……`resolve` 檢查 `expires_at`/`revoked_at`……）」。`resolve_compiler_identity`（依 opaque handle 驗證，CLI `--compiler-identity` 必需）不在前段集合內，`resolve_session_compiler_identity`（依 session 解析，bridge 必需）不在後段。判據塊 AC5-⑦ 只籠統寫「`register/resolve/revoke/list` identity」，無法判別是 4 個還是 5 個方法。
- **為何重要：** `:2397` 的 M2-13 seam allowlist 是 `rg` 可核對的封閉集合；若 M2-08 交付的方法集與之不符，M2-13 的 adapter AC 將無法通過。同一條 AC 內兩份不同的 API 集合也讓 `prod-files=12` 的 `runtime.rs` 交付範圍不可機械判定。
- **最小修復：** 把 `:2038` 前段集合補為五元 `{register_compiler_identity, resolve_compiler_identity, resolve_session_compiler_identity, revoke_compiler_identity, list_compiler_identities}`，後段改為引用該集合；AC5-⑦ 明寫「五個方法」並對應 `:2037` 的 34 條算式（若因此增加謂詞，需同批改 `:789`/`:2094`/`:814`）。

### P2-3　`EX-M2-06` 判據塊 B7–B10 與 Verification checkbox 位序錯開（B10 對應第 8 個 checkbox）

- **位置：** `plan-20260819.md:1030`（B10）對比 `:1014`（第 8 個 checkbox）；`:1026-1029`（B7–B9）對比 `:1015-1017`（第 9–11 個 checkbox）
- **問題：** 集合上確為 11↔11 的 1:1（判據塊首行如此宣稱），但 B0–B6 與 checkbox 1–7 同序後，B7/B8/B9 對應 checkbox 9/10/11，而 B10 對應 checkbox 8。r29 P2-3 剛把 B4/B5 的同類錯位修好，本輪新增 B10 時把它追加到列末而非插入第 8 位，重新引入同一問題。
- **為何重要：** 收口時逐條對照需人工跳行；位序錯開正是 P1-1 那種「增門只改一處」的成因。
- **最小修復：** 把 `:1030` 的 B10 改編號為 B7 並上移至現 B7 之前，其餘順延（原 B7→B8、B8→B9、B9→B10），使 B0–B10 與 checkbox 嚴格同序。

### P2-4　M2-08 判據塊 AC1-1 仍寫「僅 `TerminalObservationV1` 觸發」，與 AC1 checkbox 的 `MemoryRecordEnvelopeV1` 入口語義不一致

- **位置：** `plan-20260819.md:2040`（`AC1 觸發集合精確性（2）：AC1-1 僅 `TerminalObservationV1` 觸發`）對比 `:2034`（AC1 checkbox：「外部事实触发集合精确为经校验的 `MemoryRecordEnvelopeV1`」）
- **問題：** r07/r08 已把全鏈入口語義統一為 `MemoryRecordEnvelopeV1`（`observation` 必填、`proposal` 可選、proposal-only 拒絕），AC1 checkbox 也已同步，但 `EX-M2-09` 的謂詞還原文本仍停留在舊的 `TerminalObservationV1`。測試矩陣 `:2663` 已寫 `MemoryRecordEnvelopeV1` 觸發集合。
- **為何重要：** 判據塊是 waiver 的還原依據，其措辭決定第 1、2 條謂詞怎麼被驗收；舊型名會讓實作者誤以為 AC1 只需驗證裸 observation，漏掉 proposal-only 拒絕這條已在 ADR-M2-05/12 固化的門。
- **最小修復：** `:2040` 改為「AC1-1 僅經校驗的 `MemoryRecordEnvelopeV1`（`observation` 必填、proposal-only 拒絕）觸發」。

### P2-5　ADR-M2-05 與 §0.3 未記錄 bridge session→registry 的解析契約，架構層對 r30 新增的安全邊界零痕跡

- **位置：** `plan-20260819.md:531`（ADR-M2-05 統一入口段）、`:63`（§0.3 第 3 條 bridge 路徑）
- **問題：** `:531` 只寫「bridge 在呼叫 `observe_terminal` 前把 session 綁定的 identity 寫入 envelope（呼叫方不可覆寫）」，未提 `resolve_session_compiler_identity`、`bridge_session_hmac` 或其持久化所有者；`:63` 完全未提 identity。`rg -n 'resolve_session_compiler_identity|bridge_session_hmac|bridge_session_bound_identity_matrix'` 在 `:480-660` 的整個 ADR 區段零命中。
- **為何重要：** ADR 是「為何這樣設計」的單一記錄；把一條授權邊界的機制只寫在任務卡 AC 裡，會讓後續 Revisit（ADR-M2-05 的 Revisit 條件正是「bridge protocol 變更 terminal observation 語義」）漏掉這條契約。本輪提示亦明確要求該 `rg` 同時命中 ADR。
- **最小修復：** 在 `:531` 的 CLI 身份來源句之後補一句 bridge 身份來源（引用 P1-2/P1-3 的最小修復文字），並在 `:63` 的 bridge 路徑要點末補「identity 由已認證 session 經 M2-08 registry 解析，請求體自填拒絕」。

---

**總計：P0=0，P1=3，P2=5。**

本輪在結構性維度（25 卡逐欄、DAG、表格機械列數與 514 行全覆蓋、審計表物理空行、`**Verification:**`/`**Dependencies:**` 空行、寫集/`prod-files` 逐檔點算、8 個 `EX-*` 的分子與 waiver 行、倉庫事實基線/版本面/CI job 集合/migration tip）**全部通過機械核對**；r29 的 Claude P1-1 與三條 waiver 理由 P2 已關閉。未關閉的是 Codex r29 P1 的另一半（bridge identity 的簽發端與 principal 來源，P1-3）與它在規範層留下的兩處不一致（表職責八列 P1-2、判據算式 32≠33 P1-1）。

VERDICT: FAIL
EXIT=0
