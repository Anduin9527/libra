# plan-20260819 R30-r31 review — Claude（評審期間凍結：plan sha256 e1b87f8b…、template 49f94da3…）
# reviewer: claude -p --effort xhigh --allowedTools Read Grep Glob (read-only, prompt via stdin)
# date: 2026-09-22T04:05:28+08:00
e1b87f8b109bcec1623b5a2ebe75f252b6a2381210dbd672a2df8df5ea29c250  docs/development/plan/plan-20260819.md
49f94da35c8828e72caaae52006f52c84c72647016dbce188cad77715b2f7f4f  docs/development/plan/plan-template.md
# review prompt: plan-20260819.review/review-prompt-r31.md

## plan-20260819 R30-r31 只讀評審（Claude）

**被審版本（凍結）：** plan `e1b87f8b109bcec1623b5a2ebe75f252b6a2381210dbd672a2df8df5ea29c250`、template `49f94da35c8828e72caaae52006f52c84c72647016dbce188cad77715b2f7f4f`、plan-status `e38a5fa7d63f…`（逐字等於 `r30-r31-sha256.txt`；進出場各 `sha256sum` 一次，未變）。**全程只讀，未修改倉庫任何檔案。**

### 一、r30 findings 逐條關閉核對

| r30 finding | 關閉？ | 實測證據 |
|---|---|---|
| Codex P1：bridge 認證簽發面缺失 | **部分**（見 P1-2） | `:997` 已補「已認證 ingress 在 handshake 時以與 CLI 同源的 vault/config 派生 principal 呼叫 `register_compiler_identity(principal, bridge_session=Some(...))`…`resolve_session_compiler_identity(session_id, repo, worktree)` 僅查驗、不創建」；`principal` 已從 resolve 簽名移除。**但沒有任何卡交付該 principal 派生**（見 P1-2） |
| Claude P1-1：`EX-M2-07` 算式 32≠33 | ✅ | `:1032` 新增 `AC3d bridge session 簽發/解析(6)`，逐項相加 `2+4+9+1+3+6+2+2+2+5+1+1 = 38`；`:792` waiver、`:802` 全域口徑、`:1066` Granularity、`:828` 審計表全為 `38/8@EX-M2-07`；`rg -n '33/8@EX-M2-07\|M2-16C=33'` 僅命中 `:454` 歷史行 |
| Claude P1-2：表職責 `memory_compiler_identity` 七欄 | ✅ | `:329` 已補 `bridge_session_hmac`（置於 `principal_hmac` 後，含「NULL 表示 CLI/嵌入來源」語義），與 `:1434`/`:1448` 八列、`:2039` 一致 |
| Claude P1-3：簽發端缺失 / `principal` 無可信來源 | **部分**（見 P1-2） | 簽發端已指定（`register_compiler_identity(..., bridge_session)`、M2-08 `runtime.rs`，DAG `M208→M216C` 順序正確）；`principal` 的來源只寫到「vault/config」層級，無 owner/檔案/AC/VER |
| Claude P2-1：review log 輪次筆誤 | ❌（見 P2-1） | `:2861`/`:2862`（r30 行）已改「R30-r29 修訂後的凍結版（sha256 `b78d3c89…`）」；但新增的 `:2862`/`:2863`（r31 行）寫「R30-r30/**r36** 修订后」——同欄位第六次同類筆誤 |
| Claude P2-2：M2-08 AC5 兩套 API 集合 | ✅（判據塊除外，見 P2-4） | `:2039` 前段已補為五元 `{register_compiler_identity, resolve_compiler_identity, resolve_session_compiler_identity, revoke_compiler_identity, list_compiler_identities}` 並明寫「五方法」 |
| Claude P2-3：B7–B10 與 checkbox 位序錯開 | ✅ | `sed -n '1007,1017p'` 的 11 個 checkbox 與 `:1020-1030` 的 B0–B10 **逐條同序**（B6=approval、B7=`bridge_session_bound_identity_matrix`、B8=`bridge_observation_idempotency`、B9=docs rg、B10=零直寫守衛） |
| Claude P2-4：`EX-M2-09` AC1-1 舊型名 | ✅ | `:2045` 現為「AC1-1 僅經校驗的 `MemoryRecordEnvelopeV1`（`observation` 必填、proposal-only 拒絕）觸發」 |
| Claude P2-5：ADR/§0.3 無契約痕跡 | **部分**（見 P2-2） | ADR-M2-05（`:532`）已補完整簽發/解析契約；**§0.3（`:40-51`）仍零命中**，但 `:453` 修訂歷史自述「ADR-M2-05/§0.3 補契約痕跡」 |

### 二、本輪機械核對（r31 指定項）

- **計數自洽：** `:1032` 算式逐項相加 = **38**（含 `AC3d(6)`），與 `:792`（waiver 理由「共 38 條」且括號含「**bridge session 簽發與解析**」）、`:802`（`M2-16C=38`）、`:1066`、`:828` 五處一致；`VER=11/8@EX-M2-06` 四處一致，`:1019` 判據首行「11 條 VER 門（與本卡 Verification 的 **11** 個 checkbox 一一對應）」實測 11↔11 ✓。`EX-M2-06` 理由（`:793`）含 approval/`ActionClass`。
- **識別面 `rg` 覆蓋：** `rg -n 'bridge_session_hmac|resolve_session_compiler_identity|register_compiler_identity'` 命中 `:329`（表職責八欄）、`:532`（ADR-M2-05）、`:997/:1014/:1030`（M2-16C AC+VER+B7）、`:1434/:1448`（M2-02 A1/VER）、`:2039`（M2-08 AC5 五方法）、`:2391/:2392/:2394`（M2-13）——**唯獨 §0.3（`:40-51`）零命中**。
- **`EX-M2-09` AC5-⑦：** `:2044` 仍為「⑦ `register/resolve/revoke/list` identity」，未寫「五方法」（見 P2-4）。
- **可複製命令零 `\|`：** `rg -n '`[^`]*\\\|[^`]*`'` 僅 2 命中（`:455`、`:2856`），兩者皆為**描述該次修復本身**的字面量，非可執行命令 ✓。
- **表格機械列數：** 以「恰 N 個 `|` 且以 `|` 開頭」分桶：N=3→105、N=4→116、N=5→86、N=6→152、N=7→12、N=9→19、N=16→27，合計 **517 = `rg -c '^\|'` 的 517**，無其它寬度；扣除 `:455`/`:2856` 兩行 `\|` 的 +1 偏移後真實分佈 N=6→154、N=7→10，與 r30 的 151/10 相差 +3 恰為本輪新增的 1 條修訂歷史行 + 2 條 review log 行 ✓。waiver 表與依賴表各 8 列、審計表 27 行全 16 格 ✓。
- **審計表末行空行（位元組核對）：** `sed -n '831,835p' | cat -A` → `:832` M2-15 資料行、`:833` 純 `$`（物理空行）、`:834` `**Verification 新增标记：**`；`rg -n '^\|.*\|\*\*'` 零命中 ✓。
- **空行統一：** 多行 `rg -U '^\*\*Verification:\*\*\n\S'` 與 `rg -U '\S\n\*\*Dependencies:\*\*'` 均零命中（25 處皆合規）✓。
- **25 卡逐欄一致：** 25 條 `**Granularity:**` 與審計表 `:808-832` 的 `type/recovery/complete/self-contained/AC/VER/landing/prod-files/scope/deps/writeset/release/split-from/exception` 逐格比對一致（`axis` 欄有 8 處分隔符/措辭差異，見 P2-5）；AC/VER 分子與實際 checkbox 逐卡點算一致（M2-02 9/9、M2-16C 10 checkbox→38 謂詞/11、M2-13 15 VER 拆分 command_test 7 + lib 2 + memory_episode_test 2 + compat 4、M2-05 10 VER、M2-08 8 VER）；`prod-files` 與寫集逐檔點算一致（M2-05=17、M2-08=12、M2-02=11、M2-16C=7、M2-13=5、M2-01K=6）；G-04 上限全部滿足，唯一超限者 M2-05 帶 `L-exception:EX-M2-03`。
- **DAG 與 `Dependencies` 逐邊比對：** 無環、無缺邊（`M216A→M201`、`M207→M216B→M208`、`M208→M216C`、`M212→M216C`、`M216C→M213`、`M216D→M216D1`、`M216D/M214C→M216E`、D1 條件虛線邊）✓；Phase 0 不含 M2-16B（`:748` 在 Phase 2）✓；完成判據「24 张收口卡 + M2-16D1 條件性」點算一致 ✓。
- **waiver 集合：** `EX-M2-01..07,09` 共 8 筆、8 列；`EX-M2-08` 僅存於歷史行（已撤回）✓。
- **plan-status：** `:46` 含 r18–r30 全部輪次與計數（r30 Codex 0/1/1、Claude 0/3/5，與 log 一致），`r31 待跑` ✓；`:201` M2-16A 為 `blocked`、禁止開工 ✓。

### 三、倉庫事實基線（核對日成立）

`Cargo.toml version = "0.23.36"`；版本面 = 三處（`tests/compat/version_surface_sync.rs:11-13`「now: `Cargo.toml`, `install.sh`, `install.ps1`」）；migration tip = `2026091901_operation_boundary_claim_columns`（`src/internal/db/migration.rs:1862-1865`，`sql/migrations/` 末檔一致）；`base.yml` job 集合 = `format/clippy/owner-liveness-macos/redundancy/test/network-remotes/opencode-export-linux`（7，與 DEP-M2-CI-02 逐字一致）；`release.yml` = `build-and-upload/upload-install-scripts/update-homebrew-tap/verify-homebrew-formula/request-stable-manifest`（5，與 DEP-M2-CI-03 一致）；`src/internal/ai/agent_bridge/authorization.rs:19 pub enum ActionClass`、`:32 pub fn classify`；`protocol.rs:532 allowlist_has_exactly_20_methods`、`tests/agent_bridge_protocol_test.rs:15 protocol_v1_allowlist_is_exactly_20_methods`（M2-16C 的 20→22 前提成立）；`ingress.rs:22-26 BridgeContext { conn, repository_id, worktree_id }`、`rg -c 'principal' src/internal/ai/agent_bridge/` = **0**；`rg -ln 'principal' src/` 只命中 `session/jsonl.rs`、`observed_agents/opencode_export.rs`、`vault.rs`（SSH cert 的 `valid_principals`）、`hardening.rs`，**無任何「已認證 repo principal」提供者**。模板 v2.8 的 `web/`/`worker/` 歷史註記（`:198/:228-230/:251/:262/:339/:443/:703`）、`:251` 的 `opencode-export-linux` 示例、`:262` 單一 `sql/**` 均成立。全計劃對 `providers`/`context_budget`/`prompt`/`client`/`mcp`/`orchestrator`/`web`/`operation_wrapper`/`test-provider` 的引用全部落在 §0.1 歷史表、ADR Context/Alternatives、禁止清單或零命中守衛，**無執行性依賴** ✓。

**證據限制：** `$SRC_CLONE`（`/run/media/genedna/data/libra-pr456`）在本次沙箱工作目錄策略之外，無法執行 `rev-parse`/`merge-base`/158-path 計數；`DEP-M2-SRC-01`（`:714`）已規定「來源 clone 不可用即 `Lifecycle=blocked`」並要求雙評審至少一輪在可訪問 clone 的環境歸檔，故不計為 finding，但本輪不構成該項證據。

---

## P0（0 條）

無。

---

## P1（2 條）

### P1-1　`memory_compiler_identity` 的 retention/prune 無人擁有：M2-02 明文把它交給 M2-08，M2-08 只接了 `memory_terminal_observation`；而本輪把簽發改成「每個 bridge handshake 一行」，使這張自稱「有界本地运行状态」的表實際無界

- **位置：** `plan-20260819.md:329`（表職責）、`:1425`（M2-02 AC3 的交接句）、`:1473`（M2-02 Security）、`:2039`（M2-08 AC5）、`:2044`（`EX-M2-09` 判據塊 AC5 明細）、`:997`（M2-16C AC3d 的 handshake 簽發）
- **問題：** `:1425` 明寫「本卡只驗證遷移可獨立檢查的 schema/redaction 合同（含 `memory_compiler_identity` 表結構），**retention/prune 與 identity 簽發/驗證行為由 M2-08 承接**」，`:1473` 重複同一交接。但 M2-08 的 AC5（`:2039`）只寫「**`memory_terminal_observation`** 的 retention/prune 由本卡 `job_sql.rs` 拥有：每 root 最近 N=8 generation、TTL 30 天、每仓 ≤10,000 行…」，identity 部分只有「`resolve` 檢查 `expires_at`/`revoked_at`，過期/撤銷/跨 repo-worktree/未知 handle 一律拒絕」——那是**讀時拒絕**，不是**刪行**。實測 `rg -c 'prune'` = 18 處，逐條檢視全部落在 `memory_terminal_observation` 或 `context_selection_receipt`，**無一處針對 `memory_compiler_identity`**；`:2044` 的 AC5 明細 ⑧⑨⑩⑪⑫⑬ 也全部是 observation 的 retention/admission 謂詞。同時 `:329` 把該表分類為「有界本地运行状态」，卻只列 `issued_at/expires_at/revoked_at` 三個時間欄，沒有任何 TTL 裁剪、每倉行數上限或 prune 事務語義——這是該分類下**唯一**沒有量化邊界的表（對照 `memory_terminal_observation` 的 N=8/30 天/10,000、`context_selection_receipt_retention` 的 30 天/10,000、`memory_compile_job`/`memory_compile_observer_state` 的「每 root/每 ref 一行」硬約束）。
- **為何重要：** r30 之前 identity 行只由操作者顯式執行 `libra memory identity register`（`:2392`）產生，頻率極低，無界尚可容忍；**本輪新增的 `:997` AC3d 把簽發改成「已認證 ingress 在 handshake 時呼叫 `register_compiler_identity(principal, bridge_session=Some(...))`」**，即**每一條 bridge session 落一行**，且 `:1030` 的 `bridge_session_bound_identity_matrix` 還要求「重啟後同一 session 解析一致」（行必須跨重啟保留，不能靠進程生命週期回收）。於是 `.libra/libra.db` 會隨 agent 會話數單調增長，既沒有刪除路徑，也沒有任何具名回歸會發現它；`revoke` 只寫 `revoked_at`，同樣不刪行。這是一條由本輪補丁直接引入的、寫在 SQLite 本地庫上的無界增長面，且跨了一個**已在計劃正文裡宣告卻沒有接收方**的所有權交接——正是本系列反覆治理的「交接只寫一半」失效模式（比照 `job_sql` 交接、entity 所有權、`mod.rs` 註冊三次修訂）。
- **最小修復：** 在 M2-08 AC5（`:2039`）把 identity 的 retention/prune 明確納入本卡 `job_sql.rs`（例如：`revoked_at`/`expires_at` 過期後保留 TTL 30 天、每倉 ≤N 行、原子事務裁剪、被任何未處理 job 或未過期 snapshot 引用的 handle 不可刪、prune fail-closed），並在 `job_crash_recovery_retention_and_identity_matrix` 增加對應分支；同步 `:329` 的表職責欄寫出該邊界、`:1425`/`:1473` 的交接句指向具體條款；因 AC5 謂詞數增加，需同批更新 `:2044` 的 AC5 明細與算式、`:789` 的 `EX-M2-09` waiver 分子、`:2095` Granularity 與 `:814` 審計表的 `34/8@EX-M2-09`、`:802` 的全域口徑。若選擇另一路線（handshake 簽發改為 `(repository_id, worktree_id, bridge_session_hmac)` 冪等 upsert + 上述 TTL），仍須把邊界與具名回歸寫進同一組欄位。

### P1-2　`register_compiler_identity(principal, …)` 所消費的「已認證本地 repo principal」沒有任何卡交付：無 owner、無檔案、無 AC/VER，唯一錨點「與 CLI 同源」在 DAG 上指向**下游**的 M2-13，而倉庫今日無此表面

- **位置：** `plan-20260819.md:997`（M2-16C AC3d）、`:532`（ADR-M2-05 統一入口）、`:2392`（M2-13 identity 授權契約）、`:2039`（M2-08 AC5）、`:1375`（M2-01K 寫集）
- **問題：** 三處都把 principal 的來源寫到「vault/config」這一層就停住：`:997`「以**與 CLI 同源**的 vault/config 派生 principal 呼叫 `register_compiler_identity(...)`」、`:532` 同句、`:2392`「綁定**已認證的本地 repo principal**（由 vault/config 派生；缺省即該 principal，顯式 `--principal` 與之不符時穩定拒絕）」。實測：
  1. **倉庫無此表面。** `rg -ln 'principal' src/` 只有 4 個檔案——`src/internal/ai/session/jsonl.rs`、`src/internal/ai/observed_agents/opencode_export.rs`、`src/internal/vault.rs`（`:374` 是 SSH 憑證的 `"valid_principals": ["git"]`，與此無關）、`src/internal/ai/hardening.rs`（redaction 名單）；`rg -c 'principal' src/internal/ai/agent_bridge/` = **0**；`BridgeContext` 只有 `{conn, repository_id, worktree_id}`（`ingress.rs:22-26`）。
  2. **無卡交付。** 25 張卡的 `Implementation write set` 中，唯一觸及 `src/internal/{vault.rs,config.rs}` 的是 M2-01K（`:1375`「最小加密配置 helper」），而 M2-01K 的 8 條 AC 全部圍繞 keyed-digest seed 的生成/域分離/丟失語義，**無一條**提到派生 principal；M2-08 的 `register_compiler_identity` 把 principal 當**入參**；M2-16C 寫集（`:1040`）只有 `agent_bridge/*` + `command/agent/bridge.rs` + `utils/error.rs`；M2-13 寫集（`:2424`）只有 `cli.rs`/`command/memory.rs`/`command/mod.rs`/`utils/{error,output}.rs`。
  3. **錨點方向倒置。** 「與 CLI 同源」裡的 CLI 指 M2-13，而 DAG（`:697`）是 `M216C → M213`——M2-16C 執行時 M2-13 尚未交付，無從「同源」。
- **為何重要：** 這是整條 trust gate 授權鏈的**根輸入**：`compiler_identity` 的可信性完全建立在「principal 由服務端派生、請求體不可自報」（`:532`、`:597` ADR-M2-12、`:1954` M2-07 AC7）。執行到 M2-16C 時，實作者只有三條路：(a) 自行在 `ingress.rs` 發明一套派生，M2-13 再發明第二套——兩套若不同源，`:2392` 的「顯式 `--principal` 與之不符時穩定拒絕」就沒有可比對的基準，`memory_identity_register_rejects_forged_principal`（`:2392`/`:2413`）也失去可判定的「正確值」；(b) 超出寫集去改 `vault.rs`/`config.rs`（違反 G-10 寫集聲明）；(c) 取用 `BridgeContext` 之外唯一帶身份色彩的材料——請求體的 `agent_id`/`workspace_id`（`sql/migrations/2026081801_agent_bridge_capture.sql:13-29` 的 `agent_bridge_session` 欄位、`session.open` 直接採納請求值）——那正是 r23 為 CLI `--principal` 關閉、`:997` 自己又明令禁止的自報通道。另外模板 G-05（`plan-template.md:446`）明文禁止「見上文／同上一卡式跨卡隱式約定」，要求「確屬跨卡共享的約定要提升為全局工程約束或 ADR」——目前 ADR-M2-05 裡的措辭與卡內完全相同，同樣沒有落到可執行的 owner。
- **最小修復：** 指定唯一 owner 並落到可機械核對的三件事：在 **M2-01K**（已擁有 `src/internal/{vault.rs,config.rs}` 的最小 helper 與 `keyed_digest.rs`，且位於 Phase 0，早於所有消費者）或 M2-07 `trust.rs` 的寫集中加入具名函數（如 `authenticated_repo_principal(repo, worktree) -> Result<Principal>`），寫明派生材料（哪個 vault 條目或 config key）、缺失時的穩定拒絕碼，配一條 AC 與一條具名 VER；然後把 `:997`、`:532`、`:2392` 的「與 CLI 同源的 vault/config 派生 principal」改為**直接引用該函數名**，並在 M2-16C/M2-13 的 `Dependencies` 顯式列入該 owner 卡。owner 卡的 `prod-files`/AC/VER 分子須與 waiver 行、Granularity、審計表同批更新。

---

## P2（5 條）

### P2-1　review log Scope 欄輪次筆誤第六次復發（`R30-r30/r36`），再次違反同檔 `:798` 的機械檢查

- **位置：** `plan-20260819.md:2862`、`:2863`；規則行 `:798`
- **問題：** r31 兩行的 Scope 寫「R30-r30/**r36** 修订后的完整计划（r31 期間凍結）」；本計畫不存在 r36 輪。`:798`「**Review log 機械檢查：** Round 欄輪次號必須等於 Scope 欄所寫修訂版之輪次 + 1（例：r30 行的 Scope 必須寫 R30-r29）」正是 r30 為此新增；r30 兩行（`:2861`/`:2862` 前身）已被修正為「R30-r29 修訂後的凍結版（sha256 `b78d3c89…`）」，但同一次編輯新增的 r31 兩行又寫錯（歷次：r26 修 `r31`、r28 修 `r33`、r29 修 `r34`、r30 修 `r35`、本輪 `r36`）。`rg -n 'r3[2-9]|r4[0-9]'` 在全文只命中這兩行（其餘為 `bc1be587e4`/路徑字串）。
- **為何重要：** review log 是雙 PASS 的唯一審計軌跡；輪次筆誤讓收口時無法把被審 sha256 對回正確輪次。新加的機械檢查連續兩輪在同一次編輯中被違反，該檢查本身也失去可信度。
- **最小修復：** 兩行改為「R30-r30 修訂後的凍結版（sha256 `e1b87f8b…`）」；建議把該檢查寫成可執行斷言（新增行的 Scope 欄輪次必須等於 Round 欄減一）而非純文字提醒。

### P2-2　§0.3 的 bridge 路徑仍無 identity 契約痕跡，與修訂歷史 `:453` 的自述不符

- **位置：** `plan-20260819.md:45`（§0.3 第 3 條 bridge 路徑）、`:46`（嵌入式路徑）；對照 `:453`
- **問題：** `:453` 修訂歷史自述「…**ADR-M2-05/§0.3 補契約痕跡**」，但實測 `rg -n 'bridge_session_hmac|resolve_session_compiler_identity|register_compiler_identity'` 的 11 處命中中**沒有任何一處落在 `:40-51`**；`:45` 仍只列 envelope 欄位後接「M2 只验证与编译」，`:46` 完全未提 identity。ADR-M2-05（`:532`）確已補齊，§0.3 未動。
- **為何重要：** §0.3 是「R30 架构决策（取代旧 Code-era 绑定）」的入口摘要，執行者與後續 Revisit 多從此處起讀；一條授權邊界只存在於 ADR 內文與任務卡 AC，而摘要層零痕跡，容易在下一次改 bridge 協議時被漏掉（ADR-M2-05 的 Revisit 條件正是「bridge protocol 变更 terminal observation 语义」）。更直接的問題是修訂歷史記錄了未發生的修改，破壞了該表作為修訂審計軌跡的可信度。
- **最小修復：** 在 `:45` 的 bridge 路徑要點末補一句「`compiler_identity` 由已認證 ingress 在 handshake 簽發並經 M2-08 registry 解析（`bridge_session_hmac` 綁定），請求體自填一律拒絕」；或把 `:453` 的自述改為只宣稱 ADR-M2-05。

### P2-3　M2-16C 寫集的括注「只呼叫 M2-08 的 `observe_terminal` API」與本輪新增的 AC3d 直接矛盾

- **位置：** `plan-20260819.md:1040`（Implementation write set 末括注）；對照 `:997`（AC3d）
- **問題：** `:1040` 結尾寫「（**只呼叫** M2-08 的 `observe_terminal(MemoryRecordEnvelopeV1)` API；不修改或重实现 `memory/{runtime,job}` 語義。）」，是一個封閉集合聲明；而 `:997` 的 AC3d 要求本卡再呼叫 `register_compiler_identity(principal, bridge_session=Some(...))` 與 `resolve_session_compiler_identity(session_id, repo, worktree)` 兩個 `MemoryRuntime` 方法。r30 的補丁只改了 AC，沒有回填寫集括注。
- **為何重要：** M2-13 的同型約束（`:2394` 的 adapter seam allowlist）是 `rg` 可核對的封閉集合；M2-16C 的這句括注扮演同樣角色。兩份規範對同一張卡給出不同的允許呼叫集，收口時無法機械判定 bridge 層是否越界。
- **最小修復：** `:1040` 括注改為「只呼叫 M2-08 的 `MemoryRuntime::{observe_terminal, register_compiler_identity, resolve_session_compiler_identity}`；不修改或重实现 `memory/{runtime,job}` 語義」。

### P2-4　`EX-M2-09` 判據塊 AC5-⑦ 仍寫四個名字，未同步 AC5 正文的「五方法」；`:453` 自述已同步

- **位置：** `plan-20260819.md:2044`（AC5 明細 ⑦）；對照 `:2039`（AC5 正文）、`:453`（修訂歷史）
- **問題：** `:2039` 已定案為五元集合並明寫「（五方法）」，但 `:2044` 的 ⑦ 仍是「`register/resolve/revoke/list` identity」——`resolve` 一詞同時要承擔 `resolve_compiler_identity`（依 handle，CLI 路徑必需）與 `resolve_session_compiler_identity`（依 session，bridge 路徑必需）兩個語義不同的方法。`:453` 修訂歷史記為「M2-08 **五方法集合與 AC5-⑦**」，但 ⑦ 實際未變（`list` 是 r23 加的）。
- **為何重要：** 判據塊是 `EX-M2-09`（34 條謂詞）被審批的還原依據；⑦ 若只還原出四個方法，收口時無法判定第五個方法是否在已審批的豁免範圍內，且 `:2394` 的 M2-13 seam allowlist 也依賴這個集合是封閉且一致的。
- **最小修復：** `:2044` 的 ⑦ 改為「⑦ `register/resolve(handle)/resolve_session/revoke/list` **五方法** identity seam」；若因此拆為兩條謂詞，需同批更新 AC5(13)、算式 34、`:789` waiver、`:2095` Granularity、`:814` 審計表與 `:802` 全域口徑。

### P2-5　審計表 `axis` 欄與 8 張卡的 `Granularity: axis=` 措辭不一致（`/` vs `+`、截斷）

- **位置：** `plan-20260819.md:810/811/812/822/824/825/826/831`（審計表）對照 `:1487/:1566/:1645/:2366/:2518/:2582/:905/:1269`
- **問題：** M2-02（`core projection/job schema` vs `core projection+job schema`）、M2-02F（`FTS5 build/schema capability` vs `…build+schema…`）、M2-02R（`receipt ledger/retention` vs `receipt ledger+retention`）、M2-12（`context injection/receipt` vs `context injection+receipt`）、M2-14（`benchmark/article` vs `benchmark+article`）、M2-14C（`scoped coverage/CI gate` vs `scoped coverage+CI gate`）、M2-16A（`domain-layer port + MemoryConfidence` vs `… + MemoryConfidence rebuild`）、M2-16E（`#456 closure / mapping` vs `#456 closure / migration map`）共 8 行不逐字相同。
- **為何重要：** R30-r27 曾以同一標準把 M2-03 的 `axis` 不一致列為 finding 並修正（`:2860` review log、`:459` 修訂歷史），此後審計表被當作「`G-*` 的機器可核對摘要」逐格比對；殘留 8 行意味著該欄仍需人工判讀，與已建立的比對口徑不一致。
- **最小修復：** 把這 8 行的審計表 `axis` 逐字改成卡內 `axis=` 的值（或反向統一），使該欄可用字串相等比對。

---

**總計：P0=0，P1=2，P2=5。**

本輪在結構性維度（25 卡逐欄、DAG 與 `Dependencies` 逐邊、表格 517 行機械列數全覆蓋、審計表物理空行、`**Verification:**`/`**Dependencies:**` 空行、寫集與 `prod-files` 逐檔點算、8 個 `EX-*` 的分子/waiver/判據塊/全域口徑四處一致、M2-16C 的 38/11 與 B0–B10↔11 checkbox 同序、倉庫事實基線/版本面/CI job 集合/migration tip/模板 v2.8）**全部通過機械核對**；r30 的 Claude P1-1、P1-2、P2-2、P2-3、P2-4 均已關閉。未關閉的是 Codex r30 P1 / Claude r30 P1-3 的剩餘半邊——本輪把 bridge identity 的**簽發呼叫點**寫清楚了，但沒有寫出它所消費的 principal 由誰交付（P1-2），也沒有為因此變成「每 session 一行」的 `memory_compiler_identity` 接上 M2-02 已宣告的 retention 交接（P1-1）。

VERDICT: FAIL
EXIT=0
