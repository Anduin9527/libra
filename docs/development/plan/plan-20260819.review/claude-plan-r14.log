# plan-20260819 R30-r14 review — Claude（session /exit 後重跑）
# reviewer: claude -p --effort xhigh --allowedTools Read Grep Glob (read-only, prompt via stdin)
# date: 2026-09-21T23:54:28+08:00
f57eed378b8b17bff5261461909f7cbdda5ec88c492cbca9b3743d0b33f436ff  docs/development/plan/plan-20260819.md
# review prompt: plan-20260819.review/review-prompt-r14.md

Review complete. Below are my findings against the r13-revised plan, template v2.8, and the live repository at `45ad1f7f2`.

## 一、r13 findings 逐条核对（实际核对命令与结果）

| r13 finding | 核对方式与结果 | 结论 |
|---|---|---|
| Codex P1-1 `mod.rs` 註冊所有權 | 逐卡读写集：M2-04 `:1709`、M2-06 `:1857`、M2-07 `:1943`、M2-08 `:2014`（含 `agent_bridge/mod.rs` 首建）、M2-09 `:2082`、M2-10 `:2148`、M2-11 `:2216`、M2-12 `:2285`、M2-16D1 `:1130` 均已含对应 `mod.rs`；`prod-files`/审计表同步 | ⚠️ **仅部分关闭**（M2-02/02F/02R/16D1 仍缺父模块注册，见 P1-1） |
| Codex P1-2 五个 `EX-*` 逐条门族枚举 | `:1387-1396`（M2-02 A1–A9+VER）、`:1465-1475`（M2-02F）、`:1544-1554`（M2-02R）、`:1766-1768`（M2-05 V1–V10 + A1–A8）、`:1914-1924`（M2-07 T1–T10）均已存在具名可复制命令；EX-M2-02 括注已从 `4+3+2+2=11` 改为 `command_test 2、lib 2、memory_episode_test 2、compat 4`=10 | ⚠️ **部分关闭**（分子与 waiver 行/审计表仍不一致，见 P1-3） |
| Codex P1-3 `CompilerIdentityV1` 授权语义 | `rg -n "CompilerIdentityV1"` → 仅 4 处：`:450`(修订历史)、`:579`(ADR-M2-12)、`:970`(M2-16C AC3)、`:1910`(M2-07 AC7)；M2-07 已有 `trust_gate_rejects_forged_compiler_identity`、M2-16C 已有 `bridge_record_rejects_mismatch_proposal_only_and_forged_identity` | ⚠️ **语义已写入，但型别无归属**（见 P1-4） |
| Codex P2-1 M2-13 `command_test` module | `:2365` 写集为「`tests/command/mod.rs`（新增 `command_test` module `memory_test`）」，VER 全走 `--test command_test` | ✅ 关闭 |
| Claude P1-1 四卡写集泛称 | `:1266`（M2-01 改「單元測試位於已具名的 `domain.rs`/`canonical.rs`/`validation.rs` 的 `#[cfg(test)]`」）、`:1639`（M2-03 改 `history.rs` 的 `#[cfg(test)] mod tests`）、`:2497`（M2-14C 已显式列 `Cargo.toml`+`integration-test-plan.md`）、`:2564`（M2-15 已列版本面集合三处） | ✅ 关闭（M2-03 遗留见 P2-2） |
| Claude P2-1 EX-M2-02 算术 | `:775` 现为 10 道门四分项，合计 10 | ✅ 关闭 |
| Claude P2-3 三卡 `Dependencies` 前空行 | `rg -n -B1 '^\*\*Dependencies:'` → 25 张卡无一被 checkbox 直接相邻 | ✅ 关闭 |
| Claude P2-8 M2-16C AC6 第二道守卫 | `:973` 已含 `allowlist_has_exactly_20_methods` 与 `tests/agent_bridge_protocol_test.rs::protocol_v1_allowlist_is_exactly_20_methods`；实测两处确存（`protocol.rs:532`、`agent_bridge_protocol_test.rs:15`） | ✅ 关闭 |
| Claude P2-4 review log 轮次笔误 | `:2764`/`:2766` 已修正；但新增的 `:2768-2769` 写「R30-r13/**r15** 修订后」 | ⚠️ 同型回归（P2-5） |
| Claude P2-5 修订历史重复行 | `:450`、`:451` **两行同为 R30-r13** | ⚠️ 同型回归（P2-4） |
| Claude P2-6/P2-7 M2-04/M2-08 计数 | M2-04 `prod-files=9`（写集 9 档）但 `:1713` 写「8 个以内」；M2-08 `prod-files=11`（写集 11 档）但 `:2018` 写「9 个以内」 | ❌ **反向未关闭**（P2-1） |
| Codex r13 隐含项：审计表末行分行 | `sed -n '803,808p' \| cat -A`：`805`=M2-15 数据行、`806`=`**Verification 新增标记：**`、`807`=`$` —— **两者之间无空行** | ❌ **回归**（P1-2） |

## 二、本轮实测通过的仓库事实

| 计划自述 | 核对命令 | 结果 |
|---|---|---|
| 版本面三处、`compat_version_surface_sync` 唯一权威 | `rg -n … tests/compat/version_surface_sync.rs` → `:13` 注释与断言只覆盖 `Cargo.toml`/`install.sh`/`install.ps1`；`Cargo.toml:3 = 0.23.36` | ✅ |
| `base.yml` 7 job / `release.yml` 5 job / CodeQL `analyze`+matrix | Grep `^  [a-zA-Z0-9_-]+:$` → `format/clippy/owner-liveness-macos/redundancy/test/network-remotes/opencode-export-linux`；`build-and-upload/upload-install-scripts/update-homebrew-tap/verify-homebrew-formula/request-stable-manifest`；`codeql.yml:21,44,46`；`base.yml` 恰 422 行 | ✅ 与 DEP-M2-CI-02/03、`:2541` 锚点完全一致 |
| migration tip `2026091901` | `ls sql/migrations/ \| tail` | ✅ `2026092101+` 重编正确 |
| 事实基线锚点 | `ai/mod.rs:53-68`（无 memory）、`src/internal/ai/memory` 不存在、`cli.rs:668-746`、`model/mod.rs`=48 行、`op.rs:1144 !name.starts_with("libra/")`、`Cargo.toml:66-70` sqlx-sqlite、`memory.md` 2392 行（`:498/621/638/981/1362/1959` 全部命中预期小节）、`redaction.rs` `RedactedBytes` | ✅ 全部成立 |
| 既有 target / 文档存在 | `tests/{db_migration_test,operation_restore_faults}.rs`、`Cargo.toml` 的 `compat_help_examples_banner`/`compat_command_docs_examples_section`/`compat_serial_registry`/`operation_restore_faults`、`help_examples_banner.rs:53 VISIBLE_COMMANDS`（硬编码）、`docs/{commands,commands/zh-CN,development/commands}/agent.md`、`deepseek-harness.md`、`tracing/agent.md` | ✅；`docs/commands/memory.md` 确不存在（M2-13 新建） |
| 25 卡逐栏一致性 | 逐行比对审计表 `:781-805` 与 25 张卡 `Granularity` 的 type/AC/VER/landing/prod-files/scope/deps/writeset/release/exception | ✅ 仅 `split-from` 一处不一致（P2-3）；VER/AC checkbox 实计与分子全部吻合 |
| 零命中守卫、裸 `git` | 全部守卫为模板 exit-code 三分支形式；`rg 'git (ls-tree\|grep\|merge-base\|diff\|rev-parse)'` 的活动行全部在 `$SRC_CLONE` 作用域 | ✅ |
| 旧 seam 残留 | `rg 'observe_terminal\(TerminalObservationV1\)'` → 零命中 | ✅ |

**沙箱限制（非计划缺陷）：** `$SRC_CLONE`（`/run/media/genedna/data/libra-pr456`）与 `../libra-backend` 在本会话被权限拒绝，故 merge-base `2ebd409…`、158 路径清单、13 份后端 `*.en.md` 未能复核；计划已把 pin 校验写成 M2-16E 的卡内先决命令（`:1178`、`:1191`），失败 fail loud，设计上可接受。

---

## P0（0 条）

无。

## P1（4 条）

**P1-1｜`mod.rs` 注册所有权仍有四处缺口，四张卡自己的 `--lib` VER 命令无法编译**

- **位置：** `plan-20260819.md:1413`（M2-02 写集）、`:1492`（M2-02F 写集）、`:1573`（M2-02R 写集）、`:1130`（M2-16D1 写集）；对应 VER `:1407`、`:1481`、`:1559`、`:1118`；模板 `plan-template.md:456-462`（G-10）。
- **问题：** r13 为关闭 Codex P1-1 给 M2-04/06/07/08/09/10/11/12/16D1 补了 `mod.rs`，但**漏了四处父模块注册**：① M2-02 首建 `src/internal/ai/memory/job_sql.rs`，写集无 `memory/mod.rs`；② M2-02F 首建 `memory/fts_sql.rs`，写集无 `memory/mod.rs`；③ M2-02R 首建 `memory/context/{receipt,receipt_store,mod}.rs`，只有 `context/mod.rs`，**没有把 `mod context;` 写进 `memory/mod.rs`**；④ M2-16D1 首建 `memory/adapters/agent_run.rs`，写集有 `memory/mod.rs` 但**没有 `adapters` 的模块根文件**（`adapters/mod.rs` 或 `adapters.rs`）。而 M2-16A 的 AC3（`:833`）明确要求 `memory/mod.rs` 的导出清单「只含本卡移植的領域層型別，下游卡各自在自己的寫集內擴充」。
- **为何重要：** Rust 不编译未被 `mod` 声明的文件。四张卡的 VER 会直接失败而非空跑：`cargo test --lib internal::ai::memory::context::receipt`（M2-02R `:1559`）解析不到 `memory::context`；`memory_episode_test observer_job_schema_transaction`（M2-02 `:1407`）、`fts_external_content_atomic`（M2-02F `:1481`）、`--lib internal::ai::memory::adapters`（M2-16D1 `:1118`）同理。这也是 G-10（不可豁免）的写集缺项，`prod-files` 随之失真。
- **最小修复：** 四处写集各加一行——M2-02、M2-02F、M2-02R 加 `src/internal/ai/memory/mod.rs`（`prod-files` 12→13、6→7、7→8），M2-16D1 加 `src/internal/ai/memory/adapters/mod.rs`（`prod-files` 2→3）；同步各卡 `Files likely touched` 与审计表 `:783/784/785/803`。

**P1-2｜粒度审计表末行与 `**Verification 新增标记：**` 之间的空行再次丢失（第四次回归）**

- **位置：** `plan-20260819.md:805`（M2-15 数据行）与 `:806`（`**Verification 新增标记：**` 段落）；实测 `sed -n '803,808p' docs/development/plan/plan-20260819.md | cat -A` → `806` 紧接 `805`，`807` 才是 `$`。
- **问题：** GFM 规定表格「在首个空行或另一个块级结构开始处结束」，紧随表格行的普通段落会被解析成**表格的又一行**（GFM spec 表格示例 205）。该段落是全计划 `(new)` 标记的唯一权威声明，现被吸进审计表内。
- **为何重要：** ① 审计表是模板要求「便于机械核对与脚本校验」的产物，多出一行非法数据行会让任何按列解析的校验脚本报错或静默错位；② `(new)` 标记段落一旦被当成表格单元渲染，25 张卡的 `(new)` 判定失去可读来源（模板「Verification 判定口径」要求新增 test fn 必须标 `(new)`）；③ 该项已在 R30-r09/r10/r12/r13 四轮被点名并「修复」过，本轮随 r13 的行号位移再次丢失，属确定性回归。
- **最小修复：** 在 `:805` 与 `:806` 之间插入一个空行；并在 review log 或修订历史注明该行是易回归点（每次改审计表后用 `sed -n '<last>,+2p' | cat -A` 复核）。

**P1-3｜三处 `EX-*` waiver 分子与卡内/审计表不一致，门族 waiver 准入条件④不成立**

- **位置：** waiver 表 `:775`（EX-M2-02）、`:773`（EX-M2-04）、`:772`（EX-M2-05）；对照卡内 `:2342-2344`、`:2391`、`:1914`、`:1969`、`:1766-1768`、`:1815`；审计表 `:796`、`:790`、`:788`；模板 `plan-template.md:530-532`（门族 waiver 准入条件 ③「分子如实写作 `n/上限@EX-ID`」、④「门族增减时同批更新 waiver 行与粒度审计表」）。
- **问题：** 三处数值互相矛盾：
  1. **EX-M2-02**：waiver 行写「AC/VER 分子如實寫 `10/8@EX-M2-02`」，但卡内 `:2342` 与 Granularity `:2391`、审计表 `:796` 的 AC 分子是 **`40/8@EX-M2-02`**（按独立谓词重算的结果，本身正确）。waiver 行把 AC 低报了 4 倍。
  2. **EX-M2-04**：waiver 行写「AC/VER 分子如實寫 `9/8@EX-M2-04`」，但 M2-07 判据规范 `:1914` 明写「9 條 AC + **10 條 VER**」，Granularity `:1969` 与审计表 `:790` 也是 `VER=10/8@EX-M2-04`。
  3. **EX-M2-05**：判据规范 `:1768` 的 AC 谓词分组为 `4+8+3+1+3+2+1+3`，实际合计 **25**，但该行结尾、waiver `:772`、Granularity `:1815`、审计表 `:788` 四处都写 **24**。
- **为何重要：** waiver 表是 AC/VER 越过 G-03 上限的**唯一合法依据**，模板判定规则要求审计表的 `@EX-ID` 与 waiver 行同时成立；分子不一致即准入条件③④不满足，M2-13/M2-07/M2-05 三张卡按模板判为「粒度不达标、不得开工」。这与 r13 Codex P1-2 是同一条款，本轮只关闭了枚举部分、没关闭数值部分。
- **最小修复：** `:775` 改为「AC 分子 `40/8@EX-M2-02`、VER 分子 `10/8@EX-M2-02`（10 道門：…）」；`:773` 改为「AC 分子 `9/8@EX-M2-04`、VER 分子 `10/8@EX-M2-04`」；EX-M2-05 二选一——把 A1–A8 分组合计改写为 25 并同步 `:772/1768/1815/788` 四处为 `25/8@EX-M2-05`，或重新归并某一组使其真为 24。

**P1-4｜`CompilerIdentityV1` 是 trust gate 的授权输入，却没有任何卡拥有它的首建、落点、版本门与解析路径**

- **位置：** `:579`（ADR-M2-12 Decision）、`:970`（M2-16C AC3）、`:1910`（M2-07 AC7）；对照 §0.4 移植所有权 `:64`、M2-01 AC4 版本门清单 `:1249`、M2-07 写集 `:1943`、M2-16C 写集 `:993`、M2-08 AC5 `:1994`。
- **问题：** r13 为关闭 Codex P1-3，在三处写入「授權由認證 ingress/session 或進程內 registry 解析的 `CompilerIdentityV1` 決定，`model_label` 僅顯示」，但这个新型别：① 不在 §0.4「移植所有權」的首建表里（`TerminalObservationV1`/`TerminalEvidenceRefV1`/`MemoryRecordEnvelopeV1`/`TerminalObservationFingerprintV1`/`EpisodeProposalV1` 全都有明确的首建卡与文件）；② 不在 M2-01 AC4 的 version-gate 清单里，而计划内其余每个 host-facing `*V1` 都在；③ **没有任何卡负责「认证 session → identity」的解析**：M2-16C AC3 自陈「本卡只做 bridge 側傳輸與入參校驗…語義級規則由 M2-08/M2-07 擁有」，而只有 bridge ingress 才持有已认证 session；M2-08 的 `with_compiler_model` 进程内注册 AC（`:1994`）也只讲 model 不讲 identity。
- **为何重要：** 这是自动信任门的授权边界（GC-M2-12 / ADR-M2-06）。按当前写法执行：M2-16C 交付的 bridge record 不会把认证身份带下去，M2-07 的 trust gate 就没有 allowlist 输入——要么所有 host proposal 永久 Quarantined（M2「自动 Episode」目标失效、完成判据 `:2802` 的必要门无法通过），要么实现者回落到 `model_label`（正是 r13 明令禁止的伪造面）。同时违反 G-05（卡必须在不读其它卡正文的前提下可执行）。
- **最小修复：** 在 §0.4 移植所有权补一行「`CompilerIdentityV1` → M2-07 `memory/trust.rs`（或 `evidence.rs`）首建」，并入 EX-M2-04 的门族与 M2-01 AC4 的 version-gate 清单；在 M2-16C 的 AC 与写集补「把已认证 bridge session 解析出的 `CompilerIdentityV1` 传入 `observe_terminal`（不接受请求体自填）」，在 M2-08 的 `with_compiler_model` AC 补「进程内注册同时登记 identity」；三处各保留已有的 forged-identity 回归。

## P2（7 条，不阻塞）

1. **`Files likely touched` 与 `prod-files` 在 7 张卡自相矛盾（r13「+1」修订的连带回归）。** `:1643` M2-03「3 个生产文件」vs `prod-files=4`；`:1713` M2-04「8 个以内」vs `9`；`:1861` M2-06「9 个以内」vs `10`；`:1947` M2-07「9 个以内」vs `10`；`:2018` M2-08「9 个以内」vs `11`；`:2086` M2-09「7 个以内」vs `8`；`:2220` M2-11「8 个以内」vs `9`。其中 M2-03（写集只有 3 个生产文件）、M2-09（写集 5 个）、M2-11（写集 7 个）的 `prod-files` 连写集都对不上。虽然模板 `:466` 声明 `Files likely touched` 只是估计值、并发判定以写集为准，但 `prod-files` 是 G-04 规模判定的输入，应可由写集机械复核。**修复：** 按写集重算七卡 `prod-files` 并同步 `Files likely touched` 与审计表（全部仍在 `M` 的 ≤12 内）。
2. **M2-03 写集具名了不存在的 `tests/ai_history_test.rs`，且把测试文件算进 `prod-files`。** `:1639` 写「`tests/ai_history_test.rs`（如既有 target 名不同，以開工日 `tests/INDEX.md` 為準）」——实测 `ls tests/ai_history_test.rs` 为 No such file；该卡四条 VER 全是 `--lib`，history 测试实际位于 `src/internal/ai/history.rs` 的 `#[cfg(test)]`。模板 G-04 明确「本卡自己的测试文件**不计入**」，但 `prod-files=4` 只有把这个幻影文件算进去才凑得出。**修复：** 删除该项或改为实际存在的 target，`prod-files` 改 3。
3. **M2-16D1 的 `split-from` 卡内写 `M2-16D`、审计表写 `N/A`（G-09）。** `:1156` Granularity `split-from=M2-16D` 与标题「自 M2-16D 拆出」、修订历史 `:463`「M2-16D → M2-16D,M2-16D1」一致，但审计表 `:803` 的 split-from 列是 `N/A`。这是 25 卡逐栏比对中唯一的列级不一致。**修复：** `:803` 改为 `M2-16D`。
4. **修订历史出现两行同轮次 R30-r13（`:450`、`:451`）。** 两行内容部分重叠（都记了写集/门族修订），与 r11 合并 r10 重复行、r13 合并 r12 重复行属同型回归。**修复：** 合并为一行。
5. **review log 新增行的 Scope 写「R30-r13/**r15** 修订后的完整计划」（`:2768`、`:2769`）。** 计划内不存在 r15 轮次。**修复：** 统一写「R30-r13 修订后的完整计划」。
6. **M2-02F / M2-02R 判据规范块的 VER 门族构成与实际 Verification 清单不符（总数对、分项错）。** `:1475` 写「三條 `memory_episode_test`、兩條 `db_migration_test`」，实际 `:1479-1486` 是 `memory_episode_test` ×2、`db_migration_test` ×3；`:1554` 写「四條 `memory_episode_test`、兩條 `db_migration_test`」，实际 `:1558-1567` 是 ×3 / ×3。门族 waiver 的补偿要求是「逐條枚舉具名命令」，分项写错会让复核者对不上。**修复：** 两处按实际清单改写分项。
7. **模板 `plan-template.md:233` 的 `sql/publish/**`（Worker D1）行仍无「0.23.x 已拆除；保留仅供历史计划」注记。** 相邻 `:229`/`:230` 的 `worker/` 两行都已加该注记，`:233` 未加；实测本仓库 `sql/` 下只有 `migrations/` 与两份 bootstrap SQL。该表面在本计划内永不触发，仅为模板自洽问题。**修复：** 补同样注记。

---

**结论：** r13 的 4 条 P1 中，Codex P1-1（`mod.rs`）与 P1-2（门族枚举）各只关闭一半，P1-3（compiler identity）写入了语义但引入了无归属型别，Claude P1-1（写集泛称）已关闭；同时 r09–r13 反复收敛的审计表分行与 waiver 分子两条审计线本轮双双回归。事实基线、版本面集合、CI job 集合、migration tip、25 卡 AC/VER/deps/DAG/Granularity 逐栏（除 `split-from` 一处）均实测成立。

VERDICT: FAIL
EXIT=0
