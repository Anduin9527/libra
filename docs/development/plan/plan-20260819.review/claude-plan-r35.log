# plan-20260819 R30-r35 review — Claude（評審期間凍結：plan sha256 0cd9c198…、template 49f94da3…）
# reviewer: claude -p --effort xhigh --allowedTools Read Grep Glob (read-only, prompt via stdin)
# date: 2026-09-22T04:53:09+08:00
0cd9c19833bd201999feef367f0a5981905200ade270787a5a2679896c392c10  docs/development/plan/plan-20260819.md
49f94da35c8828e72caaae52006f52c84c72647016dbce188cad77715b2f7f4f  docs/development/plan/plan-template.md
# review prompt: plan-20260819.review/review-prompt-r35.md

## R30-r35 複審結果（凍結版 plan sha256 `0cd9c198…`、template `49f94da3…`）

### 一、r34 findings 逐條核對（全部關閉）

| r34 finding | 核對命令 | 結果 |
|---|---|---|
| **P1（雙方一致）** 審計表 M2-13/M2-16C `scope` 格被覆蓋成 `4/5`/`4/7` | `grep -n '^| M2-13 |' / '^| M2-16C |'` | **已關閉**：`:832` = `… \| 4/5 \| M \| M2-01K(+principal),M2-16C,M2-12 \|`；`:837` = `… \| 4/7 \| M \| M2-01K(+principal),M2-12,M2-16B,M2-08,DEP-M2-SRC-01,DEP-M2-DSH-01（非阻塞） \|`；與 `:2468`/`:1075` 卡內 `Granularity` 逐格相同 |
| **P2-2** 卡邊界空行（M2-16C→M2-16D、M2-08→M2-09） | `rg -U '\S\n### Task M2-'` | **零命中**；規則已寫入 `:806`「Card-boundary 機械檢查」 |
| **P2-3** `EX-M2-09` AC5 標籤粘連 `AC5-8AC5-9AC5-10` | `rg -n 'AC5-' → :2062` | **已拆分**：`AC5-8 retention N=8；AC5-9 TTL 30 天；AC5-10 每倉 ≤10,000 行；`，全塊 `AC5-1..AC5-17` 唯一連續 |
| **P2-4a** `EX-M2-07` 分組不按 checkbox 序 | `:1039` vs `:1004–1013` | **已重排**：`AC1 協議版本(2)+AC2 recall(4)+AC3 bridge session(6)+AC4 ActionClass(1)+AC5a record(9)+AC5b identity(3)+AC5c minor(2)+AC6(2)+AC7(2)+AC8(5)+AC9(1)+AC10(1)`，逐項相加 **= 38**，與 10 個 checkbox 同序 |
| **P2-4b** `**Files likely touched:**` 缺分隔空格 | `rg -n '^\*\*[^*]+:\*\*\S'` | **零命中** |
| **P2-1** review log Scope 輪次筆誤 | `sed -n '2881,2886p'` | **未關閉** → 見 P2-1 |

### 二、本輪機械核對（通過項）

- **25 卡 checkbox 點算**（按 `Acceptance criteria:`→`Verification:`→`Dependencies:` 區間點 `^- \[ \]`）：25 卡分子與 `Granularity`/審計表**全部一致**，含 waiver 卡的謂詞還原（`:811` 口徑 M2-05=25、M2-07=9、M2-13=45、M2-16C=38、M2-08=38）。抽樣：M2-16C AC box 10 / VER box **11**、M2-13 AC 10 / VER **15**、M2-08 AC 8 / VER 8、M2-02 9/9、M2-05 8/10、M2-15 9/2。
- **審計表**：`rg -c '^(\|[^|]*){15}\|\s*$'` = **27**（header+sep+25 卡）；25 行逐格 = 卡內 `Granularity`（含 M2-15 `immutable-release`/`0/0`/`S`）。末行 `:841` → `:842` 純 `$` → `:843` `**Verification 新增标记：**`（`cat -A` 位元組確認物理空行）。
- **算式自洽**：`EX-M2-09` `2+4+4+3+17+2+3+3 = 38`（AC5 明細點算 17）；`EX-M2-02` `4+3+2+2+7+1+1+1+1+8+1+12+2 = 45`、V 清單 15 項、拆分 `command_test 7 + lib 2 + memory_episode_test 2 + compat 4 = 15`；`EX-M2-06` B0–B10 **11 門**與 11 個 VER checkbox 1:1 同序且位於全部 VER 之後（`cat -A` 確認前後單空行）；`EX-M2-05` `4+8+3+1+3+2+1+3 = 25` / 10 門；`EX-M2-04` 9/10；`EX-M2-01` A1–A9 + 9 門；`EX-M2-10` 9/7。9 筆 waiver 全為 8 列。
- **格式門**：`rg -U '\*\*Verification:\*\*\n\S'` 與 `rg -U '\S\n\*\*Dependencies:\*\*'` 均零命中；`` rg -n '`[^`]*\\\|[^`]*`' `` 僅 `:459`/`:2874` 兩條描述該修復本身的歷史文本；`^\|` 共 530 行（較 r34 的 527 多 3 = 修訂歷史 1 + review log 2），無寬度異常。
- **欄位完整性**：`^\*\*Full-suite trigger:\*\*` = **25**；DAG 邊 `M201K --> M216C`/`M201K --> M213` 僅在第 3 個「實施順序」mermaid（`:682`/`:683`），第 1 個架構圖無此二邊；Phase 0 = M2-16A/M2-01/M2-01K（不含 M2-16B）；追溯表 25 行；完成判據 24 張收口卡 + M2-16D1 條件性；里程碑 M0–M4 與 DAG/Phase 一致；故障矩陣 proposal 行 4 格。
- **plan-status**：`:46` 含 r18–r34 全部輪次與計數（r34「兩方 `FAIL`，0×P0/1×P1、3–4×P2：審計 scope 格」與兩份 log 一致）+ `r35 待跑`；`:201` M2-16A `blocked`、❌ 禁止開工。

### 三、倉庫事實基線（核對日 `45ad1f7f2` 成立）

`Cargo.toml version = "0.23.36"`；版本面 = **三處**（`tests/compat/version_surface_sync.rs:11-13`「now: `Cargo.toml`, `install.sh`, `install.ps1`」）；migration tip = `2026091901_operation_boundary_claim_columns`（`sql/migrations/202609*` 最高號）；`base.yml` jobs = `format/clippy/owner-liveness-macos/redundancy/test/network-remotes/opencode-export-linux`（**7**，與 DEP-M2-CI-02 逐字一致）；`release.yml` = `build-and-upload/upload-install-scripts/update-homebrew-tap/verify-homebrew-formula/request-stable-manifest`（**5**，與 DEP-M2-CI-03 一致）；`authorization.rs:19 pub enum ActionClass`、`protocol.rs:62 METHOD_ALLOWLIST`/`:532 allowlist_has_exactly_20_methods`、`tests/agent_bridge_protocol_test.rs:15 protocol_v1_allowlist_is_exactly_20_methods`（20→22 前提成立）、`tests/agent_bridge_approval_test.rs` 既有、`agent_bridge/` 無 `memory.rs`（M2-16C 首建）、`src/internal/ai/` 無 `memory/`（M2-16A 移植）。`providers`/`context_budget`/`client.rs`/`prompt/`/`mcp/`/`orchestrator/`/`web/`/`operation_wrapper` 均不存在，全計劃對其引用只落在 §0.1 歷史表、ADR Context、禁止清單與零命中守衛，**無執行性依賴**。模板 v2.8 的 ER-13/ER-14 雙 env、`:251` `opencode-export-linux`（帶 `web-check` 歷史註記）、`:262` 單一 `sql/**` 均成立。

**本輪新增證據**：`$SRC_CLONE`（`/run/media/genedna/data/libra-pr456`）本輪可經 Glob 列舉（`.git`、`docs/commands/memory.md` 等），但本會話的 Bash 策略拒絕對該路徑執行 `git -C`，故 merge-base `2ebd409…` 與 158-path 計數本輪仍由 r34 Codex log 的歸檔輸出承擔（`codex-plan-r34.log`：`merge-base` = `2ebd409045b5114707986b66a7a6b42b5a4a4b7e`、`diff --name-only | wc -l` = 158），符合 `DEP-M2-SRC-01`「雙評審至少一輪在可訪問環境歸檔」的要求。

---

## P0（0 條）

無。

## P1（0 條）

無。

---

## P2（3 條）

### P2-1　review log 的 Scope 輪次筆誤第十次復發（`R30-r34/r40`），計劃自設的 `:807` 機械檢查在其被新增的同一欄位上連續失效

- **位置：** `plan-20260819.md:2882`、`:2883`；規則行 `:807`
- **問題：** 實測 `sed -n '2882,2883p'` 兩行 Scope 逐字為「`R30-r34/r40` 修订后的完整计划（r35 期間凍結）」。本計劃不存在 r40 輪。`:807` 規定「Round 欄輪次號必須等於 Scope 欄所寫修訂版之輪次 + 1（例：r30 行的 Scope 必須寫 R30-r29）」——Round = `R30-r35`，Scope 應為 `R30-r34`。歷次同一筆誤：`r31`(r26 修)、`r33`、`r34`、`r35`、`r36`、`r37`、`r38`、`r39`(r34)，本輪 `r40`。同表上一輪的 r34 行（`:2880`/`:2881`「R30-r33 修訂後的凍結版（sha256 `93415b73…`）」）格式正確，可見是新增行的一次性疏漏而非口徑分歧。
- **為何重要：** review log 是雙 PASS 的唯一審計軌跡，Scope 的輪次號決定被審 sha256 能否回指正確修訂版。一條自設機械檢查連續十輪在自己管轄的欄位上失效，該檢查對編輯者已無實際約束力。不阻塞任何任務卡執行，故列 P2。
- **最小修復：** 兩行 Scope 改為「R30-r34 修訂後的凍結版（sha256 `0cd9c198…`）」；並把 `:807` 從純文字提醒改寫為可執行斷言（例如：`rg -n '^\| R30-r([0-9]+) \|' ` 抽出 Round 與 Scope 輪次後比對 `Round − 1`），與 `:806` 的 Card-boundary 檢查同格式落地。

### P2-2　§0.2 `:25` 的 producer 證據括注與實際 `rg` 結果不符：`src/command/cat_file.rs` 對 `IntentSpec` 零命中，實際命中的是 `session/jsonl.rs` 等四檔

- **位置：** `plan-20260819.md:25`（§0.2 結論段）
- **問題：** 該行寫「`Task`/`IntentSpec` 物件本身也已无 in-repo producer（核对日 `rg` **只命中** `src/command/cat_file.rs` 的只读类型名表与 `history.rs` 测试）」。實測：
  - `rg -c 'IntentSpec' src` → `session/jsonl.rs:31`、`agent_run/context_pack.rs:1`、`agent_run/mod.rs:1`、`agent_run/task.rs:1`（4 檔 34 處），**`src/command/cat_file.rs` 與 `history.rs` 皆不在命中集合內**。
  - `rg 'IntentSpec' src/command/cat_file.rs` → `No matches found`。
  - `cat_file.rs:155-184` 確實存在一張只讀類型名表，但其內容是小寫字串 `"intent"`/`"task"`/`"intent_event"`…，不是 `IntentSpec`。
  - 括注未給出 pattern，因此任何 reviewer/執行代理按最自然的字面 pattern 復算都會得到與「只命中」相反的結果。
- **為何重要：** 這一行是 ADR-M2-05「Episode root 身份改由 host 提供 opaque root ID」的事實依據，也是 M2-16D 審計的起點；ER-02 要求開工時按行號刷新事實基線，一個無法復算的證據括注會讓刷新者誤判結論已失效。**實質結論本身經核對成立**（`rg 'IntentSpec::|use .*IntentSpec|: IntentSpec|IntentSpec \{|-> IntentSpec' src` **零命中**；`rg 'git_internal::internal::object::(task|intent|…)' src` 僅命中 `agent_run/task.rs:21` 的一行 doc comment），故不影響架構成立性，列 P2。
- **最小修復：** 把括注改為可復算的具名命令與真實結果，例如：「核對日 `rg -n 'IntentSpec::|use .*IntentSpec|: IntentSpec|IntentSpec \{' src` 零命中；`rg -n 'git_internal::internal::object::(task|intent)' src` 僅命中 `src/internal/ai/agent_run/task.rs:21` 的 doc comment；`src/command/cat_file.rs:155-184` 只保留小寫類型名字串表」。

### P2-3　`EX-M2-09` 判據塊的 AC5 明細留在導語段落內，AC1–AC8 的項目符號清單缺 `AC5` 一項

- **位置：** `plan-20260819.md:2062`（導語段）與 `:2064–2070`（AC1/AC2/AC3/AC4/**AC6**/AC7/AC8 七個 bullet）
- **問題：** 導語段先寫「AC 謂詞 38 條 = AC1(2)+AC2(4)+AC3(4)+AC4(3)+AC5(17)+AC6(2)+AC7(3)+AC8(3)」，隨即在**同一段落**內展開 `AC5-1..AC5-17`；其後的 bullet 清單從 AC1 排到 AC8 卻**跳過 AC5**。其餘 8 個 `EX-*` 判據塊（`EX-M2-01` A1–A9、`EX-M2-05` A1–A8、`EX-M2-06` B0–B10、`EX-M2-07` AC1–AC10、`EX-M2-10` K1–K3）都是「導語一行 + 逐條 bullet」的統一形態。
- **為何重要：** 判據塊是 waiver 准入條件②（門族清單在卡內逐條枚舉）的落點，收口時要按條核對 38 條謂詞是否都在已審批範圍內。目前佔全卡 **17/38**（最大的一組）的 AC5 不在可逐行掃描的 bullet 序列裡，按 `^- AC` 解析的核對腳本會漏掉它，人工核對也會在 AC4 與 AC6 之間看到跳號。枚舉本身完整、總數正確，故列 P2。
- **最小修復：** 把 `AC5 runner/model/identity/retention（17）：AC5-1 …；AC5-17 identity prune fail-closed` 抽成一個獨立 bullet，插在 `AC4 lease/fence（3）` 與 `AC6 有界退避 vs 穩定失敗（2）` 之間，導語只保留「AC 謂詞 38 條 = AC1(2)+…+AC8(3)」一行；分子 38 與各分組大小不變。

---

**總計：P0=0，P1=0，P2=3。**

r34 的 1 條 P1 與 3 條 P2（審計 `scope` 格、卡邊界空行、`AC5-n` 標籤拆分、`EX-M2-07` 分組重排、`Files likely touched` 空格）本輪全部閉合，且未引入新的結構性回歸。本輪三條 P2 全為審計軌跡／證據措辭／判據塊排版類，不改動任何謂詞、依賴語義、ADR 或發布流程，亦不影響 25 卡的可執行性。結構性維度（25 卡 checkbox 點算、15 列審計表逐格一致、9 筆 waiver 的 8 列格式與算式、B0–B10 ↔ 11 checkbox 同序、DAG 無環且邊落點正確、Phase/里程碑/追溯表/完成判據點算、倉庫事實基線／版本面三處／CI job 集合／migration tip／模板 v2.8）本輪全部通過。

VERDICT: PASS
EXIT=0
