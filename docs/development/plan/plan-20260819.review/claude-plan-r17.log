# plan-20260819 R30-r17 review — Claude
# reviewer: claude -p --effort xhigh --allowedTools Read Grep Glob (read-only, prompt via stdin)
# date: 2026-09-22T00:46:06+08:00
ae5bc1623527f4d1d70954acef04f2cdb2b1127e75ecff1d553236ff1dca2a38  docs/development/plan/plan-20260819.md
# review prompt: plan-20260819.review/review-prompt-r17.md

## plan-20260819 R30-r17 复审（Claude，只读）

**被审文件：** `docs/development/plan/plan-20260819.md`
**首次实测 SHA-256：** `ae5bc1623527f4d1d70954acef04f2cdb2b1127e75ecff1d553236ff1dca2a38`（与归档 `plan-20260819.review/r30-r17-sha256.txt` 一致 → 本轮结论绑定此版本，2839 行）
**⚠️ 评审期间文件被并发修改：** 复核后段实测 `sha256sum` 变为 `4473d4dd79216ca5ead4e1f2141125addca9585511c632c1168a6c60260f4625`（mtime `2026-09-22 00:59:02`，2840 行），内容为 Codex r17（已出 `FAIL 0×P0/2×P1/2×P2`）的修订。下文每条 finding 同时标注它在漂移版 `4473d4dd…` 中的状态。
**对照：** `plan-template.md` v2.8（`0a3b7d85…`）、`plan-status.md`、仓库 `main` @ `45ad1f7f2`（crate `0.23.36`）
**只读声明：** 本轮未修改仓库任何文件（全部为 `Read`/`Grep`/`sed`/`grep`/`ls`/`wc`/`sha256sum`/`stat`）。

---

### 一、r16 findings 逐条核对（实际命令与结果）

| r16 finding | 核对方式与结果 | 结论 |
|---|---|---|
| Codex P1-1 / Claude P1-4：M2-16C 缺 `**Dependencies:**` 字段 | `grep -c '^\*\*Dependencies:'` = **25** = `grep -c '^### Task M2-'` = 25；`:1009` 为完整字段行；`grep -n -B1 '^\*\*Dependencies:'` 的前置行全为空行 | ✅ 关闭 |
| Claude P1-2 / Codex P1-2+P1-3：M2-05 / M2-13 判据规范首行门数错位 | `:1787` = 「10 條 VER + 25 條 AC」，枚举 V1–V10 = 10、AC 分组 `4+8+3+1+3+2+1+3=25`；`:2363` = 「11 條 VER + 41 條 AC」，V1/V1b/V2..V10 = 11、`4+3+2+2+7+8+1+12+2=41`；waiver `:775`/`:778`、Granularity `:1836`/`:2413`、审计表 `:791`/`:799` 五处全一致（实测 VER 计数 10 / 11） | ✅ 关闭 |
| Claude P1-3 / Codex P1-6：22-method exact-allowlist 守卫无 VER 执行 | `:987` 已去掉过滤器，改跑整个 `cargo test --test agent_bridge_protocol_test`；`:983` `--lib internal::ai::agent_bridge` 覆盖 `allowlist_has_exactly_22_methods`。实测基线两处守卫均存在：`src/internal/ai/agent_bridge/protocol.rs:532 allowlist_has_exactly_20_methods`、`tests/agent_bridge_protocol_test.rs:15 protocol_v1_allowlist_is_exactly_20_methods` | ✅ 关闭 |
| Claude P1-1 / Codex P1-4：`memory_terminal_observation` 字段合同 / retention / owner | 表职责 `:328` 新增完整行（脱敏 envelope、HMAC principal、`proposal` 只存 digest/引用、N=8 / TTL 30 天 / ≤10,000 行 / 原子裁剪 / 未处理不可删）；M2-02 AC3 `:1399`、Security `:1446`、M2-08 AC5 `:2015` 四处口径一致 | ⚠️ **部分**（新增的 VER 门未进判据规范块 → P1-2；行为判据落在 schema 卡 → P1-3） |
| Codex P1-5：CLI identity 通道（创建 / 查找 / 作用域绑定 / 失效 / 写集）未定义 | `:515` 改写为「由 `libra agent session` 建立的本地 `agent_session` handle（绑定 principal/repository/worktree，含有效期与撤销）」。实测该机制不存在，且无卡交付（详见 P1-1） | ❌ **未关闭** |
| Claude P2-1：修订历史重复 r15 行 | `grep -n 'R30-r1[0-9]'` → `:451` 仅一行 R30-r15 | ✅ 关闭 |
| Claude P2-3：归档校验和与被审文件不符 | `r30-r17-sha256.txt` = `ae5bc162…` = 本轮首次实测值 | ✅ 关闭 |
| Claude P2-4：判据规范具名命令与实际 VER 名不一致 | `:1416` 已写 `observer_job_schema_migration_transaction`（= `:1427`）；`:2364` V1b 已写 `memory_record_rejects_forged_identity_and_binds_session_identity`（= `:2379`） | ✅ 关闭 |
| Claude P2-2 / Codex P2-1：review log 轮次笔误 | `:2794` 仍写「R30-r15/**r19** 修訂後」；`:2796`/`:2797` 写「R30-r16/**r21** 修订后」 | ❌ 同型第 4 次回归（P2-1） |
| Claude P2-5：M2-13 Files 11→5 / M2-12 prod 9→8 | M2-13 `:2391`「5 个生产文件以内」、`prod-files=5` ✅；M2-12 `prod-files` 已改 8（`:2332`、审计表 `:798`），但 `:2310` Files 仍写「9 个生产文件以内」 | ⚠️ 部分（P2-5） |

### 二、本轮全量机械核对（实测通过项）

| 项 | 命令 / 结果 |
|---|---|
| 事实基线锚点 | `ls src/internal/ai/memory` → 不存在；`ls src/command/memory.rs` → 不存在；`wc -l src/internal/model/mod.rs` = 48（`:1-42` 有效）；`sed -n '2023,2025p' src/utils/client_storage.rs` = `pub fn put(`；`sed -n '1144p' src/command/op.rs` = `&& !name.starts_with("libra/")`；`docs/commands/memory.md`、`docs/benchmarks/` 均不存在 ✅ |
| migration tip | `ls sql/migrations/ \| tail` → `2026091901_operation_boundary_claim_columns.sql` 为最新 ✅（`2026092101+` 重编正确） |
| 版本面集合 | `tests/compat/version_surface_sync.rs:11-13` 明写三处（`Cargo.toml`/`install.sh`/`install.ps1`）；`Cargo.toml:3 = 0.23.36` ✅ 与模板 v2.8 一致 |
| CI job 集合 | `base.yml` 7 job（`format/clippy/owner-liveness-macos/redundancy/test/network-remotes/opencode-export-linux`）、`release.yml` 5 job（`build-and-upload/upload-install-scripts/update-homebrew-tap/verify-homebrew-formula/request-stable-manifest`）、`codeql.yml:21 analyze` ✅ 与 DEP-M2-CI-02/03 逐项一致 |
| FTS5 零实现 | `Grep FTS5|bm25\(` over `src/` → 0 命中 ✅ |
| 25 卡 AC/VER 实计 | 逐卡按 `**Acceptance criteria:**`→`**Verification:**`→`**Dependencies:**` 区间数 `- [ ]`：16A 8/4、16B 7/5、16C 8/9、16D 6/4、16D1 8/6、16E 8/4、01 8/4、01K 8/5、02 9/**9**、02F 9/8、02R 9/8、03 7/4、04 8/7、05 8ck(25 谓词)/10、06 8/5、07 9/10、08 8/8、09 8/5、10 7/4、11 8/5、12 8/5、13 9ck(41 谓词)/11、14 8/5、14C 8/6、15 9/2 —— 与审计表/Granularity 分子逐栏一致 ✅ |
| 审计表 | 25 数据行；末行 M2-15（`:808`）与 `**Verification 新增标记：**`（`:810`）之间 `:809` 为空行（`cat -A` 实测 `$`）✅ |
| DAG ↔ `Dependencies` | 28 条边与 25 卡逐条比对，无环；`M207→M216B→M208`、`M208→M216C`、`M216D→M216E`、D1 条件边均在 ✅ |
| 测试 target / 文档锚点 | `tests/{db_migration_test,operation_restore_faults,command_test,agent_bridge_protocol_test}.rs`、`Cargo.toml` 的 `compat_help_examples_banner`/`compat_command_docs_examples_section`/`compat_serial_registry` 均存在；M2-05 的 33 份文档（EN 11 + zh 11 + dev 11）与 11 个 `src/command/*.rs` callsite、M2-01K 的 config 三份、M2-16C 的 agent 四份全部存在 ✅ |
| `src/internal/mod.rs` 无 `ref_policy` | `grep -n ref_policy src/internal/mod.rs` → 零命中 ✅（确需 M2-05 新增） |
| 零残留 | `test-provider`/`code_ui_scenarios`/`--test-threads`/`<source clone>`/`<libra-pr507>`/`batch-release child` 仅出现在 §0.1 删除面表、修订历史与 review log；裸 `git` 仅出现在规则句、历史证据与禁止句，活动卡命令全部走 `$SRC_CLONE` ✅ |
| `mainline.md` receipt owner | `docs/development/gap/mainline.md:279` 仍指向已删的 `context_budget/receipt.rs`，与计划 M2-01 AC3（`:1268`「待 M2-01 实施时改为 `memory/context/receipt.rs`」）的登记一致 ✅ |
| 模板 v2.8 | `:526` 门族 waiver 四项准入条件、`:537` 审计表判定规则、`:265` C 组 PR→squash→merge-SHA tag、`:347-353` ER-14 范围、`:703` 双 env 收口门 —— 与计划 §0.5 / 发布分组一致 ✅ |

**沙箱限制（非计划缺陷）：** `$SRC_CLONE`（`/run/media/genedna/data/libra-pr456`）与 `../libra-backend` 在本会话不可读，merge-base `2ebd409…`、158 路径清单、后端 `*.en.md` 未能复核；计划已把 pin 校验写成 M2-16E 卡内先决命令（`:1198`、`:1211`），fail loud，设计上可接受。

---

## P0（0 条）

无。

---

## P1（3 条）

### P1-1｜CLI `CompilerIdentityV1` 的唯一身份来源在仓库中不存在，也没有任何卡交付它——`libra agent session` 没有签发子命令，`agent_session` 表没有 principal / 有效期 / 撤销列，且 `session_id` 是可枚举的明文拼接字符串

- **位置：** `plan-20260819.md:515`（ADR-M2-05 envelope identity 段；漂移版 `:516`）、`:2357`（M2-13 AC5 `EX-M2-02`；漂移版 `:2358`）、`:2379`（VER `memory_record_rejects_forged_identity_and_binds_session_identity`）、`:2387`（M2-13 `Implementation write set`；漂移版 `:2388`）、`:2035`（M2-08 write set；漂移版 `:2036`）；对照 ADR-M2-12 `:592`、完成判据 `:2830`。
- **实际核对命令与结果：**
  1. `grep -n "pub enum SessionSubcommand" -A 20 src/command/agent/session.rs` → 只有 `List` / `Show` / `Stop` / `Resume` / `DeriveToolCalls`（`:23-41`）。**没有任何 create / issue / grant 子命令**；漂移版改写的「由既有 `libra agent session`（list/show/derive）的 session 記錄**簽發**」同样不成立——这三个都是只读命令。
  2. `sed -n '11,35p' sql/migrations/2026050303_agent_capture.sql` → `agent_session` 列 = `session_id, agent_kind, provider_session_id, thread_id, state, working_dir, worktree_id, parent_commit, parent_session_id, metadata_json, redaction_report, started_at, last_event_at, stopped_at, schema_version`；`grep -rn "ALTER TABLE .agent_session." sql/migrations/*.sql` → 后续只加 `sync_revision`、`repo_id`、`workspace_id`、`workspace_fence`、`scope_state`。**无 principal 列、无 `expires_at`、无 `revoked_at`**（`state`/`stopped_at` 表达的是「会话结束」，不是「凭据撤销」）。
  3. `grep -n "INSERT INTO agent_session" src/internal/ai/hooks/runtime.rs` → `:777`，唯一写入者是 hooks capture 对**外部 agent** hook envelope 的 upsert；`agent_kind` 是 `claude_code/cursor/codex/gemini/opencode/copilot/factory_ai` 的封闭 CHECK，没有「Libra CLI 操作者」这一类。
  4. `sed -n '118,132p' src/internal/ai/hooks/runtime.rs` → `build_ai_session_id` = `format!("{provider}{DELIM}{provider_session_id}")`，注释明写它「用作目录名与 metadata key」，配套的 `redact_session_id` 只在日志里遮蔽。该值可被 `libra agent session list` 直接枚举、也落在磁盘目录名上，**不含 Libra 自有熵，无法充当不可伪造的 bearer handle**。
  5. `grep -n "agent_session" docs/development/plan/plan-20260819.md` → 只有 `:33`（事实基线：捕获表）、`:515`、`:2357` 三处。M2-13 写集（`src/cli.rs`、`src/command/memory.rs`、`src/command/mod.rs`、`src/utils/{error,output}.rs` + 测试/文档）与 M2-08 写集（`memory/{observer,job,job_state,runner,job_sql,runtime,mod}.rs` + `agent_bridge/*`）**都不含** `src/command/agent/session.rs`、`agent_session` 迁移或任何身份签发模块。
- **问题：** 计划把「受认证、绑定 principal、含有效期与撤销的本地 session handle」当作**既有事实**引用，并以它作为 CLI `memory record` 接受 host `proposal` 的唯一授权凭据；实测该机制的三项安全属性（principal 绑定、有效期、撤销）在 `agent_session` 上全部不存在，签发路径不存在，且没有任何卡的写集 / `prod-files` / AC / VER / DAG 承接它。附带：ADR-M2-05 把 registry API 写成「M2-08 `trust.rs`」，而 `trust.rs` 按 §0.4（`:64`）与 M2-07 写集（`:1964`）归 M2-07，M2-08 写集里没有该文件（见 P2-6）。
- **为何重要：** ① ADR-M2-12（`:592`）明确把「host 携带 `EpisodeProposalV1`」设计为**无进程内 model 时的必要补录路径**，完成判据（`:2830`）又把「嵌入 API / `libra memory record` 路径」列为**必要门**（bridge 路径因 `DEP-M2-DSH-01` 非阻塞而可记 N/A）。identity 来源不存在 ⇒ 要么 M2-13 对所有 CLI proposal 永久拒绝、补录能力落空、必要门无法满足；要么实施者在 M2-13 里临时发明 session 签发 + principal 绑定 + 撤销，这超出该卡写集与单一行为轴（违反 G-01 / G-05 / G-10，且 G-05/G-10 永不可豁免）。② 这是**安全边界**问题而非措辞问题：若实施者按字面把可枚举的 `session_id` 当 handle，trust gate 的 allowlist 就退化为「任何能读本地 DB 或跑 `libra agent session list` 的进程都能冒充 compiler identity」，与 ADR-M2-06 的 fail-closed 承诺和 `trust_gate_rejects_forged_compiler_identity` 的目的直接冲突。③ 这正是 Codex r16 P1-5 的原问题，本轮用一个不存在的机制作答，未关闭。
- **最小修复（二选一）：**
  - **(a) 收窄到可交付面（推荐，零新卡）：** 把 CLI identity 来源改为「仅进程内 registry」——`libra memory record` 只在当前进程已通过 `MemoryRuntime::with_compiler_model` 登记 `CompilerIdentityV1` 时接受 `proposal`，否则**一律拒绝 proposal 并让 job 保持 pending**（与 ADR-M2-12 `:592` 的「无 proposal 时 pending」同语义）；删除 `:515`/`:2357` 的 `agent_session` handle 表述与 `--agent-session` 入参，VER 改为 `memory_record_rejects_forged_identity_and_requires_process_registry`。
  - **(b) 补齐前置面：** 新增一张前置卡（或并入 M2-08）交付身份签发：`agent_session`（或新表）的 `principal` / `expires_at` / `revoked_at` 列迁移 + 高熵不可枚举 handle + `libra agent session` 的签发/撤销子命令 + registry 解析 API，并把这些文件写入该卡 `Implementation write set`、`prod-files`、AC/VER 与实施顺序 DAG，再由 M2-13 引用。
- **漂移版状态：** `4473d4dd…` 的 `:516` 仍写「handle 由既有 `libra agent session`（list/show/derive）的 session 記錄簽發，綁定 principal/repository/worktree 並含有效期與撤銷」，`:2388` 的 M2-13 写集与 `:2036` 的 M2-08 写集均未增补任何签发面 → **仍未关闭**。

### P1-2｜M2-02 的 `EX-M2-01` 判据规范块写「8 條 VER 門」并只枚举 8 条，而本卡实际 VER = 9、分子写 `VER=9/8@EX-M2-01`；漏掉的正是 r16 新增的唯一补救门

- **位置：** `plan-20260819.md:1407`（首行「9 條 AC 判據 + 8 條 VER 門」）、`:1416`（VER 门枚举）、`:1420-1428`（实际 9 条 VER）、`:1460`（Granularity `VER=9/8@EX-M2-01`）、审计表 `:786`、waiver `:774`；模板 `plan-template.md:526` 准入条件 ②④、`:537` 判定规则。
- **实际核对：** `sed -n '1418,1429p' … | grep -c '^- \[ \]'` = **9**；`:1416` 枚举「五條 `db_migration_test` + `observer_job_schema_migration_transaction` + `lib internal::model::memory` + `compat_serial_registry`」= **8**；缺的是 `:1428` 的 `memory_episode_test terminal_observation_snapshot_retention_and_redaction`。
- **为何重要：** 门族 waiver 的准入条件 ②「门族清单在卡内判据规范块**逐条**枚举」与 ④「门族增减时同批更新 waiver 行与粒度审计表」缺一不可，`:537` 的判定是「任一列不达标即不得开工」。这与 r14（EX-M2-05 24 vs 25）、r15/r16（EX-M2-02 10/40 vs 11/41；EX-M2-05 11/41 vs 10/25）是同一条款、同一形态的**第四次**出现；更严重的是本轮恰好漏掉的那一条，正是 r16 P1-1 为「终态观测持久化」新增的唯一补救门——判据规范块因此在纸面上把该门排除在授权范围外。
- **最小修复：** `:1407` 改为「9 條 AC 判據 + 9 條 VER 門」，并在 `:1416` 末尾补 `memory_episode_test terminal_observation_snapshot_retention_and_redaction`。（若按 P1-3 把该门移交 M2-08，则 M2-02 VER 回到 8，首行无需改，但需同步 Granularity/审计表为 `8/8`。）
- **漂移版状态：** `4473d4dd…` 已把该门移交 M2-08，M2-02 VER = 8、判据规范 `VER 門（8）` 与分子一致 → **已在评审期间关闭**。

### P1-3｜M2-02（migration 卡）的 AC3 与同名 VER 要求验证 retention 的**运行期**语义（原子裁剪 / 未处理不可删 / prune fail-closed / 删除前验证 keyed digest），但同一句已把 retention 所有权判给 M2-08 `job_sql.rs`；M2-02 写集内没有任何可实现它的生产文件

- **位置：** `plan-20260819.md:1399`（M2-02 AC3）、`:1428`（VER `terminal_observation_snapshot_retention_and_redaction`）、`:1434`（M2-02 `Implementation write set`）、`:328`（表职责行「retention 由 M2-08 拥有」）、`:2035`（M2-08 写集含 `job_sql.rs`）、DAG `:660-670`。
- **实际核对：** M2-02 写集 = `sql/migrations/2026092101_memory_core{,_down}.sql` + `src/internal/db/migration.rs` + `tests/SERIAL_REGISTRY.tsv` + `.config/nextest.toml` + `src/internal/model/memory_*` + `src/internal/model/mod.rs` + `tests/{db_migration_test,memory_episode_test,INDEX.md}` + `sql/migrations/README.md`——**不含** `src/internal/ai/memory/job_sql.rs`。DAG 上 M2-08 在 M2-02 之后 6 张卡（`M2-02→02F→02R→03→04→05→06→07→08`）。
- **问题：** 「删除前验证 keyed digest」需要 HMAC 计算，SQLite 触发器无法表达；「未处理不可删 / TTL 30 天 / 每仓 ≤10,000 行的原子 prune」也被同句显式判给 M2-08。因此 M2-02 既无法满足该 AC 谓词，也无法让同名 VER 真正为绿。实施者只剩两条违规出路：在 M2-02 里实现 M2-08 的 prune（破坏声明的所有权、写集与 `prod-files=12`），或写一条名不副实的弱测试（模板 `:497`「幽灵验收」）。
- **为何重要：** G-05 要求 AC「自洽可判定」、`Verification` 是「可直接复制执行的确切命令」。把下游卡的运行期行为判据挂在上游 schema 卡上，会在整个计划的第一张迁移卡就卡住执行；而这张卡是 M2-02F/02R/03..15 的全串行前置。
- **最小修复：** M2-02 AC3 只保留本卡可判定的 schema 谓词（表/主键/唯一键/索引存在、principal 列只能存 HMAC、`proposal` 列只存 digest + 对象引用、无原始正文列、processed 标记列），把「N=8 / TTL 30 天 / ≤10,000 行 / 原子裁剪 / 未处理不可删 / prune 前 digest 校验」整段移入 M2-08 AC；同时把 `terminal_observation_snapshot_retention_and_redaction` 从 M2-02 VER 移到 M2-08 VER（M2-02 改留一条 schema/脱敏列断言，如 `terminal_observation_schema_and_redaction_columns`），并同批更新两卡分子、判据规范块与审计表。
- **漂移版状态：** `4473d4dd…` 的修订历史 `:451` 记「`memory_terminal_observation` retention/prune 全部移交 M2-08（`job_crash_recovery_and_observation_retention_matrix`），M2-02 只保留 schema/redaction 合同（`VER=8/8`）」，实测 M2-02 VER 已降为 8 → **已在评审期间关闭**。

---

## P2（8 条，不阻塞）

1. **review log 轮次笔误第四次同型回归。** `:2794` 写「R30-r15/**r19** 修訂後的計劃」、`:2796`/`:2797` 写「R30-r16/**r21** 修订后的完整计划」——计划不存在 r19 / r21 轮次（r14/r15/r16 都出现过同型笔误）。漂移版 `:2795` 的 `r19` 仍在。**修复：** 统一写「R30-r15 修訂後」「R30-r16 修訂後」，并在本轮结束后把 r17 行的 Result/Evidence 回填实测 SHA。

2. **`plan-status.md` 落后一轮。** `:46` 与 `:201` 在被审版本中仍写「r16 待跑」「r01–r15 雙評審皆 `FAIL`」，而 r16 双方 log 已存在且均 FAIL（计划 `:2794-2795` 已记录）。**修复：** 同步 r16 结果 + r17 待跑。（漂移版已更新为「r01–r17 皆 FAIL；r18 待跑」，但那是在 Claude r17 尚未出结论前预填的。）

3. **`EX-M2-06` 判据规范块缺首行门数，且 B 列表与 VER 非一一对应、含一条无命令的门。** `:989` 的块没有其余五块都有的「本卡獨立門族 = N 條 VER」首行（对照 `:1407`/`:1486`/`:1565`/`:1787`/`:1935`），B0–B11 共 **12** 条对应 **9** 条 VER；其中 `:996` 的 `B7 錯誤碼/文檔：docs/error-codes.md 同步與 EN/zh/agent.en.md 文檔門` **没有可复制执行的具名命令**，不满足模板 `:526` 准入条件 ①。**修复：** 补首行「= 9 條 VER 門」；把 B7 并入 B10 的 `rg` 命令并补具名 `cargo test --test compat_error_codes_doc_sync`，重编号使 B 列表与 9 条 VER 一一对应。

4. **M2-16C 的判据规范块仍插在 Verification 列表中间。** `:989-1001` 夹在 VER 项 `:987` 与 `:1003` 之间，把该卡 VER 清单切成三段列表；其余五张带判据规范的卡都把该块放在完整 VER 列表之后。虽因前后有空行不再吞掉 `**Dependencies:**`（P1 已关闭），但与其余五卡格式不一致，仍会干扰机械提取。**修复：** 整块移到 `:1007` 之后、`:1009` 之前。

5. **M2-12 `Files likely touched` 与 `prod-files` 不同口径。** `:2310` 写「9 个生产文件以内」，而 Granularity `:2332` 与审计表 `:798` 为 `prod-files=8`；写集（`:2306`）机械计数 = `context/{memory,receipt,mod}.rs` 3 + `memory/{reader,view,delivery,mod}.rs` 4 + `agent_bridge/protocol.rs` 1 = **8**。**修复：** `:2310` 改为「8 个生产文件」。

6. **ADR-M2-05 把 registry API 的 owner 文件写错卡。** `:515`（漂移版 `:516`）写「M2-08 `trust.rs` 的 registry API」，但 `trust.rs` 按 §0.4（`:64`）与 M2-07 写集（`:1964`/漂移版 `:1965`）归 **M2-07**；M2-08 写集（`:2035`）没有 `trust.rs`，其 registry 实际在 `runtime.rs`（`:2015` 的 `MemoryRuntime::with_compiler_model`）。**修复：** 改为「M2-07 `trust.rs` 定义的 `CompilerIdentityV1` + M2-08 `runtime.rs` 的 registry API」。

7. **M2-16C 的 AC 谓词密度与本计划对 M2-05/M2-13 的口径不一致。** `:977` 的 AC6 一条内含 EN 文档 / zh 文档 / 开发文档 / 后端 `agent.en.md` / `COMPATIBILITY.md` / `Cargo.toml`+`tests/INDEX.md` 登记 / `METHOD_ALLOWLIST` / 两处守卫重命名 / `UNKNOWN_METHOD` 文案 ≈ 9 个独立谓词，`:974` 的 AC3 同样密集，但该卡写 `AC=8/8` 且无 AC waiver；而同一份计划已对 M2-05（8 checkbox → 25 谓词 → `EX-M2-05`）与 M2-13（9 checkbox → 41 谓词 → `EX-M2-02`）按模板 `:494`「多轴伪装」反模式做了谓词还原。**修复：** 对 M2-16C 也做谓词还原并登记 waiver，或明确声明本计划统一按 checkbox 计数、把 M2-05/M2-13 的谓词展开只作为判据规范正文（不改分子口径）。

8. **本轮被审文件在评审过程中被并发修改，review-log 无法稳定绑定版本。** 首次实测 `ae5bc162…`（与 `r30-r17-sha256.txt` 一致），复核后段实测 `4473d4dd…`（mtime `00:59:02`，2840 行）；计划 `:451` 已写入「R30-r17（Codex r17；Claude r17 **進行中**，其結論併入 r18）」，即修订在 Claude r17 完成前就已落盘。这违反计划自订 review 协议（`:2750`「log 必须包含被审文件的 SHA-256」）的可绑定前提。**修复：** 双评审同轮次期间冻结计划文件，两方 log 落盘后再统一修订并递进轮次号；或为 Claude 的实际被审版本单独归档校验和。

---

**结论：** r16 的 10 条 findings 中，M2-16C `Dependencies` 字段、M2-05/M2-13 判据首行门数、22-method exact-allowlist 守卫可执行性、修订历史重复行、归档校验和、判据规范具名命令一致性均已实测关闭；`memory_terminal_observation` 的字段合同 / retention 数值 / access owner 已补齐但新增的门未进判据规范块（P1-2）且行为判据错放在 schema 卡（P1-3，二者均在评审期间的 `4473d4dd…` 修订中关闭）；**Codex r16 P1-5 的 CLI identity 通道未关闭——计划以「`libra agent session` 签发的、绑定 principal、含有效期与撤销的 `agent_session` handle」为 CLI 唯一授权来源，实测该命令没有签发子命令、该表没有 principal/有效期/撤销列、`session_id` 是可枚举的明文拼接串，且没有任何卡交付它；该缺口同时是必要补录路径的执行阻断与 trust gate 的安全弱点，在漂移版 `4473d4dd…` 中仍然成立**（P1-1）。事实基线锚点、版本面集合、CI job 集合、migration tip、25 卡 AC/VER/deps/DAG/审计表逐栏一致性、审计表末行段落与 `Dependencies` 空行本轮全部实测成立。

VERDICT: FAIL
EXIT=0
