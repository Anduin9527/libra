# plan-20260819 R30-r28 review — Claude（評審期間凍結：plan sha256 f570cc8a…、template 49f94da3…）
# reviewer: claude -p --effort xhigh --allowedTools Read Grep Glob (read-only, prompt via stdin)
# date: 2026-09-22T03:16:39+08:00
f570cc8a71d242ea30adbaa3a40ae84b51498b8b67c10f2b3931e0a3e340d8bc  docs/development/plan/plan-20260819.md
49f94da35c8828e72caaae52006f52c84c72647016dbce188cad77715b2f7f4f  docs/development/plan/plan-template.md
# review prompt: plan-20260819.review/review-prompt-r28.md

I've completed the full review. Here are my findings.

---

# plan-20260819 R30-r28 review — Claude

**被審版本（凍結）：** plan `f570cc8a71d242ea30adbaa3a40ae84b51498b8b67c10f2b3931e0a3e340d8bc`、template `49f94da35c8828e72caaae52006f52c84c72647016dbce188cad77715b2f7f4f`（與 `r30-r28-sha256.txt` 一致，`sha256sum` 實測）
**範圍：** 全文計畫 + `plan-template.md` v2.8 + `plan-status.md` + 倉庫事實核對。**未修改任何檔案**（`git status --short` 與進場快照一致）。

## 一、r27 findings 逐條關閉核對

| r27 finding | 關閉？ | 實測證據 |
|---|---|---|
| Codex P1 / Claude P1-2：`ActionClass` 未入 AC/VER | **部分**（見 P1-1/P1-2/P1-3） | `rg -n 'ActionClass' plan-20260819.md` → `:992`（AC3）、`:1024`（EX-M2-07 塊）、`:453/:454`（修訂歷史）。VER `:1008` 具名 `agent_bridge_approval_test memory_recall_is_normal_and_record_requires_approval`。倉庫實證：`src/internal/ai/agent_bridge/authorization.rs:19 pub enum ActionClass`（`Normal`/`Dangerous`）、`:32 pub fn classify`（`_ => Dangerous` 預設）、`tests/agent_bridge_approval_test.rs` 存在 |
| Claude P1-1：M2-03 審計列 | ✅ | `:808` = `7/8 \| 5/8 \| 2/3`；卡內 `:1703` `AC=7/8; VER=5/8; landing=2; prod-files=3` |
| Claude P1-3：M2-05 V9 佔位符 | ✅ | `:1818` 為字面 brace-expansion 33 份清單（11×3），無 `$(...)`。33 份檔案全部實測存在（`Glob docs/commands/{...}.md`、`zh-CN/`、`docs/development/commands/`） |
| P2：`$SRC_CLONE` 加引號 | ✅ | `rg -n '\$SRC_CLONE'` 全 13 處皆為 `"$SRC_CLONE"`；`<source clone>` 佔位符零命中 |
| P2：`**Verification:**` 空行統一 | ✅ | `rg -n -A1 '^\*\*Verification:\*\*'` → 25 處全部後接空行 |
| P2：模板 `:251` 改 `opencode-export-linux` | ✅ | `plan-template.md:251` 已用 `opencode-export-linux` + `libra status --porcelain=v1`，並附 web-check 歷史註記 |
| （歷史回歸點）審計表末行獨立段落 | ✅ | `sed -n '827,831p' \| cat -A` → `:827` 資料行、`:828` 為純 `$` 空行、`:829` 為標記段落；`rg -n '^\|.*\|\*\*'` 零命中 |
| （歷史回歸點）表格欄數 | ✅ | `tr -cd '\|\n'`：waiver 表 10 行全 9 分隔符、依賴表 9 行全 9、審計表 27 行全 16、故障矩陣 16 行全 5（唯一 6 者為 `:2720` 的合法轉義 `<file\|->`） |
| （歷史回歸點）`Dependencies` 前空行 | ✅ | `rg -B1` → 25 處全部前接空行 |

**倉庫事實基線（核對日成立）：** `src/internal/ai/` 無 `memory`/`providers`/`context_budget`/`client.rs`/`prompt`/`mcp`/`orchestrator`/`web`/`operation_wrapper.rs`；migration tip = `2026091901_operation_boundary_claim_columns`；`Cargo.toml` `version = "0.23.36"`、`edition = "2024"`、無 `autotests = false`（頂層 `tests/*.rs` 確可自動發現）、`uuid` 已含 `v7`；版本面 = `Cargo.toml`/`install.sh`/`install.ps1` 三處（`tests/compat/version_surface_sync.rs:13`）；`base.yml` job 集合 = `format/clippy/owner-liveness-macos/redundancy/test/network-remotes/opencode-export-linux`（7，與 DEP-M2-CI-02 逐字一致）；`release.yml` = `build-and-upload/upload-install-scripts/update-homebrew-tap/verify-homebrew-formula/request-stable-manifest`（5，與 DEP-M2-CI-03 一致）；`protocol.rs:532 allowlist_has_exactly_20_methods`、`tests/agent_bridge_protocol_test.rs:15 protocol_v1_allowlist_is_exactly_20_methods` 兩道守衛皆存在；`error_codes_doc_sync` 只斷言單向（error.rs → doc），M2-05「複用映射」安全；`tests/compat/help_examples_banner.rs:53 VISIBLE_COMMANDS` 確為硬編碼清單；`AGENTS.md` 存在。25 卡 `AC/VER/landing/prod-files` 與審計表逐格比對**全部一致**；DAG 與 25 卡 `Dependencies` 無環、無缺邊。

---

## P0（0 條）

無。

---

## P1（4 條）

### P1-1　`EX-M2-06` waiver 分子與判據塊未隨 r27 的第 10 道 VER 門更新，且塊內自相矛盾

- **位置：** `plan-20260819.md:790`（waiver 行）、`:1013`（判據規範塊首行）、`:1014-1022`（B0–B8）、`:1056`（Granularity）、`:823`（審計表）、`:1002-1011`（VER checkbox）
- **問題：** r27 為 `ActionClass` 新增了第 10 道 VER 門（`:1008` `agent_bridge_approval_test ...`），`Granularity`（`VER=10/8@EX-M2-06`）與審計表（`10/8@EX-M2-06`）已改，但 **waiver 行 `:790` 仍寫「VER 分子如實寫 `9/8@EX-M2-06`」**。判據規範塊首行更自相矛盾：「**10 條** VER 門（與本卡 Verification 的 **9 個** checkbox 一一對應）」——實測 checkbox 為 **10**（`sed -n '1000,1012p' | grep -c '^- \[ \]'` = 10），而塊內只枚舉 B0–B8 共 **9** 項（`grep -c '^- B'` = 9），缺的正是新增的 approval 門。
- **為何重要：** `EX-M2-06` 的豁免對價就是「判據規範逐條枚舉具名命令，VER 分子如實寫」。分子在 waiver 與卡/審計表之間不一致、且有一道已宣告的 VER 門不在豁免枚舉內，等於該門未被任何已審批的 waiver 覆蓋；G-03 上限豁免的審批記錄（Approver / R30-r15）綁在一個已不存在的數字上。這是模板 waiver 規則違規，也是 r16–r19 反覆處理過的同一失效模式再次發生。
- **最小修復：** ①`:790` 改為 `VER 分子如實寫 10/8@EX-M2-06`；②`:1013` 改為「本卡獨立門族 = 10 條 VER 門（與本卡 Verification 的 **10** 個 checkbox 一一對應）」；③在 B5 之後插入 `- B6 agent_bridge_approval_test::memory_recall_is_normal_and_record_requires_approval（memory.recall=Normal／memory.episode.record=Dangerous）。` 並把原 B6–B8 順延為 B7–B9，使 10 門與 10 個 checkbox 嚴格 1:1。

### P1-2　`EX-M2-07` AC 分子三處仍為 31，與卡/審計表的 32 不一致；判據塊算式自身即等於 32

- **位置：** `plan-20260819.md:789`（waiver 行「AC 分子如實寫 `31/8@EX-M2-07`」）、`:797`（**AC 計數口徑**「…M2-16C=31…」）、`:1024`（判據塊算式尾端「= 31」）、`:1056`（`AC=32/8@EX-M2-07`）、`:823`（審計表 `32/8@EX-M2-07`）
- **問題：** r27 在 AC3 插入 `ActionClass` 謂詞（+1），Granularity 與審計表改為 32，但 waiver 行、全域 AC 計數口徑行、判據塊結論數字三處仍是 31。更關鍵的是判據塊 `:1024` 自己的算式 `2+4+9+1+3+2+2+2+5+1+1` **等於 32**，卻書面寫「= 31」——該塊是 waiver 的唯一對價文本，其結論與自身算式衝突。
- **為何重要：** `:797` 是全計畫的 AC 計數權威口徑行，與卡內分子衝突會讓收口時無法機械判定 M2-16C 是否仍在已審批的豁免範圍內；waiver 行的分子是審批對象本身，錯值即豁免登記無效（模板「除下表登記的 `EX-*` 外無其它 waiver」）。
- **最小修復：** 把 `:789`、`:797`、`:1024` 三處的 `31` 全部改為 `32`（`:1024` 只需把尾端 `= 31` 改為 `= 32`，算式本身不動）。

### P1-3　M2-16C 的 `Implementation write set` / `Files likely touched` 未包含 `tests/agent_bridge_approval_test.rs`

- **位置：** `plan-20260819.md:1030`（Implementation write set）、`:1034`（Files likely touched）；對應 AC `:992`、VER `:1008`
- **問題：** AC3 要求「以 `agent_bridge_approval_test::memory_recall_is_normal_and_record_requires_approval` 斷言」，VER 亦具名執行該用例。實測 `tests/agent_bridge_approval_test.rs` 現有用例為 `dangerous_actions_default_to_deny` / `enforce_denies_without_or_with_denied_approval` / `bind_actor_rejects_spoof` / `malformed_approval_is_invalid_params` / `dispatch_enforces_approval_for_dangerous_mutation`（`rg -n '^fn |^async fn'`），**不含該用例**，故必須由本卡新增；但該檔案在寫集與 `Files likely touched` 中皆未出現（`rg -n 'agent_bridge_approval_test'` 僅命中 `:453/:992/:1008`）。同卡另外兩個測試落點（`tests/agent_bridge_memory_test.rs`、`tests/agent_bridge_protocol_test.rs`）都已具名列入。
- **為何重要：** 執行時本卡必然改動一個宣告寫集之外的檔案，違反審計表 `writeset=serialized-at-M2-12` 的寫集合同與模板的寫集紀律（r12/r13 已為同一問題把三張卡的泛稱寫集改為具名）；ER-04 的 allowlist diff 判據會把這次改動判為越界。
- **最小修復：** 在 `:1030` 的 `tests/agent_bridge_protocol_test.rs` 之後補 `` 、`tests/agent_bridge_approval_test.rs` ``，並在 `:1034` 的括注補一句「`agent_bridge_approval_test.rs` 為既有檔案，本卡新增 `memory_recall_is_normal_and_record_requires_approval`」。`prod-files=7` 與 `landing=4` 不受影響（測試檔不計入 prod）。

### P1-4　M2-02 對 `memory_terminal_observation` 是否持久化 canonical proposal **正文** 自相矛盾（AC3/表職責 vs A9/Security）

- **位置：** `plan-20260819.md:330`（SQLite 表職責）、`:1416`（AC3）對比 `:1433`（判據塊 A9）、`:1464`（Security and privacy）
- **問題：** 表職責與 AC3 明確規定存**正文**：`:330`「`proposal_canonical_json`（由 M2-07 正規化/脫敏後的 canonical proposal **正文**，不存模型原始響應）」；`:1416`「`proposal` 以 M2-07 正規化/脫敏後的 **canonical JSON** 與 keyed digest 同事務存入」。但同一張卡的 A9 判據門寫「斷言 `memory_terminal_observation` **只含**脫敏 envelope/HMAC principal/`proposal` **digest 引用**」，Security 段也寫「**只存**脫敏 envelope + keyed digest 與 `proposal` **引用**」——兩處的「只含/只存 … 引用」把 `proposal_canonical_json` 這一欄排除在允許內容之外。
- **為何重要：** 這是持久化資料合同的內部衝突，兩種實作都會壞事：若照 A9/Security 只存 digest，則 r19 引入 `proposal_canonical_json` 所要保障的 proposal carrier 重放能力失效——M2-08 AC5⑥ 的「observation-only→附加 proposal、替換 proposal、相同 proposal 重放三態具名回歸」、`job_crash_recovery_retention_and_identity_matrix` 的「CLI stdin 補錄與 host-proposal 的 observe→crash→restart→Write Episode」、以及故障矩陣 `:2723`「proposal 附加/替換窗口」行全部沒有資料來源（重啟後只剩 digest，無法重建 proposal，且不得要求 host 重送）；若照 AC3/表職責存正文，則本卡自己的 A9 斷言門（`observer_job_schema_migration_transaction`）必然失敗。無論哪邊，M2-02 都無法按現文執行。
- **最小修復：** 把 `:1433` A9 與 `:1464` Security 的允許內容列表對齊 AC3，例如：A9 改為「斷言 `memory_terminal_observation` 只含脫敏 envelope、HMAC 化 principal、M2-07 正規化/脫敏後的 `proposal_canonical_json` 與其 keyed digest 綁定，不含模型原始響應、transcript 或可逆 principal」；Security 同步改為「只存脫敏 envelope + HMAC principal + `proposal_canonical_json` + keyed digest」。

---

## P2（3 條）

### P2-1　review log 的 Scope 欄輪次筆誤 `R30-r27/r33`

- **位置：** `plan-20260819.md:2847`、`:2848`
- **問題：** 兩行 r28 條目的 Scope 欄寫「R30-r27/**r33** 修订后的完整计划」；本計畫不存在 r33 輪。
- **為何重要：** review log 是雙 PASS 的唯一審計軌跡，輪次筆誤會讓收口時無法把被審 sha256 對回正確輪次（r22/r24/r26 已分別修過 `r26`/`r29`/`r31` 同類筆誤）。
- **最小修復：** 兩行改為「R30-r27 修訂後的凍結版（sha256 `f570cc8a…`）」。

### P2-2　M2-02 A1 的 `memory_compiler_identity` 逐列斷言未涵蓋主鍵 `handle_hmac`

- **位置：** `plan-20260819.md:1425`（A1）、`:1439`（VER 括注）；對照表職責 `:329`
- **問題：** 表職責把 `handle_hmac` 定為該表主鍵，但 A1 與 VER 括注只要求斷言 `principal_hmac/repository_id/worktree/issued_at/expires_at/revoked_at` 六列。CLI `--compiler-identity <handle>` 與 M2-08 `resolve_compiler_identity` 的查找鍵正是 `handle_hmac`。
- **為何重要：** 最關鍵的查找鍵與唯一性約束不在 schema 斷言範圍內，migration 漂移（例如漏建唯一索引）不會被 `memory_compiler_identity_schema` 捕獲。
- **最小修復：** A1 與 `:1439` 括注改為「`handle_hmac`（主鍵/唯一）+ `principal_hmac/repository_id/worktree/issued_at/expires_at/revoked_at` 共七列與約束」。

### P2-3　M2-03 的 `axis` 在卡內與審計表用詞不一致

- **位置：** `plan-20260819.md:1703`（`axis=typed ref CAS primitive`）vs `:808`（審計表 `ref CAS primitive`）
- **問題：** 同一欄位兩種寫法。其餘 24 卡的 `axis` 皆逐字一致。
- **為何重要：** 審計表宣稱是卡內 `Granularity` 的機械投影；逐欄比對腳本化時會誤報漂移。
- **最小修復：** 二者取其一（建議審計表 `:808` 改為 `typed ref CAS primitive`）。

---

**總計：P0=0，P1=4，P2=3。** P1-1/P1-2 是 r27 修訂只改了一半（Granularity + 審計表改了，waiver 行、全域計數口徑行與判據規範塊沒改）；P1-3 是同一次修訂漏掉的寫集落點；P1-4 是 r19 引入 proposal carrier 時留下的、迄今未被任何一輪觸及的資料合同內部衝突。

VERDICT: FAIL
EXIT=0
