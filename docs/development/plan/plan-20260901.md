# SB-01 pkt-line 解析 fail-closed 化计划（2026-09-01）

> **执行基线刷新（2026-09-15，R54）:** 独立main基线 `6dd2364d1e0654f01e6afa894a11e14863bb0f29`；FIX-PKT-01的签名/main/tag/gh已完成（v0.22.29），远端三平台与两项CodeQL已成功、两Linux产物下载SHA/size已核对，其余D仍待取得。PKT-01的最终0.22.30状态现已通过check/fmt/scopedClippy、五项focused、四项guard与完整默认10055/10055（零失败/重试），全量前后2346个tracked文件与HEAD一致；当前只补终态事实，准备精确八文件签名提交和实际main/tag/gh，尚不宣称该C或D完成。原十四卡及FIX卡的最终义务不缩减，旧tag示例不可作为本次证据。

### 2026-09-15 执行约定（优先于下文旧环境与命令示例）

- **VCS 与所有权：** 当前实际为 Git worktree（`.git` 文件，无 `.libra`），所有状态、精确暂存、SSH 签名+DCO 提交与非强制推送使用 Git；不转换仓库。执行者仅在自己的 `main` worktree 开发，保留既有未跟踪测试 fixture，不操作其他 agent 的分支、index、工作树或 stash。提交后检查实际 `gpgsig` 和 `Signed-off-by`，push 后核对远端精确 SHA。
- **测试：** 新进程 source 用户原 checkout 的 `.env.test` 与 `.env.live-test` 的绝对路径；本次只读实测 `CARGO_BUILD_JOBS=16`、`NEXTEST_TEST_THREADS=16`、`LIBRA_SKIP_WEB_BUILD=1`，以后仍读取最新配置而非另加线程覆盖。沿用 `CARGO_INCREMENTAL=0`、独立 target 与隔离 global/system config fixture。具名 focused 门与原 T-1/T-2 全量门不删减；全量使用无过滤默认 `cargo nextest run --all --no-fail-fast --retries 2`，单列原有 ignored、失败及 retry，不用历史 MIG 全绿替代本卡结果。沿用用户已批准的限定 target Clippy `-D warnings` 与 fmt，不运行本机 all-target/all-feature Clippy、全量 Web 构建或本机 release build/install；多平台 release 构建/安装由 D 组实际证据承接。
- **测试文件权限（R46）：** 测试与其 CLI 子进程继承调用 shell 的正常 umask（本机实测0022），不把私有日志策略的077全局传给测试。私有 evidence/config 目录0700须实测并由启动脚本检查；077仅作用于创建目录的子 shell。原077失败及同一缓存二进制的077 red/0022 green均留证，不能以单例诊断代替完整重跑。
- **超时：** 测试使用独立 tool config 的 120s slow 提示、600s 默认单例上限和10s grace；两个 MCP 案例的1200s上限来自仓库 `.config/nextest.toml`，不能仅 source env 后声称已接入超时。编译另行观察进程/CPU/日志，未输出但仍活跃的编译不当作测试死锁；超时测试终止后保留证据、分析并前滚修复，不静默跳过或降并发。
- **发布：** 唯一发布者为本执行 agent。每个独立/家族发布点在当时最新 `main` 上做 patch +1，核对五个版本面与工具链生成的 `Cargo.lock`；先推 main，再推精确 tag，随后 `gh release create -R libra-tools/libra --verify-tag --notes-file ...`，notes 总结本切片真实修改和测试。前卡 C/提交/tag/gh 已完成即可推进下一卡，不等待 D；保留 `remote-pending`，D 成功前不标 `done/complete`。十四卡最终完成条件仍要求各自适用 D 实证，不将创建 Release 等同健康发布。REL-PKT-A 三子卡仍分别本地提交、在家族树逐卡验收/审查，随 PKT-05 一起推送发布，不推出无法编译的中间态。
- **实际版本窗口（R44 修复前置后）：** 无外部抢先发布时，FIX-PKT-01→0.22.29、PKT-01→0.22.30、PKT-05→0.22.31、PKT-06→0.22.32、PKT-07→0.22.33、PKT-10→0.22.34、PKT-09→0.22.35、PKT-08→0.22.36、PKT-12→0.22.37、PKT-11→0.22.38、PKT-13→0.22.39、PKT-14→0.22.40；仅为预期，不预占 tag。PKT-01 原0.22.29只是未发布工作树，现有测试运行完成前保留不动，随后按修复窗口恢复与重验；没有旧 tag 可被覆盖。发生非 fast-forward 或版本变化则重新核实、验收和登记，不强推。PKT-05 的 Homebrew fail-closed/sentinel 义务保持；旧 D 循环的版本列表不可执行，实际记录按每卡实际 tag/SHA 绑定。
- **R44 恢复窗口与 D：** 原十四张 PKT 卡及113个具名行为测试不删减，另加 FIX-PKT-01 的四个回归。FIX-PKT-01 独立审查/提交/发版，不是家族子卡；测试修复先行，PKT-01 的已审查补丁在私有证据目录归档并保持哈希可核对。由于新增恢复卡也发生在 PKT-05 workflow 修正前，旧 R43 的单一首卡 sentinel 豁免显式扩展为两个已登记切片（FIX-PKT-01、PKT-01）的实际 tag/SHA：两者仍必须取得8 job、formula-commit-sha、实际 tap 版本及其余 D 证据，不能假称已有 homebrew-verify-pass artifact；PKT-05 及全部后继仍必须取得真实 sentinel。此处只调整前置恢复的实际版本身份，不扩展 PKT 行为范围，不使用历史 tag 的绿色记录。
- **网站与文档：** 用户可见变化按 ER-06a 同卡同步 EN、zh-CN、开发文档及网站；网站仍是原 checkout 的逻辑 sibling `libra-backend`，使用已核实的实际路径，不在临时 worktree 旁臆造仓库。写前必须再次确认唯一 Git metadata、`cf` 分支和精确目标无无关修改。PKT-01 是未接入调用方的纯 helper，用户文档/网站 N/A；后续每卡开工前刷新其文档写集及语义门。
- **开工复核：** `src/git_protocol.rs:81-103` 的 panic 解析器与三处异步读取仍未修复；`src/internal/protocol/mod.rs:72-149`、fetch 锚点未漂移；push parser 现为 `src/command/push.rs:2249-2300`，pull discovery mapper 现为 `src/command/pull.rs:1013`。当前 `release.yml:641-688` 仍有吞错的 Homebrew verify，尚无 `homebrew-verify-pass` artifact，不能视为 PKT-05 已实现。执行前还须复核跨卡测试的阶段可达性、共享类型/转换的写集归属与完整审查结论；本段不预先认证任何任务门。

**R41 开工实测（尚非任务验收）：** 本机 fmt check 通过；双 env + 独立 target/config scopes 下，`compat_all_production_unwrap_guard`、`compat_extra_production_unwrap_guard`、`compat_version_surface_sync` 三个 target 共4项通过，退出0，编译2m56s（仅第三方既有future-incompatibility提示），未运行全量。一次临时shell版本核对因未处理Windows CRLF返回非零；读取确认五面实际均0.22.28，修正只读核对脚本后通过，仓库installer未改动。检查期间远端main新增九份issue计划文档，核对源码/测试/配置/版本面零变化后，已非强制快进到 `4b96a5256e14c9d686c796e8036bdc7f782cc061`；本计划未提交修改保留，原feature工作树未动。独立Claude Code只读R41审查已退出0，实际 verdict 为 FAIL（B1/B2、L1–L5）；R42复审亦已退出0，实际 FAIL（后继 PKT-07→10 marker 保持性 F1），并明确 PKT-01 READY=YES；R43已补该契约，尚不宣称全计划PASS；baseline守卫通过不代替新功能测试、全量或审查PASS。

> **测试标准迁移（2026-09-12，模板 v2.5）:** 本计划尚未执行。所有未执行任务卡、ER-13 全量门及完成判据中的全量收口门统一运行 `source .env.test && source .env.live-test && cargo nextest run --all --no-fail-fast --retries 2`。既有 focused 命令与历史运行记录不变；本计划中旧的 `cargo test --all` 表述在上述范围内失效，仅可用于诊断，不能作为通过证据。

> 模板版本: v2.2（按 [`plan-template.md`](plan-template.md) 成稿；整份符合 v2.2，含 ER-13 分层测试执行、每卡 `Full-suite trigger` 登记与 G-08 家族卡）
> **测试标准迁移（2026-09-12）:** 本计划尚未执行的所有任务卡及计划收口门采用模板 v2.5：全量测试一律运行 `source .env.test && source .env.live-test && cargo nextest run --all --no-fail-fast --retries 2`。既有 focused 命令保持不变；`cargo test --all` 仅限诊断，不构成通过证据。
> 触发: 用户指令（2026-09-03）——依据 `plan-long.md` 第十次竞品审计（2026-09-03（第十次））Top-1 差距（SB-01），要求按模板立计划承载 SB-01 的 pkt-line 切片任务，经 **Codex 与 Claude 双评审至 PASS** 后与 `plan-long.md` 一并提交。文件名 `plan-20260901.md` 为用户指定（成稿日 2026-09-03）。
> 关系: 承接 `plan-long.md` 的 **SB-01**（消除生产路径可触发 panic）；不触碰 `plan-20260825.md` / `plan-20260827.md` / `plan-20260830.md` 的写集。

## 文档职责

本文解决 **Libra 网络协议 pkt-line 解析路径可被远端畸形输入触发 panic / 巨量分配，且协议错误在多个 CLI 边界被错误归类为 `LBR-NET-001`、错误文本回显远端字节** 的问题。目标是：pkt-line 帧解析全面 fail-closed（同步解析器 + fetch/git/ssh 异步读取循环与头部解析），引入树内类型化错误 `PktLineError` 与 `PushError::Protocol`，在全部受影响的 CLI 映射点（fetch / clone / ls-remote / pull / push）以固定 marker 适配归入 `LBR-NET-002`，**本计划新写或改写的错误文本**零远端字节回显（协议解析、传输错误与 SSH 诊断路径），帧长校验单一事实源（GC-02）。

本文只规划任务，不宣称实现完成。落地时每个任务都必须先刷新源码锚点，再按任务卡验收。

### 适用范围

- `src/git_protocol.rs`（帧长校验 helper、marker 前缀常量、同步 `read_pkt_line`、`PktLineError` 类型及单测）。
- `src/internal/protocol/mod.rs`（`:92` 调用方传播与 marker 契约；`:147-149` capability 回显收口）。
- `src/command/push.rs`（`:2120` / `:2131` 传播、`PushError::Protocol` 新变体、`:878-900` discovery 映射 marker 分支、CLI 映射 `Protocol → LBR-NET-002`、`:2149-2151` 状态行改 `Protocol` 分类及 `:3434-3443` 锁定测试更新）。
- `src/command/fetch.rs`（`map_fetch_discovery_error:943-957` marker 适配、`:882-887` FetchObjects 分支 marker 适配、`:3650-3668` `read_hex_4`、`:3672-3680` 异步读取）。
- `src/command/pull.rs:267,928-944,989-1001`（Discovery 与 FetchObjects 映射 marker 适配，R13/R15 Codex P1-8）。
- `src/command/ls_remote.rs:210-221`、`src/command/clone.rs:1012-1046,1059-1063`（discovery 与 fetch 阶段错误映射 marker 适配）。
- `src/command/clone.rs:1024-1028`（ssh-keyscan 盲追加提示收口，R17 Codex P1-1）、`src/internal/protocol/git_client.rs:138-156,170-173`、`src/internal/protocol/ssh_client.rs:164-204,218-257,273-284,296-299,319-329,429-433,449-459,491-497,535-550`（读取循环与头部解析下界/marker 化；ssh 交互判定与 BatchMode/inherit 分支 :164-204；git discovery 包装点 :173 透传；`:273-284` / `:319-329` / `:449-459` 三处 pkt-line 包装点脱敏；`:296-299` / `:429-433` / `:491-497` process-output 脱敏；`describe_process_output`（`:535-537`）与 `describe_status_with_stderr`（`:539-550`））。
- 测试：上述文件内 `#[cfg(test)]` 单测（无新增 `[[test]]` target）。
- 文档：`docs/error-codes.md`（复用 `LBR-NET-002`，无新增条目核对）、`COMPATIBILITY.md`（错误归类行同步，PKT-10 承载；R32 Codex P1-3 口径更正）。

### 非目标

- SB-01 其余面（DB / HEAD / ToolRegistry 全面 fallible）：尚未排期（DEFER-02）。
- `git_internal` 外部 crate 的写 / index-pack delta 防护及为其 `GitError` 新增变体：外部依赖（DEFER-01）。
- `RemoteDiscoveryError` 全面类型化迁移：独立立项（DEFER-04）。
- **远端 `ng` 拒绝原因透传维持现状**（ADR-PKT-02）。
- **既有未触碰回显面的收口**：`https_client.rs:390-392` 的远端 `Content-Type` 回显等既有面不在本计划写集内，登记 DEFER-06；本计划「零回显」口径限定于新写或改写的错误文本（见「成功定义」）。
- pkt-line 写路径（`add_pkt_line_string` 上限）：写方向非远端可触发面（永久非目标）。
- 传输层重试/重连策略：维持现状（ER-10 口径）。
- CHANGELOG 0.22.1..0.22.10 文档债：随下一次 release 收口（DEFER-03）。

### 成功定义

- 用户/系统行为变化：远端返回畸形 pkt-line（非 hex 头、不足 4 字节、`len∈1..3`、声明 payload 超过实际到达/剩余字节）时，fetch/clone/ls-remote/pull/push（git/ssh/https 三传输、discovery/upload-pack/receive-pack 三阶段）以稳定错误 `LBR-NET-002` 干净失败，不再 panic、不发生 ~4 GiB 级回绕分配；**本计划新写或改写的错误文本**零远端字节回显（协议解析、传输错误与 SSH 诊断路径——pkt-line 三点 + process-output 全部用户可见面）；既有未触碰面（https `Content-Type` 回显）不在口径内（DEFER-06）；合法帧（flush `0000`、`len==4` 空 payload、`0xffff` 上限帧）语义不变；非协议网络错误仍归类 `LBR-NET-001`；receive-pack 状态行语法违规归类 `PushError::Protocol`（`LBR-NET-002`，与同函数 `unpack`/`ng` 分支一致）；远端 `ng` 拒绝原因按 Git 惯例透传（ADR-PKT-02）。
- 机器接口变化：无新增错误码 / flag / schema；协议错误显式映射 `StableErrorCode::NetworkProtocol`（`src/utils/error.rs:375`）。
- 文档/测试/发布证据：`docs/error-codes.md` 无需新增（核对登记）；具名单测覆盖畸形矩阵、全部边界映射、零回显与合法帧回归（全部标 `(new)`，经失败闭合存在性脚本验证）；每个发布切片独立 bump + `gh` release + 全部 8 个 release job 记录的 D 组证据。
- 完成标记：十四张任务卡全部 `done/complete` + 全量收口门全绿 + Codex/Claude 双评审 PASS。

## 事实基线

> 所有行号和源码锚点必须在开工当天刷新（ER-02）。以下锚点核对于 2026-09-03（R5 评审复核，含 Claude R5 P2-1 的函数区间更正）。

| 类别 | 当前事实 | 证据 |
|---|---|---|
| 代码入口 | 同步 `read_pkt_line`（函数范围 `:81-103`）：`copy_to_bytes(4)` 短头 panic；非 UTF-8 `expect`（:90）、非 hex `panic!`（:92）、payload 拷贝 `copy_to_bytes(pkt_length - 4)`（:99）无下界/截断防护 | `src/git_protocol.rs:81-103`；INVARIANT 注释 :86-88 |
| 错误类型 | `GitError` 来自外部 crate `git_internal`，树内无法新增变体；`PushError` 为树内枚举（`:235`） | `src/git_protocol.rs:13`；`src/command/push.rs:235` |
| 边界映射（fetch） | `map_fetch_discovery_error`：`NetworkError(_) → NetworkUnavailable`（LBR-NET-001）、`IOError → map_fetch_io_error`、`_ → NetworkProtocol`（LBR-NET-002 兜底） | `src/command/fetch.rs:943-957` |
| 边界映射（fetch-objects） | `FetchError::FetchObjects` 走 `map_fetch_io_error` → `NetworkUnavailable` | `src/command/fetch.rs:882-887` |
| 边界映射（push） | discovery 映射：`NetworkError(detail)` 先判 timeout 否则 `DiscoveryFailed`、`other => DiscoveryFailed`；CLI 映射 `DiscoveryFailed`/`Network(..) → NetworkUnavailable` | `src/command/push.rs:878-900`、`:374-376`、`:404-406` |
| 边界映射（ls-remote） | `Discovery` 分支：`NetworkError(_)|IOError(_) → NetworkUnavailable`；`_ → NetworkProtocol` | `src/command/ls_remote.rs:210-221` |
| 边界映射（clone） | `FetchError::Discovery`：`NetworkError(_) → NetworkUnavailable`；`FetchError::FetchObjects`/`PacketRead` 无条件 `NetworkUnavailable`（`clone.rs:1059-1063`）；`_ → NetworkProtocol` |
| 边界映射（pull） | pull 的 Discovery 与 FetchObjects 路径把协议形状错误映射 `NetworkUnavailable`（`LBR-NET-001`） | `src/command/pull.rs:267,928-944,989-1001`（R13 Codex P1-3） |
| push 回显与归类 | `PushError::Network(format!("unexpected receive-pack status line: {line}"))` 回显远端状态行且归类 `LBR-NET-001`；同函数 `unpack` 失败（`RemoteUnpackFailed`）与 `ng` 格式错（`RemoteRefUpdateFailed`）均归类 `LBR-NET-002`；既有测试锁定逐字回显 | `src/command/push.rs:2149-2151`、`:398-403`、`:3434-3443` |
| 代码入口（fetch 异步） | `read_hex_4` 返回 `u32`，`(len - 4) as usize` 在 `len∈1..3` 回绕 → **一次性 ~4 GiB 级分配 + EOF/超时**（release 不必然 panic）；`read_hex_4` 错误文本回显远端原始头且无 marker 前缀 | `src/command/fetch.rs:3650-3680`（:3677；lossy 回显 :3653-3661、字面 `'{hex_str}'` 插值 :3665）；调用方 `:2349` |
| 代码入口（git/ssh） | `read_advertisement` 循环：`len: usize`，`len∈1..3` 回绕 → 确定性 capacity overflow panic；头解析错误文本两处字面不同——git 为 `Invalid pkt-line length: {e}`（大写 I）、ssh 为 `invalid pkt-line length: {e}`（小写 i），均无 marker 前缀；git discovery 包装点 `"Failed to read response: {e}"`（:170-173）会破坏 starts_with | `src/internal/protocol/git_client.rs:138-156`（:151、:143-146、:170-173）、`src/internal/protocol/ssh_client.rs:218-257`（:243、:235-238；循环 :223-255） |
| ssh stderr 回显（pkt-line 包装点） | `describe_process_output`（**:535-537**，转发 `describe_status_with_stderr`）回显 stderr，共 3 处 read 错误路径 | `src/internal/protocol/ssh_client.rs:273-284`、`:319-329`、`:449-459` |
| ssh stderr 回显（process-output 全路径） | 非零退出/状态路径 3 处 + 2 个格式化函数：`:296-299`、`:429-433`、`:491-497`；`describe_status_with_stderr`（**:539-550**）；**交互路径 `Stdio::inherit()`（`:198-203`）远端 stderr 直通用户终端**（R9 Codex P1-4） | 同左 |
| 既有回显面（不在写集） | `mod.rs:147-149` 回显远端 capability（PKT-03 收口）；`https_client.rs:390-392` 回显远端 `Content-Type`（DEFER-06） | 同左 |
| discovery API | `RemoteClient::discovery_reference` 返回 `Result<_, GitError>`；https 直连路径调用 `parse_discovered_references` | `src/command/fetch.rs:190-200`、`src/internal/protocol/https_client.rs:405` |
| 稳定码 | `LBR-NET-002` = packet-line/sideband/协议错误；分类器 `is_network_protocol_error` 命中 `pkt-line` 短语 → `NetworkProtocol`（兜底层） | `docs/error-codes.md:106`、`src/utils/error.rs:375`、`:1488-1490`、`:1680-1701` |
| 测试守卫 | 生产守卫 target（**只扫描裸 `.unwrap()`**；`compat_extra` 的 `TARGET_FILES` 为固定 9 文件清单，仅 `git_protocol.rs` 与本计划相关） | `Cargo.toml`：`compat_extra_production_unwrap_guard`（`:246-248`）、`compat_all_production_unwrap_guard`（`:250-252`）；`tests/compat/extra_production_unwrap_guard.rs`（`TARGET_FILES`） |
| 外部参照 | jj@0a9b86970、dolt@01dea76505、go-git@2ef9e4b0（SB-01 优先级证据，E2） | plan-long 第十次审计快照（2026-09-03 核对） |

### 当前缺口

| ID | 缺口 | 影响 | 证据 | 计划动作 |
|---|---|---|---|---|
| GAP-01 | 帧长校验分散且缺失 | 解析可被畸形输入打崩/巨量分配 | `src/git_protocol.rs:95,99` | PKT-01 |
| GAP-02 | 同步解析 panic 面及调用方传播缺失 | fetch/push 网络可触发 panic（P1） | `git_protocol.rs:85,90,92`、`mod.rs:92`、`push.rs:2120,2131` | PKT-02/03/04（家族） |
| GAP-03 | 协议错误在全部 8 个 CLI 映射点（含 clone fetch 阶段）被显式归为 `LBR-NET-001`（8 为逻辑映射点口径；各点的 `PacketRead` 子臂细化由 PKT-10 门 3/4/21-24 覆盖，不改变 8 点计数，R34 Codex P2-1） | 稳定码归类错误，误导重试 | `fetch.rs:943-957,882-897`、`push.rs:878-900,374-376,404-406`、`ls_remote.rs:210-221`、`clone.rs:1012-1046,1059-1063`、`pull.rs:267,928-944,989-1001` | PKT-04/06/09/10 |
| GAP-04 | fetch 异步循环回绕大分配；`read_hex_4`/git/ssh 头解析错误无 marker 且回显；push 状态行回显且归类错误 | 内存尖峰 + 信息泄漏 + 归类失真 | `fetch.rs:3677,3653-3666`、`git_client.rs:143-146`、`ssh_client.rs:235-238`、`push.rs:2149-2151` | PKT-07/09/13 |
| GAP-05 | git/ssh 循环回绕 panic；ssh 6 处用户可见 stderr 回显面 | 网络可触发 panic；错误文本泄漏远端数据（恶意远端可非零退出绕过三点 sentinel） | `git_client.rs:151`、`ssh_client.rs:243,273-284,296-299,319-329,429-433,449-459,491-497,535-550` | PKT-08/11/12 |

## 与其它计划的关系

| 计划/文档 | 关系 | 本计划处理 |
|---|---|---|
| `plan-long.md` | SB-01（横切工程门禁）pkt-line 切片 | 完成后更新「日期计划索引」行与 SB-01 状态 |
| `plan-20260827.md` / `plan-20260830.md` | 同为横切（SB-04 / SB-02）先例 | 不触碰其写集 |
| `plan-template.md` | v2.2 结构与 ER/GC/G 规则事实源 | 全文按其执行；冲突时以模板为准 |
| `docs/error-codes.md` | `LBR-NET-002` 已覆盖 packet-line 协议错误 | 复用，不新增码（ADR-PKT-01）；PKT-13 统一核对登记 |

## 评审结论与修订记录

成稿自审（R0–R5，2026-09-03；R6 起转入 Codex/Claude 外部双评审，逐轮处置见「修订历史」与「Codex review log」）：

| 维度 | 结论 | 修订动作 |
|---|---|---|
| 合理性 | 网络 panic / 巨量分配 / 错误归类失真 / 回显是 AGENTS.md P1，且为第十次审计 Top-1 | 无 |
| 可行性 | 十四卡均为有界改造；家族卡 + staging 流程解决编译不可分割；边界矩阵覆盖 8 映射点中的 6 个（余 2 点归 PKT-04/06） | R4/R5 扩展 |
| 任务卡粒度 | 十四卡各单一行为轴，AC 按独立谓词逐条计数 ≤8（PKT-08 经 EX-05 登记为 10 门门族：8 条客户端×帧类谓词 + payload/partial-header EOF 两门，R25 扩展） | R5 按 Codex P1-1 再拆 |
| 依赖与顺序 | 家族 → 发布点 → 三条独立链；同文件卡串行；发布串行 | R4/R5 补齐 |
| 完整性 | 测试（畸形矩阵+8 映射点锚定+零回显 sentinel+合法帧回归+泛化分支回归+marker 头解析）、失败闭合 fn 存在性脚本、文档核对、D 组 8 job 记录（R24 更正） | R5 按 P1-5 补齐 |
| 安全性 | fail-closed；零回显口径显式化（新写/改写文本；capability 收口入 PKT-03；https Content-Type 登记DEFER-06；ng reason 豁免 ADR-PKT-02） | R4/R5 决议 |
| 功能正确性 | 合法帧语义保持；状态行语法违规归类与同函数兄弟分支一致（NET-002） | R5 按 Claude P1-1 修正 |
| 接口兼容 | 无新增公开错误码；`PushError::Protocol` 为树内枚举新增变体 | R3 说明 |
| 数据流与控制流 | 解析纯函数；失败即中止；家族 staging 保证编译完整性 | R5 按 P1-6 修正 |
| 性能与容量 | O(1)；消除 ~4 GiB 误分配 | R2 修正 |
| 可靠性与容错 | 失败路径：中止当前操作、错误上抛，无半解析状态 | 无 |
| 可维护性 | helper/marker 常量单点 + 类型化错误 + 共享脱敏/头解析辅助 | R5 强化 |

### 修订历史

**2026-09-15 执行修订 R41（待审）：** 根据用户本轮目标，登记独立 Git worktree/main、真实0.22.28基线、动态patch窗口、沿用原双env/16槽/隔离/超时与限定本机构建范围、异步D及内容化gh notes；保留十四卡、REL-PKT-A、全部行为验收与D完成义务。受影响引用为执行命令、版本窗口、VCS/签名、ER-06a网站路径及最终证据；不改写R1–R40历史，也不把旧tag结果记为本计划通过。R41 实际 FAIL 见下方新增历史行；R42 修订待复审，不预先登记 PASS。

| 日期 | 触发 | 变更内容 | 原卡 → 新卡 | 受影响的引用 |
|---|---|---|---|---|
| 2026-09-12 | 用户指令：全量测试标准迁移 | 尚未执行任务卡、ER-13 全量门与完成判据收口门改用模板 v2.5 的 `cargo nextest` 命令；focused 命令不变，`cargo test --all` 仅作诊断 | 无卡拆分 | 顶部迁移注记、ER-13、完成判据 |
| 2026-09-03 | R0 成稿（用户指令 + 第十次审计 Top-1） | 初稿：PKT-01/PKT-02 两卡 | — | 全文 |
| 2026-09-03 | Codex R1 FAIL（P1-1..8）+ Claude R1 未产出（模型限额，该轮无判定，R2 起补位） | 两卡拆三卡；截断帧；ADR 固定短语+分类器；零回显；具名 fn；D 组；immutable-release；补 G-*/defaults | @R0 两卡 → @R1 三卡 | 全文 |
| 2026-09-03 | Codex R2 + Claude R2 FAIL | ADR 类型化+显式映射；短头；ssh stderr；守卫 target 更正；计数重构；T-1；D 组 5 job；fetch 定性修正 | @R1 三卡 → @R2 五卡 | 全文 |
| 2026-09-03 | Codex R3 + Claude R3 FAIL | G-08 家族卡；marker 适配 + DEFER-04；PushError::Protocol；单轴重切；ssh 三处补全；push.rs:2149 纳入；@R 世代标记；守卫口径修正 | @R2 五卡 → @R3 五卡（重指派） | 全文 |
| 2026-09-03 | Codex R4 FAIL（P1-1..5）+ Claude R4 仅 P2×2（PASS，具名接受） | 家族生命周期修正；PKT-04/09 拆分；PKT-10 边界矩阵；ADR-PKT-02（ng reason 豁免）；PKT-11 SSH process-output；锚点更正；显式边 | @R3 五卡 → @R4 十一卡 | 全文 |
| 2026-09-03 | Codex R5 FAIL（P1-1..7）+ Claude R5 FAIL（P1×1、P2×3） | PKT-08 拆客户端×帧类 8 谓词；PKT-10 纳入 clone fetch 阶段；PKT-13 异步头解析 marker 化；capability 收口入 PKT-03 + DEFER-06；失败闭合 fn 存在性脚本 + (new)；REL-PKT-A staging 流程；plan-long 索引同步 | @R4 十一卡 → @R5 十四卡（新增 PKT-12/13） | 全文 |
| 2026-09-03 | Codex R6 基础设施故障（chatgpt.com 404，未产出评审，R7 重跑）+ Claude R6 FAIL（P1×1、P2×1） | marker 检测语义裁决 starts_with（串首锚定）；SSH 三点对 marker 错误原样透传；PKT-03 增检测语义契约 AC；PKT-01/06/07/13 VER 更正 | PKT-12 @R5 → @R6（口径重写，编号不变） | ADR-PKT-01、PKT-03、PKT-12、PKT-13、测试矩阵、粒度审计表 |
| 2026-09-03 | Codex R7 FAIL（P1-1..4）+ Claude R7 FAIL（P1×3、P2×2） | PKT-13 纳入 git_client.rs:173 包装点透传与 git:// 回归；12 卡 Verification 内联完整非缩写 expected；PKT-11 debug 只记状态/长度/摘要；D 组绑定 tag ref；默认覆盖表补齐；审计表同步；PKT-02 补 compat_extra+compat_all；PKT-03/04/09 补 compat_all；标点清理 | @R6 十四卡 → @R7 十四卡 | 全文 |
| 2026-09-03 | Codex R8 FAIL（P1-1..5）+ Claude R8 FAIL（P1×3、P2×4） | PKT-10 门族 waiver EX-01（AC=20/8@EX-01）；PKT-13 Granularity.deps 补 PKT-07、AC=7/8；tag 顺序修正（libra tag+push 先行）；PKT-05 过滤查询；判定口径去占位符+矩阵全名；锚点批量更正；PKT-13 git/ssh 零命中守卫；DEFER-06 休眠风险注记 | @R7 十四卡 → @R8 十四卡（新增 EX-01） | 全文、waiver 表、粒度审计表、测试矩阵 |
| 2026-09-03 | Codex R9 FAIL（P1-1..5）+ Claude R9 FAIL（P1×1、P2×5） | PKT-07 验收收窄（AC=5/8，零回显移交 PKT-13）；零命中守卫与 fn 存在性脚本全部内联自包含；PKT-10 exception=EX-01 + 16 门→命令映射；PKT-11 纳入交互路径 Stdio::inherit（EX-02）；ADR-PKT-02 ng reason 转义+限长；锚点批量更正（error.rs :1680-1701 等） | @R8 → @R9 十四卡（新增 EX-02） | 全文、粒度审计表、测试矩阵 |
| 2026-09-03 | Codex R10 FAIL（P1-1..5）+ Claude R10 FAIL（P1×2、P2×2） | ADR-PKT-02 扩展 refname 校验与共用净化；PKT-07/PKT-13 sentinel 归属调整；新增 ADR-PKT-03（SSH 捕获路径 BatchMode、host-key fail-closed 指引，DEFER-07 登记）；EX-01 理由精确分解；适用范围/事实基线补 git_client.rs:170-173；PKT-06 证据分支边界更正 | @R9 → @R10 十四卡（新增 ADR-PKT-03、DEFER-07） | 全文、测试矩阵 |
| 2026-09-03 | Codex R11 FAIL（P1-1..6）+ Claude R11 FAIL（P0×1、P1×4、P2×2） | 修订历史确定性重建（去重、严格时序，本行）；零回显 sentinel 归属唯一化至 PKT-13 并入其执行命令；EX-01 门→命令映射全名可执行；PKT-09 拆出 PKT-14（ng 输入校验与渲染卫生）；ADR-PKT-03 指引去除盲追加 ssh-keyscan；PKT-11 文档面扩展（docs/commands/{clone,fetch,config}.md EN+zh + COMPATIBILITY.md）；ADR-PKT-03 影响面表述更正 + ssh_client.rs:164-204 纳入范围；追溯表 fn 计数同步；矩阵 SSH 行去重+反引号修复；PKT-11 判据规范块（EX-02 补偿条件）；lossy 回显锚点 :3653-3661；plan-long 索引补 ADR-PKT-03 | @R10 十四卡 → @R11 十四卡（新增 PKT-14） | 全文、waiver 表、粒度审计表、测试矩阵、追溯表 |
| 2026-09-03 | Codex R12 会话超时中断（R13 重跑）+ Claude R12 FAIL（P1×5） | ①PKT-12 幽灵 sentinel AC 移除（归属唯一化至 PKT-13，PKT-13 AC=8/8，R12 Claude P1-1/Codex P1-1）；②PKT-11 计数与 EX-02 算术对齐判据规范（AC=11/8@EX-02，去「3 处 pkt-line 邻接」错项，R12 Claude P1-2）；③发布窗口顺序单调化（PKT-14 置于 v0.22.21 末位，禁止版本倒退，R12 Claude P1-3）；④plan-long 索引补 PKT-14（R12 Claude P1-4）；⑤PKT-11 文档面补 `docs/development/commands/{clone,fetch}.md`（R12 Claude P1-5） | @R11 十四卡 → @R12 十四卡（内容修订，编号不变） | 全文、粒度审计表、测试矩阵、waiver 表、plan-long 索引 |
| 2026-09-03 | Codex R13 FAIL（P1-1..5）+ P2-3 + Claude R13 未产出（该轮无判定） | ①修订历史 R11/R12 顺序更正 + G-09/完成判据口径改为 R1–R13（P1-1）；②PKT-14 补粒度审计行/Phase/Milestone/完成条件（P1-2）；③pull.rs 两处映射点纳入 PKT-10（门族 20 门，expected/矩阵/追溯同步，P1-3）；④PKT-12 增 ssh read-error kill+reap AC 与回归（P1-4）；⑤PKT-13 双过滤命令拆分（VER=6/8，P1-5）；⑥PKT-14 收口守卫覆盖全部写集生产文件新增行（P2-3） | @R12 十四卡 → @R13 十四卡 | 全文、粒度审计表、测试矩阵、追溯表、里程碑 |
| 2026-09-03 | Codex R14 未产出（两次 900s 超时；R15 单独重跑 30min）+ Claude R14 FAIL（P0×1、P1×3、P2×3） | ①修订历史 R13/R12 顺序互换更正（P0-1）；②PKT-14 审计行归位（误贴追溯表行移除 + 审计表补第 14 行，P1-1）；③PKT-12 登记 **EX-03**（AC=9/8@EX-03，判据规范块，P1-2）；④EX-01 门→命令映射与判据规范编号统一（1-20 对齐，P1-3）；⑤成功定义「十四张任务卡」（P2-1）；⑥waiver 计数「3 项」（P2-2）；⑦Cargo.toml compat_all 锚点 :250-252（P2-3） | @R13 十四卡 → @R14 十四卡（新增 EX-03） | 全文、waiver 表、粒度审计表、测试矩阵、追溯表 |
| 2026-09-03 | Codex R15 FAIL（P1-1..8）+ Claude R15 FAIL（P1×1、P2×2，见后） | ①PKT-02/03 纳入空 discovery 响应 fail-closed（P1-2）；②PKT-07 纳入 reach_pack 截断 EOF 语义与回归（P1-1）；③EX-01 重复编号清理、pull 门入映射（P1-3）；④EX-02 门 11 文档检查可执行化（P1-4）；⑤PKT-09 文档闭环 push.md + error-codes 核对（P1-5）；⑥PKT-14 收口守卫命令化（P1-6）；⑦ADR-PKT-03 禁 ssh-keyscan 管道写入（P1-7）；⑧pull 统一入职责/成功定义/ADR/证据/摘要/plan-long 索引（P1-8） | @R14 → @R15 十四卡 | 全文、测试矩阵、plan-long 索引 |
| 2026-09-03 | Codex R16 未产出（该轮无判定，R17 重跑覆盖）+ Claude R16 FAIL（P0×1、P1×3、P2×2） | 修订历史 R12-R15 物理重排（rows 排序重建，追加规则=表尾追加）；EX-01 映射三组重复项删除；ADR-PKT-03 Decision 去 ssh-keyscan（与 PKT-11 AC 一致）；PKT-09 文档义务归位（自身 Docs+AC，PKT-11 收窄）；G-09/完成判据改「修订历史全部行」；ADR-PKT-01 Context 补 pull.rs 两锚点 | @R15 → @R16 十四卡（内容修订） | 全文、plan-long 索引 |
| 2026-09-03 | Codex R17 FAIL（P1-1..8）+ Claude R17 FAIL（P0×1、P1×3、P2×3） | ①clone.rs:1024-1028 ssh-keyscan 提示收口入 PKT-11（Codex P1-1）；②git/ssh payload 截断 EOF 入 PKT-08（Codex P1-2）；③PKT-14 diff 基线 v0.22.10..HEAD（Claude P1-2）；④BatchMode passphrase 影响披露（Claude P1-3）；⑤PKT-03 EX-04（Codex P1-4）；⑥PKT-12 exception=EX-03（Codex P1-5）；⑦PKT-10 门数统一 20（Codex P1-6）；⑧PKT-11 Docs/AC/写集一致（Codex P1-8）；⑨plan-long 归属 PKT-04/06/10（Claude P2-1）；⑩PKT-09 docs 命令化（Claude P2-2）；⑪pull.rs 锚点拆分（Claude P2-3）；⑫R16 行补登（Claude P0-1） | @R16 → @R17 十四卡（新增 EX-04） | 全文、waiver 表、粒度审计表、plan-long 索引 |
| 2026-09-03 | Codex R18 FAIL（P1-1..8）+ Claude R18 FAIL（P1×5） | ①PKT-11 passphrase/洪水 fn 入 expected/矩阵/门族（12 门）；②PKT-03 空响应 2 fn；③PKT-04 泛化分支 2 fn（去占位符）；④PKT-10 16→20 门统一；⑤PKT-08 typed frame-read error；⑥PKT-14 diff fail-closed；⑦PKT-11 stderr 洪水上限；⑧review log 补 R16/R17 | @R17 → @R18 十四卡 | 全文、测试矩阵 |
| 2026-09-03 | Codex R19 FAIL（P1-1..5）+ Claude R19 FAIL（P0×3、P1×5） | PKT-03 判据规范块（EX-04 门数 9）；PKT-08 EX-05 登记；修订历史/评审日志补 R18 行；PKT-11 五处 12 门统一；PKT-09 VER=6/8；PKT-14 VER=3/8；PKT-10 checkbox 20；PKT-08 矩阵/追溯第 9 fn | @R18 → @R19 十四卡 | 全文、粒度审计表 |
| 2026-09-03 | Codex R20 FAIL（P1-1..9）+ Claude R20 PASS（P2×3 residual，具名接受=用户） | R19 全部处置落地；R19/R20 行补登；Codex R20 P1-1..9 逐项收口（①历史 R19 行；②PKT-08 Granularity；③PKT-03 端到端归属；④fail-open 措辞；⑤PKT-10 门数；⑥逐卡 D 组；⑦PKT-05 tag 顺序；⑧文档责任；⑨PKT-13 deps——与 review log R20 行逐条对应，R27 Codex P1-5） | @R18 → @R20 十四卡 | 全文 |
| 2026-09-03 | Codex R21 FAIL（P1-1..11）+ Claude R21 FAIL（P1×5、P2×2） | 修订历史去重、review log 补 R19/R20、EX-01 门映射去重、waiver 计数 5 项、PKT-11 config.md 补入、PKT-11 landing 2/2、PKT-02 fail-closed 措辞、PKT-10 门数统一、PKT-05 tag 顺序、PKT-13 deps 补 PKT-08、PKT-10 文档责任 | @R20 → @R21 十四卡 | 全文 |
| 2026-09-03 | Codex R22 FAIL（P1-1..8）+ Claude R22 FAIL（P0×1、P1×2、P2×2） | 修订历史严格时序重建（去重排序）、review log 同轮相邻重建、EX-01 门映射逐条唯一化、PKT-08 Granularity 9/8@EX-05 统一、PKT-11 13/8@EX-02 统一、push 缺 flush fail-closed、"十三"→"十四"清理、INVARIANT 锚点 :86-88 | @R21 → @R22 十四卡 | 全文 |
| 2026-09-03 | Codex R23 FAIL（P1-1..18）+ P2×2 | ①补 `## 已决议设计决策` 父章节（P1-1）；②修订历史 R21/R22 时序重排（P1-2）；③review log 严格时序重建+去重（P1-3）；④PKT-09 AC=6/8（P1-4）；⑤PKT-10 AC 重构为 20 门 checkbox+删重复摘要段（P1-5）；⑥EX-02 12→13 门、判据规范重编号、文档检查补 `ssh.strictHostKeyChecking`（P1-6/P1-15）；⑦PKT-13 VER=5/8（P1-7）；⑧审计表从卡面 Granularity 逐字重生成+PKT-08 重复 exception 清理+PKT-13 deps 补 PKT-08（含 Dependencies 字段与显式边）（P1-8）；⑨测试矩阵行与 expected 集合对齐（分类器 fn 入 PKT-02 expected）（P1-9）；⑩追溯表 PKT-09 4 fn、PKT-12 8 fn（P1-10）；⑪PKT-01/02 守卫 panic 字面量正则修正（改为匹配 `panic!(`）（P1-11）；⑫占位符清除（D 组收口循环、PKT-05 取证、判据口径示例）（P1-12）；⑬D 组 event/ref 显式锁定+逐 job conclusion 断言（P1-13）；⑭EX-01 门 5-6 重复映射删除（P1-14）；⑮PKT-09 文档检查补 `docs/error-codes.md`（P1-16）；⑯PKT-12 零回显 sentinel 作用域拆分澄清（SSH 三点 vs fetch 异步）（P1-17）；⑰PKT-10/11 Estimated scope 4/4、2/2（P1-18）；⑱PKT-10 证据表补 pull 两映射点行（P2-1）；⑲自审范围说明更新（P2-2） | @R22 → @R23 十四卡 | 全文、waiver 表、粒度审计表、测试矩阵、追溯表、实施顺序、review log |
| 2026-09-03 | Codex R24 FAIL（P1-1..10）+ P2×2 | ①计划级判据口径脚本具体化为 PKT-01 完整实例（去未定义 `$expected`）（P1-1）；②PKT-03 门→命令拆为两条可复制命令并澄清分类器 fn 归属（P1-2）；③测试矩阵 PKT-02/PKT-04 expected 集合各自合并为单行（P1-3）；④测试矩阵全部 Rust 命令补 `source .env.test &&` 前缀（P1-4）；⑤review log Result 统一精确枚举（未产出轮次登记 FAIL+原因移列，去 `**PASS**` 加粗）（P1-5）；⑥D 组 `.jobs \| length` 修正（P1-6）；⑦D 组/PKT-05 按 release.yml 实况改为 8 job 记录（4 build leg + 4 下游，全文 9 处同步）（P1-7）；⑧verify-homebrew-formula 吞错面补正向 log 证明（`Verified libra <ver>`）+ update-homebrew-tap artifact 绑定注记（P1-8）；⑨PKT-10 零命中守卫补 pull.rs（P1-9）；⑩兼容与文档收口具体化必需文件清单并对 PKT-09/PKT-11 面禁止 N/A（P1-10）；⑪INVARIANT 锚点 :86-88（P2-1）；⑫DEFER-02 重启条件补 PKT-14（P2-2） | @R23 → @R24 十四卡 | 全文、D 组、PKT-05、测试矩阵、review log、事实基线、非目标与延后项、兼容与文档收口 |
| 2026-09-03 | Codex R25 FAIL（P1-1..9）+ P2×1 | ①PKT-01 全文件 rg 守卫移至 PKT-02（本卡时点旧解析器 expect/panic! 尚存），PKT-01 VER=3/8（P1-1）；②PKT-11 Description 去除「stderr 原文移至 debug 日志」矛盾措辞（P1-2）；③PKT-11 stderr 上限扩展至全部 6 处捕获点并按三路径分子断言（P1-3）；④PKT-08 补 partial-header EOF 门（AC=10/8@EX-05=10 门，expected/判据规范/矩阵同步）（P1-4）；⑤PKT-09 写集/Files likely touched 纳入 push 文档与 error-codes、文档检查绑定 receive-pack 状态行语义（P1-5）；⑥PKT-10 AC 标题改标准字段名（P1-6）；⑦依赖边纯 ID 化——删幽灵边 `PKT-08 -> PKT-11`、PKT-13 Dependencies 去自引用与链内 ID 混排（P1-7）；⑧D 组/PKT-05 补 run headBranch=tag 断言（P1-8）；⑨D 组 log 取证先捕获后 grep-F 且校验上游状态（P1-9）；⑩自审/风险表「6 映射点」→ 8 适配点全量（P2-1） | @R24 → @R25 十四卡 | 全文、PKT-01/08/09/10/11、EX-05、实施顺序、D 组、测试矩阵、粒度审计表、风险表 |
| 2026-09-03 | Codex R26 FAIL（P1-1..6）+ P2×2 | ①PKT-03 零命中守卫补实际插值点 `{other}`（mod.rs:147-149，R26 Codex P1-1）；②PKT-05 证据表改「进入条件，执行期核对」诚实表述（P1-2）；③PKT-05 取证补 total=8 与 4 个下游 job 名断言（P1-3）；④PKT-06 审计表 axis 与卡面统一为「fetch discovery 边界归类」（P1-4）；⑤追溯表 PKT-08 改 10 fn（P1-5）；⑥PKT-09 `validate_receive_pack_response` 标注既有回归、不入 `(new)` 闭合集（P1-6）；⑦GAP-03 去重复计数、自审/DEFER-06 统一 8 映射点口径（P2-1）；⑧PKT-05 Out of scope 补 PKT-14（P2-2） | @R25 → @R26 十四卡 | 全文、PKT-03/05/06/08/09、GAP-03、DEFER-06、粒度审计表、追溯表 |
| 2026-09-03 | Codex R27 FAIL（P1-1..5） | ①全部 14 处 fn 存在性脚本改为先捕获 test list 并逐段检查状态（pipefail+显式 `\|\|` 闭合，空列表/管道失败均 fail-closed；经四路径实测验证）（P1-1）；②PKT-11 Dependencies 去除正文任务 ID 混排（P1-2）；③G-03/自审 PKT-08 口径改 10 门 EX-05（P1-3）；④PKT-11 文档门 13 逐文件断言 BatchMode+`ssh.strictHostKeyChecking`（含开发文档与 COMPATIBILITY.md），AC/Files likely touched 同步（P1-4）；⑤R4 Claude Result 修正为 PASS（仅 P2 具名接受）、R20 修订行补 P1-1..9 逐项映射、R1/R13/R16 未产出侧补登记（P1-5） | @R26 → @R27 十四卡 | 全文、全部存在性脚本、G-03、自审表、PKT-11、修订历史、review log |
| 2026-09-03 | Codex R28 FAIL（P1-1..4）+ P2×1 | ①review log 补 R23-R28 同轮 Claude 行（本闭环仅执行 Codex 评审，Claude 侧如实登记 FAIL+未产出原因，禁止虚构 PASS，P1-1）；②`len∈1..3` 稳定码绑定——PKT-02/07/08 具名 fn 内断言 marker/`LBR-NET-002`（AC 扩展，fn 集不变）（P1-2）；③PKT-12 三处透传门扩展至 `wait_with_output` 成功/收集失败双路径（P1-3）；④ADR-PKT-03 + PKT-11 增加脱敏前 host-key 内部分类信号（`HostKeyUnconfirmed`），clone 指引不再依赖 stderr 原文匹配并覆盖 SSH→clone 路径回归（P1-4）；⑤D 组循环与 PKT-05 取证补 `set -o pipefail` 与逐管道失败分支（P2-1） | @R27 → @R28 十四卡 | 全文、PKT-02/07/08/11/12、ADR-PKT-03、review log、D 组 |
| 2026-09-03 | Codex R29 FAIL（P1-1..4）+ P2×1 + Claude R29 未产出（该轮无判定） | ①ADR-PKT-03 host-key 信号具体化为跨层树内契约 `SshFailureKind::HostKeyUnconfirmed`（枚举/退出码 255+固定模式/经 `GitError` 结构化载荷传递/脱敏剥离位置/SSH→clone 测试三段断言）（P1-1）；②PKT-10 增 ⑦PacketRead 非 marker 普通 IO 误归类修正（门 3/9 与判据规范扩展，与成功定义/PKT-08 EOF 口径闭合）（P1-2）；③PKT-10 文档责任归位——写集/Files/Docs impact 纳入四命令 EN+zh + error-codes/COMPATIBILITY 核对，Verification 增文档检查（VER=4/8），收口表补 ls-remote/pull 行（P1-3）；④PKT-14 收口守卫改双基线 diff（`v0.22.10..HEAD` + commit→工作树 `v0.22.10`），未提交改动不再绕过（P1-4）；⑤PKT-11 ssh-keyscan 守卫改显式三分支模板（P2-1） | @R28 → @R29 十四卡 | 全文、ADR-PKT-03、PKT-10/11/14、粒度审计表、兼容与文档收口 |
| 2026-09-03 | Codex R30 FAIL（P1-1..7）+ P2×1 + Claude R30 未产出（该轮无判定） | ①ADR-PKT-03 host-key carrier 定型为唯一树内固定前缀常量 `SSH_HOST_KEY_UNCONFIRMED_SIGNAL`（构造/提取/剥离三点 + 三段测试契约，去「新增或扩展」二选一）（P1-1）；②PKT-12 纳入 SSH 内层读取包装点（`ssh_client.rs:234`/`:253`）透传——AC=10/8@EX-03=10 门，expected/判据规范/矩阵/追溯同步（P1-2）；③PKT-10 fetch/pull 独立 PacketRead 分支精确门 21/22（AC=22/8@EX-01=22 门，expected +2 fn、矩阵/追溯/EX-01 同步）（P1-3）；④PKT-11 ssh-keyscan 守卫回归 if/then/else 三分支（`rg; rc=$?` 形态在 bash -e 下失活，P1-4）；⑤PKT-14 双基线守卫改 `if bad=$(...)` 显式失败闭合（P1-5）；⑥D 组/PKT-05 Homebrew 正向证明改 artifact 路线（`formula-commit-sha` 下载 + tap formula 版本断言），废弃 run-log grep（summary 不入 log + 脚本源码假阳性，P1-6）；⑦正向证明改 bash `case` 模式匹配，消除 `grep -Fq` 管道 SIGPIPE 假失败（P1-7）；⑧PKT-13 Dependencies 正文去除重复任务 ID（P2-1） | @R29 → @R30 十四卡 | 全文、ADR-PKT-03、PKT-10/11/12/13/14、EX-01/EX-03、测试矩阵、追溯表、粒度审计表、D 组 |
| 2026-09-03 | Codex R31 FAIL（P1-1..3）+ P2×2 + Claude R31 未产出（该轮无判定） | ①PKT-10 增 fetch/pull PacketRead marker 正向门 23/24（AC=24/8@EX-01=24 门，expected +2 fn、矩阵/追溯同步）（P1-1）；②PKT-05 承载 `release.yml` `verify-homebrew-formula` 吞错面 fail-closed 收口（AC=11/12、VER=4/12，Files likely touched 增 workflow），D 组自 v0.22.12 起断言 `homebrew-verify-pass` sentinel artifact、v0.22.11 显式豁免并注明（P1-2）；③文档唯一责任卡裁决——PKT-06 收窄为零文档写入、PKT-10 写集补 `docs/error-codes.md`/`COMPATIBILITY.md` 并增 COMPATIBILITY 检查、PKT-13 澄清零文档写入（P1-3）；④D 组/PKT-05 run id 查询改 `.[0].databaseId // empty` 显式拒空（P2-1）；⑤总述 CLI 列表补 pull（P2-2）；⑥PKT-06 锚点 :942-960→:943-957 | @R30 → @R31 十四卡 | 全文、PKT-05/06/10/13、EX-01、测试矩阵、追溯表、粒度审计表、D 组、文档收口 |
| 2026-09-03 | Codex R32 FAIL（P1-1..7） | ①四处带尾随中文的围栏闭合行改为独立 ``` 行（R32 Codex P1-1）；②PKT-14 双基线守卫改单命令 awk（多段 grep 管道错误码掩蔽，P1-2）；③文档责任同卡闭环——PKT-04/06/07/08 各自承载 push.md/fetch.md/clone.md（EN+zh）同步与 grep 门（VER 相应 +1），PKT-13 汇总核对 grep 门（VER=6/8），适用范围 COMPATIBILITY 口径更新（P1-3）；④PKT-05 sentinel 契约统一为「失败非零且成功上传」（P1-4）；⑤PKT-10 标题 20→24 门、Full-suite 三个→四个 CLI（P1-5）；⑥PKT-05 sentinel VER 自包含化 + ADR/PKT-11 `ssh -T <host>`→具体示例（P1-6）；⑦修订历史 R29/R30/R31 行补 Claude 未产出登记（P1-7） | @R31 → @R32 十四卡 | 全文、PKT-04/05/06/07/08/10/13/14、适用范围、修订历史 |
| 2026-09-03 | Codex R33 FAIL（P1-1..5） | ①PKT-05 gpgsig 检查、PKT-09/PKT-10 error-codes 检查改 pipefail/单命令 awk（裸管道上游失败掩蔽，P1-1）；②PKT-13 双 cargo 命令 `&&` 串联（P1-2）；③ADR-PKT-03/PKT-11 host-key 提取层明确为内层 `GitError` 载荷（外层 `FetchError` Display 包装破坏串首锚定）+ 命中后不嵌入 detail + 层级契约子断言（P1-3）；④GC-05 补全——PKT-04/06/07/08/09/10 写集增 `docs/development/commands/*.md`，PKT-09 增 COMPATIBILITY.md push 行（P1-4）；⑤全部文档门改逐文件语义行 awk 断言（LBR-NET-002 与 discovery/pkt-line/截断 EOF/状态行同行共现，P1-5） | @R32 → @R33 十四卡 | 全文、PKT-04/05/06/07/08/09/10/13、ADR-PKT-03、文档收口 |
| 2026-09-03 | Codex R34 FAIL（P1-1..2）+ P2×1 | ①粒度审计表 PKT-04 AC 9/8→8/8（与卡面一致，P1-1）；②PKT-05 写集矛盾消解——`release.yml` 纳入 Implementation write set（G-10 管理）、Out of scope 去除「任何行为变更」矛盾、Granularity/审计表 landing=1/prod-files=1（P1-2）；③「8 个映射点」明确为逻辑映射点口径、PacketRead 子臂细化不改变计数（GAP-03/ADR-PKT-01/风险表注记，P2-1） | @R33 → @R34 十四卡 | 全文、PKT-04/05、GAP-03、ADR-PKT-01、风险表、粒度审计表 |
| 2026-09-03 | Codex R35 FAIL（P1-1..2） | ①D 组/PKT-05 run 绑定改为 tag commit SHA 解析（gh api git/ref/tags + annotated peel）+ `headSha==tag_commit` 选取，废弃 `--branch` 过滤（tag 推送不可靠，P1-1）；②G-10 共享文档面显式边——新增 `PKT-10 -> PKT-08`（clone/fetch.md）、`PKT-10 -> PKT-09`、`PKT-09 -> PKT-11`（COMPATIBILITY.md/error-codes.md）三条边，PKT-08/09/11 deps 与 Granularity/审计表同步，PKT-06/07/10 writeset 注明 fetch.md 序列化，G-10 规则与并发声明更新（P1-2） | @R34 → @R35 十四卡 | 全文、D 组、PKT-05/08/09/11、实施顺序、G-10、并发声明、粒度审计表 |
| 2026-09-03 | Codex R36 FAIL（P1-1..2）+ P2×1 | ①PKT-05 sentinel VER 改 SHA 绑定查询（去残留 `--branch`，与 D 组同口径）（P1-1）；②D 组/PKT-05 全部 gh 命令显式 `-R libra-tools/libra`、`gh api` 用全路径（本 checkout 无 `.git`，隐式仓库上下文不自包含）（P1-2）；③PKT-09 Docs impact 补 `COMPATIBILITY.md`/开发文档登记（P2-1） | @R35 → @R36 十四卡 | 全文、D 组、PKT-05/09 |
| 2026-09-03 | Codex R37 FAIL（P1-1..6） | ①D 组 run 选取同时绑定 `headSha`+`headBranch`、PKT-05 sentinel 块补 `gh run watch` 前置与 headBranch 绑定（P1-1）；②全部 `gh release create` 命令与示例补 `-R libra-tools/libra`（P1-2）；③EX-01 门 7-12/13-18 各拆为 6 个独立 fn（expected 14→24）、EX-04 门 8/9 拆独立 fn（expected 7→9）、EX-03 门 9 拆独立 fn（expected 9→10）——门与 fn 一一对应，矩阵/判据规范/门映射同步（P1-3）；④G-08 生命周期补 `locally-accepted → ER-05 review PASS → C 组` 门（REL-PKT-A、PKT-02/03/04 Lifecycle、PKT-05 AC）（P1-4）；⑤修订历史/review log 三处反引号内字面 `\|` 转义（P1-5）；⑥标题层级按模板 v2.2 恢复——`## 执行检查必备需求（强制）`、任务卡粒度规则以 `###` 移入「## 任务卡」之下（P1-6） | @R36 → @R37 十四卡 | 全文、D 组、PKT-03/05/10/12、EX-01/03/04、测试矩阵、追溯表、修订历史、章节结构 |
| 2026-09-03 | Codex R38 FAIL（P1-1..4）+ P2×1 | ①D 组/PKT-05/sentinel run 选取 `--json` 补 `headBranch` 字段（jq 读取未请求字段恒空，P1-1）；②sentinel 块 `$ver`→字面 `v0.22.12`（未定义变量，P1-2）；③sentinel 下载前补 `gh run watch --exit-status`（P1-3）；④PKT-10 文档检查围栏闭合行改独立 ``` 行（P1-4）；⑤D 组标题「正向 log 证明」→「artifact+tap 正向证明」（P2-1） | @R37 → @R38 十四卡 | 全文、D 组、PKT-05/10 |
| 2026-09-03 | Codex R39 FAIL（P1-1..2）+ P2×1 | ①PKT-05 主取证块 `$ver`→字面 `v0.22.12`（未定义变量残留，P1-1）；②D 组/PKT-05/sentinel 的 sha/typ 提取管道补显式 `||` 失败闭合与 peel 失败分支（P1-2）；③PKT-10 证据表 pull.rs 区间更正（Discovery=928-930+989-1001、FetchObjects=931-936、PacketRead=937-944 单列），聚合计更正为 `pull.rs:267,928-944,989-1001`（P2-1） | @R38 → @R39 十四卡 | 全文、D 组、PKT-05/10、适用范围、GAP-03、ADR-PKT-01、追溯表 |
| 2026-09-03 | Codex R40 **PASS**（P0×0、P1×0、P2×0） | 无需修订——全部维度（模板结构、机械一致性、Verification 可执行性、源码锚点 13 文件、安全口径、EX-01..05 门族、发布面）核查通过 | @R39 → @R40（评审通过，内容未变） | — |
| 2026-09-15 | Claude Code R41 FAIL（B1/B2、L1–L5，P2×6）；Codex 源码复核发现 GitError Display 包装问题 | R42：helper/error 可见性与 Display 消费 marker；测试无 panic；空响应四命令验收移至 PKT-10；读取循环与真实传输验收分层，PKT-12/13 增端到端门；补共享类型与 error-codes 写集；修正 PKT-05 默认；host-key 匹配原始载荷 | @R41 → @R42 十四卡，复审待定 | 当前执行约定、ADR、PKT-01/02/03/05/08/09/10/11/12/13、审计/矩阵/追溯；原始日志 `/mnt/gitmono/tmp/libra-pkt-execution.mc0nXP/r41-preflight-review.log` |
| 2026-09-15 | Claude Code R42 FAIL（后继卡 F1 P1；PKT-01 READY=YES） | R43：PKT-07 长度/截断错误保留 marker 与稳定码双断言；首卡 sentinel 豁免绑定实际切片；PKT-05 安全/性能一致；共享写集与 raw payload 契约补充；两种 E2E 注入机制明确；Codex 根据 R42 建议具名批准 EX-01/03/06 | @R42 → @R43 十四卡，PKT-01 实现与 F1 修订复核正在运行；不记全计划 PASS | PKT-07、执行补充、waiver、PKT-12/13；`/mnt/gitmono/tmp/libra-pkt-execution.mc0nXP/r42-preflight-review.txt` |
| 2026-09-15 | 全量首试暴露 push checkpoint fixture 未就绪，独立源码诊断支持有界 drain + 严格 marker 检查 | R44：ER-10 新增独立 FIX-PKT-01，原十四卡保留；PKT-01 lifecycle blocked；修复先占下一 patch，原未发布补丁归档；前 PKT-05 两切片的 D 例外绑定实际身份；不改生产防线 | 新增 FIX-PKT-01 -> PKT-01；原卡不换号，恢复卡追加于 Phase 1 | 执行约定、依赖、卡面、粒度审计、追溯、测试矩阵、里程碑、风险及完成门；`agent-push-race-review.txt`（仅诊断，非实现验收） |

| 2026-09-15 | R45 FIX-PKT-01 实现与审查PASS | 四项fixture回归、serial生成及209→213数量pin；red/green与守卫失败闭环，限定Clippy/独立审查通过；bump0.22.29未发布 | FIX-PKT-01实现验收，原十四卡保留 | 本卡执行记录与R45双review |
| 2026-09-15 | R46 post-bump全量失败（私有脚本umask077），诊断后独立复核PASS | 不改产品/断言；私有umask局部化、目录0700入口检查、独立失败归档与R2证据；同一二进制077 red/0022 green；必须完整重跑 | 执行脚本修正，不新增产品任务卡或bump | 本卡R46记录、失败JUnit及umask-diagnosis审查 |

| 2026-09-15 | R47–R51编译故障诊断，CI工具版本对齐 | 四次full未开始测试；单target与完整cargo诊断成功，资源记录无已确认OOM；私有CI0.9.143已校验，不改全局安装 | 无产品范围或验收集合变化 | FIX-PKT-01 R47–R51独立失败日志与诊断记录 |
| 2026-09-15 | R52最终默认全量PASS，Codex证据核对PASS | 10050/10050、零失败/重试，前后来源一致；只补终态事实，准备签名提交及实际0.22.29发布 | 原15卡与C/D义务不变 | FIX-PKT-01 R52、R6 JUnit/time/source快照 |

| 2026-09-15 | R53 FIX C实际完成，PKT-01恢复重验 | 签名6dd2364d1与main/tag/gh绑定；FIX的D进行中；恢复精确helper并bump实际0.22.30 | 原批准C/D异步规则，未放宽终态 | 两卡R53记录、真实Release和workflow links |

| 2026-09-15 | R54 PKT-01最终默认全量PASS，FIX远端部分实证 | 10055/10055、零失败/重试、来源前后一致；同hash实现审查保持有效，准备实际.30的C；FIX三个平台/CodeQL与两Linux下载已验证 | 原15卡/C/D规则不变 | PKT-01 R54与三JUnit；FIX D部分记录 |

**R43 执行补充（2026-09-15，Codex 登记）：** R42 已确认 PKT-01 开工就绪，卡本身仍须独立实现审查。F1 已在 PKT-07 既有 AC/具名测试中补 marker carrier 与稳定码双断言；F3 已统一 PKT-05 安全/性能正文。F2：`homebrew-verify-pass` 的唯一首卡豁免按本计划 PKT-01 的实际 release tag/SHA 记录绑定（即 PKT-05 workflow 收口前的切片），绝不能按旧字面 tag v0.22.11 判定；PKT-05 及后继全部要求实际 sentinel。下方旧版本循环仍为历史示例、不得原样执行；执行脚本必须从真实卡片记录取得身份，拒绝未登记 tag。F5：共享源文件写入序列为 PKT-01→02→08→13，除原 G-10 文件链外同样串行。F7/F8：后续每卡开工必刷新语义文档门与源码行号，不以既有泛化文档命中冒充阶段行为说明。F9：PKT-06 AC1、PKT-10 门1–6 的 marker 判断必须检查模式匹配的原始 NetworkError(detail)/内层 IO 错误，禁止用预格式化 message 或 GitError Display；既有正反向具名测试须构造带真实外层包装的 carrier，防止串首误判。上述要求不删减测试、不改变十四卡依赖终态。

## 已决议设计决策

### ADR-PKT-01: 类型化 `PktLineError` + 外部类型边界 marker 适配 + 显式映射 `LBR-NET-002`

- **Status:** Accepted
- **Context:** 稳定码映射两层：① CLI 边界显式映射（`fetch.rs:943-957`、`fetch.rs:882-887`、`push.rs:878-900`、`push.rs:374-376/:404-406`、`ls_remote.rs:210-221`、`clone.rs:1012-1046,1059-1063`、`pull.rs:267,928-936,937-944,989-1001`）优先于 ② 字符串分类器（`error.rs:1680-1701`）。pkt-line 错误目前被包装为 `GitError::NetworkError` / `PushError::Network` / `IoError`，在上述边界落 `LBR-NET-001`。`GitError` 为外部 crate 类型不可加变体；`PushError` 为树内枚举可加变体。
- **Decision:** 三层机制：① **树内类型**：`PktLineError`（固定短语 + 固定原因枚举，零回显）承载解析与读取循环错误（含异步头部解析）；push 侧新增 `PushError::Protocol { detail }`（`detail` 以 marker 前缀开头、由 `PktLineError` 转换而来）仅承载协议错误。② **外部类型边界 marker 适配**：判定必须读取模式匹配得到的 `GitError::NetworkError(detail)` 原始载荷或 `IOError` 的内层错误，不能对 `GitError::to_string()` 使用串首匹配；锁定的 git-internal 0.8.6 Display 会添加 `Network Error: ` / `IO Error: `。树内常量 `PKT_LINE_PROTOCOL_ERROR_PREFIX = "pkt-line protocol error: "`（GC-02 单点）随错误文本进入 `GitError::NetworkError`/`IoError`；在 **8 个 CLI 映射点**（fetch discovery、fetch FetchObjects、push discovery、ls-remote、clone discovery、clone fetch 阶段、pull discovery、pull FetchObjects）的显式分支内识别前缀 → `NetworkProtocol`（`LBR-NET-002`），其余维持原归类——每个适配点以具名测试做 marker/非 marker 双向锚定（8 为逻辑映射点口径；`PacketRead` 子臂的归类细化由 PKT-10 门 3/4/21-24 承载，R34 Codex P2-1）。**检测语义为 `starts_with`（串首锚定，R6 Claude P1-1）**：`contains` 会被远端可控字段（如 HTTPS `Content-Type` 头值）伪造子串命中而误判 `LBR-NET-002`，故拒绝；为使 `starts_with` 在传输包装后仍成立，SSH 三处包装点（`ssh_client.rs:273-284/:319-329/:449-459`）对 marker 错误**原样透传**（不套 `"SSH read failed: "` 等前置字面量，经共享辅助函数实现），非 marker 错误按现状包装——保证 marker 在最终用户可见文本中仍位于串首。③ **分类器兜底**：错误文本含 `pkt-line` 使 `is_network_protocol_error` 对未经适配路径成立。收到畸形帧一律中止当前操作。
- **Alternatives considered:** ① 仅靠分类器——拒绝（R2 证伪）；② 为 `GitError` 新增变体——拒绝（外部 crate）；③ 全面 `RemoteDiscoveryError` 迁移——DEFER-04；④ 静默跳过坏帧——拒绝（GC-07）；⑤ 错误文本附远端字节摘要——拒绝（反射面）；⑥ `contains` 检测语义——拒绝（远端可控字段可伪造子串命中，R6 Claude P1-1 证伪），改用 `starts_with` + SSH 透传保持串首锚定。
- **Consequences:** 协议错误归类由 `LBR-NET-001` 修正为 `LBR-NET-002`（既有码语义收敛）；marker 前缀成为跨层契约（常量单点 + 三层测试锚定）；对宽松远端兼容性下降。
- **Revisit when:** DEFER-04 落地或 `git_internal` 提供协议错误变体时移除 marker 层。

### ADR-PKT-02: 远端 `ng` 拒绝原因透传不属于「零回显」口径

- **Status:** Accepted
- **Context:** `push.rs:2140-2147` 把 receive-pack `ng` 状态行的远端 `reason`（如 "protected branch hook declined"）存入 `PushError::RemoteRefUpdateFailed` 并透传，测试 `:3404-3416` 锁定。
- **Decision:** 「零远端字节回显」限定于**协议解析与传输错误路径**（pkt-line 帧、advertisement、process-output 诊断）；远端对自身 ref 更新的拒绝原因是远端的权威用户消息，按 Git 上游惯例**透传但经安全化处理**：①`ng` 行的 `refname` 先校验属于本地 `expected_refs`——未知 refname 不透传，改归 `PushError::Protocol` 固定短语（R10 Codex P1-1）；②`reason` 与（校验通过的）`refname` 共用控制字符转义（`C0`/`DEL`/CSI）+ 200 字符限长（超出截断加省略号），防终端转义注入与日志洪水（R9 Codex P1-5）；③恶意 sentinel 覆盖人读与 `--json` 输出（refname 与 reason 两条路径）。
- **Alternatives considered:** ① 固定短语替换——拒绝：破坏 Git 兼容 UX；② allowlist——拒绝：开放集维护成本高。
- **Consequences:** `ng` reason 经控制字符转义（C0/DEL/CSI）与 200 字符限长后保留可读性（与 Git 上游一致）；残余风险为远端可注入**可读**误导文本（不可注入控制序列），由本 ADR 显式接受；`unpack ok`/状态行解析本身不回显（`unexpected receive-pack status line` 由 PKT-09 改归类 `Protocol` 并固定短语）。
- **Revisit when:** 上游 Git 改变 `ng` reason 惯例，或 Libra 引入结构化错误展示（DEFER-05）。

### ADR-PKT-03: SSH 捕获路径强制 BatchMode，host-key 交互 fail-closed

- **Status:** Accepted
- **Context:** `ssh_client.rs` 的 `interactive` 判定依据是 **libra 自身进程 stdin 是否为 TTY**（`:171` `is_terminal()`；`:180-182` 条件分支），非交互路径现状已强制 BatchMode（`CLAUDE.md` 既有设计）；`Stdio::inherit()` 分支（`:198-203`）命中的是**真实终端用户**（如终端里 `libra clone ssh://…`），远端 stderr 可直写用户终端（R9 Codex P1-4）。改为捕获后，OpenSSH 的 host-key 确认提示无法经事后渲染完成交互（R10 Codex P1-4 / Claude P1-2）。承载该逻辑的 `ssh_client.rs:164-204` 已纳入适用范围与 PKT-11 写集（R11 Claude P1-1）。
- **Decision:** 所有捕获路径的 SSH 子进程强制追加 `-o BatchMode=yes`：host key 未确认时 SSH 以 fail-closed 失败，Libra 返回稳定指引错误（含带外指纹核对说明、一次性 `ssh -T git@github.com` 交互 TOFU 步骤（以 GitHub 为例，实际仓库主机据实替换域名）与 `ssh.strictHostKeyChecking` 配置说明；**明确排除 `ssh-keyscan` 管道写入 known_hosts**，R16 Claude P1-2），不渲染任何远端提示原文；TOFU 工作流由用户手动完成一次后恢复自动化。与 `d57a908` 的 `ssh.strictHostKeyChecking`（默认 `ask`）并存：Libra 发起的捕获路径覆盖为 `BatchMode=yes`，交互确认由用户手动 `ssh` 完成。**host-key 失败的判定在脱敏前完成，唯一树内 carrier 为固定前缀常量（R28/R29/R30 Codex P1-4/P1-1）**：树内新增 `pub(crate) const SSH_HOST_KEY_UNCONFIRMED_SIGNAL: &str = "ssh host-key unconfirmed: ";`（GC-02 单点，与 `PKT_LINE_PROTOCOL_ERROR_PREFIX` 同型契约）；`ssh_client.rs` 捕获侧在脱敏前依据**退出码 255 + stderr 固定模式**（OpenSSH 固定文案前缀 `Host key verification failed.`、`REMOTE HOST IDENTIFICATION HAS CHANGED`）构造携带该前缀的 `GitError::NetworkError` 字符串载荷（外部 crate 类型边界的既有通道，与 ADR-PKT-01 marker 适配同构）；提取点 `clone.rs:1012-1029` 消费**内层 `GitError` 载荷**（即 `对 FetchError::Discovery.source 模式匹配 GitError::NetworkError(detail) 后读取原始 detail（不是 source.to_string()）`——外层 `FetchError` Display 会前置 `failed to discover references from '{remote}': ` 包装破坏串首锚定，故判定必须作用于内层 source，R33 Codex P1-3）以 `starts_with(SSH_HOST_KEY_UNCONFIRMED_SIGNAL)` 识别；命中后**直接生成固定错误与指引短语，不嵌入内层 detail**；剥离点 `describe_status_with_stderr` 渲染前剥离全部 stderr 原文与信号前缀，用户可见文本不含任何远端字节。PKT-11 落地脱敏后，`clone.rs` 既有的 `"host key verification failed"` 原文匹配不可达，改为消费该信号。三段测试契约绑定 `…_host_key_fail_closed_guidance`：①信号产出（255+模式→前缀载荷）、②脱敏剥离（原文与前缀均不出现在用户可见文本）、③clone 指引（**内层 `GitError::NetworkError(detail)` 原始 detail** starts_with 消费→固定短语，且固定错误不嵌入内层 detail——含 SSH→`FetchError::Discovery` 包装→clone 臂的层级契约子断言，R33 Codex P1-3）。
- **Alternatives considered:** ①白名单匹配 host-key 提示并保真渲染——拒绝：OpenSSH 提示文案随版本/locale 变化，匹配脆弱；指纹渲染引入截断/转义风险（Claude R10 P1-2）；②受限终端中介（pty 代理 + 逐行过滤）——拒绝：复杂度高，属独立设计（登记 DEFER-07）。
- **Consequences:** **附加影响（R17 Claude P1-3）**：`BatchMode=yes` 同时禁用私钥 passphrase 交互——口令保护私钥且无 ssh-agent 的用户在已确认主机上也会认证失败；错误提示须明确指向 ssh-agent（PKT-11 AC）。
- **Consequences:** **真实终端用户**首次连接未知主机时，从「像 `git clone` 一样看到 OpenSSH 指纹确认提示」变为「fail-closed + 手动 TOFU 指引」——这是一次有意的交互 UX 回退，换取远端 stderr 不再直通终端（R11 Claude P1-1 更正：受影响的是交互用户而非自动化调用，后者现状已 fail-closed）；`known_hosts` 已确认的主机不受影响；行为变化同步至用户文档（PKT-11）。
- **Revisit when:** DEFER-07（受限终端中介）立项或上游提供结构化 host-key 确认接口。

## 全局工程约束

以下约束对本文所有任务生效（定义全文见 [`plan-template.md`](plan-template.md)）：GC-01 现状核实前置；GC-02 单一事实源；GC-03 Git 互操作与 Libra 扩展边界；GC-04 输出与错误契约；GC-05 文档同步；GC-06 测试索引；GC-07 安全默认值（fail-closed）；GC-08 原子性与恢复；GC-09 并发与资源生命周期；GC-10 性能预算；GC-11 生产 panic 禁止；GC-12 精确暂存（只 `libra add <相关路径>`）。

## 执行检查必备需求（强制）

按 [`plan-template.md`](plan-template.md) **ER-01..ER-13 全文生效**，不削弱任何一项。提交用 `libra add <本卡相关路径>` + `libra commit -s -m`，提交后按 ER-07 校验 `gpgsig`；签名预检先读 `commit.gpgSign`（未设置回退 `vault.signing`）。


## 字段全局默认与例外

- **Release boundary 默认:** `independent`；家族子卡/发布点见 REL-PKT-A
- **Task type 默认:** `implementation`（PKT-05 为 `release`）
- **Rollback mode 默认:** `immutable-release`
- **Migration and rollback 默认:** `N/A`
- **Security and privacy 默认:** 继承 GC-07、GC-11（fail-closed；零回显口径见 ADR-PKT-01/02）
- **Performance budget 默认:** 继承 GC-10
- **Docs and compatibility impact 默认:** 按 GC-05；除 PKT-13 统一核对登记外均为「核对、预期零变更」

**默认覆盖**：

| 任务 | 偏离的字段 | 取值与理由 |
|---|---|---|
| PKT-02/03/04 | `Release boundary` / `Version increment` / `Release write set` / `C/D coverage from` | `family child of REL-PKT-A` / `N/A` / `N/A` / `PKT-05`：G-08 家族子卡，编译不可分割（R7 Codex P2-1） |
| PKT-05 | `Task type` / `Release boundary` / `Implementation write set` / `Security and privacy` / `Performance budget` | `release` / `family release point of REL-PKT-A` / `.github/workflows/release.yml`（G-10 管理） / Homebrew/tap 验证失败必须使发布变红、成功须实际上传 sentinel / 验证有界：无新增产品行为，但明确修改 CI fail-closed 行为 |

**规则 waiver（`EX-*`）**：本计划登记 **5 项有效**门族型 waiver（EX-01/PKT-10、EX-02/PKT-11、EX-03/PKT-12、EX-05/PKT-08、EX-06/PKT-13；R43 Codex 根据 R42 建议具名批准扩展，非全计划 PASS）；EX-04 历史保留但不再适用。

| 例外 ID | 任务（或 `ALL/<作用域>`） | 豁免项 | 理由与补偿措施 | Approver | Review round | 证据 | 有效期 |
|---|---|---|---|---|---|---|---|
| EX-05 | PKT-08 | G-03 条目上限（门族型验收） | git/ssh 下界 + payload/partial-header 截断 EOF 的机械门族（2 下界 + 6 合法帧回归 + typed payload-EOF 分类 + partial-header EOF 分类 = 10 门），拆卡违反 G-01/G-02；补偿 = 判据规范逐条枚举 + 门→命令映射 | Codex R18 P1-5 提出 typed EOF；R25 Codex P1-4 提出 partial-header EOF；Claude R19 复核确认计数 | R18（R19/R25 扩展） | 本卡 AC 清单 | 本计划内 |
| EX-04 | PKT-03 | 历史 G-03 门族登记（R42 不再需要豁免） | 四命令空响应端到端门完整移至 PKT-10，PKT-03 保留 8 门，未删除最终覆盖 | 原 R17 记录保留；R42 待复审 | R17 / R42 | PKT-03 八门与 PKT-10 门25 | 历史保留，不再适用 |
| EX-01 | PKT-10 | G-03 条目上限（门族型验收） | 精确分解（R13 扩展 pull、R29/R30/R31 扩展 PacketRead 后）：6 个 marker 映射门 + 6 个非-marker 回归门 + 6 个传输×归类矩阵门 + 1 零回显 sentinel 门 + 1 常量单点门 + 4 个 PacketRead 门（fetch/pull 非 marker IO 修正 + marker 正向）+ 1 四命令空响应端到端门 = 25 门，逐门计数超上限，拆卡违反 G-01/G-02（同一边界归类轴的实现/测试/文档不得拆散）；补偿 = 判据规范块逐条枚举 + 每门为可复制具名命令 | Codex（2026-09-15 R43 具名批准；依据 Claude Code R42 建议，已核对同轴/门数与补偿；EX-06 注入机制如下卡面） | R43（保留原轮次历史） | 本卡「判据规范（非计数正文）」块 | 本计划内 |
| EX-03 | PKT-12 | G-03 条目上限（门族型验收） | 精确分解（R42 扩展为 11，R43 已具名批准）：3 个外层透传门（discovery/fetch_objects/send_pack，双路径子断言）+ 1 内层透传门（`ssh_client.rs:234`/`:253`）+ 1 非-marker 回归门 + 1 零回显 sentinel 门 + 1 marker 串首门 + 1 共享辅助门 + 1 kill/reap 门 + 1 挂起回归门 + 1 SSH 帧错误真实端到端稳定码门 = 11 门，逐门计数超上限，拆卡违反 G-01/G-02（SSH pkt-line 错误面为同一恢复轴）；补偿 = 判据规范块逐条枚举 + 门→具名命令映射（同 EX-01 口径） | Codex（2026-09-15 R43 具名批准；依据 Claude Code R42 建议，已核对同轴/门数与补偿；EX-06 注入机制如下卡面） | R43（保留原轮次历史） | 本卡 AC 清单 | 本计划内 |
| EX-02 | PKT-11 | G-03 条目上限（门族型验收） | 精确分解（R23 Codex P1-6 复核为 13）：2 个格式化函数门 + 3 处状态/非零退出点门 + 1 恶意 stderr sentinel 门 + 1 debug 摘要门 + 1 stderr 洪水上限门 + 3 交互路径门（BatchMode/host-key 指引/交互捕获 sentinel）+ 1 passphrase 指引门 + 1 文档同步门 = 13 门，逐门计数超上限，拆卡违反 G-01/G-02（SSH 错误面卫生为同一恢复轴）；补偿 = 本卡「判据规范（非计数正文，EX-02 门族枚举）」块逐条枚举 + 门→具名命令映射（同 EX-01 口径） | Codex R9/R11 提出缺口；Claude R9/R11 复核确认；R23 Codex 复核计数 | R9（R11/R12/R23 扩展） | 本卡判据规范块 | 本计划内 |
| EX-06 | PKT-13 | G-03 条目上限（门族型验收） | 原8项 AC + 1 git:// 帧长/截断错误真实端到端门 = 9项；同一协议错误映射轴，判据与具名 fn 一一登记，不减少原9个 fn | Codex（2026-09-15 R43 具名批准；依据 Claude Code R42 建议，已核对同轴/门数与补偿；EX-06 注入机制如下卡面） | R43（保留原轮次历史） | PKT-13 AC/expected/矩阵 | 本计划内 |

#### 任务卡粒度审计表

| 任务 | type | axis | recovery | complete | self-contained | AC | VER | landing / prod-files | scope | deps | writeset | release | split-from | exception |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| FIX-PKT-01 | implementation | checkpoint fixture 的后台索引就绪 | 独立前滚补丁 | yes | yes | 6/8 | 3/8 | 1/0 | S | none | I 无交叉、R 串行 | independent | ER-10 执行期新增 | N/A |
| PKT-01 | implementation | 帧长校验与 marker 常量单一事实源 | 发布修复版本（immutable-release），降级=回装上一 tag | yes | yes | 7/8 | 3/8 | 1/1 | S | FIX-PKT-01 | no-overlap | independent | N/A（主轴首卡） | N/A |
| PKT-02 | implementation | 同步解析器 fail-closed | 随家族发布点前滚修复，降级=回装上一 tag | yes | yes | 8/8 | 5/8 | 1/1 | M | PKT-01 | no-overlap（家族内与 03/04 不相交） | family child of REL-PKT-A | PKT-01@R1 | N/A |
| PKT-03 | implementation | discovery 传播、marker 契约与 capability 收口（门族） | 随家族发布点前滚修复，降级=回装上一 tag | yes | yes | 8/8 | 3/8 | 1/1 | S | PKT-02 | no-overlap（家族内与 02/04 不相交） | family child of REL-PKT-A | PKT-02@R2 | N/A |
| PKT-04 | implementation | push 调用方传播与 Protocol 变体 | 随家族发布点前滚修复，降级=回装上一 tag | yes | yes | 8/8 | 3/8 | 1/1 | M | PKT-02 | no-overlap（家族内与 02/03 不相交） | family child of REL-PKT-A | PKT-02@R2 | N/A |
| PKT-05 | release | 家族 A 聚合发布 | 前滚修复发新版本，降级=回装上一 tag | yes | yes | 11/12 | 4/12 | 1/1 | M | PKT-02,PKT-03,PKT-04 | 序列化于 REL-PKT-A 窗口 | family release point of REL-PKT-A | N/A（G-08 新建发布点） | N/A |
| PKT-06 | implementation | fetch discovery 边界归类 | 发布修复版本（immutable-release），降级=回装上一 tag | yes | yes | 5/8 | 4/8 | 1/1 | M | PKT-05 | 序列化于 PKT-07（同文件 fetch.rs/fetch.md）；与 PKT-09 不相交 | independent | PKT-03@R2 | N/A |
| PKT-07 | implementation | fetch 异步下界与 EOF 语义（单点 helper 消费） | 发布修复版本（immutable-release），降级=回装上一 tag | yes | yes | 6/8 | 4/8 | 1/1 | M | PKT-06 | 序列化于 PKT-06（同文件 fetch.rs/fetch.md） | independent | PKT-03@R2 | N/A |
| PKT-08 | implementation | git/ssh 读取下界与 payload 截断 EOF（门族） | 发布修复版本（immutable-release），降级=回装上一 tag | yes | yes | 10/8@EX-05 | 4/8 | 3/3 | M | PKT-05,PKT-10 | clone/fetch.md 文档面序列化于 PKT-10；共享 git_protocol.rs 经 PKT-01/02 串行；代码面与 PKT-06/07/09 不相交 | independent | PKT-03@R1（异步循环轴） | EX-05 |
| PKT-09 | implementation | push 状态行归类与回显修复 | 发布修复版本（immutable-release），降级=回装上一 tag | yes | yes | 6/8 | 6/8 | 1/1 | M | PKT-05,PKT-10 | COMPATIBILITY.md/error-codes.md 文档面序列化于 PKT-10；push.md 序列化于 PKT-04（经家族 PKT-05）；其余 no-overlap | independent | PKT-04@R5 | N/A |
| PKT-10 | implementation | clone/ls-remote/pull/fetch-objects 边界归类（门族） | 发布修复版本（immutable-release），降级=回装上一 tag | yes | yes | 25/8@EX-01 | 4/8 | 4/4 | M | PKT-07 | 序列化于 PKT-07（同文件 fetch.rs/fetch.md） | independent | PKT-06@R4 | EX-01 |
| PKT-11 | implementation | SSH process-output 全路径脱敏（含交互路径与文档同步，BatchMode 决议） | 发布修复版本（immutable-release），降级=回装上一 tag | yes | yes | 13/8@EX-02 | 3/8 | 2/2 | M | PKT-12,PKT-09 | 序列化于 PKT-12（同文件）与 PKT-09（COMPATIBILITY.md） | independent | PKT-08@R4 | EX-02 |
| PKT-12 | implementation | SSH pkt-line 三点脱敏（门族） | 发布修复版本（immutable-release），降级=回装上一 tag | yes | yes | 11/8@EX-03 | 2/8 | 1/1 | M | PKT-08 | 序列化于 PKT-08（同文件） | independent | PKT-08@R5 | EX-03 |
| PKT-13 | implementation | 异步头部解析 marker 化 | 发布修复版本（immutable-release），降级=回装上一 tag | yes | yes | 9/8@EX-06 | 6/8 | 4/4 | M | PKT-07,PKT-08,PKT-10,PKT-12,PKT-11 | 共享 git_protocol.rs 经 PKT-01/02/08 串行；序列化于 PKT-07/10（fetch.rs）与 PKT-08/12/11（git/ssh_client.rs） | independent | PKT-07@R5 | EX-06 |
| PKT-14 | implementation | push ng 输入校验与渲染卫生 | 发布修复版本（immutable-release），降级=回装上一 tag | yes | yes | 5/8 | 3/8 | 1/1 | M | PKT-09 | 序列化于 PKT-09（同文件） | independent | PKT-09@R11 | N/A |

#### Verification 判定口径

零命中守卫一律用模板三分支模板（区分 `rg` 退出码 0/1/>1）。**`compat_*_production_unwrap_guard` 只扫描裸 `.unwrap()`**——expect/panic 证据靠 rg 零命中；`compat_extra` 的 `TARGET_FILES` 为固定 9 文件清单（仅 `git_protocol.rs` 与本计划相关），**只登记于写集命中该清单的卡（PKT-01/02 所在文件）**，其余卡不列（R5 Claude P2-2）。全部新增 fn 标 `(new)` 并经以下**失败闭合**脚本防零匹配空跑（R5 Codex P1-5）：

```bash
# ① 具名 fn 存在性（失败闭合：缺任一即 FAIL）。下例为 PKT-01 的完整可执行实例（expected 已内联定义；其余各卡在各自 Verification 内联同样的脚本与各自的 expected 全集）：
expected="pkt_frame_payload_len_ok_flush_zero pkt_frame_payload_len_rejects_len_below_four pkt_frame_payload_len_ok_len_4_empty pkt_frame_payload_len_ok_normal_and_upper_bound pkt_frame_payload_len_rejects_over_0xffff"
missing=$(bash -c 'set -o pipefail; list=$(source .env.test && cargo test --lib -- --list 2>/dev/null | sed "s/: test\$//" | awk -F"::" "{print \$NF}" | sort) || { echo "TEST LIST PIPELINE FAILED" >&2; exit 1; }; comm -23 <(printf "%s\n" $1 | sort) <(printf "%s\n" "$list")' _ "$expected") || { echo "FN EXISTENCE CHECK FAILED (pipeline error)"; exit 1; }
[ -z "$missing" ] || { echo "MISSING FNS: $missing"; exit 1; }
# ② 执行（存在性通过后，零匹配不可能再发生）
source .env.test && cargo test --lib pkt_frame_
```

## 执行检查必备需求补充：D 组远端后置门（逐发布切片登记）

每个发布切片完成 C 组第 ⑧⑨ 步后，取得**全部 8 个 job 记录**的绿色证据（分发通道 job 失败不得放行 `complete`；R24 Codex P1-7 按 release.yml 实况更正：`build-and-upload` 为 4 路 matrix leg，加 4 个下游 job = 8 条 job 记录）：

| job（8 条记录） | 判据 |
|---|---|
| `build-and-upload` × 4 leg（linux-amd64 / linux-arm64 / darwin-arm64 / windows-amd64） | 各 leg `conclusion=success`（固定查询：workflow=`release.yml`、event=`push`、ref=本切片 tag） |
| `upload-install-scripts` | install.sh / install.ps1 随 tag 更新成功（job 内含 stale 版本拒绝与 CDN 版本校验步，失败即红） |
| `update-homebrew-tap` | formula 更新成功——job 内失败路径虽 `exit 0`，但 `formula-commit-sha` artifact 以 `if-no-files-found: error` 上传（`if: always()`）：更新未完成时该步必红，故绿色 ⟺ formula 已更新/已一致 |
| `verify-homebrew-formula` | `brew install` 版本校验通过——job 结论被 `set +e` 吞错且成功标记写 `$GITHUB_STEP_SUMMARY`（不入 run log），故正向取证走 **artifact + tap 内容**（R30 Codex P1-6/P1-7）：run 的 `formula-commit-sha` artifact 必须存在且可用（`update-homebrew-tap` 成功路径绑定），tap 在该 commit 的 `Formula/libra.rb` 含本切片版本号；且自 v0.22.12 起（PKT-05 收口 `verify-homebrew-formula` 吞错面，R31 Codex P1-2）断言仅成功路径产生的 `homebrew-verify-pass` sentinel artifact 存在 |
| `request-stable-manifest` | stable manifest 签名请求成功 |

（版本→tag：PKT-01 `v0.22.11`、PKT-05 `v0.22.12`、PKT-06 `v0.22.13`、PKT-07 `v0.22.14`、PKT-10 `v0.22.15`、PKT-09 `v0.22.16`、PKT-08 `v0.22.17`、PKT-12 `v0.22.18`、PKT-11 `v0.22.19`、PKT-13 `v0.22.20`、PKT-14 `v0.22.21`。家族子卡 PKT-02/03/04 不 bump、不推送。）

**D 组逐 job 收口循环（全部切片统一取证命令，R23 Codex P1-12/P1-13 命令化；R24 Codex P1-6/P1-7/P1-8 按 release.yml 实况修正）：workflow=`release.yml`、event=`push`、ref=tag 分支显式锁定，逐切片断言 run conclusion=success、8/8 job 全绿、4 个 build leg 齐全、4 个下游 job 齐全，并对 verify-homebrew-formula 做 artifact+tap 正向证明（R38 Codex P2-1 文案同步）：**

```bash
set -o pipefail
downstream_jobs='upload-install-scripts update-homebrew-tap verify-homebrew-formula request-stable-manifest'
for ver in v0.22.11 v0.22.12 v0.22.13 v0.22.14 v0.22.15 v0.22.16 v0.22.17 v0.22.18 v0.22.19 v0.22.20 v0.22.21; do
  obj=$(gh api "repos/libra-tools/libra/git/ref/tags/$ver") || { echo "TAG REF MISSING for $ver"; exit 1; }
  sha=$(printf '%s' "$obj" | jq -r '.object.sha') || { echo "TAG SHA PARSE FAILED for $ver"; exit 1; }
  typ=$(printf '%s' "$obj" | jq -r '.object.type') || { echo "TAG TYPE PARSE FAILED for $ver"; exit 1; }
  if [ "$typ" = "tag" ]; then sha=$(gh api "repos/libra-tools/libra/git/tags/$sha" --jq '.object.sha') || { echo "TAG PEEL FAILED for $ver"; exit 1; }; fi
  case "$sha" in ????????????????????????????????????????) ;; *) echo "BAD TAG COMMIT SHA for $ver: $sha"; exit 1;; esac
  run_id=$(gh run list -R libra-tools/libra --workflow=release.yml --event push --json databaseId,headSha,headBranch --limit 50 --jq "[.[] | select(.headSha==\"$sha\" and .headBranch==\"$ver\")][0].databaseId // empty")
  [ -n "$run_id" ] || { echo "NO RUN for $ver (tag commit $sha)"; exit 1; }
  rb=$(gh run view -R libra-tools/libra "$run_id" --json headBranch,headSha --jq '.headBranch + "@" + .headSha')
  case "$rb" in "$ver@$sha") ;; *) echo "REF MISMATCH for $ver: run bound to $rb (want $ver@$sha)"; exit 1;; esac
  gh run watch -R libra-tools/libra "$run_id" --exit-status || { echo "RUN $ver conclusion != success"; exit 1; }
  names=$(gh run view -R libra-tools/libra "$run_id" --json jobs --jq '.jobs[].name' | tr '\n' ' ') || { echo "JOB NAME FETCH FAILED for $ver"; exit 1; }
  ok=$(gh run view -R libra-tools/libra "$run_id" --json jobs --jq '[.jobs[] | select(.conclusion=="success")] | length') || { echo "JOB STATUS FETCH FAILED for $ver"; exit 1; }
  total=$(gh run view -R libra-tools/libra "$run_id" --json jobs --jq '.jobs | length') || { echo "JOB COUNT FETCH FAILED for $ver"; exit 1; }
  builds=$(gh run view -R libra-tools/libra "$run_id" --json jobs --jq '[.jobs[] | select(.name | startswith("build-and-upload"))] | length') || { echo "BUILD LEG FETCH FAILED for $ver"; exit 1; }
  [ "$total" -eq 8 ] || { echo "JOB COUNT for $ver: $total != 8"; exit 1; }
  [ "$ok" -eq 8 ] || { echo "NOT ALL GREEN for $ver: $ok/8"; exit 1; }
  [ "$builds" -eq 4 ] || { echo "BUILD LEGS for $ver: $builds != 4"; exit 1; }
  for j in $downstream_jobs; do case " $names " in *" $j "*) ;; *) echo "MISSING JOB $j for $ver"; exit 1;; esac; done
  tmpd=$(mktemp -d) || { echo "MKTEMP FAILED for $ver"; exit 1; }
  gh run download -R libra-tools/libra "$run_id" -n formula-commit-sha -D "$tmpd" || { echo "FORMULA-SHA ARTIFACT MISSING for $ver (update-homebrew-tap success path not reached)"; rm -rf "$tmpd"; exit 1; }
  fsha=$(tr -d '[:space:]' < "$tmpd/formula-commit-sha.txt")
  rm -rf "$tmpd"
  [ ${#fsha} -eq 40 ] || { echo "BAD FORMULA SHA for $ver: $fsha"; exit 1; }
  formula=$(curl -fsSL "https://raw.githubusercontent.com/libra-tools/homebrew-libra/${fsha}/Formula/libra.rb") || { echo "TAP FORMULA FETCH FAILED for $ver"; exit 1; }
  case "$formula" in *"version \"${ver#v}\""*) ;; *) echo "TAP FORMULA NOT AT ${ver#v} for $ver"; exit 1;; esac
  if [ "$ver" != "v0.22.11" ]; then
    vtmpd=$(mktemp -d) || { echo "MKTEMP FAILED for $ver"; exit 1; }
    gh run download -R libra-tools/libra "$run_id" -n homebrew-verify-pass -D "$vtmpd" || { echo "HOMEBREW VERIFY PASS ARTIFACT MISSING for $ver (verify-homebrew-formula success path not proven)"; rm -rf "$vtmpd"; exit 1; }
    rm -rf "$vtmpd"
  fi
  echo "OK: $ver run=$run_id tag_commit=$sha D-group 8/8 green + tap formula positive proof"
done
```

（run 绑定先解析 tag 的 commit SHA（annotated tag 自动 peel），再以 `headSha==tag_commit` 选取 run——`--branch` 对 tag 推送不可靠，R35 Codex P1-1；Homebrew 正向证明走 `formula-commit-sha` artifact + tap formula 版本断言，bash `case` 模式匹配替代 `grep -Fq` 管道以消除 SIGPIPE 假失败（R30 Codex P1-6/P1-7）；`// empty` 显式拒绝空 run id（R31 Codex P2-1）；verify-homebrew-formula 吞错面由 PKT-05 随 v0.22.12 收口——sentinel artifact `homebrew-verify-pass` 断言自 v0.22.12 起生效，v0.22.11 豁免并注明（该切片时点 workflow 尚未收口，其正向证据为 formula-commit-sha artifact + tap 版本断言，R31 Codex P1-2）

**tag 绑定（R7 Codex P1-4；R8 Codex P1-3 修正顺序；R23 Codex P1-12 示例具体化）**：每个发布切片的发布步必须显式创建并推送 tag。固定顺序以 PKT-01 为例（其余切片按「发布窗口顺序」行同构执行）：①`libra tag "v0.22.11"`；②`libra push origin "v0.22.11"`（触发 `release.yml` 的 `push` `v*`）；③`gh release create -R libra-tools/libra "v0.22.11" --title "v0.22.11" --generate-notes --verify-tag`（tag 已在远端，`--verify-tag` 校验通过并补 Release 元数据）。**不得**依赖 `gh release create` 自动建 tag；D 组取证一律以上方收口循环为唯一取证命令（其内部已锁定 workflow/event/ref），禁止以未过滤的 run 列表或其它版本的绿色 run 冒充本切片证据。

## 实施顺序

- `FIX-PKT-01 -> PKT-01`（R44：全量暴露的测试 fixture 就绪屏障修复，独立发布；不把该修复混入 PKT-01 生产写集）
- `PKT-01 -> PKT-02`
- `PKT-02 -> PKT-03`
- `PKT-02 -> PKT-04`
- `PKT-02 -> PKT-05`
- `PKT-03 -> PKT-05`
- `PKT-04 -> PKT-05`
- `PKT-05 -> PKT-06`
- `PKT-06 -> PKT-07`
- `PKT-07 -> PKT-10`
- `PKT-10 -> PKT-08`
- `PKT-10 -> PKT-09`
- `PKT-09 -> PKT-11`
- `PKT-07 -> PKT-13`
- `PKT-10 -> PKT-13`
- `PKT-05 -> PKT-09`
- `PKT-05 -> PKT-08`
- `PKT-08 -> PKT-12`
- `PKT-08 -> PKT-13`
- `PKT-09 -> PKT-14`
- `PKT-12 -> PKT-11`
- `PKT-12 -> PKT-13`
- `PKT-11 -> PKT-13`

### 依赖登记表

无跨计划 / 外部依赖。

| ID | direction | 类型 | 对象 | Owner | 产物与可用性判据 | 证据 | 超时与失败策略 |
|---|---|---|---|---|---|---|---|
| N/A | — | — | 无外部依赖 | — | — | — | — |

### 发布分组与并发窗口

| ID | 成员 | 唯一发布点 | 窗口规则 | 失败回滚顺序 | 理由 |
|---|---|---|---|---|---|
| REL-PKT-A | PKT-02、PKT-03、PKT-04 | PKT-05 | **staging/验收流程**：三子卡各自本地提交但保持 `in-progress`、不推送；三张补丁合并到未推送家族树后，在家族树上逐卡运行 A/B 门并转 `locally-accepted`，逐卡取得 ER-05 review PASS（R37 Codex P1-4）；PKT-05 在该家族树上执行 C 组、bump `v0.22.12`、`gh` 发布；子卡随 C/D 覆盖转 `done/complete`；窗口期禁止插入其它切片 | 按依赖逆序前滚（解析器 → 调用方 → push 边界） | `read_pkt_line` 签名迁移与调用方编译不可分割（G-08；R5 P1-6） |

**并发声明:** 家族子卡并发实现；PKT-06→07→10→13 串行（`fetch.rs`/`fetch.md`）；PKT-08→12→11→13 串行（`git/ssh_client.rs`）；PKT-10→08（`clone.md`/`fetch.md` 文档面）与 PKT-10→09→11（`COMPATIBILITY.md`/`error-codes.md` 文档面）串行（R35 Codex P1-2）；`push.rs` 代码面（PKT-09）与 fetch 链代码面不相交可并发；发布一律串行。

**发布者:** 执行本计划的单一 Agent/人（ER-12）。

**发布窗口顺序:** PKT-01（`v0.22.11`）→ PKT-05（`v0.22.12`）→ PKT-06（`v0.22.13`）→ PKT-07（`v0.22.14`）→ PKT-10（`v0.22.15`）→ PKT-09（`v0.22.16`）→ PKT-08（`v0.22.17`）→ PKT-12（`v0.22.18`）→ PKT-11（`v0.22.19`）→ PKT-13（`v0.22.20`）→ PKT-14（`v0.22.21`）；（PKT-14 可与 PKT-08/12/11/13 并行**实现**——`push.rs` 写集与它们不相交——但发布严格按上列单调顺序串行，R12 Claude P1-3：禁止版本倒退）同一时刻只允许一个切片处于「已 bump 未完成推送 / 未触发 release」状态。

### Phase 1: 帧长校验单一事实源（PKT-01）

**进入条件:** ER-01/ER-02 完成。**退出条件:** PKT-01 `complete`。

### Phase 2: 同步解析与调用方 fail-closed（家族 REL-PKT-A：PKT-02/03/04，发布点 PKT-05）

**进入条件:** PKT-01 `complete`。**退出条件:** PKT-05 `complete`（子卡随 C/D 转 `done/complete`）。

### Phase 3: 边界归类与异步读取面（PKT-06、PKT-07、PKT-09、PKT-08、PKT-10）

**进入条件:** PKT-05 `complete`。**退出条件:** 五卡 `complete`。

### Phase 4: SSH 诊断面与异步头解析收口（PKT-12、PKT-11、PKT-13、PKT-14）

**进入条件:** PKT-08 `complete`（PKT-14 进入条件为 PKT-09 `complete`）。**退出条件:** 四卡 `complete` + 全量收口门全绿。

## 任务卡

### 任务卡粒度规则（强制，G-01..G-11）

以下规则对全部任务卡强制；完整定义以模板为准（本节按模板 v2.2 置于「## 任务卡」之下，R37 Codex P1-6）：

- **G-01 单一行为轴**：十四卡各一轴（见粒度审计表）；恢复模式统一 `immutable-release`。
- **G-02 完整可交付**：实现、测试、文档、索引同步归属同一卡。
- **G-03 条目上限与计数口径**：`implementation` 卡 AC ≤8、`release` 卡 AC ≤12，**按独立 pass/fail 谓词与独立验证门计数**（PKT-08 经 EX-05 登记为 10 门门族：8 条客户端×帧类谓词 + payload/partial-header EOF 两门；R2–R5 重构、R25 扩展）。
- **G-04 规模上限**：开工态只允许 S/M；无 L/XL 卡。
- **G-05 Agent 可独立执行**：`Verification` 为确切命令并绑定具名测试 fn；全部新 fn 标 `(new)` 并经**失败闭合存在性脚本**（见「Verification 判定口径」）防零匹配空跑。
- **G-06 依赖闭合且无环**：依赖边与各卡 `Dependencies` 一致（含显式 `PKT-02 -> PKT-05`）。
- **G-07 发布切片对齐**：默认每卡独立发布；PKT-02/03/04 按 G-08 组成家族、共用发布点 PKT-05。
- **G-07a**：不适用（家族卡不是 batch-release）。
- **G-08 家族卡**：`read_pkt_line` 签名迁移与全部同步调用方编译不可分割——家族 **staging/验收流程**：子卡各自本地提交但保持 `in-progress`；三张补丁合并到未推送家族树后，在家族树上逐卡运行 A/B 门并转 `locally-accepted`，逐卡取得 ER-05 review PASS（R37 Codex P1-4）；PKT-05 在该家族树上执行 C 组并发布；子卡随 PKT-05 C/D 覆盖转 `done/complete`。
- **G-09 拆分协议**：谱系见「修订历史」全部行（R1 起全部评审轮次）；`split-from` 用 `@R<世代>` 标记。
- **G-10 写集与并发**：同文件卡串行（PKT-06→07→10→13；PKT-08→12→11→13）；共享文档面串行（PKT-10→08：clone/fetch.md；PKT-10→09→11：COMPATIBILITY.md/error-codes.md）；其余写集不相交可并发。
- **G-11 任务类型**：除 PKT-05 为 `release` 外均为 `implementation`。

### Task PKT-01: 共享帧长校验 helper 与 marker 常量（单一事实源）

**Task type:** `implementation`

**Lifecycle / Acceptance:** `in-progress` / `locally-accepted`（R54：最终0.22.30的check/fmt/scopedClippy、focused/guards、完整默认全量及同源码双review全部通过；准备签名提交/main/tag/gh，D待取得）

**执行记录（2026-09-15，A/B 已通过，未完成 C/D）：** 在独立 main worktree 添加公开纯 helper、公开 `PktFrameError`、消费 crate 内 marker 的固定 Display 与五个具名单测；旧 parser/transport 未改动。双 env 16/16 + 独立 target/config 下 `cargo test --lib pkt_frame_` 实跑5项全部通过（0失败/忽略，编译3m18s）；fmt check 通过。具名存在性实证5/5唯一（4942项 listing，重编译3m28s）；all/extra unwrap 两守卫及版本面两守卫共4项通过（重编译3m10s）。Codex 自审 PKT-01 实现 PASS；限定 lib/bin/三个相关测试 target 的 scoped-Clippy `-D warnings` 已通过（1m55s，进程退出0，仅既有第三方 future-incompatibility advisory）；独立实现审查已退出0、结论 PKT-01 implementation/tests/scope PASS，并确认 R43 关闭后继 F1；实际 Clippy 终态与 +106/-0 差异补齐两项 P2 证据，P3 取舍由 Codex 具名记录于 pkt01-review-resolution.md。证据目录 `/mnt/gitmono/tmp/libra-pkt-execution.mc0nXP`：`pkt01-focused.log`、`pkt01-test-list.log`、`pkt01-local-gates.log`、`pkt01-self-review.md`。C 组已启动：五版本面已从0.22.28提升至0.22.29，Windows installer 保留 CRLF；Cargo.lock 由 cargo check 自动刷新，仅本包版本变化。后 bump check/fmt/scoped-Clippy/默认全量正按顺序运行，日志 pkt01-release-gates.log。尚未提交/tag/release，不标 complete；首卡真实 tag/SHA 在推送时登记。

**C 组历史运行故障记录（2026-09-15，关机中断，非通过）：** 后 bump `cargo check --lib --bin libra` 通过（1m20s），fmt 与限定 Clippy 通过（52.55s）；默认全量编译4m37s后启动 run `801d750c-9e1d-49f1-884c-1c6c1bca9386`（10051项、230 binaries、3项既有 skipped）。`command_test::command::agent_push_test::agent_push_after_prune_requires_force_rewrite_and_lease_protects` 首次6.259s失败于 `agent_push_test.rs:462`：`agent clean --all` 因持久 object-index repair 队列未清空返回 `LBR-IO-002`（repaired=0、remaining=true），第二次7.177s通过；不得把 retry 抹去或记为无不稳定项。当前继续原 no-fail-fast 进程，不重启、不修改其被测源码。源码核对发现 push 的直接 checkpoint seed 没有索引就绪屏障，而对应 `agent_clean_test.rs:181-204` 已有有界 marker 等待；这是待受控复现的竞态线索，不预先宣称既有失败或生产缺陷。独立定向诊断正在执行。证据为 `pkt01-release-gates.log`、`pkt01-agent-push-diagnosis.md`；若确认为越界 fixture 缺陷，先登记 `FIX-* -> PKT-01`，再实施、验收和重新安排未发布版本，不突破本卡生产写集。

**Description:** 在 `src/git_protocol.rs` 新增公开共享纯函数 `pub fn pkt_frame_payload_len(declared_len: u32) -> Result<usize, PktFrameError>` 与常量 `pub(crate) const PKT_LINE_PROTOCOL_ERROR_PREFIX: &str = "pkt-line protocol error: ";`（GC-02 单点）：`0`（flush）→ `Ok(0)`；`1..3` → `Err`；`4` → `Ok(0)`；`5..=0xffff` → `Ok(len - 4)`；`>0xffff` → `Err`。公开 `PktFrameError` 与公开 helper 的返回类型匹配；错误的生产 `Display` 实现消费 crate 内 marker 常量并输出固定原因，故尚无调用方时也无 dead_code/private_interfaces，不加 allow 绕过。Display 文本在既有五类具名单测内锚定。唯一行为轴：帧长校验规则与 marker 前缀常量集中化。

**Out of scope:**

- 同步解析器改造：PKT-02；异步循环/头部：PKT-07/08/13；错误码新增：ADR-PKT-01 复用 `LBR-NET-002`。

**Current evidence:**

| 事实 | 证据 |
|---|---|
| 旧解析器仍未消费新增 helper（下一家族卡接入） | `src/git_protocol.rs:129-151`，payload copy `:147` |
| 帧长上限 `0xffff` | `src/git_protocol.rs:165-167`（旧 writer 文档，本卡不改 writer） |
| 新增 helper/marker 的唯一生产定义，未删除旧代码 | `src/git_protocol.rs:63,69,101`；实际 Git diff +106/-0，SHA-256 55c0c8f2910d93b940bb1b66702b64e1d62511c7173f8788be1d512d3fddd76e |

**Acceptance criteria:**

- [x] `declared_len == 0` 返回 `Ok(0)`（flush 语义锚定）。
- [x] `declared_len ∈ 1..3` 返回 `Err`。
- [x] `declared_len == 4` 返回 `Ok(0)`（空 payload）。
- [x] `declared_len ∈ 5..=0xffff` 返回 `Ok(len - 4)`。
- [x] `declared_len > 0xffff` 返回 `Err`。
- [x] 具名单测覆盖上述五类输入并全部通过；直接断言 Result 与固定 Display 文本，测试块也不使用 unwrap/expect/panic，兼容 PKT-02 后续全文件守卫。
- [x] helper 生产路径零 `unwrap`/`expect`/`panic!`（本卡时点旧解析器 `:138`/`:140` 的 `expect`/`panic!` 尚未移除，全文件 rg 零命中守卫由 PKT-02 承载（其写集删除全部旧解析器 panic 面，R25 Codex P1-1）；本卡以 `compat_all`/`compat_extra` 作 unwrap 面回归）。

**Verification:**

- [x] 失败闭合存在性 + 执行（内联可执行，fn 全集 `(new)`）：
  ```bash
  expected="pkt_frame_payload_len_ok_flush_zero pkt_frame_payload_len_rejects_len_below_four pkt_frame_payload_len_ok_len_4_empty pkt_frame_payload_len_ok_normal_and_upper_bound pkt_frame_payload_len_rejects_over_0xffff"
  missing=$(bash -c 'set -o pipefail; list=$(source .env.test && cargo test --lib -- --list 2>/dev/null | sed "s/: test\$//" | awk -F"::" "{print \$NF}" | sort) || { echo "TEST LIST PIPELINE FAILED" >&2; exit 1; }; comm -23 <(printf "%s\n" $1 | sort) <(printf "%s\n" "$list")' _ "$expected") || { echo "FN EXISTENCE CHECK FAILED (pipeline error)"; exit 1; }
  [ -z "$missing" ] || { echo "MISSING FNS: $missing"; exit 1; }
  source .env.test && cargo test --lib pkt_frame_
  ```
- [x] `source .env.test && cargo test --test compat_all_production_unwrap_guard`
- [x] `source .env.test && cargo test --test compat_extra_production_unwrap_guard`（`git_protocol.rs` 在其 `TARGET_FILES` 内）

**Full-suite trigger:** `T-1: 新增 GC-02 单一事实源共享 helper 与跨层契约常量，执行阶段跑全量`

**Dependencies:** `FIX-PKT-01`（R44 执行期修复前置；原功能主轴首卡不变）。

**Deliverables:** N/A（代码卡）。

**Implementation write set:** `src/git_protocol.rs`（含 `#[cfg(test)]`）

**Release write set:** Inherited（= ER-08 版本面五处 + `Cargo.lock` 工具链刷新 + release artifact）

**Files likely touched:** `src/git_protocol.rs`

**Docs and compatibility impact:** N/A：当前仅新增未接入parser/transport的纯helper、错误类型与Rustdoc；用户命令、稳定码、配置、数据格式及网站行为无变化。生产调用方与用户文档同步由后继行为卡承担。

**Rollback mode:** `immutable-release`

**Migration and rollback:** N/A（恢复=发布修复版本，降级=回装上一 `v*` tag）

**Security and privacy:** 纯函数无 I/O；错误不含输入字节回显

**Performance budget:** O(1)；无分配；继承 GC-10

**Estimated scope:** `S`（1 落点 / 1 生产文件）

**R53 恢复与重验启动（2026-09-15）：** FIX-PKT-01的main/tag/gh真实C已核对；从私有归档恢复原+106/-0 helper补丁（patch SHA256=`48b9594a6d4d32db047948cdad5cf371617ad4727f21cfc8c9b960bed7b9ae8e`，完整 `src/git_protocol.rs` SHA256=`55c0c8f2910d93b940bb1b66702b64e1d62511c7173f8788be1d512d3fddd76e`，与独立审查内容相同）。实际远端main为6dd2364d1且v0.22.30不存在，五版本面由0.22.29提升至0.22.30；Cargo.lock未手改，由接下来的cargo check刷新。使用私有CI0.9.143、原双env/16槽/正常umask与超时，依次执行check/fmt/scopedClippy、具名listing、五项focused、三target guards及无过滤完整默认nextest；独立 `pkt01-restored-*` 日志/三份JUnit/来源快照，不能复用旧失败全量或FIX的10050-case运行冒充本卡C。实现review继续以相同源码hash绑定，实际门终态仍待取得。

**R54 最终post-bump门闭环（2026-09-15）：** exec12166已退出0；Cargo check51.84s、fmt、限定Clippy50.80s通过，工具链仅将本包lock版本刷新为0.22.30。listing4942项中五个helper名字唯一存在；focused run `0aa9378e-e495-4ba9-9732-3c436ff4aea0` 编译2m54s、执行0.070s，5/5无retry；guards run `6e7474d2-dcfb-4994-8cd0-6c2b532cb31b` 编译2m28s、执行0.040s，4/4无retry。随后完整默认 run `57865dbb-e8e1-41d9-9ea1-fa79e6d1fa57` 编译4m15s、测试1352.124s，10055/10055通过（230 binaries、零失败/重试、3 slow、3原有ignored）；两项MCP首次227.000s/439.629s通过。本卡五项helper与前置四项fixture回归也均在full中首次通过。三份JUnit的UUID、唯一case数、selection与failure/error/skipped/retry节点已逐项核验，full SHA256=`62b364175cbbd2d8369e5b5fc8227d1ac103b446b5313b67ee9d1d06c7dd3b9a`；全量前后HEAD6dd2364d1/2346tracked快照相同，内容SHA256=`95f32dbf6c5a60410356b5ee9a6003aaafd9ddf1e33027a7beb353006a674686`。GNU Time含full编译26m48.36s/maxRSS26817256KiB/退出0；原始日志、三JUnit、time、来源快照与 `pkt01-restored-results-summary.json` 独立留存，不混用FIX或早期中断运行。Codex按最终补丁与AC审计PASS；完整src/git_protocol.rs仍为已独立PASS审查的SHA55c0c8f2910d93b940bb1b66702b64e1d62511c7173f8788be1d512d3fddd76e，+106/-0，未接入原parser/transport。本轮全量后仅补计划事实、准备精确八文件提交和真实C；当前不能把该准备写成已推送或D完成。

**Version increment:** `patch`（原 `0.22.28 -> 0.22.29` 工作树尚未发布；R44 后由 FIX-PKT-01 先占下一实际 patch，本卡预期 `0.22.29 -> 0.22.30`，必须按当时远端状态重核、重新工具生成 lockfile 并重跑全部 C 门；不复用本轮未终结测试作为新版本的验收）

**Release boundary:** `independent`（完整 C 组 + D 组取证）

**C/D coverage from:** `self`

**Granularity:** `type=implementation; axis=帧长校验与 marker 常量单一事实源; recovery=发布修复版本（immutable-release），降级=回装上一 tag; complete=yes; self-contained=yes; AC=7/8; VER=3/8; landing=1; prod-files=1; scope=S; deps=FIX-PKT-01; writeset=no-overlap; release=independent; split-from=N/A（主轴首卡）; exception=N/A`

**重启终态核对（2026-09-15）：** 原 run `801d750c-9e1d-49f1-884c-1c6c1bca9386` 的日志已明确记录 SIGTERM 及 `error: test run failed`，不是仍在运行或观察超时：执行1286.230s，8762/10051项已运行并通过（10 slow、1 flaky），3项既有 skipped，1289项因信号未执行。保留 push/prune 首败次过记录；本轮无完整全量验收，不能复用其 JUnit 当作完成证据。重启后 origin/main 新增两条仅改 issue #477 文档的提交，核对源码/测试/配置/版本面零变化后快进至 `60133c38aa36ac2e6ab9411d61cdf11125f7b887`。已验证 PKT-01 原文件 SHA256 与归档补丁一致，备份全部自有未提交写集后暂时移出 helper、恢复五面0.22.28（lockfile交工具链刷新），先构造 FIX-PKT-01 未接入屏障的 red 证据；不改生产安全拒绝。

### Task FIX-PKT-01: agent push checkpoint fixture 的索引就绪屏障

**Task type:** `implementation`

**Lifecycle / Acceptance:** `in-progress` / `remote-pending`（R53：A/B、双review、最终全量及签名/main/tag/gh已完成；实际8 job、下载/manifest/tap及CodeQL的D证据待取得）

**受控 red 证据（2026-09-15 重启后）：** main=`60133c38a`，五版本面与工具链恢复的 Cargo.lock 均为0.22.28；`git diff --exit-code HEAD -- src Cargo.toml Cargo.lock web/package.json worker/package.json install.sh install.ps1` 为0，确认原生产/版本基线。仅加入测试及尚未接入的检查 helper，受控 delay hook 为250ms且只在直接 seed 期间生效。run `2e8aa5b5-bd28-49f4-af71-9425a21fa30a` 编译3m38s后，指定 `agent_push_seed_waits_for_durable_index_retirement` 在4.900s明确失败于“checkpoint seed returned before durable index readiness”，捕获 seed 返回时13个marker；之后清理drain成功，nextest退出100（非编译/配置失败），无retry。log/JUnit分别为 `fix-pkt01-red.log`、`fix-pkt01-red-junit.xml`，源测试SHA256=`d945eafb16540bd926c571c95deb4e0843b4647d2ea412bed9697a9675213394`。随后仅在seed返回前接入10秒有界drain和严格marker检查；四项新测试按classifier的完整命名lane登记，生成external group从209增至213个test filter，7个binary filter不变。修复后green、Clippy、完整全量和发布尚待验证。

**修复后 focused green（2026-09-15）：** run `e5404a8d-2c9a-4053-8b97-88c583ac059a` 编译3m53s，执行9.940s，7/7通过、无retry、退出0：全部6个 `command::agent_push_test::` 案例及既有 `agent_clean_fails_closed_until_pending_index_marker_can_retire`。四个新名字实际唯一执行，延迟回归5.080s通过，原push/prune/force-rewrite/lease链2.808s通过，clean安全拒绝1.507s通过。其它3412项为focused选择外案例，不能计作全量。日志/JUnit为 `fix-pkt01-green.log` / `fix-pkt01-green-junit.xml`；保留原失败断言与安全断言。随后启动四guard target和限定Clippy的本地门，尚无其终态，不宣称全量、review或发布完成。

**Description:** ER-10 执行期越界修复，唯一行为轴是 `agent_push_test` 的直接 checkpoint fixture 返回时已完成其索引写入。当前 helper 未等待后台 FIFO，后续独立 `agent clean --all` 可能正确地拒绝仍有索引修复锁的仓库；修复 fixture，不放宽产品拒绝条件。独立诊断已退出0并明确支持该方向，但原失败瞬间的锁快照缺失，受控回归证据仍须补齐。

**Out of scope:** 不改 `src/**`、生产 preflight、索引队列、lease/force-rewrite 语义；不改 `agent_clean_test` 的既有 helper；不降低槽数、不为 `agent clean` 加重试、不增加生产 failpoint；不夹带 PKT-01 helper。

**Current evidence:** 首次全量 run `801d750c-9e1d-49f1-884c-1c6c1bca9386` 中原 push/prune 用例首试6.259s失败、次试7.177s通过；`agent_push_test.rs:192-273` 缺少屏障，`agent_clean_test.rs:181-204` 已有相应等待。`client_storage.rs:2663,2670,2733` 的 `remaining` 来源与当前小型 fixture 结合，指向父测试进程的 queued writer shard lock；不是协议解析报错。Claude Code 定向源码覆核见 `agent-push-race-review.txt`，区分源代码推导与尚未捕获的具体运行交错。上述材料不代替 red/green 回归。

**Acceptance criteria:**

- [x] seed helper 返回前，以 `ClientStorage::wait_for_background_tasks_until` 对直接库调用所属队列做10秒有界 drain；超时明确失败，不使用无界等待。
- [x] drain 后严格检查本 fixture 的 `object-index-repair` 目录：不存在或为空才就绪；剩余 marker、目录读取错误及 entry 错误均明确失败，不把任意 I/O 错误当作空目录，不调用自动 replay 隐藏终态失败。
- [x] 受控队列延迟回归证明修复前 helper 可在 marker 未退役时返回，修复后返回时 marker 已清空；使用既有 delay hook 与 restoring guard，子 CLI 不继承该注入，记录原 helper 的 red 与修复后 green 实际证据。
- [x] 具名回归分别覆盖 terminal marker 拒绝、marker 路径 I/O 错误拒绝及缺失/空目录接受；保留原 push→prune→普通 push 拒绝→force-rewrite 成功→lease 拒绝链，原 `agent_clean_fails_closed_until_pending_index_marker_can_retire` 继续通过。
- [x] 新 serial 注解按 classifier 结果登记并重新生成 nextest 配置；双 env 与16/16不变，全部 scoped 验证、fmt/Clippy、独立审查及修复后完整默认全量通过。
- [x] 用户文档/网站 N/A：仅测试 fixture 与其调度登记，生产 CLI、错误码、配置、数据格式无变化；不新增 `--test` target，`tests/INDEX.md` 无目标级变更。release notes 说明 fixture 竞态修复及实际测试，不宣称 pkt-line 已接入。

**Verification:**

- [x] `command_test` 下四个新名字唯一存在且实跑通过：`agent_push_seed_waits_for_durable_index_retirement`、`agent_push_seed_readiness_rejects_terminal_marker`、`agent_push_seed_readiness_rejects_marker_path_io_error`、`agent_push_seed_readiness_accepts_missing_or_empty_marker_dir`；另实跑 `command::agent_push_test::` 全部用例及上述既有 clean fail-closed 用例。每次均用执行约定的双 env wrapper 与 nextest timeout tool config，不把 listing 当作执行。
- [x] `compat_serial_registry`、`compat_all_production_unwrap_guard`、`compat_extra_production_unwrap_guard`、`compat_version_surface_sync`；`sh tests/NEXTEST_GROUPS.sh` 后生成结果一致；fmt 与限定 lib/bin/上述 target 和 `command_test` 的 Clippy `-D warnings` 通过。
- [x] 最终 bump 后默认 `cargo nextest run --all --no-fail-fast --retries 2` 全量终态和新 JUnit 留证；自审与独立实现审查通过后才签名+DCO提交、非强制推 main/tag、以内容摘要调用 gh 发布。

**Full-suite trigger:** `T-1: nextest 仓库配置及 installer 版本面；另须重跑全量验证本次失败修复`

**Dependencies:** 无；`FIX-PKT-01 -> PKT-01` 单向前置，不形成 family C 覆盖环。

**Deliverables:** 本卡 sanitized red/green、诊断、审查、C/D 记录。

**Implementation write set:** `tests/command/agent_push_test.rs`、`tests/SERIAL_REGISTRY.tsv`、生成的 `.config/nextest.toml`，及 `tests/compat/serial_registry.rs` 中本次新增四项成员对应的数量锁定断言（209→213）；本计划执行记录为协调面。

**Release write set:** Inherited（ER-08 五版本面及 Cargo 工具生成的 lockfile）。

**Files likely touched:** 同 Implementation/Release write set，禁止暂存其他工作树或 PKT-01 的生产补丁。

**Docs and compatibility impact:** N/A（理由及核对见 AC6）；无新增测试 target。

**Rollback mode:** `immutable-release`

**Migration and rollback:** 无运行时迁移；修复错误只能前滚，不能覆盖已发布 tag。

**Security and privacy:** 仅隔离临时 fixture；delay env 用 guard 恢复；不输出用户 env 或远端凭据，不改产品数据安全防线。

**Performance budget:** drain 硬上限10秒，仅测试 helper；期望正常情况即时完成，故障明确报错；无生产性能变化。

**Estimated scope:** `S`（1 测试 fixture 落点 / 0 生产文件，2 调度登记/生成文件）

**本地守卫故障与写集补充（2026-09-15）：** run `96ff881f-70b3-4def-a9e5-2667b15f6a46`，四target共24项实际执行，23通过，`nextest_groups_toml_matches_generator_and_registry` 因原数量锁定209而实测213失败，退出100；生成内容逐字一致、registry推导的全部成员逐项相等均已先通过。遗漏属于本卡新增四项serial成员的同轴同步，实施前将该数量断言纳入I写集，G-01..G-11复核仍为单一fixture就绪轴、AC6/VER3、0生产文件、S scope；不删除数量断言、不削弱完整成员比对。仅同步209→213并重跑该失败项；此前23项证据保留，最终post-bump完整全量仍须重新通过。原pipeline在guard失败时停止，Clippy尚未运行；失败log/JUnit另存，不以脚本启动冒充完成。

**本地门闭环（2026-09-15）：** 上述唯一失败项在 run `428b4331-6c2e-4d94-9a19-861b910cbdb9` 重编译2m42s后以0.098s通过，无retry、退出0；与未受数量断言改动影响的原23项合计覆盖四target全部判据，不宣称同一run 24/24。之后限定 `cargo clippy --lib --bin libra --test command_test --test compat_serial_registry --test compat_all_production_unwrap_guard --test compat_extra_production_unwrap_guard --test compat_version_surface_sync -- -D warnings` 实际1m10s通过、退出0，只有既有第三方future-incompat notice。fmt、diff check、37个bash块语法与15卡/23边结构门亦通过；自审代码/测试/范围PASS。开始独立Claude实现审查，完整post-bump全量与C/D仍须后续取证。

**R45 实现审查与处置（2026-09-15）：** 独立Claude Code CLI已正常退出0，字面 `VERDICT: PASS`，P0/P1 none；源代码核实队列归属、marker退役早于counter递减、返回点取样及完整成员登记。其P2-1所述父进程env向并发libtest邻居外溢风险由Codex具名接受为诊断模式的剩余风险：本计划用户执行覆盖采用nextest一测一进程，不能被旧CLAUDE.md中的cargo-test口径覆盖；本回归的子CLI全部先于delay guard启动，只给本文件command factory加env_remove也无法保护另一模块factory，故不虚报该建议能修复跨模块风险。P3-2同样依现有进程隔离契约接受；P3-3明确当前timeout分支及目录iterator-entry错误分支只有源码审查、没有直接回归（路径I/O错误已实跑）；P3-4为非阻断测试诊断建议；P3-5保留已实际red/green复现的250ms注入及10秒上限，最终16/16全量仍须验证；P3-6按精准暂存排除既有test-scopes与handoff，不移动/删除他人fixture。完整处置见私有证据 `fix-pkt01-review-disposition.md`；该PASS不认证尚未执行的post-bump全量、提交或D组。

**C 组启动（2026-09-15）：** 发布预检先拒绝落后于origin/main的bump，未写版本；刷新后核实新增四笔提交只改issue #477/#479/#481/#490计划文档，`src/tests/.config/Cargo/build.rs/installers/web/worker` 全部零差异。独立main随后非强制快进至 `96e1331da408a9e1eeb74acb0309733d7f1781f4`，复核远端精确SHA相等且v0.22.29未被占用后，五版本面从0.22.28提升至0.22.29，install.ps1保留CRLF。已启动 `fix-pkt01-release-gates.sh`：Cargo check工具链刷新lockfile→post-bump fmt/限定Clippy→无过滤默认nextest（16/16、现有超时/serial配置）；全量前后对所有tracked内容及HEAD生成快照并要求一致。当前仅为启动记录，完整终态、新JUnit、签名提交和C/D发布证据均尚待取得。

**R46 全量失败归因与恢复（2026-09-15）：** exec10861已退出100；run `c031ff44-e077-49b8-af80-ff45ebd6b623` 实际执行10050项/230binaries，1791.933s，10049 passed（6 slow）、1 failed、3既有ignored。唯一失败 `compat_mail_am_basic::empty_file_umask_and_symlink_sections_apply` 三试4.006s/3.932s/4.022s均在 `mail_am_basic_test.rs:1684` 观察mode100600；不是flaky通过。两个MCP均最终通过。本卡七项fixture/clean案例及四guard所有用例均通过，post-bump cargo check51.56s、限定Clippy1m08s与fmt亦通过，但全量仍失败。新JUnit UUID/count核对一致、SHA256=`845961a14e2b0d1c4818c804bea6029b00c979ee25808a6b57f4880a3343c89f`；前后2337个tracked文件与HEAD快照完全相等，内容SHA256=`36826072e04404e418fcc16cc38cc3c59290f262482f45cecb3f66e88d98d5bc`。原log、失败JUnit与summary分别保存于 `fix-pkt01-release-gates.log`、`fix-pkt01-failed-full-junit.xml`、`fix-pkt01-failed-full-summary.json`，不覆盖、不当作绿灯。

**R46 原因与受控诊断：** 私有runner及wrapper意外全局设置077，真实nextest `/proc/953372/status` 为0077，调用shell实际0022；`CliFixture::command` 的env_clear不清除进程umask，产品 `apply.rs:132` 正确执行 `base & !process_umask()`。修正只限私有脚本：077放进mkdir子shell，runner移除全局覆盖，evidence/config目录0700实测且由入口强制检查；产品与测试断言零修改。以同一缓存测试二进制（SHA256=`34b08d2f6b287caae0cc905a5dbb829c1f22b89d25d05c58039cd0e9749fd1f2`）做两个30秒有界子进程诊断：显式077得到同一断言、退出101；继承0022通过、退出0。另一次cargo诊断只等待活nextest的build锁，90秒退出124且未执行/编译，不计作red或green。证据在 `umask-diagnosis/`；这些诊断不代替完整默认套件。

**R46 独立覆核与后续门：** Claude首轮168.625s/19turns字面PASS但同时列P1-a/b/c，执行者按未闭环处理；已增加目录权限入口检查、R2日志/快照/JUnit独立命名及失败归档存在性前置，停止锁等待诊断并改用不重编译的缓存二进制。复核64.545s/4turns正常退出0，字面 `VERDICT: PASS — no P0 and no P1 remaining`，session=`0174c152-7c26-4488-b5b9-e65c9c58e4b5`。Codex已核实实际权限/进程退出/失败JUnit与同一二进制red-green；只接受其源码复核，不将review当作亲自执行测试。归档后核对新增九笔上游提交仅改issue计划，非强制快进至a87e8bfa3，生产/测试/配置/版本零上游变化。R2将保持双env、16/16、默认无过滤集合及现有超时，使用 `fix-pkt01-r2-*` 证据名；尚未取得新的全量终态、签名提交或发布。

**R47 R2 编译终态与同配置恢复（2026-09-15）：** exec72226已退出101；R2的cargo check50.78s、fmt/diff、限定Clippy1m03s均通过，真实cargo/nextest进程umask0022。完整nextest在编译 `agent_hook_span_test` 时由clang报告 `unable to execute command: Killed`，没有生成run UUID、没有开始测试、没有新JUnit；不把旧失败JUnit归给R2。可读kernel日志未发现OOM记录，可见cgroup的oom/oom_kill计数均0，调用者未能确定信号来源；事后磁盘约3TB可用、内存约77GiB可用，不将原因臆断为OOM。R2前后2346个tracked文件/HEAD快照相同，内容SHA256=`04ab3ac91254a14c575d5556cc9926e606136023fa8349dd5c0f35d5821ce4cd`。保留 `fix-pkt01-r2-release-gates.log` 与 `fix-pkt01-r2-build-failure-summary.json`。本次没有源码修复或新版本；仅重试未完成的默认全量编译/执行，继承同一源码/版本上已通过的check/fmt/scopedClippy，不重复它们。保持16/16、双env、正常umask、全部默认用例及既有超时，不清缓存或跳过失败target；R3证据独立命名，仍须完整终态与前后来源证明。

**R48 R3 终态与单 target 诊断（2026-09-15）：** exec4757已退出101；默认full在编译 `ai_agent_baseline_test` 时再次由clang报告 `unable to execute command: Killed`，仍未开始测试、无UUID/新JUnit。前后来源相同（2346 tracked、HEADa87e8bfa、SHA256=`e5a57358c8d5bdd4eed482620aad62dc09db3e98133f2bdce90446c652d29780`），日志与 `fix-pkt01-r3-build-failure-summary.json` 独立留存。实际链接参数是 `-fuse-ld=mold`（clang22.1.8/mold2.42.0）；可读kernel/oomd日志无相关记录，用户确认没有手动停止，仍不能确定信号发送者。首次time诊断因当时 `/usr/bin/time` 不存在而退出127，未启动编译；随后Python采样的单target `cargo test --no-run --test ai_agent_baseline_test` 在相同env/16 slots/正常mask下成功（exec91754退出0，163.410s，最高子进程RSS14576712KiB），只证明该target可以编译，不是测试或全量通过。用户现已安装GNU Time1.10。下一轮R4仍运行完整默认nextest，增加time-v及仅观察本次子进程树/主机内存的采样，不改源码、并发、flags、缓存、用例或超时；R2 check/fmt/scopedClippy继承条件再次逐项验证。完整全绿、C/D仍待取得。

**R48 R4 全量编译再次失败：** exec58089已退出101，完整nextest未进入测试，`agent_rpc_external_test` 链接再次被Killed；原始日志、time-v、每5秒资源采样与 `fix-pkt01-r4-build-failure-summary.json` 均独立保存。time-v为3m31.83s、maxRSS18067436KiB、无swap；采样进程树峰值23697608KiB、主机MemAvailable最低61304888KiB，5秒间隔不能排除更短瞬时峰值，仍无可读kernel/oomd OOM记录。前后tracked快照完全相等：HEADa87e8bfa、2346文件、SHA256=`c50a8f695222a28e4c6c7f351f28dcb4f226241f5160068b2e4c7018b5140c2c`。没有测试UUID/新JUnit，不算全量失败用例或绿灯。暂不盲重跑full；64次小型clang/mold链接探针（16并行）全部成功、0.382s，仅为环境诊断；随后仅在诊断进程PATH前置记录后原样exec clang的私有wrapper，保留RPC target完整链接argv和rustc临时输入以便独立复现，未改Rust flags、并发、仓库文件或系统linker。诊断不能替代原默认全量门。

**R49 编译诊断通过与正常 full 恢复：** RPC单target诊断exec45361退出0（2m44.78s/maxRSS14558448KiB），未运行测试；初版logger遗漏owned target内会删除的.o，因此原两条argv不可重放，留存限制记录，不虚报捕获完整。修正logger为先复制所有直接.o再原样exec系统clang后，全部target的 `cargo test --no-run --all --message-format json-render-diagnostics` 实际编译通过（exec46320退出0、4m09s，build-finished success=true，231条新记录的直接对象均核实存在）。源码、flags、16 jobs、系统compiler与缓存均未改；这只是带私有PATH记录器的编译诊断，未执行测试，也未确定先前SIGKILL来源。现恢复正常PATH下完整默认nextest，仍保留time-v/资源观察及独立R5证据；不再重跑已完成check/fmt/scopedClippy，入口继续验证实现/版本hash。正常环境全量/C/D尚待完成。

**R50 正常 full R5 终态与信号追踪准备：** exec39961已退出101，`compat_legacy_short_api_pre_r0_equivalence` 的clang/mold链接被Killed，无测试UUID/新JUnit；前后2346 tracked与HEAD一致，SHA256=`acd9e7a4bbfc7d42f0a0d3f64b6dd2570ad3aa45bb0b36e6283a35366453bb97`，失败summary/log/time/samples独立留存。此前完整cargo编译通过而nextest编译失败只是观察差异，尚非nextest归因。当前只读实测cargo-nextest为0.9.144（9718c77af），与AGENTS的CI0.9.143 pin分开记录；未替换全局工具或回填历史运行版本。失败target的已保存输入直接链接0.266s成功；16并行的64次相同输入链接全部通过（6.287s），未执行测试。另发现 `build.rs:27` 监看不存在的 `web/tailwind.config.ts`，与Cargo官方FAQ所述持续重编译条件相符；该独立build问题未修改，不构成SIGKILL原因证明。GDB只读诊断和临时kernel signal trace脚本已准备；后者需要root，`sudo -n`实际返回需要密码，已请用户在其终端启动限定compiler/linker SIGKILL的10分钟临时tracefs实例，不能把脚本准备写成已捕获信号。完整默认full、C/D仍未完成，原15卡不缩减。

**R51 CI 工具版本对齐（2026-09-15）：** 核心trace仍未启动，sudo认证请求保持待用户动作；没有把等待当成授权。另行只读核实 `.github/workflows/base.yml:261-266` 实际固定cargo-nextest0.9.143，而本机全局binary当前为0.9.144。为对齐仓库的已规定验收工具，在私有目录下载官方0.9.143发行资产并同时验证GitHub asset digest与发布的sha256文件（tar SHA256=`66786b9abe23920d022a182d1416b1bbc8130dd4872a9553d76985a1708dcd1e`；binary SHA256=`d97959a56feb7a1297576c1b47d9aaf7b5badd6be596e695ed203fa4490884ea`），实际版本60fa45f63/2026-08-04。全局binary哈希前后未变；不替换系统安装、不改CI或被测源码。下一轮R6使用该私有CI版本运行同一默认无过滤完整命令，双env/16/16/timeout/正常clang/umask保持；版本输出与来源单独留证。此为已发现工具偏差的控制变量验证，不能预先归因为0.9.144缺陷。完整测试/C/D仍待实际终态；核心trace若由用户启动，仍可独立观察信号来源。未采用手工metadata/archive或缩减全量选择。






**R52 最终全量实证与本地收口（2026-09-15）：** exec3028已退出0；run `c18f6507-312e-44ec-8581-31d1a8d402b3` 编译4m22s，实际测试1404.857s，10050/10050通过（230 binaries、零失败/重试、3 slow、3原有ignored）。两项MCP首次226.225s/460.093s通过，原umask失败用例首次3.768s通过，fixture全部六项及原clean安全拒绝均首次通过。JUnit UUID、10050个testcase、failure/error/skipped/retry节点与日志核对一致，SHA256=`a4f636148a93797dba8c8d5177c489645eccbdca4eea28ba590d4a94e1d19db2`；前后tracked/HEAD快照相同（2346文件，内容SHA256=`3dab4fb70936074d69b2c6ab6a011d9963c885c74639c2ead7412c42c98e24c8`）。GNU Time实测含编译27m48.78s、maxRSS27201392KiB、退出0。原始log、新JUnit、time与summary独立留存为 `fix-pkt01-r6-*`，不覆盖失败证据。R2已通过的check/fmt/scopedClippy对应实现/版本hash在R6入口重新核实一致；本轮之后只补计划事实，不改受测实现/测试/配置/版本。Codex逐项审计AC1–6及前两项Verification通过，独立R45实现PASS与R46脚本复核PASS继续有效。接下来精确暂存11个本卡文件、校验SSH签名+DCO、非强制推main/精确tag并创建内容化gh Release；这些动作及D证据仍须实际取得。sudo信号追踪不再是本轮前置，未启动trace，不以成功运行推断旧SIGKILL发送者。

**R53 C组实际闭环（2026-09-15）：** 精确暂存11个本卡文件，handoff与既有test-scopes未纳入；全量后逐文件SHA256核对只有本计划终态文字变化、生产src与HEAD零差异。提交 `6dd2364d1e0654f01e6afa894a11e14863bb0f29` 的SSH签名实际verify-commit通过、gpgsig和DCO均存在；非强制push后远端main精确等于该SHA，随后新建并推送 `v0.22.29`，gh以真实修改/测试notes和 `--verify-tag` 创建 [Release](https://github.com/libra-tools/libra/releases/tag/v0.22.29)。只读API已确认tag commit、非draft Release与唯一 `release.yml`/push/tag run [34934437110](https://github.com/libra-tools/libra/actions/runs/34934437110)，四个build leg正在运行；exact-head main/push CodeQL [34934413859](https://github.com/libra-tools/libra/actions/runs/34934413859) 两job正在运行。尚无成功artifact/tap/manifest证据，不标done/complete。实际C使PKT-01可恢复；D异步追踪但最终必须关闭。

**R54 D部分实证（2026-09-15，尚未完成）：** exact-head main/push CodeQL run `34934413859` 两job全部completed/success；release run `34934437110` 的linux-amd64/linux-arm64/darwin-arm64已completed/success，Windows仍in_progress，下游job尚待实际终态。两Linux版本路径已真实GET HTTP200并与同run的SHA/size artifact逐字节核对：amd64为116963992 bytes / `b9e372b359cef033af76b5a4b7d1e10c122c98dd4d73936143f0fbac026adb3f`，arm64为100453368 bytes / `6b139babc3d545667da0bd950a8ec1139dab0260417757ea3adc26a30b1c3c62`；只下载验证，没有执行或安装release binary。保留该观察时点及证据，不将3平台、CodeQL或两个下载结果冒充完整8job、formula/manifest/installers的D闭环。

**Version increment:** `patch`（预期独立 `0.22.28 -> 0.22.29`，发布前重新核对远端；原 PKT-01 未发布 bump 将先撤回到已归档补丁，再在本卡之后按实际版本递增）。

**Release boundary:** `independent`（不是不可分割家族，完整自身 C/D；不等待 D 后再恢复 PKT-01）。

**C/D coverage from:** `self`；D 同本计划实际8个 release job、真实下载/manifest/tap 与适用 CodeQL 证据。

**Granularity:** `type=implementation; axis=checkpoint fixture 的后台索引就绪; recovery=独立前滚补丁; complete=yes; self-contained=yes; AC=6/8; VER=3/8; landing=1; prod-files=0; scope=S; deps=none; writeset=I 与 PKT 主轴无交叉，R 串行; release=independent; split-from=ER-10 执行期新增; exception=N/A`

### Task PKT-02: 同步 `read_pkt_line` fail-closed 化与 `PktLineError` 引入（家族子卡）

**Task type:** `implementation`

**Lifecycle / Acceptance:** `pending` / 空（家族 staging：本地提交后保持 `in-progress`；家族树 A/B 门过 → `locally-accepted` → ER-05 review PASS（R37 Codex P1-4）；经 PKT-05 C/D → `done/complete`）

**Description:** 自 PKT-01@R1 拆出。引入树内类型 `PktLineError`（固定短语，零回显），实现 `From<PktFrameError> for PktLineError` 复用帧长原因；分类器测试只经公开 `CliError` stable-code 路径，不修改 `utils/error.rs` 私有函数可见性，同步 `read_pkt_line` 改为 `Result<_, PktLineError>`：取头前校验剩余 ≥4 字节、头部 fallible、消费 helper、payload 截断校验。唯一行为轴：同步解析器 fail-closed。**本卡不发布**（随家族 REL-PKT-A 经 PKT-05 统一发布）。

**Out of scope:**

- `mod.rs` / `push.rs` 调用方传播：PKT-03/PKT-04（同家族）；异步循环：PKT-07/08/13；CLI 映射：PKT-06/09/10；DB/HEAD/ToolRegistry：DEFER-02。

**Current evidence:**

| 事实 | 证据 |
|---|---|
| `copy_to_bytes(4)` 短头 panic | `src/git_protocol.rs:85` |
| `expect`（:90）/`panic!`（:92） | `src/git_protocol.rs:90,92` |
| `copy_to_bytes(pkt_length - 4)`（:99）无下界/截断防护 | `src/git_protocol.rs:99` |
| INVARIANT 注释与实际不符 | `src/git_protocol.rs:86-88` |

**Acceptance criteria:**

- [ ] 剩余字节不足 4（1–3 字节短头）返回 `Err`，`copy_to_bytes(4)` panic 消除。
- [ ] 非 UTF-8 头返回 `Err`（删除 `:90` `expect`）。
- [ ] 非 hex 头返回 `Err`（删除 `:92` `panic!`），`:86-88` INVARIANT 注释改写为与实现一致。
- [ ] `declared_len ∈ 1..3` 返回 `Err`（消费 PKT-01 helper，无本地重复实现），错误文本携带 `PKT_LINE_PROTOCOL_ERROR_PREFIX`（具名 fn 内断言 marker，保障最终归类 `LBR-NET-002`，R28 Codex P1-2）。
- [ ] 截断帧（声明 payload 超过剩余缓冲）在 `:99` 拷贝前返回 `Err`。
- [ ] 空 buffer 输入的返回值与现状一致（成功且不消费字节；仅指 parser 层面对空 buffer 的无消费成功——discovery 收到空响应必须报 `LBR-NET-002`，由 PKT-03 拒绝空响应、PKT-10 四命令端到端稳定码门承接）。
- [ ] flush `0000` 输入返回成功且头部已消费、payload 为空。
- [ ] `len == 4` 输入返回空 payload 成功帧。

**Verification:**

- [ ] 失败闭合存在性 + 执行（内联可执行，fn 全集 `(new)`）：
  ```bash
  expected="read_pkt_line_rejects_short_header read_pkt_line_rejects_non_utf8_header read_pkt_line_rejects_non_hex_header read_pkt_line_rejects_len_below_four read_pkt_line_rejects_truncated_frame read_pkt_line_preserves_empty_buffer read_pkt_line_preserves_flush read_pkt_line_preserves_len_4_empty_payload pkt_line_error_text_matches_protocol_classifier"
  missing=$(bash -c 'set -o pipefail; list=$(source .env.test && cargo test --lib -- --list 2>/dev/null | sed "s/: test\$//" | awk -F"::" "{print \$NF}" | sort) || { echo "TEST LIST PIPELINE FAILED" >&2; exit 1; }; comm -23 <(printf "%s\n" $1 | sort) <(printf "%s\n" "$list")' _ "$expected") || { echo "FN EXISTENCE CHECK FAILED (pipeline error)"; exit 1; }
  [ -z "$missing" ] || { echo "MISSING FNS: $missing"; exit 1; }
  source .env.test && cargo test --lib read_pkt_line_
  ```
- [ ] 零命中守卫（三分支，内联可执行）：
  ```bash
  if rg -n 'unwrap\(|expect\(|panic!\(' src/git_protocol.rs; then echo "FAIL: forbidden pattern found"; exit 1; else rc=$?; if [ "$rc" -ne 1 ]; then echo "ERROR: rg failed with exit $rc"; exit "$rc"; fi; echo "OK: zero hits"; fi
  ```
- [ ] `source .env.test && cargo test --lib pkt_line_error_text`（`PktLineError` 固定前缀契约 + 分类器兜底锚定）
- [ ] `source .env.test && cargo test --test compat_all_production_unwrap_guard`
- [ ] `source .env.test && cargo test --test compat_extra_production_unwrap_guard`（`git_protocol.rs` 在其 `TARGET_FILES` 内；本卡为删除该文件 `expect`/`panic!` 的直接卡，R7 Claude P1-3）

**Full-suite trigger:** `N/A（family child：C 覆盖继承 PKT-05；A 组 focused 命令在家族树上仍须通过）`

**Dependencies:** `PKT-01`。

**Deliverables:** N/A（代码卡）。

**Implementation write set:** `src/git_protocol.rs`（含 `#[cfg(test)]`）

**Release write set:** N/A（family child）

**Files likely touched:** `src/git_protocol.rs`

**Docs and compatibility impact:** Inherited（核对登记，预期零变更）

**Rollback mode:** `immutable-release`

**Migration and rollback:** N/A（随家族发布点前滚）

**Security and privacy:** fail-closed；`PktLineError` 固定短语零回显

**Performance budget:** O(1)；继承 GC-10

**Estimated scope:** `M`（1 落点 / 1 生产文件；协议解析行为变化）

**Version increment:** `N/A`（family child）

**Release boundary:** `family child of REL-PKT-A`

**C/D coverage from:** `PKT-05`

**Granularity:** `type=implementation; axis=同步解析器 fail-closed; recovery=随家族发布点前滚修复，降级=回装上一 tag; complete=yes; self-contained=yes; AC=8/8; VER=5/8; landing=1; prod-files=1; scope=M; deps=PKT-01; writeset=no-overlap（家族内与 03/04 不相交）; release=family child of REL-PKT-A; split-from=PKT-01@R1; exception=N/A`

### Task PKT-03: discovery 调用方传播、marker 契约与 capability 回显收口（家族子卡）

**Task type:** `implementation`

**Lifecycle / Acceptance:** `pending` / 空（家族 staging 同 PKT-02，含 ER-05 review PASS 门）

**Description:** 自 PKT-02@R2 拆出。在 `mod.rs:92` 把 `PktLineError` 按消费 marker 常量包装为携带 `PKT_LINE_PROTOCOL_ERROR_PREFIX` 前缀的 `GitError::NetworkError`；收口同函数内既有 capability 回显面（`:147-149` 改固定短语）。唯一行为轴：discovery 调用方传播、marker 契约与同函数错误文本卫生。

**Out of scope:**

- 各 CLI 映射点 marker 识别：PKT-06/09/10；push 调用方：PKT-04；`RemoteDiscoveryError` 全面类型化：DEFER-04。

**Current evidence:**

| 事实 | 证据 |
|---|---|
| `parse_discovered_references` 消费 `read_pkt_line`，返回 `Result<_, GitError>` | `src/internal/protocol/mod.rs:13,72-75,92` |
| 既有协议形状失败以 `GitError::NetworkError(format!(…))` 包装 | `src/internal/protocol/mod.rs:85-88,102-104` |
| `:147-149` 错误文本回显远端 capability 内容 | `src/internal/protocol/mod.rs:147-149` |

**Acceptance criteria:**

- [ ] `mod.rs:92` 消费 `Result` 并把 `PktLineError` 包装为携带 `PKT_LINE_PROTOCOL_ERROR_PREFIX` 前缀的 `GitError::NetworkError`。
- [ ] `parse_discovered_references` 对**空响应**（远端提前 EOF/零字节 advertisement，非合法 `0000` flush）返回 `Err(GitError::NetworkError(detail))` 且原始 detail 以 marker 开头；四命令最终 `LBR-NET-002` 验收归 PKT-10 门25，消除静默成功（R15 Codex P1-2）。
- [ ] `parse_discovered_references` 在畸形响应下返回 `Err` 且不再 panic（含空响应 fail-closed，R15 Codex P1-2）。
- [ ] `mod.rs:147-149` capability 回显改为固定短语（错误文本不再插入远端 capability 内容）。
- [ ] 新写/改写错误文本零回显（sentinel：恶意 capability 字节不出现）。
- [ ] marker 前缀契约具名测试通过（供 PKT-06/09/10 适配与分类器兜底）。
- [ ] marker 检测语义（`starts_with` 串首锚定）具名契约测试通过（R6 Claude P1-1：锚定 `PKT_LINE_PROTOCOL_ERROR_PREFIX` 的判定口径）。
- [ ] 包装合法响应回归通过（正常 discovery 行为不变）。

**Verification:**

- [ ] 失败闭合存在性 + 执行（内联可执行，fn 全集 `(new)`）：
  ```bash
  expected="pkt_line_discovery_propagates_pkt_line_error pkt_line_discovery_marker_prefix_contract pkt_line_discovery_marker_detection_semantics_starts_with pkt_line_discovery_capability_echo_fixed_phrase pkt_line_discovery_capability_echo_sentinel pkt_line_discovery_rejects_empty_response pkt_line_discovery_rejects_malformed_no_panic pkt_line_discovery_valid_response_regression"
  missing=$(bash -c 'set -o pipefail; list=$(source .env.test && cargo test --lib -- --list 2>/dev/null | sed "s/: test\$//" | awk -F"::" "{print \$NF}" | sort) || { echo "TEST LIST PIPELINE FAILED" >&2; exit 1; }; comm -23 <(printf "%s\n" $1 | sort) <(printf "%s\n" "$list")' _ "$expected") || { echo "FN EXISTENCE CHECK FAILED (pipeline error)"; exit 1; }
  [ -z "$missing" ] || { echo "MISSING FNS: $missing"; exit 1; }
  source .env.test && cargo test --lib pkt_line_discovery_
  ```
- [ ] `source .env.test && cargo test --test compat_all_production_unwrap_guard`
- [ ] 零命中守卫（三分支，内联可执行）（本卡新增错误构造；覆盖实际插值点 `{other}`/`{capability}`/`{pkt_length_bytes}`，R26 Codex P1-1）：
  ```bash
  if rg -n '\{other\}|\{capability|\{pkt_length_bytes\}|payload 插值' src/internal/protocol/mod.rs; then echo "FAIL: forbidden pattern found"; exit 1; else rc=$?; if [ "$rc" -ne 1 ]; then echo "ERROR: rg failed with exit $rc"; exit "$rc"; fi; echo "OK: zero hits"; fi
  ```

**判据规范（非计数正文，R42 八门枚举；EX-04 不再适用）：**

1. 传播（`pkt_line_discovery_propagates_pkt_line_error`）；2. marker 前缀契约（`…_marker_prefix_contract`）；3. 检测语义 starts_with（`…_marker_detection_semantics_starts_with`）；4. capability 固定短语（`…_capability_echo_fixed_phrase`）；5. capability sentinel（`…_capability_echo_sentinel`）；6. 空响应拒绝（`…_rejects_empty_response`）；7. 畸形响应不 panic（`pkt_line_discovery_rejects_malformed_no_panic`，R37 Codex P1-3 拆分）；8. 合法响应回归（`pkt_line_discovery_valid_response_regression`，R37 Codex P1-3 拆分）。门→命令（每门可直接复制执行，R24 Codex P1-2）：门 1-8 = `source .env.test && cargo test --lib pkt_line_discovery_`（门与 expected fn 一一对应）；分类器兜底锚定 = `source .env.test && cargo test --lib pkt_line_error_text`（该 fn 归属 PKT-02 expected 第 9 fn，由 PKT-02 Verification 承载执行）。

**Full-suite trigger:** `N/A（family child：C 覆盖继承 PKT-05）`

**Dependencies:** `PKT-02`。

**Deliverables:** N/A（代码卡）。

**Implementation write set:** `src/internal/protocol/mod.rs`（含 `#[cfg(test)]`）

**Release write set:** N/A（family child）

**Files likely touched:** `src/internal/protocol/mod.rs`

**Docs and compatibility impact:** Inherited（核对登记，预期零变更）

**Rollback mode:** `immutable-release`

**Migration and rollback:** N/A（随家族发布点前滚）

**Security and privacy:** fail-closed；零回显

**Performance budget:** O(1)；继承 GC-10

**Estimated scope:** `S`（1 落点 / 1 生产文件）

**Version increment:** `N/A`（family child）

**Release boundary:** `family child of REL-PKT-A`

**C/D coverage from:** `PKT-05`

**Granularity:** `type=implementation; axis=discovery 传播、marker 契约与 capability 收口（门族）; recovery=随家族发布点前滚修复，降级=回装上一 tag; complete=yes; self-contained=yes; AC=8/8; VER=3/8; landing=1; prod-files=1; scope=S; deps=PKT-02; writeset=no-overlap（家族内与 02/04 不相交）; release=family child of REL-PKT-A; split-from=PKT-02@R2; exception=N/A`

### Task PKT-04: push 调用方传播与 `PushError::Protocol` 变体（家族子卡）

**Task type:** `implementation`

**Lifecycle / Acceptance:** `pending` / 空（家族 staging 同 PKT-02，含 ER-05 review PASS 门）

**Description:** 自 PKT-02@R2 拆出。在树内枚举 `PushError`（`:235`）新增 `Protocol { detail }` 变体（`detail` 以 marker 前缀开头、由 `PktLineError` 转换而来，仅承载协议错误）；`:2120` / `:2131` 传播；push discovery 映射（`:878-900`）新增 marker 分支 → `PushError::Protocol`；push CLI 映射新增 `Protocol → LBR-NET-002` 分支。唯一行为轴：push 调用方协议错误传播与变体引入。

**Out of scope:**

- 状态行回显与归类收口：PKT-09（`Network(..)` 泛化分支的其它构造点维持 `LBR-NET-001`）。
- fetch/clone/ls-remote 边界：PKT-06/10；git/ssh 客户端：PKT-08。

**Current evidence:**

| 事实 | 证据 |
|---|---|
| `PushError` 为树内枚举 | `src/command/push.rs:235` |
| report 解析消费同步 `read_pkt_line` | `src/command/push.rs:2120`、`:2131` |
| push discovery 映射把 marker 文本落 `DiscoveryFailed`（→ NET-001） | `src/command/push.rs:878-900` |

**Acceptance criteria:**

- [ ] 新增 `PushError::Protocol { detail }` 变体（仅承载以 marker 前缀开头的协议错误）。
- [ ] `src/command/push.rs:2120` 把 `PktLineError` 传播为 `PushError::Protocol`。
- [ ] `src/command/push.rs:2131` 把 `PktLineError` 传播为 `PushError::Protocol`。
- [ ] push discovery 映射（`:878-900`）新增 marker 分支 → `PushError::Protocol`。
- [ ] push CLI 映射新增 `Protocol → StableErrorCode::NetworkProtocol`（`LBR-NET-002`）分支。
- [ ] push 畸形/截断 pkt-line 响应具名单测通过（`Err(PushError::Protocol{..})` 且稳定码 `LBR-NET-002`）。
- [ ] `DiscoveryFailed` 泛化分支维持 `NetworkUnavailable`（`LBR-NET-001`）回归断言通过。
- [ ] `Network(..)` 泛化分支维持 `NetworkUnavailable`（`LBR-NET-001`）回归断言通过——**断言范围仅覆盖其它构造点（如 `:867` 传输配置失败），状态行分支由 PKT-09 重新归类**。
  （push discovery 归类变化的 `docs/commands/push.md`（EN+zh）同步与 `docs/error-codes.md` 核对由 Verification 文档检查门承载，R32 Codex P1-3。）

**Verification:**

- [ ] 失败闭合存在性 + 执行（内联可执行，fn 全集 `(new)`）：
  ```bash
  expected="pkt_line_push_protocol_variant_exists pkt_line_push_discovery_marker_maps_to_protocol pkt_line_push_cli_maps_protocol_to_lbr_net_002 pkt_line_push_malformed_response_unit pkt_line_push_discovery_failed_regression_stays_net_001 pkt_line_push_network_variant_regression_stays_net_001"
  missing=$(bash -c 'set -o pipefail; list=$(source .env.test && cargo test --lib -- --list 2>/dev/null | sed "s/: test\$//" | awk -F"::" "{print \$NF}" | sort) || { echo "TEST LIST PIPELINE FAILED" >&2; exit 1; }; comm -23 <(printf "%s\n" $1 | sort) <(printf "%s\n" "$list")' _ "$expected") || { echo "FN EXISTENCE CHECK FAILED (pipeline error)"; exit 1; }
  [ -z "$missing" ] || { echo "MISSING FNS: $missing"; exit 1; }
  source .env.test && cargo test --lib pkt_line_push_
  ```
- [ ] `source .env.test && cargo test --test compat_all_production_unwrap_guard`
- [ ] 文档检查（R32/R33 Codex P1-3/P1-5：逐文件语义行断言 + 开发文档）：
  ```bash
  for f in docs/commands/push.md docs/commands/zh-CN/push.md docs/development/commands/push.md; do awk 'tolower($0) ~ /lbr-net-002/ && tolower($0) ~ /discovery|pkt-line|packet-line/ {f=1} END {exit f?0:1}' "$f" || { echo "MISSING semantic line (LBR-NET-002 + discovery/pkt-line): $f"; exit 1; }; done
  awk 'tolower($0) ~ /lbr-net-002/ && tolower($0) ~ /discovery|pkt-line|packet-line/ {f=1} END {exit f?0:1}' docs/error-codes.md || { echo "error-codes LBR-NET-002 row does not cover pkt-line discovery semantics"; exit 1; }
  echo "OK: push discovery docs synced"
  ```

**Full-suite trigger:** `N/A（family child：C 覆盖继承 PKT-05）`

**Dependencies:** `PKT-02`。

**Deliverables:** N/A（代码卡）。

**Implementation write set:** `src/command/push.rs`（含 `#[cfg(test)]`）、`docs/commands/push.md`、`docs/commands/zh-CN/push.md`、`docs/development/commands/push.md`（push discovery 归类变化随本卡发布同步，R32/R33 Codex P1-3 同卡闭环）

**Release write set:** N/A（family child）

**Files likely touched:** `src/command/push.rs`, `docs/commands/push.md`, `docs/commands/zh-CN/push.md`, `docs/development/commands/push.md`

**Docs and compatibility impact:** push discovery 协议错误归类 `LBR-NET-002` 变化随本卡同步 `docs/commands/push.md`（EN+zh）、`docs/development/commands/push.md` 并核对 `docs/error-codes.md` `LBR-NET-002` 语义行（R32/R33 Codex P1-3）

**Rollback mode:** `immutable-release`

**Migration and rollback:** N/A（随家族发布点前滚）

**Security and privacy:** fail-closed；协议/网络错误分类互不污染；零回显

**Performance budget:** O(1)；继承 GC-10

**Estimated scope:** `M`（1 落点 / 1 生产文件；边界归类行为变化）

**Version increment:** `N/A`（family child）

**Release boundary:** `family child of REL-PKT-A`

**C/D coverage from:** `PKT-05`

**Granularity:** `type=implementation; axis=push 调用方传播与 Protocol 变体; recovery=随家族发布点前滚修复，降级=回装上一 tag; complete=yes; self-contained=yes; AC=8/8; VER=3/8; landing=1; prod-files=1; scope=M; deps=PKT-02; writeset=no-overlap（家族内与 02/03 不相交）; release=family child of REL-PKT-A; split-from=PKT-02@R2; exception=N/A`

### Task PKT-05: 家族 REL-PKT-A 聚合发布点（v0.22.12）

**Task type:** `release`

**Lifecycle / Acceptance:** `pending` / 空

**Description:** 家族 REL-PKT-A（PKT-02/03/04）的唯一发布点（G-08，staging 流程见 `REL-PKT-A`）：在家族树上执行 ER-04 C 组全套——版本面五处 `0.22.12` parity 与 bump、`Cargo.lock` 工具链刷新、fmt/clippy、全量收口测试（T-2）、`cargo build --release`、安装、`libra add` + `libra commit -s -m`、`libra push origin main`、`gh release create -R libra-tools/libra v0.22.12`、D 组 8 job 记录取证（含 4 个 build leg，见「D 组逐 job 收口循环」）。并承载 release 基础设施收口：`release.yml` 的 `verify-homebrew-formula` 吞错面 fail-closed 化（brew tap/install/版本校验失败路径返回非零，**且**成功路径上传 `homebrew-verify-pass` sentinel artifact；R31/R32 Codex P1-2/P1-4）——该收口随本卡发布（v0.22.12）生效，D 组自 v0.22.12 起断言 sentinel artifact。子卡随本卡 C/D 覆盖转为 `done/complete`。

**Out of scope:** 产品行为变更（由子卡承载；release 基础设施 fail-closed 收口除外——`release.yml` `verify-homebrew-formula` 收口由本卡承载，见 Description/AC，R34 Codex P1-2）；PKT-06..14 的发布。

**Current evidence:**

| 事实 | 证据 |
|---|---|
| 家族三子卡 `Acceptance=locally-accepted`（家族树 A/B 门已过） | 本计划任务卡状态（**进入条件，执行期核对**：当前全部 `pending`，三子卡经家族树 A/B 门后方转 `locally-accepted`——本卡 C 组不得在该条件成立前启动，R26 Codex P1-2） |
| 版本面五处权威 | `tests/compat/version_surface_sync.rs` |
| release.yml 触发面 | `.github/workflows/release.yml`（`on.push.tags: v*`） |

**Acceptance criteria:**

- [ ] PKT-02、PKT-03、PKT-04 均 `Acceptance=locally-accepted`（家族树 A/B 门已过）且逐卡 ER-05 review PASS 有据（R37 Codex P1-4），其写集全部并入发布树。
- [ ] 版本面五处 parity 预检一致并 bump 至 `0.22.12`。
- [ ] `Cargo.lock` 仅由工具链刷新（无手改）。
- [ ] `cargo +nightly fmt --all --check` 与 `cargo clippy --all-targets --all-features -- -D warnings` 通过。
- [ ] `source .env.test && cargo test --all` 全绿（家族命中 ER-13 T-2）。
- [ ] `cargo build --release` + 安装成功。
- [ ] `release.yml` `verify-homebrew-formula` 吞错面收口：brew tap/install/版本校验失败路径返回非零，**且**成功路径上传 `homebrew-verify-pass` sentinel artifact（两分支同时成立；D 组自 v0.22.12 起断言 sentinel，R31/R32 Codex P1-2/P1-4）。
- [ ] `libra push origin main` → `libra tag "v0.22.12"` → `libra push origin "v0.22.12"`（触发 release.yml）→ `gh release create -R libra-tools/libra "v0.22.12" --verify-tag`。
- [ ] D 组 8 个 job 记录全部绿色（含 4 个 `build-and-upload` matrix leg）。
- [ ] release note / 兼容证据落盘（协议错误归类变化说明）。
- [ ] 家族引入的具名守卫在本发布树全绿，三子卡随之转为 `done/complete`。

**Verification:**

- [ ] `bash -o pipefail -c 'libra cat-file -p HEAD | grep -q "^gpgsig"'`（ER-07；管道失败闭合，R33 Codex P1-1）
- [ ] `homebrew-verify-pass` sentinel artifact 断言（自包含、SHA 绑定与主取证块同口径，R32/R36 Codex P1-6/P1-1；workflow 收口随本卡发布生效）：
  ```bash
  set -o pipefail
  obj=$(gh api "repos/libra-tools/libra/git/ref/tags/v0.22.12") || { echo "TAG REF MISSING for v0.22.12"; exit 1; }
  sha=$(printf '%s' "$obj" | jq -r '.object.sha') || { echo "TAG SHA PARSE FAILED for v0.22.12"; exit 1; }
  typ=$(printf '%s' "$obj" | jq -r '.object.type') || { echo "TAG TYPE PARSE FAILED for v0.22.12"; exit 1; }
  if [ "$typ" = "tag" ]; then sha=$(gh api "repos/libra-tools/libra/git/tags/$sha" --jq '.object.sha') || { echo "TAG PEEL FAILED for v0.22.12"; exit 1; }; fi
  case "$sha" in ????????????????????????????????????????) ;; *) echo "BAD TAG COMMIT SHA for v0.22.12: $sha"; exit 1;; esac
  run_id=$(gh run list -R libra-tools/libra --workflow=release.yml --event push --json databaseId,headSha,headBranch --limit 50 --jq "[.[] | select(.headSha==\"$sha\" and .headBranch==\"v0.22.12\")][0].databaseId // empty")
  [ -n "$run_id" ] || { echo "NO RUN for v0.22.12 (tag commit $sha)"; exit 1; }
  gh run watch -R libra-tools/libra "$run_id" --exit-status || { echo "RUN v0.22.12 conclusion != success"; exit 1; }
  vtmpd=$(mktemp -d) || { echo "MKTEMP FAILED for v0.22.12"; exit 1; }
  gh run download -R libra-tools/libra "$run_id" -n homebrew-verify-pass -D "$vtmpd" || { echo "HOMEBREW VERIFY PASS ARTIFACT MISSING for v0.22.12 (verify-homebrew-formula success path not proven)"; rm -rf "$vtmpd"; exit 1; }
  rm -rf "$vtmpd"; echo "OK: v0.22.12 homebrew-verify-pass sentinel present (tag commit $sha)"
  ```
- [ ] v0.22.12 run 取证（event=push 显式锁定 + 8/8 job 全绿断言 + homebrew 正向证明，R23 Codex P1-12 命令化、R24 Codex P1-6/P1-7/P1-8 实况修正）：
  ```bash
  set -o pipefail
  obj=$(gh api "repos/libra-tools/libra/git/ref/tags/v0.22.12") || { echo "TAG REF MISSING for v0.22.12"; exit 1; }
  sha=$(printf '%s' "$obj" | jq -r '.object.sha') || { echo "TAG SHA PARSE FAILED for v0.22.12"; exit 1; }
  typ=$(printf '%s' "$obj" | jq -r '.object.type') || { echo "TAG TYPE PARSE FAILED for v0.22.12"; exit 1; }
  if [ "$typ" = "tag" ]; then sha=$(gh api "repos/libra-tools/libra/git/tags/$sha" --jq '.object.sha') || { echo "TAG PEEL FAILED for v0.22.12"; exit 1; }; fi
  case "$sha" in ????????????????????????????????????????) ;; *) echo "BAD TAG COMMIT SHA for v0.22.12: $sha"; exit 1;; esac
  run_id=$(gh run list -R libra-tools/libra --workflow=release.yml --event push --json databaseId,headSha,headBranch --limit 50 --jq "[.[] | select(.headSha==\"$sha\" and .headBranch==\"v0.22.12\")][0].databaseId // empty")
  [ -n "$run_id" ] || { echo "NO RUN for v0.22.12 (tag commit $sha)"; exit 1; }
  rb=$(gh run view -R libra-tools/libra "$run_id" --json headBranch,headSha --jq '.headBranch + "@" + .headSha')
  case "$rb" in "v0.22.12@$sha") ;; *) echo "REF MISMATCH for v0.22.12: run bound to $rb (want v0.22.12@$sha)"; exit 1;; esac
  gh run watch -R libra-tools/libra "$run_id" --exit-status || { echo "RUN v0.22.12 conclusion != success"; exit 1; }
  names=$(gh run view -R libra-tools/libra "$run_id" --json jobs --jq '.jobs[].name' | tr '\n' ' ') || { echo "JOB NAME FETCH FAILED for v0.22.12"; exit 1; }
  [ "$(gh run view -R libra-tools/libra "$run_id" --json jobs --jq '.jobs | length')" -eq 8 ] || { echo "TOTAL JOBS != 8 for v0.22.12"; exit 1; }
  [ "$(gh run view -R libra-tools/libra "$run_id" --json jobs --jq '[.jobs[] | select(.conclusion=="success")] | length')" -eq 8 ] || { echo "JOBS NOT ALL GREEN for v0.22.12"; exit 1; }
  [ "$(gh run view -R libra-tools/libra "$run_id" --json jobs --jq '[.jobs[] | select(.name | startswith("build-and-upload"))] | length')" -eq 4 ] || { echo "BUILD LEGS != 4 for v0.22.12"; exit 1; }
  for j in upload-install-scripts update-homebrew-tap verify-homebrew-formula request-stable-manifest; do case " $names " in *" $j "*) ;; *) echo "MISSING DOWNSTREAM JOB $j for v0.22.12"; exit 1;; esac; done
  tmpd=$(mktemp -d) || { echo "MKTEMP FAILED for v0.22.12"; exit 1; }
  gh run download -R libra-tools/libra "$run_id" -n formula-commit-sha -D "$tmpd" || { echo "FORMULA-SHA ARTIFACT MISSING for v0.22.12"; rm -rf "$tmpd"; exit 1; }
  fsha=$(tr -d '[:space:]' < "$tmpd/formula-commit-sha.txt")
  rm -rf "$tmpd"
  [ ${#fsha} -eq 40 ] || { echo "BAD FORMULA SHA for v0.22.12: $fsha"; exit 1; }
  formula=$(curl -fsSL "https://raw.githubusercontent.com/libra-tools/homebrew-libra/${fsha}/Formula/libra.rb") || { echo "TAP FORMULA FETCH FAILED for v0.22.12"; exit 1; }
  case "$formula" in *"version \"0.22.12\""*) ;; *) echo "TAP FORMULA NOT AT 0.22.12 for v0.22.12"; exit 1;; esac
  vtmpd=$(mktemp -d) || { echo "MKTEMP FAILED for v0.22.12"; exit 1; }
  gh run download -R libra-tools/libra "$run_id" -n homebrew-verify-pass -D "$vtmpd" || { echo "HOMEBREW VERIFY PASS ARTIFACT MISSING for v0.22.12"; rm -rf "$vtmpd"; exit 1; }
  rm -rf "$vtmpd"
  echo "OK: v0.22.12 run=$run_id tag_commit=$sha 8/8 jobs success + tap formula positive proof"
  ```
- [ ] 手工证据（sanitized）：`download.libra.tools` 可获取 v0.22.12 artifact

**Full-suite trigger:** `T-2: Task type=release（家族发布点），C 组第 ④ 步跑全量`

**Dependencies:** `PKT-02`、`PKT-03`、`PKT-04`。

**Deliverables:** N/A（release 卡）。

**Implementation write set:** `.github/workflows/release.yml`（`verify-homebrew-formula` fail-closed 收口：失败路径返回非零且成功路径上传 `homebrew-verify-pass` sentinel artifact，R34 Codex P1-2 纳入 G-10 管理）

**Release write set:** Inherited（= ER-08 版本面五处 + `Cargo.lock` 工具链刷新 + release artifact）

**Files likely touched:** `Cargo.toml`, `web/package.json`, `worker/package.json`, `install.sh`, `install.ps1`, `Cargo.lock`, `.github/workflows/release.yml`

**Docs and compatibility impact:** release note / 兼容证据（协议错误归类变化）

**Rollback mode:** `immutable-release`

**Migration and rollback:** N/A（恢复=发布修复版本，降级=回装上一 tag）

**Security and privacy:** Homebrew/tap 验证失败必须使发布变红，成功须实际上传 sentinel；无新增产品行为，但明确修改 CI fail-closed 行为。

**Performance budget:** CI 验证有界；沿用各步骤超时，不引入无限重试或额外本地全量 Web 构建。

**Estimated scope:** `M`（发布点；按 ER-08 全套动作计）

**Version increment:** `patch`（`0.22.11` → `0.22.12`；家族子卡不 bump）

**Release boundary:** `family release point of REL-PKT-A`

**C/D coverage from:** `self`

**Granularity:** `type=release; axis=家族 A 聚合发布; recovery=前滚修复发新版本，降级=回装上一 tag; complete=yes; self-contained=yes; AC=11/12; VER=4/12; landing=1; prod-files=1; scope=M; deps=PKT-02,PKT-03,PKT-04; writeset=序列化于 REL-PKT-A 窗口; release=family release point of REL-PKT-A; split-from=N/A（G-08 新建发布点）; exception=N/A`

### Task PKT-06: fetch discovery 边界 marker 适配（LBR-NET-002 归类）

**Task type:** `implementation`

**Lifecycle / Acceptance:** `pending` / 空

**Description:** 自 PKT-03@R2 拆出。在 `map_fetch_discovery_error`（`:943-957`）`NetworkError(_)` 分支内增加 marker 适配：携带 `PKT_LINE_PROTOCOL_ERROR_PREFIX` 时映射 `StableErrorCode::NetworkProtocol`（`LBR-NET-002`），其余维持 `NetworkUnavailable`（`LBR-NET-001`）。唯一行为轴：fetch discovery 边界协议错误归类。

**Out of scope:** fetch 异步/`read_hex_4`：PKT-07/13；clone/ls-remote/FetchObjects：PKT-10；push：PKT-04/09。

**Current evidence:**

| 事实 | 证据 |
|---|---|
| `NetworkError(_) → NetworkUnavailable` 显式分支（含 with_hint 收尾）先于分类器 | `src/command/fetch.rs:948-950` |
| `_ => NetworkProtocol` 兜底存在（:955 单行；:953-954 为 IOError 分支尾部） | `src/command/fetch.rs:955` |
| PKT-03 已保证 marker 前缀进入文本 | `src/internal/protocol/mod.rs:92`（PKT-03 产物） |

**Acceptance criteria:**

- [ ] `map_fetch_discovery_error` 对携带 marker 前缀的 `GitError::NetworkError` 映射 `StableErrorCode::NetworkProtocol`（`LBR-NET-002`）。
- [ ] 不携带前缀的 `GitError::NetworkError` 维持 `NetworkUnavailable`（`LBR-NET-001`）回归。
- [ ] fetch 稳定码锚定测试通过（显式映射层，marker/非 marker 双向）。
- [ ] fetch 错误文本零回显 sentinel 测试通过。
- [ ] 分类器兜底锚定测试通过（`PktLineError` 文本命中 `is_network_protocol_error`）。

**Verification:**

- [ ] 失败闭合存在性 + 执行（内联可执行，fn 全集 `(new)`）：
  ```bash
  expected="pkt_line_fetch_marker_maps_to_lbr_net_002 pkt_line_fetch_non_marker_stays_net_001 pkt_line_fetch_zero_echo_sentinel pkt_line_fetch_classifier_fallback_anchored"
  missing=$(bash -c 'set -o pipefail; list=$(source .env.test && cargo test --lib -- --list 2>/dev/null | sed "s/: test\$//" | awk -F"::" "{print \$NF}" | sort) || { echo "TEST LIST PIPELINE FAILED" >&2; exit 1; }; comm -23 <(printf "%s\n" $1 | sort) <(printf "%s\n" "$list")' _ "$expected") || { echo "FN EXISTENCE CHECK FAILED (pipeline error)"; exit 1; }
  [ -z "$missing" ] || { echo "MISSING FNS: $missing"; exit 1; }
  source .env.test && cargo test --lib pkt_line_fetch_
  ```
- [ ] `source .env.test && cargo test --test compat_all_production_unwrap_guard`
- [ ] 文档检查（fetch discovery 归类变化随本卡发布同步，R32/R33 Codex P1-3/P1-5）：
  ```bash
  for f in docs/commands/fetch.md docs/commands/zh-CN/fetch.md docs/development/commands/fetch.md; do awk 'tolower($0) ~ /lbr-net-002/ && tolower($0) ~ /discovery|pkt-line|packet-line/ {f=1} END {exit f?0:1}' "$f" || { echo "MISSING semantic line (LBR-NET-002 + discovery/pkt-line): $f"; exit 1; }; done
  echo "OK: fetch discovery docs synced"
  ```
- [ ] 零命中守卫（三分支，内联可执行）（本卡新增错误构造，无远端字节插值）：
  ```bash
  if rg -n 'pkt-line protocol error: \{' src/command/fetch.rs; then echo "FAIL: forbidden pattern found"; exit 1; else rc=$?; if [ "$rc" -ne 1 ]; then echo "ERROR: rg failed with exit $rc"; exit "$rc"; fi; echo "OK: zero hits"; fi
  ```

**Full-suite trigger:** `T-1: 触及 fetch 全局错误映射与 GC-02 marker 契约消费，执行阶段跑全量`

**Dependencies:** `PKT-05`。

**Deliverables:** N/A（代码卡）。

**Implementation write set:** `src/command/fetch.rs`（含 `#[cfg(test)]`）、`docs/commands/fetch.md`、`docs/commands/zh-CN/fetch.md`、`docs/development/commands/fetch.md`（R32/R33 Codex P1-3 同卡闭环）

**Release write set:** Inherited（= ER-08 版本面五处 + `Cargo.lock` 工具链刷新 + release artifact）

**Files likely touched:** `src/command/fetch.rs`, `docs/commands/fetch.md`, `docs/commands/zh-CN/fetch.md`, `docs/development/commands/fetch.md`

**Docs and compatibility impact:** fetch discovery 协议错误归类 `LBR-NET-002` 变化随本卡同步 `docs/commands/fetch.md`（EN+zh）；ls-remote/clone/pull 面由 PKT-10 承载（R31/R32 Codex P1-3 分工）

**Rollback mode:** `immutable-release`

**Migration and rollback:** N/A（恢复=发布修复版本，降级=回装上一 `v*` tag）

**Security and privacy:** 协议/网络错误分类互不污染；零回显

**Performance budget:** O(前缀长) 常数；继承 GC-10

**Estimated scope:** `M`（1 落点 / 1 生产文件；边界归类行为变化）

**Version increment:** `patch`（`0.22.12` → `0.22.13`，版本面五处同步 + 工具链刷新 `Cargo.lock` + `gh release create -R libra-tools/libra v0.22.13`）

**Release boundary:** `independent`（完整 C 组 + D 组取证）

**C/D coverage from:** `self`

**Granularity:** `type=implementation; axis=fetch discovery 边界归类; recovery=发布修复版本（immutable-release），降级=回装上一 tag; complete=yes; self-contained=yes; AC=5/8; VER=4/8; landing=1; prod-files=1; scope=M; deps=PKT-05; writeset=序列化于 PKT-07（同文件 fetch.rs/fetch.md）；与 PKT-09 不相交; release=independent; split-from=PKT-03@R2; exception=N/A`

### Task PKT-07: fetch 异步读取下界与零回显

**Task type:** `implementation`

**Lifecycle / Acceptance:** `pending` / 空

**Description:** 自 PKT-03@R2 拆出。在 `src/command/fetch.rs` 异步 `read_pkt_line`（`:3672-3680`）消费 PKT-01 helper（`len∈1..3` → `Err`，消除 u32 回绕 ~4 GiB 级分配路径）。`read_hex_4`（`:3650-3668`）错误文本 marker 化、零回显与跨传输 helper 收口由 PKT-13 承接（本卡先行发布，不验收该面）。唯一行为轴：fetch 异步读取帧长 fail-closed。

**Out of scope:** `map_fetch_discovery_error`：PKT-06；`read_hex_4` 文本：PKT-13；`FetchObjects` 分支：PKT-10；git/ssh 客户端：PKT-08。

**Current evidence:**

| 事实 | 证据 |
|---|---|
| `(len - 4) as usize` 回绕 → ~4 GiB 分配 + EOF/超时 | `src/command/fetch.rs:3677`（`read_hex_4` 返回 `u32`，:3650） |
| 调用方 `:2349` 已有 `Err` 分支可承接 | `src/command/fetch.rs:2349-2352` |

**Acceptance criteria:**

- [ ] fetch 异步 `read_pkt_line` 在 `len ∈ 1..3` 返回 `Err`，回绕大分配路径消除；错误必须保留 PKT-01 `PktFrameError` 的 marker-bearing Display，经 IO carrier 与消费点映射 `LBR-NET-002`。既有具名 fn 同时断言原始 carrier marker 与最终稳定码，保证 PKT-10 收窄为 marker-only 分类后仍成立（R42 F1）。
- [ ] flush `0000` 在异步路径行为不变。
- [ ] `len == 4` 空 payload 在异步路径行为不变。
- [ ] `0xffff` 上限帧在异步路径行为不变。
- [ ] `reach_pack` 后的截断 header/payload `UnexpectedEof` 不再被当作正常结束：区分「帧边界 EOF」与「部分 header/payload EOF」，后者用 PKT-02 已有 `PktLineError` 的对应截断原因产生 marker-bearing IO carrier，返回 `LBR-NET-002`，既有「完整 pack 后截断 pkt-line」回归同时断言 carrier marker 与稳定码（R42 F1；不得伪装成长度下界错误，也不得延至 PKT-13 才加 marker）。
- [ ] fetch 异步读取点消费 PKT-01 helper（单点消费；跨传输 helper 收口与零回显由 PKT-13 承接，R9 Codex P1-1）。

**Verification:**

- [ ] 失败闭合存在性 + 执行（内联可执行，fn 全集 `(new)`）：
  ```bash
  expected="pkt_line_fetch_async_rejects_len_below_four pkt_line_fetch_async_flush_regression pkt_line_fetch_async_len4_regression pkt_line_fetch_async_upper_bound_regression pkt_line_fetch_async_reach_pack_truncated_eof pkt_line_fetch_async_helper_single_source"
  missing=$(bash -c 'set -o pipefail; list=$(source .env.test && cargo test --lib -- --list 2>/dev/null | sed "s/: test\$//" | awk -F"::" "{print \$NF}" | sort) || { echo "TEST LIST PIPELINE FAILED" >&2; exit 1; }; comm -23 <(printf "%s\n" $1 | sort) <(printf "%s\n" "$list")' _ "$expected") || { echo "FN EXISTENCE CHECK FAILED (pipeline error)"; exit 1; }
  [ -z "$missing" ] || { echo "MISSING FNS: $missing"; exit 1; }
  source .env.test && cargo test --lib pkt_line_fetch_async_
  ```
- [ ] 零命中守卫（三分支，内联可执行）：
  ```bash
  if rg -n 'len - 4' src/command/fetch.rs; then echo "FAIL: forbidden pattern found"; exit 1; else rc=$?; if [ "$rc" -ne 1 ]; then echo "ERROR: rg failed with exit $rc"; exit "$rc"; fi; echo "OK: zero hits"; fi
  ```
- [ ] `source .env.test && cargo test --test compat_all_production_unwrap_guard`
- [ ] 文档检查（异步截断 EOF 语义随本卡发布同步，R32/R33 Codex P1-3/P1-5）：
  ```bash
  for f in docs/commands/fetch.md docs/commands/zh-CN/fetch.md docs/development/commands/fetch.md; do awk 'tolower($0) ~ /lbr-net-002/ && tolower($0) ~ /truncat|截断|eof/ {f=1} END {exit f?0:1}' "$f" || { echo "MISSING semantic line (LBR-NET-002 + truncation EOF): $f"; exit 1; }; done
  echo "OK: fetch async EOF docs synced"
  ```

**Full-suite trigger:** `T-1: 消费 GC-02 共享 helper 于网络热路径，执行阶段跑全量`

**Dependencies:** `PKT-06`（同文件 `fetch.rs`，I–I 序列化）。

**Deliverables:** N/A（代码卡）。

**Implementation write set:** `src/command/fetch.rs`（含 `#[cfg(test)]`）、`docs/commands/fetch.md`、`docs/commands/zh-CN/fetch.md`、`docs/development/commands/fetch.md`（R32/R33 Codex P1-3 同卡闭环）

**Release write set:** Inherited（= ER-08 版本面五处 + `Cargo.lock` 工具链刷新 + release artifact）

**Files likely touched:** `src/command/fetch.rs`, `docs/commands/fetch.md`, `docs/commands/zh-CN/fetch.md`, `docs/development/commands/fetch.md`

**Docs and compatibility impact:** fetch 异步截断 EOF 语义（映射 `LBR-NET-002`）随本卡同步 `docs/commands/fetch.md`（EN+zh）与 `docs/development/commands/fetch.md`（R32/R33 Codex P1-3）

**Rollback mode:** `immutable-release`

**Migration and rollback:** N/A（恢复=发布修复版本，降级=回装上一 `v*` tag）

**Security and privacy:** fail-closed；零回显

**Performance budget:** O(1)；消除 ~4 GiB 误分配；继承 GC-10

**Estimated scope:** `M`（1 落点 / 1 生产文件）

**Version increment:** `patch`（`0.22.13` → `0.22.14`，版本面五处同步 + 工具链刷新 `Cargo.lock` + `gh release create -R libra-tools/libra v0.22.14`）

**Release boundary:** `independent`（完整 C 组 + D 组取证）

**C/D coverage from:** `self`

**Granularity:** `type=implementation; axis=fetch 异步下界与 EOF 语义（单点 helper 消费）; recovery=发布修复版本（immutable-release），降级=回装上一 tag; complete=yes; self-contained=yes; AC=6/8; VER=4/8; landing=1; prod-files=1; scope=M; deps=PKT-06; writeset=序列化于 PKT-06（同文件 fetch.rs/fetch.md）; release=independent; split-from=PKT-03@R2; exception=N/A`

### Task PKT-08: git/ssh 读取下界（客户端×帧类）

**Task type:** `implementation`

**Lifecycle / Acceptance:** `pending` / 空

**Description:** 自 PKT-03@R1（异步循环轴）拆出。在 `git_client.rs:151`、`ssh_client.rs:243` 消费 PKT-01 helper（`len∈1..3` → `Err`），合法帧（flush/空 payload/上限）在两客户端回归。头解析文本 marker 化由 PKT-13 承接；pkt-line 错误路径 stderr 脱敏由 PKT-12 承接。唯一行为轴：git/ssh 客户端读取帧长 fail-closed。

**Out of scope:** 头解析文本：PKT-13；stderr 脱敏：PKT-12/11；CLI 边界：PKT-06/09/10。

**Current evidence:**

| 事实 | 证据 |
|---|---|
| `vec![0u8; len - 4]` 无下界（回绕 → 确定性 panic） | `src/internal/protocol/git_client.rs:151`、`src/internal/protocol/ssh_client.rs:243` |

**Acceptance criteria:**

- [ ] `git_client.rs` 读取循环在 `len ∈ 1..3` 返回 `Err`（经 helper），fn 在读取循环边界断言 typed frame-read error 的 marker 原始载荷，并经公开 CliError 分类路径断言 `LBR-NET-002`（不冒充尚待包装透传的真实 CLI）（R28 Codex P1-2）。
- [ ] `ssh_client.rs` 读取循环在 `len ∈ 1..3` 返回 `Err`（经 helper），fn 在读取循环边界断言 typed frame-read error 的 marker 原始载荷，并经公开 CliError 分类路径断言 `LBR-NET-002`（不冒充尚待包装透传的真实 CLI）（R28 Codex P1-2）。
- [ ] flush `0000` 在 `git_client.rs` 行为不变。
- [ ] flush `0000` 在 `ssh_client.rs` 行为不变。
- [ ] `len == 4` 空 payload 在 `git_client.rs` 行为不变。
- [ ] `len == 4` 空 payload 在 `ssh_client.rs` 行为不变。
- [ ] `0xffff` 上限帧在 `git_client.rs` 行为不变。
- [ ] git/ssh 合法长度声明后 payload 截断 EOF 在读取循环边界经共享 typed frame-read error 返回 marker 错误，公开 CliError 分类路径为 `LBR-NET-002`（协议 EOF 与 transport timeout/其它 IO 分离：EOF→`NetworkProtocol`，超时与其它传输错误保留 `NetworkUnavailable`；R17/R18 Codex P1-5）。
- [ ] git/ssh 实际只收到 1–3 字节 header 即 EOF（partial header）在读取循环边界经共享 typed frame-read error 返回 marker 错误，公开 CliError 分类路径为 `LBR-NET-002`（单 fn 覆盖双客户端子断言，R25 Codex P1-4）。
- [ ] `0xffff` 上限帧在 `ssh_client.rs` 行为不变。

**阶段边界：** 共享 typed frame-read error 位于 `src/git_protocol.rs`，普通 IO 保留自身原因；SSH / git 外层包装尚未透传时不得宣称真实 CLI 已全为 NET-002。对应真实帧长1..3、partial-header EOF、payload EOF 的端到端测试分别归 PKT-12 / PKT-13；本卡文档须准确披露阶段状态，不提前承诺后继版本行为。

**Verification:**

- [ ] 失败闭合存在性 + 执行（内联可执行，fn 全集 `(new)`）：
  ```bash
  expected="pkt_line_client_git_rejects_len_below_four pkt_line_client_ssh_rejects_len_below_four pkt_line_client_git_flush_regression pkt_line_client_ssh_flush_regression pkt_line_client_git_len4_regression pkt_line_client_ssh_len4_regression pkt_line_client_git_upper_bound_regression pkt_line_client_ssh_upper_bound_regression pkt_line_client_truncated_payload_eof_net_002 pkt_line_client_partial_header_eof_net_002"
  missing=$(bash -c 'set -o pipefail; list=$(source .env.test && cargo test --lib -- --list 2>/dev/null | sed "s/: test\$//" | awk -F"::" "{print \$NF}" | sort) || { echo "TEST LIST PIPELINE FAILED" >&2; exit 1; }; comm -23 <(printf "%s\n" $1 | sort) <(printf "%s\n" "$list")' _ "$expected") || { echo "FN EXISTENCE CHECK FAILED (pipeline error)"; exit 1; }
  [ -z "$missing" ] || { echo "MISSING FNS: $missing"; exit 1; }
  source .env.test && cargo test --lib pkt_line_client_
  ```
- [ ] 零命中守卫（三分支，内联可执行）：
  ```bash
  if rg -n 'len - 4' src/internal/protocol/git_client.rs src/internal/protocol/ssh_client.rs; then echo "FAIL: forbidden pattern found"; exit 1; else rc=$?; if [ "$rc" -ne 1 ]; then echo "ERROR: rg failed with exit $rc"; exit "$rc"; fi; echo "OK: zero hits"; fi
  ```
- [ ] `source .env.test && cargo test --test compat_all_production_unwrap_guard`
- [ ] 文档检查（git/ssh pkt-line fail-closed 语义随本卡发布同步，R32/R33 Codex P1-3/P1-5）：
  ```bash
  for f in docs/commands/clone.md docs/commands/zh-CN/clone.md docs/commands/fetch.md docs/commands/zh-CN/fetch.md docs/development/commands/clone.md docs/development/commands/fetch.md; do awk 'tolower($0) ~ /lbr-net-002/ && tolower($0) ~ /pkt-line|packet-line/ {f=1} END {exit f?0:1}' "$f" || { echo "MISSING semantic line (LBR-NET-002 + pkt-line): $f"; exit 1; }; done
  echo "OK: git/ssh transport docs synced"
  ```

**判据规范（非计数正文，EX-05 门族枚举；R19 Claude P0-2）：**

1. git len∈1..3 拒绝；2. ssh len∈1..3 拒绝；3-4. flush 双客户端回归；5-6. len==4 双客户端回归；7-8. 0xffff 双客户端回归；9. typed payload-EOF 分类（git/ssh 稳定码断言）；10. partial-header EOF 分类（`pkt_line_client_partial_header_eof_net_002`，git/ssh 双子断言，R25 Codex P1-4）。门→命令：`source .env.test && cargo test --lib pkt_line_client_`（覆盖 1-10）。

**Full-suite trigger:** `T-1: 新增共享 helper 消费点并触及共享协议客户端模块，执行阶段跑全量`

**Dependencies:** `PKT-05`、`PKT-10`（clone/fetch.md 文档面序列化，R35 Codex P1-2）。

**Deliverables:** N/A（代码卡）。

**Implementation write set:** `src/git_protocol.rs`（共享 typed error）、`src/internal/protocol/git_client.rs`、`src/internal/protocol/ssh_client.rs`（含 `#[cfg(test)]`）、`docs/commands/{clone,fetch}.md`（EN+zh）、`docs/development/commands/{clone,fetch}.md`（R32/R33 Codex P1-3 同卡闭环）

**Release write set:** Inherited（= ER-08 版本面五处 + `Cargo.lock` 工具链刷新 + release artifact）

**Files likely touched:** `src/git_protocol.rs`, `src/internal/protocol/git_client.rs`, `src/internal/protocol/ssh_client.rs`, `docs/commands/{clone,fetch}.md`（EN+zh）, `docs/development/commands/{clone,fetch}.md`

**Docs and compatibility impact:** git/ssh pkt-line fail-closed 语义（`LBR-NET-002`）随本卡同步 `docs/commands/{clone,fetch}.md`（EN+zh）与 `docs/development/commands/{clone,fetch}.md`（R32/R33 Codex P1-3）

**Rollback mode:** `immutable-release`

**Migration and rollback:** N/A（恢复=发布修复版本，降级=回装上一 `v*` tag）

**Security and privacy:** fail-closed

**Performance budget:** O(1)；继承 GC-10

**Estimated scope:** `M`（3 落点 / 3 生产文件）

**Version increment:** `patch`（`0.22.16` → `0.22.17`，版本面五处同步 + 工具链刷新 `Cargo.lock` + `gh release create -R libra-tools/libra v0.22.17`）

**Release boundary:** `independent`（完整 C 组 + D 组取证）

**C/D coverage from:** `self`

**Granularity:** `type=implementation; axis=git/ssh 读取下界与 payload 截断 EOF（门族）; recovery=发布修复版本（immutable-release），降级=回装上一 tag; complete=yes; self-contained=yes; AC=10/8@EX-05; VER=4/8; landing=3; prod-files=3; scope=M; deps=PKT-05,PKT-10; writeset=clone/fetch.md 文档面序列化于 PKT-10；共享 git_protocol.rs 经 PKT-01/02 串行；代码面与 PKT-06/07/09 不相交; release=independent; split-from=PKT-03@R1（异步循环轴）; exception=EX-05`

### Task PKT-09: push 状态行归类（`PushError::Protocol`）与回显修复

**Task type:** `implementation`

**Lifecycle / Acceptance:** `pending` / 空

**Description:** 自 PKT-04@R5 拆出。`:2149-2151` 的「既非 `ok ` 亦非 `ng ` 前缀」状态行错误由 `PushError::Network`（`LBR-NET-001`）**重新归类为 `PushError::Protocol`**（marker 固定短语，与同函数 `RemoteUnpackFailed`/`RemoteRefUpdateFailed` 的 `LBR-NET-002` 归类一致，消除「重试可修复」的错误引导）；`:3434-3443` 锁定测试同步更新；补 receive-pack 畸形端到端稳定码与零回显测试。唯一行为轴：push 状态行错误归类与文本卫生。

**Out of scope:** `ng` reason 透传：ADR-PKT-02 维持现状；`Protocol` 变体与传播：PKT-04（家族）。

**Current evidence:**

| 事实 | 证据 |
|---|---|
| 状态行走 `PushError::Network`（NET-001），同函数兄弟分支为 NET-002 | `src/command/push.rs:2149-2151`、`:398-403` |
| 既有测试锁定逐字回显 | `src/command/push.rs:3434-3443` |

**Acceptance criteria:**

- [ ] `:2149-2151` 状态行错误改为 `PushError::Protocol`（marker 固定短语，不再插入 `{line}`）。
- [ ] `:3434-3443` 锁定测试同步更新为新归类与新短语断言并通过。
- [ ] receive-pack 畸形/截断 pkt-line 端到端测试通过（稳定码 `LBR-NET-002`）。
- [ ] push 状态行错误文本零回显 sentinel 测试通过（恶意状态行字节不出现）。
- [ ] push 对缺失最终 flush `0000` 的截断响应返回 `PushError::Protocol`/`LBR-NET-002`（R22 Codex P1-1）。
（`ng` 输入校验与渲染卫生由 PKT-14 承接，R11 Codex P1-3。）
- [ ] 状态行分支与同函数 `unpack`/`ng` 分支稳定码一致性断言通过（三者均 `LBR-NET-002`）。

**Verification:**

- [ ] 失败闭合存在性 + 执行（内联可执行，fn 全集 `(new)`）：
  ```bash
  expected="pkt_line_push_status_line_is_protocol_variant pkt_line_push_status_line_locked_test_updated pkt_line_push_malformed_receive_pack_end_to_end_lbr_net_002 pkt_line_push_status_line_zero_echo_sentinel"
  missing=$(bash -c 'set -o pipefail; list=$(source .env.test && cargo test --lib -- --list 2>/dev/null | sed "s/: test\$//" | awk -F"::" "{print \$NF}" | sort) || { echo "TEST LIST PIPELINE FAILED" >&2; exit 1; }; comm -23 <(printf "%s\n" $1 | sort) <(printf "%s\n" "$list")' _ "$expected") || { echo "FN EXISTENCE CHECK FAILED (pipeline error)"; exit 1; }
  [ -z "$missing" ] || { echo "MISSING FNS: $missing"; exit 1; }
  source .env.test && cargo test --lib pkt_line_push_
  ```
- [ ] `source .env.test && cargo test --lib validate_receive_pack_response`（**既有回归测试**，push.rs `#[cfg(test)]` 既有 fn，非本卡 `(new)`、不纳入 expected 闭合集与 `(new)` 计数，R26 Codex P1-6 口径登记）
- [ ] `source .env.test && cargo test --test compat_all_production_unwrap_guard`
- [ ] 零命中守卫（三分支，内联可执行）：
  ```bash
  if rg -n 'status line: \{line\}' src/command/push.rs; then echo "FAIL: forbidden pattern found"; exit 1; else rc=$?; if [ "$rc" -ne 1 ]; then echo "ERROR: rg failed with exit $rc"; exit "$rc"; fi; echo "OK: zero hits"; fi
  ```

- [ ] `docs/commands/push.md`（EN+zh）已同步状态行稳定码归类变化（R16 Claude P1-3）。
- [ ] `docs/error-codes.md` `LBR-NET-002` 语义行核对完成（涵盖 receive-pack 状态行）。
  ```bash
  for f in docs/commands/push.md docs/commands/zh-CN/push.md docs/development/commands/push.md; do awk 'tolower($0) ~ /lbr-net-002/ && tolower($0) ~ /status line|状态行|receive-pack/ {f=1} END {exit f?0:1}' "$f" || { echo "MISSING semantic line (LBR-NET-002 + receive-pack status line): $f"; exit 1; }; done
  awk 'tolower($0) ~ /lbr-net-002/ && tolower($0) ~ /status line|状态行|receive-pack/ {f=1} END {exit f?0:1}' docs/error-codes.md || { echo "error-codes LBR-NET-002 row does not cover receive-pack status line"; exit 1; }
  awk 'tolower($0) ~ /lbr-net-002/ && tolower($0) ~ /push|状态行|status line/ {f=1} END {exit f?0:1}' COMPATIBILITY.md || { echo "COMPATIBILITY.md push row does not cover LBR-NET-002"; exit 1; }
  echo "OK: push docs + error-codes + COMPATIBILITY synced"
  ```

  （逐文件语义行断言 + 开发文档与兼容矩阵，R25 Codex P1-5、R33 Codex P1-4/P1-5）

**Full-suite trigger:** `T-1: 触及 push 全局错误映射与共享 marker 契约，执行阶段跑全量`

**Dependencies:** `PKT-05`、`PKT-10`（COMPATIBILITY.md/error-codes.md 文档面序列化，R35 Codex P1-2）。

**Deliverables:** N/A（代码卡）。

**Implementation write set:** `docs/error-codes.md`（status line / receive-pack 语义行，序列化于 PKT-10）、`src/command/push.rs`（含 `#[cfg(test)]`）、`docs/commands/push.md`、`docs/commands/zh-CN/push.md`、`docs/development/commands/push.md`、`COMPATIBILITY.md`（push 状态行归类行，R33 Codex P1-4 纳入）

**Release write set:** Inherited（= ER-08 版本面五处 + `Cargo.lock` 工具链刷新 + release artifact）

**Files likely touched:** `src/command/push.rs`, `docs/commands/push.md`, `docs/commands/zh-CN/push.md`, `docs/development/commands/push.md`, `docs/error-codes.md`, `COMPATIBILITY.md`

**Docs and compatibility impact:** **登记（R16 Claude P1-3 归位；R33/R36 Codex P1-4/P2-1 扩展）**：push 状态行归类 `LBR-NET-001`→`LBR-NET-002` 属错误码语义变化——本卡负责同步 `docs/commands/push.md`（EN+zh）、`docs/development/commands/push.md`、`COMPATIBILITY.md`（push 状态行归类行）与核对 `docs/error-codes.md` `LBR-NET-002` 语义行（见 Verification 文档检查）

**Rollback mode:** `immutable-release`

**Migration and rollback:** N/A（恢复=发布修复版本，降级=回装上一 `v*` tag）

**Security and privacy:** 零回显；`ng` reason 豁免见 ADR-PKT-02

**Performance budget:** O(1)；继承 GC-10

**Estimated scope:** `M`（1 落点 / 1 生产文件）

**Version increment:** `patch`（`0.22.15` → `0.22.16`，版本面五处同步 + 工具链刷新 `Cargo.lock` + `gh release create -R libra-tools/libra v0.22.16`）

**Release boundary:** `independent`（完整 C 组 + D 组取证）

**C/D coverage from:** `self`

**Granularity:** `type=implementation; axis=push 状态行归类与回显修复; recovery=发布修复版本（immutable-release），降级=回装上一 tag; complete=yes; self-contained=yes; AC=6/8; VER=6/8; landing=1; prod-files=1; scope=M; deps=PKT-05,PKT-10; writeset=COMPATIBILITY.md/error-codes.md 文档面序列化于 PKT-10；push.md 序列化于 PKT-04（经家族 PKT-05）；其余 no-overlap; release=independent; split-from=PKT-04@R5; exception=N/A`

### Task PKT-10: clone / ls-remote / fetch-objects 边界适配矩阵

**Task type:** `implementation`

**Lifecycle / Acceptance:** `pending` / 空

**Description:** 自 PKT-06@R4 拆出。在剩余 CLI 映射点为 marker 错误增加 `NetworkProtocol`（`LBR-NET-002`）适配：①`ls_remote.rs:210-221`（Discovery）；②`clone.rs:1012-1046`（Discovery）；③`clone.rs:1059-1063`（`FetchError::FetchObjects`/`PacketRead`，clone fetch 阶段）；④`fetch.rs:882-887`（`FetchError::FetchObjects`）；⑤-⑥`pull.rs` 的 Discovery 与 FetchObjects 映射（`src/command/pull.rs:267,928-944,989-1001`，R13 Codex P1-3）；⑦fetch/pull `PacketRead` 臂的**非 marker 普通 IO（连接重置等）从既有 `NetworkProtocol` 误归类修正为 `NetworkUnavailable`**——仅 marker 错误（协议形状）落 `LBR-NET-002`，与成功定义「非协议网络错误仍归类 `LBR-NET-001`」及 PKT-08 EOF 分类口径闭合（R29 Codex P1-2）。唯一行为轴：clone/ls-remote/pull/fetch-objects 边界协议错误归类。

**Out of scope:** fetch discovery 分支：PKT-06；push：PKT-04/09；host-key hint 逻辑：维持。

**Current evidence:**

| 事实 | 证据 |
|---|---|
| `ls_remote.rs:210-221`：`NetworkError(_)|IOError(_) → NetworkUnavailable` | `src/command/ls_remote.rs:210-221` |
| `NetworkError(_) → NetworkUnavailable`（含 host-key hint） | `src/command/clone.rs:1012-1043` |
| `FetchObjects`/`PacketRead` 无条件 `NetworkUnavailable` | `src/command/clone.rs:1059-1063` |
| `FetchError::FetchObjects → map_fetch_io_error → NetworkUnavailable` | `src/command/fetch.rs:882-887` |
| pull Discovery 映射把协议形状错误映射 `NetworkUnavailable` | `src/command/pull.rs:267,928-930,989-1001`（R13 Codex P1-3；R39 Codex P2-1 区间更正） |
| pull FetchObjects 映射把协议形状错误映射 `NetworkUnavailable` | `src/command/pull.rs:931-936`（R13 Codex P1-3；R39 Codex P2-1 区间更正） |
| pull 独立 PacketRead 分支把非 timeout 错误映射 `NetworkProtocol` | `src/command/pull.rs:937-944`（R29/R30 Codex P1-2/P1-3） |

**Acceptance criteria:**（门族 25 门逐门 checkbox，EX-01；与「判据规范」编号 1-25 一一对应，R23 Codex P1-5、R30/R31 Codex P1-3 扩展）

- [ ] 门 1：`ls_remote.rs:210-221`（Discovery）marker → `LBR-NET-002`。
- [ ] 门 2：`clone.rs:1012-1046`（Discovery）marker → `LBR-NET-002`。
- [ ] 门 3：`clone.rs:1059-1063`（FetchObjects/PacketRead，clone fetch 阶段）marker → `LBR-NET-002`；`PacketRead` 臂非 marker 普通 IO（连接重置等）修正为 `NetworkUnavailable`（仅 timeout 维持既有 `NetworkUnavailable`，R29 Codex P1-2）。
- [ ] 门 4：`fetch.rs:882-887`（FetchObjects）marker → `LBR-NET-002`。
- [ ] 门 5：`pull.rs` Discovery marker → `LBR-NET-002`。
- [ ] 门 6：`pull.rs` FetchObjects marker → `LBR-NET-002`。
- [ ] 门 7：`ls_remote.rs` 非 marker 维持 `NetworkUnavailable`/`IoReadFailed` 原归类。
- [ ] 门 8：`clone.rs`（Discovery）非 marker 维持原归类。
- [ ] 门 9：`clone.rs`（fetch 阶段）非 marker 维持原归类——`PacketRead` 臂普通 IO 修正为 `NetworkUnavailable`（R29 Codex P1-2）、其余分支维持既有归类。
- [ ] 门 10：`fetch.rs`（FetchObjects）非 marker 维持原归类。
- [ ] 门 11：`pull.rs`（Discovery）非 marker 维持原归类。
- [ ] 门 12：`pull.rs`（FetchObjects）非 marker 维持原归类。
- [ ] 门 13：参数化矩阵 git × marker → `LBR-NET-002`。
- [ ] 门 14：参数化矩阵 git × 非 marker → `LBR-NET-001`。
- [ ] 门 15：参数化矩阵 ssh × marker → `LBR-NET-002`。
- [ ] 门 16：参数化矩阵 ssh × 非 marker → `LBR-NET-001`。
- [ ] 门 17：参数化矩阵 https × marker → `LBR-NET-002`（含 clone fetch 阶段端到端）。
- [ ] 门 18：参数化矩阵 https × 非 marker → `LBR-NET-001`。
- [ ] 门 19：边界适配错误文本零回显 sentinel 测试通过。
- [ ] 门 20：marker 判别统一消费 PKT-01 常量，无重复字面量（GC-02）。
- [ ] 门 21：`fetch.rs` 独立 `PacketRead` 分支（`:888-897`）非 marker 普通 IO（连接重置等）修正为 `NetworkUnavailable`（R30 Codex P1-3）。
- [ ] 门 22：`pull.rs` 独立 `PacketRead` 分支（`:937-944`）非 marker 普通 IO（连接重置等）修正为 `NetworkUnavailable`（R30 Codex P1-3）。
- [ ] 门 23：`fetch.rs` 独立 `PacketRead` 分支 marker 错误（本卡构造真实 PacketRead carrier 验证 mapper；PKT-13 另验实际异步头解析路径）→ `LBR-NET-002` 正向断言（R31 Codex P1-1）。
- [ ] 门 24：`pull.rs` 独立 `PacketRead` 分支 marker 错误 → `LBR-NET-002` 正向断言（R31 Codex P1-1）。

- [ ] 门 25：空 advertisement 经真实 fetch/clone/ls-remote/pull 命令路径失败，返回 LBR-NET-002，不仅测试手工构造 marker 的 mapper（从 PKT-03 移入，fn pkt_line_discovery_empty_response_regression_fetch_clone_lsremote_pull）。

**判据规范（非计数正文，EX-01 门族枚举）：**

1. `ls_remote.rs:210-221` marker → `LBR-NET-002`（`pkt_line_matrix_ls_remote_marker_maps_net_002`）；2. `clone.rs:1012-1046`（Discovery）marker → `LBR-NET-002`（`pkt_line_matrix_clone_discovery_marker_maps_net_002`）；3. `clone.rs:1059-1063`（FetchObjects/PacketRead）marker → `LBR-NET-002` 且 PacketRead 非 marker 普通 IO → `NetworkUnavailable`（`pkt_line_matrix_clone_fetch_phase_marker_maps_net_002`，R29 Codex P1-2）；4. `fetch.rs:882-897`（FetchObjects/PacketRead）marker → `LBR-NET-002` 且 PacketRead 非 marker 普通 IO → `NetworkUnavailable`（`pkt_line_matrix_fetch_objects_marker_maps_net_002`）；5. `pull.rs` Discovery marker → `LBR-NET-002`（`pkt_line_matrix_pull_discovery_marker_maps_net_002`，R13 P1-3）；6. `pull.rs` FetchObjects marker → `LBR-NET-002`（`pkt_line_matrix_pull_fetch_objects_marker_maps_net_002`，R13 P1-3）；7. ls_remote 非 marker（`pkt_line_matrix_non_marker_ls_remote_stays_net_001`）；8. clone Discovery 非 marker（`pkt_line_matrix_non_marker_clone_discovery_stays_net_001`）；9. clone fetch 阶段非 marker（`pkt_line_matrix_non_marker_clone_fetch_phase_stays_net_001`）；10. fetch FetchObjects 非 marker（`pkt_line_matrix_non_marker_fetch_objects_stays_net_001`）；11. pull Discovery 非 marker（`pkt_line_matrix_non_marker_pull_discovery_stays_net_001`）；12. pull FetchObjects 非 marker（`pkt_line_matrix_non_marker_pull_fetch_objects_stays_net_001`）；13. git × marker（`pkt_line_matrix_parametrized_git_marker_maps_net_002`）；14. git × 非 marker（`pkt_line_matrix_parametrized_git_non_marker_stays_net_001`）；15. ssh × marker（`pkt_line_matrix_parametrized_ssh_marker_maps_net_002`）；16. ssh × 非 marker（`pkt_line_matrix_parametrized_ssh_non_marker_stays_net_001`）；17. https × marker（`pkt_line_matrix_parametrized_https_marker_maps_net_002`，含 clone fetch 阶段端到端）；18. https × 非 marker（`pkt_line_matrix_parametrized_https_non_marker_stays_net_001`）；19. 边界适配零回显 sentinel（`pkt_line_matrix_zero_echo_sentinel`）；20. marker 判别消费 PKT-01 常量（`pkt_line_matrix_marker_constant_single_source`）；21. `fetch.rs` 独立 PacketRead 分支非 marker 普通 IO 修正（`pkt_line_matrix_fetch_packet_read_non_marker_io_stays_net_001`，R30 Codex P1-3）；22. `pull.rs` 独立 PacketRead 分支非 marker 普通 IO 修正（`pkt_line_matrix_pull_packet_read_non_marker_io_stays_net_001`，R30 Codex P1-3）；23. `fetch.rs` 独立 PacketRead 分支 marker → `LBR-NET-002` 正向（`pkt_line_matrix_fetch_packet_read_marker_maps_net_002`，R31 Codex P1-1）；24. `pull.rs` 独立 PacketRead 分支 marker → `LBR-NET-002` 正向（`pkt_line_matrix_pull_packet_read_marker_maps_net_002`，R31 Codex P1-1）。

25. 空响应四命令真实回归（`pkt_line_discovery_empty_response_regression_fetch_clone_lsremote_pull`）；门23/24均是 mapper 级真实 carrier 构造，不替代 PKT-13 真实解析验收。

**门 → 具名验证命令映射（EX-01 补偿条件；全部可直接复制执行，R11 Codex P1-2）**：

- 门 1-4（marker 映射：ls-remote/clone-Discovery/clone-fetch/fetch-Objects）：`source .env.test && cargo test --lib pkt_line_matrix_ls_remote_marker_maps_net_002`；`source .env.test && cargo test --lib pkt_line_matrix_clone_discovery_marker_maps_net_002`；`source .env.test && cargo test --lib pkt_line_matrix_clone_fetch_phase_marker_maps_net_002`；`source .env.test && cargo test --lib pkt_line_matrix_fetch_objects_marker_maps_net_002`
- 门 5-6（pull 两映射点 marker）：`source .env.test && cargo test --lib pkt_line_matrix_pull_discovery_marker_maps_net_002`；`source .env.test && cargo test --lib pkt_line_matrix_pull_fetch_objects_marker_maps_net_002`
- 门 7（ls_remote 非 marker）：`source .env.test && cargo test --lib pkt_line_matrix_non_marker_ls_remote_stays_net_001`
- 门 8（clone Discovery 非 marker）：`source .env.test && cargo test --lib pkt_line_matrix_non_marker_clone_discovery_stays_net_001`
- 门 9（clone fetch 阶段非 marker）：`source .env.test && cargo test --lib pkt_line_matrix_non_marker_clone_fetch_phase_stays_net_001`
- 门 10（fetch FetchObjects 非 marker）：`source .env.test && cargo test --lib pkt_line_matrix_non_marker_fetch_objects_stays_net_001`
- 门 11（pull Discovery 非 marker）：`source .env.test && cargo test --lib pkt_line_matrix_non_marker_pull_discovery_stays_net_001`
- 门 12（pull FetchObjects 非 marker）：`source .env.test && cargo test --lib pkt_line_matrix_non_marker_pull_fetch_objects_stays_net_001`
- 门 13（git × marker）：`source .env.test && cargo test --lib pkt_line_matrix_parametrized_git_marker_maps_net_002`
- 门 14（git × 非 marker）：`source .env.test && cargo test --lib pkt_line_matrix_parametrized_git_non_marker_stays_net_001`
- 门 15（ssh × marker）：`source .env.test && cargo test --lib pkt_line_matrix_parametrized_ssh_marker_maps_net_002`
- 门 16（ssh × 非 marker）：`source .env.test && cargo test --lib pkt_line_matrix_parametrized_ssh_non_marker_stays_net_001`
- 门 17（https × marker）：`source .env.test && cargo test --lib pkt_line_matrix_parametrized_https_marker_maps_net_002`
- 门 18（https × 非 marker）：`source .env.test && cargo test --lib pkt_line_matrix_parametrized_https_non_marker_stays_net_001`
- 门 19（sentinel）：`source .env.test && cargo test --lib pkt_line_matrix_zero_echo_sentinel`
- 门 20（常量单点）：`source .env.test && cargo test --lib pkt_line_matrix_marker_constant_single_source`
- 门 21（fetch PacketRead 非 marker IO 修正）：`source .env.test && cargo test --lib pkt_line_matrix_fetch_packet_read_non_marker_io_stays_net_001`
- 门 22（pull PacketRead 非 marker IO 修正）：`source .env.test && cargo test --lib pkt_line_matrix_pull_packet_read_non_marker_io_stays_net_001`
- 门 23（fetch PacketRead marker 正向）：`source .env.test && cargo test --lib pkt_line_matrix_fetch_packet_read_marker_maps_net_002`
- 门 24（pull PacketRead marker 正向）：`source .env.test && cargo test --lib pkt_line_matrix_pull_packet_read_marker_maps_net_002`

- 门 25：`source .env.test && cargo test --lib pkt_line_discovery_empty_response_regression_fetch_clone_lsremote_pull`

**Verification:**

- [ ] 失败闭合存在性 + 执行（内联可执行，fn 全集 `(new)`）：
  ```bash
  expected="pkt_line_matrix_ls_remote_marker_maps_net_002 pkt_line_matrix_clone_discovery_marker_maps_net_002 pkt_line_matrix_clone_fetch_phase_marker_maps_net_002 pkt_line_matrix_fetch_objects_marker_maps_net_002 pkt_line_matrix_pull_discovery_marker_maps_net_002 pkt_line_matrix_pull_fetch_objects_marker_maps_net_002 pkt_line_matrix_non_marker_ls_remote_stays_net_001 pkt_line_matrix_non_marker_clone_discovery_stays_net_001 pkt_line_matrix_non_marker_clone_fetch_phase_stays_net_001 pkt_line_matrix_non_marker_fetch_objects_stays_net_001 pkt_line_matrix_non_marker_pull_discovery_stays_net_001 pkt_line_matrix_non_marker_pull_fetch_objects_stays_net_001 pkt_line_matrix_parametrized_git_marker_maps_net_002 pkt_line_matrix_parametrized_git_non_marker_stays_net_001 pkt_line_matrix_parametrized_ssh_marker_maps_net_002 pkt_line_matrix_parametrized_ssh_non_marker_stays_net_001 pkt_line_matrix_parametrized_https_marker_maps_net_002 pkt_line_matrix_parametrized_https_non_marker_stays_net_001 pkt_line_matrix_zero_echo_sentinel pkt_line_matrix_marker_constant_single_source pkt_line_matrix_fetch_packet_read_non_marker_io_stays_net_001 pkt_line_matrix_pull_packet_read_non_marker_io_stays_net_001 pkt_line_matrix_fetch_packet_read_marker_maps_net_002 pkt_line_matrix_pull_packet_read_marker_maps_net_002 pkt_line_discovery_empty_response_regression_fetch_clone_lsremote_pull"
  missing=$(bash -c 'set -o pipefail; list=$(source .env.test && cargo test --lib -- --list 2>/dev/null | sed "s/: test\$//" | awk -F"::" "{print \$NF}" | sort) || { echo "TEST LIST PIPELINE FAILED" >&2; exit 1; }; comm -23 <(printf "%s\n" $1 | sort) <(printf "%s\n" "$list")' _ "$expected") || { echo "FN EXISTENCE CHECK FAILED (pipeline error)"; exit 1; }
  [ -z "$missing" ] || { echo "MISSING FNS: $missing"; exit 1; }
  source .env.test && cargo test --lib pkt_line_matrix_ && cargo test --lib pkt_line_discovery_empty_response_regression_fetch_clone_lsremote_pull
  ```
- [ ] 零命中守卫（三分支，内联可执行）（适配点只比对常量；覆盖本卡 6 个映射点所在的全部 4 个文件，R24 Codex P1-9 补 pull.rs）：
  ```bash
  if rg -n 'pkt-line protocol error: "' src/command/ls_remote.rs src/command/clone.rs src/command/fetch.rs src/command/pull.rs; then echo "FAIL: forbidden pattern found"; exit 1; else rc=$?; if [ "$rc" -ne 1 ]; then echo "ERROR: rg failed with exit $rc"; exit "$rc"; fi; echo "OK: zero hits"; fi
  ```
- [ ] `source .env.test && cargo test --test compat_all_production_unwrap_guard`
- [ ] 文档检查（R29 Codex P1-3；四命令 EN+zh 同步边界归类变化 + error-codes 语义行核对）：
  ```bash
  for f in docs/commands/ls-remote.md docs/commands/zh-CN/ls-remote.md docs/commands/clone.md docs/commands/zh-CN/clone.md docs/commands/fetch.md docs/commands/zh-CN/fetch.md docs/commands/pull.md docs/commands/zh-CN/pull.md docs/development/commands/ls-remote.md docs/development/commands/clone.md docs/development/commands/fetch.md docs/development/commands/pull.md; do awk 'tolower($0) ~ /lbr-net-002/ && tolower($0) ~ /pkt-line|packet-line|discovery/ {f=1} END {exit f?0:1}' "$f" || { echo "MISSING semantic line (LBR-NET-002 + pkt-line/discovery): $f"; exit 1; }; done
  awk 'tolower($0) ~ /lbr-net-002/ && tolower($0) ~ /pkt-line|packet-line/ {f=1} END {exit f?0:1}' docs/error-codes.md || { echo "error-codes LBR-NET-002 row does not cover pkt-line"; exit 1; }
  awk 'tolower($0) ~ /lbr-net-002/ && tolower($0) ~ /pkt-line|packet-line|归类/ {f=1} END {exit f?0:1}' COMPATIBILITY.md || { echo "MISSING LBR-NET-002 semantic note in COMPATIBILITY.md"; exit 1; }
  echo "OK: boundary docs + error-codes + COMPATIBILITY synced"
  ```

  （逐文件语义行断言 + 开发文档，R33 Codex P1-5；COMPATIBILITY 归类行检查 R33 Codex P1-4）

**Full-suite trigger:** `T-1: 触及四个 CLI 边界的全局错误映射与共享 marker 契约，执行阶段跑全量`

**Dependencies:** `PKT-07`（同文件 `fetch.rs` 序列化）。

**Deliverables:** N/A（代码卡）。

**Implementation write set:** `src/command/ls_remote.rs`、`src/command/clone.rs`、`src/command/fetch.rs`、`src/command/pull.rs`（含 `#[cfg(test)]`）、`docs/commands/{ls-remote,clone,fetch,pull}.md`（EN+zh）、`docs/development/commands/{ls-remote,clone,fetch,pull}.md`、`docs/error-codes.md`（`LBR-NET-002` 语义行）、`COMPATIBILITY.md`（错误归类行，R31/R33 Codex P1-3/P1-4 纳入）

**Release write set:** Inherited（= ER-08 版本面五处 + `Cargo.lock` 工具链刷新 + release artifact）

**Files likely touched:** `src/command/ls_remote.rs`, `src/command/clone.rs`, `src/command/pull.rs`, `src/command/fetch.rs`, `docs/commands/{ls-remote,clone,fetch,pull}.md`（EN+zh）, `docs/error-codes.md`, `COMPATIBILITY.md`

**Docs and compatibility impact:** **登记（R29 Codex P1-3）**：ls-remote/clone/fetch/pull 边界协议错误归类 `LBR-NET-001`→`LBR-NET-002`（含 PacketRead 非 marker IO 修正）属用户可见错误码语义变化——本卡负责同步 `docs/commands/{ls-remote,clone,fetch,pull}.md`（EN+zh）与核对 `docs/error-codes.md` `LBR-NET-002` 语义行、`COMPATIBILITY.md` 错误归类行（见 Verification 文档检查）

**Rollback mode:** `immutable-release`

**Migration and rollback:** N/A（恢复=发布修复版本，降级=回装上一 `v*` tag）

**Security and privacy:** 协议/网络错误分类互不污染；零回显

**Performance budget:** O(前缀长) 常数；继承 GC-10

**Estimated scope:** `M`（4 落点 / 4 生产文件；边界归类行为变化）

**Version increment:** `patch`（`0.22.14` → `0.22.15`，版本面五处同步 + 工具链刷新 `Cargo.lock` + `gh release create -R libra-tools/libra v0.22.15`）

**Release boundary:** `independent`（完整 C 组 + D 组取证）

**C/D coverage from:** `self`

**Granularity:** `type=implementation; axis=clone/ls-remote/pull/fetch-objects 边界归类（门族）; recovery=发布修复版本（immutable-release），降级=回装上一 tag; complete=yes; self-contained=yes; AC=25/8@EX-01; VER=4/8; landing=4; prod-files=4; scope=M; deps=PKT-07; writeset=序列化于 PKT-07（同文件 fetch.rs/fetch.md）; release=independent; split-from=PKT-06@R4; exception=EX-01`

### Task PKT-11: SSH process-output 全路径脱敏

**Task type:** `implementation`

**Lifecycle / Acceptance:** `pending` / 空

**Description:** 自 PKT-08@R4 拆出。SSH 子进程用户可见错误去除远端 stderr 原文：`describe_status_with_stderr`（`:539-550`）与 `describe_process_output`（`:535-537`，转发前者）改为「退出状态 + 固定短语」，stderr 原文不入任何日志——`tracing::debug!` 仅记录退出状态、字节长度与不可逆摘要（R25 Codex P1-2 措辞统一）；使 `:296-299`（discovery 非零退出）、`:429-433`（fetch_objects 状态任务）、`:491-497`（send_pack 非零退出）三处一次收口。唯一行为轴：SSH process-output 用户可见面脱敏。

**Out of scope:** pkt-line 三处 read 错误包装点：PKT-12（先行，同文件串行）；异步头解析：PKT-13。

**Current evidence:**

| 事实 | 证据 |
|---|---|
| 非零退出/状态路径回显 stderr 共 3 处 | `src/internal/protocol/ssh_client.rs:296-299`、`:429-433`、`:491-497` |
| `describe_process_output`（:535-537）转发 `describe_status_with_stderr`（:539-550）拼接 stderr 原文 | 同左 |
| 交互态 `Stdio::inherit()` 直通远端 stderr（`is_terminal()` 判定） | `src/internal/protocol/ssh_client.rs:164-204`（:171、:180-182、:198-203） |

**Acceptance criteria:**

- [ ] `describe_status_with_stderr` 返回「退出状态 + 固定短语」，不再包含 stderr 原文；stderr 原文不入日志：`tracing::debug!` 仅记录退出状态、字节长度与不可逆摘要（R7 Codex P1-3）。
- [ ] `describe_process_output`（`:535-537`）随前者语义收窄（转发不变，输出不含 stderr 原文）。
- [ ] `ssh_client.rs:296-299`（discovery 非零退出）错误文本零 stderr 原文。
- [ ] `ssh_client.rs:429-433`（fetch_objects 状态任务）错误文本零 stderr 原文。
- [ ] `ssh_client.rs:491-497`（send_pack 非零退出）错误文本零 stderr 原文。
- [ ] 恶意 stderr sentinel 测试通过：远端非零退出 + 恶意 stderr 注入下，用户可见错误不含任何 stderr 字节。
- [ ] debug 级 trace 仅记录退出状态、stderr 字节长度与不可逆摘要（如 SHA-256 前 16 位）、**不记录 stderr 原文**的断言测试通过（防日志注入与敏感信息入日志，R7 Codex P1-3）。
- [ ] stderr 采集有硬上限（如 64 KiB 截断后按摘要记录）：**全部捕获路径**（含 `wait_with_output` 的 stdout/stderr 无界捕获与 `read_to_end` 无界增长，`ssh_client.rs` 共 6 处捕获点）改用有界收集（R25 Codex P1-3），洪水回归测试按 discovery/fetch_objects/send_pack 三路径分别断言上限生效（单 fn 三子断言）。
- [ ] `docs/commands/{clone,fetch,config}.md`（EN+zh）、`docs/development/commands/{clone,fetch,config}.md` 与 `COMPATIBILITY.md` ssh 行已同步 BatchMode/host-key fail-closed 行为，**逐文件**含 `ssh.strictHostKeyChecking` 配置说明（R11 Codex P1-5；R12 Claude P1-5 补开发文档面；R27 Codex P1-4 检查扩展至开发文档与兼容矩阵）。
- [ ] 捕获路径强制 `BatchMode=yes`（交互态 stderr 不再 `Stdio::inherit()`，改为捕获 + 经脱敏渲染；R10 Codex P1-4 / ADR-PKT-03）。
- [ ] passphrase 场景回归：口令保护私钥且无 agent 时，错误提示明确指向 ssh-agent（R17 Claude P1-3）。
- [ ] host key 未确认时 fail-closed：返回稳定指引错误，**不提供盲追加命令**（`ssh-keyscan … >> known_hosts` 会接受 MITM 主机键，R11 Codex P1-4）；指引为：①经可信渠道（服务商控制台/带外）核对主机指纹后手动添加，或②一次性 `ssh -T git@github.com` 交互 TOFU（以 GitHub 为例，实际仓库主机据实替换域名；OpenSSH 显示指纹供人工核对，**禁止以 `ssh-keyscan` 管道写入 known_hosts——该命令无带外指纹校验**，R15 Codex P1-7），并说明 `ssh.strictHostKeyChecking` 配置项。不渲染任何远端提示原文。host-key 失败分类在脱敏前经唯一树内 carrier 完成（按 ADR-PKT-03）：`SSH_HOST_KEY_UNCONFIRMED_SIGNAL` 固定前缀常量（GC-02 单点）——`ssh_client.rs` 捕获侧以退出码 255 + stderr 固定模式在脱敏前构造携带该前缀的 `GitError` 载荷，`clone.rs:1012-1029` 消费**内层 `GitError` 载荷**（`对 FetchError::Discovery.source 模式匹配 GitError::NetworkError(detail) 后读取原始 detail（不是 source.to_string()）`，非外层 `FetchError` Display——外层包装前缀会破坏 `starts_with` 串首锚定，R33 Codex P1-3）并以 `starts_with` 识别，命中后直接生成固定指引错误、不嵌入内层 detail；PKT-11 脱敏后 `clone.rs` 不再依赖 `"host key verification failed"` 原文匹配（R28/R29/R30 Codex P1-4）；三段契约测试（信号产出/脱敏剥离/clone 指引含层级契约子断言）绑定 `…_host_key_fail_closed_guidance` fn（覆盖真实 SSH→clone 路径）；同时更新 clone.rs 既有约3895行的测试，删除对盲追加 ssh-keyscan 提示的旧期望，改锁定可信核验与手动 TOFU 指引。
- [ ] 交互路径 sentinel 测试通过：远端 stderr 含恶意控制序列时用户终端不出现原文。

**Verification:**

- [ ] 失败闭合存在性 + 执行（内联可执行，fn 全集 `(new)`）：
  ```bash
  expected="pkt_line_client_describe_status_no_stderr pkt_line_client_describe_process_output_no_stderr pkt_line_client_nonzero_exit_discovery_zero_stderr pkt_line_client_status_task_zero_stderr pkt_line_client_send_pack_zero_stderr pkt_line_client_malicious_stderr_sentinel pkt_line_client_debug_trace_records_status_length_digest pkt_line_client_batch_mode_enforced pkt_line_client_host_key_fail_closed_guidance pkt_line_client_interactive_stderr_captured_sanitized pkt_line_client_batch_mode_passphrase_error_points_to_agent pkt_line_client_stderr_flood_capped"
  missing=$(bash -c 'set -o pipefail; list=$(source .env.test && cargo test --lib -- --list 2>/dev/null | sed "s/: test\$//" | awk -F"::" "{print \$NF}" | sort) || { echo "TEST LIST PIPELINE FAILED" >&2; exit 1; }; comm -23 <(printf "%s\n" $1 | sort) <(printf "%s\n" "$list")' _ "$expected") || { echo "FN EXISTENCE CHECK FAILED (pipeline error)"; exit 1; }
  [ -z "$missing" ] || { echo "MISSING FNS: $missing"; exit 1; }
  source .env.test && cargo test --lib pkt_line_client_
  ```
- [ ] 零命中守卫（三分支，内联可执行）（用户可见错误构造点；tracing::debug! 行命中为预期，逐条核对）：
  ```bash
  if rg -n 'stderr: \{stderr\}' src/internal/protocol/ssh_client.rs; then echo "FAIL: forbidden pattern found"; exit 1; else rc=$?; if [ "$rc" -ne 1 ]; then echo "ERROR: rg failed with exit $rc"; exit "$rc"; fi; echo "OK: zero hits"; fi
  ```
- [ ] `source .env.test && cargo test --test compat_all_production_unwrap_guard`

**判据规范（非计数正文，EX-02 门族枚举；R11 Claude P1-4 立块，R23 Codex P1-6/P1-15 复核为 13 门并与 13 条 AC 一一对应）：**

1. `describe_status_with_stderr` 无 stderr 原文（`pkt_line_client_describe_status_no_stderr`）；2. `describe_process_output` 同（`…_describe_process_output_no_stderr`）；3-5. `:296-299` / `:429-433` / `:491-497` 三点零 stderr（`…_nonzero_exit_discovery_zero_stderr` / `…_status_task_zero_stderr` / `…_send_pack_zero_stderr`）；6. 恶意 stderr sentinel（`…_malicious_stderr_sentinel`）；7. debug 仅状态/长度/摘要（`…_debug_trace_records_status_length_digest`）；8. stderr 洪水上限（`…_stderr_flood_capped`，覆盖全部 6 处捕获点并按 discovery/fetch_objects/send_pack 三路径分子断言）；9. BatchMode 强制（`…_batch_mode_enforced`）；10. host-key fail-closed 指引（`…_host_key_fail_closed_guidance`；含脱敏前内部分类信号驱动与真实 SSH→clone 路径子断言，R28 Codex P1-4）；11. 交互 stderr 捕获脱敏（`…_interactive_stderr_captured_sanitized`）；12. passphrase 指引（`…_batch_mode_passphrase_error_points_to_agent`）；13. 文档同步核对（可执行存在性检查，含 `ssh.strictHostKeyChecking` 配置说明）：
  ```bash
  for f in docs/commands/clone.md docs/commands/zh-CN/clone.md docs/commands/fetch.md docs/commands/zh-CN/fetch.md docs/commands/config.md docs/commands/zh-CN/config.md docs/development/commands/clone.md docs/development/commands/fetch.md docs/development/commands/config.md COMPATIBILITY.md; do grep -q 'BatchMode' "$f" || { echo "MISSING BatchMode note: $f"; exit 1; }; grep -q 'ssh\.strictHostKeyChecking' "$f" || { echo "MISSING ssh.strictHostKeyChecking note: $f"; exit 1; }; done
  if rg -n 'ssh-keyscan.*>>' src/command/clone.rs docs/commands/ docs/development/commands/; then echo "FAIL: blind ssh-keyscan guidance found"; exit 1; else rc=$?; if [ "$rc" -ne 1 ]; then echo "ERROR: rg failed with exit $rc"; exit "$rc"; fi; echo "OK: docs synced, no blind keyscan"; fi
  ```

  （`COMPATIBILITY.md` 核对 ssh 行 BatchMode/host-key fail-closed/`ssh.strictHostKeyChecking` 表述，R27 Codex P1-4 逐文件断言）。门→命令：1-12 由 `source .env.test && cargo test --lib pkt_line_client_` 覆盖；门 13 为上列可执行检查。

**Full-suite trigger:** `T-1: 触及共享协议客户端模块的用户可见错误面，执行阶段跑全量`

**Dependencies:** `PKT-12`、`PKT-09`（COMPATIBILITY.md 文档面序列化，R35 Codex P1-2；同文件 `ssh_client.rs` 序列化；pkt-line 三点先行；读取下界经传递闭包先于本卡，R27 Codex P1-2 去除正文中的任务 ID 混排）。

**Deliverables:** N/A（代码卡）。

**Implementation write set:** `src/internal/protocol/ssh_client.rs`、`src/command/clone.rs:1024-1028`、`docs/commands/{clone,fetch,config}.md`（EN+zh）、`docs/development/commands/{clone,fetch,config}.md`、`COMPATIBILITY.md`（含 `#[cfg(test)]`）

**Release write set:** Inherited（= ER-08 版本面五处 + `Cargo.lock` 工具链刷新 + release artifact）

**Files likely touched:** `src/internal/protocol/ssh_client.rs`, `src/command/clone.rs`, `docs/commands/{clone,fetch,config}.md`（EN+zh）, `docs/development/commands/{clone,fetch,config}.md`, `COMPATIBILITY.md`

**Docs and compatibility impact:** **扩展（R11/R15 Codex；R16 Claude P1-3 收窄为本卡范围）**：BatchMode/host-key fail-closed 是用户可见行为变化——本卡负责同步 `docs/commands/{clone,fetch,config}.md`（EN+zh）、`docs/development/commands/{clone,fetch,config}.md` 与 `COMPATIBILITY.md`（ssh 行：BatchMode 覆盖与 fail-closed 指引）；衔接第十次审计已登记的 `ssh.strictHostKeyChecking` 文档债（push 状态行归类变化由 PKT-09 自身卡负责）

**Rollback mode:** `immutable-release`

**Migration and rollback:** N/A（恢复=发布修复版本，降级=回装上一 `v*` tag）

**Security and privacy:** 用户可见错误全面去除远端 stderr 原文；诊断移至 debug trace

**Performance budget:** O(1)；继承 GC-10

**Estimated scope:** `M`（2 落点 / 2 生产文件）

**Version increment:** `patch`（`0.22.18` → `0.22.19`，版本面五处同步 + 工具链刷新 `Cargo.lock` + `gh release create -R libra-tools/libra v0.22.19`）

**Release boundary:** `independent`（完整 C 组 + D 组取证）

**C/D coverage from:** `self`

**Granularity:** `type=implementation; axis=SSH process-output 全路径脱敏（含交互路径与文档同步，BatchMode 决议）; recovery=发布修复版本（immutable-release），降级=回装上一 tag; complete=yes; self-contained=yes; AC=13/8@EX-02; VER=3/8; landing=2; prod-files=2; scope=M; deps=PKT-12,PKT-09; writeset=序列化于 PKT-12（同文件）与 PKT-09（COMPATIBILITY.md）; release=independent; split-from=PKT-08@R4; exception=EX-02`

### Task PKT-12: SSH pkt-line 三点脱敏

**Task type:** `implementation`

**Lifecycle / Acceptance:** `pending` / 空

**Description:** 自 PKT-08@R5 拆出。在 `ssh_client.rs` 三处 pkt-line read 错误包装点（`:273-284` discovery、`:319-329` fetch_objects、`:449-459` send_pack）：对携带 marker 前缀的错误**原样透传**（不套 `"SSH read failed: "` 等前置字面量，保持 `starts_with` 串首锚定成立，R6 Claude P1-1），且不拼接 `describe_process_output()` 的 stdout/stderr；非 marker 错误按现状包装。三处经共享辅助函数一次实现（GC-02）。唯一行为轴：SSH pkt-line 错误路径透传与脱敏。

**Out of scope:** 非零退出/状态路径与 describe 函数本体：PKT-11（后继，同文件串行）；读取下界：PKT-08（先行）。

**Current evidence:**

| 事实 | 证据 |
|---|---|
| pkt-line 错误路径 stderr 回显包装点 3 处 | `src/internal/protocol/ssh_client.rs:273-284`、`:319-329`、`:449-459`（`describe_process_output` :535-537 → `describe_status_with_stderr` :539-550） |

**Acceptance criteria:**

- [ ] ssh `:273-284`（discovery）包装点对 marker 错误原样透传（不套前置字面量，不拼接 `describe_process_output` 输出）；成功与 `wait_with_output` 收集失败两条路径统一经共享 helper 透传，失败分支同样不加 `"SSH ..."` 前缀（R28 Codex P1-3）。
- [ ] ssh `:319-329`（fetch_objects）包装点对 marker 错误原样透传；成功与收集失败两条路径同上统一（R28 Codex P1-3）。
- [ ] ssh `:449-459`（send_pack）包装点对 marker 错误原样透传；成功与收集失败两条路径同上统一（R28 Codex P1-3）。
- [ ] 非 marker 错误按现状包装（`"SSH read failed: "` 等前缀保留）回归通过。
- [ ] 零回显 sentinel 测试通过：畸形帧 + 恶意 stderr 注入下三处 SSH pkt-line 包装点错误文本均不含远端字节（本卡专属 sentinel `pkt_line_client_zero_echo_sentinel`；fetch 异步路径 sentinel `pkt_line_fetch_async_zero_echo_sentinel` 唯一归属 PKT-13——两 fn 名与作用域均不重叠，R23 Codex P1-17 拆分澄清）。
- [ ] marker 串首位置回归测试通过：透传后的最终用户可见文本仍以 `PKT_LINE_PROTOCOL_ERROR_PREFIX` 开头（R11 Codex P1-1；fetch 异步零回显 sentinel 唯一归属 PKT-13，本卡 sentinel 限 SSH 三点）。
- [ ] 三处包装点经共享辅助函数实现（GC-02，无三份重复判别逻辑）。
- [ ] SSH 内层读取包装点（`ssh_client.rs` `read_advertisement` 错误包装 `:234`、`:253`）对 marker 错误同样原样透传（不套 `"SSH read failed: "` 前缀），与外层三点共用同一 helper，保证 marker 在内层包装后仍位于串首（R30 Codex P1-2）。
- [ ] 读取错误发生后**立即 kill 子进程并以有界超时 reap**（防恶意远端保持 SSH 子进程挂住 CLI；R13 Codex P1-4）。
- [ ] 回归测试通过：发送 malformed frame 后子进程不退出场景下，CLI 在超时预算内返回错误而非挂起。

- [ ] 真实 SSH 读取→内外包装→CLI 链路中，帧长1..3、partial-header EOF、payload EOF 均返回 LBR-NET-002；fn `pkt_line_client_ssh_frame_errors_end_to_end_net_002` 不以手造 marker 替代输入字节。注入经现有 `LIBRA_SSH_COMMAND`（ssh_client.rs 开工基线170行）选择本地假 SSH 子进程，发送真实坏帧；环境变化按 keyed serial/env 恢复与注册规则执行（必要的 registry/generator 写集在本卡开工刷新），不依赖公网。

**Verification:**

- [ ] 失败闭合存在性 + 执行（内联可执行，fn 全集 `(new)`）：
  ```bash
  expected="pkt_line_client_ssh_wrapper_marker_passthrough_discovery pkt_line_client_ssh_wrapper_passthrough_fetch_objects pkt_line_client_ssh_wrapper_passthrough_send_pack pkt_line_client_ssh_inner_wrapper_marker_passthrough pkt_line_client_non_marker_wrapped_regression pkt_line_client_zero_echo_sentinel pkt_line_client_marker_at_string_start pkt_line_client_shared_helper_single_source pkt_line_client_ssh_read_error_reaps_child pkt_line_client_ssh_read_error_no_hang pkt_line_client_ssh_frame_errors_end_to_end_net_002"
  missing=$(bash -c 'set -o pipefail; list=$(source .env.test && cargo test --lib -- --list 2>/dev/null | sed "s/: test\$//" | awk -F"::" "{print \$NF}" | sort) || { echo "TEST LIST PIPELINE FAILED" >&2; exit 1; }; comm -23 <(printf "%s\n" $1 | sort) <(printf "%s\n" "$list")' _ "$expected") || { echo "FN EXISTENCE CHECK FAILED (pipeline error)"; exit 1; }
  [ -z "$missing" ] || { echo "MISSING FNS: $missing"; exit 1; }
  source .env.test && cargo test --lib pkt_line_client_
  ```
- [ ] `source .env.test && cargo test --test compat_all_production_unwrap_guard`

**判据规范（非计数正文，EX-03 门族枚举；R14 Claude P1-2）：**

1-3. 三处外层透传门（`…_ssh_wrapper_marker_passthrough_discovery` / `…_passthrough_fetch_objects` / `…_passthrough_send_pack`；各 fn 覆盖成功与 `wait_with_output` 收集失败双路径子断言，R28 Codex P1-3）；4. 非-marker 按现状包装回归（`…_non_marker_wrapped_regression`）；5. 零回显 sentinel（`…_zero_echo_sentinel`，SSH 三点专属；fetch 异步 sentinel `pkt_line_fetch_async_zero_echo_sentinel` 归属 PKT-13）；6. marker 串首位置（`…_marker_at_string_start`）；7. 共享辅助单点（`…_shared_helper_single_source`）；8. kill/reap（`…_ssh_read_error_reaps_child`）；9. 挂起回归（`pkt_line_client_ssh_read_error_no_hang`，R37 Codex P1-3 拆分）；10. 内层读取包装点透传（`…_ssh_inner_wrapper_marker_passthrough`，`ssh_client.rs:234`/`:253`，R30 Codex P1-2）。11. SSH 帧错误真实端到端分类（`pkt_line_client_ssh_frame_errors_end_to_end_net_002`）。门→命令：`source .env.test && cargo test --lib pkt_line_client_`（覆盖 1-11，门与 expected fn 一一对应）。

**Full-suite trigger:** `T-1: 触及共享协议客户端模块的错误面，执行阶段跑全量`

**Dependencies:** `PKT-08`（同文件 `ssh_client.rs` 序列化；下界先行）。

**Deliverables:** N/A（代码卡）。

**Implementation write set:** `src/internal/protocol/ssh_client.rs`（含 `#[cfg(test)]`）

**Release write set:** Inherited（= ER-08 版本面五处 + `Cargo.lock` 工具链刷新 + release artifact）

**Files likely touched:** `src/internal/protocol/ssh_client.rs`

**Docs and compatibility impact:** Inherited（核对登记，预期零变更）

**Rollback mode:** `immutable-release`

**Migration and rollback:** N/A（恢复=发布修复版本，降级=回装上一 `v*` tag）

**Security and privacy:** 协议错误路径三处禁用 stderr 拼接（GC-07）

**Performance budget:** O(1)；继承 GC-10

**Estimated scope:** `M`（1 落点 / 1 生产文件）

**Version increment:** `patch`（`0.22.17` → `0.22.18`，版本面五处同步 + 工具链刷新 `Cargo.lock` + `gh release create -R libra-tools/libra v0.22.18`）

**Release boundary:** `independent`（完整 C 组 + D 组取证）

**C/D coverage from:** `self`

**Granularity:** `type=implementation; axis=SSH pkt-line 三点脱敏（门族）; recovery=发布修复版本（immutable-release），降级=回装上一 tag; complete=yes; self-contained=yes; AC=11/8@EX-03; VER=2/8; landing=1; prod-files=1; scope=M; deps=PKT-08; writeset=序列化于 PKT-08（同文件）; release=independent; split-from=PKT-08@R5; exception=EX-03`

### Task PKT-13: 异步头部解析 marker 化（fetch/git/ssh 三传输）

**Task type:** `implementation`

**Lifecycle / Acceptance:** `pending` / 空

**Description:** 自 PKT-07@R5 拆出。三处异步头部解析的错误文本收敛为 marker-producing：`read_hex_4`（`fetch.rs:3653-3666`，删除 `'{hex_str}'` 回显，字面插值在 :3665）、`git_client.rs:143-146`、`ssh_client.rs:235-238`（`Invalid pkt-line length` 无 marker）统一经共享转换产生携带 `PKT_LINE_PROTOCOL_ERROR_PREFIX` 的错误（`PktLineError` → `IoError`/`GitError` 边界），使三传输坏头经 PKT-06/10 适配后归入 `LBR-NET-002`。唯一行为轴：异步头部解析错误 marker 化（含 `git_client.rs:173` discovery 包装点的原样透传与 git:// 端到端回归）。

**Out of scope:** 各映射点适配：PKT-06/10；stderr 脱敏：PKT-12/11；同步解析器：PKT-02。

**Current evidence:**

| 事实 | 证据 |
|---|---|
| `read_hex_4` 错误文本回显远端头且无 marker | `src/command/fetch.rs:3653-3666` |
| `Invalid pkt-line length: {e}` 无 marker（git/ssh 头解析） | `src/internal/protocol/git_client.rs:143-146`、`src/internal/protocol/ssh_client.rs:235-238` |

**Acceptance criteria:**

- [ ] `read_hex_4` 错误文本改为 marker 前缀固定短语（`invalid packet line header '` 零命中）。
- [ ] `git_client.rs:143-146` 头解析错误文本携带 marker 前缀。
- [ ] `ssh_client.rs:235-238` 头解析错误文本携带 marker 前缀。
- [ ] 三传输坏头参数化测试通过（经 PKT-06/10 适配后稳定码 `LBR-NET-002`）。
- [ ] 非 marker 既有错误回归通过（不受影响）。
- [ ] `git_client.rs:173` 的 `"Failed to read response: {e}"` 包装点对 marker 错误原样透传（不套前置字面量，保持 `starts_with` 串首锚定；R7 Codex P1-1）。
- [ ] fetch 异步路径零回显 sentinel 测试通过（自 PKT-07 移入，覆盖 `read_hex_4` 改造后的完整路径；R10/R11 Codex P1-3，R12 Claude P1-1 归属更正）。
- [ ] 头解析/转换收敛为共享 helper（GC-02，无三份重复实现）。

- [ ] 真实 git:// 读取→discovery 包装→CLI 链路中，帧长1..3、partial-header EOF、payload EOF 均返回 LBR-NET-002；fn `pkt_line_header_git_frame_errors_end_to_end_net_002` 不以手造 marker 替代输入字节（EX-06 第9项）。注入采用现有 `tests/protocol_timeout_recovery.rs:16-23` 的本地 TcpListener/127.0.0.1:0 模式，经真实 git:// 协议客户端读取，端口与任务有界清理、不依赖公网。

**Verification:**

- [ ] 失败闭合存在性 + 执行（内联可执行，fn 全集 `(new)`）：
  ```bash
  expected="pkt_line_header_fetch_hex4_marker pkt_line_header_git_client_header_marker pkt_line_header_ssh_client_header_marker pkt_line_header_three_transports_bad_header_net_002 pkt_line_header_non_marker_regression pkt_line_header_marker_at_string_start_regression pkt_line_header_git_discovery_wrapper_passthrough pkt_line_header_shared_helper_single_source pkt_line_fetch_async_zero_echo_sentinel pkt_line_header_git_frame_errors_end_to_end_net_002"
  missing=$(bash -c 'set -o pipefail; list=$(source .env.test && cargo test --lib -- --list 2>/dev/null | sed "s/: test\$//" | awk -F"::" "{print \$NF}" | sort) || { echo "TEST LIST PIPELINE FAILED" >&2; exit 1; }; comm -23 <(printf "%s\n" $1 | sort) <(printf "%s\n" "$list")' _ "$expected") || { echo "FN EXISTENCE CHECK FAILED (pipeline error)"; exit 1; }
  [ -z "$missing" ] || { echo "MISSING FNS: $missing"; exit 1; }
  source .env.test && cargo test --lib pkt_line_header_ && source .env.test && cargo test --lib pkt_line_fetch_async_
  ```
- [ ] 零命中守卫（三分支，内联可执行）：
  ```bash
  if rg -n "invalid packet line header '" src/command/fetch.rs; then echo "FAIL: forbidden pattern found"; exit 1; else rc=$?; if [ "$rc" -ne 1 ]; then echo "ERROR: rg failed with exit $rc"; exit "$rc"; fi; echo "OK: zero hits"; fi
  ```
- [ ] 零命中守卫（三分支，内联可执行，大小写敏感）：
  ```bash
  if rg -n 'Invalid pkt-line length' src/internal/protocol/git_client.rs; then echo "FAIL: forbidden pattern found"; exit 1; else rc=$?; if [ "$rc" -ne 1 ]; then echo "ERROR: rg failed with exit $rc"; exit "$rc"; fi; echo "OK: zero hits"; fi
  ```
- [ ] 零命中守卫（三分支，内联可执行，大小写敏感）：
  ```bash
  if rg -n 'invalid pkt-line length' src/internal/protocol/ssh_client.rs; then echo "FAIL: forbidden pattern found"; exit 1; else rc=$?; if [ "$rc" -ne 1 ]; then echo "ERROR: rg failed with exit $rc"; exit "$rc"; fi; echo "OK: zero hits"; fi
  ```
- [ ] `source .env.test && cargo test --test compat_all_production_unwrap_guard`
- [ ] 汇总文档核对检查（头解析 marker 化的三传输面已在各行为卡同步——本卡核对登记，R32/R33 Codex P1-3/P1-5）：
  ```bash
  for f in docs/commands/fetch.md docs/commands/clone.md docs/commands/zh-CN/fetch.md docs/commands/zh-CN/clone.md docs/development/commands/fetch.md docs/development/commands/clone.md; do awk 'tolower($0) ~ /lbr-net-002/ && tolower($0) ~ /pkt-line|packet-line/ {f=1} END {exit f?0:1}' "$f" || { echo "MISSING semantic line (LBR-NET-002 + pkt-line): $f"; exit 1; }; done
  awk 'tolower($0) ~ /lbr-net-002/ && tolower($0) ~ /pkt-line|packet-line/ {f=1} END {exit f?0:1}' docs/error-codes.md || { echo "MISSING error-codes semantic"; exit 1; }
  echo "OK: header-marker docs coverage verified"
  ```

**Full-suite trigger:** `T-1: 触及共享协议客户端模块与 marker 契约，执行阶段跑全量`

**Dependencies:** `PKT-07`、`PKT-08`、`PKT-10`、`PKT-12`、`PKT-11`（R25/R30 Codex 纯 ID 化，与「实施顺序」五条入边一一对应；同文件串行由直接边与传递闭包共同保证——fetch.rs 链覆盖 discovery/异步段，git_client.rs 的读取下界卡与本卡为该文件仅有的两个写入方，先后由入边保证）。

**Deliverables:** N/A（代码卡）。

**Implementation write set:** `src/git_protocol.rs`（共享头解码/转换）、`src/command/fetch.rs`、`src/internal/protocol/git_client.rs`、`src/internal/protocol/ssh_client.rs`（含 `#[cfg(test)]`）

**Release write set:** Inherited（= ER-08 版本面五处 + `Cargo.lock` 工具链刷新 + release artifact）

**Files likely touched:** `src/git_protocol.rs`, `src/command/fetch.rs`, `src/internal/protocol/git_client.rs`, `src/internal/protocol/ssh_client.rs`

**Docs and compatibility impact:** Inherited（**汇总核对登记**：本卡为依赖链终点，汇总全计划 `docs/error-codes.md` / `COMPATIBILITY.md` 核对结论——文档写入动作归 PKT-09/PKT-10/PKT-11 各自卡，本卡零文档写入，R31 Codex P1-3 分工澄清）

**Rollback mode:** `immutable-release`

**Migration and rollback:** N/A（恢复=发布修复版本，降级=回装上一 `v*` tag）

**Security and privacy:** fail-closed；零回显（修复 `read_hex_4` 既有回显）

**Performance budget:** O(1)；继承 GC-10

**Estimated scope:** `M`（4 落点 / 4 生产文件）

**Version increment:** `patch`（`0.22.19` → `0.22.20`，版本面五处同步 + 工具链刷新 `Cargo.lock` + `gh release create -R libra-tools/libra v0.22.20`）

**Release boundary:** `independent`（完整 C 组 + D 组取证）

**C/D coverage from:** `self`

**Granularity:** `type=implementation; axis=异步头部解析 marker 化; recovery=发布修复版本（immutable-release），降级=回装上一 tag; complete=yes; self-contained=yes; AC=9/8@EX-06; VER=6/8; landing=4; prod-files=4; scope=M; deps=PKT-07,PKT-08,PKT-10,PKT-12,PKT-11; writeset=共享 git_protocol.rs 经 PKT-01/02/08 串行；序列化于 PKT-07/10（fetch.rs）与 PKT-08/12/11（git/ssh_client.rs）; release=independent; split-from=PKT-07@R5; exception=EX-06`

### Task PKT-14: push `ng` 输入校验与渲染卫生（refname/reason 安全化）

**Task type:** `implementation`

**Lifecycle / Acceptance:** `pending` / 空

**Description:** 自 PKT-09@R11 拆出（R11 Codex P1-3）。按 ADR-PKT-02 扩展口径：`ng` 行 `refname` 先校验属于本地 `expected_refs`（未知 refname 不透传，改归 `PushError::Protocol` 固定短语）；校验通过的 `refname` 与 `reason` 共用控制字符转义（C0/DEL/CSI）与 200 字符限长；恶意 sentinel 覆盖人读与 `--json` 两面。唯一行为轴：push `ng` 输入校验与渲染卫生。

**Out of scope:** 状态行归类与回显修复：PKT-09（先行，同文件串行）；`ng` reason 结构化展示：DEFER-05。

**Current evidence:**

| 事实 | 证据 |
|---|---|
| `refname`/`reason` 均来自远端状态行并进入用户可见错误 | `src/command/push.rs:2140-2147`、Display 输出 `:299` |
| 无 refname 校验、无转义、无限长 | 同上（2026-09-03 核对） |

**Acceptance criteria:**

- [ ] `ng` 行 `refname` 校验属于本地 `expected_refs`；未知 refname 改归 `PushError::Protocol` 固定短语。
- [ ] 校验通过的 `refname` 与 `reason` 共用控制字符转义（C0/DEL/CSI 转义序列）。
- [ ] 200 字符限长（超出截断加省略号）。
- [ ] 恶意 `ng` sentinel（人读输出）通过：控制序列不出现在终端输出。
- [ ] 恶意 `ng` sentinel（`--json` 输出）通过：转义后字段不含原始控制字节。

**Verification:**

- [ ] 失败闭合存在性 + 执行（内联可执行，fn 全集 `(new)`）：
  ```bash
  expected="pkt_line_push_ng_reason_escaped_and_capped pkt_line_push_refname_validated_or_protocol pkt_line_push_ng_sentinel_human_and_json"
  missing=$(bash -c 'set -o pipefail; list=$(source .env.test && cargo test --lib -- --list 2>/dev/null | sed "s/: test\$//" | awk -F"::" "{print \$NF}" | sort) || { echo "TEST LIST PIPELINE FAILED" >&2; exit 1; }; comm -23 <(printf "%s\n" $1 | sort) <(printf "%s\n" "$list")' _ "$expected") || { echo "FN EXISTENCE CHECK FAILED (pipeline error)"; exit 1; }
  [ -z "$missing" ] || { echo "MISSING FNS: $missing"; exit 1; }
  source .env.test && cargo test --lib pkt_line_push_
  ```
- [ ] `source .env.test && cargo test --test compat_all_production_unwrap_guard`
- [ ] 计划级收口守卫（可执行，R13 Codex P2-3 / R15 Codex P1-6 / R29 Codex P1-4 双基线）：
  ```bash
  files="src/git_protocol.rs src/internal/protocol/mod.rs src/command/push.rs src/command/fetch.rs src/command/ls_remote.rs src/command/clone.rs src/command/pull.rs src/internal/protocol/git_client.rs src/internal/protocol/ssh_client.rs"
  set -o pipefail
  for base in "v0.22.10..HEAD" "v0.22.10"; do
    diffout=$(libra diff "$base" -- $files 2>&1) || { echo "ERROR: libra diff $base failed"; exit 1; }
    if bad=$(printf '%s\n' "$diffout" | awk '/^\+\+\+/ {next} /^\+/ && /unwrap\(|expect\(|panic!\(/ && !/INVARIANT/ {print; f=1} END {exit f?0:1}'); then
      echo "FAIL: forbidden pattern in added lines ($base):"; echo "$bad"; exit 1
    else
      grc=$?
      if [ "$grc" -ne 1 ]; then echo "ERROR: awk failed with exit $grc"; exit "$grc"; fi
    fi
  done
  echo "OK: committed range + worktree-inclusive diffs clean"
  ```

  （`v0.22.10..HEAD` 覆盖已提交范围，`v0.22.10`（commit→工作树）覆盖 C 组执行中尚未提交的生产改动——两基线都必须零命中；`#[cfg(test)]` 区块内命中须逐条注明测试属性；本命令为收口证据，登记进任务记录）

**Full-suite trigger:** `T-1: 触及 push 全局错误渲染面与共享净化 helper，执行阶段跑全量`

**Dependencies:** `PKT-09`（同文件 `push.rs` 序列化；状态行归类先行）。

**Deliverables:** N/A（代码卡）。

**Implementation write set:** `src/command/push.rs`（含 `#[cfg(test)]`）

**Release write set:** Inherited（= ER-08 版本面五处 + `Cargo.lock` 工具链刷新 + release artifact）

**Files likely touched:** `src/command/push.rs`

**Docs and compatibility impact:** Inherited（核对登记，预期零变更）

**Rollback mode:** `immutable-release`

**Migration and rollback:** N/A（恢复=发布修复版本，降级=回装上一 `v*` tag）

**Security and privacy:** 控制字符转义 + 限长（GC-07）；ADR-PKT-02 口径实现

**Performance budget:** O(1)；继承 GC-10

**Estimated scope:** `M`（1 落点 / 1 生产文件）

**Version increment:** `patch`（`0.22.20` → `0.22.21`，版本面五处同步 + 工具链刷新 `Cargo.lock` + `gh release create -R libra-tools/libra v0.22.21`）

**Release boundary:** `independent`（完整 C 组 + D 组取证）

**C/D coverage from:** `self`

**Granularity:** `type=implementation; axis=push ng 输入校验与渲染卫生; recovery=发布修复版本（immutable-release），降级=回装上一 tag; complete=yes; self-contained=yes; AC=5/8; VER=3/8; landing=1; prod-files=1; scope=M; deps=PKT-09; writeset=序列化于 PKT-09（同文件）; release=independent; split-from=PKT-09@R11; exception=N/A`

## 测试矩阵

R44 恢复卡补充：FIX-PKT-01 的四个新 `agent_push_seed_*` 回归及两个原 push 用例、原 clean fail-closed 用例分别以本卡 Verification 为准；下表原十四卡113个具名用例保持完整。新名字不得仅存在于本文，必须实际 listing 唯一且执行通过。

| 类别 | 必须覆盖（具名 fn，全部 `(new)`） | Target / command |
|---|---|---|
| 单元（helper） | `pkt_frame_payload_len_ok_flush_zero`、`pkt_frame_payload_len_rejects_len_below_four`、`pkt_frame_payload_len_ok_len_4_empty`、`pkt_frame_payload_len_ok_normal_and_upper_bound`、`pkt_frame_payload_len_rejects_over_0xffff` | `source .env.test && cargo test --lib pkt_frame_` |
| 单元（同步解析，PKT-02 expected 全集 9 fn） | `read_pkt_line_rejects_short_header`、`read_pkt_line_rejects_non_utf8_header`、`read_pkt_line_rejects_non_hex_header`、`read_pkt_line_rejects_len_below_four`、`read_pkt_line_rejects_truncated_frame`、`read_pkt_line_preserves_empty_buffer`、`read_pkt_line_preserves_flush`、`read_pkt_line_preserves_len_4_empty_payload`、`pkt_line_error_text_matches_protocol_classifier`（`PktLineError` 固定前缀契约 + 分类器兜底锚定） | `source .env.test && cargo test --lib read_pkt_line_` + `source .env.test && cargo test --lib pkt_line_error_text` |
| 单元（discovery 传播） | `pkt_line_discovery_propagates_pkt_line_error`、`pkt_line_discovery_marker_prefix_contract`、`pkt_line_discovery_marker_detection_semantics_starts_with`、`pkt_line_discovery_capability_echo_fixed_phrase`、`pkt_line_discovery_capability_echo_sentinel`、`pkt_line_discovery_rejects_empty_response`、`pkt_line_discovery_rejects_malformed_no_panic`、`pkt_line_discovery_valid_response_regression` | `source .env.test && cargo test --lib pkt_line_discovery_`（PKT-03 门 1-8 一一对应，R37 Codex P1-3 拆分） |
| 单元（push 家族，PKT-04 expected 全集 6 fn） | `pkt_line_push_protocol_variant_exists`、`pkt_line_push_discovery_marker_maps_to_protocol`、`pkt_line_push_cli_maps_protocol_to_lbr_net_002`、`pkt_line_push_malformed_response_unit`、`pkt_line_push_discovery_failed_regression_stays_net_001`、`pkt_line_push_network_variant_regression_stays_net_001`（后两 fn 为 `DiscoveryFailed`/`Network` 泛化分支维持 NET-001 回归，其它构造点） | `source .env.test && cargo test --lib pkt_line_push_` |
| 单元（fetch 边界） | `pkt_line_fetch_marker_maps_to_lbr_net_002`、`pkt_line_fetch_non_marker_stays_net_001`、`pkt_line_fetch_zero_echo_sentinel`、`pkt_line_fetch_classifier_fallback_anchored` | `source .env.test && cargo test --lib pkt_line_fetch_` |
| 单元（fetch 异步） | `pkt_line_fetch_async_rejects_len_below_four`、`pkt_line_fetch_async_flush_regression`、`pkt_line_fetch_async_len4_regression`、`pkt_line_fetch_async_upper_bound_regression`、`pkt_line_fetch_async_reach_pack_truncated_eof`、`pkt_line_fetch_async_helper_single_source` | `source .env.test && cargo test --lib pkt_line_fetch_async_` |
| 单元（push 收口） | `pkt_line_push_status_line_is_protocol_variant`、`pkt_line_push_status_line_locked_test_updated`、`pkt_line_push_malformed_receive_pack_end_to_end_lbr_net_002`、`pkt_line_push_status_line_zero_echo_sentinel` | `source .env.test && cargo test --lib pkt_line_push_`（与家族行合并过滤） |
| 单元（push ng 卫生） | `pkt_line_push_ng_reason_escaped_and_capped`、`pkt_line_push_refname_validated_or_protocol`、`pkt_line_push_ng_sentinel_human_and_json` | `source .env.test && cargo test --lib pkt_line_push_`（同前缀） |
| 单元（边界矩阵） | `pkt_line_matrix_ls_remote_marker_maps_net_002`、`pkt_line_matrix_clone_discovery_marker_maps_net_002`、`pkt_line_matrix_clone_fetch_phase_marker_maps_net_002`、`pkt_line_matrix_fetch_objects_marker_maps_net_002`、`pkt_line_matrix_pull_discovery_marker_maps_net_002`、`pkt_line_matrix_pull_fetch_objects_marker_maps_net_002`、`pkt_line_matrix_non_marker_ls_remote_stays_net_001`、`pkt_line_matrix_non_marker_clone_discovery_stays_net_001`、`pkt_line_matrix_non_marker_clone_fetch_phase_stays_net_001`、`pkt_line_matrix_non_marker_fetch_objects_stays_net_001`、`pkt_line_matrix_non_marker_pull_discovery_stays_net_001`、`pkt_line_matrix_non_marker_pull_fetch_objects_stays_net_001`、`pkt_line_matrix_parametrized_git_marker_maps_net_002`、`pkt_line_matrix_parametrized_git_non_marker_stays_net_001`、`pkt_line_matrix_parametrized_ssh_marker_maps_net_002`、`pkt_line_matrix_parametrized_ssh_non_marker_stays_net_001`、`pkt_line_matrix_parametrized_https_marker_maps_net_002`、`pkt_line_matrix_parametrized_https_non_marker_stays_net_001`、`pkt_line_matrix_zero_echo_sentinel`、`pkt_line_matrix_marker_constant_single_source`、`pkt_line_matrix_fetch_packet_read_non_marker_io_stays_net_001`、`pkt_line_matrix_pull_packet_read_non_marker_io_stays_net_001`、`pkt_line_matrix_fetch_packet_read_marker_maps_net_002`、`pkt_line_matrix_pull_packet_read_marker_maps_net_002`、`pkt_line_discovery_empty_response_regression_fetch_clone_lsremote_pull` | `source .env.test && cargo test --lib pkt_line_matrix_` + `source .env.test && cargo test --lib pkt_line_discovery_empty_response_regression_fetch_clone_lsremote_pull`（EX-01 门 1-25 一一对应，R37 Codex P1-3 拆分） |
| 单元（git/ssh 下界） | `pkt_line_client_git_rejects_len_below_four`、`pkt_line_client_ssh_rejects_len_below_four`、`pkt_line_client_git_flush_regression`、`pkt_line_client_ssh_flush_regression`、`pkt_line_client_git_len4_regression`、`pkt_line_client_ssh_len4_regression`、`pkt_line_client_git_upper_bound_regression`、`pkt_line_client_ssh_upper_bound_regression`、`pkt_line_client_truncated_payload_eof_net_002`、`pkt_line_client_partial_header_eof_net_002` | `source .env.test && cargo test --lib pkt_line_client_` |
| 单元（SSH pkt-line 脱敏） | `pkt_line_client_ssh_wrapper_marker_passthrough_discovery`、`pkt_line_client_ssh_wrapper_passthrough_fetch_objects`、`pkt_line_client_ssh_wrapper_passthrough_send_pack`、`pkt_line_client_ssh_inner_wrapper_marker_passthrough`、`pkt_line_client_non_marker_wrapped_regression`、`pkt_line_client_zero_echo_sentinel`、`pkt_line_client_marker_at_string_start`、`pkt_line_client_shared_helper_single_source`、`pkt_line_client_ssh_read_error_reaps_child`、`pkt_line_client_ssh_read_error_no_hang`、`pkt_line_client_ssh_frame_errors_end_to_end_net_002` | `source .env.test && cargo test --lib pkt_line_client_`（EX-03 门 1-11 一一对应，R37 Codex P1-3 拆分） |
| 单元（SSH 全路径） | `pkt_line_client_describe_status_no_stderr`、`pkt_line_client_describe_process_output_no_stderr`、`pkt_line_client_nonzero_exit_discovery_zero_stderr`、`pkt_line_client_status_task_zero_stderr`、`pkt_line_client_send_pack_zero_stderr`、`pkt_line_client_malicious_stderr_sentinel`、`pkt_line_client_debug_trace_records_status_length_digest`、`pkt_line_client_batch_mode_enforced`、`pkt_line_client_host_key_fail_closed_guidance`、`pkt_line_client_interactive_stderr_captured_sanitized`、`pkt_line_client_batch_mode_passphrase_error_points_to_agent`、`pkt_line_client_stderr_flood_capped` | `source .env.test && cargo test --lib pkt_line_client_`（同前缀） |
| 单元（头解析 marker 化） | `pkt_line_header_fetch_hex4_marker`、`pkt_line_header_git_client_header_marker`、`pkt_line_header_ssh_client_header_marker`、`pkt_line_header_three_transports_bad_header_net_002`、`pkt_line_header_non_marker_regression`、`pkt_line_header_marker_at_string_start_regression`、`pkt_line_header_git_discovery_wrapper_passthrough`、`pkt_line_header_shared_helper_single_source`、`pkt_line_fetch_async_zero_echo_sentinel`、`pkt_line_header_git_frame_errors_end_to_end_net_002` | `source .env.test && cargo test --lib pkt_line_header_` + `source .env.test && cargo test --lib pkt_line_fetch_async_` |
| 兼容（守卫） | 生产 unwrap 面（不覆盖 expect/panic——证据靠 rg 零命中；`compat_extra` 仅 PKT-01 登记） | `source .env.test && cargo test --test compat_all_production_unwrap_guard`、`source .env.test && cargo test --test compat_extra_production_unwrap_guard` |
| 集成 | ls-remote/clone/fetch/push 失败路径（mock 畸形响应；fetch 路径用 release 构建复现） | 各卡手工证据项（sanitized） |
| 迁移 / 性能 / live | N/A | — |

## 追溯表

| 任务 | 来源/证据 | Libra 落点 | 文档/兼容动作 | 指定测试 |
|---|---|---|---|---|
| FIX-PKT-01 | run 801d750c 的 push/prune retry；定向独立诊断 | `tests/command/agent_push_test.rs` + serial/nextest 登记 | fixture-only，用户/网站文档 N/A；release note 如实登记 | 四个 `agent_push_seed_*` + 原 push/clean + serial/unwrap/version 守卫 + 全量 |
| PKT-01 | plan-long SB-01（第十次审计 Top-1）；`git_protocol.rs:95,99` | `src/git_protocol.rs` | 核对登记（PKT-13 统一） | `--lib pkt_frame_` |
| PKT-02 | 同上；`git_protocol.rs:81-103` | `src/git_protocol.rs` | 同上 | `--lib read_pkt_line_` + `--lib pkt_line_error_text` |
| PKT-03 | 同上；`mod.rs:13,72-75,85-88,92,102-104,147-149` | `src/internal/protocol/mod.rs` | 同上 | `--lib pkt_line_discovery_` |
| PKT-04 | 同上；`push.rs:235,878-900,2120,2131` | `src/command/push.rs` | 同上 | `--lib pkt_line_push_` |
| PKT-05 | ER-08 / G-08；`release.yml` | 版本面五处 + `Cargo.lock` | release note/兼容证据 | D 组 8 job 记录 + 全量 |
| PKT-06 | 同上；`fetch.rs:943-957`、`https_client.rs:405` | `src/command/fetch.rs` | 同上 | `--lib pkt_line_fetch_` |
| PKT-07 | 同上；`fetch.rs:3650-3680,2349` | `src/command/fetch.rs` | 同上 | `--lib pkt_line_fetch_async_` |
| PKT-08 | 同上；`git_client.rs:151`、`ssh_client.rs:243` | `src/git_protocol.rs`、`src/internal/protocol/{git_client,ssh_client}.rs` | 同上 | `--lib pkt_line_client_`（下界+EOF 10 fn） |
| PKT-09 | 同上；`push.rs:2149-2151,3434-3443,398-403` | `src/command/push.rs` | 同上 | `--lib pkt_line_push_`（收口 4 fn） |
| PKT-10 | 同上；`ls_remote.rs:210-221`、`clone.rs:1012-1046,1059-1063`、`pull.rs:267,928-936,937-944,989-1001`、`fetch.rs:882-897` | `src/command/{ls_remote,clone,pull,fetch}.rs` | 同上 | `--lib pkt_line_matrix_` + `--lib pkt_line_discovery_empty_response_regression_fetch_clone_lsremote_pull`（门族 25 门） |
| PKT-11 | 同上；`ssh_client.rs:164-204,296-299,429-433,491-497,535-550` | `src/internal/protocol/ssh_client.rs` | 同上 | `--lib pkt_line_client_`（SSH 全路径 12 fn） |
| PKT-12 | 同上；`ssh_client.rs:234,253,273-284,319-329,449-459` | `src/internal/protocol/ssh_client.rs` | 同上 | `--lib pkt_line_client_`（pkt-line 脱敏 11 fn（含真实端到端）） |
| PKT-13 | 同上；`fetch.rs:3653-3666`、`git_client.rs:143-146,170-173`、`ssh_client.rs:235-238` | `src/git_protocol.rs`、`src/command/fetch.rs`、`src/internal/protocol/{git_client,ssh_client}.rs` | `docs/error-codes.md`/`COMPATIBILITY.md` **统一核对登记** | `--lib pkt_line_header_` + `--lib pkt_line_fetch_async_`（10 fn，含真实端到端） |
| PKT-14 | ADR-PKT-02 扩展（R10/R11）；`push.rs:2140-2147,299` | `src/command/push.rs` | 同上 | `--lib pkt_line_push_`（ng 卫生 3 fn） |

## 里程碑验收与回滚

| 里程碑 | 完成条件 | 发布/证据 | 回滚或前滚 |
|---|---|---|---|
| M0 | 锚点刷新 + ER-01 通过 | 任务记录 | N/A |
| M1 | FIX-PKT-01 与 PKT-01 均 `complete`，绑定各自实际 tag/SHA（旧 v0.22.11 无效；两份 D 组8 job记录） | red/green、version/`gh run`/review PASS | 前滚发新版本；降级=回装上一 tag |
| M2 | 家族 PKT-02/03/04 经 PKT-05 `done/complete`，`v0.22.12` | 同上 | 同上 |
| M3 | PKT-06 `v0.22.13`、PKT-07 `v0.22.14`、PKT-10 `v0.22.15` | 同上 | 同上 |
| M4 | PKT-09 `v0.22.16`、PKT-08 `v0.22.17`、PKT-12 `v0.22.18` | 同上 | 同上 |
| M5 | PKT-11 `v0.22.19`、PKT-13 `v0.22.20` | 同上 | 同上 |
| M6 | PKT-14 `v0.22.21`，全量收口门全绿 | 同上 + 全量日志 | 同上 |

### 故障恢复矩阵

| 故障点 | 可接受残留 | 恢复动作 | 禁止结果 |
|---|---|---|---|
| 畸形帧中途到达 | 连接中止、无本地状态残留 | 上抛 `PktLineError`/marker 错误（`LBR-NET-002`），操作失败返回 | 跳帧续解析 / panic / ~4GiB 分配 / 静默成功 |
| 家族子卡本地提交后发现缺陷 | 未推送、保持 `in-progress` | 家族窗口内修复后重新过 A/B 门 | 子卡单独推送（绕过发布点）/ 跳过家族树 A/B 门直标 locally-accepted |
| 发布 push 失败 | 本地已提交 | 按 ER-09（非 ff 先 pull/merge 再验收；网络类不 blind retry） | blind retry / 回退已发布 artifact |
| 已发布版本缺陷 | artifact 不可撤回 | 前滚发新版本；降级=回装上一 tag | 回退已推送提交 / 宣称撤回 artifact |
| release 次要 job 失败 | 二进制已上传 | 阻断 `complete`，前滚重发 | 带红通过 |

## 风险登记

| 风险 | 影响 | 缓解 | 任务 |
|---|---|---|---|
| 直接 seed 返回时后台索引仍持锁，或等待把终态错误藏掉 | cleanup 测试误触安全拒绝；重试绿色掩盖 fixture 缺陷 | 有界 drain + 严格 marker/I/O 检查，保留生产 fail-closed 回归及原始 retry，red/green 后重跑全量 | FIX-PKT-01（前置 PKT-01） |
| marker 前缀契约被后续代码无意破坏 | 中（归类回退 NET-001） | 三层具名锚定（解析契约 / 8 逻辑映射点全量——含 `PacketRead` 子臂细化门 PKT-10 门 3/4/21-24 / 分类器兜底）+ 常量单点；PKT-07 长度/截断错误在生产时即携带 marker，后继分类收窄不得破坏已发布测试 | PKT-02/03/06/07/10/13 |
| `PushError::Protocol` 与泛化分支边界被误改 | 中 | 泛化分支 NET-001 回归断言（收窄口径） | PKT-04/09 |
| ssh 用户可见面未来新增回显点 | 中 | describe 函数单点收口 + sentinel | PKT-11/12 |
| 零匹配空跑使 focused 门形同虚设 | 中（验收造假风险） | 失败闭合 fn 存在性脚本（comm 比对）+ `(new)` 标记 | 全部 |
| 家族 staging 流程执行偏差（跳过家族树 A/B 门） | 中（验收无效） | REL-PKT-A 窗口规则 + 故障恢复矩阵禁止项 | PKT-02/03/04/05 |
| 对宽松远端兼容性下降 | 低（仅拒绝规范禁止帧） | 合法帧回归（PKT-02/07/08） | 全部 |
| `ng` reason 透传的注入面（与 Git 上游一致） | 低（ADR-PKT-02 显式豁免） | DEFER-05 结构化展示 | — |

## 性能与容量摘要

| 操作 | 单次成本 | 累积成本 | 预算/上限 | 验证 |
|---|---|---|---|---|
| pkt-line 帧长校验 | O(1) | 与帧数线性（既有） | 无新增预算；消除 ~4GiB 误分配 | 单元回归 |
| marker 前缀检查 | O(前缀长) 常数 | 每错误一次 | 无新增预算 | 边界测试 |

## 兼容与文档收口

- [ ] `COMPATIBILITY.md` 已同步（ssh 行 BatchMode/host-key fail-closed 表述，PKT-11 承载，**禁止 N/A**；其余行由 PKT-13 统一核对登记，零变更结论亦须落盘）。
- [ ] `docs/commands/push.md` 与 `docs/commands/zh-CN/push.md` 已同步状态行归类变化（PKT-09 承载，**禁止 N/A**）。
- [ ] `docs/commands/{ls-remote,clone,fetch,pull}.md` 与 `docs/commands/zh-CN/{ls-remote,clone,fetch,pull}.md` 已同步边界协议错误归类变化（PKT-10 承载，**禁止 N/A**，R29 Codex P1-3）。
- [ ] `docs/commands/{clone,fetch,config}.md` 与 `docs/commands/zh-CN/{clone,fetch,config}.md` 已同步 BatchMode/host-key 行为（PKT-11 承载，**禁止 N/A**）。
- [ ] `docs/development/commands/{clone,fetch,config}.md` 已同步（PKT-11 承载，**禁止 N/A**）。
- [ ] `docs/error-codes.md` `LBR-NET-002` 语义行核对结论已落盘（PKT-09 核对 + PKT-13 统一登记；**禁止 N/A**——「既有语义收敛、零条目变更」的结论亦须落盘）。
- [ ] `tests/INDEX.md` 已同步，或说明 `N/A`（预期 N/A：无新增 `--test` target）。
- [ ] `Cargo.toml` `[[test]]` 已同步，或说明 `N/A`（预期 N/A）。
- [ ] `plan-long.md` 日期计划索引或 LR 状态已同步（SB-01 / 日期计划索引行）。

## Codex review log

Result 只允许 `PASS` 或 `FAIL`。`FAIL` 必须列出 P0/P1 条目并在下一轮复审关闭；P2 可由具名责任人书面接受为 residual risk，但不改变本轮 `FAIL` 记录（ER-05）。评审未产出（模型限额/基础设施故障/会话超时）的轮次 Result 一律登记为 `FAIL`，未产出原因与重跑轮次记于 P0/P1 列——此类轮次无判定效力，不计入 PASS 谱系（R24 Codex P1-5）。本计划按用户指令执行 **Codex 与 Claude 双评审**，两者均须 PASS。

| Round | Scope | Result | P0/P1 | P2 处置 | Evidence |
|---|---|---|---|---|---|
| R1 (Codex) | plan 全文 + 源码锚点 | FAIL | P1-1..8——R1 关闭 | 无 P2 | `$SCRATCH/review.codex.r1.txt` |
| R1 (Claude) | — | FAIL | 未产出（模型限额，该轮无判定；R2 起 `--model sonnet` 重跑） | — | `$SCRATCH/review.claude.r1.txt` |
| R2 (Codex) | v2 | FAIL | P1-1..8——R2 关闭 | 无 P2 | `$SCRATCH/review.codex.r2.txt` |
| R2 (Claude) | v2 | FAIL | P1 push 验证缺口（→PKT-04/09） | P2-1/2/3 采纳 | `$SCRATCH/review.claude.r2.txt` |
| R3 (Codex) | v3 | FAIL | P1-1..4——R3 关闭 | 无 P2 | `$SCRATCH/review.codex.r3.txt` |
| R3 (Claude) | v3 | FAIL | P1×2——R3 关闭 | P2×3 采纳 | `$SCRATCH/review.claude.r3.txt` |
| R4 (Codex) | v4 | FAIL | P1-1..5——R4 关闭 | 无 P2 | `$SCRATCH/review.codex.r4.txt` |
| R4 (Claude) | v4 | PASS | 无 P0/P1 | P2×2 采纳（锚点/显式边，具名接受=用户） | `$SCRATCH/review.claude.r4.txt` |
| R5 (Codex) | v5 | FAIL | P1-1 PKT-08 谓词超限（→拆 PKT-12）；P1-2 clone fetch 阶段映射缺口（→PKT-10 扩展）；P1-3 异步头解析无 marker（→PKT-13）；P1-4 capability/Content-Type 回显（→PKT-03 收口 + DEFER-06 收窄口径）；P1-5 零匹配空跑（→失败闭合脚本 + `(new)`）；P1-6 家族验收顺序不可达（→REL-PKT-A staging 流程）；P1-7 plan-long 索引不同步（→已更新）——R5 修订全部关闭 | 无 P2 | `$SCRATCH/review.codex.r5.txt` |
| R5 (Claude) | v5 | FAIL | P1-1 状态行应归类 `PushError::Protocol`（→PKT-09 重新归类 + PKT-04 回归收窄）——R5 修订关闭 | P2-1 describe 函数锚点更正（:535-537/:539-550）；P2-2 移除幽灵守卫行（PKT-06/07/08/10）；P2-3 文档收口指针移至 PKT-13——均采纳 | `$SCRATCH/review.claude.r5.txt` |
| R6 (Codex) | v6 | FAIL | 未产出（chatgpt.com 后端 404，基础设施故障，该轮无判定；R7 重跑） | — | `$SCRATCH/review.codex.r6.txt` |
| R6 (Claude) | v6 | FAIL | P1-1 marker 检测语义未定义 + SSH 包装破坏串首锚定 vs contains 可伪造（→ADR-PKT-01 裁决 `starts_with` + PKT-12 原样透传 + PKT-03 契约测试）——R6 修订关闭 | P2-1 PKT-01/06/07/13 VER 计数更正（4/3/3/3）——已修 | `$SCRATCH/review.claude.r6.txt` |
| R7 (Codex) | v7 | FAIL | P1-1 git:// 包装点 :170 破坏 starts_with（→PKT-13）；P1-2 expected 集不完整/缩写（→全名内联+矩阵展开）；P1-3 stderr 转 debug 仍为日志注入（→状态/长度/摘要）；P1-4 D 组未绑 tag ref（→固定 workflow/event/ref 查询）——R7 修订全部关闭 | P2-1 默认覆盖表补齐；P2-2 审计表同步——已修 | `$SCRATCH/review.codex.r7.txt` |
| R7 (Claude) | v7 | FAIL | P1-1 审计表未同步 R6 修订（PKT-01 VER/PKT-03 AC）；P1-2 PKT-13 依赖三处矛盾（→统一 PKT-07/10/12/11）；P1-3 PKT-02 漏 compat_extra（→补齐）——R7 修订全部关闭 | P2-1 PKT-03/04/09 补 compat_all；P2-2 标点清理——均采纳 | `$SCRATCH/review.claude.r7.txt` |
| R8 (Codex) | v8 | FAIL | P1-1 PKT-10 谓词 16 超限（→EX-01 门族 waiver）；P1-2 PKT-13 Granularity.deps 漏 PKT-07（→补齐）；P1-3 `--verify-tag` 顺序（→先 libra tag+push）；P1-4 PKT-05 未过滤查询（→--branch v0.22.12）；P1-5 脚本占位符/矩阵缩写（→内联命令+全名展开）——R8 修订全部关闭 | P2-1 git_protocol 锚点 :117-119——已修 | `$SCRATCH/review.codex.r8.txt` |
| R8 (Claude) | v8 | FAIL | P1-1 PKT-13 AC 实为 7（→7/8）；P1-2 git/ssh 旧文本缺零命中守卫（→两条大小写敏感 rg）；P1-3 PKT-05 gh run list 未过滤（→--branch v0.22.12）——R8 修订全部关闭 | P2-1 审计表 writeset 对齐；P2-2 锚点漂移批量更正；P2-3 git/ssh 头文本大小写差异注明；P2-4 DEFER-06 补分类器休眠风险——均采纳 | `$SCRATCH/review.claude.r8.txt` |
| R9 (Codex) | v9 | FAIL | P1-1 PKT-07 验收范围（→收窄移交 PKT-13）；P1-2 裸 rg/未内联脚本（→全部内联三分支与自包含脚本）；P1-3 PKT-10 exception 字段（→EX-01+门映射）；P1-4 交互路径 Stdio::inherit（→PKT-11 EX-02）；P1-5 ng reason 转义限长（→ADR-PKT-02+PKT-09）——R9 修订全部关闭 | — | `$SCRATCH/review.codex.r9.txt` |
| R9 (Claude) | v9 | FAIL | P1-1 PKT-10 exception 字段（同上）；P2×5 锚点漂移与 EX-01 归因（→批量更正）——R9 修订全部关闭 | P2×5 均采纳 | `$SCRATCH/review.claude.r9.txt` |
| R10 (Codex) | v10 | FAIL | P1-1 refname 远端可控未净化（→ADR-PKT-02 扩展+PKT-09）；P1-2 ng 转义未入 expected（→PKT-09 expected 扩展）；P1-3 PKT-07 sentinel 归属（→移入 PKT-13）；P1-4 交互 SSH 缺 BatchMode 决议（→ADR-PKT-03+EX-02 扩展）；P1-5 PKT-13 守卫裸 rg（→内联三分支）——R10 修订全部关闭 | — | `$SCRATCH/review.codex.r10.txt` |
| R10 (Claude) | v10 | FAIL | P1-1 修订历史/评审日志缺 R9（→重建补齐）；P1-2 host-key 提示渲染缺保真验收（→ADR-PKT-03 BatchMode 决议消除渲染层）——R10 修订全部关闭 | P2-1 适用范围/事实基线补 git :170-173；P2-2 PKT-06 证据分支边界更正——均采纳 | `$SCRATCH/review.claude.r10.txt` |
| R11 (Codex) | v11 | FAIL | P1-1 零回显 sentinel 双卡归属（→唯一化 PKT-13）；P1-2 EX-01 门映射不可执行（→全名命令）；P1-3 PKT-09 谓词超限（→拆出 PKT-14）；P1-4 ssh-keyscan 盲追加 MITM 风险（→指纹核对/交互 TOFU）；P1-5 PKT-11 文档面缺口（→clone/fetch/config EN+zh + COMPATIBILITY.md）；P1-6 修订历史乱序重复（→确定性重建）——R11 修订全部关闭 | — | `$SCRATCH/review.codex.r11.txt` |
| R11 (Claude) | v11 | FAIL | P0-1 修订历史乱序重复（→确定性重建）；P1-1 ADR-PKT-03 影响面表述错误+范围缺口（→更正+ssh_client.rs:164-204 纳入）；P1-2 追溯表计数漂移（→同步）；P1-3 矩阵 SSH 行重复/反引号（→修复）；P1-4 EX-02 缺判据规范块（→补齐）——R11 修订全部关闭 | P2-1 lossy 锚点 :3653-3661；P2-2 plan-long 索引补 ADR-PKT-03——均采纳 | `$SCRATCH/review.claude.r11.txt` |
| R12 (Codex) | v11 | FAIL | 未产出（900s 超时被终止，无 VERDICT，该轮无判定；R13 重跑） | — | `$SCRATCH/review.codex.r12.txt`（截断） |
| R12 (Claude) | v11 | FAIL | P1-1 PKT-12 幽灵 sentinel AC（→移除，归属唯一化 PKT-13 AC=8/8）；P1-2 PKT-11 计数/EX-02 算术矛盾（→11/8 对齐判据规范）；P1-3 发布顺序版本倒退（→单调化 PKT-14 置末）；P1-4 plan-long 索引缺 PKT-14（→补齐）；P1-5 PKT-11 漏开发文档面（→补齐）——R12 修订全部关闭 | — | `$SCRATCH/review.claude.r12.txt` |
| R13 (Codex) | v12 | FAIL | P1-1 修订历史 R12 先于 R11+完成判据口径（→更正）；P1-2 PKT-14 生命周期缺口（→补齐）；P1-3 pull.rs 映射点遗漏（→PKT-10 扩展 20 门）；P1-4 ssh read-error 挂起风险（→kill+reap AC）；P1-5 PKT-13 双过滤命令（→拆分）——R13 修订全部关闭 | P2-3 unwrap/expect/panic 守卫覆盖（→PKT-14 计划级收口守卫） | `$SCRATCH/review.codex.r13.txt` |
| R13 (Claude) | — | FAIL | 未产出（该轮无判定） | — | — |
| R14 (Codex) | v13 | FAIL | 未产出（两次 900s 超时，该轮无判定；R15 单独 30min 重跑） | — | `$SCRATCH/review.codex.r14.txt`（截断） |
| R14 (Claude) | v13 | FAIL | P0-1 修订历史 R13 先于 R12（→互换）；P1-1 PKT-14 审计行误贴追溯表+审计表缺行（→归位）；P1-2 PKT-12 计数 9/8 无 waiver（→EX-03）；P1-3 EX-01 编号不一致（→统一 1-20）——R14 修订全部关闭 | P2-1 成功定义十四张；P2-2 waiver 计数 3 项；P2-3 Cargo.toml :250-252——均采纳 | `$SCRATCH/review.claude.r14.txt` |
| R15 (Codex) | v14 | FAIL | P1-1 reach_pack EOF 语义（→PKT-07）；P1-2 空响应 fail-open（→PKT-02/03）；P1-3 EX-01 重复编号（→清理）；P1-4 EX-02 门 11 不可执行（→命令化）；P1-5 文档闭环（→push.md/config 开发文档）；P1-6 PKT-14 守卫 prose（→命令化）；P1-7 ssh-keyscan 残留（→移除）；P1-8 pull 未统一各面（→统一）——R15 修订全部关闭 | P2×2（AC 计数校准/历史行号）一并采纳 | `$SCRATCH/review.codex.r15.txt` |
| R15 (Claude) | v14 | FAIL | P1-1 追溯表 PKT-10 fn 计数（→同步 10 fn）；P2×2（成功定义/适用范围 pull 措辞）——采纳 | — | `$SCRATCH/review.claude.r15.txt` |
| R16 (Codex) | v15 | FAIL | 未产出（R16 仅 Claude 侧执行，Codex 侧该轮跳过，无判定） | — | — |
| R16 (Claude) | v15 | FAIL | P0-1 修订历史 R12-R15 物理重排；P1-1 EX-01 映射三组重复（→删除）；P1-2 ADR-PKT-03 Decision 去 ssh-keyscan；P1-3 PKT-09 文档义务归位——R16 修订全部关闭 | P2-1 G-09/完成判据改全部行；P2-2 ADR-PKT-01 Context 补 pull——均采纳 | `$SCRATCH/review.claude.r16.txt` |
| R17 (Codex) | v16 | FAIL | P1-1 clone.rs:1024-1028 ssh-keyscan 提示（→PKT-11）；P1-2 git/ssh payload 截断 EOF（→PKT-08）；P1-3 PKT-03 谓词超限（→EX-04）；P1-4 PKT-10 门数矛盾（→统一 20）；P1-5 PKT-12 exception 缺失（→EX-03）；P1-6 PKT-11 Docs/AC/写集（→一致）；P1-7 PKT-14 守卫 prose（→命令化）；P1-8 pull 未统一各面（→统一）——R17 修订全部关闭 | P2×3（passphrase/plan-long 归属/docs 命令化）——采纳 | `$SCRATCH/review.codex.r17.txt` |
| R17 (Claude) | v16 | FAIL | P0-1 R16 未登记（→补登）；P1-1 EX-01 重复行残留（→逐条唯一化）；P1-2 PKT-14 diff 无基线（→v0.22.10..HEAD）；P1-3 BatchMode passphrase 未披露（→ADR+AC）——R17 修订全部关闭 | P2-1 plan-long 归属 PKT-04/06/10；P2-2 PKT-09 docs 命令化；P2-3 pull.rs 锚点拆分——均采纳 | `$SCRATCH/review.claude.r17.txt` |
| R18 (Codex) | v17 | FAIL | P1-1 review log 缺 R16/R17（→补登）；P1-2 PKT-10 16/20 门矛盾+…缩写+重复映射（→统一 20 门全名）；P1-3 PKT-03 exception 缺失（→EX-04）；P1-4 PKT-05 gh run list 未过滤（→--branch）；P1-5 git/ssh payload EOF 分类（→typed error）；P1-6 PKT-14 diff `\|\| true`（→fail-closed）；P1-7 PKT-11 12 AC 门族（→EX-02 扩展）；P1-8 ssh stderr 无界（→64KiB 上限+洪水回归）——R18 修订全部关闭 | — | `$SCRATCH/review.codex.r18.txt` |
| R18 (Claude) | v17 | FAIL | P1-1..5 验收增补未同步测试绑定/计数（→全部同步）——R18 修订全部关闭 | — | `$SCRATCH/review.claude.r18.txt` |
| R19 (Codex) | v18 | FAIL | P1-1..8——PKT-03 EX-04 判据规范块/门数 9、PKT-08 EX-05 登记、修订历史补 R18 行、PKT-11 五处 12 门统一、PKT-09 VER=6/8、PKT-14 VER=3/8、PKT-10 checkbox 20、PKT-08 矩阵/追溯第 9 fn——R19 修订全部关闭 | — | `$SCRATCH/review.codex.r19.txt` |
| R19 (Claude) | v18 | FAIL | P0×3（PKT-03 判据规范块/PKT-08 EX-05/修订历史 R18 行）+P1×5（PKT-11 五处 12 门等）——R19 修订全部关闭 | P2×2 采纳 | `$SCRATCH/review.claude.r19.txt` |
| R20 (Codex) | v19 | FAIL | P1-1 历史 R19 行；P1-2 PKT-08 Granularity；P1-3 PKT-03 端到端归属；P1-4 fail-open 措辞；P1-5 PKT-10 门数；P1-6 逐卡 D 组；P1-7 PKT-05 tag 顺序；P1-8 文档责任；P1-9 PKT-13 deps——R20 修订全部关闭 | P2×1 landing；P2-2 scope——已修 | `$SCRATCH/review.codex.r20.txt` |
| R20 (Claude) | v19 | PASS | 无 P0/P1；P2×3 residual 由具名责任人接受 | 接受人=用户 | `$SCRATCH/review.claude.r20.txt` |
| R21 (Codex) | v20 | FAIL | P1-1..11（修订历史去重/EX-01 去重/PKT-02 措辞/PKT-10 门数/PKT-05 tag/PKT-13 deps/文档责任/PKT-11 config/PKT-14 diff fail-closed）——R21 修订全部关闭 | P2×2 | `$SCRATCH/review.codex.r21.txt` |
| R21 (Claude) | v20 | FAIL | P1-1 修订历史重复行（→去重）；P1-2 review log 缺 R19/R20（→补登）；P1-3 EX-01 门 5-6 重复（→删除）；P1-4 waiver 计数 4→5；P1-5 PKT-11 config.md 补入 | P2-1 landing 2/2；P2-2 锚点精度——均采纳 | `$SCRATCH/review.claude.r21.txt` |
| R22 (Codex) | v21 | FAIL | P1-1 push 缺 flush fail-open（→PKT-09 AC）；P1-2 PKT-10 门数/映射（→统一 20 门）；P1-3 PKT-08 计数（→9/8@EX-05）；P1-4 PKT-11 AC=13（→EX-02）；P1-5 PKT-10 scope；P1-6 PKT-14 diff 测试误报（→排除）；P1-7 历史顺序（→重建）；P1-8 集成测试——R22 修订全部关闭 | — | `$SCRATCH/review.codex.r22.txt` |
| R22 (Claude) | v21 | FAIL | P0-1 修订历史 R21 先于 R20（→严格时序重建）；P1-1 review log R20 Claude 位置（→同轮相邻重建）；P1-2 EX-01 门 5-6 重复（→逐条唯一化）——R22 修订全部关闭 | P2-1/2 锚点精度——采纳 | `$SCRATCH/review.claude.r22.txt` |
| R23 (Codex) | v22 | FAIL | P1-1..18——缺 `## 已决议设计决策` 父章节、修订历史 R22 先于 R21、review log 乱序重复、PKT-09/10/11/13 计数失真、审计表与卡面 Granularity 漂移、矩阵/追溯 fn 集合缺口、panic 守卫漏检（`panic!(` 不匹配）、D 组占位符+event/ref 未锁定、EX-01 映射重复、EX-02 门数 12≠13、PKT-09 文档检查缺口、PKT-12 sentinel 双归属、PKT-10/11 scope 失真——R23 修订全部关闭 | P2×2（PKT-10 pull 证据行、自审范围说明）一并采纳 | `/tmp/opencode/libra-review/review.codex.r23.txt` |
| R23 (Claude) | v22 | FAIL | 未产出（R23 闭环仅执行 Codex 评审，Claude 侧无判定；如需双评审补跑由用户指令触发） | — | — |
| R24 (Codex) | v23 | FAIL | P1-1..10——计划级脚本未定义 `$expected`、EX-04 门映射拼接、矩阵 PKT-02/04 集合拆行、矩阵缺 env 前缀、review log Result 违反枚举、D 组 `.jobs \| length` 误用、D 组 5 job 与 release.yml 8 job 记录实况不符、homebrew 吞错缺正向证明、PKT-10 守卫漏 pull.rs、文档收口 N/A 绕过门——R24 修订全部关闭 | P2×2（INVARIANT 锚点 :86-88、DEFER-02 补 PKT-14）一并采纳 | `/tmp/opencode/libra-review/review.codex.r24.txt` |
| R24 (Claude) | v23 | FAIL | 未产出（R24 闭环仅执行 Codex 评审，Claude 侧无判定） | — | — |
| R25 (Codex) | v24 | FAIL | P1-1..9——PKT-01 全文件守卫时点不可满足、PKT-11 debug 措辞矛盾、stderr 上限未覆盖全部捕获点、PKT-08 缺 partial-header EOF、PKT-09 写集/文档检查缺口、PKT-10 AC 标题非标准字段、依赖边幽灵/缺失与自引用、D 组 ref 未断言、log 管道未闭合——R25 修订全部关闭 | P2×1（6→8 适配点）一并采纳 | `/tmp/opencode/libra-review/review.codex.r25.txt` |
| R25 (Claude) | v24 | FAIL | 未产出（R25 闭环仅执行 Codex 评审，Claude 侧无判定） | — | — |
| R26 (Codex) | v25 | FAIL | P1-1..6——PKT-03 守卫漏实际插值点 `{other}`、PKT-05 证据表提前声称 locally-accepted、PKT-05 取证缺 total/下游名断言、PKT-06 axis 不一致、追溯表 PKT-08 9→10 fn、PKT-09 既有测试未标注口径——R26 修订全部关闭 | P2×2（GAP-03/DEFER-06 8 点口径、PKT-05 out of scope 补 PKT-14）一并采纳 | `/tmp/opencode/libra-review/review.codex.r26.txt` |
| R26 (Claude) | v25 | FAIL | 未产出（R26 闭环仅执行 Codex 评审，Claude 侧无判定） | — | — |
| R27 (Codex) | v26 | FAIL | P1-1..5——fn 存在性脚本管道失败闭合缺失、PKT-11 依赖正文 ID 混排复活幽灵边、G-03/自审 PKT-08 8 谓词口径过期、PKT-11 文档检查缺开发文档/兼容矩阵逐文件断言、review log 与修订历史映射缺口（R4 Result/R20 处置/未产出轮登记）——R27 修订全部关闭 | 无 P2 | `/tmp/opencode/libra-review/review.codex.r27.txt` |
| R27 (Claude) | v26 | FAIL | 未产出（R27 闭环仅执行 Codex 评审，Claude 侧无判定） | — | — |
| R28 (Codex) | v27 | FAIL | P1-1..4——review log R23-R27 缺同轮 Claude 行、`len∈1..3` 未绑定稳定码回归、PKT-12 wait_with_output 失败分支破坏 starts_with、host-key 分类信号未定义——R28 修订全部关闭 | P2×1（D 组裸管道 pipefail）一并采纳 | `/tmp/opencode/libra-review/review.codex.r28.txt` |
| R28 (Claude) | v27 | FAIL | 未产出（R28 闭环仅执行 Codex 评审，Claude 侧无判定） | — | — |
| R29 (Codex) | v28 | FAIL | P1-1..4——host-key 信号缺跨层契约、PacketRead 非 marker IO 误归类与成功定义冲突、PKT-10 文档责任缺口、PKT-14 diff 守卫漏未提交改动——R29 修订全部关闭 | P2×1（PKT-11 守卫三分支化）一并采纳 | `/tmp/opencode/libra-review/review.codex.r29.txt` |
| R29 (Claude) | v28 | FAIL | 未产出（R29 闭环仅执行 Codex 评审，Claude 侧无判定） | — | — |
| R30 (Codex) | v29 | FAIL | P1-1..7——host-key carrier 非唯一契约、SSH 内层包装点未纳入、fetch/pull PacketRead 门未闭环、PKT-11/PKT-14 守卫在 bash -e/pipefail 下失活、Homebrew 正向证明走 log 不可行且 grep -Fq 有 SIGPIPE 假失败——R30 修订全部关闭 | P2×1（PKT-13 依赖正文 ID 重复）一并采纳 | `/tmp/opencode/libra-review/review.codex.r30.txt` |
| R30 (Claude) | v29 | FAIL | 未产出（R30 闭环仅执行 Codex 评审，Claude 侧无判定） | — | — |
| R31 (Codex) | v30 | FAIL | P1-1..3——fetch/pull PacketRead marker 正向断言缺失、verify-homebrew-formula 吞错面无 artifact 级正向证明、文档责任分工矛盾（PKT-06/PKT-10/PKT-13）——R31 修订全部关闭 | P2×2（run id null 拒绝、总述补 pull）一并采纳 | `/tmp/opencode/libra-review/review.codex.r31.txt` |
| R31 (Claude) | v30 | FAIL | 未产出（R31 闭环仅执行 Codex 评审，Claude 侧无判定） | — | — |
| R32 (Codex) | v31 | FAIL | P1-1..7——四处围栏闭合行带尾随文本、PKT-14 多段 grep 错误码掩蔽、PKT-04/06/07/08/13 文档责任跨卡延迟违反 G-02/GC-05、PKT-05 sentinel 两分支契约不可同时满足、PKT-10 标题 20 门/三个 CLI 过期、PKT-05 验证项含省略号与模板变量、修订历史缺 R29-R31 Claude 登记——R32 修订全部关闭 | 无 P2 | `/tmp/opencode/libra-review/review.codex.r32.txt` |
| R32 (Claude) | v31 | FAIL | 未产出（R32 闭环仅执行 Codex 评审，Claude 侧无判定） | — | — |
| R33 (Codex) | v32 | FAIL | P1-1..5——三处裸管道失败掩蔽、PKT-13 双命令无失败串联、host-key 提取层错误（外层 Display 破坏串首锚定）、GC-05 开发文档/COMPATIBILITY 责任缺口、文档门仅通用字样可被既有文档假满足——R33 修订全部关闭 | 无 P2 | `/tmp/opencode/libra-review/review.codex.r33.txt` |
| R33 (Claude) | v32 | FAIL | 未产出（R33 闭环仅执行 Codex 评审，Claude 侧无判定） | — | — |
| R34 (Codex) | v33 | FAIL | P1-1..2——粒度审计表 PKT-04 AC 9/8 与卡面 8/8 矛盾、PKT-05 workflow 改动未入写集且 Out of scope 矛盾——R34 修订全部关闭 | P2×1（8 映射点逻辑口径澄清）一并采纳 | `/tmp/opencode/libra-review/review.codex.r34.txt` |
| R34 (Claude) | v33 | FAIL | 未产出（R34 闭环仅执行 Codex 评审，Claude 侧无判定） | — | — |
| R35 (Codex) | v34 | FAIL | P1-1..2——`gh run list --branch` 对 tag 推送不可靠且未绑定 commit SHA、PKT-06/07/08/09/10/11 共享文档面缺显式串行边（G-10）——R35 修订全部关闭 | 无 P2 | `/tmp/opencode/libra-review/review.codex.r35.txt` |
| R35 (Claude) | v34 | FAIL | 未产出（R35 闭环仅执行 Codex 评审，Claude 侧无判定） | — | — |
| R36 (Codex) | v35 | FAIL | P1-1..2——PKT-05 sentinel 残留 `--branch` 查询、gh 命令未显式绑定仓库——R36 修订全部关闭 | P2×1（PKT-09 Docs impact 补 COMPATIBILITY）一并采纳 | `/tmp/opencode/libra-review/review.codex.r36.txt` |
| R36 (Claude) | v35 | FAIL | 未产出（R36 闭环仅执行 Codex 评审，Claude 侧无判定） | — | — |
| R37 (Codex) | v36 | FAIL | P1-1..6——run 选取缺 headBranch 绑定/sentinel 缺 watch、`gh release create` 未绑定仓库、waiver 门与 fn 非一一对应、G-08 生命周期缺 ER-05 PASS 门、表格内字面管道符未转义、章节层级偏离模板——R37 修订全部关闭 | 无 P2 | `/tmp/opencode/libra-review/review.codex.r37.txt` |
| R37 (Claude) | v36 | FAIL | 未产出（R37 闭环仅执行 Codex 评审，Claude 侧无判定） | — | — |
| R38 (Codex) | v37 | FAIL | P1-1..4——run 选取 jq 读取未请求的 headBranch 字段、sentinel 未定义 `$ver`、sentinel 下载前缺 run 完成等待、PKT-10 文档检查围栏闭合行带尾随文本——R38 修订全部关闭 | P2×1（D 组标题文案同步 artifact+tap）一并采纳 | `/tmp/opencode/libra-review/review.codex.r38.txt` |
| R38 (Claude) | v37 | FAIL | 未产出（R38 闭环仅执行 Codex 评审，Claude 侧无判定） | — | — |
| R39 (Codex) | v38 | FAIL | P1-1..2——PKT-05 主取证块未定义 `$ver` 残留、sha/typ 提取管道缺显式失败闭合——R39 修订全部关闭 | P2×1（pull.rs 证据区间更正）一并采纳 | `/tmp/opencode/libra-review/review.codex.r39.txt` |
| R39 (Claude) | v38 | FAIL | 未产出（R39 闭环仅执行 Codex 评审，Claude 侧无判定） | — | — |
| R40 (Codex) | v39 | **PASS** | 无 P0/P1/P2——模板结构、机械一致性（AC/VER/审计表/矩阵/依赖边 22 条/版本窗口/waiver 门族）、Verification（37 个 bash 块 `bash -n` 通过、失败闭合完整）、源码锚点（13 文件零漂移）、安全口径（fail-closed/零回显/marker 串首契约）、发布面（G-08 生命周期、8 job 记录、SHA/ref 绑定）全部核查通过 | — | `/tmp/opencode/libra-review/review.codex.r40.txt` |
| R40 (Claude) | v39 | FAIL | 未产出（R40 闭环仅执行 Codex 评审并取得 PASS；Claude 侧按用户指令范围未执行，无判定） | — | — |
| R41 (Claude Code) | 开工刷新 @4b96a52，Rust/测试/配置与670ac39相同 | FAIL | B1 未接入 helper 可见性/marker dead_code；B2 后继全文件守卫；L1 空响应 CLI 验收时点；L2 读取循环/真实传输分层；L3 共享 helper 写集；L4 error-codes 写集；L5 PKT-05 默认冲突——R42 修订回应，待复审 | P2×6：源码行号按各卡开工刷新，其余在 R42 明确处理或保留实际 artifact 证明义务 | `/mnt/gitmono/tmp/libra-pkt-execution.mc0nXP/r41-preflight-review.log` |
| R42 (Claude Code) | R42 执行修订 | FAIL | R41 B1/B2、L1–L4 已关闭；PKT-01 READY=YES。新增 F1：PKT-07 长度/截断 IO 未明确携带 marker，PKT-10 分类收窄会回退稳定码——R43 补原始 carrier + 稳定码双断言，待后续复核 | F2–F9：PKT-01 sentinel 例外按切片绑定、PKT-05 安全/性能正文、waiver 具名批准、共享写集说明、E2E 注入机制、各卡文档/源码刷新与 raw payload 判定；不得把本轮 FAIL 记为全计划 PASS | `/mnt/gitmono/tmp/libra-pkt-execution.mc0nXP/r42-preflight-review.jsonl`（终止0，result.subtype=success；审查结论 FAIL） |

| R43 (Claude Code) | PKT-01 实现/测试/范围 + R42 F1 定向复核 | PASS | PKT-01 无代码 P0/P1，R43 已关闭 PKT-07→10 marker 保持性 F1；非全计划最终审查 | 两项 P2 证据已由现有进程终态与 Git +106/-0 差异补齐，P3 由 Codex 具名记录取舍；C/D 仍待完成 | `/mnt/gitmono/tmp/libra-pkt-execution.mc0nXP/pkt01-review.txt`、`pkt01-review-resolution.md` |
| R44 (Codex) | FIX-PKT-01 卡面及前置恢复顺序，G-01..G-11 源码/结构复核 | PASS | 单一 fixture 行为轴，独立发布且不推迟 bump；I 与 PKT 主轴不重叠、R 串行；依赖单向，原十四卡及全部行为门保留；不是 FIX 实现/最终验收 PASS | 独立诊断已结束，red/green、实现审查、C/D 仍待完成；不把原失败的具体交错推导写成已抓到锁快照 | `agent-push-race-review.txt`、本卡 AC/写集/恢复窗口；结构脚本不代替语义门 |
| R45 (Codex) | FIX-PKT-01 实现、受控red/green、serial数量同步及本地门 | PASS | 生产源码零变更；7项focused及四guard全部判据、限定Clippy的实际退出已核实；无P0/P1 | Claude P2/P3按卡内与disposition记录具名处置；post-bump全量/C/D不在本次PASS内 | red/green/guard/clippy日志与独立JUnit；`fix-pkt01-review-disposition.md` |
| R45 (Claude Code) | FIX-PKT-01 源码、差异和本地执行摘要 | PASS | 字面 `VERDICT: PASS`，remaining P0/P1 none；正常退出0，543.682s/35 turns；独立源代码检查，不宣称亲自运行测试 | P2-1诊断模式env隔离及P3-2..6已由Codex逐项记录；不替代最终全量和发布审查 | `fix-pkt01-implementation-review.jsonl` / `.txt` 与disposition |

| R46 (Codex) | 全量终态/umask归因/私有脚本修正 | PASS | 源码/进程mask/同一缓存二进制red-green与完整失败JUnit相互吻合；不修改产品权限或弱化断言；全量仍须重跑 | 目录权限/独立证据名/终态归档前置已实接线；scope仅执行harness，非全量或发布PASS | `fix-pkt01-failed-full-summary.json`、`umask-diagnosis/codex-evidence.md` |
| R46a (Claude Code) | 私有umask脚本诊断 | PASS | 字面PASS但列P1-a/b/c；执行者按未闭环处理，未据此放行 | 三项在R46b关闭；不把首轮的PASS标题当作无P1 | `umask-diagnosis/review.json` / `.txt` |
| R46b (Claude Code) | 上述三项定向复核 | PASS | 字面no P0 and no P1 remaining；退出0，64.545s/4turns | P2证据措辞修正；目录保护边界和归档存在性限制如实登记，完整重跑/C/D仍待执行 | `umask-diagnosis/review-followup.json` / `.txt` |

## 非目标与延后项

| ID | 延后内容 | 原因 | 重启条件 | 承接位置 |
|---|---|---|---|---|
| DEFER-01 | `git_internal` 写 / index-pack delta 防护及 `GitError` 新变体 | 外部依赖，无改动权 | 上游升级窗口或 vendoring 决策 | 独立计划/ADR |
| DEFER-02 | SB-01 其余面（DB / HEAD / ToolRegistry fallible） | 单一行为轴拆分 | PKT-01..14 发布后按 SB-01 判据继续 | 后续日期计划 |
| DEFER-03 | CHANGELOG 0.22.1..0.22.10 文档债 | 与本行为轴无关 | 下一次 release 收口窗口 | 独立 docs 卡 |
| DEFER-04 | `RemoteDiscoveryError` 全面类型化迁移 | 跨模块 API 重构；marker 适配已达成同等用户可见行为 | 按 ADR-PKT-01「Revisit when」立项 | 独立计划/ADR |
| DEFER-05 | `PushError::RemoteRefUpdateFailed.reason` 结构化展示（`--json` 分离 reason 字段） | 现状与 Git 上游一致（ADR-PKT-02） | 结构化错误展示立项时 | 独立计划 |
| DEFER-07 | 受限终端中介（pty 代理 + 逐行过滤）承载 SSH host-key 交互确认 | 复杂度高，超出本计划单一行为轴；ADR-PKT-03 已 fail-closed | 上游提供结构化 host-key 确认接口，或自动化场景强需求 | 独立计划/ADR |
| DEFER-06 | 既有未触碰回显面收口（`https_client.rs:390-392` 远端 `Content-Type` 回显等） | 不在本计划写集内；本计划「零回显」口径限定于新写/改写文本（R5 Codex P1-4 认可的收窄路径）。**注（R8 Claude P2-4）**：分类器兜底列表含字面量 `content-type`（`error.rs:1692`），当前因 8 个映射点的显式分支先于分类器命中而未激活；未来重构显式分支顺序时不得无意打开该口子 | 下一轮错误文本卫生计划 | 独立 docs/代码卡 |

## 完成判据

计划只有在以下条件全部满足后才能标记完成：

- [ ] 所有任务卡满足粒度规则 `G-*`：无未登记的 L 例外、无 XL 卡、无碎片卡、家族/合并发布均已登记 `REL-PKT-A`（含 staging/验收流程）、写集冲突已消解；「任务卡粒度审计表」已填齐（含 @R 世代谱系）。
- [ ] FIX-PKT-01、PKT-01、PKT-05..PKT-14 `Lifecycle=done` 且 `Acceptance=complete`（各自含 D 组8 job记录证据）；家族子卡 PKT-02/03/04 经家族树 A/B 门与 PKT-05 C/D 覆盖转为 `done/complete`；原十四卡不因恢复卡新增而删减。
- [ ] 所有任务的 Verification 命令（含失败闭合 fn 存在性脚本）已运行并记录结果。
- [ ] **计划完成门 / 全量收口门（ER-13）**：全部任务卡完成后运行一次 `cargo +nightly fmt --all --check`、`cargo clippy --all-targets --all-features -- -D warnings`、`source .env.test && cargo test --all` 全绿；全量暴露的 Bug 已全部前滚修复并重跑至全绿（各卡 T-1/T-2 已跑全量，收口门为最终兜底）。
- [ ] 必要的 docs/compat/error-code/test-index 更新已完成（含 N/A 结论登记）。
- [ ] 每张卡的 `Rollback mode=immutable-release` 已被实际验证或记录为不可验证的原因。
- [ ] **Codex review 与 Claude review 最终结论均为 `PASS`**，P0/P1 全部关闭；仅 P2 residual risk 允许保留且有具名接受人。
- [ ] 如有发布要求：每个发布切片已 `patch + 1`、版本面五处一致、`Cargo.lock` 仅由工具链刷新、构建/安装/提交/推送完成，且已按「发布窗口顺序」行以各切片 tag（v0.22.11..v0.22.21）完成 `gh release create` 触发 `release.yml` 并取得 D 组发布证据（ER-08，取证命令见「D 组逐 job 收口循环」）。
- [ ] 「修订历史」已记录成稿后的全部规范性变更（G-09），含修订历史全部行（R1 起全部评审轮次）触发的拆分谱系与归属唯一化决议。
- [ ] `plan-long.md` 相关状态或日期计划索引已同步（SB-01 / 日期计划索引行）。
