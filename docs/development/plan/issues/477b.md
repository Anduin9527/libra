# Issue #477b：历史改写命令族收口后续 — 新缺陷跟进计划（2026-09-24）

> 模板版本：v2.6；继承 [plan-template.md](../plan-template.md) 的 GC-01..13、ER-01..13、G-01..11。
> 来源：与 [issues/477.md](477.md)（已收口，v0.22.49）同族；本计划承接 #477 收口后在 libra **0.23.49** 全量双轨跑中发现的**新缺陷**。
> 状态：设计计划（草稿）；全部任务卡 `pending`，尚未进行 Codex review。

## 文档职责

本计划承接 **#477（历史改写命令族，已收口）** 之后遗留/新发现的缺陷。`#477` 计划（`commit / branch / switch / checkout / tag / merge / rebase / reset / cherry-pick / revert`）已于 v0.22.49 收口并关闭，但其在 libra 0.23.49 全量双轨跑（TC 1..3603）中仍暴露出新的失败。本计划对这些新缺陷做**归并与归属决策**并承接实现，避免放任为新无主缺口。

### 归属决策（本次评估结论）

| 新 Issue | 命令 | 归属 | 理由 |
|---|---|---|---|
| #528 | cherry-pick（on detached HEAD） | **并入 #495**（cherry-pick/revert 子追踪） | #495 正是 #477 收口后专门跟踪 cherry-pick/revert 序列状态与未合并索引的子 issue；#528 属其范畴 |
| #522 | checkout | **本计划（477b）** | #495 只覆盖 cherry-pick/revert；checkout 不在其内 |
| #523 | rebase | **本计划（477b）** | `rebase --rebase-merges` 等新缺口，超出 #495 |
| #525 | commit | **本计划（477b）** | `commit -S`/`--pathspec-from-file`/消息文件缺口，超出 #495 |
| #526 | branch | **本计划（477b）** | branch/upstream 缺口，超出 #495 |
| #527 | merge | **本计划（477b）** | merge 冲突/选项缺口，超出 #495 |
| #533 | tag | **本计划（477b）** | tag 目标/`--points-at` 缺口；与 [issues/498.md](498.md)（tag 目标参数）协调 |
| #536 | reset | **本计划（477b）** | reset 参考解析/锁定分支缺口；与 [issues/479.md](479.md)（plumbing）协调 |

> 结论：**#528 并入 #495（cherry-pick/revert）**；**#522/#523/#525/#526/#527/#533/#536 由本计划（477b）承接**。无需另开“history-rewrite follow-up”单列计划——本文件即该后续计划。

### 适用范围

- `checkout / rebase / commit / branch / merge / tag / reset` 在 0.23.49 上的新失败（#522/#523/#525/#526/#527/#533/#536）。
- 与 #477 已收口实现的行为回归/文案/选项契约差异。
- 与相关家族计划（#478 log/diff/grep、#479 plumbing、#498 tag、#495 cherry-pick/revert）的边界。

### 非目标

- cherry-pick/revert（#495 承接）。
- `log/diff/grep/blame/format-patch/am/apply` 等（#478 承接）。
- plumbing（cat-file/update-index/update-ref/ls-files/rev-parse/read-tree，由 #479 承接）。

### 成功定义

- 本计划清单内的每条新失败在对应命令上被对齐 Git 或明确归类为 unsupported（含文档/Compatibility 归类）。
- 与 #495/#478/#479/#498 无重复承接；跨计划依赖已登记。
- 各任务卡可独立验收；完成判据全勾选后才标记完成。

## 事实基线

| 类别 | 当前事实 | 证据 |
|---|---|---|
| 来源 | libra 0.23.49 全量双轨跑 TC 1..3603（git 对照 2.55.0.782 vs libra 0.23.49）；FAIL 2877 | `libra-testcases/results/libra-0.23.49/notes.md` |
| checkout | 260 个 FAIL：`local changes would be overwritten`、pathspec 解析、`file` 参数 | #522 |
| rebase | 112 个 FAIL：`--rebase-merges` 不支持、`--apply/--merge/-x/-m/--signoff` 被拒 | #523 |
| commit | 103 个 FAIL：`-S`、`--pathspec-from-file`、消息文件 `-` 读取失败 | #525 |
| branch | 88 个 FAIL：branch 已存在、对象名解析、`--create-reflog` | #526 |
| merge | 67 个 FAIL：冲突 banner、`--verbose`、`--no-signoff` | #527 |
| tag | 65 个 FAIL：`--points-at` 重复、对象名解析、位置参数 | #533 |
| reset | 111 个 FAIL：`invalid reference`、锁定分支拒绝 | #536 |

### 当前缺口

- 上述命令在 #477 收口后仍存在的行为/选项/引用解析缺口（详见各 Issue 的 comment 场景）。

## 与其它计划的关系

- **#495**：cherry-pick/revert 子追踪 — #528 并入；本计划不含 cherry-pick/revert。
- **#478**：log/show/diff/grep/blame/notes/reflog（含 format-patch/am/apply） — 与 #523/#525 的部分输出/选项无关，边界分明。
- **#479**：plumbing — #536 reset 的引用解析与 #479 的 `rev-parse :<path>`/`update-ref` 共享参考解析逻辑。
- **#498**：tag 目标参数 — 与 #533 的 tag 目标/`--points-at` 在同一命令族，作为子计划协调，避免重复。

### 依赖登记表

| 依赖 | 类型 | 说明 |
|---|---|---|
| DEP-477b-01 | 复用 | 参考/`rev-parse` 解析与 #479 共享（#536/#533） |
| DEP-477b-02 | 串行 | tag 目标与 `issues/498.md` 写集串行（#533 与 #498 合并验收） |
| DEP-477b-03 | 边界 | cherry-pick/revert 归 #495，不重复（#528） |

## 任务卡

### Task HW-01: 复核各新 Issue 的失败模式并归类
- 输入：#522/#523/#525/#526/#527/#533/#536 的失败场景。
- 输出：每条失败是「对齐 Git」还是「明确 unsupported（含文档归类）」，形成 ADR-HW-01。
- 验收：与 #495/#478/#479/#498 无重复。

### Task HW-02: checkout（#522）行为修复
- 输出：`local changes would be overwritten`、pathspec 解析、`file` 参数与 Git 对齐。
- 验收：对应 Git 上游场景通过 + 回归守卫。

### Task HW-03: rebase（#523）/ commit（#525）
- 输出：`--rebase-merges`、`--apply/--merge`、`-S`、`--pathspec-from-file`、消息文件读取对齐。
- 验收：对应上游场景通过 + 回归守卫。

### Task HW-04: branch（#526）/ merge（#527）
- 输出：branch 已存在/upstream、merge 冲突/选项对齐。
- 验收：对应上游场景通过 + 回归守卫。

### Task HW-05: tag（#533）+ 目标参数（与 #498 协调）
- 输出：tag `--points-at`/对象解析/位置参数对齐；与 #498 合并验收。
- 验收：对应上游场景通过 + 回归守卫。

### Task HW-06: reset（#536）
- 输出：`invalid reference`、锁定分支拒绝与 Git 对齐（参考 #479 复用）。
- 验收：对应上游场景通过 + 回归守卫。

### Task HW-07: 发布与文档收口
- 输入：上述实现。
- 输出：相关文档、`COMPATIBILITY.md`、发布 + 证据；与 #495/#478/#479/#498 边界清点。
- 验收：完成判据全勾选。

## 里程碑

- M1：HW-01 `done`（归类/决策）。
- M2：HW-02..06 `done`（各命令修复）。
- M3：HW-07 `done`（发布/文档收口）。

## Codex review log

（设计计划，尚未开始。）
