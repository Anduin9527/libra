# Issue #468：Data collection and refinement 计划（2026-09-24）

> 模板版本：v2.6；继承 [plan-template.md](../plan-template.md) 的 GC-01..13、ER-01..13、G-01..11。
> 来源：[libra-tools/libra#468](https://github.com/libra-tools/libra/issues/468)（Data collection and refinement）；关联 [#500](https://github.com/libra-tools/libra/issues/500)（中央化统计存储）、[#451](https://github.com/libra-tools/libra/issues/451)（version-aware memory）。
> 状态：设计计划（草稿）；全部任务卡 `pending`，尚未进行 Codex review。

## 文档职责

本计划承接 issue #468：为 libra 的 Agent/统计/诊断数据建立**采集与精炼（collection and refinement）**管道，即如何采集会话/诊断/性能/记忆数据，并做去噪、分型、聚合，供后续统计存储（#500）与 episode memory（#451）使用。目标是让数据采集具备可观测、可精炼、可复用的结构，而非散落的日志。

本文只规划任务，不宣称实现完成。落地时每个任务都必须先刷新源码锚点，再按任务卡验收。

### 适用范围

- 数据源：会话/诊断/性能/metrics/记忆事件的采集点。
- 精炼：去噪、归一、字段校正、聚合（agg 与 dedup）。
- 输出：面向 #500 统计存储与 #451 episode memory 的数据契约；与 `../libra-backend` 的衔接。
- 相关文档与测试。

### 非目标

- 中央化统计存储本身（#500）：本计划只做采集与精炼管道。
- 完整记忆重设计（plan-20260819 R30）：本计划的数据采集边界与之衔接，但不重复其设计。

### 成功定义

- 采集点覆盖主要数据源，输出结构化数据；精炼可去噪/归一/聚合。
- 与 #500 统计存储、#451 episode memory 的数据契约一致；端到端可用。
- 提供采集/精炼测试与文档；相关约束登记。
- 各任务卡可独立验收；完成判据全勾选后才标记完成。

## 与其它计划的关系

- **#500**：中央化统计存储 — 本计划是其上游采集/精炼管道；两者数据契约一致。
- **#451**：version-aware memory — 本计划提供 episode 数据源。
- **plan-20260819**：记忆重设计 — 采集边界协调。

### 依赖登记表

| 依赖 | 类型 | 说明 |
|---|---|---|
| DEP-468-01 | 衔接 | 与 #500 统计数据契约一致 |
| DEP-468-02 | 衔接 | 与 #451 memory 数据源一致 |

## 任务卡

### Task DC-01: 盘点数据源与采集点
- 输出：数据源清单与采集点；统一入口。
- 验收：覆盖主要数据源，形成 ADR-DC-01。

### Task DC-02: 精炼管道（去噪/归一/聚合）
- 输出：精炼逻辑与数据契约；测试覆盖。
- 验收：去噪/归一/聚合有效，输出可消费。

### Task DC-03: 与 #500/#451 衔接
- 输出：接通统计存储与 episode memory 的数据流。
- 验收：端到端场景通过。

### Task DC-04: 文档与收口
- 输出：文档、测试、发布/证据。
- 验收：完成判据全勾选。

## 里程碑

- M1：DC-01 `done`（盘点）。
- M2：DC-02/03 `done`（管道 + 衔接）。
- M3：DC-04 `done`（文档收口）。

## Codex review log

（设计计划，尚未开始。）
