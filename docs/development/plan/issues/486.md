# Issue #486：upstream ahead/behind 计数修复计划（2026-09-14）

> 模板版本：v2.6；继承 [plan-template.md](../plan-template.md) 的 GC-01..13、ER-01..13、G-01..11。
> 来源：[libra-tools/libra#486](https://github.com/libra-tools/libra/issues/486)。
> 状态：设计计划；任务卡 `pending`，尚未进行 Codex review（见「Codex review log」）。

## 文档职责

本文解决 issue #486：`libra status` 与 `libra branch -vv` 报告的 upstream ahead/behind 计数错误。只要本地分支与 upstream 共享历史且两者不相等，计数就会失真：upstream 是本地的直接祖先时仍报告 `behind N`，本地落后时报告大量虚假的 `ahead`，真正分叉时两侧都偏大。目标是用单一、可证明正确的 commit 图算法替换 `src/command/status.rs::compute_ahead_behind`，让所有渲染器输出与 `rev-list --left-right --count` 及 Git 一致的计数。

本文只规划任务，不宣称实现完成。落地时必须先刷新源码锚点，再按任务卡验收。

### 适用范围

- ahead/behind 计数算法：新增 `src/internal/merge_base.rs::ahead_behind`，与 merge-base 共用 commit 图遍历核心。
- 消费者：`status` 的 `--short --branch` / porcelain v1、porcelain v2 的 `# branch.ab`、长格式 tracking 句子、`--json` 的 `upstream` 对象，以及 `branch -vv` 的 tracking 段。
- 计数不可得的形态（shallow 边界、缺失 commit 对象、unborn 分支）的输出契约。
- 同步：`docs/commands/status.md`、`docs/commands/branch.md` EN + zh-CN，`docs/development/commands/{status,branch}.md`，`COMPATIBILITY.md` 的 `status`/`branch` 行，以及 `../libra-backend` 网站文档。

### 非目标

- commit-graph generation 读取器与基于 generation 的提前终止（DEFER-01）。
- `--no-ahead-behind` 跳过遍历（Git `AHEAD_BEHIND_QUICK` 的 `[different]`）以及 JSON 契约调整（DEFER-02）。
- 把 `rev-list --left-right --count` 与 `log A...B` 迁移到新 helper（DEFER-03）。
- 长格式 tracking 句子的措辞对齐（如 Git 的 “and can be fast-forwarded” 与 pull hint，DEFER-04）。
- 让 `merge_bases` / `is_ancestor` 感知 shallow 边界（DEFER-05）。

### 成功定义

- issue 复现形态：upstream 是本地的直接父提交时，`status --short --branch` 输出 `## main...origin/main [ahead 1]`，porcelain v2 为 `# branch.ab +1 -0`，JSON 为 `ahead: 1, behind: 0`，`branch -vv` 为 `[origin/main: ahead 1]`。
- 落后、分叉、相等、含 merge 的历史，其计数与 `libra rev-list --left-right --count <upstream>...HEAD` 及 git 2.54 一致。
- 计数不可得时不再输出虚假数字：porcelain v2 省略 `# branch.ab`，JSON 为 `null` 并附 warning。
- 仓库中只剩一个 ahead/behind 实现；merge-base 既有行为与性能上界不变。
- 本卡完成 C 组发布（`patch + 1` + `gh` 触发 `release.yml`）并取得 D 组证据，然后全量收口门全绿，本计划才可标记完成。

## 事实基线

> 核对日 2026-09-14。行号在开工当天刷新。核对时 `libra status --short --branch` 为 `## main...origin/main`，另有未跟踪的 `docs/development/plan/issues/483.md`、`484.md`（其它未开工计划）；`Cargo.toml` 版本 `0.22.26`，本机安装 `libra` 为 `0.22.25`。

| 类别 | 当前事实 | 证据 |
|---|---|---|
| 复现（ahead） | remote 20～30 个线性提交，clone 后本地再提交 1 个：`status --short --branch` → `[ahead 1, behind 29]`；porcelain v2 → `# branch.ab +1 -21`（20 提交历史）；长格式 → “have diverged, and have 1 and 21 different commits each”；JSON → `"ahead":1,"behind":21`；`branch -vv` → `[origin/main: ahead 1, behind 29]`；`rev-list --left-right --count origin/main...main` → `0 1`（正确） | 2026-09-14 scratch 实测（`libra 0.22.25`） |
| 复现（behind） | remote 再前进 2 个提交并 fetch：`status -sb` → `[ahead 19, behind 2]`（正确为 `behind 2`）；`rev-list` → `2 0` | 同上 |
| 复现（分叉） | 本地回退 2 个提交后再提交 2 个（相对 upstream：本地独有 2、upstream 独有 4）：`status -sb` → `[ahead 19, behind 4]`（正确为 `[ahead 2, behind 4]`，仅 ahead 错）；`rev-list --left-right --count origin/main...main` → `4 2`（左侧为 upstream 独有） | 同上 |
| 复现（含 merge） | remote 历史含一次 merge，本地再提交 1 个：`[ahead 1, behind 39]` | 同上 |
| 相等 | `local == remote` 时短路返回 `(0, 0)`，输出正确 | 同上；`src/command/status.rs:6256-6258` |
| 缺陷根因 | `compute_ahead_behind` 为交替 BFS：一个 commit 被对侧发现后改记为 common，但 commonness 从不传播给它的祖先；对侧已入队（及之后继续展开）的祖先全部留在 `remote_only` / `local_only`。一侧命中 common 后不再展开，另一侧却会走完全部历史 | `src/command/status.rs:6242-6308` |
| 其它缺陷 | `Commit::try_load` 失败时静默停止该分支遍历（shallow 与真实缺失不作区分，结果静默偏小）；doc 注释声称的「减少冗余工作」不成立 | `src/command/status.rs:6279`、`:6296`、`:6242-6254` |
| 调用方 | `resolve_upstream_info`（status 全部渲染器与 JSON 共用）；`branch_upstream_segment`（`branch -vv`）。无其它调用方 | `src/command/status.rs:6232`（函数 `:6163`，调用点 `:1567`、`:3537`）；`src/command/branch.rs:2109` |
| 渲染器 | 短格式对 `None` 计数使用 `unwrap_or(0)`，与「相等」无法区分；porcelain v2 对 `None` 输出 `# branch.ab +0 -0`（unborn 分支上已是虚假数据）；长格式对 `None` 不输出 tracking 句子 | `src/command/status.rs:6044-6090`（`:6069-6071`）、`:6099-6135`、`:4379-4400` |
| `--no-ahead-behind` | 只抑制文本输出；计数仍被计算，JSON 仍输出 `ahead`/`behind` | `src/command/status.rs:228-231`；实测 `--json status --no-ahead-behind` 仍含 `"ahead":1,"behind":21` |
| 共享算法 | `src/internal/merge_base.rs` 是 merge-base / diff `A...B` / merge / rebase / am 的单一实现：committer-date 优先队列、`Paint{lhs,rhs,stale}` 标记、保守排空（无 generation 读取器，不做提前退出），读取上界 `3 × (commits + edges)`；`CommitSource` trait + 测试用 `TestGraph` + 12 000 提交规模基准 | `src/internal/merge_base.rs:1-45`、`:60-200`（`CommitSource` `:76`、`Paint` `:113`）、`:209-293`（`paint_down_to_common_many`）、`:330`、`:469-540`（`TestGraph`）、`:986-1040`（基准） |
| shallow 边界 | 单一解析器 `read_shallow_boundaries()`；rev-list 在排除侧容忍缺失对象 | `src/command/fetch.rs:2698-2720`；`src/command/rev_list.rs:495-505` |
| 读取策略 | tiered 存储的 `LIBRA_READ_POLICY=offline|local` 可使远端层 commit 在本地不可得 | `src/utils/read_policy.rs:3-71` |
| 测试为何未发现 | porcelain v2 用例只覆盖相等（`+0 -0`，走短路）；JSON 用例的 upstream 指向根提交的父关系（无祖先可误计）；`branch -vv` 用例的 tracking ref 未 fetch | `tests/command/status_test.rs:714-787`（`test_status_porcelain_v2_branch_metadata_includes_upstream_counts`）；`tests/command/status_json_test.rs:162-207`；`tests/command/branch_test.rs:2283-2320`（`branch_vv_shows_upstream_segment`） |
| 文档 | JSON 说明称 `ahead`/`behind` 仅在 `gone` 时为 `null`（未覆盖 unborn）；长格式与 JSON 样例；`branch -vv` 说明 | `docs/commands/status.md:425-445`、`:495-505`、`:576-581`；`docs/commands/zh-CN/status.md:242`、`:302-303`、`:379`；`docs/commands/branch.md:58`；`docs/commands/zh-CN/branch.md:55`；`../libra-backend/apps/tanstack-app/content/docs/commands/status.en.md:338-340`、`branch.en.md:61`；`COMPATIBILITY.md:191`（status）、`:206`（branch） |
| 规模基线 | 本仓库 3 519 个提交：`libra merge-base HEAD~1 HEAD`（保守排空）2.24 s；`rev-list --left-right --count HEAD~1...HEAD` 4.40 s；`status --short --branch`（相等短路）0.86 s；均含进程启动 | 2026-09-14 本机实测 |
| 网站仓库 | `../libra-backend` 为 Git 仓库，分支 `cf`（`## cf...origin/cf`） | 2026-09-14 |
| 测试环境 | `.env.live-test` 本机不存在，仓库无 example | 2026-09-14 |
| 外部参照（语义） | git@`1630431f326e15fcde608827b5ff38422528eb59`（本地副本 `/Volumes/Data/competition/git/git`）：`remote.c:2249-2310` `stat_branch_pair` 内部运行 `rev-list --left-right ours...theirs`，按 `SYMMETRIC_LEFT` 计数；`commit-reach.c:1216-1290` `ahead_behind` 以位图传播可达性，按 generation 优先队列排序，全部入队项 STALE 时停止 | 核对日 2026-09-14 |
| 外部参照（实跑） | `/usr/bin/git` 2.54.0：ahead 1 → `## main...origin/main [ahead 1]`、`# branch.ab +1 -0`、“Your branch is ahead of 'origin/main' by 1 commit.”、`[origin/main: ahead 1]`；behind 2 → `[behind 2]`、`+0 -2`、“… behind 'origin/main' by 2 commits, and can be fast-forwarded.”；分叉 1/2 → `[ahead 1, behind 2]`、`+1 -2`；相等 → 无括号、`+0 -0`、“up to date”、`[origin/main]` | 2026-09-14 scratch 实测 |

### 当前缺口

| ID | 缺口 | 影响 | 证据 | 计划动作 |
|---|---|---|---|---|
| GAP-01 | commonness 不向祖先传播，共享历史被计入 ahead 或 behind | 所有与 upstream 不相等的分支计数错误；Agent 与 CI 按 JSON 误判需要 pull | `src/command/status.rs:6269-6305` | AB-01 |
| GAP-02 | ahead/behind 与 merge-base 各自实现 commit 图遍历 | 同类缺陷可在一处修复、另一处残留（违反 GC-02） | `status.rs:6255`；`internal/merge_base.rs:1-13` | AB-01 |
| GAP-03 | 缺失 commit 静默截断，shallow 与损坏不作区分 | 计数静默偏小，看起来可信 | `status.rs:6279`、`:6296` | AB-01 |
| GAP-04 | 计数不可得（含 unborn）时 porcelain v2 仍输出 `+0 -0`，短格式与「相等」无法区分 | 机器消费者读到虚假数据 | `status.rs:6069-6071`、`:6099-6135` | AB-01 |
| GAP-05 | 既有测试只覆盖相等与无祖先形态 | 缺陷自引入起未被发现 | 事实基线「测试为何未发现」 | AB-01 |
| GAP-06 | `.env.live-test` 本机缺失 | v2.6 全量门无法执行 | 事实基线「测试环境」 | DEP-AB-01 |

## 与其它计划的关系

| 计划/文档 | 关系 | 本计划处理 |
|---|---|---|
| `plan-long.md` | 无对应 LR/SB/UP 编号；issue 计划不属于日期计划 | N/A |
| [`plan-20260903.md`](../plan-20260903.md) | merge 系列计划，依赖 `internal::merge_base` | AB-01 只抽出共享遍历核心，merge-base 语义与上界不变（AC3），命中 T-1 跑全量 |
| [`issues/483.md`](483.md)、[`issues/484.md`](484.md) | 写集只在 `COMPATIBILITY.md` 相交（不同行） | DEP-AB-04：不与相交卡并发实施，由单一发布者串行发布；只在既有物理行内修改 |
| `docs/development/commands/_compatibility.md` | 兼容分级事实源 | `status` 保持 `supported`，`branch` 保持 `partial`；只追加说明 |

## 评审结论与修订记录

| 维度 | 结论 | 修订动作 |
|---|---|---|
| 合理性 | issue 只描述 ahead 形态，实测 behind 与分叉同样错误，影响所有有 upstream 的分支 | 成功定义覆盖四种形态 |
| 可行性 | `merge_base.rs` 已有正确的双侧标记遍历与测试基础设施，只需暴露最终标记 | ADR-AB-01 复用遍历核心 |
| 任务卡粒度 | 算法、两个调用方、不可得形态与文档属于同一行为轴（计数正确性）；单独拆出消费者会留下「新 helper 无消费者」或「旧实现仍在用」的中间态 | 单卡 AB-01（M） |
| 依赖与顺序 | 无计划内依赖；与 483/484 仅 `COMPATIBILITY.md` 文件级相交 | DEP-AB-04 |
| 完整性 | 不可得形态会改变 porcelain v2 与 JSON 契约 | ADR-AB-03 与文档同步列入 AC |
| 安全性 | 不再输出虚假计数；不引入网络访问 | ADR-AB-03；tiered 读取策略下只读本地可得对象 |
| 功能正确性 | 日期偏斜、criss-cross、无公共历史、shallow 均需覆盖 | M-AB 矩阵带独立 oracle |
| 接口兼容 | 计数修正是 bug fix；porcelain v2 在不可得时省略 `# branch.ab`（与 Git「commit present 才输出」一致） | ADR-AB-03；文档四处登记 |
| 数据流与控制流 | merge-base 共用核心，重构有回归风险 | AC3 要求既有 merge-base 测试与基准原样通过 |
| 性能与容量 | 保守排空为 O(commits + edges)；缺陷实现在「ahead」形态下本就走完远端全部历史 | 读取上界与实测不劣于旧实现 1.5 倍（AC2、性能预算） |
| 可靠性与容错 | 读取失败必须可区分 | `MergeBaseError::Load` → 计数不可得 + warning |
| 可维护性 | 删除 `compute_ahead_behind`，只保留单一实现 | AC7 零命中守卫 |

### 修订历史

| 日期 | 触发 | 变更内容 | 原卡 → 新卡 | 受影响的引用 |
|---|---|---|---|---|
| 2026-09-14 | 初稿自审 + 实测 | 把 behind/分叉形态与计数不可得契约纳入范围；确定复用 `internal::merge_base` 遍历核心 | 无拆卡 | 成功定义、ADR、测试矩阵 |

## 已决议设计决策

实现时若需偏离本节，必须先修改计划并说明原因，不得在代码中静默改语义。

### ADR-AB-01: 复用 merge-base 的标记遍历实现 ahead/behind

- **Status:** Accepted
- **Context:** GAP-01/02。`paint_down_to_common_many` 已按「每个 commit 在获得新标记时才重新入队」遍历到不动点，排空后每个被访问 commit 的 `lhs`/`rhs` 标记恰为其对两个 tip 的可达性。它只返回候选集，没有暴露最终标记。
- **Decision:**
  1. 把遍历循环提取为模块私有的共享核心，返回最终 `painted` 表与 `WalkStats`；`paint_down_to_common_many` 改为该核心的薄包装，行为不变。
  2. 新增 `pub fn ahead_behind(local: &ObjectHash, upstream: &ObjectHash, boundaries: &HashSet<ObjectHash>) -> Result<AheadBehind, MergeBaseError>`：`local == upstream` 返回 `(0, 0)`；否则排空遍历，`ahead` = 只带 `lhs` 的 commit 数，`behind` = 只带 `rhs` 的 commit 数。
  3. 终止方式保持保守（排空队列），读取上界沿用 `3 × (commits + edges)`。
  4. 提供 `ahead_behind_with<S: CommitSource>` 供 `TestGraph` 测试。
- **Alternatives considered:**
  - 修补交替 BFS——commonness 传播需要重新入队语义，最终就是本方案，且会留下第二份实现。
  - 两个完整可达集合求差——内存 O(history) × 2，且与 merge-base 重复。
  - 按提交日期提前终止（Git 旧式 `everybody_uninteresting` 启发式）——无 generation 读取器时日期偏斜会导致错计，与 `merge_base.rs:25-29` 的既定结论冲突。
- **Consequences:** status 在不相等时遍历两侧可达历史的并集；该成本与缺陷实现在「ahead」形态下相当。提前终止记为 DEFER-01。
- **Revisit when:** commit-graph generation 读取器落地。

### ADR-AB-02: shallow 边界与缺失对象

- **Status:** Accepted
- **Context:** GAP-03。shallow clone 的边界 commit 父对象本就不存在；tiered 离线读取策略或损坏也会导致 commit 不可读。
- **Decision:**
  1. 调用方经 `fetch::read_shallow_boundaries()` 读取边界，并传给 `ahead_behind`；边界 commit 在遍历中视为无父提交（与 Git shallow graft 一致）。
  2. 读取 shallow 文件失败，或遍历中非边界 commit 读取失败（`MergeBaseError::Load`）时，计数为「不可得」（ADR-AB-03），不猜测数字。
  3. 边界感知只存在于 `ahead_behind` 使用的 `CommitSource` 包装中，不改变 `merge_bases` 等既有 API（DEFER-05）。
- **Alternatives considered:** status 在缺失对象时整体失败——会让 tiered 离线仓库和部分损坏仓库连 status 都无法运行，而计数只是附加信息，拒绝。继续静默截断——输出看似可信的错误数字，拒绝。
- **Consequences:** status 需要一次 shallow 文件读取（不存在时为空集）。
- **Revisit when:** DEFER-05 被排期。

### ADR-AB-03: 计数不可得时的输出契约

- **Status:** Accepted
- **Context:** GAP-04。`UpstreamInfo.ahead/behind` 为 `Option`，但短格式与 porcelain v2 把 `None` 当 0。Git porcelain v2 只在「upstream 已设置且 commit 存在」时输出 `# branch.ab`。
- **Decision:** 当 `gone == false` 且计数为 `None`（unborn 分支，或 ADR-AB-02 的不可得）时：
  - 短格式 / porcelain v1：`## <branch>...<upstream>`，不带括号；
  - porcelain v2：输出 `# branch.upstream`，省略 `# branch.ab`；
  - 长格式：不输出 tracking 句子（沿用 `status.rs:4393-4400`）；
  - JSON：`ahead`/`behind` 为 `null`，`gone: false`；
  - `branch -vv`：输出 `[<upstream>]`。
  - 仅「不可得」情形（非 unborn）经 `utils::error::emit_warning` 发出 warning：`cannot count commits ahead/behind '<upstream>': <原因>`。
- **Alternatives considered:** 新增 `[unknown]` 文本标记——偏离 Git porcelain，脚本难以兼容，拒绝。
- **Consequences:** porcelain v2 在 unborn 分支上不再输出虚假的 `+0 -0`。这是契约修正，必须写入 EN/zh 文档、`COMPATIBILITY.md` 与 dev 文档。
- **Revisit when:** 需要在文本输出中区分「不可得」与「相等」。

### ADR-AB-04: Git 参照与判据矩阵

- **Status:** Accepted
- **Context:** 计数正确性需要独立于被测实现的判据。
- **Decision:** 单元测试以测试内的朴素 oracle（两个完整祖先集合求对称差，仅用于测试）为准；集成测试以同一 fixture 上的 `libra rev-list --left-right --count` 与 git 2.54 实跑为准。开工时对 M-RENDER 全部行用 git 复跑，并把 sanitized 对照写入 `LIBRA_TEST_SCRATCH_DIR`。下列矩阵是 AC 引用的判据规范，每行必须有对应测试。

  **M-AB（`ahead_behind` 单元矩阵，`TestGraph`）**

  | 行 | 形态 | 期望 |
  |---|---|---|
  | A1 | `local == upstream` | `(0, 0)` |
  | A2 | upstream 为 local 的父提交，下方主干 1 000 个提交（issue 复现） | `(1, 0)` |
  | A3 | local 为 upstream 的祖先，相差 k=3 | `(0, 3)` |
  | A4 | 线性分叉：共同祖先之上 local 2 个、upstream 5 个 | `(2, 5)` |
  | A5 | local 侧含一次 merge（合入 3 个提交的侧枝） | 与 oracle 一致 |
  | A6 | upstream 侧含一次 merge | 与 oracle 一致 |
  | A7 | criss-cross 历史 | 与 oracle 一致 |
  | A8 | 无公共历史 | `(|reach(local)|, |reach(upstream)|)` |
  | A9 | 日期偏斜：一侧 tip 早于全部主干，另一侧晚于全部主干 | 与 oracle 一致 |
  | A10 | 公共祖先在一侧经长路径、另一侧经短路径到达（缺陷形态：对侧先入队祖先），覆盖多种日期顺序 | 与 oracle 一致 |
  | A11 | shallow 边界 commit 的父提交不在图中，且边界集合包含该 commit | 与「边界视为根」的 oracle 一致，不报错 |
  | A12 | 非边界 commit 缺失 | `Err(MergeBaseError::Load)` |

  **M-RENDER（生产入口集成矩阵）** — fixture：≥ 5 个提交的线性主干；tracking ref 按 `tests/command/status_json_test.rs:185-190` 的方式写入 `refs/remotes/origin/main`，并配置 `branch.main.remote/merge`

  | 行 | 形态 | `status -sb` | porcelain v2 | 长格式 | JSON `upstream` | `branch -vv` |
  |---|---|---|---|---|---|---|
  | R1 | upstream = `HEAD~1`（issue 复现） | `[ahead 1]` | `+1 -0` | “ahead of 'origin/main' by 1 commit.” | `1` / `0` | `[origin/main: ahead 1]` |
  | R2 | local = upstream 的祖先，相差 2 | `[behind 2]` | `+0 -2` | “behind 'origin/main' by 2 commits.” | `0` / `2` | `[origin/main: behind 2]` |
  | R3 | 分叉 local 1 / upstream 2 | `[ahead 1, behind 2]` | `+1 -2` | “have diverged … 1 and 2” | `1` / `2` | `[origin/main: ahead 1, behind 2]` |
  | R4 | 相等 | 无括号 | `+0 -0` | “up to date” | `0` / `0` | `[origin/main]` |
  | R5 | local 侧含 merge 的分叉历史 | 各列计数均等于 `rev-list --left-right --count origin/main...HEAD` 的输出 | — | — | — | — |

  **M-UNKNOWN（计数不可得）**

  | 行 | 形态 | 期望 |
  |---|---|---|
  | U1 | `.libra/shallow` 列出 R1 fixture 的某个提交，并删除其父提交对象 | 计数按边界计算且正确；无 warning |
  | U2 | 删除 upstream 侧某个非边界提交的对象文件 | `status` exit 0；短格式无括号；porcelain v2 无 `# branch.ab`；长格式无 tracking 句子；JSON `ahead`/`behind` 为 `null`、`gone: false`；输出不可得 warning；`branch -vv` 为 `[origin/main]` |
  | U3 | unborn 分支已配置 upstream | porcelain v2 无 `# branch.ab`；JSON 为 `null`；无 warning |

- **Alternatives considered:** 在测试中调用系统 git——仓库测试约定不依赖 git 二进制，拒绝。
- **Consequences:** 矩阵行变化时同批修改 ADR、测试与修订历史。
- **Revisit when:** 渲染契约（DEFER-02/04）变化。

## 全局工程约束

继承模板 GC-01..GC-13（GC-13 为 N/A：不触及 SQLite schema 或连接建构）。计划级补充：

- **GC-AB-01 生产入口判据:** M-RENDER 与 M-UNKNOWN 必须由子进程 `run_libra_command` 测试证明；`TestGraph` 单测只证明算法（M-AB），不替代渲染判据。
- **GC-AB-02 串行登记:** 新增集成测试优先使用 `run_libra_command(args, cwd)`，不使用 `ChangeDirGuard`；需要写入 tracking ref 而必须 `#[serial(cwd)]` 时，同步 `tests/SERIAL_REGISTRY.tsv`，重跑 `sh tests/NEXTEST_GROUPS.sh`，并通过 `compat_serial_registry`。
- **GC-AB-03 证据目录:** git 对照与性能数字写入 `LIBRA_TEST_SCRATCH_DIR`（持久介质），不写系统 `/tmp`，不含私有路径（ER-11）。

## 执行检查必备需求（强制）

完整继承模板 ER-01..ER-13。计划级补充：

- **ER-AB-01:** C④ 与计划收口门必须在 DEP-AB-01 满足后执行 `source .env.test && source .env.live-test && cargo nextest run --all --no-fail-fast --retries 2`；不得以 `cargo test --all` 替代。
- **ER-AB-02:** 开工时的 ER-02 核对必须在同一台机器、同一 ≥ 3 000 提交 fixture 的 R1 形态上，记录旧实现与新实现的 `status --short --branch` 耗时（各取 3 次中位数），作为性能预算的判据证据。

## 实施顺序

- `DEP-AB-01 -> AB-01`（仅约束 C④ 与收口门）
- `DEP-AB-04 -> AB-01`

### 依赖登记表

| ID | direction | 类型 | 对象 | Owner | 产物与可用性判据 | 证据 | 超时与失败策略 |
|---|---|---|---|---|---|---|---|
| DEP-AB-01 | incoming | 环境前置 | 仓库根 `.env.live-test` | 发布者 / 仓库维护者 | 文件存在，且 `source .env.test && source .env.live-test` 返回 0 | 2026-09-14 本机缺失 | 未满足时本卡停在 `locally-accepted`，收口门 `blocked`；不得降级 |
| DEP-AB-02 | incoming | 外部仓库 | `../libra-backend` 网站文档 | docs owner | ER-06a：Git 仓库、`cf` 分支，写入前重核 | 2026-09-14 `## cf...origin/cf` | 阻塞时 `blocked`，不得绕过 |
| DEP-AB-03 | outgoing | issue 回复 | libra-tools/libra#486 | 用户 | 发布版本、M-RENDER R1 证据，以及 behind/分叉同样受影响的说明 | issue #486 | 对外动作，发出前须经用户确认 |
| DEP-AB-04 | incoming | 跨计划写集互斥 | [`issues/483.md`](483.md)、[`issues/484.md`](484.md) 中修改 `COMPATIBILITY.md` 的卡 | 单一发布者 | 开工时这些卡不处于 `in-progress`；发布不交错 | 2026-09-14 三份计划均为 `pending` | 冲突时后开工者等待并按 GC-01 刷新锚点 |

### 发布分组与并发窗口

无 `REL-*`：AB-01 为 `independent`。

**并发声明:** 单卡；与 issues/483、484 的并发约束见 DEP-AB-04。

**发布者:** 执行本计划的主 Agent（单一发布者），与 issues/483、484 共用同一串行发布队列。

**发布窗口顺序:** AB-01 单独一个窗口；进入窗口时从 `Cargo.toml` 读取版本（核对日基线 `0.22.26`）。

### Phase 0: 基线冻结

**目标:** 计划通过 Codex review；依赖状态已记录。

**进入条件:**

- 本计划成稿。

**退出条件:**

- 「Codex review log」有一轮 `PASS`；DEP-AB-01、DEP-AB-04 状态已记录。

### Phase 1: 计数修复

**目标:** AB-01 发布。

**进入条件:**

- Phase 0 退出。

**退出条件:**

- AB-01 为 `done/complete`，且全量收口门全绿。

## 任务卡

### 字段全局默认与例外

- **Release boundary 默认:** `independent`。
- **Task type 默认:** `implementation`。
- **Rollback mode 默认:** `revert`（源码单提交 revert；已发布 artifact 按 ER-08 发新 patch 前滚）。
- **Migration and rollback 默认:** N/A（无 schema、存储格式或数据迁移）。
- **Security and privacy 默认:** 继承 GC-07、GC-11；只读本地可得对象，不发起网络读取。
- **Version increment 默认:** `patch`。
- **C/D coverage from 默认:** `self`。
- **D 组默认:** `.github/workflows/release.yml`，由 `gh release create "v${VERSION}"`（`push` `v*` tag）触发；判据为 run 全部 job 成功，且 `download.libra.tools` 能取到对应版本。
- **Lifecycle / Acceptance 默认:** `pending` / 空。

**默认覆盖:** 无。

**规则 waiver:** 无 `EX-*`。

### 任务卡粒度审计表

| 任务 | type | axis | recovery | complete | self-contained | AC | VER | landing / prod-files | scope | deps | writeset | release | split-from | exception |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| AB-01 | implementation | upstream ahead/behind 计数正确性 | revert 单提交 | yes | yes | 8/8 | 7/8 | 2/3 | M | DEP-AB-01, DEP-AB-04 | 跨计划序列化见 DEP-AB-04 | independent | N/A | N/A |

### Task AB-01: 以共享标记遍历替换 `compute_ahead_behind`

**Task type:** `implementation`

**Lifecycle / Acceptance:** `pending` / 空

**Description:** 按 ADR-AB-01..03，在 `src/internal/merge_base.rs` 提供正确的 `ahead_behind`（复用 merge-base 标记遍历核心，感知 shallow 边界），令 `status` 与 `branch -vv` 改用它，删除 `compute_ahead_behind`，并修正计数不可得时的渲染契约。唯一行为轴是「upstream ahead/behind 计数的正确性」。

**Out of scope:** generation 提前终止（DEFER-01）；`--no-ahead-behind` 跳过遍历（DEFER-02）；rev-list/log 迁移（DEFER-03）；长格式措辞（DEFER-04）；merge-base 感知 shallow（DEFER-05）。

**Current evidence:**

| 事实 | 证据 |
|---|---|
| 缺陷实现 | `src/command/status.rs:6242-6308` |
| 调用点 | `src/command/status.rs:6232`；`src/command/branch.rs:2109` |
| 渲染器把 `None` 当 0 | `src/command/status.rs:6069-6071`、`:6099-6135` |
| 共享遍历核心与测试设施 | `src/internal/merge_base.rs:209-293`、`:469-540`、`:986-1040` |
| shallow 边界解析器 | `src/command/fetch.rs:2698` |
| 复现数据与 git 2.54 对照 | 事实基线「复现」「外部参照（实跑）」 |

**Acceptance criteria:**

- [ ] `ahead_behind` 通过 ADR-AB-04 的 M-AB A1–A12（与测试 oracle 一致）。
- [ ] 读取上界：12 000 提交的线性与日期偏斜 `TestGraph` 上，`ahead_behind` 的 `node()` 读取数 ≤ `3 × (commits + edges)`。
- [ ] merge-base 既有行为不变：`internal::merge_base` 的既有测试与 `bench_merge_base_scaling` 不改断言即通过。
- [ ] `status` 的短格式、porcelain v2、长格式与 JSON 通过 M-RENDER R1–R5。
- [ ] `branch -vv` 通过 M-RENDER 的 `branch -vv` 列。
- [ ] 计数不可得形态通过 M-UNKNOWN U1–U3。
- [ ] 单一事实源：`compute_ahead_behind` 在 `src/` 中零命中，`status` 与 `branch` 均调用 `internal::merge_base::ahead_behind`。
- [ ] ER-06a 同步：
  - `docs/commands/status.md` 与 `docs/commands/zh-CN/status.md`：tracking 计数定义为与 `rev-list --left-right --count <upstream>...HEAD` 相同的对称差提交数；JSON 说明中 `ahead`/`behind` 为 `null` 的三种情形；porcelain v2 省略 `# branch.ab` 的条件。
  - `docs/commands/branch.md` 与 zh-CN 的 `-vv` 说明（计数不可得时省略）。
  - `docs/development/commands/status.md` 与 `branch.md:70`（共享 helper）。
  - `COMPATIBILITY.md:191`、`:206` 原行追加。
  - `../libra-backend/apps/tanstack-app/content/docs/commands/status.en.md:338-340` 与 `branch.en.md:61`。

**Verification:**

- [ ] `source .env.test && cargo test --lib internal::merge_base`（new：`ahead_behind_matrix`、`ahead_behind_read_bound`；既有用例原样）
- [ ] `source .env.test && cargo test --test command_test status_test`（new：`test_status_branch_counts_upstream_ancestor_486`、`test_status_branch_counts_behind_and_diverged`、`test_status_branch_counts_merge_history_matches_rev_list`、`test_status_branch_counts_unavailable`）
- [ ] `source .env.test && cargo test --test command_test status_json_test`（new：`json_status_upstream_counts_shapes`、`json_status_upstream_counts_unavailable_are_null`）
- [ ] `source .env.test && cargo test --test command_test branch_test`（new：`branch_vv_counts_upstream_shapes`）
- [ ] 零命中守卫（模板「Verification 判定口径」退出码模板）：`if rg -n "compute_ahead_behind" src; then echo "FAIL: forbidden pattern found"; exit 1; else rc=$?; if [ "$rc" -ne 1 ]; then echo "ERROR: rg failed with exit $rc"; exit "$rc"; fi; echo "OK: zero hits"; fi`
- [ ] `source .env.test && cargo test --test compat_ledger_schema`
- [ ] 手工证据：M-RENDER 各行与 git 2.54 的实跑对照，以及 ER-AB-02 的新旧耗时（存 `LIBRA_TEST_SCRATCH_DIR`）

**Full-suite trigger:** `T-1: src/internal/merge_base.rs 是 merge/rebase/am/diff 共用的单一事实源`

**Dependencies:** `DEP-AB-01`（仅约束 C④）、`DEP-AB-04`

**Deliverables:** N/A

**Implementation write set:** `src/internal/merge_base.rs`、`src/command/status.rs`、`src/command/branch.rs`、`tests/command/status_test.rs`、`tests/command/status_json_test.rs`、`tests/command/branch_test.rs`、`docs/commands/status.md`、`docs/commands/zh-CN/status.md`、`docs/commands/branch.md`、`docs/commands/zh-CN/branch.md`、`docs/development/commands/status.md`、`docs/development/commands/branch.md`、`COMPATIBILITY.md`、`../libra-backend/apps/tanstack-app/content/docs/commands/status.en.md`、`../libra-backend/apps/tanstack-app/content/docs/commands/branch.en.md`

**Release write set:** Inherited

**Files likely touched:** 同 Implementation write set；按 GC-AB-02 需要串行 lane 时追加 `tests/SERIAL_REGISTRY.tsv` 与生成的 `.config/nextest.toml`

**Docs and compatibility impact:** `status` 与 `branch` 命令页 EN + zh-CN + 网站页（计数定义、`null` 情形、porcelain v2 省略条件）；`COMPATIBILITY.md` 两行追加；错误码 N/A（不新增，warning 无稳定码）。后端写入前记录 Git 仓库类型与 `cf` 分支证据（DEP-AB-02）。

**Rollback mode:** `revert`

**Migration and rollback:** Inherited

**Security and privacy:** 计数不可得时不输出猜测值；tiered 离线读取策略下不触发远端读取。

**Performance budget:**
- 算法：读取 ≤ `3 × (commits + edges)`（AC2）。
- 实测：ER-AB-02 的 R1 fixture 上，新实现 `status --short --branch` 中位耗时不超过旧实现的 1.5 倍。
- 相等形态保持短路，不遍历。

**Estimated scope:** `M`（3 个生产文件；porcelain v2 / JSON 不可得契约属公开行为修正）

**Version increment:** `patch`

**Release boundary:** `independent`

**C/D coverage from:** `self`

**Granularity:** `type=implementation; axis=upstream ahead/behind 计数正确性; recovery=revert 单提交，恢复旧 BFS 与旧渲染; complete=yes; self-contained=yes; AC=8/8; VER=7/8; landing=2; prod-files=3; scope=M; deps=DEP-AB-01,DEP-AB-04; writeset=跨计划序列化见 DEP-AB-04; release=independent; split-from=N/A; exception=N/A`

## 测试矩阵

执行阶段按 ER-13 只跑与本卡相关的行；整张表由收口阶段的全量门统一覆盖。

| 类别 | 必须覆盖 | Target / command |
|---|---|---|
| 单元 | M-AB A1–A12；读取上界；merge-base 既有用例与基准 | `source .env.test && cargo test --lib internal::merge_base` |
| 集成 | M-RENDER R1–R5；M-UNKNOWN U1–U3 | `source .env.test && cargo test --test command_test status_test`；`status_json_test`；`branch_test` |
| 兼容 | `COMPATIBILITY.md` 行锚；串行登记（若新增 lane） | `--test compat_ledger_schema`；`--test compat_serial_registry` |
| 迁移 | N/A（无 schema 或存储格式变化） | N/A |
| 安全 | 不输出猜测计数；不触发远端读取 | M-UNKNOWN U2 |
| 性能 | 读取上界；新旧实测耗时 | `internal::merge_base` 基准；ER-AB-02 手工证据 |
| 回归（全量） | merge / rebase / am / diff `A...B` 共用 merge-base 核心 | `source .env.test && source .env.live-test && cargo nextest run --all --no-fail-fast --retries 2`（T-1，DEP-AB-01） |

## 追溯表

| 任务 | 来源/证据 | Libra 落点 | 文档/兼容动作 | 指定测试 |
|---|---|---|---|---|
| AB-01 | issue #486；`status.rs:6242-6308`；git@1630431f32 `remote.c:2249-2310`、`commit-reach.c:1216-1290`；2026-09-14 实测 | `src/internal/merge_base.rs`、`src/command/status.rs`、`src/command/branch.rs` | status/branch 命令页 EN/zh/网站、dev 文档、`COMPATIBILITY.md:191`、`:206` | `internal::merge_base::tests::ahead_behind_matrix`、`status_test::test_status_branch_counts_upstream_ancestor_486`、`branch_test::branch_vv_counts_upstream_shapes` |

## 里程碑验收与回滚

| 里程碑 | 完成条件 | 发布/证据 | 回滚或前滚 |
|---|---|---|---|
| M0 | 计划 Codex review PASS；DEP-AB-01/04 状态已记录 | review log | N/A |
| M1 | AB-01 `done/complete`；全量收口门全绿 | patch 版本、`release.yml` run、git 对照与性能证据 | revert 源码 + 新 patch 前滚 |

### 故障恢复矩阵

| 故障点 | 可接受残留 | 恢复动作 | 禁止结果 |
|---|---|---|---|
| 遍历中 commit 读取失败 | 无 | 计数不可得 + warning（ADR-AB-03） | 输出猜测计数；status 整体失败 |
| shallow 文件读取失败 | 无 | 计数不可得 + warning | 忽略边界后报告错误数字 |
| merge-base 核心重构引入回归 | 本地未提交改动 | 修复直至既有 merge-base 测试与全量通过 | 修改既有 merge-base 断言以通过 |
| 发布步 `gh release create` 失败 | 已推送的 `main` 提交 | 按 ER-09 记录，下一窗口补 tag/release | blind retry；只推 `main` 就宣称已发布 |
| D 组 `release.yml` 失败 | 已存在的 tag | 前滚修复，发新 patch | 回退已推送提交或删除已发布 artifact |

## 风险登记

| 风险 | 影响 | 缓解 | 任务 |
|---|---|---|---|
| 抽取遍历核心改变 merge-base 结果或上界 | 高：merge/rebase/am/diff 行为变化 | AC3 既有测试原样通过；T-1 全量 | AB-01 |
| 大仓库上 status 变慢 | 中：交互延迟 | 读取上界 + ER-AB-02 实测不劣于旧实现 1.5 倍；提前终止记为 DEFER-01 | AB-01 |
| 日期偏斜导致计数错误 | 高：再次错计 | 保守排空，不按日期提前终止；M-AB A9/A10 | AB-01 |
| porcelain v2 省略 `# branch.ab` 影响依赖 `+0 -0` 的脚本 | 低：仅 unborn 与不可得形态 | 与 Git 契约一致；文档登记 | AB-01 |
| shallow 仓库误报不可得 | 中：shallow clone 失去计数 | ADR-AB-02 边界视为根；M-UNKNOWN U1 | AB-01 |
| 与 issues/483、484 并发修改 `COMPATIBILITY.md` | 低：行锚冲突 | DEP-AB-04；只在原物理行内追加 | AB-01 |
| `.env.live-test` 缺失阻塞全量门 | 中 | DEP-AB-01，禁止降级 | AB-01 |

## 性能与容量摘要

| 操作 | 单次成本 | 累积成本 | 预算/上限 | 验证 |
|---|---|---|---|---|
| `ahead_behind`（不相等） | 每个可达 commit 至多入队 3 次 | O(commits + edges)，驻留一张标记表 | 读取 ≤ `3 × (commits + edges)` | `internal::merge_base` 基准 |
| `ahead_behind`（相等） | O(1) 短路 | O(1) | 不遍历 | M-AB A1 |
| `status --short --branch`（R1，≥ 3 000 提交） | 进程 + 遍历 | — | 中位耗时 ≤ 旧实现 × 1.5 | ER-AB-02 手工证据 |
| shallow 边界读取 | 一次文件读取 | O(边界数) | 文件不存在时为空集 | M-UNKNOWN U1 |

## 兼容与文档收口

- [ ] `COMPATIBILITY.md` 的 `status`（第 191 行）与 `branch`（第 206 行）已在原物理行内追加，文件行数不变。
- [ ] `docs/commands/status.md`、`docs/commands/branch.md` 已同步。
- [ ] `docs/commands/zh-CN/status.md`、`docs/commands/zh-CN/branch.md` 已同步。
- [ ] `docs/development/commands/status.md`、`branch.md` 已同步。
- [ ] `docs/error-codes.md`：N/A（不新增错误码）。
- [ ] `tests/INDEX.md`：N/A（只在既有 `command_test` 模块内新增用例）。
- [ ] `Cargo.toml` `[[test]]`：N/A（无新 target）。
- [ ] `../libra-backend` 网站 `status.en.md`、`branch.en.md` 已在已验证的 `cf` 分支同步（DEP-AB-02）。
- [ ] `plan-long.md`：N/A（issue 计划，不进日期计划索引，无 LR 编号）。

## Codex review log

本计划尚未进行 Codex review。按 ER-05，Phase 0 必须至少有一轮 `PASS` 后才能开工。

| Round | Scope | Result | P0/P1 | P2 处置 | Evidence |
|---|---|---|---|---|---|

## 非目标与延后项

| ID | 延后内容 | 原因 | 重启条件 | 承接位置 |
|---|---|---|---|---|
| DEFER-01 | commit-graph generation 读取器与 generation 序提前终止（Git `ahead_behind`） | 需要新的持久化读取器与失效处理；无 generation 时按日期提前终止不安全 | 大仓库 status 延迟被报告，或 commit-graph 读取器排期 | 待立计划 |
| DEFER-02 | `--no-ahead-behind` 跳过遍历并调整 JSON 契约（Git `[different]`） | 改变 JSON 字段语义，独立的性能轴 | DEFER-01 未落地而性能不可接受 | 待立 issue |
| DEFER-03 | `rev-list --left-right --count` 与 `log A...B` 迁移到 `ahead_behind` | 各自有 golden 输出与遍历选项，需独立回归 | 发现其计数与 helper 不一致，或性能需要 | 待立计划 |
| DEFER-04 | 长格式 tracking 句子措辞对齐 Git（“and can be fast-forwarded”、pull hint） | 措辞差异，与计数正确性无关 | 外部用例断言长格式措辞 | 待立 issue |
| DEFER-05 | `merge_bases` / `is_ancestor` 感知 shallow 边界 | 影响 merge/rebase 的 shallow 行为，超出本 issue | shallow 仓库 merge-base 报错被报告 | 待立 issue |

## 完成判据

计划只有在以下条件全部满足后才能标记完成：

- [ ] AB-01 满足 `G-*`，「任务卡粒度审计表」已填齐。
- [ ] AB-01 的 AC 全部满足，且 `Lifecycle=done` **且** `Acceptance=complete`（含 D 组 `release.yml` 证据）。
- [ ] 所有 Verification 命令已运行并记录结果；ADR-AB-04 每个矩阵行都有对应测试，M-RENDER 有 git 2.54 对照证据。
- [ ] **全量收口门（ER-13）**：`cargo +nightly fmt --all --check`、`cargo clippy --all-targets --all-features -- -D warnings`、`source .env.test && source .env.live-test && cargo nextest run --all --no-fail-fast --retries 2` 全绿（DEP-AB-01 已满足）。
- [ ] **全量暴露的 Bug 已全部处理（ER-13）**：本计划引起的失败已前滚修复并重跑全量至全绿；既有失败附计划基线 commit 上同样失败的复现证据，并登记 `FIX-*` 或 `DEFER-*`。
- [ ] docs、compat 同步完成（见「兼容与文档收口」）。
- [ ] `Rollback mode` 已验证或记录不可验证的原因。
- [ ] Codex review 最终为 `PASS`，P0/P1 全部关闭。
- [ ] 已 `patch + 1`、版本面五处一致、`Cargo.lock` 仅由工具链刷新，并已用 `gh release create v<version>` 触发 `release.yml` 且取得 Cloudflare 发布证据（ER-08）。
- [ ] 「修订历史」已记录成稿后的全部规范性变更。
- [ ] DEP-AB-03：经用户确认后，已在 #486 回复发布版本与证据（或记录用户决定不回复）。
