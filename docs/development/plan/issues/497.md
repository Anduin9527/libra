# Issue #497：删除最后一个被跟踪文件后 commit 报 "nothing to commit" 修复计划（2026-09-24）

> 模板版本：v2.6；继承 [plan-template.md](../plan-template.md) 的 GC-01..13、ER-01..13、G-01..11。
> 来源：[libra-tools/libra#497](https://github.com/libra-tools/libra/issues/497)；由 [#477](https://github.com/libra-tools/libra/issues/477) 范围外拆出（2026-09-15 决定）；关联 [#470](https://github.com/libra-tools/libra/issues/470)（mode/索引一致性）。
> 状态：设计计划（草稿）；全部任务卡 `pending`，尚未进行 Codex review。

## 文档职责

本计划解决 issue #497（commit 系列）：当删除仓库中**最后一个被跟踪文件**后执行 `libra commit` / `commit -a`，libra 误报 `nothing to commit, working tree clean`，而没有真正提交这次删除。Git 的行为是：删除最后一个被跟踪文件（清空 worktree 内容）后 `git commit -a` / `git add -A && git commit` 应产生一个提交（树为空，相对于 HEAD 为一次删除），而不是“nothing to commit”。

「文档职责」全文：目标是对齐 Git 的「删除最后一个被跟踪文件」语义——该删除是**应从工作树删除、并落入索引与提交**的有效变更，而非空工作树。

本文只规划任务，不宣称实现完成。落地时每个任务都必须先刷新源码锚点，再按任务卡验收。

### 适用范围

- `libra commit` / `commit -a` / `commit -am` 在“删除最后一个被跟踪文件”下的判定与输出。
- 索引 vs 工作树的“是否有变更”判定；删除（D）与 untracked 空目录的区分。
- 相关的 `status`/`ls-files` 输出一致性。
- 同步：commit 文档、`COMPATIBILITY.md`、`../libra-backend` 文档、compat 守卫。

### 非目标

- `add` 家族（#484/#489/#491–#494）的删除/未合并语义。
- 交互式 commit、`--amend` 空消息（#478）。
- 空树提交（`--allow-empty`）的独立行为。

### 成功定义

- 删除最后一个被跟踪文件后，`libra add -A && libra commit`（或 `libra commit -a`）**成功**产生一个删除型提交；`commit` 不再误报 `nothing to commit`。
- 若工作树确实干净（无任何变更），`commit` 仍按 Git 报 `nothing to commit, working tree clean`（exit 1）。
- 移植对应的 Git 上游场景（删除最后一个被跟踪文件 → commit 成功），并有回归守卫。
- 任务卡取得 C 组发布与 D 组证据，收口门全绿后才标记完成。

## 事实基线

| 类别 | 当前事实 | 证据 |
|---|---|---|
| 复现 | `commit -m` 在删除最后一个被跟踪文件后报 `nothing to commit`；应产生删除提交 | #497；#477 的 HF-05 相关（文案不分型） |
| 相邻 | 索引 mode 一致性由 #470 覆盖（D3/D5：commit 在 mode change 上也误报 nothing to commit） | `issues/470.md` |
| 关联 | `-s`/`-S` 分支在脏树描述差异（HF-05） | `issues/477.md`（HF-05） |

### 当前缺口

- 空 worktree（无被跟踪文件）时，libra 的“是否有变更”判定误判为“无变更”；
- 删除型变更是否正确地计入“应提交”集合。

## 与其它计划的关系

- **#470**：索引/工作树物化与 mode 变更检测 — 与 #497 在 `commit`/索引判定上相关；两者在 `commit` 输出文案上有重叠，需协调。
- **#477（已收口）**：本 issue 为其范围外拆出；`#495` 只跟踪 cherry-pick/revert，**不覆盖**本 issue。
- **#478**：commit 的 `--amend`/空消息等由 #478 承接；与 #497 不重叠（#497 只处理删除最后文件的提交判定）。

## 任务卡

### Task CD-01: 复现并定位“删除最后一个被跟踪文件”的提交判定
- 输入：#497 复现步骤。
- 输出：定位“无变更”误判的代码位置；区分「空 worktree」与「存在删除型变更」。
- 验收：给出根因与 ADR-CD-01。

### Task CD-02: 让删除型变更计入提交
- 输入：CD-01。
- 输出：删除最后一个被跟踪文件后 `commit -a` / `add -A && commit` 成功产生删除提交；同时保持「真正干净 worktree 报 nothing to commit」。
- 验收：对应 Git 上游场景通过。

### Task CD-03: 输出文案与状态一致性
- 输入：CD-02。
- 输出：`commit`/`status`/`ls-files` 输出与 Git 行为一致（删除场景不误报）。
- 验收：相关 compat 守卫通过。

### Task CD-04: 测试、文档与发布收口
- 输入：上述实现。
- 输出：回归测试（删除最后被跟踪文件 → commit 成功）、commit 文档与 `COMPATIBILITY.md` 同步、C 组发布 + D 组证据。
- 验收：收口门全绿。

## 里程碑

- M1：CD-01 `done`（根因）。
- M2：CD-02/03 `done`（行为修复 + 一致性）。
- M3：CD-04 `done`（测试/文档/发布）。

## Codex review log

（设计计划，尚未开始。）
