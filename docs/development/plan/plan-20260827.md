# 测试执行模型迁移：多进程 + 多目录拆分计划（2026-08-27）

> 模板版本: v2.2（按 [`plan-template.md`](plan-template.md) 成稿；整份符合 v2.2，含 ER-13 分层测试执行与每卡 `Full-suite trigger` 登记）
> 触发: 用户指令（2026-08-27）——`plan-20260825.md` TA-03 的度量/抗剥落门族按卡面需 ~12 小时机时，基线采集已跑 2h+ 超出预期；用户裁决**停止 TA-03 baseline-v2 执行**，改按「多进程 + 多目录」的方式对测试拆分重新立计划
> 用户裁决: TA-03 的 12 小时级度量/抗剥落门族**豁免**（ADR-NP-03；本计划以 nextest 原生廉价门等价替代；已采得的 1 次有效基线样本以本文登记常量续用）
> 关系: 本计划**承接并替代** `plan-20260825.md` 的 TA-06/TA-07 与 TA-03 的验证经济学（转换本体由 NP-00 落地）；`plan-20260825.md` 的 TA-05 与 PS-* 仍归原计划

## 文档职责

把全量测试从「单进程内锁串行」迁移到「**每测一进程 + 每测一目录**」的执行模型：cargo-nextest 默认一测一进程，进程级全局状态（cwd / env / hash_kind）随进程隔离**天然消解**，互斥只剩进程外部资源（真实云端、共享磁盘故障注入面），由注册表机械生成的 nextest 互斥组承载。每测一目录已是既有事实（全部用例 `tempfile::tempdir()` 隔离 + spawn 型用例每测独立 HOME/XDG），本计划只做验证与守卫，不做用例改写。

前置事实：`plan-20260825.md` TA-01（fail-closed 分类器，38 轮 Codex PASS）、TA-02（内容锚站点键）、TA-04（端口卫生）已提交；TA-03 的机械转换已在工作树完成且功能门全绿，其 12 小时级验证经济学被用户豁免——NP-00 以功能门口径落地该转换，作为互斥组生成的登记基座。

本文只规划任务，不宣称实现完成。落地时每个任务都必须先刷新源码锚点，再按任务卡验收。

### 适用范围

- 工作树内已完成的 TA-03 机械转换产物的落地（`tests/SERIAL_MANIFEST.tsv`、`tests/SERIAL_CONVERT.sh`、`tests/SERIAL_CLASSIFY.sh` 站点图通道（`SERIAL_CLASSIFY_SITES_FILE`，converter 的定位依赖）、89 个被转换测试文件、`tests/SERIAL_REGISTRY.tsv`、`tests/compat/serial_registry.rs` 零无 key 常驻守卫、`tests/INDEX.md`、`tests/compat/README.md`、`plan-20260729.md` DEFER-09 关闭句、`plan-20260825.md` 承接簿记）。
- `.config/nextest.toml`（新建，生成物）及其生成器 `tests/NEXTEST_GROUPS.sh` 与 drift 守卫。
- 本地执行面：cargo-nextest 钉版安装契约、`test-provider` 等 feature 配方到 nextest profile 的映射。
- CI 执行面：`.github/workflows/base.yml` 中 `compat-offline-core` 与 `compat-offline-command` 两个 job 的**逐段**运行器切换（真实 CI 面见事实基线，含枚举 target、doctest、feature 门控段与 command_test 分片）。
- `CLAUDE.md` 构建/测试/CI 段、`tests/INDEX.md`、`tests/compat/README.md` 的运行器文档。

### 非目标

- 不改任何用例的断言、名称与语义；不动 `tests/harness/**` 结构（继承 GC-PS-05）。
- 不改 `src/**`（含 `src/utils/test.rs` 的 `CWD_LOCK`——一测一进程下自然退化为单进程内部细节）。
- 不删除 `#[serial(...)]` 具名属性——注册表与互斥组生成的登记语义源，运行时惰性零成本。
- 不在本计划内变更 `CLAUDE.md`「Quality Acceptance Criteria」三门的**权威口径**——`source .env.test && cargo test --all` 仍是完成契约；nextest 是**附加**执行面。权威口径若要迁移到 nextest，须按模板 ER-04 的同批同步规则（`CLAUDE.md` + `AGENTS.md` + 采用现行模板的计划）另行立卡，本计划登记为 `DEFER-NP-04`。
- 不做 fixture 磁盘模板缓存（`DEFER-NP-01`）；不做 cwd 显式路径重构（`DEFER-PS-06` 继承兜底）；不做进程内直调（`DEFER-NP-03`）。
- 不动 `plan-20260825.md` 的 TA-05 与 PS-* 卡。

### 成功定义

- 转换落地：`tests/**` 零无 key `#[serial]` 常驻守卫全绿；用例具名集合守恒（唯一新增 = 守卫自身交付的不变量用例，已登记）。
- `cargo nextest run` 全量在本机绿（外部资源组互斥取证；两连跑零新失败）。
- 实测提速：nextest 全量单次墙钟 ≤ **0.5×** 基线常量（5921 s → ≤ 2960 s）为硬门；工程预期 15–25 min。
- CI 两个 job 切换后全绿，回退路径注释保留。
- 所有非延后任务 `Lifecycle=done` 且 `Acceptance` 达到收口态；完成判据全绿（含模板三门全量收口）。

## 事实基线

> 锚点核对日期：2026-08-27（本会话实测）。开工时按 ER-02 刷新。

| 类别 | 当前事实 | 证据 |
|---|---|---|
| 全量基线（登记常量） | **5921 s（98.7 min）**：PRE_SHA=`89081277a`、独立 worktree（`CARGO_TARGET_DIR` 隔离）、安静机器、`source .env.test && cargo test --all --no-fail-fast`，222 个二进制全遍历；唯一失败为基线环境伪影（`test_e2e_mcp_flow` 硬编码 `target/debug/libra` 与隔离 target 目录失配；主树同测 2/2 绿）。**原始日志与样本文件已随 2026-08-27 机器重启丢失**——本行为**用户裁决背书的登记常量**（方法如上），NP-03 以其为对照，不再重采 | 本行即权威登记（ADR-NP-03）；方法可复现：`git worktree add <dir> 89081277a` + 上述命令 |
| fail-fast 陷阱 | `cargo test --all` 默认在首个失败二进制后停止：两次早期样本（6564 s / 308 s，rc=101）因此作废；`--lib` 的 16 个进程 spawn/RPC 用例在并发负载下按既有已知形态剥落 | 记忆 `libra_test_suite_macos_gotchas`；基线方法因此固定为 `--no-fail-fast` |
| 转换产物（工作树，未提交） | manifest 冻结 1185 行（none 316 / lane:cwd 579 / global 204=197fn+7site / lane:env 42 / lane:env+cwd 27 / lane:hash_kind 3 / lane:hash_kind+cwd 3 / lane:cloud_live 10 / lane:workspace_failpoints 1）；转换 89 文件（另有 `tests/SERIAL_CLASSIFY.sh` 站点图通道增量与之同批）；幂等（重跑输出 `converted 0 file(s)`）；转换后分类器 none==0；注册表 869 行再生成；守卫 17/17；fmt 全绿 | `tests/SERIAL_MANIFEST.tsv`（`awk -F'\t' '{c[$2]++} END{for(k in c)print k,c[k]}'`）；`sh tests/SERIAL_CONVERT.sh`；`sh tests/SERIAL_CLASSIFY.sh`；`cargo test --test compat_serial_registry` |
| 用例守恒 | 转换前后 `cargo test --all -- --list` 具名集合 diff 恰好一行：新增 `no_unkeyed_serial_attributes_remain`（守卫交付物） | 可复现命令 = NP-00 Verification 第 4 条免 stash worktree 重放脚本（卡内全文，直接可复制执行） |
| 每测一目录已成立 | 全部用例 `tempfile::tempdir()` 隔离；spawn 型每测独立 HOME/XDG/USERPROFILE；固定端口段已消除（TA-04） | `tests/command/mod.rs:67-93`；`plan-20260825.md` TA-04 修订行 |
| serial_test 锁是进程内的 | 无 key `#[serial]` 只锁空串 key，具名 key 锁均为进程内静态——跨进程互斥必须由运行器承载 | `plan-20260729.md` R101 实证 |
| 进程外部资源面 | 一测一进程下仍需真互斥：`lane:cloud_live` 10、`lane:workspace_failpoints` 1、`global` 204（持全资源 key 集，须入全部外部组） | manifest 派生计数（本表行 3） |
| cargo-nextest 未安装 | `which cargo-nextest` → not found；`Cargo.toml` 与 CI 无 nextest 痕迹 | 2026-08-27 实测 |
| **真实 CI 面**（NP-04 迁移对象） | `compat-offline-core` job 内含多段：⓪ 前置 `compat_matrix_alignment` 单测（`cargo test --test compat_matrix_alignment compatibility_matrix_matches_cli_commands -- --exact`）① 枚举式 `cargo test --lib --bins` + 除 `command_test` 外的具名 `--test` 集合（显式命名以避免 `cargo test --all` 静默跳过 required-features target）② `cargo test --doc` ③ `--features test-provider` 的 Code UI 场景段（`--test-threads=1`）④ `--features otlp` 的 `otlp_telemetry`（`--test-threads=1`）⑤ `--features keyring` 的 `auth_keyring_backend`（`--test-threads=1`）⑥ `--features test-upgrade` 段；另有独立 job `compat-offline-command`（`command_test` 分片半）。**不是**单条 `cargo test --all` | `.github/workflows/base.yml:333-460`（2026-08-27 逐段核读） |
| 模板版本 | v2.2（ER-13 已并入） | `plan-template.md`（HEAD `93e452f1c`） |

### 当前缺口

| ID | 缺口 | 影响 | 证据 | 计划动作 |
|---|---|---|---|---|
| GAP-NP-01 | 全量测试单进程串行执行，墙钟 5921 s（98.7 min） | 每次收口门 / CI 反馈周期以小时计，是当前最大开发摩擦 | 事实基线行 1（用户裁决背书常量） | NP-02（切换）、NP-03（取证 ≤2960 s 硬门） |
| GAP-NP-02 | 无 key `#[serial]` 的进程内锁在每测一进程模型下失效面已解除但未落地——转换产物（89 文件 + 清单 + 注册表再生 + 守卫）滞留工作树 | 转换成果不可被后续卡消费；工作树越久越易被并发改动冲蚀 | `git status`（tests/ 89 转换文件未提交）；事实基线行 3–5 | NP-00（落地 + 三门 + 守恒） |
| GAP-NP-03 | 进程外共享资源（cloud_live 207 / workspace_failpoints 198 / 站点 7）无调度器级互斥表达 | 直接切 nextest 会让这些用例并发互踩 | `tests/SERIAL_REGISTRY.tsv` 键隶属计数（2026-08-27 实测） | NP-01（组派生 + drift 守卫） |
| GAP-NP-04 | CI 六段执行面全部为 cargo test 单进程形态 | 提速不达 CI；本地/CI 执行面漂移 | `base.yml:326-460` 逐段核读（事实基线「真实 CI 面」行） | NP-04（逐段切换/保留） |

## 与其它计划的关系

- **plan-20260825.md**：TA-01/02/04 已完成提交（本计划前置）；TA-03 转换本体由 NP-00 落地、度量门族经用户裁决豁免（ADR-NP-03）；TA-06/TA-07 由 NP-01/NP-02/NP-04 承接。**双向登记**：NP-00 的写集包含在 `plan-20260825.md` 落 TA-03 豁免修订行与 TA-06/07 的「由 plan-20260827 承接」状态标注；TA-05 与 PS-* 留原计划（TA-05 建议在 NP-03 度量后落地以分离归因）。
- **plan-20260824.md**：暂停中；本计划不触碰其写集。
- **plan-20260729.md**：`DEFER-09` 关闭句随 NP-00 落地，措辞更正为「TA-01/TA-02（plan-20260825）+ NP-00（plan-20260827，转换落地，度量门族按 ADR-NP-03 豁免）承接关闭」。

## 评审结论与修订记录

| 维度 | 结论 | 修订动作 |
|---|---|---|
| 合理性 | 值得做：全量 >60 min 是当前最大开发摩擦；用户裁决明确方向（多进程+多目录），nextest 是零改写路径 | 无 |
| 可行性 | 六卡全 S/M；转换本体已在树、组生成机械派生、CI 逐段处置——每卡可独立交付 | 无 |
| 任务卡粒度 | 单轴逐卡核对（审计表 15 列全达标）；无 L/XL；no-release 卡承载点显式 | 无 |
| 依赖与顺序 | 串行链无环；no-release 先于承载点（NP-03/05 → NP-04）；DEP 表四项登记 | R2 修复承载顺序 |
| 完整性 | 功能门全保留（ADR-NP-03 只豁免度量半）；回滚逐卡 revert；文档三处收口 | 无 |
| 安全性 | 无新监听面；nextest 钉版官方源（GC-NP-04）；不触 secret | 无 |
| 功能正确性 | 用例语义零改动（GC-NP-01）；两连跑 + 组并发取证兜剥落 | 无 |
| 接口兼容 | 用户可见行为零变化；`#[serial]` 属性保留为登记源；CLI/schema 不动 | 无 |
| 数据流与控制流 | 互斥从进程内锁迁移到调度组——键隶属派生保证互斥强度不丢（R3 修复计数语义） | R3 |
| 性能与容量 | 硬门 ≤0.5×基线常量；top-20 归因驱动后续（DEFER 升格通道） | 无 |
| 可靠性与容错 | 故障恢复矩阵五行（重放树残留/新剥落/门不达/CI 红/证据丢失）；全部卡内门免 stash | R9 |
| 可维护性 | 单一事实源链：manifest → 注册表 → nextest 组，全机械生成 + drift 守卫 | 无 |

### 修订历史

| 日期 | 触发 | 变更内容 | 原卡 → 新卡 | 受影响的引用 |
|---|---|---|---|---|
| 2026-08-28 | NP-01 确认评审 Codex-R2 FAIL（`gpt-5.6-terra`，2×P1） | ① 提交态漂移收口：973b3262f 带入的生成器 profile 段（NP-02 写集）未与生成物同提交——干净检出下 drift 守卫必红、被未提交工作树掩盖 → 再生成的 `.config/nextest.toml`（含 profile 段）随本轮提交入库，恢复 HEAD 自洽（中间提交为 forward-fix 现实，不回滚）；② NP-02 组并发取证命令实参修正 `cloud_live` → `external`（脚本只接受 external 组名），AC 措辞同步 | 无新卡 | NP-02 Verification/AC、`.config/nextest.toml`、修订记录 |
| 2026-08-28 | NP-00 确认评审 Codex-R1 FAIL（`gpt-5.6-terra`，1×P1；换模型复审） | NP-00 `Implementation write set` 漏列提交实际新建的 `plan-20260827.md` 自身（99 文件 vs 卡面 98；白名单第 10 项本已显式允许）→ 写集、`Files likely touched`（同上继承）、Docs impact 三处补列 | 无新卡 | NP-00、修订记录 |
| 2026-08-28 | NP-01 确认评审 Codex-R1 FAIL（`gpt-5.6-terra`，1×P1；用户指令换模型复审已 PASS 卡） | ADR「新外部 lane 自动并入」与实现的硬编码白名单矛盾（`lane:third_party_service` 不会入组且守卫无法发现）→ 外部性改**补集派生**：{cwd, env, hash_kind} 闭集之外的具名键一律外部（新键默认外部，fail-safe）；生成器 awk、守卫 `external_lane`（含合成回归断言）、overlap 取证脚本三处同一规则；ADR-NP-01/NP-01 Description 同步。当前键集下成员不变（toml 逐字节不变） | 无新卡 | ADR-NP-01、NP-01、tests/NEXTEST_GROUPS.sh、守卫、tests/nextest_group_overlap_check.py、修订记录 |
| 2026-08-28 | NP-01 执行评审 Codex-R1 FAIL（`gpt-5.5`，1×P1） | fn 过滤形态修正：聚合二进制（如 `command_test`）经嵌套模块产生带模块路径的完整测试名，`test(=<裸名>)` 等值不命中、互斥静默丢失 → 生成器改发**锚定正则** `test(/(^\|::)<fn>$/)`（fn 名经守卫的重复名拒绝保证全树唯一，末段锚定即精确；同名非串行测试的假想过匹配只会把它加进互斥组——安全方向）；守卫反解析、卡内对账 grep、Description 同步；另在 NP-02 Verification 增 `cargo nextest list -E 'group(external)'` 实际成员对账（运行器可用后核证过滤真实命中） | 无新卡 | NP-01、NP-02 Verification、tests/NEXTEST_GROUPS.sh、守卫、修订记录 |
| 2026-08-28 | NP-01 执行期现状核对（nextest 语义） | ADR-NP-01/NP-01 卡修订：nextest test-group 为排他成员制（每测试仅归属首个匹配 override 的组），卡面「复合行入两组」不可表达、按原写法 197 条复合行的 workspace_failpoints 互斥会被静默丢弃 → 组形态改**外部键并集单组 `external`**（208 fn + 7 站点宿主 target，max-threads=1；互斥强度 ≥ 两组语义，唯一代价为纯 cloud_live×纯 workspace_failpoints 的 10×1 无害串行）；Description/AC/对账脚本同步（注册表键隶属事实 207/198/7 保留） | 无新卡 | ADR-NP-01、NP-01、修订记录 |
| 2026-08-28 | Codex-R13 **PASS**（`gpt-5.5`，0×P0/P1） | 计划定稿。评审实测清单：manifest 1185 行分布、converter 幂等输出、转换后 none==0（869 行）、守卫 17/17、冻结重放逐字节相等（含 93 站点路径派生）、注册表键隶属 207/198/7、provider 十 target 与 base.yml 一致、v2.2 结构/卡字段/15 列审计表/表列一致性/跨计划簿记齐备；nextest 命令形态对上游文档核验不构成阻塞 | 无新卡 | 计划进入执行（NP-00 起） |
| 2026-08-28 | Codex-R12 FAIL（`gpt-5.5`，4×P1） | 全部收口：① NP-00 提交面门可执行化——新增 staged 白名单门（白名单 = 固定产物清单 ∪ 转换前站点图派生文件集，实测 93 文件超集、并发节点脏文件机械排除；`comm -23` 断言 staged ⊆ 白名单），AC 措辞同步；② NP-04 nextest 计数与 provider 形态锚点允许行内 env 前缀，并补 provider 段专项形态断言（env 亦可位于 step 的 env: 块）；③ 发布挂起窗口显式登记——independent 卡 C 组发布步与 `release.yml` D 证据统一延后至 v0.21.27 补发窗口（继承「方案②」口径），执行期以 `Acceptance=locally-accepted` 关闭、补发后翻转 `complete`，完成判据首条同步；④ R10 行反引号跨度内裸管道符改写为文字表述 | 无新卡 | NP-00 AC/Verification、NP-04 Verification、发布分组与并发窗口、完成判据、修订记录 |
| 2026-08-28 | Codex-R11 FAIL（`gpt-5.5`，1×P1） | NP-00 写集补漏：`tests/SERIAL_CLASSIFY.sh`（未提交的站点图通道 `SERIAL_CLASSIFY_SITES_FILE`，`tests/SERIAL_CONVERT.sh` 逐 span 改写的定位依赖）登记入范围列表、NP-00 Description/AC 提交面/Implementation write set/事实基线行 3 五处——漏登会导致按声明提交面落地时 converter 失去其分类器支撑 | 无新卡 | 范围、事实基线、NP-00、修订记录 |
| 2026-08-28 | Codex-R10 FAIL（`gpt-5.5`，1×P1） | 修订历史表 R8 行内联命令的裸管道符转义为 `\|`（该行由 6 列错列还原为 5 列）；全表扫描反引号跨度内裸管道符，共 1 处，无其它错列行 | 无新卡 | 修订历史表结构 | 
| 2026-08-28 | Codex-R9 FAIL（`gpt-5.5`，1×P1） | 守恒门去 stash 化收口：NP-00 守恒重采改为临时 `git worktree`（`--detach` 于 PRE_REF，独立 `CARGO_TARGET_DIR`）重放转换前树采 `--list`，与当前树 diff；主树零写入、可与并发进程共存；AC 措辞、测试矩阵证据行、自审可靠性行、故障恢复矩阵行（stash 中断 → 重放树残留清理）同步改写——计划内不再存在任何 stash 依赖 | 无新卡 | NP-00 AC/Verification、测试矩阵、自审表、故障恢复矩阵、修订记录 |
| 2026-08-28 | Codex-R8 FAIL（`gpt-5.5`，3×P1） | 全部收口：① NP-00 首块拆三段独立门——冻结门改为 `git archive PRE_REF`（默认 HEAD、落地后用 `<落地提交>^`）重放转换前 tests/ 树 + `SERIAL_CLASSIFY_ROOT` 指向重放树跑分类器，与冻结清单互 diff（实测 1185 行逐字节相等、9.7s；免 stash 可与并发进程共存）；none==0 门改在转换后分类器输出上断言（不再与转换前清单互 diff——行数不同属预期）；幂等门保留 R7 形态；对应 AC 措辞同步；② 补模板必备 `### 当前缺口` 章（GAP-NP-01..04：墙钟 5921s / 转换产物滞留工作树 / 进程外资源无调度表达 / CI 六段 cargo test 形态，逐条挂证据与计划动作）；③ 提交面门改 staged 白名单——`git diff --cached --name-only \| grep -c '^src/'` 为 0，替换全树 `src/` 干净要求（共享树中并发节点的 `src/` 脏文件容忍存在） | 无新卡 | NP-00 Verification/AC、当前缺口（新章）、修订记录 |
| 2026-08-28 | Codex-R7 FAIL（`gpt-5.5`，4×P1） | 全部收口：① NP-00 幂等门假门修复——先采转换前 `git status` 基线，再断言 converter 输出 `converted 0 file(s)`，最后 diff 转换后状态与基线（原先两次转换后状态互比恒真）；② NP-04 保留门补全——五个保留段（alignment/doctest/otlp/keyring/test-upgrade 三行体）全部入断言，且所有锚点改 `^\s*` 非注释行形态（nextest 计数与保留段检查都不再被 ADR-NP-02 注释满足）；③ `tests/nextest_group_overlap_check.py` 登记进 NP-02 Implementation write set（新增交付物，Verification 组并发取证的执行前提）；④ NP-04 卡内 `exception` 与审计表逐字对齐（`Full-suite `T-1``） | 无新卡 | NP-00/NP-02/NP-04 Verification 与 write set、粒度机械拷贝、修订记录 |
| 2026-08-27 | Codex-R6 FAIL（`gpt-5.5`，3×P1） | 全部收口：① 卡内 `Granularity` 改为审计表逐字机械拷贝——六卡 `writeset` 统一为模板 `序列化于 TASK-ID` 形态（NP-00=序列化于 NP-01..NP-05、NP-01=NP-00,NP-02、NP-02=NP-01,NP-03、NP-03=NP-02,NP-05、NP-04=NP-05、NP-05=NP-03,NP-04），NP-02 `recovery` 补齐表值、NP-00 landing 去除非表注记；② 钉版占位符消除——cargo-nextest 钉为 **0.9.143**（crates.io max_stable，2026-08-04 发布、2026-08-27 API 核查），GC-NP-04/DEP-NP-03/NP-02 Description/AC 四处落具体版本与 `--locked` 安装命令；③ provider target 集补齐——NP-02 Description/Verification 与 NP-04 处置 ③ 三处以 base.yml:401 的十个 `--test` target 逐字枚举替换 `<原 target 集>` 占位 | 无新卡 | NP-00..NP-05 Granularity、既有决策 GC-NP-04、依赖登记表、NP-02、NP-04、修订记录 |
| 2026-08-27 | Codex-R5 FAIL（`gpt-5.5`，4×P1） | 全部收口：① 补 12 维自审表于修订历史之前（模板 :118 结构）；② 依赖登记表展开为模板全列（direction/类型/对象/Owner/产物与可用性判据/证据/超时与失败策略），四项 DEP 逐列补齐；③ 缺失卡字段补齐——NP-03/NP-04 补 `Current evidence`、NP-03/NP-05 补 `Out of scope`；④ NP-04 首条 Verification 的 `cargo test --all` 零命中门为假（注释按 ADR-NP-02 保留旧命令、当前 :347 亦有命中）→ 换为「切换段 ≥3 处 nextest + 三个保留段逐字未动」的可执行断言块 | 无新卡 | 评审结论与修订记录、依赖登记表、NP-03/NP-04/NP-05、修订记录 |
| 2026-08-27 | Codex-R4 FAIL（`gpt-5.5`，2×P1） | 全部收口：① provider 命令语义修复——nextest profile 只配运行器行为（组/线程/junit），Cargo feature 与 env 必须显式随命令：NP-02 Verification 与 Description、NP-04 处置 ③ 统一为 `LIBRA_ENABLE_TEST_PROVIDER=1 LIBRA_SKIP_WEB_BUILD=0 cargo nextest run --features test-provider --profile test-provider <原 target 集>`（`SKIP_WEB_BUILD=0` 保持场景所需真实内嵌 web 资产，与 base.yml 原语义一致）；② CI 清单补前置段——`compat-offline-core` 内 `compat_matrix_alignment` `--exact` 单测（base.yml:330）登记为处置 ⓪ 并明令保留 cargo test 原样，五段 → **六段**（事实基线/NP-04 Description/AC/风险行/测试矩阵五处同步） | 无新卡 | 事实基线 CI 行、NP-02、NP-04、风险登记、测试矩阵、修订记录 |
| 2026-08-27 | Codex-R3 FAIL（`gpt-5.5`，3×P1） | 全部收口：① NP-01 对账语义改**键隶属派生**——转换后注册表的原 `global` fn 行已展开为复合 lane，纯 `global` 仅余 7 条站点行；组成员 = lane 键集含该 key 的全部行（`cloud_live` 207 = 10 纯 + 197 复合、`workspace_failpoints` 198 = 1 + 197、站点 7 按宿主 target 入两组），Description/AC/对账脚本三处同步（实测计数逐一核对）；② 卡内 `Granularity` 摘要与新实施顺序机械同步（NP-04 `deps=NP-05`、NP-05 `deps=NP-03`，writeset 序列化串同步），审计表一致；③ NP-05 补两段可执行 Verification（文档命令逐字实跑 + 两计划回链零断链检查），VER 计数 1→2 双处同步 | 无新卡 | NP-01、NP-04/NP-05 Granularity、粒度审计表、修订记录 |
| 2026-08-27 | Codex-R2 FAIL（`gpt-5.5`，4×P1） | 全部收口：① `plan-20260825.md` 完成判据连贯化——非延后卡清单/计划完成门测试面/发版清单/DEFER-09 行四处改为承接口径（TA-03/06/07 经本计划核销、权威三门不随执行面切换）；② 承载顺序修复——实施顺序改 `NP-03 -> NP-05 -> NP-04`（no-release 卡先于承载点执行，承载点 C 组收口覆盖其最终变更；`CLAUDE.md` CI 段终态移入 NP-04 写集），NP-04/NP-05 的 Dependencies 同步；③ 粒度审计表展开为模板 15 列机械 schema、逐卡照抄 `Granularity` 值；④ G-05 可执行性——NP-00 三段门/冒烟/守恒重采、NP-01 对账、NP-02 全量与组并发取证、NP-03 计时与 top-20 全部换成卡内完整 bash 块（守恒证据不再指向 /tmp 脚本） | 无新卡 | 实施顺序、NP-00..NP-05 Verification、粒度审计表、事实基线行 4、plan-20260825 完成判据 |
| 2026-08-27 | Codex-R1 FAIL（`gpt-5.5`，7×P1） | 全部收口：① 补齐 v2.2 强制章节（本表、依赖登记表、发布分组与并发窗口、字段全局默认与例外、粒度审计表、追溯表、故障恢复矩阵、性能与容量摘要、兼容与文档收口）与每卡完整字段（`Full-suite trigger`/`Deliverables`/`Release write set`/`Files likely touched`/docs/security/migration 族）；② NP-03 词表修正——`measurement` → **`audit`**（no-code/no-config 成立：只执行命令并把数值写回计划文档）、`Rollback mode: N/A` → `forward-only`、`Version increment: none` → `N/A`、`Release boundary: independent` 与继承 C/D 矛盾 → **`no-release`（承载点 NP-04）**；③ NP-05 同型修正（docs 卡 `no-release` + 承载点 NP-04 + `Deliverables` 文件清单补齐）；④ ER-13 修复——每张会推送的卡补 `Full-suite trigger`（NP-04 客观命中 **T-1**：`.github/workflows/**`），完成判据恢复模板三门全量收口（`cargo test --all`），nextest 为附加证据，权威口径迁移另登记 `DEFER-NP-04`；⑤ 双向承接补齐——`plan-20260825.md` 落修订行与 TA-03/06/07 状态标注、`plan-20260729.md` DEFER-09 措辞更正（见「与其它计划的关系」）；⑥ CI 事实修正——事实基线换成 `base.yml:333-460` 的真实多段面（枚举 target/doctest/四个 feature 段/command 分片 job），NP-04 迁移面逐段重写；⑦ /tmp 证据文件失效——5921 s 改为**用户裁决背书的登记常量**（方法留档、重启丢失注明），守恒证据改为可重采脚本 | 无新卡 | 全文结构、NP-03、NP-05、NP-04、事实基线、完成判据、非目标、DEFER 登记 |
| 2026-08-27 | 自审 R0 | 成稿；用户指令与豁免裁决登记入标题块与 ADR-NP-03；DEFER-NP-01..03 与升格通道登记 | N/A → NP-00..NP-05 | 全文 |

## 已决议设计决策

- **ADR-NP-01（互斥组的唯一来源；2026-08-28 执行期修订）**：`.config/nextest.toml` 的 test-groups 一律由 `tests/SERIAL_REGISTRY.tsv` 机械生成，禁止手写成员；生成器与 drift 守卫随卡交付。**组形态 = 外部键并集单组 `external`**（成员 = lane 键集含任一外部键——`cloud_live`、`workspace_failpoints`——的 fn 行，加纯 `global` 站点行的宿主 target 整 target；`max-threads = 1`）。修订依据：nextest 的 test-group 是**排他成员制**（每个测试仅归属首个匹配 override 的组），复合行「入两组」不可表达，按原两组写法 197 条复合行的 `workspace_failpoints` 互斥会被静默丢弃；并集单组的互斥强度 ≥ 原两组语义，唯一代价是纯 `cloud_live`（10）与纯 `workspace_failpoints`（1）之间的无害额外串行。外部性判据 = **补集派生**：进程内闭集 {cwd, env, hash_kind}（分类器进程模型定义）之外的任何具名键都视为外部——新键默认外部、自动并入 `external`（fail-safe 方向，至多多串行）；生成器/守卫/overlap 取证三处同一规则，守卫含合成回归（`lane:some_future_service+cwd` 必须入组）。cwd/env/hash_kind 不生成组。
- **ADR-NP-02（运行器双轨窗口）**：CI 切换后保留一步回退——`base.yml` 注释保留原命令行，回退 = revert 单提交；本地 `cargo test` 始终可用（属性仍在，单进程语义不回退）。
- **ADR-NP-03（豁免的边界）**：被豁免的是 TA-03 的 3+3 采样度量门与 5 连跑/极端线程门族；**不豁免**功能正确性门（冻结/幂等/none==0/守恒/守卫/focused）。基线以 5921 s 登记常量续用（原始样本文件随机器重启丢失，方法已档）。依据：用户 2026-08-27 裁决。
- **ADR-NP-04（权威测试口径不随卡漂移）**：`CLAUDE.md` 三门中的 `cargo test --all` 在本计划全程保持权威完成契约；nextest 为附加执行面。口径迁移须按模板 ER-04 同批同步规则另行立卡（`DEFER-NP-04`）。

## 全局工程约束

- GC-NP-01：继承 `plan-20260825.md` GC-PS-05——测试文件只允许属性行与登记文件变更，断言/名称/harness 结构不动。
- GC-NP-02：`.config/nextest.toml` 是生成物——人工编辑视为 drift，由守卫打红。
- GC-NP-03：提交面精确暂存（GC-12 继承）；并发节点共享工作树，逐文件 `git add`，禁 `-a`。
- GC-NP-04：cargo-nextest 版本钉住为 **`0.9.143`**（`cargo install cargo-nextest --version 0.9.143 --locked`；钉版依据 = crates.io max_stable，2026-08-04 发布、2026-08-27 API 核查），CI 与本地同版本；升级独立成卡。

## 执行检查必备需求

本计划继承模板 v2.2 的 ER-01..ER-13 全部条款，无计划级增补。每张会推送的卡按 ER-13 在卡内登记 `Full-suite trigger`；完成判据含全量收口门（见文末）。

## 实施顺序

- `NP-00 -> NP-01`
- `NP-01 -> NP-02`
- `NP-02 -> NP-03`
- `NP-03 -> NP-05`
- `NP-05 -> NP-04`

> 顺序说明：NP-05（docs，no-release）先于其承载点 NP-04 执行并提交，产物随 NP-04 的发布进入用户渠道——承载点的 C 组收口测试运行于包含 NP-05 最终变更的树上，继承因此成立；`CLAUDE.md` 的 CI 段终态由 NP-04 自己的写集承载（切换与文档同卡同批）。

### 依赖登记表

| ID | direction | 类型 | 对象 | Owner | 产物与可用性判据 | 证据 | 超时与失败策略 |
|---|---|---|---|---|---|---|---|
| DEP-NP-01 | incoming | 跨计划 | `plan-20260825#TA-01/TA-02/TA-04`（已提交）+ TA-03 工作树产物 | plan-20260825（同一执行会话） | fail-closed 分类器 + 内容锚注册表 + 端口卫生在 main；TA-03 转换树完整（幂等重跑 `converted 0 file(s)`） | commits `b6959e5e2`/`9b9b8c834`/`ef6e673d2`；`sh tests/SERIAL_CONVERT.sh`（2026-08-27） | 已成立，无等待；若转换树被并发改动 → NP-00 开工 ER-01 检出并按 converter 幂等重放恢复 |
| DEP-NP-02 | incoming | 跨计划 | `plan-20260824.md`（暂停态） | 用户 | `tests/**` 与 CI 写集无并发写方 | 用户 2026-08-27 前指令；`git log` 无 0824 写集提交 | 若 0824 恢复执行 → 本计划 NP-04 前重协调写窗（ER-01 每卡检查兜底） |
| DEP-NP-03 | incoming | 外部服务 | crates.io `cargo-nextest`（钉版）+ self-hosted runner 安装权限 | Nextest 上游 / runner 管理员 | `cargo install cargo-nextest --version 0.9.143 --locked` 本地与 runner 均成功（钉版依据：crates.io max_stable `0.9.143`，2026-08-04 发布，2026-08-27 API 核查） | NP-02/NP-04 开工时安装输出留档 | 安装失败 ≤3 次重试；仍失败 → NP-02 置 blocked，回落 cargo test（语义未回退） |
| DEP-NP-04 | outgoing | 跨计划 | `plan-20260825.md`（接收方） | plan-20260825（同一执行会话） | TA-03 豁免登记 + TA-06/07 superseded 标注 + 完成判据承接口径已写入（NP-00 落地、NP-05 终态） | `plan-20260825.md` 修订行与卡状态（2026-08-27） | 接收方文档未同步时本计划不得关闭（NP-05 回链闭环门守卫） |

## 发布分组与并发窗口

全部卡**串行**执行（实施顺序即全序），无并发窗口需求；**单一发布者** = 执行本计划的当前会话（并发节点不发布本计划写集）。会推送的卡（NP-00/01/02/04）各自 `Release boundary = independent`；NP-03/NP-05 为 `no-release`，C/D 覆盖承载点 = **NP-04**。无 `REL-*` 聚合组。

**发布挂起窗口（继承用户既定「方案②」口径，登记于 `plan-20260825.md` 修订记录）**：ER-12 版本窗口由 `plan-20260824.md` DF-05（v0.21.27）占用，因此本计划执行期内，independent 卡的 C 组止于「提交 + 推送 + 本地三门」，`patch + 1` / `gh release` 步与 `release.yml` D 组证据统一延后至 v0.21.27 解锁后的**补发窗口**：届时按实施顺序逐卡补版本号、`gh release` 与 `release.yml` 运行证据，补齐后该卡 `Acceptance` 由 `locally-accepted` 翻转为 `complete`。NP-04 的 D 组（push 后 `gh run` 首轮 CI 结果）不依赖版本发布，按卡内 Verification 原样执行。

## 字段全局默认与例外

| 字段 | 全局默认 | 例外 |
|---|---|---|
| Lifecycle / Acceptance | pending / 空 | — |
| Rollback mode | revert | NP-03（forward-only：度量档案与簿记只前进） |
| Version increment | patch | NP-03/NP-05：`N/A`（no-release，产物随 NP-04 的发布进入用户渠道） |
| Release boundary | independent | NP-03/NP-05：no-release（承载点 NP-04） |
| C/D coverage from | self | NP-03/NP-05：NP-04 |
| Security and privacy | 继承 GC-07/GC-11（无新监听面、无密钥外泄面；nextest 仅本地/CI 执行器） | — |
| Docs and compatibility impact | 见各卡 | — |
| Full-suite trigger | none（执行期跑本卡 A 组 focused） | NP-04：`T-1: 触碰 .github/workflows/**` |

## 任务卡

### Task NP-00: 落地冻结 manifest 机械转换（功能门口径）

**Task type:** implementation

**Lifecycle / Acceptance:** done / locally-accepted（2026-08-28：三段门+守卫 17/17+冒烟 4/4+守恒 9161→9162 恰守卫一行+白名单 99⊆103+fmt 绿；提交 `315132afe` 已推送；执行评审 Codex R1 PASS（`gpt-5.5`）；换模型确认评审 `gpt-5.6-terra` 2 轮至 PASS（R1 写集漏列计划文件自身 P1 已收口）；D 组随发布挂起窗口补发）

**Description:** 把工作树内已完成的 TA-03 机械转换以功能门口径落地提交：冻结 manifest、转换器、分类器站点图通道增量（`tests/SERIAL_CLASSIFY.sh` 的 `SERIAL_CLASSIFY_SITES_FILE`，converter 依赖）、89 个被转换文件、再生成的注册表、零无 key 常驻守卫与文档同步；同批完成三处承接簿记（`plan-20260825.md` 豁免修订行 + TA-03/06/07 状态标注、`plan-20260729.md` DEFER-09 措辞更正）。原卡 3+3 采样度量门与 5 连跑/极端线程门族经用户裁决豁免（ADR-NP-03），由 NP-03 等价替代。唯一行为轴 = 转换产物的落地经济学。

**Out of scope:** 新转换逻辑；度量与抗剥落（NP-03）；互斥组（NP-01）。

**Current evidence:** 事实基线表行 3–5。

**Acceptance criteria:**

- [ ] 冻结门 / none==0 门 / 幂等门（本卡 Verification 首块三段独立门，模板退出码；冻结门经 `git archive` 转换前状态重放）通过并留档
- [ ] `compat_serial_registry` 17/17 绿（含零无 key 常驻不变量与失败信息双修法）
- [ ] 用例守恒重采（免 stash）：转换前状态经临时 git worktree 重放采集 `--list`，与当前树 `--list` diff 恰好为已登记的一行守卫交付物新增
- [ ] 冒烟四 target 绿（`compat_status_wave0_register`、`workspace_lease_test`、`intent_flow_test`、`ai_projection_resolver_test`）
- [ ] 三处承接簿记落地且双向可回链（0825 修订行 + 卡状态标注；0729 DEFER-09 措辞）
- [ ] 提交面精确（可执行白名单门，见 Verification 对应条目）：staged 集合 ⊆ 固定产物清单 ∪ 转换前站点图派生文件集（93 文件超集：89 实际改写 + 4 分类到位无需改写）；并发节点的任何脏文件（含 `src/` 与 `tests/` 下非站点文件）机械排除、不入提交面

**Verification:**

- [ ] 冻结/幂等/none==0 三段门（模板退出码）：

  ```bash
  # 门 1/3：清单冻结——转换前状态经 git archive 重放（免 stash、可与并发进程共存；
  # 落地提交后复现：PRE_REF=<落地提交>^；实测 1185 行逐字节相等、9.7s）
  PRE_REF="${PRE_REF:-HEAD}"
  rm -rf /tmp/np00_prewt && mkdir -p /tmp/np00_prewt
  git archive "$PRE_REF" COMPATIBILITY.md Cargo.toml tests | tar -x -C /tmp/np00_prewt \
    || { echo "ERROR: archive failed"; exit 2; }
  mkdir -p /tmp/np00_prewt/.git
  SERIAL_CLASSIFY_ROOT=/tmp/np00_prewt sh tests/SERIAL_CLASSIFY.sh > /tmp/np00_pre_m \
    || { echo "ERROR: pre-state classifier failed"; exit 2; }
  [ -s /tmp/np00_pre_m ] || { echo "ERROR: empty pre-state output"; exit 2; }
  diff -u tests/SERIAL_MANIFEST.tsv /tmp/np00_pre_m || { echo "FAIL: manifest not frozen"; exit 1; }
  # 门 2/3：转换后 none==0（转换后分类器输出是注册表基底，行数与转换前清单不同属预期，不互 diff）
  sh tests/SERIAL_CLASSIFY.sh > /tmp/np00_post_m || { echo "ERROR: classifier failed"; exit 2; }
  n=$(awk -F'\t' '$2 == "none"' /tmp/np00_post_m | wc -l | tr -d ' ')
  [ "$n" -eq 0 ] || { echo "FAIL: $n unkeyed remain"; exit 1; }
  # 门 3/3：converter 幂等（先采基线、断言输出、再比树）
  git status --porcelain=v1 -- tests/ > /tmp/np00_pre || { echo "ERROR: git failed"; exit 2; }
  out=$(sh tests/SERIAL_CONVERT.sh) || { echo "ERROR: converter failed"; exit 2; }
  printf '%s\n' "$out" | grep -qF 'converted 0 file(s)' \
    || { echo "FAIL: not idempotent (converter rewrote files): $out"; exit 1; }
  diff <(git status --porcelain=v1 -- tests/) /tmp/np00_pre \
    || { echo "FAIL: tests/ tree changed across converter rerun"; exit 1; }
  echo "OK: frozen + none==0 + idempotent"
  ```

- [ ] staged 白名单门（全派生、可复制执行；复用门 1/3 的 /tmp/np00_prewt 重放树）：

  ```bash
  SERIAL_CLASSIFY_ROOT=/tmp/np00_prewt SERIAL_CLASSIFY_SITES_FILE=/tmp/np00_pre_sites \
    sh tests/SERIAL_CLASSIFY.sh > /dev/null || { echo "ERROR: pre-state sites map failed"; exit 2; }
  { awk -F'\t' '{print $2}' /tmp/np00_pre_sites
    printf '%s\n' \
      tests/SERIAL_MANIFEST.tsv tests/SERIAL_CONVERT.sh tests/SERIAL_CLASSIFY.sh \
      tests/SERIAL_REGISTRY.tsv tests/compat/serial_registry.rs tests/compat/README.md \
      tests/INDEX.md docs/development/plan/plan-20260825.md \
      docs/development/plan/plan-20260729.md docs/development/plan/plan-20260827.md
  } | sort -u > /tmp/np00_allow
  git diff --cached --name-only | sort -u > /tmp/np00_staged
  extra=$(comm -23 /tmp/np00_staged /tmp/np00_allow)
  [ -z "$extra" ] || { echo "FAIL: staged files outside NP-00 whitelist:"; echo "$extra"; exit 1; }
  echo "OK: staged face within derived whitelist ($(wc -l < /tmp/np00_staged | tr -d ' ') files)"
  ```

  转换前站点图派生集实测 93 个唯一路径（89 实际改写 + 4 分类到位无需文本改写），并发节点脏文件（如 `tests/compat/fastcdc_feature_gate_guard.rs`）实测不在派生集内。

- [ ] `source .env.test && LIBRA_SKIP_WEB_BUILD=1 cargo test --test compat_serial_registry`
- [ ] 冒烟四 target：

  ```bash
  source .env.test
  for t in compat_status_wave0_register workspace_lease_test intent_flow_test ai_projection_resolver_test; do
    LIBRA_SKIP_WEB_BUILD=1 RUST_MIN_STACK=33554432 cargo test --test "$t" || { echo "FAIL: $t"; exit 1; }
  done
  echo "OK: smoke green"
  ```

- [ ] 守恒重采（免 stash：转换前状态经临时 git worktree 重放，主树零写入、可与并发进程共存；diff 必须恰为守卫交付物一行）：

  ```bash
  set -uo pipefail
  source .env.test
  ( LIBRA_SKIP_WEB_BUILD=1 cargo test --all -- --list ) | sed -n 's/: test$//p' | sort > /tmp/np00_after
  PRE_REF="${PRE_REF:-HEAD}"   # 落地提交后复现：PRE_REF=<落地提交>^
  git worktree remove --force /tmp/np00_conswt 2>/dev/null || true
  git worktree add --detach /tmp/np00_conswt "$PRE_REF" || { echo "ERROR: worktree add failed"; exit 2; }
  ( cd /tmp/np00_conswt && LIBRA_SKIP_WEB_BUILD=1 CARGO_TARGET_DIR=/tmp/np00_constarget cargo test --all -- --list ) \
    | sed -n 's/: test$//p' | sort > /tmp/np00_before \
    || { git worktree remove --force /tmp/np00_conswt; echo "ERROR: pre-state list failed"; exit 2; }
  git worktree remove --force /tmp/np00_conswt || echo "WARN: worktree cleanup failed (non-blocking, see 故障恢复矩阵)" 
  diff /tmp/np00_before /tmp/np00_after > /tmp/np00_conserve; rc=$?
  [ "$rc" -le 1 ] || { echo "ERROR: diff failed"; exit 2; }
  [ "$(grep -c '^>' /tmp/np00_conserve)" -eq 1 ] \
    && grep -q '^> no_unkeyed_serial_attributes_remain$' /tmp/np00_conserve \
    && [ "$(grep -c '^<' /tmp/np00_conserve)" -eq 0 ] \
    || { echo "FAIL: conservation delta unexpected"; cat /tmp/np00_conserve; exit 1; }
  echo "OK: conserved (single registered deliverable addition)"
  ```

**Full-suite trigger:** none（未触碰 `tests/harness/**`、`tests/helpers/**`、`tests/command/mod.rs` 与 T-1 面；执行期由 A 组 focused + 冒烟承担，全量由完成判据收口门兜底）

**Dependencies:** DEP-NP-01

**Deliverables:** N/A（implementation）

**Implementation write set:** `tests/SERIAL_MANIFEST.tsv`；`tests/SERIAL_CONVERT.sh`；`tests/SERIAL_CLASSIFY.sh`（站点图通道 `SERIAL_CLASSIFY_SITES_FILE` 增量——converter 逐 span 改写的定位依赖，随本卡同提交）；89 个被转换测试文件（manifest 派生集）；`tests/SERIAL_REGISTRY.tsv`；`tests/compat/serial_registry.rs`；`tests/compat/README.md`；`tests/INDEX.md`；`docs/development/plan/plan-20260825.md`；`docs/development/plan/plan-20260729.md`；`docs/development/plan/plan-20260827.md`（本计划文件自身——首次入库 + 卡状态/修订行随卡演进，白名单第 10 项）

**Release write set:** Inherited（C 组版本面五处 + `Cargo.lock`）

**Files likely touched:** 同上

**Docs and compatibility impact:** `tests/compat/README.md` + `tests/INDEX.md`（已在树）；两计划簿记 + 本计划文件自身（三计划文档随卡同提交）

**Rollback mode:** revert

**Migration and rollback:** revert 单提交回到未转换树；注册表/manifest 皆纯文本登记面

**Security and privacy:** 继承默认

**Performance budget:** 门执行 ≤ 15 min

**Estimated scope:** S

**Version increment:** patch

**Release boundary:** independent

**C/D coverage from:** self

**Granularity:** `type=implementation; axis=转换产物落地经济学; recovery=revert 单提交; complete=yes; self-contained=yes; AC=6/8; VER=4/8; landing=1; prod-files=0; scope=S; deps=DEP-NP-01; writeset=序列化于 NP-01..NP-05; release=independent; split-from=plan-20260825 TA-03; exception=用户裁决豁免度量门族（ADR-NP-03）`

### Task NP-01: 互斥组生成器与 drift 守卫

**Task type:** implementation

**Lifecycle / Acceptance:** done / locally-accepted（2026-08-28：生成器幂等 cmp、并集 external 组 208 锚定正则 + 7 binary、drift 守卫 18/18、一字节反例、键隶属对账 207/198/7、fmt 绿；提交 `2f9973b22`+`da8df7a86`+`6c6bcf809` 已推送；执行评审 Codex 3 轮至 PASS（`gpt-5.5`）；换模型确认评审 `gpt-5.6-terra` 3 轮至 PASS（R1 外部键补集派生、R2 提交态漂移 + overlap 实参，均收口）；D 组随发布挂起窗口补发）

**Description:** 新建 `tests/NEXTEST_GROUPS.sh`：读 `tests/SERIAL_REGISTRY.tsv`，机械生成 `.config/nextest.toml`。**成员派生按 key 隶属、组形态为外部键并集单组 `external`**（ADR-NP-01 2026-08-28 修订：nextest test-group 排他成员制，复合行双组不可表达；并集互斥强度 ≥ 两组语义）：lane 键集含任一**外部键**（= {cwd, env, hash_kind} 补集，当前即 `cloud_live`/`workspace_failpoints`）的 fn 行入 `external` 组（10 纯 cloud_live + 197 复合 + 1 纯 workspace_failpoints = **208**）、纯 `global` 站点行按宿主 target 整 target 入组（**7** 个 `binary(=<target>)`）；`max-threads = 1`；fn 成员用锚定正则 `test(/(^\|::)<fn>$/)` 精确过滤（聚合二进制的完整测试名带模块路径，裸名等值不命中；fn 名经守卫保证全树唯一，末段锚定即精确——NP-01 执行评审 R1 修正）。键隶属事实不变：含 `cloud_live` 207、含 `workspace_failpoints` 198。cwd/env/hash_kind 键不生成组（ADR-NP-01）。`tests/compat/serial_registry.rs` 增 drift 守卫（重生成逐字节一致 + 成员派生对账）。唯一行为轴 = 调度器可见互斥的机械派生。

**Out of scope:** 运行器安装与执行（NP-02）；CI（NP-04）；profile 段内容（NP-02 经生成器扩展）。

**Current evidence:** 事实基线行 7（外部资源面计数）。

**Acceptance criteria:**

- [ ] `.config/nextest.toml` 生成物落库；头注「generated — do not edit」+ 生成命令
- [ ] 组集与成员完全由注册表**键隶属**派生：`external` 组 = 并集 208 条 fn 过滤（10 纯 cloud_live + 197 复合 + 1 纯 workspace_failpoints）+ 7 条站点宿主 `binary(=<target>)`；注册表键隶属事实对账（含 cloud_live 207、含 workspace_failpoints 198、纯 global 站点 7）逐条成立
- [ ] drift 守卫：重生成 `cmp` 一致；人工改一字节反例断言失败
- [ ] 生成器幂等且确定（两跑一致）
- [ ] `tests/compat/README.md` + `tests/INDEX.md` 登记（GC-06）

**Verification:**

- [ ] `sh tests/NEXTEST_GROUPS.sh && sh tests/NEXTEST_GROUPS.sh --stdout | cmp - .config/nextest.toml && echo OK`
- [ ] `source .env.test && LIBRA_SKIP_WEB_BUILD=1 cargo test --test compat_serial_registry`
- [ ] 对账（注册表 lane 计数 vs toml 组成员计数）：

  ```bash
  cl=$(awk -F'\t' 'NR>1 && $2 ~ /(^|[:+])cloud_live($|[+])/' tests/SERIAL_REGISTRY.tsv | wc -l | tr -d ' ')
  wf=$(awk -F'\t' 'NR>1 && $2 ~ /(^|[:+])workspace_failpoints($|[+])/' tests/SERIAL_REGISTRY.tsv | wc -l | tr -d ' ')
  gl=$(awk -F'\t' 'NR>1 && $2=="global"' tests/SERIAL_REGISTRY.tsv | wc -l | tr -d ' ')
  echo "key-membership: cloud_live=$cl workspace_failpoints=$wf pure-global(sites)=$gl"
  [ "$cl" -eq 207 ] && [ "$wf" -eq 198 ] && [ "$gl" -eq 7 ] || { echo "FAIL: registry key counts drifted"; exit 1; }
  tf=$(grep -c "^filter = 'test(/" .config/nextest.toml | tr -d ' ')
  tb=$(grep -c "^filter = 'binary(=" .config/nextest.toml | tr -d ' ')
  [ "$tf" -eq 208 ] && [ "$tb" -eq 7 ] || { echo "FAIL: toml member counts tf=$tf tb=$tb"; exit 1; }
  echo "OK"  # 成员级（逐 fn）对账断言由 NP-01 交付的守卫用例承载
  ```

**Full-suite trigger:** none（新增生成器与配置 + 守卫；`.config/` 不在 T-1 面）

**Dependencies:** NP-00

**Deliverables:** N/A（implementation）

**Implementation write set:** `tests/NEXTEST_GROUPS.sh`（新）；`.config/nextest.toml`（新）；`tests/compat/serial_registry.rs`；`tests/compat/README.md`；`tests/INDEX.md`

**Release write set:** Inherited

**Files likely touched:** 同上

**Docs and compatibility impact:** README/INDEX 行（默认覆盖）

**Rollback mode:** revert

**Migration and rollback:** revert 删除生成器与 toml；无状态

**Security and privacy:** 继承默认

**Performance budget:** 生成器 ≤ 5 s；守卫增量 ≤ 10 s

**Estimated scope:** S

**Version increment:** patch

**Release boundary:** independent

**C/D coverage from:** self

**Granularity:** `type=implementation; axis=调度器互斥的机械派生; recovery=revert 单提交; complete=yes; self-contained=yes; AC=5/8; VER=3/8; landing=0; prod-files=0; scope=S; deps=NP-00; writeset=序列化于 NP-00,NP-02; release=independent; split-from=plan-20260825 TA-06; exception=N/A`

### Task NP-02: 本地 nextest 执行起立

**Task type:** implementation

**Lifecycle / Acceptance:** pending / 空

**Description:** 钉版安装 cargo-nextest `0.9.143`（GC-NP-04：`cargo install cargo-nextest --version 0.9.143 --locked`），把既有执行配方映射为 nextest profile：默认 profile（普通全量）与 `test-provider` profile——**profile 只承载运行器行为（组/线程/junit），Cargo feature 与 env 必须显式随命令给出**：`LIBRA_ENABLE_TEST_PROVIDER=1 LIBRA_SKIP_WEB_BUILD=0 cargo nextest run --features test-provider --profile test-provider --test code_ui_scenarios --test harness_self_test --test code_codex_default_web_test --test ai_code_ui_headless_test --test code_codex_runtime_test --test code_ui_remote_lease_matrix --test code_ui_remote_sse_matrix --test code_ui_remote_state_matrix --test code_mcp_dual_entry_test --test code_ui_perf_smoke_test`（base.yml:401 逐字十 target）（原 `--test-threads=1` 语义由互斥组承载，`LIBRA_SKIP_WEB_BUILD=0` 保持场景所需真实 web 资产）；`otlp`/`keyring`/`test-upgrade` 三个 feature 段保持 cargo test 原样（required-features 单 target，nextest 收益为零，留在 NP-04 的 CI 面原段）。首次 `cargo nextest run` 全量两连跑零新失败；新剥落逐条回退标注并登记。唯一行为轴 = 本地运行器切换。

**Out of scope:** CI（NP-04）；度量结论（NP-03——本卡只要求绿）。

**Current evidence:** 事实基线行 8（nextest 未安装）。

**Acceptance criteria:**

- [ ] 钉版安装命令入 `CLAUDE.md`（`cargo install cargo-nextest --version 0.9.143 --locked` + `cargo nextest --version` 校验输出）
- [ ] 两个 profile 落 `.config/nextest.toml`（profile 段同为生成物，经 NP-01 生成器扩展）
- [ ] `cargo nextest run --all` 两连跑零新失败；`test-provider` profile 覆盖 base.yml:401 十个 `--test` target（与 Description 命令逐字一致）且绿
- [ ] 外部组互斥取证：`external` 组内并发度 = 1（nextest 事件/junit 取证）
- [ ] 新剥落（如有）回退标注 + 注册表登记现象；交付物零已知剥落

**Verification:**

- [ ] `source .env.test && LIBRA_SKIP_WEB_BUILD=1 cargo nextest run --all` ×2（junit 留档：`--profile default`，`target/nextest/default/junit.xml` 复制归档）
- [ ] `source .env.test && LIBRA_ENABLE_TEST_PROVIDER=1 LIBRA_SKIP_WEB_BUILD=0 cargo nextest run --features test-provider --profile test-provider --test code_ui_scenarios --test harness_self_test --test code_codex_default_web_test --test ai_code_ui_headless_test --test code_codex_runtime_test --test code_ui_remote_lease_matrix --test code_ui_remote_sse_matrix --test code_ui_remote_state_matrix --test code_mcp_dual_entry_test --test code_ui_perf_smoke_test` ×1（十 target 逐字取自 base.yml:401；feature 选择是 Cargo 语义、profile 只配运行器行为，二者必须同给；`LIBRA_SKIP_WEB_BUILD=0` 保持 CI 原语义——场景需要真实内嵌 web 应用）
- [ ] 实际组成员对账（NP-01 R1 修正的过滤真实命中核证）：`source .env.test && LIBRA_SKIP_WEB_BUILD=1 cargo nextest list -E 'group(external)' | grep -cE '^\s'` 结果 = 208 + 7 个站点宿主 target 的全部用例数（以 `cargo nextest list --test <target>` 逐一求和为准）；任何缺项即 FAIL
- [ ] 组并发取证（junit 时间戳重叠检查；具体断言脚本随 NP-02 交付并在此回填命令）：

  ```bash
  python3 tests/nextest_group_overlap_check.py target/nextest/default/junit.xml external \
    || { echo "FAIL: external group overlapped"; exit 1; }
  ```

**Full-suite trigger:** none（`.config` + `CLAUDE.md` + 可能的个别属性回退；本卡 A 组即全量 nextest 两连跑，另由完成判据三门兜底）

**Dependencies:** NP-01；DEP-NP-03

**Deliverables:** N/A（implementation）

**Implementation write set:** `.config/nextest.toml`（经生成器）；`tests/NEXTEST_GROUPS.sh`（profile 段生成）；`tests/nextest_group_overlap_check.py`（**新增，本卡交付**——组并发取证脚本，Verification 第三段的执行前提）；`CLAUDE.md`；（如有回退）个别测试文件属性行 + `tests/SERIAL_REGISTRY.tsv`

**Release write set:** Inherited

**Files likely touched:** 同上

**Docs and compatibility impact:** `CLAUDE.md` 构建/测试段

**Rollback mode:** revert

**Migration and rollback:** revert 后回到 cargo test（属性未删、语义不回退）

**Security and privacy:** 继承默认；nextest 为本地执行器，无网络面

**Performance budget:** 本卡不设墙钟门（NP-03 承载）

**Estimated scope:** M

**Version increment:** patch

**Release boundary:** independent

**C/D coverage from:** self

**Granularity:** `type=implementation; axis=本地运行器切换; recovery=revert（属性未删、语义不回退）; complete=yes; self-contained=yes; AC=5/8; VER=3/8; landing=0; prod-files=0; scope=M; deps=NP-01,DEP-NP-03; writeset=序列化于 NP-01,NP-03; release=independent; split-from=plan-20260825 TA-06; exception=N/A`

### Task NP-03: nextest 原生度量取证

**Task type:** audit

**Lifecycle / Acceptance:** pending / 空

**Out of scope:** 任何代码/配置改动（发现需要即按 ER-03 重分类）；多样本统计（ADR-NP-03 单样本对照）；CI 时长（NP-04 D 组取证）。

**Current evidence:** 基线常量 5921 s（事实基线行 1，用户裁决背书）；nextest 计时/junit 能力见其配置参考（NP-02 交付后即有 `target/nextest/default/junit.xml`）。

**Description:** 以登记常量 5921 s 为对照，在安静机器采 `cargo nextest run --all` 单次墙钟。硬门 ≤ 0.5×（≤ 2960 s）。导出 per-test 计时 top-20 慢用例清单（为 TA-05 / DEFER-NP-01 / DEFER-NP-02 提供靶点）。数值写回本计划性能摘要与 `plan-20260825.md` 承接簿记。本卡替代被豁免的 TA-03 度量门族（ADR-NP-03）。no-code / no-config：只执行命令并把结果写入计划文档。唯一行为轴 = 提速证据。

**Out of scope:** 任何代码/配置改动（发现需要改动即按 ER-03 重分类）。

**Acceptance criteria:**

- [ ] 墙钟样本落档（命令、机器状态、rc）；硬门 ≤ 2960 s
- [ ] top-20 慢用例清单落档并回链三个延后项
- [ ] 本计划性能摘要 + `plan-20260825.md` 簿记回填

**Verification:**

- [ ] 计时（模板退出码）：

  ```bash
  source .env.test
  s=$(date +%s)
  LIBRA_SKIP_WEB_BUILD=1 cargo nextest run --all || { echo "ERROR: run failed"; exit 2; }
  e=$(date +%s); w=$((e-s))
  echo "wall=${w}s baseline=5921s bound=2960s"
  [ "$w" -le 2960 ] || { echo "FAIL: ${w}s > 2960s"; exit 1; }
  echo "OK"
  ```

- [ ] top-20 慢用例（junit 解析，结果表写回本计划性能摘要）：

  ```bash
  python3 - <<'PY'
  import xml.etree.ElementTree as ET
  t=ET.parse('target/nextest/default/junit.xml').getroot()
  rows=sorted(((float(c.get('time',0)),c.get('classname',''),c.get('name','')) for c in t.iter('testcase')),reverse=True)[:20]
  for w,cl,n in rows: print(f"{w:8.1f}s  {cl}::{n}")
  PY
  ```

**Full-suite trigger:** none（audit，无推送产物；文档随 NP-04 发布进入渠道）

**Dependencies:** NP-02

**Deliverables:** 本计划「性能与容量摘要」更新行；`plan-20260825.md` 簿记行；top-20 清单（计划内表格）

**Implementation write set:** `docs/development/plan/plan-20260827.md`；`docs/development/plan/plan-20260825.md`

**Release write set:** N/A（no-release）

**Files likely touched:** 同上

**Docs and compatibility impact:** 两计划文档

**Rollback mode:** forward-only（度量档案与簿记只前进；错误以更正行修复）

**Migration and rollback:** N/A

**Security and privacy:** 继承默认

**Performance budget:** 采样 ≤ 1 次全量 + 整理 ≤ 30 min

**Estimated scope:** S

**Version increment:** N/A（no-release；产物随 NP-04 发布进入用户渠道）

**Release boundary:** no-release（承载点 NP-04）

**C/D coverage from:** NP-04

**Granularity:** `type=audit; axis=提速证据; recovery=forward-only 更正行; complete=yes; self-contained=yes; AC=3/20; VER=2/20; landing=0; prod-files=0; scope=S; deps=NP-02; writeset=序列化于 NP-02,NP-05; release=no-release(承载 NP-04); split-from=plan-20260825 TA-03 度量门; exception=单样本对照替代 3+3 中位数（ADR-NP-03，用户裁决）`

### Task NP-04: CI 执行面切换

**Task type:** implementation

**Lifecycle / Acceptance:** pending / 空

**Current evidence:** 真实 CI 面六段见事实基线「真实 CI 面」行（`base.yml:326-460` 2026-08-27 逐段核读：⓪ alignment `--exact` 单测在 `base.yml:330`）；runner 为 self-hosted 池（`CLAUDE.md` CI 段）。

**Description:** 按事实基线登记的**真实 CI 面**逐段切换 `.github/workflows/base.yml`：⓪ 前置 `compat_matrix_alignment` `--exact` 单测**保留 cargo test 原样**（单用例语义、nextest 零收益）；① `compat-offline-core` 的「枚举 target + `--lib --bins`」段与 ② `compat-offline-command`（`command_test` 分片 job）→ 钉版 nextest（默认 profile；分片语义由 nextest 全局调度吸收，两 job 可合并或保留按卡内锚点刷新后判定）；③ `test-provider` 场景段 → `LIBRA_ENABLE_TEST_PROVIDER=1 LIBRA_SKIP_WEB_BUILD=0 cargo nextest run --features test-provider --profile test-provider --test code_ui_scenarios --test harness_self_test --test code_codex_default_web_test --test ai_code_ui_headless_test --test code_codex_runtime_test --test code_ui_remote_lease_matrix --test code_ui_remote_sse_matrix --test code_ui_remote_state_matrix --test code_mcp_dual_entry_test --test code_ui_perf_smoke_test`（feature/env/十 target 逐字保留，profile 仅换运行器行为）；④ `cargo test --doc` 保留原样（nextest 不跑 doctest）；⑤ `otlp`/`keyring`/`test-upgrade` 三个 required-features 单 target 段保留 cargo test 原样。原命令行注释保留（ADR-NP-02）。唯一行为轴 = CI 执行面。

**Out of scope:** 其它 workflow（codeql/release/nightly）；权威三门口径（ADR-NP-04）。

**Acceptance criteria:**

- [ ] 六段处置逐段落地（⓪ alignment 单测与 ④⑤ 原样保留、①②③ 切换）；runner 上 nextest 安装步骤幂等且钉版
- [ ] CI 全绿一轮（push 后 D 组取证）
- [ ] 回退注释 + `CLAUDE.md` CI 段同步
- [ ] ⓪ alignment 单测、④ doctest 与 ⑤ 三个 feature 段语义零变化（命令逐字保留）

**Verification:**

- [ ] 切换取证（注释保留旧命令，故不用零命中门；断言 = 三个被切换段都已是 nextest、三个保留段逐字未动）：

  ```bash
  W=.github/workflows/base.yml
  # 非注释行锚定（^\s* 后直接是命令）——ADR-NP-02 的旧命令注释（# 前缀）不会满足任何一条
  n=$(rg -c '^\s*([A-Za-z_][A-Za-z0-9_]*=\S+\s+)*cargo nextest run' "$W" || true)
  [ "${n:-0}" -ge 3 ] || { echo "FAIL: expected >=3 non-comment nextest invocations, got ${n:-0}"; exit 1; }
  rg -q '^\s*(LIBRA_ENABLE_TEST_PROVIDER=1\s+LIBRA_SKIP_WEB_BUILD=0\s+)?cargo nextest run --features test-provider --profile test-provider' "$W" \
    || { echo "FAIL: provider segment not on pinned nextest shape (env may live in the step env: block)"; exit 1; }
  rg -q '^\s*run: cargo test --test compat_matrix_alignment compatibility_matrix_matches_cli_commands -- --exact$' "$W" \
    || { echo "FAIL: alignment step altered"; exit 1; }
  rg -q '^\s*cargo test --doc' "$W" || { echo "FAIL: doctest step altered"; exit 1; }
  rg -q '^\s*cargo test --features otlp --test otlp_telemetry -- --test-threads=1$' "$W" \
    || { echo "FAIL: otlp step altered"; exit 1; }
  rg -q '^\s*cargo test --features keyring --test auth_keyring_backend -- --test-threads=1$' "$W" \
    || { echo "FAIL: keyring step altered"; exit 1; }
  rg -q '^\s*cargo test --features test-upgrade \\$' "$W" \
    && rg -q '^\s*--test upgrade_auto_test \\$' "$W" \
    && rg -q -- '^\s*--test upgrade_publish_contract_test -- --test-threads=1$' "$W" \
    || { echo "FAIL: test-upgrade step altered"; exit 1; }
  echo "OK: >=3 live nextest invocations; alignment/doctest/otlp/keyring/test-upgrade preserved"
  ```
- [ ] push 后 `gh run` 首轮结果（D 组）

**Full-suite trigger:** `T-1: 触碰 .github/workflows/**` —— C 组第 ④ 步跑 `source .env.test && cargo test --all`

**Dependencies:** NP-05（承载顺序：本卡 C 组收口运行必须包含 NP-03/NP-05 的最终变更）

**Deliverables:** N/A（implementation）

**Implementation write set:** `.github/workflows/base.yml`；`CLAUDE.md`

**Release write set:** Inherited

**Files likely touched:** 同上

**Docs and compatibility impact:** `CLAUDE.md` CI 段

**Rollback mode:** revert

**Migration and rollback:** revert 单提交回 cargo test（注释即回退说明书）

**Security and privacy:** runner 安装 nextest 二进制——钉版 + 官方发布源，GC-NP-04

**Performance budget:** CI 时长预期下降（取证于 D 组首轮，不设硬门）

**Estimated scope:** M

**Version increment:** patch

**Release boundary:** independent

**C/D coverage from:** self（含 D 组远端门）

**Granularity:** `type=implementation; axis=CI 执行面; recovery=revert 单提交; complete=yes; self-contained=yes; AC=4/8; VER=2/8; landing=0; prod-files=0; scope=M; deps=NP-05; writeset=序列化于 NP-05; release=independent; split-from=plan-20260825 TA-07; exception=Full-suite `T-1``

### Task NP-05: 文档与承接簿记收口

**Task type:** docs

**Lifecycle / Acceptance:** pending / 空

**Out of scope:** 代码/配置改动（docs 卡 no-code/no-config）；`CLAUDE.md` CI 段（NP-04 写集）；权威三门口径变更（DEFER-NP-04）。

**Current evidence:** 三处文档现状——`CLAUDE.md` 构建/测试段无 nextest 记述、`tests/INDEX.md` 与 `tests/compat/README.md` 已含 NP-00 落地的转换记述（工作树）；`plan-20260825.md` 承接行已在（NP-00 落地时提交）。

**Description:** `CLAUDE.md` 构建/测试段（本地 nextest 附加口径；CI 段终态由 NP-04 写集同卡承载）、`tests/INDEX.md`、`tests/compat/README.md` 终态同步；`plan-20260825.md` 承接终态（TA-06/07「由 plan-20260827 承接完成」+ 性能摘要回填）；本计划完成判据自检清单预填（NP-04 后打勾）。no-code / no-config。先于承载点 NP-04 执行，产物随其发布进入渠道。唯一行为轴 = 文档终态一致性。

**Acceptance criteria:**

- [ ] 三处运行器文档与实际命令逐字一致（复制可执行）
- [ ] `plan-20260825.md` 承接行与本计划回链闭环
- [ ] 完成判据逐条打勾留档

**Verification:**

- [ ] 文档命令逐条实跑（每条以退出码判定；命令与文档正文逐字一致）：

  ```bash
  set -uo pipefail
  # CLAUDE.md 构建/测试段中声明的本地 nextest 命令必须原样可执行
  source .env.test
  LIBRA_SKIP_WEB_BUILD=1 cargo nextest run --all --no-run \
    || { echo "FAIL: documented nextest command does not build"; exit 1; }
  # 三处文档提及的守卫/生成器命令
  sh tests/NEXTEST_GROUPS.sh --stdout | cmp - .config/nextest.toml \
    || { echo "FAIL: documented generator command drifted"; exit 1; }
  LIBRA_SKIP_WEB_BUILD=1 cargo test --test compat_serial_registry \
    || { echo "FAIL: documented guard command red"; exit 1; }
  echo "OK: documented commands executable"
  ```

- [ ] 回链闭环检查（两计划互引零断链）：

  ```bash
  if rg -n 'plan-20260827' docs/development/plan/plan-20260825.md >/dev/null; then :; else
    echo "FAIL: 0825 -> 0827 link missing"; exit 1; fi
  if rg -n 'plan-20260825' docs/development/plan/plan-20260827.md >/dev/null; then :; else
    echo "FAIL: 0827 -> 0825 link missing"; exit 1; fi
  echo "OK: cross-links intact"
  ```

**Full-suite trigger:** none（docs）

**Dependencies:** NP-03

**Deliverables:** `CLAUDE.md`（构建/测试段本地 nextest 附加口径；CI 段终态归 NP-04 写集）；`tests/INDEX.md`；`tests/compat/README.md`；`docs/development/plan/plan-20260825.md`；`docs/development/plan/plan-20260827.md`（自检清单）

**Implementation write set:** 同 Deliverables

**Release write set:** N/A（no-release）

**Files likely touched:** 同上

**Docs and compatibility impact:** 全部为文档面

**Rollback mode:** revert

**Migration and rollback:** N/A

**Security and privacy:** 继承默认

**Performance budget:** ≤ 30 min

**Estimated scope:** S

**Version increment:** N/A（no-release；产物随 NP-04 发布进入用户渠道）

**Release boundary:** no-release（承载点 NP-04）

**C/D coverage from:** NP-04

**Granularity:** `type=docs; axis=文档终态一致性; recovery=revert; complete=yes; self-contained=yes; AC=3/20; VER=2/20; landing=0; prod-files=0; scope=S; deps=NP-03; writeset=序列化于 NP-03,NP-04; release=no-release(承载 NP-04); split-from=N/A; exception=N/A`

## 测试矩阵

| 面 | 门 | 承载卡 |
|---|---|---|
| 转换功能正确性 | 冻结/幂等/none==0/守恒重采/守卫 17 用例/冒烟四 target | NP-00 |
| 互斥组派生 | 对账 + drift 守卫 + 反例 | NP-01 |
| 运行器正确性 | 全量两连跑零新失败 + 组并发取证 | NP-02 |
| 提速 | ≤0.5×基线常量硬门 + top-20 清单 | NP-03 |
| CI | 六段处置 diff 评审 + 首轮全绿（D 组） | NP-04 |
| 文档一致性 | 命令逐条实跑 | NP-05 |

## 追溯表

| 来源 | 承接 |
|---|---|
| 用户裁决（2026-08-27：停 TA-03 基线、豁免度量门族、按多进程多目录重立计划） | 标题块、ADR-NP-03、NP-00、NP-03 |
| plan-20260825 TA-03（转换本体） | NP-00 |
| plan-20260825 TA-06 | NP-01、NP-02 |
| plan-20260825 TA-07 | NP-04 |
| plan-20260729 DEFER-09 | NP-00（关闭句措辞） |
| 本会话实测（基线 5921 s、manifest 计数、CI 面逐段核读） | 事实基线 |

## 里程碑验收与回滚

- M-NP-A（NP-00..01）：转换落地 + 组生成器——回滚 = 逐卡 revert。
- M-NP-B（NP-02..03）：本地 nextest 绿 + 提速实证——回滚 = revert 后 cargo test 全语义可用。
- M-NP-C（NP-05 → NP-04）：文档收口 + CI 切换（承载发布）——回滚 = revert base.yml 单提交。

## 故障恢复矩阵

| 故障 | 恢复 |
|---|---|
| NP-00 守恒重放树残留（worktree remove 失败/中断） | `git worktree remove --force /tmp/np00_conswt`；仍失败 `git worktree prune` + `rm -rf /tmp/np00_conswt`。重放树只读、主树零写入，残留不影响任何门 |
| NP-02 两连跑出现新剥落 | 该用例回退原标注 + 注册表登记现象（原 TA-03 条款继承）；组生成器重跑 |
| NP-03 硬门不达（>2960 s） | 停排 NP-04；按 top-20 归因——init 主项 → 升格 DEFER-NP-01；global 组拖尾 → 升格 DEFER-NP-02；均否 → 触发 DEFER-PS-06 评估 |
| NP-04 CI 首轮红 | revert base.yml 单提交（ADR-NP-02），本地复现后重上 |
| 机器重启丢 /tmp 证据 | 证据一律「落档 = 写入计划文档或仓库」，/tmp 只作暂存（本轮教训已固化于各卡 Verification 措辞） |

## 风险登记

| 风险 | 缓解 |
|---|---|
| 一测一进程暴露隐藏跨用例共享态 | NP-02 两连跑 + 回退登记条款；TA-05（原计划）降低冷启动成本 |
| nextest 版本漂移 / runner 安装失败 | GC-NP-04 钉版；安装步骤幂等 + 一步回退注释 |
| `global` 204 条全组互斥拖尾 | 全局调度吸收；NP-03 归因后按升格通道处理（DEFER-NP-02） |
| 并发节点共享工作树 | GC-NP-03 精确暂存；每卡 ER-01 开工检查 |
| alignment/doctest/required-features 段被误迁移 | NP-04 六段处置逐段列明，⓪④⑤ 明令保留原样 |

## 性能与容量摘要

- 基线常量：全量 `cargo test --all --no-fail-fast` = **5921 s**（方法与丢失注记见事实基线行 1）。
- NP-03 硬门：nextest 全量 ≤ **2960 s**；工程预期 15–25 min。
- 后续叠加（不在本计划）：TA-05（原计划）落地后预期 10–15 min 带；DEFER-NP-01 视 top-20 归因。

## 兼容与文档收口

- 用户可见行为零变化（纯测试执行面）；`CLAUDE.md` 三门权威口径不变（ADR-NP-04）。
- 文档收口面：`CLAUDE.md`、`tests/INDEX.md`、`tests/compat/README.md`、两份计划簿记（NP-00/NP-03/NP-05 分批承载）。

## Codex review log

| 轮次 | 结论 | 备注 |
|---|---|---|
| R1（2026-08-27，gpt-5.5） | FAIL：7×P1 | 结构缺章、NP-03/NP-05 词表、ER-13 削弱、双向承接、CI 事实、/tmp 证据——全部于同日修订收口（见修订记录首行） |

## 非目标与延后项

- `DEFER-NP-01`：磁盘级 fixture 模板 + APFS clonefile 克隆（keygen 摊销每套件一次；须先核实 `.libra/` 内绝对路径面）。重启条件：NP-03 top-20 显示 init 仍为主项。
- `DEFER-NP-02`：`global` 组降密（for_each_ref 23 + show_ref 19 等 command_test 热点体证明、降窄 lane）。重启条件：NP-03 显示 global 组拖尾主项。
- `DEFER-NP-03`：进程内直调（spawn → `execute(args)`，薄层真 CLI 冒烟保留）。一测一进程使直调无全局态串扰、可行性上升，但需改数千调用点，属 L 级独立计划体量。重启条件：NP-03 top-20 显示 spawn 开销为剩余主项，且另立计划承载。
- `DEFER-NP-04`：`CLAUDE.md` 三门权威口径迁移至 nextest——须按模板 ER-04 同批同步 `CLAUDE.md`/`AGENTS.md`/现行模板计划，独立立卡（ADR-NP-04）。
- `DEFER-PS-06`（继承）：cwd 显式路径重构，仅当 nextest 在 NP-02/03 被实测否决时启动。
- 升格通道：`DEFER-NP-01`/`DEFER-NP-02` 满足重启条件时按 ER-10 升格为 NP-06/NP-07 正式卡（重过 G-* 门），不得未升格即执行。

## 完成判据

- NP-00..NP-05 全部 `Lifecycle=done`。执行期收口口径：NP-00/01/02 以 `Acceptance=locally-accepted` 关闭（发布挂起窗口，见「发布分组与并发窗口」），补发窗口逐卡补 `gh release` + `release.yml` D 证据后翻转 `complete`；NP-04 取得 D 组远端证据（push 后 `gh run` 首轮 CI 结果，不依赖版本发布）后 `Acceptance=complete`；NP-03/NP-05 经承载点继承收口。
- 成功定义五条逐条核销；两计划承接簿记闭环。
- **全量收口门（模板三门，不降级）**：`cargo +nightly fmt --all --check`、`cargo clippy --all-targets --all-features -- -D warnings`、`source .env.test && cargo test --all` 全绿，暴露的 Bug 修复后重跑到绿；`cargo nextest run --all` 绿为**附加**证据（权威口径迁移见 DEFER-NP-04）。

## 任务卡粒度审计表

| 任务 | type | axis | recovery | complete | self-contained | AC | VER | landing / prod-files | scope | deps | writeset | release | split-from | exception |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| NP-00 | implementation | 转换产物落地经济学 | revert 单提交 | yes | yes | 6/8 | 4/8 | 1/0 | S | DEP-NP-01 | 序列化于 NP-01..NP-05 | independent | plan-20260825 TA-03 | 用户裁决豁免度量门族（ADR-NP-03） |
| NP-01 | implementation | 调度器互斥的机械派生 | revert 单提交 | yes | yes | 5/8 | 3/8 | 0/0 | S | NP-00 | 序列化于 NP-00,NP-02 | independent | plan-20260825 TA-06 | N/A |
| NP-02 | implementation | 本地运行器切换 | revert（属性未删、语义不回退） | yes | yes | 5/8 | 3/8 | 0/0 | M | NP-01,DEP-NP-03 | 序列化于 NP-01,NP-03 | independent | plan-20260825 TA-06 | N/A |
| NP-03 | audit | 提速证据 | forward-only 更正行 | yes | yes | 3/20 | 2/20 | 0/0 | S | NP-02 | 序列化于 NP-02,NP-05 | no-release(承载 NP-04) | plan-20260825 TA-03 度量门 | 单样本对照替代 3+3 中位数（ADR-NP-03，用户裁决） |
| NP-04 | implementation | CI 执行面 | revert 单提交 | yes | yes | 4/8 | 2/8 | 0/0 | M | NP-05 | 序列化于 NP-05 | independent | plan-20260825 TA-07 | Full-suite `T-1` |
| NP-05 | docs | 文档终态一致性 | revert | yes | yes | 3/20 | 2/20 | 0/0 | S | NP-03 | 序列化于 NP-03,NP-04 | no-release(承载 NP-04) | N/A | N/A |
