# 全局配置迁移到 `<config dir>/libra`（XDG）计划（2026-09-19）

> **模板版本:** `v2.6`。尚未开工。所有未执行任务卡、ER-13 全量门及完成判据的全量收口门一律运行 `source .env.test && source .env.live-test && cargo nextest run --all --no-fail-fast --retries 2`。`cargo test --all` 只供诊断，不能当通过证据。
>
> **本文件只规划任务，不宣称已实现。** 任何卡在最新一轮 Codex 字面 `VERDICT: PASS` 之前不得标 `in-progress`。
>
> **用户裁决（2026-09-19，本次成稿输入）:** ① 目标目录 = `<XDG_CONFIG_HOME 或 $HOME/.config>/libra`，**macOS 同样使用 `~/.config/libra`**（不用 `~/Library/Application Support`）；② 旧 `~/.libra/config.db` 采用**首次使用自动迁移**（策略 A，保留旧文件为备份）；③ **全域 vault 随之迁移**；④ `~/.libra` 继续作为 `LIBRA_HOME`（`bin/`、`env`、`upgrade/` 不动）；⑤ 发布级别 `patch`；⑥ 交付仓库规范日期计划。

## 文档职责

本文把 CLI 的**用户级（global）配置存储位置**从 Libra home（`~/.libra/config.db`）迁到平台一致的 XDG 配置目录（`<XDG_CONFIG_HOME 或 ~/.config>/libra/config.db`，macOS 同），并让全域 vault unseal key 与之同域；同时为已存在的用户数据提供一次性、可验证、可回退的自动迁移。

目标是：新用户与现有用户最终都只使用 `<config dir>/libra/config.db`；旧库在首次使用时被安全复制过去并原样保留作为备份；`LIBRA_HOME` 与升级/安装状态维持现状；三处重复的路径解析收敛为单一事实源；文档与测试不再钉死旧路径。

### 适用范围

- 用户级配置作用域（`ConfigScope::Global`）的路径决议、打开/创建、级联读取与写入。
- `LIBRA_CONFIG_GLOBAL_DB` 覆写语义（保持不变）；`XDG_CONFIG_HOME` 的采纳（仅绝对路径）。
- 旧路径 `~/.libra/config.db` 的回退读取与首次使用自动迁移（含锁、快照、校验、原子提交、失败语义）。
- 全域 vault unseal key：`~/.libra/vault-unseal-key` → `<config dir>/libra/vault-unseal-key`（0600）。
- 用户级 hooks 文件路径对齐：`dirs::config_dir()/libra/hooks.json` → 同一配置目录（macOS 读旧位置作为兼容回退）。
- 同步：`docs/commands/config.md` EN + zh-CN、`docs/development/commands/config.md`、`docs/development/account.md`、`docs/commands/init.md` EN + zh-CN、`docs/development/commands/init.md`、`COMPATIBILITY.md` 的 `config` 行、`../libra-backend` 网站对应页、`docs/error-codes.md`（仅在文案/示例需要时）。

### 非目标

- **不改** system 作用域 `/etc/libra/config.db`（及 `LIBRA_CONFIG_SYSTEM_DB`）：XDG 不适用于机器级配置。
- **不改** per-repo 存储：`.libra/libra.db`、`.libra/vault.db`、`~/.libra/vault-keys/<repo-id>`（每仓库密钥属仓库状态，不是用户级配置；不迁移、不改路径）。
- **不改** `~/.libra` 作为 `LIBRA_HOME` 的角色：`bin/`、`env`、`env.fish`、`seccomp.bpf`、`upgrade/`、`tmp/` 全部留在原处。
- **不改** SQLite schema、migration ledger、稳定错误码：本次是文件位置迁移，不是 schema 迁移（GC-13 只要求角色与隔离证明）。
- **不新增** `libra config migrate-global` 显式子命令（策略 A 自动迁移已覆盖；DEFER-GCX-01 保留重启条件）。
- **不做** 旧文件的自动删除/覆盖：迁移后旧文件永久保留，用户自行决定何时清理。
- **不做** `XDG_DATA_HOME`/`XDG_STATE_HOME` 的全面 XDG 化（cache/state 另议）。

### 成功定义

- 新用户：`HOME=$(mktemp -d) libra config set --global user.email a@b.c` 只创建 `<HOME>/.config/libra/config.db`，不创建 `<HOME>/.libra/config.db`。
- 现有用户：仅有 `~/.libra/config.db` 时，首次任何 global 配置访问都把它安全复制到 `<config dir>/libra/config.db`；旧文件字节不变；后续命令只读写新文件；`libra config doctor --global-schema` 报告迁移来源与两份路径。
- 迁移幂等：重复执行/并发执行不产生第二次写入，不损坏任一份数据。
- 失败安全：迁移失败时读取回退旧库并有可操作警告，写入 fail-closed 报可操作错误；旧库永不被修改或删除。
- 全域 vault：unseal key 迁移到 `<config dir>/libra/vault-unseal-key` 后，迁移前写入的加密值仍可解密（不轮换密钥）；旧 key 文件保留为备份。
- `LIBRA_CONFIG_GLOBAL_DB` 覆写行为与测试隔离语义完全不变；`~/.libra/vault-keys/<repo-id>` 与 `tmp/` 位置不变。
- 三处重复解析器消失，`rg` 零命中 `join(".libra").join("config.db")`（测试与迁移模块内除外，且登记 allowlist）。
- 全部非延后卡 `Lifecycle=done` 且 `Acceptance=complete`；全量收口门全绿。

## 事实基线

> 成稿核对日 2026-09-19；GCX-02/03/04 开工日 **2026-09-22** 按 ER-02 重刷（见下表「2026-09-22 重刷」列）。成稿基线 commit `4ed9154`，版本面 `0.23.0`（成稿时四处）。**2026-09-22 事实**：tip `fc6e48f`，版本面 `0.23.45` 且已收为**三处**（`web/` 与 `web/package.json` 已随 0.23.x 拆除），工作树改由 Git 承载（存在 `.git`，无 `.libra/`）。

| 类别 | 当前事实 | 证据 |
|---|---|---|
| Libra tip / 版本 | 成稿 `4ed9154` / `0.23.0`；**2026-09-22 重刷** `fc6e48f` / `0.23.45` | `git log -1 --oneline`；`Cargo.toml:3` |
| 工作树 VCS | 成稿时 `.libra/`，无 `.git`；**2026-09-22 重刷**：本检出改由 Git 承载（`.git` 存在、`.libra/` 不存在），故本轮版本控制操作使用 `git`，CLAUDE.md 的 libra-only 约定在此检出不成立 | `test -d .git`；`libra status` → `LBR-REPO-001` |
| 版本面（ER-08 权威） | ~~四处~~ → **三处**（2026-09-22 重刷）：`Cargo.toml`、`install.sh`、`install.ps1`（`web/package.json` 随 `web/` 于 0.23.x 拆除） | `tests/compat/version_surface_sync.rs`；`grep -n 'DEFAULT_VERSION=' install.sh`、`grep -n 'DefaultVersion' install.ps1`（均 `v0.23.45`） |
| global 路径解析器 #1 | `global_config_path()`：env 覆写优先，否则 `~/.libra/config.db` | `src/internal/config.rs:1261-1272` |
| global 路径解析器 #2 | `ConfigScope::get_config_path()`：同样回退 `~/.libra/config.db` | `src/command/config.rs:99-120` |
| global 路径解析器 #3 | `storage_global_config_path()`：storage/cloud 侧重复实现 | `src/utils/client_storage.rs:2556-2565` |
| system 作用域 | `/etc/libra/config.db`，`LIBRA_CONFIG_SYSTEM_DB` 覆写 | `src/command/config.rs:113-117`；`src/internal/config.rs:1268-1272` |
| 打开/创建 | new-path 缺失时 `create_dir_all(parent)` + `create_new` 建库；已存在时按 Configuration 政策校验/升级 | `src/command/config.rs:158-205`；`src/internal/db/schema.rs:746-766` |
| `LIBRA_HOME` 与耦合 | `$LIBRA_HOME` 缺省 `~/.libra`；并规定「`LIBRA_CONFIG_GLOBAL_DB` 的父目录即 Libra home」用于升级状态隔离 | `src/internal/upgrade/home.rs:1-90` |
| 全域 vault key | `~/.libra/vault-unseal-key`，0600，用于加密 global config 值 | `src/internal/vault.rs:710-730`；`src/internal/auth.rs:1-10` |
| per-repo vault key | `~/.libra/vault-keys/<repo-id>`（目录 0700，文件 0600） | `src/internal/vault.rs:868-925` |
| vault tmp | `~/.libra/tmp` | `src/command/fetch.rs:355-390`、`:461-465` |
| 用户级 hooks | `dirs::config_dir()/libra/hooks.json`（macOS = `~/Library/Application Support`） | `src/internal/ai/hooks/config.rs:118-127` |
| 保留目录守卫 | `is_global_libra_home()` 用 `resolve_libra_home()` 与 global config 父目录判定 | `src/utils/util.rs:160-175`；`src/command/init.rs:551-565` |
| doctor 契约 | `path_source` ∈ {`LIBRA_CONFIG_GLOBAL_DB`, `home`}，`report_version: 1` | `src/command/config/doctor.rs:60-95`、`:338` |
| 路径命令 | `libra config path [--global|--system]`；JSON `{action, scope, path, exists}` | `src/command/config.rs:2875-2905` |
| 测试钉死旧路径 | 默认路径断言 `home/.libra/config.db`；doctor 默认 `path_source == "home"`；fixture 已设 `XDG_CONFIG_HOME` | `tests/command/config_test.rs:1556-1590`；`tests/compat/global_config_schema_future_test.rs:688,707`、`:1936-1990` |
| 测试隔离设施 | `ConfigDbFixture` 设 `LIBRA_CONFIG_GLOBAL_DB/SYSTEM_DB`、`HOME`、`USERPROFILE`、`XDG_CONFIG_HOME` | `src/utils/test.rs:200-250` |
| 跨进程锁先例 | unix `libc::flock`（NB），Windows 共享句柄失败映射 | `src/utils/client_storage.rs:235-270`；`src/command/control_lock.rs:1381-1394` |
| 文档面 | `~/.libra/config.db` 在 `docs/` 共 42 处 | `grep -rn "\.libra/config\.db" docs/` |
| 错误码 | 未来 schema / 不支持 receipt = `LBR-CONFIG-001`；读写失败 = `LBR-IO-001/002` | `docs/error-codes.md:101,115,116` |
| 外部前置 | `.env.test` / `.env.live-test` 均在仓库根 | `ls .env.test .env.live-test` |

### 当前缺口

| ID | 缺口 | 影响 | 证据 | 计划动作 |
|---|---|---|---|---|
| GAP-GCX-01 | global 路径解析有三个复制实现 | 任何路径规则改动都会漏改一处（GC-02 违反） | 事实基线前三行 | GCX-01 |
| GAP-GCX-02 | 默认路径不是 XDG（`~/.libra/config.db`），且 macOS hooks 与 config 分居两处 | 与 dotfiles/备份工具习惯冲突；跨平台行为不一致 | 事实基线「global #1..#3」「用户级 hooks」 | GCX-01、GCX-04 |
| GAP-GCX-03 | 直接改默认路径会让有旧库的用户「新库为空、旧库待迁移」而状态分裂 | 静默丢失用户 `user.*`/`vault.*`/`auth.token.*` 视图 | `src/command/config.rs:158-205`（缺失即新建） | GCX-01（回退）、GCX-02（迁移） |
| GAP-GCX-04 | 全域 vault unseal key 与 global config 不同域 | 配置搬走后密钥留在旧目录，威胁模型描述失准 | 事实基线「全域 vault key」 | GCX-03 |
| GAP-GCX-05 | `LIBRA_HOME` 规则把 `LIBRA_CONFIG_GLOBAL_DB` 父目录当 home | 新的 XDG 默认路径会被误判为升级/安装 home | 事实基线「LIBRA_HOME 与耦合」 | GCX-01 |
| GAP-GCX-06 | doctor/`config path` 不暴露 legacy 路径与迁移状态 | 用户无法判断自己处于哪个阶段 | 事实基线「doctor 契约」 | GCX-01、GCX-02 |
| GAP-GCX-07 | 测试与文档钉死 `~/.libra/config.db` | 迁移动不了测试真值，文档误导 | 事实基线「测试钉死旧路径」「文档面」 | GCX-01..04 |

## 与其它计划的关系

| 计划/文档 | 关系 | 本计划处理 |
|---|---|---|
| [`plan-20260920.md`](plan-20260920.md) | **写集/版本面前置。** 该计划拆除 Code/Publish 并把版本面从五处收为四处（`fa08ec0` 已落地）；仍会改 `COMPATIBILITY.md`、`docs/` 与网站页 | 本计划以「四处版本面」为准；`COMPATIBILITY.md` 与 `../libra-backend` 写作与 RC-* 卡串行（DEP-GCX-02） |
| [`plan-20260910.md`](plan-20260910.md) | 数据库迁移作用域与全局配置隔离（GC-13）的事实源 | 本计划无 schema 迁移；迁移是文件级复制；按该计划的角色/隔离口径写测试 |
| [`plan-20260918.md`](plan-20260918.md) | 同样写 `COMPATIBILITY.md`、`docs/commands/*` 与网站页 | 发布窗口串行；两计划 `in-progress` 不并存于同一写集 |
| [`plan-20260822.md`](plan-20260822.md) | Operation Log v2；`LIBRA_HOME`/upgrade 状态读者 | 本计划只解耦 config 与 home，不改 upgrade 语义；若其触碰 `resolve_libra_home` 则串行 |
| [`issues/477.md`](issues/477.md) | `add`/历史改写已收口（v0.22.49） | 无交集 |
| [`plan-long.md`](plan-long.md) | 日期计划索引 | 登记本文一行（本轮已加） |
| `../libra-backend` | ER-06a 网站文档（Libra 仓库、`cf` 分支） | DEP-GCX-01；写入前用支持当前 schema 的二进制核实分支 |

## 评审结论与修订记录

| 维度 | 结论 | 修订动作 |
|---|---|---|
| 合理性 | global 配置混在安装目录里，且三处路径解析复制；XDG 化有明确用户价值 | 立计划 |
| 可行性 | 解析器收敛 + 文件级迁移；无 schema 变更；仓库已有 flock/原子写/repair 先例 | 4 张卡分两阶段 |
| 任务卡粒度 | 路径决议、迁移引擎、vault key、hooks 各一轴；无 L/XL | 见粒度审计表 |
| 依赖与顺序 | GCX-01 是其余三卡的共享前置；`COMPATIBILITY.md`/网站页与 0920/0918 串行 | 实施顺序单链 + DEP-GCX-01/02 |
| 完整性 | 迁移失败、并发、旧库损坏、密钥冲突、hooks 兼容读全部入 AC | 见各卡 |
| 安全性 | unseal key 0600/0700、原子写、冲突 fail-closed、不轮换、不删除旧库 | ADR-GCX-04；GCX-03 |
| 功能正确性 | 路径优先级、legacy 回退、迁移幂等、两库并存优先级 | ADR-GCX-01/02/03 |
| 接口兼容 | `LIBRA_CONFIG_GLOBAL_DB` 与 `--system` 不变；`path_source` 为增量值；JSON 只增字段 | ADR-GCX-06；ER-06a |
| 数据流与控制流 | 「解析 → 回退 → 迁移 → 打开」单一入口；迁移在连接获取前完成 | ADR-GCX-02/03 |
| 性能与容量 | 迁移仅在首次发生；常规路径零额外 I/O；快照大小等于旧库 | 性能摘要 |
| 可靠性与容错 | 锁 + `VACUUM INTO` 快照 + 校验 + 原子 rename；失败不破坏旧库 | ADR-GCX-03；故障矩阵 |
| 可维护性 | 三处解析器收敛为一处；迁移模块独立可测 | GC-GCX-01 |

### 修订历史

| 日期 | 触发 | 变更内容 | 原卡 → 新卡 | 受影响的引用 |
|---|---|---|---|---|
| 2026-09-19 | GCX-01 D 组收口 | `release.yml` run `35384246060` 全 job 成功（4 平台 + Homebrew tap + stable manifest + install scripts）；CDN 三平台 artifact HTTP 200；GCX-01 置 `done`/`complete` | 无拆卡 | GCX-01、里程碑 M1、完成判据 |
| 2026-09-19 | 用户要求「global 配置写入 `.config/libra` 而不是 `.libra`」，并就 6 项设计问题裁决 | 成稿：4 张卡（路径决议 / 自动迁移 / vault key / hooks）；ADR-GCX-01..07；DEP-GCX-01/02；phase 与审计表 | — | 实施顺序、依赖登记表、发布窗口、测试矩阵、追溯表、里程碑、风险、完成判据、`plan-long.md` |
| 2026-09-19 | GCX-01 执行期澄清（实现已落地、聚焦门绿） | ① legacy 提示由 `config path`/`doctor` 人读输出承载（`--quiet`/`--json` 抑制人读行，JSON 以 `source`/`legacy_*`/`migration_pending` 字段暴露），不向所有命令注入进程级 warning envelope；② `libra init` 拒绝文案改为「per-user Libra state directory（Libra home 或 global configuration directory）」，保留判定与 ADR-GCX-01 的最终规则一致（见下一行）；③ 文档同步扩到 pull/cloud/push/fetch/clone 与 init 页（EN + zh-CN + 网站）；④ GCX-01 Verification 的 `command::config` 一行改由 `compat_global_config_schema_future` 与 `command_test config_test` 覆盖，另补 `init_test` 用例 | 无拆卡 | ADR-GCX-01/06、GCX-01 AC/Verification、`src/command/init.rs`、`docs/commands/*`、兼容与文档收口 |
| 2026-09-19 | GCX-01 執行期回歸修復（T-1 全量門發現） | `is_global_libra_home` 的「位於其內」判定在 `LIBRA_CONFIG_GLOBAL_DB` 指向測試臨時 repo 目錄時，會把 repo 自己的 `.libra` 誤判為保留目錄（`GlobalHomeNotRepository`），破壞 5 個既有用例；改為「XDG/legacy config 目錄遞迴保留、`LIBRA_HOME`/env-home 僅等值比對」；聚焦 7/7 綠，全量門由 9 失敗收斂為 4 個 RC-23 歸因失敗 | 無拆卡 | `src/utils/util.rs`、GCX-01 AC/Verification、ADR-GCX-01、風險/里程碑 |
| 2026-09-19 | GCX-01 C/D 發布 `v0.23.1` | 版本面 bump `0.23.1`（`Cargo.toml`/`web/package.json`/`install.sh`/`install.ps1`，`Cargo.lock` 由工具鏈刷新）；`cargo build --release` 後安裝至 `~/.libra/bin/libra`（備份 `libra.bak-0.23.0`）；提交 `8ce1eed` 推送 `main`（`7edc775..8ce1eed`）；annotated tag `v0.23.1`（指向 `8ce1eed`）已推送；`gh release create` 成功（https://github.com/libra-tools/libra/releases/tag/v0.23.1）；D 組待 `release.yml` run `35384246060`（in_progress）與 CDN | 無拆卡 | GCX-01 Lifecycle/Acceptance、里程碑 M1、發布窗口順序、完成判據 |
| 2026-09-22 | GCX-02/03/04 开工与执行（ER-02 基线重刷 + 操作者指示「不调用 Codex review」） | ① 事实基线重刷：tip `fc6e48f`、版本 `0.23.45`、版本面**四处→三处**（`web/package.json` 随 `web/` 于 0.23.x 拆除）、本检出由 Git 而非 Libra 承载；② GCX-02 迁移引擎落在**新文件** `src/internal/config/global_migration.rs`（写集偏离登记），并新增 `src/utils/error.rs` 的确定性 IO 写失败分类规则（否则 fail-closed 误判为 `LBR-AUTH-002`）；③ GCX-03 把「密钥可读但长度异常即重新生成」收紧为 fail-closed（GC-GCX-03），`repair_global_modes` 只修复当前活动 key；④ GCX-04 把 ADR-GCX-07 §2 的只读回退从「macOS」泛化为「凡平台原生配置目录与新路径不同」（Linux no-op，额外保护 Windows）；⑤ `config_json_test` target 不存在，其断言并入 `config_test`；⑥ `VACUUM INTO` 零命中守卫存在 `src/command/config/repair.rs:658` 既有命中，登记 allowlist；⑦ DEP-GCX-01（`../libra-backend`）与 DEP-GCX-03（`.env.test` / `.env.live-test`）均不满足，按登记表分别 `blocked`（仅网站页）与降级说明；⑧ 三卡 `locally-accepted`，版本 bump 与 D 组发布未执行 | 无拆卡 | 事实基线、发布分组、GCX-02/03/04 全卡、依赖登记表、兼容与文档收口、完成判据、Codex review log |
| 2026-09-22 | 操作者裁决：本轮不调用 Codex review | 「Codex review log」新增一行记录该裁决；计划原「开工前必须有一轮字面 `VERDICT: PASS`」的门在本轮**按操作者指示豁免**，并在「完成判据」中显式标注为未满足项而非已满足项 | 无拆卡 | Codex review log、完成判据 |
| 2026-09-25 | 收并 `plans/global-config-xdg.md`（2026-09-18 草案）并删除 `plans/` | 草案的五项用户裁决已由本文承接：`<XDG_CONFIG_HOME 或 ~/.config>/libra`（macOS 同）、旧库首次使用自动迁移（策略 A）、全域 vault unseal key 随迁、`~/.libra` 保持 `LIBRA_HOME`、`patch` 发布。草案中唯一尚缺单项证据的仓库命令隔离检查登记于测试矩阵后；草案的待定方案不再作为实施要求 | 无拆卡 | 文档职责、ADR-GCX-01/02/04/05、发布分组、待补验证登记 |

## 已决议设计决策

实现时若需偏离本节，必须先修改计划并说明原因，不得在代码中静默改语义。

### ADR-GCX-01: 目标目录、解析优先级与 macOS/Windows 口径

- **Status:** Accepted（2026-09-19 用户裁决）
- **Context:** 当前默认 `~/.libra/config.db`；用户要求迁到 `.config/libra`，并明确「macOS 也用 `~/.config/libra`」而不是 `dirs::config_dir()` 的 `~/Library/Application Support`。
- **Decision:**
  1. 新增唯一解析器（放 `src/internal/config.rs`，其余两处调用它）：
     - `LIBRA_CONFIG_GLOBAL_DB` 非空 → 精确使用该文件路径（**不迁移、不读 legacy**，测试/沙箱语义不变）；
     - 否则 `XDG_CONFIG_HOME` 为**绝对路径**时 → `<XDG_CONFIG_HOME>/libra/config.db`；相对路径按 XDG 规范忽略（debug 日志）；
     - 否则 `dirs::home_dir()` → `<home>/.config/libra/config.db`（Linux/macOS/Windows 统一，Windows 为 `%USERPROFILE%\.config\libra\config.db`）；
     - home 不可得 → `None`（现状语义：global 不可用）。
  2. legacy 路径独立函数：`<home>/.libra/config.db`。
  3. 选择「活动路径」的规则（GCX-01 阶段，不做迁移）：
     - 新路径存在 → 用它；
     - 新路径不存在且 legacy 存在 → **用 legacy**（读+写同一文件，避免状态分裂）并在 stderr 输出一次性迁移预告（`--quiet`/`--json` 抑制，JSON 以字段暴露）；
     - 两者都不存在 → 新路径（首次写入时创建）。
  4. system 作用域不变（`/etc/libra/config.db`）。
  5. **保留目录强度**（`is_global_libra_home`）：XDG config 目录与 legacy `<home>/.libra` 目录按「等值或位於其內」保留（`libra init` 不得在用戶配置目錄內建立倉庫）；`LIBRA_HOME`（含 `LIBRA_CONFIG_GLOBAL_DB` 父目錄的隔離約定）僅按等值保留，因為隔離/沙箱場景允許在該目錄下放置倉庫。
- **Alternatives considered:** `dirs::config_dir()`（macOS 会落到 Application Support，与用户裁决相反，拒绝）；Windows 用 `%APPDATA%`（与「统一 `.config`」冲突，拒绝）；不采纳 `XDG_CONFIG_HOME`（偏离 XDG，拒绝）。
- **Consequences:** 新用户数据落在 `~/.config/libra`；`~/.libra` 不再承载 global 配置；旧用户在 GCX-02 前仍读写旧库。
- **Revisit when:** 上游 `dirs` 行为变化，或产品改为「各平台原生目录」。

### ADR-GCX-02: legacy 回退与首次使用自动迁移（策略 A）

- **Status:** Accepted（2026-09-19 用户裁决）
- **Context:** 现有用户数据在 `~/.libra/config.db`（含 `user.*`、`vault.*`、`auth.token.*`、`upgrade.*` 等）。用户选择「首次使用自动迁移」。
- **Decision:**
  1. 触发点：任何 global 配置连接的获取（读或写）之前；条件为「env 覆写未设置 ∧ 新路径不存在 ∧ legacy 存在」。
  2. 迁移成功后：新路径成为唯一读写目标；legacy 文件**原样保留**（备份，不重命名、不删除）。
  3. 两库并存：新路径优先，不自动合并；doctor 报告 legacy 存在并给出手工清理提示。
  4. 新路径存在但损坏/不可读：按现有 Configuration 政策失败，**不**静默回退 legacy（避免双源）。
  5. legacy 存在但不可读/不是配置库：不迁移；读取失败保持现状错误，doctor 归类。
  6. 迁移失败：读路径回退 legacy（本次调用）并给可操作警告；写路径 fail-closed（错误说明目标路径、原因、`LIBRA_CONFIG_GLOBAL_DB` 兜底），绝不写坏旧库。
- **Alternatives considered:** 显式 `migrate-global` 命令（用户选择自动，登记 DEFER-GCX-01）；永久只读回退（状态分裂，拒绝）；硬切（丢失用户数据视图，拒绝）。
- **Consequences:** 首次命令多一次拷贝耗时；`doctor`/`path` 必须暴露迁移状态；旧二进制仍可读旧库（迁移保留备份）。
- **Revisit when:** 需要「用户在升级前预迁移」或 CI 幂等脚本时（DEFER-GCX-01）。

### ADR-GCX-03: 迁移引擎（锁、快照、校验、原子提交）

- **Status:** Accepted
- **Context:** SQLite 库可能带 `-wal`/`-shm`；多进程可能同时触发；迁移必须不破坏源库。
- **Decision:**
  1. **锁**：在目标目录建 `<config dir>/libra/.config.db.migrate.lock`，unix 用 `libc::flock(LOCK_EX)`（随 `client_storage.rs:235-270` 先例），Windows 用零共享句柄失败映射；两端均需 `create_dir_all` 目标目录。若平台无法实现阻塞 flock，退化为 `create_new` 锁文件 + 陈旧锁超时回收；拿锁后**重新探测**新路径是否已被并发迁移创建。
  2. **快照**：对 legacy 以**只读连接**（`mode=ro`，绝不打开为可写）执行 `VACUUM INTO '<目标目录>/config.db.migrate.<pid>.<rand>.tmp'`（SQLite 官方一致性快照，合并 WAL）。2026-09-19 本机 SQLite 3.53.4 已验证 `file:legacy.db?mode=ro` 下 `VACUUM INTO` 成功（exit 0）；实施时断言 bundled libsqlite3-sys 版本 ≥ 3.27，低于或拒绝时改用 `sqlite3_backup` 等价接口，仍不得写入源库。
  3. **校验**：打开临时库断言 `PRAGMA integrity_check = ok`；断言 `configuration_schema_versions` 行与 legacy 一致；断言 `config_kv` 行数一致（键集合可选比对）；断言 `DatabaseRole::GlobalConfig` 的 Configuration 政策可接受。
  4. **提交**：`fs::rename(tmp, new)`（同目录原子），unix 额外 fsync 目录；临时文件在任何失败路径清理；成功路径幂等（新路径已存在即 no-op）。
  5. **角色/隔离（GC-13）**：不新增/推进 migration receipt，不创建 Repository 表；repo 侧命令不得触碰本迁移，反之亦然。
  6. **并发**：持锁者负责迁移；非持锁者等待后重新解析活动路径（不重复迁移）。
- **Alternatives considered:** `fs::copy` + 复制 `-wal`（竞态、易漏，拒绝）；`sqlite3_backup`（需引 extra 接口，`VACUUM INTO` 已足够，拒绝）；迁移进临时目录再跨目录 rename（跨设备可能失败，拒绝）。
- **Consequences:** 迁移期间有短暂锁；旧库保持可回退；`VACUUM INTO` 要求 SQLite ≥3.27（sea-orm/libsqlite3-sys 满足，实施时断言）。
- **Revisit when:** 目标目录与 legacy 不同文件系统且要求零拷贝，或需要跨设备方案。

### ADR-GCX-04: 全域 vault unseal key 随迁

- **Status:** Accepted（2026-09-19 用户裁决「vault 也搬过去」）
- **Context:** 全域加密值（`vault.*`、`auth.token.*`）由 `~/.libra/vault-unseal-key`（32 字节，0600）加密；密钥丢失即不可解密，**绝不能轮换**。
- **Decision:**
  1. 新路径 `<config dir>/libra/vault-unseal-key`（父目录 0700，文件 0600）。
  2. 读取顺序：新文件存在 → 用它；否则 legacy 存在 → 复制到新路径（原子：临时文件 + 0600 + rename），校验 32 字节后使用；否则按现状生成新密钥。
  3. 两份并存且内容不同 → **fail-closed**：报可操作错误并列出两个路径（禁止静默轮换）；内容相同 → 用新路径。
  4. `~/.libra/vault-keys/<repo-id>`（每仓库密钥）与 `~/.libra/tmp` **不动**；在 `docs/development/account.md` 明示边界。
  5. 不修改、不删除 legacy key 文件。
- **Alternatives considered:** 密钥留在 `~/.libra`（与「vault 随迁」裁决相反，拒绝）；密钥放 keyring（范围外，DEFER）；轮换密钥（不可解密既有值，拒绝）。
- **Consequences:** `account.md` 的威胁模型与存储表、`auth.rs`/`vault.rs` 模块注释同步；迁移后既有 token 仍可解密。
- **Revisit when:** 引入 OS keyring 或 `core.symlinks` 式可配置密钥位置。

### ADR-GCX-05: `LIBRA_HOME` 与 global config 路径解耦

- **Status:** Accepted
- **Context:** `resolve_libra_home()` 规则 2 把 `LIBRA_CONFIG_GLOBAL_DB` 的父目录当作 Libra home；若保留，显式覆写的测试/sandbox 仍能隔离升级状态。默认路径改为 XDG 后，默认情形不经过该规则（因为规则只在 env 设置时触发），但语义必须写清楚。
- **Decision:**
  1. `LIBRA_HOME`（缺省 `~/.libra`）继续定义 `bin/`、`env`、`seccomp.bpf`、`upgrade/`、`tmp/` 的位置。
  2. 规则 2 保留但**仅适用于显式设置 `LIBRA_CONFIG_GLOBAL_DB` 的隔离场景**（测试/沙箱），并在模块注释与 dev 文档写明「这是隔离约定，不再是默认布局描述」。
  3. `install.sh` / `install.ps1` 的 `LIBRA_HOME` 语义不变。
  4. 新测试 fixture 在需要隔离升级状态时显式设置 `LIBRA_HOME`（`ConfigDbFixture` 增加该变量），避免依赖规则 2。
- **Alternatives considered:** 删除规则 2（会让只设 `LIBRA_CONFIG_GLOBAL_DB` 的沙箱写到真实 `~/.libra/upgrade`，拒绝）；把 upgrade 也搬去 XDG（范围外，拒绝）。
- **Consequences:** `upgrade::home` 的文档与测试更新；`is_global_libra_home` 同时覆盖 `LIBRA_HOME`、XDG 配置目录与 legacy 目录。
- **Revisit when:** 安装器改为 XDG 布局（`~/.local/bin` 等）。

### ADR-GCX-06: `config path` / `doctor` 的 JSON 契约

- **Status:** Accepted
- **Context:** `doctor` 目前 `path_source ∈ {LIBRA_CONFIG_GLOBAL_DB, home}`、`report_version: 1`；消费者（含 compat 测试）钉住该值。
- **Decision:**
  1. `path_source` 取值保持 `LIBRA_CONFIG_GLOBAL_DB` 不变，新增 `xdg`（用了 `XDG_CONFIG_HOME`）、`home`（`<home>/.config` 默认）、`legacy`（回退旧库）。**不删除、不重命名**既有值。
  2. `report_version` 保持 `1`（纯增量字段）；新增 `legacy_path`、`legacy_exists`、`migration_pending`。
  3. `libra config path --json` 增加同样的 `source`/`legacy_*` 字段；人读输出仍打印活动路径（`--verbose` 时附 legacy 与来源）。
  4. 迁移失败的警告走 stderr，`--quiet`/`--json` 下抑制人读警告，只保留字段/错误码。
- **Alternatives considered:** 提升 `report_version: 2`（会无谓地要求消费方升级，拒绝）；把 `home` 改成 `xdg-default`（破坏已有断言，拒绝）。
- **Consequences:** `tests/compat/global_config_schema_future_test.rs` 与文档同步新增字段；旧消费者不受影响。
- **Revisit when:** 需要破坏性重命名字段（那时再升 `report_version`）。

### ADR-GCX-07: 用户级 hooks 文件路径对齐

- **Status:** Accepted
- **Context:** `load_hook_config()` 的 user tier 走 `dirs::config_dir()/libra/hooks.json`；macOS 与新的 config 目录不一致。
- **Decision:**
  1. 改用 ADR-GCX-01 的同一配置目录：`<config dir>/libra/hooks.json`。
  2. macOS 上保留**只读**兼容回退：新位置缺失时读 `dirs::config_dir()/libra/hooks.json`（存在则加载并 warning 提示迁移），**不写**旧位置。
  3. Linux 下两者本就同路径，无行为变化。
- **Alternatives considered:** 迁移（rename/copy）hooks 文件（用户手工文件，自动改写风险大于收益，拒绝）；不改（macOS 分裂，拒绝）。
- **Consequences:** hooks 文档与测试更新；`compat_agent_*` 守卫不受影响。
- **Revisit when:** 用户要求统一迁移用户级 hooks 文件。

## 全局工程约束

继承模板 GC-01..GC-13（GC-13 适用：迁移涉及 Configuration 角色，必须声明角色与隔离证明）。计划级补充：

- **GC-GCX-01 单一解析器:** `LIBRA_CONFIG_GLOBAL_DB` → XDG → home 的解析只允许存在于 `src/internal/config.rs` 的一个函数族；`src/command/config.rs` 与 `src/utils/client_storage.rs` 必须调用它。零命中守卫：`rg 'join\("\.libra"\)\.join\("config\.db"\)' src/` 只允许迁移/测试模块命中（登记 allowlist）。
- **GC-GCX-02 旧库不可变:** 迁移过程只读 legacy；任何路径都不得删除、重命名、截断或原地改写 legacy 文件（含失败路径）。
- **GC-GCX-03 密钥不轮换:** 全域 unseal key 的任何代码路径都不得在既有密钥可读时生成新密钥；冲突必须 fail-closed。
- **GC-GCX-04 机器可判:** 所有迁移状态（活动路径、来源、legacy 是否存在、是否待迁移）必须能从 `libra config path --json` 与 `doctor --global-schema` 读出，便于测试与运维。

## 执行检查必备需求（强制）

完整继承模板 ER-01..ER-13。计划级补充：

- **ER-GCX-01:** 每张卡开工先跑版本面 parity（当前四处；`compat_version_surface_sync` 是权威），并复核 `plan-20260920` 是否又改动版本面或 `COMPATIBILITY.md`。
- **ER-GCX-02:** 迁移相关卡必须提供「失败注入」证据：目标目录只读、旧库损坏、并发双进程、磁盘/权限错误各一条；不允许只测 happy path。
- **ER-GCX-03:** 任何读写 `HOME`/`XDG_CONFIG_HOME`/`LIBRA_*` 的新增测试必须持 `#[serial(env)]`（或既有等价 lane），并在 `tests/SERIAL_REGISTRY.tsv` 登记、`sh tests/NEXTEST_GROUPS.sh` 重新生成 `.config/nextest.toml`。

## 实施顺序

`A -> B` 表示 A 必须先于 B。GCX-01 是其余卡的共享前置；实现串行（共享 `src/internal/config.rs` 与发布队列）。

- `GCX-01 -> GCX-02`
- `GCX-01 -> GCX-03`
- `GCX-01 -> GCX-04`
- `GCX-02 -> GCX-03`（同样是单一发布队列的顺序；GCX-03 复用迁移模块的锁/原子写工具）
- `DEP-GCX-01 -> GCX-01`（网站文档写入前置）
- `DEP-GCX-01 -> GCX-03`
- `DEP-GCX-01 -> GCX-04`
- `DEP-GCX-02 -> GCX-01`（`COMPATIBILITY.md` 与 0920/0918 串行）

### 依赖登记表

| ID | direction | 类型 | 对象 | Owner | 产物与可用性判据 | 证据 | 超时与失败策略 |
|---|---|---|---|---|---|---|---|
| DEP-GCX-01 | incoming | 外部仓库 | `../libra-backend` 网站文档 | docs owner | ER-06a：Libra 仓库（`.libra/`，无 `.git`）、分支 `cf`、工作区无冲突脏文件；用支持当前 schema 的二进制读状态 | 2026-09-19 实跑 `/mnt/gitmono/libra/target/debug/libra status --short --branch` → `## cf...origin/cf`（另有一处无关脏文件）。**2026-09-22 复核：不满足** —— `ls /media/eli/data/libra/../libra-backend` 报 `No such file or directory`，本机无该检出 | 不满足（含二进制无法读取）时写网站页的卡 `blocked`；与 `plan-20260920` RC-* 卡不得同时写同一批页。**2026-09-22 生效**：GCX-02/03 的网站页同步项记 `blocked`，仓库内文档面照常完成 |
| DEP-GCX-02 | incoming | 跨计划写集互斥 | [`plan-20260920.md`](plan-20260920.md)（`COMPATIBILITY.md`、`docs/commands/*`、网站页）、[`plan-20260918.md`](plan-20260918.md)（同写集） | 单一发布者 | 相交卡不处于 `in-progress`；版本面数量按 `compat_version_surface_sync` 现场判定 | 2026-09-19：0920 的 `fa08ec0` 已把版本面收为四处；0918 未开工 | 后开工者等待；`COMPATIBILITY.md` 冲突时以发布窗口串行 |
| DEP-GCX-03 | incoming | 环境前置 | 仓库根 `.env.test` / `.env.live-test` | 发布者 | `source .env.test && source .env.live-test` 返回 0 | 2026-09-19 两文件均在。**2026-09-22 复核：不满足** —— `ls .env.test .env.live-test` 两者均 `No such file or directory`（仅 `.env.test.example` 在） | 未满足时命中全量门的卡停在 `locally-accepted`；不得降级为 `cargo test --all`。**2026-09-22 生效**：三卡停在 `locally-accepted`。语义说明：两文件只导出 L2/L3 凭据，缺失等价于相关变量未设置，L2/L3 按 CLAUDE.md 约定打印 `skipped` 而非失败，L1 覆盖不受影响 |
| DEP-GCX-04 | outgoing | 后续能力 | 显式预迁移命令 / `libra config migrate-global`（含 `--dry-run`/`--json`） | 待立计划 | 仅在用户或 CI 需要「升级前预迁移」时重启 | 本计划 DEFER-GCX-01 | 本计划不实现，不影响自动迁移 |

### 发布分组与并发窗口

无 `REL-*`：四张卡均 `independent`，各自 `patch + 1` 并按 ER-08 发布（版本面按 `compat_version_surface_sync` 现场判定；**2026-09-22 实测为三处**：`Cargo.toml`、`install.sh`、`install.ps1`）。

**并发声明:** 全串行。GCX-01/02 共享 `src/internal/config.rs`、`src/command/config.rs`；GCX-03/04 虽写不同文件，但为发布窗口不交错，统一单链。

**发布者:** 执行本计划的主 Agent（单一发布者）。与 0918/0920 的收口发布共用同一串行队列。

**发布窗口顺序:** GCX-01 → GCX-02 → GCX-03 → GCX-04。

### Phase 0: 基线冻结

**目标:** Codex `PASS`；事实基线锚点复核；DEP 状态记录。

**进入条件:**

- 本文成稿；用户 6 项裁决已记录。

**退出条件:**

- 「Codex review log」有一轮字面 `VERDICT: PASS`；DEP-GCX-01..03 状态已记录；版本面数量按 ER-GCX-01 复核。

### Phase 1: 路径决议与自动迁移

**目标:** GCX-01、GCX-02 发布：新用户落 XDG；旧用户首次使用安全迁移；`doctor`/`path` 可读出状态。

**进入条件:** Phase 0 退出。

**退出条件:** 两卡 `done`/`complete`。

**2026-09-22 状态:** GCX-01 `done`；GCX-02 `locally-accepted`（实现/测试/文档完成，未发布）→ Phase 1 **未退出**。

### Phase 2: 全域 vault key 随迁

**目标:** GCX-03 发布：unseal key 落到配置目录；既有加密值仍可解密。

**进入条件:** GCX-02 `done`/`complete`。

**退出条件:** 该卡 `done`/`complete`。

**2026-09-22 状态:** 按操作者「完成开发」的指示，GCX-03 的实现与验证与 GCX-02 在同一轮完成（进入条件的**发布**部分未满足，代码前置已满足）；该卡 `locally-accepted`，Phase 2 **未退出**。

### Phase 3: hooks 用户级对齐

**目标:** GCX-04 发布：用户级 `hooks.json` 使用同一配置目录（macOS 只读兼容回退）。

**进入条件:** GCX-03 `done`/`complete`。

**退出条件:** 该卡 `done`/`complete`；全量收口门全绿。

**2026-09-22 状态:** 同上，该卡 `locally-accepted`；全量收口门已实跑（见「完成判据」末尾的执行记录）。Phase 3 在发布完成前 **未退出**。

## 任务卡

### 字段全局默认与例外

- **Release boundary 默认:** `independent`。
- **Task type 默认:** `implementation`。
- **Rollback mode 默认:** `compensating`（数据已落到新路径；降级前按卡内补偿命令把新库/密钥复制回旧路径）。
- **Migration and rollback 默认:** N/A。
- **Security and privacy 默认:** 继承 GC-07、GC-11；证据按 ER-11 脱敏（不得在证据中打印密钥/密文）。
- **Performance budget 默认:** 继承 GC-10。
- **Version increment 默认:** `patch`。
- **C/D coverage from 默认:** `self`。
- **D 组默认:** `.github/workflows/release.yml`，由 `gh release create "v${VERSION}"`（`push` `v*` tag）触发；判据为 run 全部 job 成功且 `download.libra.tools` 可取到对应版本。
- **Lifecycle / Acceptance 默认:** `pending` / 空。

**默认覆盖:**

| 任务 | 偏离的字段 | 取值与理由 |
|---|---|---|
| GCX-01、GCX-02 | Rollback mode | `compensating`：新默认路径一旦发布，回退代码会让新写入的库/密钥失去可见性；降级前把 `<config dir>/libra/config.db` 复制回 `~/.libra/config.db` 即可（旧库仍在，幂等） |
| GCX-03 | Rollback mode | `compensating`：unseal key 迁移后回退旧二进制需把新 key 复制回 `~/.libra/vault-unseal-key`；密钥内容不变，补偿幂等 |
| GCX-04 | Rollback mode | `revert`：hooks 只读路径 + macOS 只读回退，回退无数据影响 |

**规则 waiver:** 无（AC/Ver 均在本类上限内，见审计表）。

### 任务卡粒度审计表

| 任务 | type | axis | recovery | complete | self-contained | AC | VER | landing / prod-files | scope | deps | writeset | release | split-from | exception |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| GCX-01 | implementation | global config 路径决议与 legacy 回退 | compensating | yes | yes | 8/8 | 7/8 | 3/5 | M | DEP-GCX-01, DEP-GCX-02 | 序列化首位 | independent | N/A | N/A |
| GCX-02 | implementation | legacy 库首次使用自动迁移 | compensating | yes | yes | 8/8 | 6/8 | 2/3 | M | GCX-01 | 序列化于 GCX-01 | independent | N/A | N/A |
| GCX-03 | implementation | 全域 vault unseal key 随迁 | compensating | yes | yes | 7/8 | 5/8 | 1/2 | S | GCX-01, GCX-02 | 序列化于 GCX-02 | independent | N/A | N/A |
| GCX-04 | implementation | 用户级 hooks 路径对齐 | revert | yes | yes | 5/8 | 3/8 | 1/1 | S | GCX-01 | 序列化于 GCX-03 | independent | N/A | N/A |

### Verification 判定口径

继承模板「Verification 判定口径」（零命中守卫区分 `rg` 退出码 1 与 >1）。计划级补充：

- 迁移测试必须断言 legacy 文件指纹（mtime/size/hash）在成功与失败路径下均不变。
- 仅用于定位符号的 `rg` 必须注明「锚点定位用，非判据」。

### Task GCX-01: global config 路径决议统一与 XDG 默认（含 legacy 回退）

**Task type:** `implementation`

**Lifecycle / Acceptance:** `done` / `complete`（2026-09-19：实现、聚焦门、T-1 全量门、`patch` bump `0.23.1`、四版本面、release build、安装、提交 `8ce1eed`、推送、tag `v0.23.1`、`gh release` 与 D 组全部完成——`release.yml` run `35384246060` 四个平台 build-and-upload + Homebrew + stable manifest + installer 全成功；`download.libra.tools/libra/releases/v0.23.1/{libra-linux-amd64,libra-darwin-arm64,libra-windows-amd64.exe}` 均 HTTP 200）

**Description:** 按 ADR-GCX-01/05/06，在 `src/internal/config.rs` 建立唯一解析器（`LIBRA_CONFIG_GLOBAL_DB` → 绝对 `XDG_CONFIG_HOME` → `<home>/.config/libra`），让 `src/command/config.rs` 与 `src/utils/client_storage.rs` 调用它；当新路径缺失而 legacy 存在时回退使用 legacy（读+写），并让 `config path`/`doctor` 暴露来源、legacy 路径与待迁移状态；同步保留目录守卫与文档。唯一行为轴是「global 配置的路径决议与 legacy 回退」。

**Out of scope:** 实际迁移（GCX-02）；全域 vault key（GCX-03）；hooks（GCX-04）；system 作用域（永久保持 `/etc/libra`）。

**Current evidence:**

| 事实 | 证据 |
|---|---|
| 三处重复解析 | `src/internal/config.rs:1261-1272`；`src/command/config.rs:99-120`；`src/utils/client_storage.rs:2556-2565` |
| 缺失即新建（迁移前不能直接改默认） | `src/command/config.rs:158-205` |
| `path_source` 现有取值 | `src/command/config.rs`（doctor）`src/command/config/doctor.rs:60-95`、`:338` |
| 保留目录守卫 | `src/utils/util.rs:160-175`；`src/command/init.rs:551-565` |
| XDG 先例与测试 fixture | `src/internal/ai/hooks/config.rs:118-127`；`src/utils/test.rs:200-250` |
| 测试钉死旧默认 | `tests/command/config_test.rs:1556-1590`；`tests/compat/global_config_schema_future_test.rs:688,707` |

**Acceptance criteria:**

- [x] 解析优先级与 ADR-GCX-01 一致：`LIBRA_CONFIG_GLOBAL_DB` > 绝对 `XDG_CONFIG_HOME` > `<home>/.config/libra`；相对 `XDG_CONFIG_HOME` 被忽略（有单测）；home 不可得返回 `None`。
- [x] 活动路径选择与 ADR-GCX-01 第 3 条一致：新存在→新；新缺失且 legacy 存在→legacy（读写同一文件）；都缺失→新。
- [x] 三处解析器全部收敛到单一实现；`rg 'join\("\.libra"\)\.join\("config\.db"\)' src/` 除迁移/测试 allowlist 外零命中（模板退出码守卫）。
- [x] `libra config path --global` 人读输出活动路径；`--json` 增量返回 `source`、`legacy_path`、`legacy_exists`、`migration_pending`；既有 `path`/`exists` 不变。
- [x] `doctor --global-schema` 的 `path_source` 支持 `LIBRA_CONFIG_GLOBAL_DB`/`xdg`/`home`/`legacy`，`report_version` 保持 `1`；新增 `legacy_*`/`migration_pending` 字段。
- [x] 保留目录守卫：`is_global_libra_home` 覆盖 `LIBRA_HOME`（仅等值）、XDG config 目录与 legacy 目录（等值或位于其内）；`libra init` 在 config 目录内与 Libra home 本身均拒绝。
- [x] legacy 回退时输出一次性可操作提示（含迁移将在下一版本发生），`--quiet`/`--json` 下抑制人读警告。
- [x] ER-06a 同步：`docs/commands/config.md` 与 zh-CN（scope 表、path 节、doctor 节）、`docs/development/commands/config.md`、`docs/commands/init.md` 与 zh-CN、`docs/development/commands/init.md`、`COMPATIBILITY.md:243` 原行追加、`../libra-backend` 对应配置页（按 DEP-GCX-01）。

**Verification:**

- [x] `source .env.test && cargo test --lib internal::config`（new：解析优先级、相对 XDG 忽略、legacy 选择、home 缺失）
- [x] `source .env.test && cargo test --lib internal::config`（`path_source` 映射与 JSON 增量字段改由 `compat_global_config_schema_future` 与 `command_test config_test` 覆盖；lib 侧覆盖解析器与 notice 单测）
- [x] `source .env.test && cargo test --test command_test init_test::init_refuses`（新增全局配置目录拒绝用例）
- [x] `source .env.test && cargo test --test command_test config_test`（更新 `test_config_scope_path_logic`、`test_config_cross_platform_paths`；new：legacy 回退与提示）
- [x] `source .env.test && cargo test --test compat_global_config_schema_future_test`（更新 `path_source` 断言，new：`legacy` 值）
- [x] `source .env.test && cargo clippy --all-targets --all-features -- -D warnings`
- [x] `sh tests/NEXTEST_GROUPS.sh`（若新增 `#[serial(env)]` 行，同步 `tests/SERIAL_REGISTRY.tsv`）
- [x] 手工证据：`HOME=<tmp> XDG_CONFIG_HOME=<tmp>/.config` 与「仅有 legacy」两种 HOME 的 `config path --json` + 文件树（存 `LIBRA_TEST_SCRATCH_DIR`）
- 手工证据（2026-09-19 执行记录）：`LIBRA_TEST_SCRATCH_DIR` 未设置，证据保存在 `/tmp/libra-gcx-evidence/`（fresh-home 文件树、legacy 回退的 `config path`/warning/JSON、`source` 的 xdg/home/legacy/env 四种取值、legacy 库指纹不变）。
- T-1 全量门（2026-09-19，bumped `0.23.1`）：`CARGO_BUILD_JOBS=2 cargo nextest run --all --no-fail-fast --retries 2` → **7769 run / 7765 passed / 4 failed / 3 skipped**（首次运行因 32 路平行 link 被 OOM kill，非测试失败；受限 build 並行度后重跑）。
- 4 個失敗**全部歸因** plan-20260920 的 RC-23 未提交刪除（`config_ownership` 對 `agents.toml` / prompt `rules` / `WORKSPACE_CONTEXT_CACHE` 的註冊列失去來源；`maintenance::NOT_AN_OBJECT_SOURCE` 的 `agents.toml`），與 GCX-01 無關，並轉交 RC-23 卡內清理。
- 執行期回歸與修復（同日）：`is_global_libra_home` 的「位於其內」判定在 `LIBRA_CONFIG_GLOBAL_DB` 指向測試臨時 repo 目錄時把 repo 自己的 `.libra` 判為保留（`GlobalHomeNotRepository`），破壞 5 個既有用例；改為「config 目錄遞迴保留、`LIBRA_HOME`/env-home 僅等值比對」後，7 個相關用例（含 2 個 init 拒絕）全綠。

**Full-suite trigger:** `T-1: 修改 global 配置单一事实源（`src/internal/config.rs`）与 `src/utils/client_storage.rs` 共享路径解析`

**Dependencies:** `DEP-GCX-01`（网站文档）、`DEP-GCX-02`（`COMPATIBILITY.md` 串行）

**Deliverables:** N/A

**Implementation write set:** `src/internal/config.rs`、`src/command/config.rs`、`src/command/config/doctor.rs`、`src/utils/client_storage.rs`、`src/utils/util.rs`、`src/command/init.rs`、`src/utils/test.rs`、`tests/command/config_test.rs`、`tests/compat/global_config_schema_future_test.rs`、`tests/SERIAL_REGISTRY.tsv`、`docs/commands/config.md`、`docs/commands/zh-CN/config.md`、`docs/development/commands/config.md`、`docs/commands/init.md`、`docs/commands/zh-CN/init.md`、`docs/development/commands/init.md`、`COMPATIBILITY.md`、`../libra-backend/apps/tanstack-app/content/docs/commands/config.en.md`

**Release write set:** Inherited

**Files likely touched:** 同 Implementation write set

**Docs and compatibility impact:** `config` 命令页 EN + zh-CN、`init` 命令页 EN + zh-CN、开发文档、`COMPATIBILITY.md:243` 原行、网站配置页。`path_source` 为增量值；`report_version` 不变。

**Rollback mode:** `compensating`

**Migration and rollback:** 数据不变量：legacy 文件不被本卡修改；新路径仅在本卡默认路径下创建。降级指引：先把 `<config dir>/libra/config.db` 复制回 `~/.libra/config.db`（幂等），再安装旧版本；补偿验证：`libra config path --global` 指向 legacy 且 `config get --global user.email` 可见。

**Security and privacy:** 只读 legacy + 路径解析；不打印配置值；证据按 ER-11 脱敏。

**Performance budget:** 解析 O(1) 与两次 `exists` 探测；无新增目录扫描。

**Estimated scope:** `M`

**Version increment:** `patch`

**Release boundary:** `independent`

**C/D coverage from:** `self`

**Granularity:** `type=implementation; axis=global config 路径决议与 legacy 回退; recovery=compensating（复制新库回旧路径后降级）; complete=yes; self-contained=yes; AC=8/8; VER=7/8; landing=3; prod-files=5; scope=M; deps=DEP-GCX-01,DEP-GCX-02; writeset=序列化首位; release=independent; split-from=N/A; exception=N/A`

### Task GCX-02: legacy global config DB 首次使用自动迁移

**Task type:** `implementation`

**Lifecycle / Acceptance:** `locally-accepted` / `implementation+gates complete; release pending`（2026-09-22：实现、单测、集成测试、失败注入证据、零命中守卫、文档与 `COMPATIBILITY.md` 全部完成；`fmt`/`clippy`/聚焦门全绿。未执行版本 bump 与 D 组发布，见「完成判据」末行）

**Description:** 按 ADR-GCX-02/03，在 global 配置连接获取前检测「新缺失 + legacy 存在」，以迁移锁 + `VACUUM INTO` 快照 + 完整性/行数校验 + 原子 rename 完成一次性迁移；legacy 原样保留；失败时读回退 legacy、写 fail-closed；`doctor`/`path` 暴露迁移状态与 legacy 清理提示。唯一行为轴是「legacy 库的首次使用自动迁移」。

**Out of scope:** 显式迁移命令（DEFER-GCX-01）；schema 迁移（不存在）；legacy 文件清理（用户手工，doctor 给提示）。

**Current evidence:**

| 事实 | 证据 |
|---|---|
| 连接获取入口（迁移注入点） | `src/command/config.rs:125-205`（`get_or_create_cached_connection`） |
| Configuration 角色打开/创建 | `src/internal/db/schema.rs:631-660`、`:746-766` |
| 跨进程锁先例 | `src/utils/client_storage.rs:235-270`；`src/command/control_lock.rs:1381-1394` |
| 原子写/一致性备份先例 | `src/command/config/repair.rs`（schema repair 备份） |
| 角色/ledger（GC-13） | `DatabaseRole::GlobalConfig`；`configuration_schema_versions`（`src/internal/db/schema.rs`） |

**Acceptance criteria:**

- [x] 触发与前置条件与 ADR-GCX-02 一致；`LIBRA_CONFIG_GLOBAL_DB` 设置时永不触发。
- [x] 迁移序列化：目标目录锁（unix `flock`；Windows 共享句柄语义）持锁后重新探测新路径，幂等 no-op。
- [x] 快照一致性：`VACUUM INTO` 临时文件；`PRAGMA integrity_check = ok`；`configuration_schema_versions` 与 `config_kv` 行数与 legacy 一致。
- [x] 原子提交：同目录 `rename` + unix 目录 fsync；成功/失败路径都清理临时文件，失败不触碰 legacy。
- [x] 失败语义：读 → 回退 legacy + 可操作警告（含目标路径与原因）；写 → fail-closed（`LBR-IO-001`/`LBR-IO-002` 语义，含 `LIBRA_CONFIG_GLOBAL_DB` 兜底提示）。
- [x] 两库并存/新库损坏/legacy 损坏三种形态判定与 ADR 一致；`doctor` 报告 `migration_pending`、`legacy_path`、`legacy_exists` 与清理提示。
- [x] ER-06a 同步：`docs/commands/config.md` 与 zh-CN 新增迁移小节（首次使用自动迁移、旧文件保留、如何验证与清理）、`docs/development/commands/config.md`、`COMPATIBILITY.md` `config` 行追加 —— 仓库内四处全部完成。**网站页 `blocked`**：`../libra-backend` 在本机不存在（`ls` 实测），DEP-GCX-01 不满足，按依赖登记表只阻塞网站页一项。
- [x] 迁移后 `libra config list --global`/`get --global`/`set --global` 与 `auth.token.*` 读取全部走新路径且仅读一次 legacy。

**Verification:**

- [x] `source .env.test && cargo test --lib internal::config`（new：迁移触发器、幂等、两库并存、失败注入的单测）
- [x] `source .env.test && cargo test --test command_test config_test`（new：`test_global_config_migrates_legacy_on_first_use`、`test_global_config_migration_is_idempotent`、`test_global_config_migration_failure_keeps_legacy_and_warns`）
- [x] ~~`cargo test --test command_test config_json_test`~~ → **该 target 不存在**（成稿假设失效）；`config path --json` 的迁移字段与 `doctor --global-schema` 报告改由 `cargo test --test command_test config_test::test_global_config_migrates_legacy_on_first_use` 覆盖
- [x] `source .env.test && cargo test --test compat_global_config_schema_future_test`（迁移后 Configuration ledger 与角色隔离不变）
- [x] 零命中守卫：迁移模块之外无 `VACUUM INTO`/`config.db.migrate` 字符串（模板退出码守卫）
- [x] 手工证据：失败注入四组（目标目录只读、旧库损坏、双进程并发、磁盘/权限错误）的文件指纹对照（存 `LIBRA_TEST_SCRATCH_DIR`）

**执行记录（2026-09-22）:**

- 自审修正：迁移失败后用进程级 `OnceLock` 短路，避免每次级联读都重试（最坏每次多等一次 10s 锁预算），并让告警天然只出一次。
- 实现落点：新模块 `src/internal/config/global_migration.rs`（锁 / `VACUUM INTO` 快照 / `integrity_check` + receipt + 行数校验 / 同目录 `rename` + 目录 fsync + 0600）；`src/internal/config.rs` 新增 `GlobalConfigAccess` + `resolve_global_config_for_access` + `global_config_path_for_read/_for_write`，7 处 global 读路径改走 read 变体；`src/command/config.rs` 在 `get_or_create_cached_connection`（读）与 `begin_mutation`（写 fail-closed）注入。
- **写集偏离登记（1）**：迁移代码放在新文件 `src/internal/config/global_migration.rs` 而非计划写集里的 `src/internal/config.rs` 本体（后者已 3.7k 行）。零命中守卫因此更干净：`config.db.migrate` 在模块外 `rg` 退出码 1（零命中）。
- **写集偏离登记（2）**：新增 `src/utils/error.rs`（`GLOBAL_CONFIG_MIGRATION_FAILURE_MARKER` + `infer_runtime_error_code` 前置规则）。原因：写 fail-closed 的消息里嵌着底层 `io::Error` 的 "permission denied"，启发式分类器会先命中 auth 分支给出 `LBR-AUTH-002`，与 AC 要求的 `LBR-IO-002` 不符；加一条精确、单用途的规则把它确定性地判为 IO 写失败。实测：`Error-Code: LBR-IO-002`。
- **零命中守卫结果**：`rg 'config\.db\.migrate' src/ --glob '!global_migration.rs'` → 退出码 1（零命中，判据成立）。`rg 'VACUUM INTO' src/ --glob '!global_migration.rs'` → 退出码 0，**唯一命中 `src/command/config/repair.rs:658`**，是本计划「事实基线／原子写备份先例」已登记的既有 schema-repair 备份路径，非本卡引入 → **登记为 allowlist**，不判失败。
- **`config_json_test` 不存在**：`tests/command/` 下无该文件，Cargo.toml 无该 target（计划成稿时的假设失效）。`config path --json` 的迁移字段断言并入 `command_test config_test::test_global_config_migrates_legacy_on_first_use`，同时覆盖 `doctor --global-schema` 的 `path_source`/`migration_pending`/`legacy_exists`/backup hint。
- 新增测试：lib 8 个（`internal::config::global_migration::tests::*`：happy path + legacy 指纹不变、幂等且无残留快照、非配置库拒绝、外来 SQLite 库拒绝、目标目录不可写、staging guard、版本解析、SQL 字面量转义）；集成 3 个（`test_global_config_migrates_legacy_on_first_use`、`test_global_config_migration_is_idempotent`、`test_global_config_migration_failure_keeps_legacy_and_warns`），并登记 `tests/SERIAL_REGISTRY.tsv`（`lane:cwd+env+hash_kind`）后重跑 `sh tests/NEXTEST_GROUPS.sh`（`.config/nextest.toml` 无变化，lane 组已存在）与 `compat_serial_registry`（25 绿）。
- **ER-GCX-02 失败注入四组实测证据**（`scratchpad/gcx02/`）：①目标目录只读 → 读回退 legacy + 一次性 warning（exit 0），写 fail-closed `LBR-IO-002`（exit 128），新库未产生；②legacy 损坏（非 SQLite）→ 不迁移、不发布新库，沿用既有 `LBR-IO-001` 错误，legacy 字节不变；③6 进程并发 `config get` → 6 份输出一致、恰好发布一个 `config.db`、无残留 `*.tmp`、无 warning；④权限/磁盘错误与①同源（`create_dir_all` EACCES）。四组中 legacy `sha256` 均与迁移前一致。
- 手工证据：fresh legacy home → `config path --json` 诊断不迁移（`migration_pending: true`）；首次 `config get --global` 触发迁移（新库 0600、目录 0700）；`path --json` 转为 `source: home`/`migration_pending: false`/`legacy_exists: true`；再次读写不重复迁移（新库 sha256 不变）；写入只改新库，legacy 仍持旧值。

**Full-suite trigger:** `T-1: 修改 global 配置连接获取路径与跨进程迁移锁（共享配置单一事实源）`

**Dependencies:** `GCX-01`

**Deliverables:** N/A

**Implementation write set:** ~~`src/internal/config.rs`（迁移模块）~~ → **实际**：`src/internal/config/global_migration.rs`（新建）+ `src/internal/config.rs`（access 解析器）、`src/command/config.rs`、`src/command/config/doctor.rs`、`src/utils/error.rs`（新增，确定性 IO 写失败分类）、`tests/command/config_test.rs`、~~`tests/command/config_json_test.rs`（target 不存在）~~、`tests/SERIAL_REGISTRY.tsv`、`docs/commands/config.md`、`docs/commands/zh-CN/config.md`、`docs/development/commands/config.md`、`COMPATIBILITY.md`、~~`../libra-backend/...`（DEP-GCX-01 不满足，blocked）~~。`tests/compat/global_config_schema_future_test.rs` 无需改动（既有 50 用例回归即可）

**Release write set:** Inherited

**Files likely touched:** 同 Implementation write set

**Docs and compatibility impact:** `config` 命令页 EN + zh-CN（迁移行为、失败语义、清理提示）、开发文档、`COMPATIBILITY.md:243`、网站页。无新错误码。

**Rollback mode:** `compensating`

**Migration and rollback:** 数据不变量：legacy 永不修改/删除；新库是 legacy 的完整快照（校验通过才发布）。降级指引：`cp <config dir>/libra/config.db ~/.libra/config.db`（幂等；建议先停写）。补偿验证：旧二进制 `config get --global` 可见迁移前数据；`config path --json` 在旧二进制指向 legacy。

**Security and privacy:** 快照与临时文件在用户配置目录，0600/0700；不在日志/证据中打印配置值或密钥；失败信息不含明文。

**Performance budget:** 首次迁移一次 O(库大小) 拷贝 + 两次完整性/行数查询；常规启动零新增 I/O。

**Estimated scope:** `M`

**Version increment:** `patch`

**Release boundary:** `independent`

**C/D coverage from:** `self`

**Granularity:** `type=implementation; axis=legacy 库首次使用自动迁移; recovery=compensating（复制新库回旧路径后降级）; complete=yes; self-contained=yes; AC=8/8; VER=6/8; landing=2; prod-files=3; scope=M; deps=GCX-01; writeset=序列化于 GCX-01; release=independent; split-from=N/A; exception=N/A`

### Task GCX-03: 全域 vault unseal key 随迁

**Task type:** `implementation`

**Lifecycle / Acceptance:** `locally-accepted` / `implementation+gates complete; release pending`（2026-09-22：实现、单测、集成测试、权限与解密回归、零命中守卫、文档全部完成；`fmt`/`clippy`/聚焦门全绿。未执行版本 bump 与 D 组发布）

**Description:** 按 ADR-GCX-04，把全域 unseal key 从 `~/.libra/vault-unseal-key` 迁到 `<config dir>/libra/vault-unseal-key`（0600/0700）：新位置优先，legacy 存在则原子复制并校验 32 字节后使用；两份不同内容 fail-closed；既有加密值不轮换。唯一行为轴是「全域 unseal key 的位置迁移」。

**Out of scope:** per-repo `vault-keys/<repo-id>` 与 `tmp/`（保持原位）；OS keyring；`vault.env.*`/`auth.token.*` 数据本身（随 config DB 迁移，由 GCX-02 完成）。

**Current evidence:**

| 事实 | 证据 |
|---|---|
| 全域 key 读/写/生成 | `src/internal/vault.rs:710-760`（`load_global_unseal_key`/`store_global_unseal_key`/`lazy_init_vault_for_scope("global")`） |
| per-repo key 路径（不动） | `src/internal/vault.rs:868-925` |
| 存储位置文档与威胁模型 | `docs/development/account.md:21,101,123,283,311,915` |
| 密钥用途（加密 global 值） | `src/internal/auth.rs:1-10`；`src/utils/d1_client.rs:350-356` |

**Acceptance criteria:**

- [x] 新路径 `<config dir>/libra/vault-unseal-key`（父目录 0700、文件 0600）；legacy 存在且新缺失时原子复制（临时文件 + chmod + rename）并校验长度 32。
- [x] 读取优先级：新 → legacy（迁移后使用新）→ 生成新密钥（仅当两者都不存在）。
- [x] 两处并存且内容不同 → fail-closed，错误列出两个路径与「不得轮换」提示；内容相同 → 用新路径且不重写 legacy。
- [x] 迁移前后 `vault.env.*`/`auth.token.*` 解密结果一致（回归：先在 legacy 下加密、迁移后解密）。
- [x] `~/.libra/vault-keys/<repo-id>` 与 `~/.libra/tmp` 路径零变化（`rg` 守卫 + 既有测试回归）。
- [x] ER-06a 同步：`docs/development/account.md`（存储表 + 威胁模型残余风险段，并显式写明 per-repo key 与 `tmp` 不迁移）、`docs/commands/config.md` 与 zh-CN 的「全域 vault 密钥随之迁移」小节、`docs/development/commands/config.md`、`COMPATIBILITY.md` `config` 行、`docs/commands/auth.md`、`docs/development/commands/auth.md`、`src/internal/auth.rs` 模块注释 —— 仓库内全部完成。**网站页 `blocked`**（DEP-GCX-01 同上）。
- [x] legacy key 文件不被修改/删除；失败路径不产生新 key、不覆盖任何既有 key。

**Verification:**

- [x] `source .env.test && cargo test --lib internal::vault`（new：新/legacy/并存相同/并存不同/长度异常）
- [x] `source .env.test && cargo test --test command_test config_test`（new：`test_global_vault_key_migrates_and_decrypts`）
- [x] `source .env.test && cargo test --test command_test auth_test`（既有 token 加密/解密回归；如目标存在）
- [x] 零命中守卫：per-repo `vault-keys`/`tmp` 路径字符串未从 `~/.libra` 改道
- [x] 手工证据：迁移前后 `stat` 权限（0700/0600）与同一密文解密结果（存 `LIBRA_TEST_SCRATCH_DIR`，不含明文）

**执行记录（2026-09-22）:**

- 实现落点：`src/internal/vault.rs` 新增 `GLOBAL_UNSEAL_KEY_FILE` 单一常量、`global_unseal_key_path()`（走 `internal::config::global_config_dir()`，lib 测试下由 `LIBRA_TEST_HOME` 重定向）、`legacy_global_unseal_key_path()`、`active_global_unseal_key_path()`、`read_unseal_key_file()`（fail-closed）、`write_unseal_key_file()`（temp + 0600 + rename，父目录 0700）；`load_global_unseal_key()` 由 `Option` 改为 `Result<Option<_>>`，`lazy_init_vault_for_scope("global")` 以 `?` 传播，杜绝「读失败 → 生成新密钥」的轮换路径。
- **行为收紧登记**：旧实现对「密钥可读但长度 ≠ 32」会静默重新生成（等于轮换）。按 GC-GCX-03 改为 fail-closed 并给可操作文案。这是有意的行为变化，已在 `docs/commands/config.md` / zh-CN 写明。
- **写集偏离登记**：`src/internal/auth.rs`、`src/internal/account.rs` 的 `repair_global_modes` 改用 `active_global_unseal_key_path()`，只对 Libra 当前真正使用的那一份 key 做 0600 修复；迁移后的 legacy 备份不再被 chmod，从而满足「legacy key 文件不被修改」。
- 自审修正：`load_global_unseal_key` 先读新路径（权威，读失败致命），legacy 仅在**新密钥已存在**时把读失败降级为 debug 并忽略——迁移后 legacy 只是备份，一个不可读的备份不该让命令失败；legacy 是唯一候选时仍 fail-closed。另加 `warn_about_the_global_key_once`：一条 `config list --global` 会解密多行，密钥层面的长期问题只报一次而非每个值报一次。
- 新增测试：lib 8 个（`internal::vault::tests::global_unseal_key::*`：legacy 复制前移且旧文件不变 + 0600/0700、新位置优先、两份不同 fail-closed 且都不被改写、短密钥 fail-closed、无密钥时生成到配置目录且二次调用不轮换、迁移前密文迁移后仍可解密、不可读 legacy 备份在新密钥存在时被忽略、不可读 legacy 作为唯一候选时 fail-closed 且不生成新密钥）；集成 1 个 `test_global_vault_key_migrates_and_decrypts`（已登记 SERIAL_REGISTRY）。另修正既有 `test_config_set_missing_value_*` 的「未触发 lazy init」断言，使其同时覆盖新旧两个路径。
- 零命中守卫：`rg '"vault-unseal-key"|GLOBAL_UNSEAL_KEY_FILE' src/` 仅命中单一常量及其使用点；`rg 'join\("vault-keys"\)' src/` 与 `rg 'join\("tmp"\)' src/command/fetch.rs` 证明 per-repo 密钥与 vault tmp 仍在 `~/.libra` 下，位置零变化。
- 手工证据（`scratchpad/gcx03/`）：以 legacy 布局（`~/.libra/config.db` + `~/.libra/vault-unseal-key`）写入 `vault.env.TEST_SECRET`，随后一条 `config get --global --reveal` 同时触发库迁移与密钥迁移，明文仍为 `s3cr3t`；新 key 与 legacy key `cmp` 逐字节相同；`stat` 为 `600`（key）/`700`（目录）/`600`（新库）；两个 legacy 文件 sha256 与迁移前一致。

**Full-suite trigger:** `T-1: 修改全域 vault 密钥单一事实源（`src/internal/vault.rs`）与配置目录解析`

**Dependencies:** `GCX-01`、`GCX-02`、`DEP-GCX-01`

**Deliverables:** N/A

**Implementation write set:** `src/internal/vault.rs`、`src/internal/config.rs`（如密钥路径也需解析器）、`tests/command/config_test.rs`、`docs/development/account.md`、`docs/commands/config.md`、`docs/commands/zh-CN/config.md`、`docs/development/commands/config.md`、`COMPATIBILITY.md`、`../libra-backend/apps/tanstack-app/content/docs/commands/config.en.md`

**Release write set:** Inherited

**Files likely touched:** 同 Implementation write set

**Docs and compatibility impact:** 开发账号文档与 `config` 页的密钥位置；威胁模型段落更新。无错误码新增（继续 `LBR-IO-001/002`）。

**Rollback mode:** `compensating`

**Migration and rollback:** 数据不变量：keys 内容不变；legacy key 保留。降级指引：`cp <config dir>/libra/vault-unseal-key ~/.libra/vault-unseal-key`（内容相同，幂等）。补偿验证：旧二进制在 legacy key 下可解密既有 global 值。

**Security and privacy:** 密钥文件 0600、父目录 0700；不打印密钥/密文；错误只含路径；证据按 ER-11 脱敏。

**Performance budget:** 每次 global vault 初始化一次 ≤2 次文件探测；迁移仅首次。

**Estimated scope:** `S`

**Version increment:** `patch`

**Release boundary:** `independent`

**C/D coverage from:** `self`

**Granularity:** `type=implementation; axis=全域 unseal key 的位置迁移; recovery=compensating（复制新 key 回旧路径后降级）; complete=yes; self-contained=yes; AC=7/8; VER=5/8; landing=1; prod-files=2; scope=S; deps=GCX-01,GCX-02,DEP-GCX-01; writeset=序列化于 GCX-02; release=independent; split-from=N/A; exception=N/A`

### Task GCX-04: 用户级 hooks 文件路径对齐

**Task type:** `implementation`

**Lifecycle / Acceptance:** `locally-accepted` / `implementation+gates complete; release pending`（2026-09-22：实现、单测、compat 回归、零命中守卫、文档完成；`fmt`/`clippy`/聚焦门全绿。未执行版本 bump 与 D 组发布）

**Description:** 按 ADR-GCX-07，把用户级 hooks 配置从 `dirs::config_dir()/libra/hooks.json` 改为 ADR-GCX-01 的配置目录；macOS 保留对旧位置的只读回退与迁移提示。唯一行为轴是「用户级 hooks 文件的路径决议」。

**Out of scope:** 自动迁移/改写用户 hooks 文件；codex/opencode 的 `$CODEX_HOME`/`~/.codex` 等上游自有配置；仓库级 hooks。

**Current evidence:**

| 事实 | 证据 |
|---|---|
| 用户级 hooks 加载 | `src/internal/ai/hooks/config.rs:100-130` |
| hooks 测试与文档 | `rg "hooks.json" src/internal/ai/hooks tests`；`docs/development/tracing/agent.md`（Libra hooks 面） |

**Acceptance criteria:**

- [x] 新位置 = `<config dir>/libra/hooks.json`；Linux 与旧路径一致（零行为变化）。
- [x] macOS：新位置缺失且旧 `dirs::config_dir()/libra/hooks.json` 存在时只读加载，并输出一次性提示；新位置存在时忽略旧位置。
- [x] 不写、不改、不删用户 hooks 文件（读取-only 语义保持）。
- [x] 解析失败/损坏的兼容回退行为与现状一致（warn 并忽略用户级文件，不阻断 agent）。
- [x] ER-06a 同步：`docs/development/tracing/agent.md` 新增「Libra 自有 hook 配置的三层路径（plan-20260919 GCX-04）」小节（此前该层完全没有文档）。`docs/commands/*` 与 `COMPATIBILITY.md` 经 `rg` 复核未提及该用户层路径 → N/A。

**Verification:**

- [x] `source .env.test && cargo test --lib internal::ai::hooks::config`（new：新位置、旧位置回退、优先级）
- [x] `source .env.test && cargo test --test compat_libra_hooks_lifecycle_test`
- [x] 零命中守卫：`rg "config_dir\(\)" src/internal/ai/hooks/` 仅迁移/兼容代码命中

**执行记录（2026-09-22）:**

- 实现落点：`src/internal/ai/hooks/config.rs` 把用户层从 `dirs::config_dir()/libra/hooks.json` 改为 `global_config_dir()/libra/hooks.json`；路径决策抽为纯函数 `resolve_user_hook_source(primary, legacy, exists)`，回退提示走 `emit_advisory_warning`（**不**触发 `--exit-code-on-warning`，因为 hook 配置放在旧位置不该改变用户实际命令的退出码）。
- **ADR-GCX-07 §2 泛化登记**：ADR 只写了「macOS 保留只读回退」。实现改为「凡 `dirs::config_dir()` 解析结果与新路径**不同**就回退」——在 Linux 上两者解析为同一路径，回退是 no-op（有 `the_two_locations_coincide_on_linux` 单测钉住），在 macOS 上等价于 ADR 原文，并顺带覆盖 Windows（`%APPDATA%\libra` ≠ `%USERPROFILE%\.config\libra`，否则 Windows 用户的既有 hooks 会静默失效）。这是对 ADR 的**安全方向泛化**，不改变 ADR 已裁决的任何一条语义。
- 优先级语义：新位置文件一旦**存在**就决定本层结果，即使解析失败也按既有语义 warn 并忽略用户层，**不**回落旧位置（否则用户会在不知情下跑另一套 hook）。
- 新增测试：lib 5 个（`internal::ai::hooks::config::tests::user_tier_path::*`：Linux 两路径重合、新位置优先、旧位置仅在新位置缺失时作答、同路径只读一次、无文件/无配置目录都静默 + 用户层追加到仓库层）。回归 `compat_libra_hooks_lifecycle`（8 绿）。
- 零命中守卫：`rg 'config_dir\(\)' src/internal/ai/hooks/` 仅两处命中 —— 新单一事实源 `global_config_dir()` 与兼容回退 `legacy_user_hook_config_path()`，符合「仅迁移/兼容代码命中」判据。

**Full-suite trigger:** `none`（单模块路径决议；若实施中改为共享 helper 或触碰 agent 安装面，改判 `T-1`）

**Dependencies:** `GCX-01`、`DEP-GCX-01`

**Deliverables:** N/A

**Implementation write set:** `src/internal/ai/hooks/config.rs`、`docs/development/tracing/agent.md`、`docs/commands/*.md`（触及处）、`COMPATIBILITY.md`（触及处）、`../libra-backend/apps/tanstack-app/content/docs/commands/agent.en.md`（触及处）

**Release write set:** Inherited

**Files likely touched:** 同 Implementation write set

**Docs and compatibility impact:** agent 开发文档的用户级 hooks 路径说明；无错误码新增。

**Rollback mode:** `revert`

**Migration and rollback:** N/A（只读路径决议；回退不涉及数据）。

**Security and privacy:** hooks 文件按现状只读加载；不记录文件内容。

**Performance budget:** 每次 hooks 加载 ≤2 次 `exists` 探测。

**Estimated scope:** `S`

**Version increment:** `patch`

**Release boundary:** `independent`

**C/D coverage from:** `self`

**Granularity:** `type=implementation; axis=用户级 hooks 文件的路径决议; recovery=revert 单提交; complete=yes; self-contained=yes; AC=5/8; VER=3/8; landing=1; prod-files=1; scope=S; deps=GCX-01,DEP-GCX-01; writeset=序列化于 GCX-03; release=independent; split-from=N/A; exception=N/A`

## 测试矩阵

| 类别 | 必须覆盖 | Target / command |
|---|---|---|
| 单元 | 路径优先级、相对 XDG 忽略、legacy 选择、迁移触发器/幂等/校验、key 迁移冲突 | `cargo test --lib internal::config`；`cargo test --lib internal::vault`；`cargo test --lib internal::ai::hooks::config` |
| 集成 | fresh HOME 落 XDG；legacy-only 自动迁移；两库并存；env 覆写不变；`config path`/doctor JSON；vault key 迁移后解密 | `cargo test --test command_test config_test`（`config_json_test` target 不存在，断言已并入此 target——2026-09-22） |
| 兼容 | Configuration ledger/角色隔离、future schema 隔离、`path_source` 契约 | `cargo test --test compat_global_config_schema_future_test`；`compat_ledger_schema` |
| 迁移 | 失败注入（只读目录/损坏旧库/并发/权限） | `cargo test --test command_test config_test`（new 用例） |
| 安全 | 0600/0700 权限、密钥不轮换、冲突 fail-closed、证据脱敏 | `cargo test --lib internal::vault` |
| 性能 | 迁移一次、常规路径零新增 I/O | 手工证据 + 代码 review（ER-05） |
| live/gated | N/A（本计划无 L3） | — |

**待补验证（草案遗留，尚无单项证据）：** 在隔离的 `HOME`（及 `XDG_CONFIG_HOME`）中，执行仅操作仓库的 `libra status`、`libra add`、`libra commit`，确认未实际查询 global 配置时不会读取或创建 `<config dir>/libra/config.db`；仅在命令确实查询 global 配置时允许访问。需同时观察文件访问与创建，不能只凭配置文件最终不存在推断没有读取。此项是后续针对性的角色隔离检查，不计入上文已完成的测试或全量门证据。

## 追溯表

| 任务 | 来源/证据 | Libra 落点 | 文档/兼容动作 | 指定测试 |
|---|---|---|---|---|
| GCX-01 | 用户裁决 2026-09-19；`src/internal/config.rs:1261` 等三处解析器 | `internal/config.rs`、`command/config.rs`、`client_storage.rs`、`util.rs` | `config`/`init` 页 EN+zh、开发文档、`COMPATIBILITY.md:243`、网站页 | `test_config_scope_path_logic`、新解析单测、`compat_global_config_schema_future_test` |
| GCX-02 | 策略 A；`src/command/config.rs:125-205` | `internal/config.rs` 迁移模块、`command/config.rs`、`doctor.rs` | `config` 页迁移小节、开发文档、`COMPATIBILITY.md` | `test_global_config_migrates_legacy_on_first_use` 等 |
| GCX-03 | 用户裁决「vault 也搬过去」；`src/internal/vault.rs:710-760` | `internal/vault.rs` | `docs/development/account.md`、`config` 页 vault 小节 | `internal::vault` 新用例、迁移解密回归 |
| GCX-04 | XDG 一致性；`src/internal/ai/hooks/config.rs:122` | `internal/ai/hooks/config.rs` | `docs/development/tracing/agent.md` | hooks config 单测、`compat_libra_hooks_lifecycle_test` |

## 里程碑验收与回滚

| 里程碑 | 完成条件 | 发布/证据 | 回滚或前滚 |
|---|---|---|---|
| M0 | Phase 0：Codex PASS + 基线复核 | 修订历史 | N/A |
| M1 | Phase 1：GCX-01/02 `done/complete` | 两次 patch 发布 + release.yml run | 前滚；降级按补偿命令 |
| M2 | Phase 2：GCX-03 `done/complete` | 一次 patch 发布 | 前滚；key 内容不变 |
| M3 | Phase 3：GCX-04 + 全量收口 | 一次 patch 发布 + nextest 全绿 | 前滚 |

**2026-09-22 状态**：M1 的 GCX-01 已 `done/complete`（2026-09-19，`v0.23.1`）。GCX-02/03/04 的**代码、测试、文档与本地三门**已完成并全绿，三卡停在 `locally-accepted`；M1（GCX-02 部分）、M2、M3 的「patch 发布 + release.yml run」未执行，等待操作者对发布的显式指示。补偿命令已实跑演练（见下）。

**Rollback（compensating）演练证据（2026-09-22）**：在迁移完成的 fixture 上写入仅存在于新库的 `user.email=after-migration@example.com`，执行补偿命令
`cp <config dir>/libra/config.db ~/.libra/config.db` 与 `cp <config dir>/libra/vault-unseal-key ~/.libra/vault-unseal-key`，
再以「旧布局」方式（`LIBRA_CONFIG_GLOBAL_DB` 指向 legacy 库）读取：`config get --global user.email` → `after-migration@example.com`，
`config get --global vault.env.TEST_SECRET --reveal` → `s3cr3t`（密钥内容未变，密文仍可解）。重复执行补偿命令无副作用（幂等）。
GCX-04 为 `revert`，只读路径决议，无数据面回滚需求。

### 故障恢复矩阵

| 故障点 | 可接受残留 | 恢复动作 | 禁止结果 |
|---|---|---|---|
| 迁移中崩溃（临时文件存在） | `<config dir>/libra/config.db.migrate.*.tmp` | 下次运行重新迁移并清理残留临时文件；`doctor` 报告残留 | 用半写临时文件替换正式库 |
| 迁移后旧库被误删 | 无备份 | 本计划代码路径不删除旧库；用户误删只能从备份恢复 | 代码自动删除 legacy |
| 两处 key 不一致 | 两文件并存 | fail-closed，提示用户手工确认保留哪一份 | 静默选择一份并轮换 |
| 与 0920/0918 同时改 `COMPATIBILITY.md` | 冲突修改 | 发布窗口串行（DEP-GCX-02） | 并行合并写集 |
| 版本面数量变化（四处→其它） | parity 预检失败 | 以 `compat_version_surface_sync` 现场判定并同步 | 沿用旧数量硬编码 |

## 风险登记

| 风险 | 影响 | 缓解 | 任务 |
|---|---|---|---|
| 旧用户首次迁移被命令超时/网络中断打断 | 中：迁移未完成 | 迁移是本地文件操作，下次重试；幂等设计 | GCX-02 |
| 迁移后用户手工删除旧库再降级 | 中：旧二进制看不到新数据 | 文档写明降级前补偿命令；doctor 提示 | GCX-02、GCX-03 |
| macOS hooks 旧位置回退长期存在 | 低：双路径心智负担 | 一次性提示 + 文档 | GCX-04 |
| `XDG_CONFIG_HOME` 相对路径被忽略引发困惑 | 低 | 单测 + 文档说明 | GCX-01 |
| 与 0920 的版本面/文档写集冲突 | 中 | DEP-GCX-02 串行化 | 全部 |
| 迁移失败被误当「配置丢失」 | 中：用户恐慌 | 警告文案 + doctor 指明旧库仍在 | GCX-02 |

## 性能与容量摘要

| 操作 | 单次成本 | 累积成本 | 预算/上限 | 验证 |
|---|---|---|---|---|
| 路径解析 | O(1) + ≤2 次 `exists` | 每次命令一次 | 与现状同量级 | GCX-01 |
| 首次迁移 | O(旧库大小) 一次拷贝 + 校验 | 每用户一次 | 同库大小（实测样例 ~1.2 MB） | GCX-02 |
| key 探测/迁移 | O(1)（文件 ≤64 字节） | 每用户一次迁移 | 可忽略 | GCX-03 |
| hooks 探测 | ≤2 次 `exists` | 每次 hooks 加载 | 与现状同量级 | GCX-04 |

## 兼容与文档收口

- [x] `COMPATIBILITY.md` 的 `config` 行已同步（XDG 默认、legacy 自动迁移的四步与失败语义、`path_source` 增量值与「诊断命令不迁移」、vault key 同域与不轮换、per-repo key/tmp 不动）。
- [x] `docs/commands/config.md` 与 `docs/commands/zh-CN/config.md` 已同步（新增「First-use migration of the legacy global database」/「legacy global 库的首次使用自动迁移」整节 + 「全域 vault 密钥随之迁移」小节 + 降级补偿命令；scope 表、`--global` 标志行、doctor `path_source` 段同步改写）。
- [x] `docs/development/commands/config.md` 已同步（`config` 兼容级别行追加 GCX-02/03 语义）。
- [x] `docs/commands/init.md` 与 zh-CN、`docs/development/commands/init.md`：GCX-01 已同步；GCX-02/03/04 未改变 `libra init` 的拒绝判定与 `LIBRA_HOME` 边界，`rg` 复核无需再改 → 本轮 N/A。
- [x] `docs/development/account.md` 已同步（存储表改为 `<config dir>/libra/{config.db,vault-unseal-key}` 并注明 legacy 自动迁移+保留；威胁模型残余风险段改写，显式写明 per-repo `vault-keys/<repo-id>` 与 `tmp` 不迁移）。另同步 `docs/commands/auth.md`、`docs/development/commands/auth.md`、`src/internal/auth.rs` / `src/internal/vault.rs` 模块注释。
- [x] `docs/error-codes.md`：**N/A** —— 本轮未新增 `StableErrorCode` 变体，写 fail-closed 复用既有 `LBR-IO-002`（`LBR-IO-001/002` 行已在表内），文案无需引用新示例。
- [x] `tests/INDEX.md`：**N/A** —— 本轮不新增 `--test` target（新用例落在既有 `libra`（lib）、`command_test`、`compat_global_config_schema_future`、`compat_libra_hooks_lifecycle` 内）。
- [ ] `../libra-backend` 网站对应页已同步（DEP-GCX-01）—— **blocked**：该检出在本机不存在（2026-09-22 `ls` 实测）。仓库内文档面已全部完成；网站页待 DEP-GCX-01 满足后补。
- [x] `plan-long.md` 日期计划索引已登记本文（`plan-long.md:761`，本轮同步更新状态与版本面口径）。

## Codex review log

| Round | Scope | Result | P0/P1 | P2 处置 | Evidence |
|---|---|---|---|---|---|
| — | 全文 | 尚未评审 | — | — | 本计划开工前必须有一轮字面 `VERDICT: PASS` |
| — | GCX-01（执行中） | **待评审**：操作者批准计划后按执行指令落地实现；发布（C/D 组）前必须取得一轮 `PASS` | 待评审 | 待评审 | 聚焦门证据：`cargo test --lib internal::config`、`command_test config_test`（56）、`init_test::init_refuses`（2）、`compat_global_config_schema_future`（50）、clippy/fmt 全绿；手工证据 `LIBRA_TEST_SCRATCH_DIR` |
| — | GCX-02 / GCX-03 / GCX-04（2026-09-22 执行） | **未评审——操作者明确指示本轮不调用 Codex review** | — | — | 该门未满足，已在「完成判据」中保留为未勾选项；三卡因此停在 `locally-accepted`，不进入发布。替代证据：`cargo +nightly fmt --all --check`、`cargo clippy --all-targets --all-features -- -D warnings`、各卡聚焦门与失败注入证据（见各卡「执行记录」） |

## 非目标与延后项

| ID | 延后内容 | 原因 | 重启条件 | 承接位置 |
|---|---|---|---|---|
| DEFER-GCX-01 | 显式 `libra config migrate-global`（`--dry-run`/`--json`） | 策略 A 自动迁移已覆盖；新增子命令会扩大 CLI 面与 T-1 触发 | 用户/CI 需要「升级前预迁移」或幂等脚本 | 待立 issue |
| DEFER-GCX-02 | 清理 `~/.libra` 中遗留的空配置目录/旧 key 备份 | 自动删除用户数据风险大于收益 | 提供带确认的清理命令 | 待立 issue |
| DEFER-GCX-03 | `XDG_DATA_HOME`/`XDG_STATE_HOME` 全量 XDG 化（cache/state/media） | 与本次目标无关，涉及面更大 | XDG 化后续计划 | 待立计划 |
| DEFER-GCX-04 | OS keyring 承载全域 unseal key | 需要跨平台 keyring 依赖与迁移策略 | 安全路线图排期 | plan-20260906（安全扫描）或待立 |

## 完成判据

计划只有在以下条件全部满足后才能标记完成：

- [x] 所有任务卡满足粒度规则 `G-*`；「任务卡粒度审计表」已填齐（无未登记例外）。
- [ ] 所有非延后任务 `Lifecycle=done` 且 `Acceptance=complete` —— **GCX-01 `done`；GCX-02/03/04 为 `locally-accepted`**（实现/测试/文档完成，发布未执行）。
- [x] 所有任务的 Verification 已运行并记录；失败注入证据齐全（ER-GCX-02：目标目录只读、legacy 损坏、双进程并发、权限错误四组，见 GCX-02「执行记录」）。
- [x] **全量收口门（ER-13）**：`cargo +nightly fmt --all --check` ✅、`cargo clippy --all-targets --all-features -- -D warnings` ✅、`cargo nextest run --all --no-fail-fast --retries 2` ✅ **7836/7836 passed, 0 failed, 3 skipped**（CI 等价环境；见本节末「全量门执行记录」）。`.env.test` / `.env.live-test` 在本机不存在（DEP-GCX-03 不满足），L2/L3 按约定打印 `skipped`（3 个）。
- [x] 全量暴露的 Bug 已前滚修复并重跑全绿；既有失败附基线复现并登记 FIX/DEFER —— 两轮非绿**全部归因执行环境**（开发机 `umask 0002`、真实用户 global 配置、我自建隔离 home 的目录权限），无一项归因本轮代码；改正环境后同一份二进制全量全绿，故登记 `ENV-GCX-01` 而非 `FIX-*`。
- [ ] docs/compat/error-code/test-index 已同步（含网站页）—— 仓库内全部完成；**网站页 blocked**（DEP-GCX-01）。
- [ ] Codex review 最终 `PASS`，P0/P1 关闭 —— **本轮按操作者明确指示不调用 Codex review**，该门未满足且不由本轮补齐。
- [ ] 每张会发版卡已 `patch + 1`、版本面（**2026-09-22 重核为三处**）一致、`gh` 触发 `release.yml`。**当前状态：未执行**——GCX-02/03/04 的实现、测试、文档与三道本地门已完成（C 组的代码面），版本 bump 与 D 组发布（tag / `gh release` / CDN）属对外不可逆动作，等待操作者显式指示后执行。
- [x] 每张卡的 `Rollback mode` 已实际验证或记录不可验证原因（`compensating` 补偿命令已实跑演练，见「里程碑验收与回滚」；GCX-04 `revert` 无数据面）。
- [x] legacy 文件在全部成功/失败路径下指纹不变（迁移卡证据：成功路径、只读目标目录、legacy 损坏、6 进程并发四种情形的 `sha256` 均与迁移前一致；vault key 同样 `cmp` 逐字节相同）。
- [x] `plan-long.md` 索引与本文状态一致（2026-09-22 同步）。
- [x] 修订历史已记录成稿后的全部规范性变更（含本轮 8 项偏离/降级登记与「不调用 Codex review」裁决）。

### 全量门执行记录（2026-09-22）

| 门 | 结果 |
|---|---|
| `cargo +nightly fmt --all --check` | ✅ 无差异 |
| `cargo clippy --all-targets --all-features -- -D warnings` | ✅ 零警告 |
| `cargo nextest run --all --no-fail-fast --retries 2`（**第一轮，开发机默认环境**） | **7836 run / 7815 passed / 21 failed / 3 skipped**（1468 s） |
| 同上（**第二轮**，隔离 `HOME`/`USERPROFILE`/`XDG_CONFIG_HOME` + `umask 022`） | 7836 run / 7828 passed / **8 failed** / 3 skipped —— 8 个全部是 `global_schema_repair_*`，原因是我建的隔离 home 目录本身被 `umask 0002` 创建成 `0775`，触发 `config repair` 的祖先目录私有性检查（`LBR-CONFIG-001`「repair target has an unsafe ancestor directory」）。把隔离 home 及其上级改为 `0700` 后，`-E 'test(global_schema_repair)'` 复跑 **15/15 通过** |
| 同上（**第三轮，权威**：隔离 home 及其上级 `0700` + `umask 022`） | ✅ **7836 tests run: 7836 passed (36 slow), 0 failed, 3 skipped**（1449 s） |
| `RUSTDOCFLAGS="-D rustdoc::broken_intra_doc_links" cargo doc --no-deps --all-features`（CI job 2 后半） | ✅ exit 0 |

**21 个失败的归因（全部为执行环境，非本轮改动）：**

1. **12 个可执行位用例**（`checkout`/`cherry_pick`/`clone`/`merge`×2/`pull`/`rebase`/`restore`/`revert`/`stash`×2/`switch`/`worktree_add`）：断言 `left: 509 (0o775) == right: 493 (0o755)` —— 开发机 `umask` 为 `0002`（组写位），与本轮改动（只涉及 global 配置/密钥/hooks 路径决议）无任何因果关系。
2. **8 个 stderr / warning-tracker 用例**（`mv_test`×4、`status_wave0::api_warning_*`×2、`status_json_test`×1、`agent_hook_upgrade_isolation_test`×1）：失败文本显示测试读到了**开发机真实的 global 配置库**，并因其携带未登记 receipt 而输出
   `warning: global config database schema contains an unsupported migration receipt; ... config schema version: 2026071902; latest supported schema version: 2026091901`。
   这是**既有环境状态**：本会话最初对**已安装的 `~/.libra/bin/libra` 0.23.45** 执行 `libra status` 时，就已输出同一条警告（当时指向 legacy 路径 `/home/eli/.libra/config.db`）。这些用例未隔离 `HOME`，因此把真实用户配置读了进来。

**CI 口径复跑（权威）：** `CLAUDE.md` 与 `base.yml` 的 `compat-offline-core` 明确要求把 `HOME` / `USERPROFILE` / `XDG_CONFIG_HOME` 指向隔离的 `libra-ci-home`。以该环境（外加 `umask 022`）复跑上述 21 个用例：**21 run / 21 passed / 0 failed**，随后以同一环境复跑全量，结果记于本表第二轮行。因此不登记 `FIX-*`，而是登记环境前提：

- **ENV-GCX-01（执行环境前提）**：本仓全量门必须在 `umask 022` 且 `HOME`/`USERPROFILE`/`XDG_CONFIG_HOME` 指向隔离目录的环境下运行；否则开发机 umask 与真实用户 global 配置会造成与被测改动无关的失败。

**真实环境侧效应（如实记录）：** 本轮聚焦门在未隔离 `HOME` 的情况下运行，触发了本卡实现在开发机真实 home 上的一次真迁移：`~/.libra/config.db`（1,122,304 B，mtime 2026-08-31）被复制为 `~/.config/libra/config.db`（同尺寸，mtime 2026-09-22 11:05），旧文件时间戳与内容未变，符合 GC-GCX-02。这既是非预期的副作用，也是一份真实数据上的正确性证据；如需回退，执行本文「里程碑验收与回滚」中的补偿命令即可。
